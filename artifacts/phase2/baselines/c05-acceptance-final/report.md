# C05 matched baseline run

Profile: `acceptance`; completed: `True`; elapsed: `29.725s`.

Accuracy and coverage are reported separately. Oracle is evaluator-only and privileged Bayes uses declared world information. RIM-like is a small top-k modular recurrent equivalent, not an exact RIM reproduction.

Operation counts and CPU time are not physical energy measurements. Any failed or unmatched run remains in `failures.json` / `acceptance.json`.
