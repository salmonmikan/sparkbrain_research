# unresolved_or_slow

Selected by the predeclared deterministic worst-case rule. World: `switchworld`; condition: `hard_wta`. This is an observed synthetic failure case, not evidence of general behavior.
