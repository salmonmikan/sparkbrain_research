# cross_talk_or_goal_flip

Selected by the predeclared deterministic worst-case rule. World: `switchworld`; condition: `full`. This is an observed synthetic failure case, not evidence of general behavior.
