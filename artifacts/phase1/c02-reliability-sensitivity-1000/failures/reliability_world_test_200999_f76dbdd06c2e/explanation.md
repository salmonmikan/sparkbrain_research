# unresolved_or_slow

Selected by the predeclared deterministic worst-case rule. World: `reliability_world`; condition: `full`. This is an observed synthetic failure case, not evidence of general behavior.
