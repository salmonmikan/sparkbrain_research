# SparkBrain v0.5 source binding

The checked-in v0.5 reference artifacts were generated from source commit `b9668b89d549a9d7379f086661a02c47607c5f52` on GitHub Actions using Python 3.11 and seeds `5501, 5502, 5503, 5504`.

Command:

```bash
python scripts/run_v05_experiments.py --seeds 5501,5502,5503,5504
```

The artifact commit is a documentation/result child of that source commit; it does not alter the evaluated runtime before generation.

