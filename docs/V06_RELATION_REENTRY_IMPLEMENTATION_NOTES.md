# V06-12A Relation Re-entry Implementation Notes

The reference mechanism derives one transient anonymous influence from the existing
`UntypedBoundaryConsistency` state. It owns no learned relation table.

The canonical engineering comparison holds the later Field and shared trigger fixed and varies only
the acquired, reversed, returned, reset, transplanted, or suppressed consistency component.

Required interpretation boundaries:

- a proposal is not counted as a Spark;
- only an ordinary reinjected arrival crossing the retained Field threshold counts;
- consistency state must remain byte-equivalent before and after re-entry;
- external-observation and positive-update counts must not increase;
- reset must remove the effect;
- transplant must move the effect to a naive compatible runtime;
- no-reentry must preserve the shared trigger while removing the later Field/boundary effect;
- no reward, correct action, Assembly, semantic label, or typed functional relation is permitted.
