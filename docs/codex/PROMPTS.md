# Copy-Ready Codex Prompts

Use one prompt per Codex thread/worktree. Replace no requirements with informal summaries; the task files are the contracts.

## C01

```text
Use $sparkbrain-research. Execute C01 from docs/codex/C01_ENGINE_HARDENING.md. Start by auditing the existing checkpoint, replay, validation, schemas, 26 tests, and CI scaffold rather than rewriting them. Complete every remaining acceptance criterion, run the full validation sequence, update PROJECT_STATUS/RESULTS_LEDGER/DECISION_LOG, and return the required completion report. Do not start C02 or refactor unrelated theory.
```

## C02

```text
Use $sparkbrain-research. Confirm C01 is accepted, then execute C02 from docs/codex/C02_CONTROLLED_WORLDS.md. Freeze development/test manifests before final evaluation, run the complete ablation matrix with episode-level uncertainty, preserve raw per-episode outputs, and document negative results. Do not add learned models.
```

## C03

```text
Use $sparkbrain-research. Confirm the C01 trace/control contracts are frozen, then execute C03 from docs/codex/C03_INTERACTIVE_BRAIN_LAB.md. Build the live causal Brain Lab against public APIs only, preserve the static visualizer, add forked interventions and synchronized comparison, test blind mode and export/import, and update all architecture/status docs.
```

## C04

```text
Use $sparkbrain-research. Execute C04 from docs/codex/C04_LEARNED_ROUTING.md using the frozen C01 protocol and C02 splits. Implement a learned encoder/router/active-subgraph backend, distinguish masked sparsity from actual executed work, test held-out combinations, preserve no-ignition and trace provenance, run all required ablations, and report collapse or negative results honestly.
```

## C05

```text
Use $sparkbrain-research. Execute C05 from docs/codex/C05_MATCHED_BASELINES.md. Coordinate schemas and splits with C04 but do not edit C04 model internals. Implement the required probabilistic, recurrent, Transformer, and modular baselines under parameter-, training-compute-, and quality-matched regimes. Preserve failed seeds and equal tuning budgets. Do not claim a winner from one favorable metric.
```

## C06

```text
Use $sparkbrain-research. Confirm minimum viable C04 and C05 results, then execute C06 from docs/codex/C06_EXTERNAL_VALIDATION.md. Use official/licensed dataset sources, keep restricted data out of git, freeze adapters on dev data, evaluate update-needed and no-update subsets separately, run encoder/dynamics ablations and causal evidence interventions, and update claim grades only to the supported level.
```

## C07

```text
Use $sparkbrain-research. Execute C07 from docs/codex/C07_SPIKING_BACKEND.md. Select Norse or snnTorch from current official compatibility information, predefine behavioral tolerances, implement the shared backend/trace protocol, compare rate and spike behavior, preserve raw spikes, and explicitly separate activity counts, runtime, and hardware energy. Make no efficiency claim without direct methodology.
```

## C08

```text
Use $sparkbrain-research. Execute C08 from docs/codex/C08_STRUCTURAL_PLASTICITY.md on top of the accepted learned backend. Add budgeted serialized structural events and test candidate specialization using held-out reuse plus targeted, random, and degree-matched causal ablations. Treat failure to form organs as a valid result. Never label a graph cluster an organ without the required evidence.
```

## C09

```text
Use $sparkbrain-research. Execute C09 from docs/codex/C09_PRIOR_ART_AUDIT.md in a separate documentation worktree. Search primary literature and official implementations across every required family, retain dated queries and exact overlap/non-overlap, challenge each novelty claim with the strongest counterexample, and reframe any invalidated contribution. Do not infer nonexistence from search failure.
```

## C10

```text
Use $sparkbrain-research. Execute C10 from docs/codex/C10_REPRODUCIBILITY_RELEASE.md only after primary experiments and the C09 claim audit are accepted. Build a clean reproducibility release, regenerate all primary tables/figures from raw manifests, perform the claim audit line by line, include negative results and third-party licenses, and prove the clean-room reproduction path before reporting completion.
```
