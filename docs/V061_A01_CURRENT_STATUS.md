# v0.6.1 A01 — Current Status

## Authority and scope

This document records the current development-state interpretation of A01. Immutable execution evidence and consumed identities remain authoritative over this moving status document.

A01 is still a development research line. No development result below is a formal held-out result unless explicitly marked otherwise.

## Current scientific state

A01 has progressed through P2, P3, and P4 under distinct consumed identities, and Family-B Generation-1 has now been closed at admission before STARTED.

```text
P2: positive development evidence, consumed
P3: positive development evidence, consumed
P4: terminal negative development evidence, consumed
P5: not admissible for the P4-failed mechanism
Family-B Gen1: REJECT_BEFORE_STARTED_STATIC_REDUCTION, unconsumed
```

The strongest current interpretation is mixed:

- P2/P3 showed that anonymous causal/provenance state can affect later local competition.
- Those effects remain reducible to explicit local eligibility/provenance/return-address memory families.
- P4 tested the stronger claim that merged plural ancestry could later be selectively resolved by external evidence.
- P4 failed that selective-resolution test: the tested bridge changed merged causal paths en bloc rather than selectively crediting only the historically supported lineage.
- Family-B Generation-1 was rejected before STARTED because its exact candidate dynamics are statically reproduced by its own equal-resource, equal-privilege recurrent causal-trace null, and the frozen scorer therefore cannot produce a non-reduced PASS for a gate-passing candidate.

Therefore the currently tested transient-return-address P4 mechanism is terminal at P4, and the exact Family-B Generation-1 object is terminal at admission without consuming its one-way identity. These results do **not** by themselves formally falsify every possible SparkBrain causal-circulation mechanism.

## Consumed A01 identities

Do not rerun, retune, repair in place, or silently reuse these identities:

```text
A01 MD-001

a01-md002-p2-candidate-002-ef73823f4c667aee2655d0e2

a01-md002-p3-r-only-causal-carrier-candidate-001-v1

a01-md002-p4-merged-lineage-selective-resolution-candidate-001-v1
```

The following Family-B identity remains **unconsumed**, but its exact Generation-1 scientific object is rejected before STARTED and must not be executed or rescued under the same identity:

```text
a01-family-b-distributed-field-trace-gen1-v1
```

## P4 terminal result

P4 identity:

```text
a01-md002-p4-merged-lineage-selective-resolution-candidate-001-v1
```

Exact P4 source head:

```text
1bd0099f4358e02efac7ee4acccfe5257a86c4be
```

Authority chain:

```text
freeze/a01-md002-p4-candidate-001-source-20260916
  -> 1bd0099f4358e02efac7ee4acccfe5257a86c4be

control/a01-md002-p4-candidate-001-started-20260916
  -> 1bd0099f4358e02efac7ee4acccfe5257a86c4be

preserve/a01-md002-p4-candidate-001-raw-20260916
  -> 2511454f1633d3bc6f10e3d2a99e3ddd823bb798

preserve/a01-md002-p4-candidate-001-scored-20260916
  -> 56ee762540e0519034d2e8db0ad3c6acda667ffd
```

Frozen classifier verdict:

```text
UNSUPPORTED_EN_BLOC_MERGED_CREDIT
```

Interpretation:

- plural merged ancestry was retained;
- the separating assays did not isolate credit to only the externally supported historical lineage;
- both causal paths changed together;
- the tested P4 bridge therefore did not establish selective historical-lineage resolution.

The P4 identity is consumed. The negative result must not be converted into a same-identity rescue, post-outcome threshold change, scorer change, or mechanism retune.

## P5 status

P5 is a reduction test only for a mechanism that survives P1–P4. The current P4 mechanism did not survive P4.

```text
P5 on current P4 mechanism: NOT_ADMISSIBLE
```

P5 must not be used as a rescue path for this failed P4 mechanism.

## Family-B Generation-1 pre-START closeout

The pre-P4 premechanism registry contained the distinct conceptual family:

```text
distributed-field-trace
```

After Family-A became terminal, MAIN prospectively constructed a post-P4 Generation-1 package under:

```text
research/v061-a01-family-b-gen1-execution-package-20260916
```

Prospective identity:

```text
a01-family-b-distributed-field-trace-gen1-v1
```

Prospective contract:

```text
docs/V061_A01_FAMILY_B_DISTRIBUTED_FIELD_TRACE_GEN1.md
```

Exact object inspected for the admission decision:

```text
source head: 1c203666882f43d70c62203c6a4bbdd845065e9f
runner blob: fd0b9cbfb696456dab4c3cb859310f26625df9cc
mechanism blob: f597d936a853e296f5e8d40dc054c875357e5a81
```

Admission classification:

```text
REJECT_BEFORE_STARTED_STATIC_REDUCTION
```

The candidate and N3 resource-matched recurrent causal-trace null implement the same anonymous fixed-width recurrent update arithmetic, emit the same complete scorer signature, and use the same `_resource_profile(width, privilege=0)`. The frozen scorer returns `FAIL` for failed candidate gates and otherwise returns `REDUCED_EXPLANATION` when a registered null reproduces the candidate signature with no greater resources. Consequently, a scientifically meaningful non-reduced PASS is unavailable for this exact object.

This is a static admission decision, not a scientific measurement. No Family-B STARTED/control claim, acquisition, raw artifact, score, freeze/evidence authority, or identity consumption occurred. The exact identity remains unused but is not admissible for execution or same-identity rescue.

Canonical decision record:

```text
docs/V061_A01_FAMILY_B_GEN1_PRESTART_REDUCTION_DECISION.md
```

The inspected package also had a pre-START locked-file/manifest CI drift. That defect is not a reason to repair the rejected package for execution because the stronger static reduction already closes the exact scientific object.

## Prospectivity boundary

The governing documents remain:

```text
docs/V061_PREMECHANISM_ADMISSION_AND_NEGATIVE_COMPLETION.md
docs/V061_P3_P5_CAUSAL_CREDIT_DISCRIMINATION_PROTOCOL.md
docs/V061_CROSS_LINE_EVIDENCE_FIREWALL_AND_PREMECHANISM_MATRIX.md
docs/V061_A01_POST_P4_ADMISSION_AUDIT.md
```

There are now three separate boundaries that must not be conflated:

1. A **pre-P4 fully specified successor** was not found by the post-P4 admission audit. Therefore no such package may be retroactively claimed.
2. The **post-P4 Family-B Generation-1 object** was specified prospectively, but is now rejected before STARTED because its own equal-resource recurrent null statically subsumes the frozen scorer signature.
3. A **future Family-C, Generation-2, or replacement object** may be considered only in a new Evidence Analyst cycle and under a fresh prospective identity/package. It must not be designed or tuned in this closeout run in response to the Family-B reduction.

## Current next action

The Family-B Generation-1 closeout is the end of the current MAIN scientific object. After status-only integration is complete, MAIN must return to the Evidence Analyst before designing a successor.

```text
Family-B Generation-1 one-way execution: FORBIDDEN
Family-B Generation-1 same-identity rescue/retune/rebind: FORBIDDEN
Family-B Generation-1 identity consumption: NO — remains unconsumed
Family-C / Gen2 / replacement design in this run: FORBIDDEN
P4 rerun or retune: FORBIDDEN
P5 on failed Family-A P4 mechanism: FORBIDDEN
same-identity or outcome-responsive Family-A rescue: FORBIDDEN
```
