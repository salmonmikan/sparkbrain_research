# C18 v4 correction boundary

v1--v3 artifacts remain historical evidence. v4 will execute only after a
disabled preregistration, a pure source commit, and a source pin. The runner
records preflight evidence during execution. Once result artifacts are
integrated, preflight is intentionally not rerun: repository history then has
allowed artifact/document changes after the execution pin.

## C18 v6 official status

The independently audited official v6 bundle is pinned at
`c0c242d848588d76015734a309f72fed0bd1d380` and uses official seed `1802`.
Its exact-seven inventory retains preregistration, replay and intervention
cases, checkpoint manifest, preflight evidence, report, and a static offline
Brain Lab export. Engineering is `accepted` for the deterministic
observability/replay contract, including schema validation and parent-bound
fork lineage. Scientific status is `not_supported`: the bundle is not an
efficacy, semantic-understanding, biological, or external-performance result.
