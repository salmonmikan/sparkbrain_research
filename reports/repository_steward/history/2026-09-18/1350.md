# SparkBrain Repository Steward history — 2026-09-18 13:50 JST

Selected role: `REPOSITORY_STEWARD`; no inference required.

## Governance reconciliation

- `main@ebed6abfa941c83b36d2e3bddd04e7c5fb0dbe9d` remains stable shared substrate and unprotected.
- Repository rulesets remain `0`.
- Legacy `freeze/*` branches remain `13` and were not modified.
- Authoritative annotated `evidence/*` tags are now `3`: C19-v4, C19-R2, and PD01.
- PD01 is terminal `FAIL_REDUCED_BY_FADING_MEMORY` with consumed/no-retry identity; Stewardship did not reinterpret or modify the evidence chain.
- NI01 is preformal at `research/ni01-no-ignition-selective-prediction-spec-20260918@2664951b65dd18883d3d80862e80b86ac66cf24f`; dedicated preformal and ordinary CI are green on that exact head, but formal START remains unauthorized pending fresh Evidence Analyst authority.
- SUB remains clean `no_op`.

## Issue change

Updated Issue #139 only to correct its factual inventory from one to three authoritative evidence tags and list their tag objects/evidence commits. The Issue remains open because server-side tag update/delete protection is still absent. No ruleset/protection configuration was changed.

## Main-promotion inventory

Completed the Control-Brain-requested read-only structural review of G3/G4/G5, G6 VOMM, G7 HTM-inspired temporal memory, G8 spiking temporal memory, shared comparator interfaces, snapshot/restore, privilege/resource accounting, fairness/transcript infrastructure, and reusable tests.

Strong candidates **only after neutral extraction and fresh current-main CI**:

- anonymous event / prediction-distribution primitives;
- comparator protocol shape with G3-G8 taxonomy removed;
- snapshot/restore interface and deterministic invariants;
- descriptive resource accounting decoupled from CX01 kinds;
- generic privilege-schema guards without the G3-G8 mapping;
- external-only transcript hashing/validation decoupled from CX01 worlds;
- generic no-self-training / no-target-leak / distribution-validation tests.

Conditional implementation-core candidates:

- pure `GenericRecurrentPredictor` extraction from G3;
- G6 `VariableOrderMarkovPredictor` after clean extraction and fresh CI.

Research-only / not direct promotion candidates:

- v0.6 qualification worlds/families/seeds/evidence types;
- G4 explicit Assembly comparator;
- G5 typed functional/reward-head comparator;
- G7 HTM-inspired implementation;
- G8 spiking/replay implementation;
- G3-G8 `ComparatorKind`/privilege mapping;
- CX01 scientific world/schedule/threshold/result semantics and frozen fixtures.

The old comparator branches are not direct promotion material: comparator-extension has no current checks on its exact head, while the observed G6/G7/G8 historical exact-head checks are red. No wholesale merge/cherry-pick is recommended.

## Open PRs

PR #148 (human-directives skill) and PR #149 (scheduler-registry skill) are structurally outcome-independent, mergeable, and observed CI-green. Both are `MAIN_ELIGIBLE_FOR_ORDINARY_REVIEW`; Stewardship did not merge them.

## Preservation / integrity

No legacy freeze mapping change was needed: the new C19-R2 and PD01 tags are direct evidence anchors, not mirrors of legacy freeze branches. No preserve/control/formal/evidence ref was moved or rewritten. No experiment/workflow was executed or dispatched, no identity consumed, no scientific freeze decided, and no research PR merged by Stewardship.
