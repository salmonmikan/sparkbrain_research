# MAIN history — 2026-09-18 22:50 JST

Execution mode: `RELAY`  
Evidence Analyst authority: `57464c4ad6881c27371be5305512656c6ae315d3`  
Lane: `LP01_ACTUAL_LINEAGE_CAUSAL_CREDIT_PROSPECTIVE_SPECIFICATION`

## Lease / collision reconciliation

Inherited PRIMARY lease was `WAITING_EXTERNAL` with heartbeat `2026-09-18T22:27:30+09:00`, active branch `research/lp01-actual-lineage-causal-credit-spec-20260918`, no active identity, and an explicit continuation instruction to collect exact-head checks and repair only science-invariant mechanical failures. No fresh PRIMARY `RUNNING` collision existed. RELAY acquired the MAIN lease. Evidence Analyst tip remained unchanged and SUB had no LP01 lane/fallback.

## External results collected

Prior exact head: `56b0e5ddf2ceffb0a53d517d8125f8302c2d2844`.

- `35350132766` (`lp01-prospective-readiness`) -> `completed/failure`.
- `35350132835` (`ci`) -> `completed/failure`.

Readiness passed prospective contract integrity, native lineage causality source audit, LP01 reference lineage tests, and the NON_EVIDENTIARY dev construction diagnostic before failing focused lint. CI likewise failed at lint before downstream steps.

Observed Ruff-only failures:

1. `I001` import-block formatting in `scripts/check_lp01_contract.py`.
2. `I001` import-block formatting in `scripts/check_lp01_native_causality.py`.
3. `E501` one 101-character assertion in `scripts/run_lp01_dev.py`.
4. `UP035` `Iterable` import modernization in `src/sparkbrain/lp01_lineage.py`.

No scientific contract, semantic, protocol, source-causality, or construction-test failure was observed.

## Continuation performed

Applied only mechanical repairs fixed by the lint output: removed the extra blank line in the two checker modules, wrapped the unchanged assertion string, and moved `Iterable` from `typing` to `collections.abc`.

The branch advanced through four small direct GitHub commits to final exact head:

`research/lp01-actual-lineage-causal-credit-spec-20260918@f6d59a55730c5f99cd7f30470847fc3f175bdf64`

A compare from the inherited head shows only four files changed: two one-line deletions for formatting, one assertion wrapping change, and one import replacement. No LP01 scientific question, candidate/comparator privilege, resource contract, thresholds, semantic-gap conclusion, or formal boundary changed.

Fresh final-head workflows:

- `35352468809` (`lp01-prospective-readiness`) -> `in_progress` at checkpoint.
- `35352468938` (`ci`) -> `in_progress` at checkpoint.

Per waiting policy, RELAY did not remain occupied only waiting for those workflows.

## Scientific / integrity state

New formal scientific information: `none`.

The prospective pre-formal decision remains `NO_HIGH_VALUE_OBJECT`, contingent on clean exact-head readiness/CI before durable completion. LP01 formal identity remains uncreated and unconsumed. STARTED/control, official TEST access, formal preserve, formal scoring, and formal evidence remain absent. Existing consumed identities and immutable evidence were not changed.

Integrity: `CLEAN_PRE_FORMAL_BOUNDARY_NO_IDENTITY_NO_STARTED_NO_OFFICIAL_TEST`.

## Stop / next action

Lease -> `WAITING_EXTERNAL`.

Next MAIN/RELAY cycle: collect `35352468809` and `35352468938` on `f6d59a55730c5f99cd7f30470847fc3f175bdf64`. If both are green and Analyst/head/identity/formal-boundary state is unchanged, persist `LP01_NO_HIGH_VALUE_OBJECT` as completed and STOP for fresh Evidence Analyst review. Mechanical-only failures may be repaired science-invariantly; any semantic/scientific change requires STOP. Do not create formal identity, STARTED, official TEST, formal preserve/scoring/evidence.
