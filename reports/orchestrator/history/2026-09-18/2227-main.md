# MAIN PRIMARY — LP01 prospective semantic-gap checkpoint — 2026-09-18 22:27 JST

Evidence Analyst authority: `57464c4ad6881c27371be5305512656c6ae315d3`  
Execution mode: `PRIMARY`  
Path: FAST PATH; FULL RECONCILIATION not triggered.

## Allocation consumed

Fresh Analyst allocation was `LP01_ACTUAL_LINEAGE_CAUSAL_CREDIT_PROSPECTIVE_SPECIFICATION`, with formal execution forbidden and completion restricted to either a clean prospective contract ready for Analyst review or `NO_HIGH_VALUE_OBJECT`. The matching `PRE_START_SEMANTIC_GAP` contingency requires stopping when lineage cannot be isolated from present-state, information-privilege, hidden-bypass, or resource confounds with a clean falsifier.

## Work performed

Created `research/lp01-actual-lineage-causal-credit-spec-20260918` from `main@ebed6abfa941c83b36d2e3bddd04e7c5fb0dbe9d`. Current exact head is `56b0e5ddf2ceffb0a53d517d8125f8302c2d2844`.

The branch contains:

- a prospective-only draft LP01 contract with no formal identity or execution authority;
- independent NON_EVIDENTIARY `ActualLineageIndex` and `ExplicitParentTable` reference representations;
- provenance-destroyed and bounded-recent-state construction controls;
- focused tests and a dev-only construction diagnostic;
- a native source audit for lineage causal dependencies;
- `docs/research/lp01-semantic-gap.md` recording the prospective decision.

No H7 exploratory result/settings were reused for formal parameter selection.

## Semantic finding

Current native structural lineage is serialized/inspectable metadata but is not read by the ordinary forward decision/reward path. `StructuralSparseModel.forward_step`, backend `run`/`_consume_structural`, reward eligibility, controller discovery and candidate-group selection do not use parent/tombstone ancestry. `candidate_group` mentions `lineage` only in its docstring; its algorithm ranks current routing load/coactivation.

A new delayed-credit adapter could make ancestry causal, but under the allocated matched-information/resource question the admitted parent/event relation is finite explicit state. An ordinary explicit parent-state comparator can reconstruct the same relation losslessly from the same event stream and apply the same ancestry query/credit operation. Any claimed advantage would therefore require information/resource asymmetry, a specialized operation denied to the comparator, or a different claim such as efficiency/inductive bias.

Matching Analyst contingency consumed: `PRE_START_SEMANTIC_GAP`.

Prospective conclusion: **`NO_HIGH_VALUE_OBJECT`**. This is a pre-formal specification conclusion, not scientific evidence.

## Check/fix trail

Readiness run `35349695551` on intermediate head `c6c4838e9f378a1b4527afd9a811e8c019857cc9` failed because the source checker searched raw method text and treated the `candidate_group` docstring word `lineage` as a causal code dependency. Contract integrity had passed. The checker was mechanically fixed to inspect AST names/attributes and ignore strings/docstrings, and focused formatting/iterable-handling fixes were applied without changing the scientific question or conclusion.

Exact-head checks now running:

- LP01 prospective readiness `35350132766` on `56b0e5ddf2ceffb0a53d517d8125f8302c2d2844`;
- ordinary CI `35350132835` on the same head.

## Integrity

Formal identity: none.  
STARTED/control: none.  
Official TEST: not accessed.  
Formal preservation/scoring/evidence: none.  
New formal scientific information: none.

Existing H5/NI01/PD01/C19 evidence and consumed identities were not modified, retried, rescued, or reinterpreted.

## Handoff

Lease should be `WAITING_EXTERNAL`. Relay may collect exact-head readiness/CI. If both become green and Analyst/head/identity state remains unchanged, persist `LP01_NO_HIGH_VALUE_OBJECT` as completed and STOP for fresh Analyst review. Mechanical-only failures may be repaired without scientific changes; any semantic/scientific redesign requires STOP.
