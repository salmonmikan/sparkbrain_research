# MAIN PRIMARY history — C19-R2 pre-formal specification

Timestamp: `2026-09-18 04:31 JST`
Evidence Analyst authority: `b09d90d0545a0448ea5a310f9373969e7471b15d`
Execution mode: `PRIMARY`

MAIN moved from terminally failed/consumed C19-R1 successor-v2 to the Analyst-assigned, scientifically distinct C19-R2 finite-state/state-tracker prospective specification. The stale prior Relay WAITING_EXTERNAL lease was recovered only after reconciling workflow `35260650772` as completed/failure and verifying the new Analyst handoff. R1-v1/v2 remain consumed/no-retry and were not modified, retried, salvaged, scored, or used for R2 design/tuning.

A fresh branch `research/c19-r2-fsa-state-tracker-spec-20260918` was created directly from immutable C19-v4 package `74bfe6b4a39758656f291baaa3f16236e3e71964`. R2 currently has no formal identity, no STARTED ref, no preserve/evidence ref, and no official execution authority.

The pre-formal R2 object prospectively freezes an explicit seven-state pair-reset tracker over the exact immutable C19-v4 I2 representation and deterministic projection: `RESET`, weak/strong state for `a/b/c`; deterministic leader with lexical tie-break; fixed majority threshold `0.5`; fixed transition/readout table; no cross-pair memory, raw-history lookup, external lookup, fitting/tuning/selection, or trainable parameters. Exact pair/seed/runtime/resource bindings, immutable v4 comparator binding, target-free atomic_idx source-map preservation, preserve-before-targets boundary, cluster-aware primary bootstrap, pair-IID secondary sensitivity, terminal criteria, and claim boundary were fixed before any R2 outcome exists.

Only nine R2-owned paths differ from the exact v4 parent. No immutable/v4 scientific source was modified.

Mechanical pre-START fixups under the prospectively authorized `R2_PRE_START_BLOCKER` contingency:

1. Dedicated run `35264684899` failed at lint because the workflow explicitly passed JSON to Ruff and two Python lines exceeded the configured line length. MAIN removed the JSON Ruff argument and wrapped only the two lines; no scientific semantics changed.
2. Dedicated run `35264985183` passed lint/import but one Type-7 quantile golden fixture failed due exact binary-float equality. MAIN changed only that assertion to `math.isclose(..., abs_tol=1e-12)`; the scorer and registered Type-7 calculation were unchanged.

Final exact branch head is `5d5d171cf872baed7a636fd246ab36f3a91a6716`.

Final-head checks at checkpoint:

- dedicated R2 pre-START `35265194243`: `completed/success`;
- ordinary CI `35265194183`: `in_progress`.

No new scientific measurement/result exists. No official R2 data were accessed. R2 remains pre-formal, unSTARTED, unconsumed and identity-free. SUB independent exploratory work was not touched. FULL RECONCILIATION was not triggered; FAST PATH remained sufficient.

PRIMARY stops with lease `WAITING_EXTERNAL` because no useful local critical-path work remains while ordinary CI runs. Relay/next MAIN must collect CI `35265194183` for exact head `5d5d171...` while retaining dedicated success `35265194243`. Mechanical science-invariant failure may be fixed prospectively and both gates rerun on the same new final SHA; a semantic/scientific gap requires STOP for Analyst. If CI is green, persist `R2_PRE_START_READY_FOR_ANALYST_REVIEW` and STOP for fresh Analyst authority. The current handoff does not permit formal identity reservation, STARTED, or official execution.
