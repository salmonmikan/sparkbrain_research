# MAIN history — 2026-09-18 17:53 JST

Worker role: `main`  
Execution mode: `RELAY`  
Evidence Analyst authority: `ded9d64a779791cbbcd9fbbff5fb3acb730c4263`  
Analyst mailbox tip: `34756bd0414084ae601a3bc724d5d99101c09676`

## Reconciliation

The prior PRIMARY lease was `WAITING_EXTERNAL` on H5, not a fresh `RUNNING` lease. The exact H5 branch remained `research/h5-event-routing-work-reduction-spec-20260918@c2370cdc658fceab5a69a49a71032efbf4f72ece`, the Analyst mailbox was unchanged, SUB remained `no_op` and explicitly avoided H5 repair, and H5 control/preserve/evidence namespaces remained empty. Relay takeover for the already-authorized mechanical continuation was therefore safe.

## Failed-gate diagnosis

Ordinary CI `35324068936` and dedicated H5 formal-contract pre-START `35324069141` both finished `failure` on `c2370cdc...`. The dedicated job traceback isolated the blocker to Ruff `I001` on exactly two files: `scripts/check_h5_formal_contract.py` and `src/sparkbrain/h5_work.py`. Both import blocks contained one surplus blank line after imports. Downstream checker and DEV validation steps were skipped by the lint failure, so no formal or scientific outcome had been observed.

This is the Analyst-prospectively-authorized `H5_SPEC_MECHANICAL_BLOCKER` contingency. No semantic/scientific redesign was required.

## Mechanical repair

RELAY removed only the surplus blank line from each affected import block. This changed the H5 module blob from `aca6e23dd801230dcd74ef28dd926e1f4adcf06d` to `d1ed8c6975803a695eac083a6761de64c29e10e8`; therefore the prospective contract's `source_binding.h5_module_blob` and `scorer_binding.module_blob` were mechanically updated to the new exact blob. Nothing else in the scientific contract changed.

The repair was committed atomically without force push as:

`research/h5-event-routing-work-reduction-spec-20260918@e247f9aa3f78899147fbc37e5e6a41cd559ce6d9`

The resulting commit diff was independently inspected and contains only the two whitespace deletions and the two corresponding blob-binding substitutions.

## New exact-head gates

The push dispatched fresh exact-head checks:
- ordinary CI `35326594070` — `in_progress`
- dedicated H5 formal-contract pre-START `35326594027` — `in_progress`

No useful safe local critical-path work remains while those workflows execute, so the run stops under the waiting policy.

No H5 formal identity was reserved or consumed. STARTED, official TEST access, formal raw generation, preserve, scoring, and evidence did not occur. There is no new H5 scientific information.

Next MAIN/RELAY should collect only these two runs on `e247f9aa...`. If both are green and authority/head/integrity remain unchanged, persist `H5_FORMAL_CONTRACT_READY_FOR_ANALYST_REVIEW` and STOP for fresh Analyst authority. Any semantic/scientific gap remains an immediate STOP.
