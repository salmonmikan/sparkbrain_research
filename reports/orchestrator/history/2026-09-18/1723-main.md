# MAIN history — 2026-09-18 17:23 JST

Worker role: `main`  
Execution mode: `PRIMARY`  
Evidence Analyst authority: `ded9d64a779791cbbcd9fbbff5fb3acb730c4263`  
Analyst mailbox tip: `34756bd0414084ae601a3bc724d5d99101c09676`

## Frontier

NI01/H4 was already terminal `FAIL_REDUCED_BY_CONFIDENCE_ABSTENTION` in the fresh Analyst handoff. PRIMARY therefore hard-stopped NI01 and accepted the new MAIN lane `H5_EVENT_ROUTING_WORK_REDUCTION_PROSPECTIVE_SPECIFICATION`, which authorizes specification/readiness only and requires STOP at `H5_FORMAL_CONTRACT_READY_FOR_ANALYST_REVIEW`.

FAST PATH remained valid. Fresh `main` is `ebed6abfa941c83b36d2e3bddd04e7c5fb0dbe9d`. The prior MAIN lease was stale and on terminal NI01; no concurrent H5 MAIN mutation was found. SUB remains `no_op`, and the only prior H5 branch is explicitly `EXPLORATORY_NON_EVIDENTIARY` and was not reused as evidence or as pre-frozen science.

## H5 prospective package

Created fresh branch `research/h5-event-routing-work-reduction-spec-20260918` from current main and advanced it without force to `c2370cdc658fceab5a69a49a71032efbf4f72ece`.

The package fixes a SparkBrain lazy/event-routed candidate and a dense-equivalent comparator with identical graph/state/parameter/input/seed/precision/output semantics. The comparator additionally materializes all Sparks and audits all graph edges at each queued event. Symmetric counters include queue, source/destination, state/decay/threshold-relax, eligibility-edge, route-edge, message, arithmetic, residual and fan-out bookkeeping work. The existing engine's all-edge eligibility decay is explicitly counted on both sides.

Formal sparse workload proposal is prospectively fixed to families `uniform/clustered/bursty`, sizes `128/384`, activity fractions `0.01/0.05/0.15`, horizon `24`, and eight fixed seeds (`144` sparse cells). A fully active `uniform`, size `128`, activity `1.0`, horizon `8` eight-seed dense control is diagnostic only. Quality requires logical activation error `<=1e-10`, exact fired vectors, exact processed-event counts and drained queues. Primary work reduction is `1-candidate/dense`, clustered by workload seed with 10,000 Type-7 percentile bootstrap replicates at seed `75001`. PASS is quality + CI lower `>=0.20` + every sparse activity mean `>=0.10`; FAIL is quality + CI upper `<=0.05` or all sparse activity means `<=0`; otherwise INCONCLUSIVE.

Exact prospective bindings include engine `48fa64414f539ec4a00acc9e25ffc9b1d26a4982`, model `ab450dba5943fa9184e66c00a4e992d6588bf6a5`, package `49d47f3d65f67f1c0a6b82a3f7fc2d0364e2a88c`, H5 module `aca6e23dd801230dcd74ef28dd926e1f4adcf06d`, and contract `44d2b3fa6108294ebfaa819453888c2a06c81cba`.

No formal identity was created. No STARTED, official TEST, formal raw, preservation, scoring, or evidence occurred. No new H5 scientific information exists.

## Gate state and handoff

Exact head `c2370cdc...` triggered ordinary CI `35324068936` and dedicated `H5 formal-contract pre-START` `35324069141`; both were `in_progress` at checkpoint. Lease is `WAITING_EXTERNAL` because no useful safe local critical-path work remains while these jobs execute.

Relay/next MAIN should collect those two exact-head runs only. If both succeed and Analyst/head/lease remain unchanged, persist `H5_FORMAL_CONTRACT_READY_FOR_ANALYST_REVIEW` and STOP for fresh Analyst authority. Science-invariant mechanical failures may be repaired prospectively with all new-head gates rerun. Any semantic/scientific redesign requirement is a STOP. Identity/STARTED/official TEST/preserve/score/evidence remain forbidden.
