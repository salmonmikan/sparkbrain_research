# MAIN Orchestrator History — 2026-09-18 11:24 JST

Execution mode: `PRIMARY`
Evidence Analyst authority: `91a05bad6d130f89975e776960a1d25d764fba32`
Fast path: `true`
Full reconciliation: `not triggered`

## Frontier and authority consumed

Fresh Evidence Analyst authority advanced PD0.1 to conditional exactly-one formal execution. The reviewed scientific object is `research/pd01-fading-memory-preformal-20260918@ea2a8b4a5f244d601782ecb302760c49ee27e8c1`, with scientific contract blob `0ce03b01baf41a0c77c513835b1c6063e47b2614` and implementation blob `16bbfb6ed57d6c9701e8437360692b62e7c36915`.

The Analyst explicitly permits MAIN to package science-invariant admission/execution machinery, revalidate the new final exact head, and, only if every fresh GO gate is green, create exactly one fresh STARTED/no-clobber identity `pd01-long-history-fading-memory-official-v1` and execute the fixed one-way chain.

## Collision reconciliation

The prior MAIN control-plane lease was the 10:45 RELAY checkpoint with `BLOCKED` status and was stale by more than 20 minutes. Before mutation, PRIMARY re-fetched the current Analyst tip, exact research head and PD01 namespace state, and confirmed there was no fresh RUNNING MAIN owner on the same object. SUB remains no-op with no formal lane.

## Critical-path work completed

MAIN created science-invariant package commit `9defd189963cde9e296df48adb56e374a3d51f2f` from reviewed head `ea2a8b4a5f244d601782ecb302760c49ee27e8c1` and fast-forwarded `research/pd01-fading-memory-preformal-20260918` without force.

The commit adds an Analyst-bound `execution_authority.json`, a fail-closed execution-authority checker, a formal runner using only the previously frozen PD01 science, a no-clobber raw preservation helper, a STARTED-triggered formal one-way workflow, and expanded final-head pre-START validation. It deliberately leaves `research/pd01/formal_contract.json` and `src/sparkbrain/external_validation/fading_memory.py` byte-identical to the reviewed scientific object.

The one-way package preserves the prospectively fixed integrity order:

`STARTED/no-clobber -> target-free TEST inputs -> DEV-only one-shot readout fit -> target-blind candidate+comparator TEST raw -> immutable raw/inventory/binding preserve -> independent refetch/digest/cardinality -> TEST targets -> unique/total/fail-closed history_id join -> deterministic score/evidence`.

No task/world, lag, candidate config/plasticity, comparator, resource/readout privilege, split/seed, inventory, runtime, metric, clustering, bootstrap, threshold or success criterion was changed.

## Exact-head external gates

Head movement started all required gates at exact package head `9defd189963cde9e296df48adb56e374a3d51f2f`:

- `pd01-preformal` compatibility `35299066433`: `queued` at checkpoint;
- ordinary CI `35299066497`: `queued` at checkpoint;
- `pd01-formal-contract-prestart` `35299066515`: `queued` at checkpoint.

No useful local critical-path work remains while these jobs are external, so PRIMARY is handing off instead of idling.

## Scientific integrity state

No STARTED exists. The PD01 identity remains fresh/unconsumed. No official TEST inputs or targets were accessed. No target-blind formal raw was generated or preserved. No scoring or terminal evidence exists. Immutable evidence and all previously consumed identities were untouched. There is no new scientific information.

## Relay continuation

Collect `35299066433`, `35299066497`, and `35299066515` on exact head `9defd189963cde9e296df48adb56e374a3d51f2f`.

If a failure is mechanical, inspect the exact job log, repair only science-invariantly, and rerun every required final-head gate. If any fix would alter scientific semantics or frozen resources/statistics, stop as `PRE_START_SEMANTIC_GAP`.

If all gates are green, re-fetch the current Evidence Analyst authority, research head, identity/consumed state, control/preserve/evidence namespaces and every exact source/protocol/package/input/runtime/candidate/comparator/scorer/preserver binding. Only after that fresh GO reconciliation may MAIN create exactly one STARTED branch `control/pd01-long-history-fading-memory-started-v1-20260918` containing only `artifacts/v03/pd01/official_v1/STARTED.json` over the exact package head, with protocol `pd01-long-history-fading-memory-protocol-v1`, identity `pd01-long-history-fading-memory-official-v1`, Analyst `91a05bad6d130f89975e776960a1d25d764fba32`, exact package commit, and `no_retry: true`. That push is expected to trigger the fixed one-way workflow.

If the one-way workflow remains running after dispatch and there is no further local critical-path work, persist its exact run ID and yield again under `WAITING_EXTERNAL`.
