# MAIN PRIMARY history — 2026-09-18 10:28 JST

Evidence Analyst authority: `28108fffc2e4e346bff79bf3e38d1cac27d3265c`
Execution mode: `PRIMARY`
Fast path: `true`
Full reconciliation: `not triggered`

## Frontier advanced

Advanced PD0.1 from pre-formal readiness to a fully specified matched fading-memory formal-contract proposal on `research/pd01-fading-memory-preformal-20260918`.

Starting research head: `6e409274a27d2a6abf785ba3940ab81c6d24a82f`
Checkpoint research head: `ea2a8b4a5f244d601782ecb302760c49ee27e8c1`
Proposed formal identity namespace: `pd01-long-history-fading-memory-official-v1` — **UNRESERVED**.

## Prospective contract frozen in this run

- paired remote-history/washout task with two anonymous lineages;
- intervention `(1.0, 0.0)` versus `(0.0, 1.0)` with exact symmetric post-event filler and final symmetric probe `(0.75, 0.75)`;
- lag grid `16, 32, 64, 128`, recent window `8`;
- DEV 64 base worlds seed `33031`, TEST 128 base worlds seed `77237`;
- TEST inventory 1,024 histories and 2,048 raw model-score rows;
- fixed SparkBrain v0.4 source `main@ebed6abfa941c83b36d2e3bddd04e7c5fb0dbe9d`, fixed 8x8/config/reset semantics, 64-dimensional final-probe response;
- fixed stronger comparator: deterministic sparse contractive tanh reservoir, state 64, input 2, fan-in 8, leak 0.35, recurrent row L1 0.90, input scale 0.50, bias scale 0.05, seed 90917, no recurrent fit/tune;
- identical ridge-linear DEV readout privilege for both models, alpha 0.001, 65 coefficients, one fit and no selection/retries;
- Python 3.11.16, no GPU/network, ceiling 2 CPU / 8 GiB / 4 hours;
- primary lags 64/128, accuracy effect SparkBrain minus reservoir, base-world cluster bootstrap 10,000 seed 19901 with Type-7 percentile intervals;
- PASS iff effect CI lower >= +0.10 and SparkBrain accuracy CI lower >= 0.60; FAIL_REDUCED iff effect CI upper <= +0.05; otherwise INCONCLUSIVE;
- raw-before-target / preserve-before-read integrity sequence and fail-closed invalid-evidence conditions.

Implemented the fixed reservoir, target-free input and separate target generators, equal ridge readout, candidate/comparator probe feature paths, cluster-bootstrap scoring primitives, `formal_contract.json`, `FORMAL_CONTRACT.md`, synthetic/dev-only tests, fail-closed checker, and dedicated formal-contract pre-START workflow.

## Same-run mechanical repair

Initial dedicated formal gate `35295140779` on head `938c8ed366130d160b79dcad8deb2f10088e33fa` failed only at Ruff: unused `math.exp` and a 101-character docstring line. This matched prospectively authorized `PRE_START_BLOCKER_MECHANICAL`.

Removed the unused import and wrapped the docstring only. Because the implementation Git blob changed mechanically, rebound the exact implementation blob in the formal contract and checker to `16bbfb6ed57d6c9701e8437360692b62e7c36915`. No scientific field, comparator architecture, threshold, statistic, resource choice, runtime choice, or identity semantics changed.

## External checkpoint

Exact final head `ea2a8b4a5f244d601782ecb302760c49ee27e8c1` has three active push runs:

- formal-contract pre-START `35295370268`: `in_progress`;
- ordinary CI `35295370271`: `in_progress`;
- legacy/pre-formal compatibility `35295370202`: `in_progress`.

No useful local critical-path work remains while these jobs execute. Lease transitioned to `WAITING_EXTERNAL` for Relay collection.

Relay/next MAIN must collect these exact-head runs. Mechanical science-invariant failures may be fixed under the existing contingency and all exact-head gates rerun. Semantic/scientific gaps require STOP. If required gates are green, persist `PD01_FORMAL_CONTRACT_READY_FOR_ANALYST_REVIEW` and STOP.

## Integrity / stop

No formal identity reserved. No STARTED/control ref created. No official PD0.1 TEST input/target accessed. No one-way execution dispatched. No raw preserve/evidence/scoring occurred. No immutable or consumed identity was modified. No C19/R2 per-example outcome was used for design. There is **no new scientific information**.

Even after all gates become green, a newer Evidence Analyst handoff reviewing the final exact green head is required before identity reservation, STARTED, official access, preservation/scoring, or one-way execution.
