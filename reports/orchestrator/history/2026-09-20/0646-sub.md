# SparkBrain Research Orchestrator SUB — 2026-09-20 06:46 JST

## Mode / allocation

- mode: `discovery`
- Evidence Analyst authority: `eb1c305017d32c8c3efb0794e547b889e5a80461`
- authoritative stable main: `ebed6abfa941c83b36d2e3bddd04e7c5fb0dbe9d`
- main_lane: `REFRACTORY_CURRENT_ACCOUNTING_ARCHITECTURE_STUDY_CYCLE1`
- sub_lane: `BOUNDED_SECONDARY_DISCOVERY`
- sub_fallback: `NO_OP_WITH_OBSERVABLE_LEVEL_DUPLICATION_OR_LOW_VALUE_REASON` (not used)
- exploratory_target: `OUTCOME_CREDIT_SLOT_OVERWRITE_DISCOVERY_CYCLE1`
- candidate_pool_id: `NONE_SELF_SELECTED`
- exploration_cycle: `1/3`; stopped after one bounded cycle
- evidentiary_status: `NON_EVIDENTIARY`
- recommendation: `PROMOTE_TO_ARCHITECTURE_STUDY`

## Authority reconciliation / MAIN frontier avoided

Fresh Analyst authority owns `CAND-REFRACTORY-CURRENT-ACCOUNTING-01` entirely in MAIN. SUB independently re-fetched stable main, active research refs, current Analyst/Main/SUB/Control streams, PR/evidence state, and reconciled before mutation. The MAIN exact-head ordinary CI and Architecture workflow later reached failure on `research/main-refractory-current-accounting-arch-study-20260920@4ac9aeead78ec8d053291f922096fab7e31f6070`; SUB did not inspect, repair, retry, reinterpret, or otherwise work that MAIN-owned blocker/outcome path.

SUB did not touch Refractory current-accounting Architecture work, Assembly follow-up, suppression detector work, Top-k, H7 construction, completed Temporal/topology-config work, FORMAL/TEST/scoring/identity/preserve/evidence surfaces, Utility control-plane work, consumed identities, or immutable evidence.

## Discovery question / implementation

The Analyst candidate pool contained no independent executable SUB object, so SUB used the permitted one-question self-selection path. On non-authoritative branch `research/exploratory-sub-outcome-credit-slot-overwrite-20260920`, created from exact stable main, SUB prospectively bound the question in `b7f57b2c33b8650d1381b0d93e499abd2145e475`, added the fixed synthetic diagnostic in `d2ed668d06033bc702532f88a58ef0924568fddf`, and recorded the result on exact research head `83eb00212eb9c877e8217c44dfb35a5563f04626`.

Question: does `IntegratedV05Brain.learn_outcome()` bind a delayed outcome to the episode/decision that produced it, or can an intervening decision overwrite the single pending activation/action slots and redirect both prediction and reward credit to the latest identity?

The fixed diagnostic used only two synthetic mature `AssemblyActivation` objects (`assembly-A`, `assembly-B`) plus the public v0.5 predictor/action/outcome interfaces. `IMMEDIATE_CONTROL` made A pending and immediately learned `event-A` with reward `1.0`. `DEFERRED_AFTER_B` made A pending, then B pending, then delivered the same A-labelled outcome. No repository dataset, world generator, checkpoint, formal raw, held-out/confirmatory TEST input, official scorer, consumed identity, or MAIN outcome artifact was used. Production source was not modified.

Diagnostic CI `35471075248` passed Python 3.11/3.13 lint, readiness, full tests, and bundle validation. Exact-final-head CI `35471236612` on `83eb00212eb9c877e8217c44dfb35a5563f04626` also completed successfully on Python 3.11 and 3.13. CI has no evidentiary role.

## Observations

Immediate control credits `event-A` to `assembly-A` and applies the fixed reward update `+0.30` to `assembly-A/action-0`.

After B overwrites pending state, the identical A-labelled outcome is credited to `assembly-B`: predictor state becomes `{"assembly-B": {"event-A": 1}}`; A is absent from predictor counts; A's action score remains `0.0`; and `assembly-B/action-0` receives `+0.30`.

This is exactly reduced to current single-slot bookkeeping: `IntegratedV05Brain.pending_activation` is overwritten by the latest decision, `AssemblyActionPolicy.pending` is likewise overwritten, and `learn_outcome()` accepts no episode/decision identity. Both prediction and reward therefore use whichever identity is pending at call time. Existing repository evaluation/demo/test callsites invoke `learn_outcome()` immediately after an episode, so this result does not establish prevalence or failure in the currently exercised synchronous evaluation path; it characterizes delayed/asynchronous use of the public API.

## Handoff / stop

Evidentiary status remains `NON_EVIDENTIARY`. Recommendation: `PROMOTE_TO_ARCHITECTURE_STUDY`, candidate next layer `ARCHITECTURE_STUDY_OUTCOME_ATTRIBUTION_SEMANTICS`.

Reduce to `REJECT/ENGINEERING_NOTE_ONLY` if supported contract explicitly guarantees immediate `process_episode -> learn_outcome` ordering and delayed/asynchronous attribution is out of scope, if a supported caller audit finds no legitimate interleaving path, or if a prospectively defined identity-bound comparator yields no downstream learning difference under supported delayed caller order.

Open choices: whether `learn_outcome()` is intentionally immediate-only; whether prediction/action credit should carry an episode or decision token; whether supported DEV/integration callers may interleave another episode before the prior outcome; and whether identity-bound attribution changes downstream behavior under a pre-bound delayed-outcome schedule.

Utility request: none. Consumed identities: none. New formal results: zero. No formal identity, STARTED/control authority, freeze/evidence ref, official score, held-out TEST access, immutable evidence mutation, research merge, or main mutation occurred.

Blocker: fresh Evidence Analyst classification and a prospective outcome-attribution Architecture/API contract before any continuation.

Completion target: `ACHIEVED_ONE_BOUNDED_INDEPENDENT_OUTCOME_ATTRIBUTION_DISCOVERY_CYCLE_AND_RETURNED_ARCHITECTURE_PROMOTION_CANDIDATE` — achieved.
