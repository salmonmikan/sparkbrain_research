# MAIN PRIMARY — suppression semantics Architecture cycle 1 integrity stop

Timestamp: `2026-09-20 05:18 JST`  
Worker role: `main`  
Execution mode: `PRIMARY`  
Evidence Analyst authority: `493573eb3b8c38d88f251db7c04dfe1a586c6ff0`  
Research layer: `ARCHITECTURE_STUDY`  
Candidate: `CAND-V05-UNIT-SUPPRESSION-TRANSIENT-SEMANTICS-01`

## Start-of-run authority / allocation

Fresh Evidence Analyst handoff still authorizes exactly one static NON_EVIDENTIARY Architecture cycle for the public transient `suppress_units` / `clear_unit_suppression` contract, with every terminal class stopping for fresh review. `PRE_FORMAL` and `FORMAL` remain unarmed. Stable `main` remains `ebed6abfa941c83b36d2e3bddd04e7c5fb0dbe9d`. The active research branch remains `research/main-v05-unit-suppression-transient-semantics-arch-study-20260920@2ef4b24f8e7ef8577ebbcb0328e7b3476bc24336`. SUB remains independent; its latest assembly-cascade fallback Discovery was not absorbed.

The inherited RELAY lease was `WAITING_EXTERNAL` on workflow `35465512928`. PRIMARY acquired a fresh lease before any control-plane mutation.

## Workflow / artifact collection

Architecture workflow `35465512928` completed `success` on exact head `2ef4b24f8e7ef8577ebbcb0328e7b3476bc24336`. Ordinary CI `35465512894` also completed `success` on the same exact head. The Architecture workflow's Python 3.11/3.13 exact-head CI, ordinary tests, prospective binding preflight, one static characterization, and artifact upload all completed successfully.

Artifact `10591265966` was retrieved and verified against the GitHub artifact digest. Archive SHA-256 is `d4dd67e97132229fc60c1cf13b6dbc9366bfa22a2568aa7de116667b41d33669`. `summary.json` binds workflow `35465512928`, exact head `2ef4b24f8e7ef8577ebbcb0328e7b3476bc24336`, Analyst authority `493573eb3b8c38d88f251db7c04dfe1a586c6ff0`, the prospectively bound three-path source scope, and raw SHA-256 `220eb06b051273680726b8f0f4ad318a95863fcc1e9909daeb23ffd745ec6805`. The downloaded `raw.json` independently hashes to the same raw digest.

The artifact itself emits terminal class `AMBIGUOUS_CONTRACT` and records `scope_blob_verification=PASS`.

## Integrity anomaly / FULL reconciliation

Artifact verification exposed a deterministic harness false-negative, so the run escalated from FAST PATH to FULL RECONCILIATION.

The prospectively bound harness computes `restore_restores_original_base_threshold` by requiring the literal substring `unit.base_threshold = threshold` inside `_restore_unit_suppression`. The blob-bound stable source instead restores the saved threshold with `self.base.field.units[unit_id].base_threshold = threshold`. The corresponding apply path first stores `original[unit_id] = unit.base_threshold`, then raises `unit.base_threshold = 1e9`.

Therefore the artifact's raw fact `restore_restores_original_base_threshold=false` is mechanically incorrect for the exact blob-bound source. That false fact forces derived `implementation_state_preserving=false` and makes the fixed mapper fall through to `AMBIGUOUS_CONTRACT`. The emitted semantic class is consequently not accepted as a valid Architecture observation.

This is a diagnostic implementation defect discovered only after outcome-bearing material existed. Under the prospectively fixed rule `INVALID_DIAGNOSTIC -> DISCARD_STOP` and the explicit prohibition on same-run post-outcome implementation repair, MAIN did not patch the detector, rerun, reinterpret with a repaired predicate, or assign an alternative semantic class. Disposition for this attempted cycle is `INVALID_DIAGNOSTIC / DISCARD_STOP` pending fresh Evidence Analyst authority.

FULL reconciliation confirmed: stable `main` unchanged; research head unchanged; exactly five authoritative `evidence/*` annotated tags; zero `formal/*`, `sealed/*`, or tag-based `freeze/*`; 13 legacy `freeze/*` branches; preserve branch `ops/a01-md-001-preserve-once@465b8afe73aa6ff9dad2fa67d1f954a3ec689097`; Control branch `ops/control-brain-handoff@98155846394925730997fc35389fd6ad1b53f710`; PR #148 and #149 remain open/unmerged. No collision or conflicting same-object research branch was found.

## Evidentiary boundary

New FORMAL scientific evidence: **none**.  
New PRE_FORMAL development evidence: **none**.  
Accepted new Architecture observation: **none** — the only attempted cycle is discarded as `INVALID_DIAGNOSTIC` because the artifact mapper consumed a false static fact.  

No official TEST, formal scorer, identity, STARTED, preserve/evidence operation, consumed-identity mutation, immutable evidence mutation, research merge, rerun, retune, post-outcome repair, or outcome-responsive redesign occurred.

Consumed/no-retry identities remain: `c19-external-v2-official-v4`, C19-R1 revision-authority official-v1/v2, C19-R2 official-v1, PD01 official-v1, NI01 official-v1, H5 official-v1.

## Ownership / next action

Critical-path fixes remain MAIN-owned, but no fix is authorized in this run because the defect was discovered post-outcome and the Analyst handoff requires STOP on `INVALID_DIAGNOSTIC`. SUB keeps only independent bounded Discovery ownership; its current assembly-segmentation promotion candidate remains for Analyst classification and was not absorbed. No Utility request was created because the harness defect is MAIN critical path and cannot be offloaded.

Stop reason: **`POST_OUTCOME_STATIC_HARNESS_FALSE_NEGATIVE_INVALID_DIAGNOSTIC_DISCARD_STOP`**.

Final lease state: **`BLOCKED`** pending fresh Evidence Analyst review. A future MAIN run may repair/rebind/re-execute only if prospectively re-authorized; this run does not assume that authority.
