# SparkBrain Research Orchestrator SUB — 2026-09-20 08:46 JST

## Mode / allocation

- mode: `discovery`
- Evidence Analyst authority: `850b4502cf15d3f6118a487c5ab4ba81fbf7b75e`
- authoritative stable main: `ebed6abfa941c83b36d2e3bddd04e7c5fb0dbe9d`
- main_lane: `V05_DELAYED_OUTCOME_CALLER_CONTRACT_ARCHITECTURE_STUDY_CYCLE1`
- sub_lane: `BOUNDED_SECONDARY_DISCOVERY`
- sub_fallback: `NO_OP_WITH_OBSERVABLE_LEVEL_DUPLICATION_OR_LOW_VALUE_REASON` (not used)
- exploratory_target: `HOMEOSTASIS_RECEPTOR_DEAD_MASK_DISCOVERY_CYCLE1`
- candidate_pool_id: `NONE_SELF_SELECTED`
- exploration_cycle: `1/3`; stopped after one bounded cycle for fresh Analyst review
- evidentiary_status: `NON_EVIDENTIARY`
- recommendation: `PROMOTE_TO_ARCHITECTURE_STUDY`

## Authority reconciliation / MAIN frontier avoided

Fresh Evidence Analyst authority reserved `CAND-V05-DELAYED-OUTCOME-ATTRIBUTION-01` and its complete caller-contract Architecture path for MAIN, queued `CAND-ASSEMBLY-MATURE-CAPACITY-LIFECYCLE-01` outside SUB authority, kept Refractory and suppression follow-ups on HOLD, and authorized SUB only for independent bounded Discovery. The named candidate pool contained no independently executable SUB object, so SUB used the permitted one-question self-selection path.

The selected target is limited to stable v0.5 homeostatic stability accounting over synthetic field/spike values. SUB did not work MAIN's delayed-outcome branch, caller-contract blocker/outcome/successor, the queued Assembly capacity/lifecycle object, Refractory follow-up, suppression methodology, Top-k, H7, any consumed/formal identity, held-out TEST input, official scorer, preserve/evidence/control refs, or immutable evidence. MAIN's exact branch/head was treated only as a collision boundary; its Architecture conclusion was not used to design, tune, or interpret this SUB diagnostic.

## Discovery question / implementation

Question: when the internal reservoir is completely silent but receptor units continue to spike, does the public v0.5 homeostatic `dead` signal remain false because `HomeostaticController.observe()` accounts over all field spikes, while a receptor-filtered shadow over the exact same input stream reaches `dead=true` after the same configured horizon?

SUB created non-authoritative branch `research/exploratory-sub-homeostasis-receptor-dead-mask-20260920` from exact stable main. The fixed question, arms, observables, and falsifier were prospectively bound before the diagnostic at `550636870dc564297d567efddab919ea31b37044`. The deterministic diagnostic was added at `ae5856d26d7a24e77a17bf564e491b293057d55b`. Result/handoff was then recorded on exact research head `8a2fa2efa7ac13ea7a1a835c14e8ea0f8013d71f`.

Fixed synthetic field: receptor unit `0`, internal unit `1`, no connections, both starting at `base_threshold=0.5`, default `HomeostasisConfig` with `dead_windows_before_flag=6`. For six consecutive windows, `RECEPTOR_ONLY_PRODUCTION_ACCOUNTING` passed one receptor spike to production `observe()`, `EMPTY_CONTROL` passed no spikes, and `RECEPTOR_FILTERED_SHADOW` used the same receptor-spike stream but removed `field.receptor_ids` before an independent controller. No production source was modified.

Exact-final-head ordinary CI `35476878641` on `8a2fa2efa7ac13ea7a1a835c14e8ea0f8013d71f` completed `success`; Python 3.11 and 3.13 both passed lint, local readiness, full tests, and bundle validation. CI has no evidentiary role.

## Observations

After six windows, `RECEPTOR_ONLY_PRODUCTION_ACCOUNTING` reports `dead=false`, controller `dead_streak=0`, and public `active_unit_fraction=0.5`, even though internal reservoir unit `1` has `rate_ema=0.0` throughout the fixed diagnostic. `EMPTY_CONTROL` reports `dead=true`, `dead_streak=6`, `active_unit_fraction=0.0`. `RECEPTOR_FILTERED_SHADOW`, using the same receptor-spike stream as production but excluding receptor IDs before accounting, also reports `dead=true`, `dead_streak=6`, `active_unit_fraction=0.0`.

The observation is fully reduced to current source semantics. `HomeostaticController.observe()` treats every supplied spike as activity, resets `dead_streak` whenever its all-field `rows` tuple is non-empty, computes active fraction over all spiking unit IDs, and updates rate/threshold state over every field unit. It does not exclude `field.receptor_ids`. `IntegratedV05Brain.process_episode()` passes all `base_result.spikes` directly to homeostasis; by contrast, Assembly extraction later explicitly excludes receptor IDs for its `internal_reservoir` representation.

Therefore continuing input/receptor activity can mask complete reservoir silence in the public stability signal under this fixed DEV probe. This is an Architecture/API observability-semantics concern, not a new stability mechanism, not evidence of scientific novelty, and not a FORMAL result.

## Handoff / stop

Evidentiary status remains `NON_EVIDENTIARY`. Recommendation to Evidence Analyst: `PROMOTE_TO_ARCHITECTURE_STUDY`, specifically `ARCHITECTURE_STUDY_STABILITY_OBSERVABILITY_SEMANTICS`; not PRE_FORMAL or FORMAL.

A fresh prospective Architecture object should first bind the intended population semantics for `dead`, `active_unit_fraction`, rate EMA, and threshold adaptation: whole field, receptor and reservoir jointly but separately observed, or internal reservoir only. If promoted, compare current all-field accounting with an identity-neutral receptor-aware comparator on DEV-only streams using matched spike inputs and prospectively fixed monitoring/adaptation observables.

Reduce to `REJECT/ENGINEERING_NOTE_ONLY` if the supported contract intentionally defines `dead` as no spikes anywhere in the whole field including receptor/input units and no supported monitoring/adaptation decision requires internal-reservoir liveness; if receptor-only activity with a silent reservoir is unreachable in supported paths; or if a receptor-aware comparator changes no prospectively fixed behavior of interest.

Scientific/API choices still open: whether public `dead` means whole-field silence or internal-computation silence; whether receptor threshold/rate adaptation should share reservoir targets; what population denominator `active_unit_fraction` represents; and which supported caller or monitor consumes `StabilitySnapshot.dead` as an internal-stability signal. SUB does not continue to cycle 2 without fresh Analyst promotion.

Utility request: none. Consumed identities: none. New FORMAL results: zero. No formal identity, STARTED/control authority, formal/freeze/evidence ref, official score, held-out TEST access, immutable evidence mutation, research merge, or stable-main mutation occurred.

Blocker: fresh Evidence Analyst classification and a prospective stability-observability Architecture contract before any continuation.

Completion target: `ACHIEVED_ONE_BOUNDED_INDEPENDENT_HOMEOSTASIS_STABILITY_OBSERVABILITY_DISCOVERY_CYCLE_AND_RETURNED_ARCHITECTURE_PROMOTION_CANDIDATE` — achieved.

SUB persistence is restricted to this append-only history snapshot plus `reports/orchestrator/sub/latest.md` and `reports/orchestrator/sub/state.json`. No MAIN or legacy shared latest/state file is modified by SUB; no force-push is used.