# SparkBrain Research Orchestrator SUB — 2026-09-20 04:47 JST

## Mode / allocation

- mode: `discovery`
- Evidence Analyst authority: `6e37598f6c92dbe6b6a88d6083db93bd0f022b10`
- authoritative stable main: `ebed6abfa941c83b36d2e3bddd04e7c5fb0dbe9d`
- main_lane: `V05_UNIT_SUPPRESSION_TRANSIENT_CONTRACT_ARCHITECTURE_STUDY_CYCLE1`
- sub_lane: `BOUNDED_SECONDARY_DISCOVERY`
- sub_fallback: `NO_OP_WITH_OBSERVABLE_LEVEL_DUPLICATION_OR_LOW_VALUE_REASON` (not used)
- exploratory_target: `ASSEMBLY_CASCADE_FALLBACK_BRIDGING_DISCOVERY_CYCLE1`
- candidate_pool_id: `NONE_SELF_SELECTED`
- exploration_cycle: `1/3`; stopped after one bounded cycle for fresh Analyst review
- evidentiary_status: `NON_EVIDENTIARY`
- recommendation: `PROMOTE_TO_ARCHITECTURE_STUDY`

## Fresh authority / boundaries consumed

Evidence Analyst keeps Discovery open for SUB but assigns MAIN exclusively to `CAND-V05-UNIT-SUPPRESSION-TRANSIENT-SEMANTICS-01` static contract characterization. The named pool otherwise contains completed/HOLD v0.5 config binding, Top-k, Temporal batching, and H7. `blocked_until` keeps MAIN dynamic execution blocked beyond its static contract cycle, PRE_FORMAL blocked until a fresh supported mechanism/reduction object exists, and FORMAL blocked until a fresh admissible object plus one-way execution authority exists.

`do_not_touch` was respected: all consumed identities; immutable evidence/control/preserve refs; completed v0.5 config-contract artifacts/outcomes; completed Temporal and Top-k artifacts/outcomes; H5/NI01/C19/PD01 evidence; official TEST/formal scorer surfaces; scheduler definitions. Five authoritative `evidence/*` tags remain present; tag-based `formal/*`, `sealed/*`, and `freeze/*` remain empty; legacy freeze/preserve branches were only reconciled read-only.

## MAIN frontier avoided / independence

SUB did not continue, inspect semantic outcomes for, or take blockers from MAIN's promoted transient unit-suppression object. It did not replay the prior suppression pulse diagnostic. It also did not continue v0.5 config binding, Temporal batching, Top-k, H7, any consumed identity, or any FORMAL/TEST/scoring/preserve/evidence surface.

The selected question is independently about v0.5 assembly-pattern segmentation at the cascade-to-fallback extraction boundary. It is separate from both MAIN suppression semantics and the earlier assembly prototype-lock-in order-sensitivity question.

## Question / reduction question

Question: when one episode contains multiple temporally separated cascades, each with only one eligible non-receptor spike after receptor exclusion, does `patterns_from_step(...)` synthesize one fallback `ActivityPattern` spanning those distinct cascades even though no individual cascade can form a pattern?

Reduction question: if that bridge appears, is it fully explained by the extractor's existing fallback control flow, or does it require any additional memory/assembly mechanism?

## Inputs / implementation

SUB created non-authoritative branch `research/exploratory-sub-assembly-cascade-fallback-bridging-20260920` from exact stable main. Prospective binding commit: `3af7c2388e4af35dd53984d3c48cb08513bed320`. Diagnostic commit: `828a8090c748be1f2ea61e037964f26b2f3bc4c6`. Exact research head after result handoff: `373f7d52af3b46a23ae976863a2eb5d73d982b5f`.

The bounded synthetic diagnostic used two explicit `CascadeEvent`s separated by 20 ms. Each cascade contained one receptor spike (`unit 0`) and one reservoir spike (`unit 101` at 1 ms; `unit 102` at 21 ms). Receptor exclusion leaves one eligible internal spike per cascade, below the two-spike minimum required by `pattern_from_spikes`. A matched control added a second eligible internal spike to the first cascade. A separate assembly-facing check fed the resulting fallback pattern through `TemporalAssemblyMemory(mature_episodes=3)` across three distinct synthetic episode IDs.

No production source was changed. No repository dataset, trained checkpoint, retained/confirmatory/held-out TEST input, formal raw result, official scorer, consumed identity, or MAIN outcome artifact was used. Exact-head ordinary CI run `35465287304` completed `success` on research head `373f7d52af3b46a23ae976863a2eb5d73d982b5f`; Python 3.11 and 3.13 both passed lint, local readiness, tests, and bundle validation. CI has no evidentiary role.

## Observations

For the two sparse cascades, cascade-local extraction returns no pattern. Because the pattern list is empty, the current implementation falls back to `pattern_from_spikes(...)` over all eligible internal spikes in the step. The fixed diagnostic therefore emits one pattern with `source_cascade_id=None`, `ordered_units=(101, 102)`, `unit_ids=(101, 102)`, `start_ms=1.0`, and `end_ms=21.0`, spanning both explicitly distinct cascade windows.

When the first cascade has two eligible internal spikes, cascade-specific extraction succeeds and the cross-cascade fallback is not invoked. The assembly-facing check shows the bridged fallback is not merely provenance/hash bookkeeping: it is a normal `ActivityPattern` accepted by `TemporalAssemblyMemory`, and repetition across three synthetic episode IDs matures its candidate at `episode_count=3`. The integrated v0.5 path likewise forwards returned patterns above `min_pattern_spikes` into assembly memory.

The immediate mechanism is exactly reduced to current source control flow: `if not patterns`, build one fallback from all eligible step spikes. No new memory mechanism is required. The remaining architecture question is segmentation semantics: sparse responses belonging to multiple explicit cascades can be collapsed into one episode-level assembly-facing representation.

## Falsifier / next research layer

Reduce to `REJECT/ENGINEERING_NOTE_ONLY` if the supported v0.5 contract intentionally defines fallback patterns as episode-global composites permitted to bridge distinct cascades and a fresh DEV-only boundary-preserving comparator shows no meaningful change in assembly candidate formation or downstream prediction/action observables. Also reduce if supported runtime states cannot actually produce multiple distinct cascades with fewer than two eligible reservoir spikes each after receptor exclusion.

Candidate next layer: `ARCHITECTURE_STUDY_ASSEMBLY_SEGMENTATION_SEMANTICS`, only after fresh Evidence Analyst promotion. A fresh prospective study should bind matched DEV-only episodes with equal internal spikes but different segmentation and compare current fallback against a boundary-preserving comparator, with candidate/maturation and one downstream functional observable fixed before execution.

Scientific/semantic choices still open: cascade-local versus episode-global `ActivityPattern` semantics; whether `source_cascade_id=None` is sufficient before memory formation; whether sparse cascades should remain segmented, emit partials, or intentionally form an episode composite; and whether any downstream effect is large enough to justify architecture work rather than documentation only.

## Integrity / completion

- Utility request created: `none`
- consumed identities: `0`
- new FORMAL results: `0`
- formal identity / STARTED / control authority created: `no`
- official TEST accessed: `no`
- formal scoring / evidence / preserve mutation: `no`
- research merge: `no`
- stable main modified: `no`
- blocker: fresh Evidence Analyst classification and a prospective assembly-segmentation Architecture contract before any continuation
- completion target: `ACHIEVED_ONE_BOUNDED_INDEPENDENT_ASSEMBLY_SEGMENTATION_DISCOVERY_CYCLE_AND_RETURNED_ARCHITECTURE_PROMOTION_CANDIDATE`
- completion target achieved: `yes`

Persistence is SUB-stream only. This append-only history file is the durable history snapshot; `reports/orchestrator/sub/latest.md` and `reports/orchestrator/sub/state.json` are updated separately on the same control-plane branch. No MAIN or legacy shared latest/state file is changed.