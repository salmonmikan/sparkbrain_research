# SparkBrain Research Orchestrator SUB — 2026-09-20 00:41 JST

## Mode / allocation

- mode: `discovery`
- Evidence Analyst authority: `862dd62cdce58f06e5c782b4b54212d93e40212e`
- SUB lane consumed: `BOUNDED_SECONDARY_DISCOVERY`
- formal SUB lane: none
- fallback: `NO_OP_WITH_OBSERVABLE_LEVEL_DUPLICATION_OR_LOW_VALUE_REASON` (not used)
- selected target: `FIELD_SOURCE_LINEAGE_CAP_PERMUTATION_SENSITIVITY_DISCOVERY`
- candidate_pool_id: none; one safe bounded SUB self-selection outside the MAIN-reserved candidate pool
- exploration cycle: `1/3`
- evidentiary status: `NON_EVIDENTIARY`
- recommendation: `REJECT`

## Reconciliation / MAIN frontier avoided

Fresh authoritative `main` remains `ebed6abfa941c83b36d2e3bddd04e7c5fb0dbe9d`, and fresh Evidence Analyst authority remains `862dd62cdce58f06e5c782b4b54212d93e40212e`. Analyst allocation keeps MAIN on `TEMPORAL_EXPECTATION_BATCH_PARTITION_ARCHITECTURE_STUDY_CYCLE1` and reserves SUB to independent bounded Discovery. SUB did not continue `CAND-TEMPORAL-BATCH-PARTITION-01`, inspect its outcome-bearing artifacts, modify MAIN's branch, or take any MAIN blocker/successor. The active MAIN research ref remains `research/main-temporal-batch-partition-arch-study-20260920@7fa4391bbf34cf25e10b708ce64acddf07bf7f42`. Its exact-head workflow `35451528895` completed successfully during this SUB run, but SUB did not inspect Architecture result artifacts.

Five authoritative `evidence/*` tags remain present; `formal/*`, `sealed/*`, and tag-based `freeze/*` remain empty; 13 legacy `freeze/*` branches remain preserved. Consumed control/preserve refs and immutable evidence were not touched. Open operational PRs/issues remain outside this Discovery.

## Discovery question / independence

Question: with more than `max_sources_per_unit` distinct simultaneous arrivals to one field unit, can enqueue order alone change retained `source_pulse_ids` and the serialized state hash while leaving physical field dynamics unchanged?

This is independent of MAIN because it directly probes bounded v0.4 field provenance retention on a synthetic one-unit field. Expectations, temporal-batch partitioning, assembly memory, learned Top-k routing, Structural work, H5, H7, official TEST, formal identities, preserved raw evidence, and official scoring are absent.

## Inputs / implementation

Created non-authoritative branch `research/exploratory-sub-field-source-lineage-cap-20260920` from exact `main`. Test implementation commit: `1a91187173a713d87f343b871ec16b94ee956bfb`. Prospective question/reduction binding commit and final research head: `21af9d2a5546cc081f4fe4472540ab8a17760c7c`.

Artifacts:
- `tests/discovery/test_field_source_lineage_cap_20260920.py`
- `analysis/discovery/field_source_lineage_cap_20260920.md`

Fixed synthetic setup: one unit, no recurrent connections, `max_sources_per_unit=16`; twenty distinct arrivals `p00..p19` at exactly `1.0 ms`, each current `0.06`, identical target and arrival multiset. Only enqueue order differs: ascending versus descending pulse ID order. No parameter sweep or rescue tuning was performed.

Exact-head ordinary CI run `35452444346` completed `success` on Python 3.11 and 3.13, including lint, readiness, tests, and bundle validation. CI has no scientific authority.

## Observations / reduction

Both arms emit exactly one spike and have identical physical spike observables and identical unit state after removing `source_pulse_ids`. The retained bounded lineage differs exactly as expected from insertion-order tail truncation:

- ascending enqueue retains `p04..p19`;
- descending enqueue retains `p15..p00`;
- retained-set overlap is 12 of 20 unique IDs, Jaccard `0.6`;
- full `field.state_hash()` differs because `source_pulse_ids` is serialized as part of `UnitState`.

Current implementation explains the result directly. Same-time arrivals are popped in insertion-counter order; `_source_tuple` appends unseen pulse IDs in that order and retains only the last `max_sources_per_unit` entries. Production-code search found no consumer that uses source IDs to alter current, threshold, refractory, adaptation, or another physical update. Source lineage can propagate into descendant spike IDs and hashes, but it is provenance metadata under the current dynamics.

Therefore the observation reduces exactly to bounded provenance-buffer / insertion-order bookkeeping semantics. It is not evidence for a new memory, lineage-persistence, or causal mechanism.

## Handoff / stopping decision

Evidentiary status: `NON_EVIDENTIARY`. Candidate next scientific layer: `NONE_SCIENTIFICALLY`. Recommendation to Evidence Analyst: `REJECT`. Preserve only an engineering/reproducibility note: full field state hashes are sensitive to bounded provenance insertion order even when the physical state projection is identical.

What would falsify this reduction: a fresh current-code path in which pulse/source IDs themselves causally change physical update equations or downstream behavior under otherwise identical currents/timestamps/targets. Current production-code search did not find such a consumer. If semantics change later, that would be a fresh prospective object, not cycle-2 rescue of this candidate.

Scientific choices still open for this candidate: none. Exploration stops at cycle 1; no threshold/cap retuning or alternate ordering sweep is warranted.

Utility request created: none. Consumed identities: none. New FORMAL result: none. Formal identity/STARTED/control/freeze/evidence/official score created: none. Blockers: none.

Completion target: `ACHIEVED_ONE_BOUNDED_INDEPENDENT_DISCOVERY_CYCLE_AND_STOPPED_AFTER_EXACT_BOOKKEEPING_REDUCTION`.
