# SparkBrain Research Orchestrator SUB — 2026-09-20 04:49 JST — final reconciled snapshot

## Final authority reconciliation

A newer Evidence Analyst handoff appeared after the initial SUB persistence snapshot. Fresh final authority is `493573eb3b8c38d88f251db7c04dfe1a586c6ff0`; stable `main` remains `ebed6abfa941c83b36d2e3bddd04e7c5fb0dbe9d`. The newer handoff does **not** change SUB's allocation or invalidate this run: `main_lane` remains `V05_UNIT_SUPPRESSION_TRANSIENT_CONTRACT_ARCHITECTURE_STUDY_CYCLE1`, `sub_lane` remains `BOUNDED_SECONDARY_DISCOVERY`, and `sub_fallback` remains `NO_OP_WITH_OBSERVABLE_LEVEL_DUPLICATION_OR_LOW_VALUE_REASON`. It explicitly permits one independent bounded SUB Discovery while MAIN owns the suppression-semantics critical path.

The newer handoff also confirms `DISCOVERY=OPEN` for SUB, `ARCHITECTURE_STUDY=ACTIVE_AUTHORIZED_PENDING_MAIN_PROSPECTIVE_BINDING` for `CAND-V05-UNIT-SUPPRESSION-TRANSIENT-SEMANTICS-01`, and PRE_FORMAL/FORMAL remain HOLD. `blocked_until` and `do_not_touch` remain materially unchanged for this run: no dynamic MAIN continuation beyond the static suppression-contract cycle, no PRE_FORMAL/FORMAL entry without fresh authority, and no consumed identities, completed topology-config/Temporal/Top-k/H5/NI01/C19/PD01 artifacts/outcomes, official TEST/formal scorer surfaces, immutable evidence/control/preserve refs, scheduler definitions, or PR #148/#149 governance work may be touched.

## Final SUB result

- mode: `discovery`
- selected target: `ASSEMBLY_CASCADE_FALLBACK_BRIDGING_DISCOVERY_CYCLE1`
- candidate_pool_id: `NONE_SELF_SELECTED`
- exploration_cycle: `1/3`, stopped after one bounded cycle
- branch: `research/exploratory-sub-assembly-cascade-fallback-bridging-20260920`
- prospective binding: `3af7c2388e4af35dd53984d3c48cb08513bed320`
- diagnostic commit: `828a8090c748be1f2ea61e037964f26b2f3bc4c6`
- exact research head: `373f7d52af3b46a23ae976863a2eb5d73d982b5f`
- exact-head CI: `35465287304`, `completed / success`, Python 3.11 and 3.13 both fully successful
- evidentiary_status: `NON_EVIDENTIARY`
- recommendation: `PROMOTE_TO_ARCHITECTURE_STUDY`
- candidate next layer: `ARCHITECTURE_STUDY_ASSEMBLY_SEGMENTATION_SEMANTICS`

The bounded synthetic probe shows that two explicitly distinct cascades, each left with only one eligible reservoir spike after receptor exclusion, yield no cascade-local pattern; the current fallback then constructs one `ActivityPattern` over all eligible step spikes. The fixed case produces `source_cascade_id=None`, `ordered_units=(101,102)`, and a 1–21 ms span crossing the two cascade windows. A matched control with two internal spikes in the first cascade produces a cascade-local pattern and does not invoke the cross-cascade fallback. The bridged fallback is assembly-facing: repeated across three synthetic episode IDs it matures a `TemporalAssemblyMemory` candidate.

The immediate effect is fully reduced to the current extractor control flow (`if not patterns` -> fallback over all eligible step spikes), so it is not a new memory mechanism or scientific evidence. The open architecture question is whether sparse multi-cascade responses should be allowed to collapse into one episode-level representation before memory formation. Reduce to engineering/documentation only if episode-global fallback composites are the supported contract and a fresh boundary-preserving DEV comparator shows no meaningful candidate or downstream functional difference, or if the sparse multi-cascade state is unreachable on supported runtime paths.

## MAIN frontier / integrity

MAIN's suppression-semantics object, blockers, outcome-dependent successors, and prior suppression diagnostic were not touched. v0.5 config binding, Temporal, Top-k, H7, consumed identities, FORMAL/TEST/scoring/preserve/evidence surfaces were not continued or accessed. No production source, stable main, official score, STARTED/control authority, formal/freeze/evidence ref, immutable evidence, or scheduler definition was modified. No research merge occurred.

Utility request: `none`. Consumed identities: `0`. New FORMAL results: `0`. Blocker: fresh Evidence Analyst classification and prospective assembly-segmentation Architecture contract before any continuation. Completion target `ACHIEVED_ONE_BOUNDED_INDEPENDENT_ASSEMBLY_SEGMENTATION_DISCOVERY_CYCLE_AND_RETURNED_ARCHITECTURE_PROMOTION_CANDIDATE` is achieved.

This file supersedes the same-run `0447-sub.md` only as the final point-in-time authority reconciliation; the earlier history file remains append-only and unmodified.