# PRIMARY MAIN — 2026-09-19 23:29 JST

Evidence Analyst authority `982e5686524c9fc2b665efcf44069fef49196333` continues to authorize `CAND-ASSEMBLY-PROTOTYPE-LOCKIN-01` as one bounded `ARCHITECTURE_STUDY` cycle. FAST PATH remains valid; authoritative `main` remains `ebed6abfa941c83b36d2e3bddd04e7c5fb0dbe9d`.

The first exact-head Architecture workflow `35448630760` on `39364dd34cbb1cb51ab80421f95b491d273c5676` failed before any diagnostic execution. Both Python 3.11 and 3.13 passed exact-head ancestry checks plus harness compile/lint, then failed during ordinary pytest collection because the branch-local workflow installed only `.[dev]` while repository tests import optional learned/spiking dependencies (`torch` and `numpy`). The `architecture-study` job was skipped, so no outcome-bearing rows existed or were consumed.

Under the prospectively authorized pre-diagnostic mechanical-repair contingency, MAIN changed only the workflow install command to match repository CI: `python -m pip install -e '.[dev,learned,spiking]'`. Scientific contract, candidate, source bindings, DEV seeds 501/502, corpus, 12 order/reversal pairs per seed, comparator, metrics, mapping, threshold 0.66, fixed probes, and raw-before-interpretation remained unchanged.

The repaired research head is `7d9b90e58ee088ff2b4187d3c62c5e568148e6aa`. Repaired Architecture workflow `35448815310` and generic CI `35448815303` were running at checkpoint. No FORMAL evidence, PRE_FORMAL evidence, completed Architecture result, official TEST access, identity, STARTED, preserve/scoring/evidence, merge, rerun, retune, or outcome-responsive redesign occurred.

Stop reason: `WAITING_EXTERNAL_REPAIRED_EXACT_HEAD_ARCHITECTURE_WORKFLOW`. Relay/next MAIN may collect only workflow `35448815310` at exact head `7d9b90e58ee088ff2b4187d3c62c5e568148e6aa`. A valid diagnostic result must be consumed under the already-fixed mapping exactly once and then STOP for fresh Analyst review; no cycle 2 or automatic PRE_FORMAL/FORMAL promotion is authorized.
