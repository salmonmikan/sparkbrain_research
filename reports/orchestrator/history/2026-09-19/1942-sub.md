# SparkBrain Research Orchestrator SUB — 2026-09-19 19:42 JST

## Mode / allocation

- mode: `discovery`
- Evidence Analyst authority: `5b63d21f44812ef8d4938c9ad968bb8ab481b3c5`
- formal SUB lane: none
- fallback: `NO_OP_WITH_OBSERVABLE_LEVEL_DUPLICATION_OR_LOW_VALUE_REASON`
- selected target: `PLASTICITY_REWARD_OMISSION_BASELINE_DISCOVERY`
- candidate_pool_id: none; bounded safe SUB self-selection
- exploration cycle: `1/3`, stopped early after exact implementation reduction
- evidentiary status: `NON_EVIDENTIARY`
- recommendation: `REJECT`

## MAIN frontier avoided

MAIN completed `CAND-TOPK-PA-01` cycle-2 replication and is stopped for fresh Analyst review. SUB did not touch the MAIN research branch, replicated signal, artifact, PRE_FORMAL eligibility question, comparator/redesign space, or any outcome-dependent successor. The queued `CAND-STRUCTURAL-ORDER-PATH-01` Architecture Study was also left untouched.

No consumed identity, STARTED/control authority, formal/freeze/evidence ref, official score, preserved formal raw, sealed/one-way TEST input, repository dataset, or trained checkpoint was used.

## Discovery question

Current v0.5 plasticity initializes `reward_trace=1.0` and after every `apply()` relaxes it toward `+1.0`. The bounded question was whether reward omission is actually a neutral condition, and whether recovery from a one-shot negative reward under repeated causal spike pairs requires any mechanism beyond this explicit recurrence.

## Implementation

Created non-authoritative branch `research/exploratory-sub-plasticity-reward-omission-20260919` at `bdcd068db2f7d645e323d6fc7e4e2172bcc6d1cb`.

Artifacts:
- `analysis/discovery/plasticity_reward_omission_baseline_20260919.py`
- `analysis/discovery/plasticity_reward_omission_baseline_20260919.json`
- `analysis/discovery/plasticity_reward_omission_baseline_20260919.md`

The synthetic single-edge harness mirrors the causal branch of `V05PlasticityController.apply` using the default learning rate `0.001`, `tau_plus_ms=18`, eligibility decay `0.90`, reward baseline `+1.0`, reward relaxation factor `0.85`, 40 repeated causal pairs, and lags `2/5/10/20 ms`. Conditions were omission, explicit zero reward each step, one-shot negative reward, repeated negative reward, and one-shot positive reward.

## Observations

At `5 ms` lag after 40 updates:
- omission -> weight `+0.2358218301`;
- explicit `reward(0.0)` every step -> `0.0` exactly;
- one-shot `reward(-1.0)` then omission -> `+0.1929955224`;
- repeated `reward(-1.0)` -> `-0.2358218301`;
- one-shot `reward(2.0)` then omission -> `+0.2572349840`.

Across all tested lags, the one-shot negative condition reached its minimum at step `5`, the reward trace used for learning became positive at step `6`, and weight crossed back to nonnegative at step `9`. Lag changed magnitude only through the causal STDP exponential.

The effect is fully reduced by the explicit implementation semantics: omission leaves the trace at `+1.0`, while a one-shot negative trace deterministically relaxes back toward `+1.0`; positive causal eligibility then resumes potentiation. No additional adaptive or memory mechanism is required.

## Handoff / stopping decision

`REJECT` as a scientific candidate. This is useful as an engineering/protocol constraint: in current v0.5, omitted reward is positive baseline, not neutral. Any future study intending neutral reward omission must prospectively define and implement that semantics rather than infer it from absence of `reward()` calls. A changed reward-baseline controller would be a distinct future object, not cycle-2 rescue tuning.

Candidate next research layer: none scientifically.

Utility request: none. The bounded target was self-contained and no independent helper task would materially increase information gain.

Consumed identities: none. New formal results: zero. Blockers: none.

## CI / completion

Exact-head ordinary repository CI run `35437912586` completed `success` on head `bdcd068db2f7d645e323d6fc7e4e2172bcc6d1cb`. It is ordinary repository CI and not scientific execution.

Completion target achieved: `ACHIEVED_ONE_BOUNDED_INDEPENDENT_DISCOVERY_CYCLE_AND_STOPPED_AFTER_EXACT_REDUCTION`.
