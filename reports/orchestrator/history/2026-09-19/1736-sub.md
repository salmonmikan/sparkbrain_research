# SparkBrain Research Orchestrator SUB — 2026-09-19 17:36 JST

Mode: `discovery`

Fresh Evidence Analyst authority is `3e83f1eb683ce80e326b756fa304e96688fdd3fe`. Its current allocation is `main_lane: TOPK_ROUTER_PERSISTENT_AMPLIFICATION_ARCHITECTURE_STUDY_CYCLE1`, `sub_lane: BOUNDED_SECONDARY_DISCOVERY`, `sub_fallback: null`. The SUB lane is an Analyst-authorized Discovery funnel with no fixed target. The named candidate pool is not directly usable by SUB: `CAND-TOPK-PA-01` is MAIN-owned, `CAND-H5-AUDIT-01` is Independent-Audit-owned, and `CAND-H7-RESP-01` remains a future PRE_FORMAL hold. I therefore selected one new bounded observable-level Discovery target under the explicit SUB self-selection authority.

Selected target: **`STRUCTURAL_ORDER_PATH_DEPENDENCE_DISCOVERY`**, candidate_pool_id `null`, exploration cycle **1/3**. MAIN frontier was avoided completely. During this run MAIN finished `CAND-TOPK-PA-01` with a NON_EVIDENTIARY `PERSISTENCE_COUPLED_DELAYED_AMPLIFICATION_SIGNAL` and stopped for Analyst review. SUB did not touch that branch, signal, harness, follow-up, blocker, or outcome-dependent successor.

The question is whether the existing structural-plasticity controller is path-dependent: for exactly the same multiset of synthetic unlabeled routing-load and edge-credit frames, does changing observation order lead to different final module/edge topology, and is any difference explained only by exhausting the production-wide total structural-event budget?

Implementation is on non-authoritative branch `research/exploratory-sub-structural-order-path-dependence-20260919`, exact commit `05aa366c661bba43fa0e3d5cb1957539bcedd66b`. The branch contains a deterministic standalone synthetic probe plus aggregate result/handoff artifacts. It mirrors current `main@ebed6abfa941c83b36d2e3bddd04e7c5fb0dbe9d` structural-controller thresholds, event priority/caps, source-module mask, duplicate/prune, and edge grow/prune semantics. It does **not** read repository datasets, trained checkpoints, C08 official inputs/results, held-out TEST, preserved formal raw, scorers, frozen identities, or any consumed evidence.

Cycle 1 uses five deterministic seeds, each with one fixed 12-frame multiset, and evaluates 120 permutations of the same multiset under `full`, `module_only`, and `edge_only` event families. Each family is run at total event budget 16 and at a generous reduction-control budget 64. The script samples deterministic final-graph pairs and measures module-set and edge-set Jaccard distances.

Observation: order sensitivity is concentrated in stateful module operations, not edge-only updates. With budget 16, `full` produced a mean `116.4/120` unique final graphs with mean module/edge Jaccard distances `0.1877/0.3299`; `module_only` produced `95.6/120` with `0.2066/0.2939`; `edge_only` converged to exactly one final graph in every seed (`0/0` distance). Raising the budget to 64 does not remove full/module path dependence: `full` remains `108.2/120` unique with module/edge distance `0.2301/0.3003` while ~`40.18` events of budget remain unused; `module_only` remains `70.8/120` unique with `0.2818/0.2818` and ~`41.38` budget unused. Edge-only again converges to one graph. In this synthetic harness, the effect is therefore not reduced to total-budget exhaustion or edge-event order; stateful duplicate/prune and active-slot history are the candidate source.

This remains strictly **`NON_EVIDENTIARY`**. It does not establish functional value, harm, emergent specialization, novelty, or behavior on real DEV data. No formal identity, STARTED/control authority, official score, preserve/evidence ref, formal workflow, or consumed identity was created or used. No Utility request was created because the selected bounded work was fully owned by SUB and did not need a separate helper.

Reduction/falsifier for a next layer: a prospectively fixed fresh DEV-only matched-multiset order study should reduce/reject this candidate if topology/output divergence is negligible, if divergence disappears under an identity-neutral/resource-matched module-event comparator, or if topology differs but functional/output behavior is effectively invariant. The open scientific choices are the fresh DEV-only task/regime surface, exact permutation family, identity-neutral graph matching, output/horizon metric, event-count/resource comparator, and whether homeostatic router-bias updates are an explicit factor.

Recommendation to Evidence Analyst: **`PROMOTE_TO_ARCHITECTURE_STUDY`**. Carry forward only the system-level structural path-dependence question. Do not relabel the synthetic observation as evidence. Any promoted study must be freshly specified prospectively.

Consumed identities by SUB: none. Blocker: none for cycle 1; any architecture study or cycle 2 requires fresh Analyst partition/specification. Completion target is achieved: one independent bounded Discovery question was implemented, run, reduced against an explicit budget/edge-only control, and handed off without touching MAIN or one-way surfaces.

Research-branch CI `35432790695` for `05aa366c...` was `in_progress` when observed; this is ordinary repository CI and not a scientific/formal workflow.
