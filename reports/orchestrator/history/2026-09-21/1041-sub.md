# SparkBrain SUB history — 2026-09-21 10:41 JST

- schema_version: `2`
- generation_id: `SUB-20260921T104150+0900-SYSTEM-OUTREPLAYINVALID-7C2A91E4`
- authority: `EVA-20260921T095900+0900-R32-6D2A91C4@6bf35ff0f08a981feb09abced00e157526a47cb0`
- operating_mode: `discovery`
- discovery_mode: `SYSTEM_DISCOVERY`
- target/candidate: `V05_OUTCOME_REPLAY_CREDIT_SEMANTICS_DISCOVERY_CYCLE1 / CAND-V05-OUTCOME-REPLAY-CREDIT-SEMANTICS-01`
- cycle: `1/3`
- MAIN independence: `INDEPENDENT`; MAIN-owned Assembly distributional-control object and its blockers/successor were not touched.
- rolling theory-backward accounting: prior `SYSTEM/SYSTEM/MECHANISM`; current `SYSTEM`; resulting last-three `SYSTEM/MECHANISM/SYSTEM = 1/3`; `theory_backward_exception=null`.

## Prospective contract

Question: does `learn_outcome()` consume a pending mature Assembly/action association once, or can the same outcome be replayed without a new episode and apply extra credit?

Hypothesis: replay sensitivity may follow from persistent `pending_activation` and action-policy `pending` state.

Reduction question: can any replay effect be reduced exactly to reuse of the same pending state plus ordinary predictor-count and action-score increments?

Falsifier: the second identical outcome call is rejected/no-op/state-preserving relative to a single-call arm.

Contract commit: `de62d218cae1710f4f301023f2db942b99943765`.

## Fixed inputs and implementation

The contract fixed default v0.5, 24 DEV training episodes from seed 501, a deep-copy single/replay comparator, then `training_episodes(seed=501,count=1)[0]` as the common support probe with learning/exploration disabled. No alternate seed/time/episode/reward/threshold/support search was permitted.

Branch: `research/exploratory-sub-v05-outcome-replay-credit-semantics-20260921`. Result/final head: `6fa2114ff5f68bb940abd21600a587b460293097`.

## Observation and stop

Static runtime validation before replay outcome exposure found the fixed support probe invalid. Twenty-four training episodes advance the brain clock, but the fixed count-one support episode uses default `start_ms=0.0`; `process_episode()` rejects pulses that predate `current_time_ms`. The prospectively mapped terminal is therefore `INVALID_COMPARATOR_OR_RUNTIME`.

No `learn_outcome()` replay call was executed and no start-time correction was made. Doing so would rescue/redesign the same object after its terminal condition became known.

## Handoff

- evidentiary status: `NON_EVIDENTIARY`
- proposed claim_ceiling: `SYSTEM`
- proposed preformal_eligible: `false`
- preliminary readiness: `N/A_FOR_SYSTEM_OBJECT`
- proposed hold dimensions: `hold_class=null / hold_reason=null / terminal_state=TERMINAL_FOR_CURRENT_OBJECT / queue_state=NOT_QUEUED`
- next layer: `NONE`
- recommendation: `REJECT_CURRENT_OBJECT_CONTRACT_INVALID`
- open choice: only a fresh candidate with a fresh prospective, monotonic-time-valid comparator may revisit replay semantics.

No FORMAL/PRE_FORMAL action, official TEST/scorer, held-out tuning, identity consumption, stable-main or immutable evidence/control/preserve mutation, or Utility request occurred. Exact-final-head integrity CI `35551537415` was still in progress at handoff construction.
