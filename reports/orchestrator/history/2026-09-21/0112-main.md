# SparkBrain MAIN history — 2026-09-21 01:12 JST

- schema_version: `2`
- generation_id: `MAIN-20260921T011233+0900-PRIMARY-FUNNEL21-SYSTEM-ELIGTIME-R23-4A7C91E2`
- analyst generation: `EVA-20260921T005854+0900-R23-9C4E71A2`
- analyst commit: `9efe48eea7e6655e7eae4b3f0afb3b0c0ed781be`
- authoritative main: `ebed6abfa941c83b36d2e3bddd04e7c5fb0dbe9d`
- layer: `ARCHITECTURE_STUDY`
- candidate: `CAND-V05-ELIGIBILITY-TIMEBASE-CONTRACT-01`
- claim ceiling: `SYSTEM`
- preformal eligible: `false`
- cycle: `1`

## Authority and collision reconciliation

R23 consumed and canonically closed the prior SUB state-hash Discovery result, resolving the R22 freshness blocker. The previous MAIN relay lease was BLOCKED with no active research branch or identity. SUB remained on its independent Discovery object, with no collision with the R23 MAIN target. R23 reported zero comparably executable/informative MECHANISM candidates, so `system_priority_exception.used=false` remained valid.

MAIN acquired the lease before inspection and bound the cycle to exact stable `main@ebed6abfa941c83b36d2e3bddd04e7c5fb0dbe9d`. The controlling Analyst branch remained `EVA-20260921T005854+0900-R23-9C4E71A2@9efe48eea7e6655e7eae4b3f0afb3b0c0ed781be` at each mutation freshness check.

## Prospective scope

Only static/read-only source, documentation, tests, and callsite inspection was authorized. The fixed terminal tree was:

- documented episode/apply-count clock matching implementation -> `EXPLICIT_DISCRETE_TIME_SEMANTICS`;
- documented elapsed/model-time or partition-invariance contract with call-count implementation -> `TIMEBASE_CONTRACT_IMPLEMENTATION_MISMATCH`;
- no authoritative timebase contract -> `TIMEBASE_CONTRACT_AMBIGUOUS`.

No dynamic partition diagnostic was authorized in the same run.

## Static findings

Exact stable-main `src/sparkbrain/v05/plasticity.py` defines `eligibility_decay=0.90`. `V05PlasticityController.apply(field, spikes)` has no elapsed-time argument. At the start of every call it multiplies every stored eligibility value by `eligibility_decay`; spike timestamps are used for STDP lag calculations but not to scale eligibility decay by elapsed/model time.

Exact stable-main `src/sparkbrain/v05/brain.py` calls `self.plasticity.apply(self.base.field, base_result.spikes)` once inside `IntegratedV05Brain.process_episode()` when `learn_field=true`. When `learn_field=false`, the call is skipped. Consequently the implemented decay clock is de facto apply-call count / learning-enabled episode count, not elapsed model time.

The v0.5 Theory Specification says timing-dependent weight/delay updates are bounded and reward may modulate eligibility, but gives no eligibility decay timebase. The v0.5 Master Plan lists an eligibility trace and a per-step update budget, but does not state that `eligibility_decay` is per episode/apply call or per elapsed/model-time interval. The retained v0.5 experiment protocol defines episode budgets and held-out conditions but no decay clock or partition-invariance contract. The v0.5 plasticity unit tests exercise weight-only/delay-only behavior but do not define decay-clock semantics. Stable-main callsite/code search found no separate v0.5 elapsed-time decay path.

Older v0.2 theory material mentions episodes as an eligibility/reward-learning timescale. That is historical context only and does not constitute an explicit retained v0.5 API/semantic contract.

## Prospectively mapped result

The static result therefore maps to `TIMEBASE_CONTRACT_AMBIGUOUS` under the R23 fixed tree.

This is a NON_EVIDENTIARY SYSTEM Architecture observation. MAIN does not reinterpret it as MECHANISM evidence, does not upgrade it to PRE_FORMAL, and does not use it to authorize a dynamic test.

The controlling Analyst assigned `hold_class=null`, `hold_reason=null`, `terminal_state=ACTIVE`, `queue_state=ACTIVE`, and `preformal_readiness=null`. MAIN preserves those Funnel dimensions exactly pending fresh Analyst canonicalization; the mapped terminal observation is recorded separately rather than inventing post-result HOLD dimensions.

## Integrity and refs

- stable main: `ebed6abfa941c83b36d2e3bddd04e7c5fb0dbe9d`
- authoritative evidence tags: `5`
- tag-based formal refs: `0`
- tag-based sealed refs: `0`
- tag-based freeze refs: `0`
- H5 STARTED control ref: `058e90227cd48e1c10c6ecbaed01efdec1217d0e`
- H5 raw preserve ref: `ce5797eb584344db7a512e585506fb6c59ea475b`
- stable-main CI run: `35095163387`, success
- PR #148: open, unmerged, mergeable
- PR #149: open, unmerged, mergeable

No research/scientific branch was created or modified. No workflow was dispatched. No STARTED, preserve, scoring, evidence-tag, formal/sealed/freeze, stable-main, or consumed-identity mutation occurred.

## Evidentiary accounting

- FORMAL scientific evidence: `0`
- PRE_FORMAL development evidence: `0`
- MECHANISM Architecture observations: `0`
- SYSTEM Architecture observations: `1` — `TIMEBASE_CONTRACT_AMBIGUOUS`, NON_EVIDENTIARY
- new consumed identities: `0`

## Stop / next action

Stop reason: `R23_STATIC_SYSTEM_TERMINAL_TIMEBASE_CONTRACT_AMBIGUOUS_STOP_FRESH_ANALYST_REVIEW`.

The current MAIN cycle is complete. Fresh Evidence Analyst review is required to canonicalize the result and Funnel dimensions. Any dynamic partition-invariance or elapsed-time diagnostic must be a fresh successor candidate with a fresh prospective contract; same-object continuation is not authorized.
