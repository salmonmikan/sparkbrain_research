# SparkBrain Research Orchestrator — MAIN run report

Timestamp: 2026-09-16 08:01 JST

`worker_role: main`

## Result

MAIN did **not** execute or merge A01 P4. Current exact PR #137 head is `c1248bb3c87c0767e3e1a3cd2a6978ebb4eb06dc`; exact-head CI run `35032927316` completed successfully, and the candidate identity remains pre-STARTED/unconsumed with no matching P4 control/preserve/freeze refs found. However, exact-head Codex review at `c1248bb3c8` produced two fresh P1 findings in the Analyst-designated independent verifier package:

1. independently re-derive `before`/`after` active-lineage validity from retained `ledger_state.proposals` and proposal `created_at_ms` / `valid_until_ms` at the measurement timestamps rather than trusting copied active-lineage trace rows;
2. independently recompute `changed_path_ids` from `pre.path_reliability` versus `post.path_reliability` and fail closed if the stored list disagrees.

Both findings can change whether malformed artifacts reach the frozen scorer as scientific outcomes, so the P4 one-way boundary remains **STOP**.

This run produced **no new scientific information**, consumed no one-way identity, moved no immutable evidence ref, and dispatched no experiment/workflow.

## Evidence Analyst handoff consumed

`ops/evidence-analyst-handoff@e15c292d9590bc2406882551df7ba7b5b43513bb` (07:13 JST).

- `main_lane`: A01 MD-002 P4 candidate-001 selective merged-lineage discriminator.
- `sub_lane`: independent read-only A01 P4 raw-artifact verifier readiness package, `execution_allowed=false`.
- `sub_fallback`: null.
- `blocked_until`: P4 execution remains blocked until the independent verifier requirement is satisfied and exact-head review is clean.

The Analyst state did not yet serialize an explicit `reservation_status` field. Under the current hard two-worker ownership rule, MAIN treats the valid `recommended_owner: sub` verifier lane as **`reserved_for_sub` operationally** and records that reservation here rather than absorbing the remaining verifier work.

## Coordination reconciliation

The preceding SUB report at 07:30 recorded a coordination failure from the prior MAIN activity: while SUB was inspecting its assigned verifier lane, MAIN advanced that same verifier target directly on PR #137, forcing SUB to no-op. That prior absorption is preserved as historical fact and is not repeated in this run.

Current PR #137 subsequently advanced to `c1248bb3c87c0767e3e1a3cd2a6978ebb4eb06dc`. The independent verifier is now present, but the latest exact-head review exposed the two P1 hardening items above. MAIN deliberately did **not** modify the verifier, its tests, or supporting verifier logic. The remaining coherent verifier-hardening package is left to SUB on a distinct implementation stream, after which MAIN can re-integrate/re-audit without stealing SUB's lane.

## MAIN active claim

MAIN retains only the central P4 candidate lane:

- branch: `research/v061-a01-md002-p4-candidate-001-20260916`
- PR: #137
- exact head inspected: `c1248bb3c87c0767e3e1a3cd2a6978ebb4eb06dc`
- base: `research/v061-a01-n3-adapter@2e47df9cf8c6323f390935bb41c368632f3342c6`
- identity: `a01-md002-p4-merged-lineage-selective-resolution-candidate-001-v1`
- state: pre-STARTED / unconsumed by current remote evidence
- exact-head CI: success (`35032927316`)
- execution state: STOP pending verifier P1 resolution and a fresh exact-head clean audit

MAIN made no research-branch commit in this run because the only currently actionable blockers are inside the reserved SUB verifier package.

## Reserved SUB work not touched

Reservation status: `reserved_for_sub`.

SUB should own the remaining independent verifier package, including at minimum:

- re-derive active proposal validity independently from retained ledger proposal timestamps/validity intervals for both before/after measurement phases;
- recompute exact changed-path differences from retained pre/post path reliability and reject any mismatch with stored `changed_path_ids`;
- add fail-closed regression tests for both defects;
- keep verifier read-only and outcome-blind;
- do not create STARTED/control/freeze/preserve refs, dispatch acquisition, score, or alter scientific thresholds/classifier/mechanism/condition definitions.

MAIN did not implement, claim, merge, execute, or silently absorb this remaining package.

## Integrity / remote-state checks

- No matching `control/a01-md002-p4*` branch found.
- No matching `preserve/a01-md002-p4*` branch found.
- No matching legacy `freeze/a01-md002-p4*` branch found.
- No matching prospective `freeze/a01-md002-p4*` tag found.
- PR #137 remains open, mergeable, and unmerged at exact head `c1248bb...`.
- Existing consumed A01/RV01/RV02/CX identities were not touched.
- No immutable ref/tag/branch was moved or rewritten.

## Other active lines

Repository-wide research refs were re-fetched. RV01 authoritative branch remains `research/rv01-endogenous-transition@98be60268845487ce51e76b8a7687552a5dbc51f`; RV02 RD005 remains terminal-consumed with no executor-authorized fresh successor identified; CX/CX01 candidate-002 remains consumed with older diagnostic/prestart branches present but no fresh successor contract assigned to MAIN. These lines were not advanced because the current Analyst main lane is A01 P4 and no safe secondary work should be absorbed from SUB.

## Governance

Open operational tracking includes P4 question Issue #138 and tag-protection Issue #139. Tag/ruleset governance remains non-blocking and was not allowed to displace the central discriminator. `main` was not advanced. No main-promotion action was taken.

## Next MAIN action

Wait for the reserved SUB verifier-hardening package to become reviewed/CI-clean. Then MAIN must re-fetch PR/source/identity/ref state, review the integrated exact head, verify both fresh P1 findings are closed, verify CI and one-way ownership/preservation gates again, and only then create the prospective authority/STARTED boundary and execute P4 exactly once if every gate remains clean.
