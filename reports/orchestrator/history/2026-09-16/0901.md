# SparkBrain Research Orchestrator — MAIN run report

Timestamp: 2026-09-16 09:01 JST

`worker_role: main`

## Result

No new scientific information was produced in this run. MAIN re-fetched the current repository/PR/CI/ref state and intentionally did **not** modify A01 P4 because the only actionable blockers remain inside the verifier package reserved for SUB.

The central P4 branch is still `research/v061-a01-md002-p4-candidate-001-20260916@c1248bb3c87c0767e3e1a3cd2a6978ebb4eb06dc`. PR #137 remains open and unmerged. Exact-head CI run `35032927316` remains completed successfully. The P4 identity `a01-md002-p4-merged-lineage-selective-resolution-candidate-001-v1` remains pre-STARTED/unconsumed: fresh ref checks found no matching P4 `control/*`, `preserve/*`, legacy `freeze/*` branch, or prospective `freeze/*` tag.

No newer Evidence Analyst handoff superseded `ops/evidence-analyst-handoff@e15c292d9590bc2406882551df7ba7b5b43513bb`. The Analyst still assigns MAIN the P4 discriminator and SUB the independent read-only raw-artifact verifier readiness package with `execution_allowed=false`.

## Hard two-worker ownership reconciliation

The SUB verifier package remains operationally `reserved_for_sub`. MAIN did not implement, claim, merge, execute, or silently absorb it.

The remaining verifier hardening package is still coherent and independent:

1. independently re-derive before/after active proposal validity from retained ledger proposal timestamps/validity intervals rather than trusting copied active-lineage rows;
2. independently recompute changed-path differences from retained `pre.path_reliability` and `post.path_reliability`, rejecting disagreement with stored `changed_path_ids`;
3. keep the already-reviewed independent cardinality/measurement/condition-spec checks fail-closed;
4. add/retain regression coverage without acquiring or scoring candidate output.

No distinct new P4 verifier branch or PR was visible at this inspection, and the shared orchestrator history contained no newer SUB durable record after the 08:01 MAIN report. Absence of a SUB claim was **not** treated as permission for MAIN to take the reserved package.

Reservation respected: **yes**.

## MAIN active claim

- target: A01 MD-002 P4 candidate-001 selective merged-lineage discriminator
- branch: `research/v061-a01-md002-p4-candidate-001-20260916`
- PR: #137
- exact head: `c1248bb3c87c0767e3e1a3cd2a6978ebb4eb06dc`
- base: `research/v061-a01-n3-adapter@2e47df9cf8c6323f390935bb41c368632f3342c6`
- identity: `a01-md002-p4-merged-lineage-selective-resolution-candidate-001-v1`
- exact-head CI: success, run `35032927316`
- execution state: `STOP_PENDING_RESERVED_SUB_VERIFIER_P1_FIXES`

MAIN made no research-branch commit, no merge, no workflow dispatch, no STARTED/control/preserve/freeze creation, and consumed no one-way identity.

## Scientific / integrity state

P4 remains the central HIGH/NEAR discriminator. Its scientific mechanism/scorer contract was not changed. The current STOP is integrity/readiness, not a new scientific negative result.

Existing consumed A01/RV01/RV02/CX identities remain no-rerun/no-retune and were not touched. No immutable evidence ref/tag/branch was moved or rewritten. `main` remains `ba16bf10535141c2edb29bbe3439ba0a38e71179` and was not advanced.

## Concurrency

The prior coordination anti-pattern remains explicitly avoided: MAIN did not consume the verifier lane merely because SUB had not yet published a new claim. No concurrent overwrite was attempted.

## Next MAIN action

Wait for a reviewed/CI-clean SUB verifier hardening package. Then re-fetch exact PR/source/identity/ref state, integrate only that completed package, re-audit the exact resulting head, verify all verifier P1 findings are closed, re-run exact-head CI/review and all one-way GO/STOP gates, and only then create STARTED and execute P4 exactly once if every prospective condition remains clean.
