# SparkBrain Research Orchestrator MAIN — R49 PRIMARY static-contract cycle 1

- schema_version: `2`
- generation_id: `MAIN-20260922T012600+0900-PRIMARY-FUNNEL21-SYSTEM-LOCALIZE-R49-CYCLE1-7E3C21A5`
- execution_mode: `PRIMARY`
- status: `COMPLETED`
- evidentiary_status: `NON_EVIDENTIARY_SYSTEM_ARCHITECTURE_OBSERVATION`

## Authority / lifecycle

Fresh controlling Evidence Analyst is `EVA-20260922T011108+0900-R49-7E3C21A5@7c154b6203347a4f212746e2cfe4ee7f1be2e5a4`. MAIN executed only the authorized static/read-only Architecture contract cycle 1 for `CAND-RESOURCE-SEMANTIC-ACTIVE-WORK-LOCALIZATION-01` and stopped before synthetic measurement.

Canonical Funnel-v2.1 fields remain exactly Analyst-owned: `claim_ceiling=SYSTEM`, `preformal_eligible=false`, `preformal_readiness=null` (canonical index: `NOT_APPLICABLE` for SYSTEM), `hold_class=null`, `hold_reason=null`, `terminal_state=ACTIVE`, `queue_state=ACTIVE`; `system_priority_exception.used=false` because viable executable MECHANISM remains zero. No same-object SYSTEM→MECHANISM uplift occurred.

## Static contract bound prospectively

Contract `SB-R49-C32-CREDIT-TRACE-CROSSOVER-V1` is now fixed for fresh future work only. It uses stable-main source semantics and does not touch H5. The behavioral anchor fixes a sensory `credit_source` -> hypothesis `credit_hypothesis` plastic edge at weight `0.4` with `eligibility_decay=0.9`, `learning_rate=0.2`, threshold `0.5`, ignition threshold `0.6`, reward `+1.0`, and a clean post-learning reset/probe. Stable-main source order makes eligibility decay once before every dequeued event; the source-derived anchor arithmetic is eligibility `0.81` at reward and weight `0.562` after reward. These are preregistered algebraic expectations, not measured synthetic results.

The future comparator set is fixed before outcomes: current global scan reference; dense/vectorized logical scan; active-set timestamp-lazy exact reconstruction; exact history archive; source-indexed sparse event-driven; e-prop-style event ledger constrained to the current update equations; exact event-triggered three-factor local maintenance; and SparseProp-style exact sparse/event-driven maintenance. Approximate pruning and changed learning rules are forbidden.

Semantic equality is also fixed: same graph/config/seed/event and reward schedule, same information privilege, exact discrete event/fire/ignition/prediction trajectories, and eligibility/weight/decision-state equality at `abs_tol=rel_tol=1e-12`. A mismatch invalidates the comparator rather than triggering tolerance or protocol rescue.

The resource vector is multi-dimensional and cannot be collapsed post hoc: semantic primitive-operation count; peak logical history/timestamp/active-index slots; history-entry manipulation count; and delay-diversity overhead. Event-heap/Spark/coalition/trace bookkeeping is separated. Wall-clock, cache, memory-bandwidth and energy remain outside the claim unless a later fresh contract separately binds them.

The prospective crossover grid covers total edges, in-degree, active out-degree, nonzero eligibility-set size, inter-event interval, event-count eligibility lifetime, delay diversity and reward frequency. It contains fixed one-factor sweeps around an anchor plus four preregistered sparse/dense/history-heavy/delay-heavy corners. Future runs may not add or drop grid points after viewing outcomes without a fresh successor object.

## Source basis / ordinary reductions

Stable main exposes the exact resource question without opening protected outcomes: `_decay_eligibilities()` scans every connection before every event; `_apply_reward()` scans every connection on reward; `_fire()` increments outgoing-edge eligibility; `reset()` preserves learned weights but clears episode-local eligibility/state; the dev test suite already has a reward-modulated plastic-edge hook. V05 also contains a sparse eligibility dictionary pattern, used only as a safe implementation surface, not as evidence.

This source basis is sufficient to bind optimized ordinary reductions prospectively. It does **not** establish that any reduction wins on the future grid. Ordinary reductions are therefore `PROSPECTIVELY_BOUND_NOT_MEASURED`, and the current observation is only `STATIC_CONTRACT_FEASIBLE_OUTCOME_INDEPENDENTLY`.

## Repository / integrity

Stable `main` was re-fetched immediately before final persistence and remains `ebed6abfa941c83b36d2e3bddd04e7c5fb0dbe9d`. The Evidence Analyst tip was re-read and remains R49. No `research/*` branch exists for this object. The five authoritative `evidence/*` tags are unchanged; tag-form `formal/*`, `sealed/*`, and `freeze/*` remain empty. H5 STARTED remains `058e90227cd48e1c10c6ecbaed01efdec1217d0e` and its raw-preserve ref remains `ce5797eb584344db7a512e585506fb6c59ea475b`. PR #148/#149 remain open/unmerged/mergeable.

SUB remains no-op and explicitly excludes the MAIN-owned object, so there is no collision. MAIN acquired a PRIMARY lease before mutation at `881fa426890ce8b23de26a630a6f20e324fef208`.

No research branch, scientific workflow, STARTED identity, synthetic measurement, scoring, preserve operation, immutable evidence mutation, PRE_FORMAL action or FORMAL action was created. H5 was neither opened nor reinterpreted.

## Accounting / stop

New activity in this cycle:

- FORMAL evidence: `0`
- PRE_FORMAL development evidence: `0`
- MECHANISM Architecture observations: `0`
- SYSTEM Architecture observations: `1` — static contract feasibility only
- new identity consumption: `0`

Stop reason: `R49_STATIC_CONTRACT_FEASIBLE_OUTCOME_INDEPENDENTLY_STOP_BEFORE_SYNTHETIC_MEASUREMENT`.

Next action: fresh Evidence Analyst review. Synthetic measurement remains unauthorized in this generation; any later measurement must use this exact bound workload/comparator/tolerance/resource/grid contract or be a fresh successor.
