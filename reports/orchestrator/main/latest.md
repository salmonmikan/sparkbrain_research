# MAIN latest — R73 H7 FORMAL-R1 cycle 7 input-identity blocker

Generation: `MAIN-20260922T203216+0900-PRIMARY-H7-FORMALR1-C7-INPUT-ID-BLOCK-R73`

Evidence Analyst R73 `EVA-20260922T202146+0900-R73-9A4C21E7@443c87fb44ae93845ff6b4d389196716a4efbc92` preserves H7 as `FORMALIZE / MECHANISM / RESULT_EXPOSED_DEVELOPMENT / ACTIVE`, `preformal_eligible=true`, readiness `READY`, and grants only outcome-blind cycle-7 preidentity final binding/provenance validation. Fresh FORMAL identity authority remains zero. Candidate hold dimensions remain null and ACTIVE/ACTIVE; the operational MAIN lease is blocked without reclassifying the candidate.

MAIN recovered the stale 19:17 lease and reconciled the superseding R73 generation before research mutation. The previously observed exact-source placement gap was already closed: `research/main-h7-formal-r1-preidentity-closure-r70-cycle7` equaled the R73-reviewed/green head `67ed8fad1d861463e4129d44efbb2540affd1889`; exact-head CI `35715449587` was `completed/success` and non-result-bearing.

Preidentity validation independently confirmed known reviewed-head blobs: contract `af26de3067263afcff0e727321fa173ea14de659`, scorer/preserver module `dd189f48b1a54bf08921c63d0f0ba3dbfc5c56a9`, contract preflight script `bc9eac991df4d545bddfaa3fd2809b10215cee27`, and runtime/seed preflight module `ee336cef36cfad69e5a9b4d34052c0a671c5ad0a`. `scripts/run_h7_formal_r1.py` is absent, so the exact result-bearing runner is not yet materialized or bound.

The validation also found a fail-closed FORMAL input-identity gap not fixed by the frozen contract: `Episode.split`. The contract fixes worlds, steps, fit/calibration/evaluation seed ranges, world assignment and model/comparator seeds, but does not specify the `train|dev|test|smoke` split value for the three surfaces. Repository schema includes split in `episode_id`, and `episode_examples()` propagates it into `LearnedExample`. Because FORMAL requires exact input identity binding, MAIN did not silently choose a previously unfixed split after result exposure. This is a FORMAL-integrity/protocol-identifiability blocker, not a scientific negative result and not an invitation to tune outcomes.

MAIN persisted this diagnostic as `artifacts/formal_h7_r1/preidentity_validation_r73.json` on current head `baaec4a7f1a1b6820d78ff9b33566bb809f74f42`, parent `67ed8fad1d861463e4129d44efbb2540affd1889`. This commit contains diagnostic/provenance state only and creates no identity or result. Non-result-bearing CI `35721879348` completed `success` on this exact diagnostic head. CI green validates repository conformance only; it does not resolve the missing input-identity binding or grant FORMAL authority.

Remote no-clobber observation at the validation boundary found no `refs/heads/control/h7*`, no `refs/heads/preserve/h7*`, and no `refs/tags/evidence/h7*`. This is only a current absence check; future exact identity/STARTED/preserve/evidence names must be rechecked immediately before any later authorized creation.

Other preidentity gates remain open: exact formal runner; complete runner/scorer/preserver self-binding; actual runtime/package manifests; complete prior-surface seed inventory and collision audit across PF-R1 plus all seven consumed FORMAL identities; synthetic scorer/preserve one-way totality closure; and durable NON_EVIDENTIARY PF-R1 `raw.json`/`summary.json` exact-byte preservation. The FORMAL contract's eligibility head seed is `9107020` while historical DEV-R2 defaults to `7603`; a future formal runner must implement the already-fixed formal seed without rewriting DEV-R2 history.

Scientific counts this run remain `FORMAL evidence=0 / PRE_FORMAL development result=0 / MECHANISM Architecture=0 / SYSTEM Architecture=0 / new consumed identity=0`. No FORMAL identity, STARTED, protected evaluation, result-bearing workflow, official scoring, scientific preserve/evidence ref, consumed-identity reuse, immutable scientific mutation, comparator/metric/threshold/intervention/resource/falsifier change, or historical result rewrite occurred.

MAIN status is `BLOCKED` with stop reason `R73_H7_FORMAL_R1_CYCLE7_BLOCKED_FORMAL_INPUT_IDENTITY_EPISODE_SPLIT_NOT_PROSPECTIVELY_BOUND_STOP_BEFORE_EXACT_RUNNER_IDENTITY_START_OR_RESULT`. Next action is a fresh Evidence Analyst prospective clarification or versioned binding for `Episode.split`, plus review of diagnostic head `baaec4a7f1a1b6820d78ff9b33566bb809f74f42`. Even after that clarification, identity/STARTED/protected/result-bearing execution remains STOP until all remaining preidentity gates close and a later fresh Analyst explicitly grants one-way authority.
