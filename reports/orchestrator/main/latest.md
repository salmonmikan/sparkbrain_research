# MAIN latest — R75 H7 FORMAL-R2 cycle 8 science-invariant CI recovery waiting on exact-head validation

Generation: `MAIN-20260922T222214+0900-PRIMARY-H7-FORMALR2-C8-WAIT-CI-REPAIR-R75-D7F2A9C1`

Evidence Analyst R75 `EVA-20260922T210223+0900-R75-D7F2A9C1@c190afff7e33977c530c207904bd42ce7187a20d` remains the controlling generation. H7 remains `FORMAL / MECHANISM / RESULT_EXPOSED_DEVELOPMENT / READY / ACTIVE`, revision `H7-FORMAL-R2-INPUT-SPLIT-BINDING-AND-PREIDENTITY-REVALIDATION`. The only authorized science-affecting delta remains the already-versioned `Episode.split` binding (`fit=train`, `calibration=dev`, `formal evaluation=test`, result-bearing `smoke` forbidden). FORMAL identity, STARTED, protected evaluation, result-bearing execution, official scoring, and scientific preserve/evidence remain STOP.

The prior exact-head generic CI `35727871994` succeeded on `80b88cd49fa5b9f5535feba27a75ebd3d4912406`, while preidentity manifest run `35727871981` failed before any protected/result-bearing surface. The failure was `FormalIntegrityError: runtime torch version drift`: `pyproject.toml` still pins `torch==2.13.0`, but runtime validation compared raw `torch.__version__` to the bare distribution version and therefore rejected a valid PEP-440 local build suffix.

MAIN classified this as `SCIENCE_INVARIANT_REPAIR`: the scientific package/runtime requirement is unchanged and the full raw runtime version remains preserved in the manifest. On `research/main-h7-formal-r2-input-split-r75-cycle8`, `RuntimeFreezeManifest` now validates the base version with `torch_version.split("+", 1)[0] == "2.13.0"` while continuing to record the complete `torch.__version__` string. No claim, metric/scorer meaning, threshold/tolerance, comparator, seed/exclusion policy, intervention, resource/privilege contract, falsifier, success criterion, device, Python family, OS family, thread count, determinism setting, or package pin was changed.

The repaired exact branch head is `a7a3c82416af5bd3c8bf3340cc9276f161ddb0d0`. Fresh non-result-bearing exact-head validation is now queued: `h7-formal-r2-preidentity` run `35732856997` and generic `ci` run `35732857066`. MAIN therefore stops as `WAITING_EXTERNAL`.

Scientific counts remain `FORMAL evidence=0 / PRE_FORMAL development result=0 / MECHANISM Architecture=0 / SYSTEM Architecture=0 / new consumed identity=0`. No FORMAL identity, STARTED, protected evaluation access, result-bearing workflow, official scoring, scientific preserve/evidence ref, consumed-identity reuse, immutable scientific mutation, or post-R75 science-affecting redesign occurred.

After both exact-head workflows complete, MAIN must re-read the Analyst generation and exact branch head before inspecting the non-result runtime/package manifests and continuing only the remaining R75-authorized preidentity gates: complete prior-surface seed inventory/collision audit, later create-only/no-clobber recheck, synthetic one-way scorer/preserve totality, and durable PF-R1 exact-byte provenance. Fresh one-way FORMAL authority is still required before identity creation or any result-bearing action.
