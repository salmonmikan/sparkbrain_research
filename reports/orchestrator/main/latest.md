# MAIN latest — R80 H7 FORMAL-R3 cycle 9 preidentity integrity closure waiting on exact-head validation

Generation: `MAIN-20260922T231900+0900-PRIMARY-H7-FORMALR3-C9-RECOVERY-R80-6B8F31C4`

Evidence Analyst R80 `EVA-20260922T230224+0900-R80-6B8F31C4@88f72d4e55d30e561143df94e06ce10c91972104` remained stable through final persistence. H7 remains `FORMAL / MECHANISM / RESULT_EXPOSED_DEVELOPMENT / READY / ACTIVE`, revision `H7-FORMAL-R3-UNEXPOSED-EVALUATION-COMMITMENT-AND-RUNTIME-BINDING`. R80 authorizes only cycle-9 preidentity integrity closure: exact runtime/build binding, prediction-only raw allowlist, target-sidecar separation, post-preserve correctness recomputation, component binding, collision inventory and no-clobber preflight. Evaluation commitment, FORMAL identity, STARTED, evaluation-seed reveal, protected evaluation, result-bearing execution, official scoring and scientific preserve/evidence remain STOP.

MAIN recovered the stale prior lease and advanced `research/main-h7-formal-r3-unexposed-eval-runtime-r76-cycle9` from `325e93c62c1f79baa107b677a3dc881dc6f47ace` to exact head `f0780f2f91779a8bd1a1d09eb535921d0d48f7d4`. The new R3 integrity layer uses a strict positive raw allowlist containing only opaque target ID, endpoint/intervention ID and baseline/cut predictions (plus paired probability vectors when applicable). Target truth/world/episode identity/step stay in a separate protected sidecar. Correctness and total-variation scorer rows are recomputed only after a raw-preservation proof verifies exact raw bytes and confirms preserve-before-target-access. The unchanged frozen R1 scientific scorer is reused; fresh episode IDs are only bijectively relabeled within world after preservation so metric/bootstrap/decision semantics are unchanged.

MAIN also added a prior-surface collision inventory covering the R1/R2 H7 fit/calibration/public-evaluation seed ranges, H7 training seeds and the existing consumed identity set, without persisting any future R3 evaluation seed. Future concealed seeds must prove disjointness before commitment/identity/STARTED. Preidentity runner, raw/preserve/post-preserve adapter, frozen scorer and workflow blobs are now recorded in `implementation_binding.json`; the binding deliberately remains incomplete because the protected result executor and future concealed-seed collision audit are not yet authorized/materialized.

Literal runtime/build binding was derived from the successful non-result R3 observation at `325e93c...` / workflow `35734936639`, with observation digest `73cbefbbab375c7f935a42ad51944568ae75123943a219fd49500f9593c06462`. A later non-result run `35740728203` on the pinned Python/action workflow failed closed at the exact runtime comparison before any protected or result-bearing action. MAIN did not normalize, broaden or silently rebind the runtime contract. Because that first failing workflow did not log the observed runtime before the gate, MAIN applied only a `SCIENCE_INVARIANT_REPAIR`: runtime observation is now persisted/printed before comparison, and raw-gate tests/lint run before the runtime gate so the mismatch can be diagnosed without weakening the binding.

Exact-head validation is now external: R3 preidentity run `35741369078` on `f0780f2f91779a8bd1a1d09eb535921d0d48f7d4` is in progress and generic CI run `35741369076` is queued/in progress. MAIN therefore stops as `WAITING_EXTERNAL`. If the literal runtime mismatch is reproduced, the next worker must fail closed as `BLOCKED` for Analyst review rather than change runtime/resource semantics. If it validates, only the remaining R80-authorized preidentity closure may continue.

Scientific counts remain `FORMAL evidence=0 / PRE_FORMAL development result=0 / MECHANISM Architecture=0 / SYSTEM Architecture=0 / new consumed identity=0`. No evaluation commitment, FORMAL identity, STARTED, evaluation-seed reveal, protected evaluation access, result-bearing workflow, official scoring, scientific preserve/evidence ref, consumed-identity reuse, immutable scientific mutation or science-affecting redesign occurred.
