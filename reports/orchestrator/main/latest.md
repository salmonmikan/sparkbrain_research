# MAIN Orchestrator — PRIMARY PD0.1 matched fading-memory formal contract package

Timestamp: `2026-09-18 10:28 JST`
Execution mode: `PRIMARY`
Evidence Analyst authority: `28108fffc2e4e346bff79bf3e38d1cac27d3265c`

## MAIN frontier

MAIN advanced the primary frontier from PD0.1 pre-formal readiness to a fully specified matched fading-memory formal-contract proposal on `research/pd01-fading-memory-preformal-20260918@ea2a8b4a5f244d601782ecb302760c49ee27e8c1`.

The latest Analyst authority explicitly permitted this specification/readiness work and required a stronger conventional fading-memory comparator, exact prospective world/intervention/recent-match/lag/source/runtime/reset/readout/resource/statistical contracts, and a hard stop at `PD01_FORMAL_CONTRACT_READY_FOR_ANALYST_REVIEW`. Formal identity reservation, STARTED, official TEST/target access, preservation, scoring, and one-way execution remain forbidden until a newer Analyst handoff.

## Critical-path progress

The formal proposal now freezes, prospectively and without official outcome access:

- paired synthetic remote-history/washout world with two anonymous lineages;
- exact remote intervention `(1.0, 0.0)` versus `(0.0, 1.0)`, symmetric filler, and symmetric final probe;
- lag grid `16/32/64/128`, recent window `8`, DEV `64` base worlds seed `33031`, TEST `128` base worlds seed `77237`;
- exact SparkBrain v0.4 source `main@ebed6abfa941c83b36d2e3bddd04e7c5fb0dbe9d`, fixed 8x8/config/reset semantics, and a 64-dimensional final-probe spike-response feature;
- a stronger fixed 64-state sparse contractive tanh reservoir comparator, fan-in `8`, leak `0.35`, recurrent row L1 `0.90`, input scale `0.50`, bias scale `0.05`, seed `90917`, with no recurrent fitting/tuning;
- identical one-shot ridge-linear DEV readout privilege for both models, `alpha=0.001`, 65 coefficients each, no hyperparameter selection/retry;
- TEST inventory `1,024` histories / `2,048` raw model-score rows, primary lags `64/128`, base-world cluster bootstrap `10,000` resamples seed `19901`, Type-7 percentile CI, and fixed PASS/FAIL/INCONCLUSIVE numerical criteria;
- raw-before-target/preserve-before-read integrity sequence and fail-closed invalid-evidence conditions;
- proposed namespace `pd01-long-history-fading-memory-official-v1`, explicitly **UNRESERVED**.

Added implementation primitives, formal contract JSON/Markdown, synthetic/dev-only tests, fail-closed checker, and dedicated exact-runtime pre-START workflow. No formal TEST output was generated.

## Mechanical pre-START repair

The first dedicated formal-contract gate `35295140779` failed only at Ruff: one unused `math.exp` import and one 101-character docstring line. MAIN classified this under the prospectively authorized `PRE_START_BLOCKER_MECHANICAL` contingency, removed the unused import, wrapped the docstring, and rebound the implementation Git blob in the contract/checker. No scientific field, model choice, threshold, lag, statistic, resource, or identity semantics changed.

The resulting exact head is `ea2a8b4a5f244d601782ecb302760c49ee27e8c1`.

## Workflow/check state

Fresh exact-head runs are now active:

- dedicated `pd01-formal-contract-prestart` `35295370268`: `in_progress`;
- ordinary CI `35295370271`: `in_progress`;
- compatibility `pd01-preformal` `35295370202`: `in_progress`.

No useful local critical-path work remains while those external jobs run, so PRIMARY is handing off collection rather than occupying the lane waiting.

## Scientific / integrity state

There is **no new scientific information**. MAIN did not reserve an identity, create STARTED/control refs, access official TEST inputs/targets, dispatch one-way execution, create preserve/evidence refs, score official evidence, touch immutable evidence, or retry/modify a consumed identity. SUB has no assigned PD0.1 lane and remains outside this work.

## Stop / relay condition

Lease is `WAITING_EXTERNAL`. Relay continuation is expected only to collect the exact-head runs above and apply the existing prospectively fixed contingency:

- if failures are purely mechanical and science-invariant, MAIN/Relay may repair and require all exact-head gates again;
- if a failure exposes a semantic/scientific gap, STOP for Analyst;
- if required exact-head gates are green, persist `PD01_FORMAL_CONTRACT_READY_FOR_ANALYST_REVIEW` and STOP.

Even after green gates, do **not** reserve `pd01-long-history-fading-memory-official-v1`, create STARTED, access official TEST/targets, or dispatch one-way execution without a newer Evidence Analyst authorization reviewing the final exact green head.
