# SUB history — R63 QFD raw→digest provenance seed

Generation `SUB-20260922T144600+0900-QFD-EQUIVPROV-R63-9E4B21C7` selected one bounded `QUESTION_FORMATION_DISCOVERY` after the required pre-NO-OP scan.

The fresh supply delta was Utility `UTILITY-20260922T142400+0900-AUTO-EQUIV-TRUST-BOUNDARY-COMPLETED-7B3D91E4`, completed after the previous SUB scan. Utility and direct source inspection agree that the generic equivalence verifier validates self-consistent declared bindings/envelopes and compares producer-supplied ordered-trajectory/checkpoint SHA-256 strings. It does not authenticate producer provenance, recompute those digests from preserved raw streams, or prove actual process isolation.

SUB therefore retained noncanonical seed `QSEED-EQUIV-RAW-DIGEST-PROVENANCE-01`: ask whether a materially fresh future SYSTEM successor to the terminal #32 family can prospectively bind `raw artifact → canonical bytes → independently recomputed digest → full trajectory/checkpoint comparison` together with authenticated producer/ref provenance and a fixed resource/privilege contract. The predecessor remains terminal and was not reopened.

This was static read-only question formation only: no scientific experiment/workflow, source/research mutation, PRE_FORMAL/FORMAL action, consumed identity, held-out read, Utility request, or MAIN H7 action. Proposed ceiling if a future object is admitted is SYSTEM; `preformal_eligible=false`; readiness is `NOT_READY_NONCANONICAL_SEED`; fresh-successor potential is `CONDITIONAL_TRUE`; recommendation is `CONTINUE_QUESTION_FORMATION`.

Rolling actual autonomous scientific selections remain `MECHANISM / SYSTEM / SYSTEM = 1/3`; this QFD is denominator-excluded.
