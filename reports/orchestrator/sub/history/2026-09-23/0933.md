# SUB history — R90 candidate #35 Discovery R1 contract closure

- schema_version: `2`
- generation_id: `SUB-20260923T093316+0900-DISC-C35-CONTRACT-R90-4A7E2C91`
- produced_at: `2026-09-23T09:33:16+09:00`
- operating_mode: `ANALYST_ALLOCATED_SECONDARY_RESEARCH`
- discovery_mode: `SYSTEM_DISCOVERY`
- selected_target: `CAND-35-QUEUE-FREE-SUBTHRESHOLD-STATE-CAUSAL-PRIMING`
- work_kind: `CANONICAL_DISCOVERY_R1_QUESTION_CONTRACT_CLOSURE`
- evidentiary_status: `NON_EVIDENTIARY_CANONICAL_DISCOVERY_CONTRACT_ONLY`
- recommendation: `PROMOTE_TO_ARCHITECTURE_STUDY`

## Freshness / ownership reconciliation

Evidence Analyst R90 (`EVA-20260923T090047+0900-R90-B84D2C71@91684ca2539d89c6866feecbf1a9e28f26aef2fa`) admits candidate #35 as `SYSTEM / DISCOVERY / OPEN_DEVELOPMENT / DISCOVERY-R1_AUTHORIZED`, `preformal_eligible=false`, `NOT_APPLICABLE`, `NONTERMINAL`, `QUEUED_FOR_SUB_DISCOVERY_R1`, with exact decision `GO_CAND35_SYSTEM_DISCOVERY_R1_QUESTION_CONTRACT_ONLY_NON_EVIDENTIARY`. Candidate execution is explicitly not authorized; contract closure is authorized.

MAIN owns candidate #34 Architecture R2. Its active research branch was independently re-fetched at `research/main-cand34-assembly-route-architecture-r90-cycle2@a6455a3929b86ad25fd106ea93a03604192fc3be` while SUB remained on #35. H7 R5 and PF-R1 provenance remain MAIN/control-plane critical-path surfaces and were not touched. Stable `main` remains `ebed6abfa941c83b36d2e3bddd04e7c5fb0dbe9d`; the authoritative five `evidence/*` objects and all consumed identities remain untouched.

Fresh Methodology R77 materially confirms that #35 should remain SYSTEM-scoped while ordinary local threshold/decay semantics dominate, and that any later MECHANISM residual requires a fresh successor ID. Literature R34 is relevant to #34 route identifiability, not a reason to alter #35. Independent Audit R7 remains H7-only integrity guidance. Utility PF-R1 remains proposal-only/default-deny and is unrelated to this SUB scientific task.

## PRE-NO-OP status / alternatives

The PRE-NO-OP gate was not reached because R90 supplies a valid, independent canonical `sub_lane` for candidate #35. No autonomous target was selected. Candidate #34/H7 were excluded as MAIN-owned; candidate #33 and other terminal-family successor scans were not used to displace an explicit Analyst allocation. The prior #35 QFD seed is now canonical, so this run closes its bounded Discovery R1 contract rather than manufacturing a second seed.

## Phenomenon / question / hypothesis

**Phenomenon.** A v0.5 episode can leave queue-free, decaying physical field state (`potential`, `adaptation`) after recurrent arrivals have stopped, even though the earlier endogenous-continuation object found no spontaneous empty-input continuation.

**Question.** At a prospectively fixed queue-empty post-drive anchor, does non-receptor residual `potential` and/or `adaptation` causally change the response to one fixed weak field-level cue when topology, weights, higher learned state, cue bytes and all non-intervened state are identical?

**SYSTEM hypothesis.** Residual subthreshold potential can increase, and residual adaptation can decrease, later cue sensitivity through the already-defined local field dynamics. This run asserts no new computational principle. A positive result that is fully explained by ordinary threshold margin plus the frozen 18 ms membrane / 90 ms adaptation decay remains a SYSTEM result and closes the current object at that ceiling.

## Prospectively frozen Discovery R1 contract

### Prime trajectory

Use a pristine default `IntegratedV05Brain` at exact stable-main source. Drive exactly one no-learning v0.5 motif-only episode with raw pulses:

- `A @ 0.0 ms`, magnitude `1.18`, polarity `+1`, novelty `0.25`;
- `F @ 5.0 ms`, magnitude `1.18`, polarity `+1`, novelty `0.25`;
- `C @ 7.0 ms`, magnitude `1.18`, polarity `+1`, novelty `0.25`;
- all `prediction_error=0`, `location=null`, fixed source id `cand35-prime`, empty metadata.

Invoke the v0.5 path with `learn_assembly=false`, `learn_field=false`, `explore_action=false`, fixed episode id `cand35-prime-0001`. This allows native v0.5 receptor transduction to create the physical field state while forbidding learning/retuning. Higher v0.5 state may be present but is cloned identically and is not used by the later field-level probe.

### Queue-empty anchor

After the prime's normal 32 ms settle, inspect only the serialized field queue. If non-empty, advance the embedded v0.4 field/base in fixed `+32.0 ms` increments with no input until the **first** boundary whose queue is empty. Maximum additional boundaries: `8` (`256 ms`). No post-observation cap extension is allowed. Failure to reach an empty queue within the cap is a fail-closed Discovery/Architecture reassessment, not a cue or timing retune.

The intervention checkpoint is the exact first queue-empty state. The fixed cue occurs at `anchor + 4.0 ms`, providing an outcome-independent guard beyond the stable 3 ms absolute refractory duration while preserving most of any residual subthreshold state. No input is allowed during the guard.

### State intervention scope

Clone the exact queue-empty checkpoint. Treatment scope is **all non-receptor units only**; receptor units, connections, weights, delays, traces, counters, higher v0.5 state and cue bytes are identical across arms.

- `SHAM_STATE`: exact clone, no field-state edit.
- `POTENTIAL_NULL`: for every non-receptor unit set only `potential = field.config.reset_potential`; preserve adaptation and every other field.
- `ADAPTATION_NULL`: for every non-receptor unit set only `adaptation = 0.0`; preserve potential and every other field.
- `JOINT_SUBTHRESHOLD_NULL`: apply both of the above, and no other edit. This is a prospectively fixed interaction/cue-only physical-state comparator, not a later rescue arm.
- `DELAYED_SHAM_32MS`: exact sham, no edit, but issue the identical cue `32.0 ms` later than the primary cue to expose the ordinary natural-decay direction without changing cue bytes.

Zero-valued edits leave `last_update_ms` unchanged; under the existing lazy-decay rule zero remains zero. No refractory, threshold, trace, source-id, topology, weight or delay manipulation is permitted.

### Fixed weak field-level cue

To isolate the physical field state from v0.5 receptor-bank memory, the response probe deliberately enters through the embedded field/base pulse interface rather than the v0.5 receptor bank. Freeze exactly one `SignalPulse`:

- channel `A`;
- time `anchor + 4.0 ms` (`DELAYED_SHAM_32MS`: `anchor + 36.0 ms`);
- magnitude `0.85`, polarity `+1`;
- novelty `0`, prediction_error `0`, location `null`;
- source id `cand35-cue`, empty metadata.

Static source derivation at the frozen default topology gives channel-A receptor targets `(6, 7)`. The direct pulse contributes about `0.8114050314` current per routed receptor under the frozen `input_gain=1.35` and fanout `2`; default receptor threshold is `0.46`. Each default receptor→reservoir edge is `0.47`, below the default reservoir threshold `0.76` by `0.29` before residual-state contributions. These are source-derived contract facts, not candidate observations and not evidence.

If the stable source/ref no longer yields this routing/resource surface when a later Architecture cycle is authorized, STOP for revision rather than silently normalizing the contract.

### Measurement window and complete causal-response signature

Run the fixed field-level cue for exactly `32.0 ms` after cue time, with no learning and no window extension. The **causal response signature** excludes treatment-state bytes themselves and contains only downstream response:

1. ordered spike rows with time relative to cue, `unit_id`, `potential_before_reset`, `dynamic_threshold`, and source pulse ids;
2. ordered cascade rows with start/end relative to cue, spike count, `unit_ids`, `ordered_units`, recurrence and signature;
3. ordered ignition rows with time relative to cue, score, threshold, reason, `unit_ids`, signature;
4. cue routing target ids and exact processed event counts as integrity metadata.

Serialize in canonical JSON and hash it. `field_state_hash` may be recorded only as an intervention/integrity trace; it is **not** part of the causal falsifier because direct state edits would make it differ even without any downstream causal effect.

No Assembly/prediction/action difference is required for the current SYSTEM object. Any future higher-level residual is a separate successor question.

## Ordinary reductions / comparators

The frozen ordinary panel is:

1. exact `SHAM_STATE` clone;
2. factorial state-null decomposition (`POTENTIAL_NULL`, `ADAPTATION_NULL`, `JOINT_SUBTHRESHOLD_NULL`);
3. `DELAYED_SHAM_32MS` natural-decay comparator;
4. exact local threshold-margin ledger at the first cue-driven arrival for each directly affected non-receptor unit, using the existing lazy exponential decay (`18 ms` membrane, `90 ms` adaptation), `dynamic_threshold = base_threshold + max(0, adaptation)`, and the frozen connection currents/delays;
5. hard queue-empty requirement eliminating pending-arrival carryover;
6. identical topology/weights/higher state and no learning across all arms.

The local ledger is explanatory, not a fitted model: no parameter, tolerance, threshold or cue magnitude may be changed after any response is seen.

## Falsifier / discriminator

- **Reachability failure:** queue-empty anchor not reached inside the fixed 256 ms cap, source contract drift, non-finite state, or configured event/spike resource cap hit => STOP/REASSESS; no retune.
- **Causal falsifier:** if the complete causal-response signature is exactly identical across `SHAM_STATE`, `POTENTIAL_NULL`, `ADAPTATION_NULL`, and `JOINT_SUBTHRESHOLD_NULL`, reject a queue-free subthreshold-state causal effect for this frozen SYSTEM object.
- **Component discriminator:** sham-vs-potential-null isolates potential contribution; sham-vs-adaptation-null isolates adaptation contribution; joint-null tests their combined/interaction contribution. No component is credited if its null arm leaves the response signature unchanged.
- **Ordinary reduction:** if any response divergence begins at local threshold crossings predicted by the fixed potential/adaptation decay and dynamic-threshold ledger and downstream differences are deterministic descendants under unchanged recurrence, classify the phenomenon as ordinary decaying physical excitability at SYSTEM scope. Do not uplift the same object.
- **Natural-decay check:** the delayed-sham arm is interpreted only against the predeclared exponential-decay direction; it cannot be used to tune a new cue/window.

## Implementation / observations

This run performed read-only source/history inspection and static contract derivation only. Stable source confirms: `UnitState` exposes `potential`, `adaptation`, threshold/refractory/timing state; `TemporalExcitableField` applies 18 ms / 90 ms exponential decay and serializes queue plus units; v0.5 checkpoints preserve the nested field; the default v0.5 topology has 16 receptors at threshold 0.46 and reservoir threshold 0.76 with receptor→reservoir weight 0.47. No response-bearing candidate arm was executed.

No research branch, workflow dispatch, scientific experiment, PRE_FORMAL/FORMAL action, identity consumption, protected/held-out access, scorer use, evidence/preserve ref, immutable-ref mutation, Utility request or historical PASS/FAIL rewrite occurred.

## Classification / next layer

Candidate #35 remains canonical `SYSTEM / OPEN_DEVELOPMENT / DISCOVERY-R1_AUTHORIZED`, `preformal_eligible=false`, with no hold. Discovery cycle 1 closes the remaining question-contract choices prospectively without outcome exposure. The next useful work is a bounded NON_EVIDENTIARY Architecture Study implementing clone/reset/serializer/fail-closed checks plus synthetic tests only; candidate-response execution still requires later Analyst authority.

Recommendation: **`PROMOTE_TO_ARCHITECTURE_STUDY`**.

Theory-backward accounting is unchanged: this was an Analyst-allocated canonical lane derived from a prior denominator-excluded QFD, not a new autonomous scientific selection. Rolling autonomous selections remain `MECHANISM / SYSTEM / SYSTEM = 1/3`; this run is denominator-excluded.
