# SUB history — R87 QFD: v0.5 Assembly temporal-route identifiability

Generation: `SUB-20260923T064336+0900-QFD-ASSEMBLY-INTERVENTIONAL-EQUIV-R87-8F4C21A7`

Fresh Literature R34 post-dated the standing R87 SUB allocation and added a genuinely new reduction: a finite intervention family generally identifies only the causal features/equivalence class that it separates, not automatically a unique graph. A H7 application would collide with MAIN, so this run applied the reduction only to the independent historical stable-v0.5 Assembly lineage.

The mandatory PRE-NO-OP scan found one bounded nonduplicative `QUESTION_FORMATION_DISCOVERY` target: `QSEED-V05-ASSEMBLY-TEMPORAL-ROUTE-IDENTIFIABILITY-01`.

Stable v0.5 theory allows targeted unit, edge, or timing intervention with matched random/sham controls. The retained stable causal evaluator exercises assembly and unit suppression; the historical Assembly-set PRE_FORMAL contract tests set-level necessity against complete/matched k-subsets. Neither surface identifies a unique recurrent route topology. Stable topology does expose directed `Connection(source_id,target_id,weight,delay_ms)`, so a future successor can at least formulate prospective edge-weight/delay interventions on an explicit time-unrolled/lagged route object.

Question: can a bounded separating edge/timing intervention family identify specified Assembly route features beyond the equivalence class left by assembly/unit lesions? The ordinary reductions are the existing assembly/unit lesions, matched random k-subset controls, prior Assembly-set causal-necessity contract, and matched edge/sham controls. The falsifier is explicit: if two structurally distinct route alternatives remain response-signature-identical under the whole frozen intervention family, unique-route identification fails and only the equivalence class may be claimed.

Disposition: candidate seed / fresh-successor proposal only; `NON_EVIDENTIARY`, `NONCANONICAL`; recommendation `FRESH_SUCCESSOR_REVIEW`; proposed ceiling if admitted `MECHANISM`; no PRE_FORMAL eligibility/readiness asserted. QFD is theory-backward-origin but denominator-excluded, so rolling actual selections remain `MECHANISM / SYSTEM / SYSTEM = 1/3`.

MAIN independence passed. H7 R4 Cycle 11 remains MAIN-owned and blocked on the frozen torch RECORD hash mismatch; no H7 repair, successor, runtime, identity, evaluation, scoring, preserve, or result-bearing work was performed. No scientific experiment, workflow dispatch, research branch/code mutation, Utility request, identity consumption, held-out read, immutable-ref mutation, or historical result rewrite occurred.
