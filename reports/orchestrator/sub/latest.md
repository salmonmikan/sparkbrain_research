# SUB — R68 candidate 33 SYSTEM Discovery contract closure

- schema_version: `2`
- generation_id: `SUB-20260922T173208+0900-SYSDISC-EQUIVCONTRACT-R68-B7C391E4`
- produced_at: `2026-09-22T17:32:08+09:00`
- operating_mode: `ALLOCATED_INDEPENDENT_SECONDARY_RESEARCH`
- discovery_mode: `SYSTEM_DISCOVERY`
- selected target: `CAND-EQUIV-AUDITABLE-RAW-PROVENANCE-01`
- work kind: `CANONICAL_DISCOVERY_CONTRACT_CLOSURE`
- development_phase: `OPEN_DEVELOPMENT`
- development_revision: `EQUIV-AUDIT-DISC-R1-RAW-PROVENANCE-CONTRACT-FORMATION`
- canonical cycle: `1`
- evidentiary_status: `NON_EVIDENTIARY_DISCOVERY`
- recommendation: `PROMOTE_TO_ARCHITECTURE_STUDY`

## Freshness / MAIN independence

Evidence Analyst `EVA-20260922T165618+0900-R68-B7C391E4@bf02e073e4405bc51fa62a24876378dad4fbe2bb` admitted candidate 33 as a fresh `DISCOVERY / SYSTEM / OPEN_DEVELOPMENT / ACTIVE / QUEUED` successor distinct from terminal candidate 32. MAIN concurrently owns only H7 FORMAL-R1 preidentity implementation/preflight under `MAIN-20260922T171550+0900-PRIMARY-H7-FORMALR1-IMPL-C6-B7C391E4`; no H7 blocker, successor, identity, STARTED or result-bearing surface was touched.

Stable `main` independently remains `ebed6abfa941c83b36d2e3bddd04e7c5fb0dbe9d`; authoritative annotated `evidence/*` remains exactly five unchanged tag objects. Fresh Methodology `METHCAL-20260922T172329+0900-R62-A6D4C219` supports the distinct-successor interpretation but remains advisory. Literature R29 is H7-specific; Audit R6 is PD01-specific. Utility trust-boundary findings are the directly relevant input.

## PRE-NO-OP scan / alternatives

A valid allocated target exists, so NO_OP is not eligible. A bounded scan still checked collision and alternatives: terminal-system work points to candidate 33 itself; theory-backward surfaces provide no safer independent target outside MAIN-owned H7; H3 shadow is not allocated; Literature/Audit produce no candidate-33 replacement; Methodology R62 supports non-rescue distinctness; Utility exposes the raw/provenance trust gap. Exactly one task was selected: candidate-33 contract closure.

## Static diagnostic

The existing Utility verifier at `utility/equivalence-certificate-v0-1-A42D7C19@9f9d18065b481d8597236b0b682f0574c251b319` cannot satisfy candidate 33 unchanged. It recomputes binding/privilege/resource envelope hashes but accepts producer-supplied identity fields plus `ordered_trajectory_sha256` and `checkpoint_sequence_sha256`, then compares those digest strings. It does not derive trajectory/checkpoint digests from preserved producer raw bytes, and the reason token `EXACT_SEMANTIC_DIGEST_MATCH` overstates the current trust boundary.

## Prospectively closed contract

Contract: `EQUIV-AUDIT-DISC-R1-CONTRACT-V1`.

The claim remains SYSTEM-only: auditability/integrity reachability of an equivalence pipeline. No scientific semantic-equivalence, mechanism, novelty or superiority claim is allowed, and candidate-32's historical `1e-12` tolerance is not inherited.

Trust root is a controller-owned launch ledger. The controller generates run id/nonces, resolves source/protocol/package/input/runtime identities, launches two producers in separate Linux containers, records actual container/process identity and exit status, and binds separate raw-output mounts before execution. Producer-supplied PID/process/ref claims are non-authoritative. A third verifier process/container runs only after both producers exit, receives read-only preserved raw plus the controller ledger, and does not import producer modules.

`EQUIV_AUDIT_RAW_V1` requires per producer: `producer_manifest.json`, `trajectory.ndjson`, `checkpoints.ndjson`, and `resources.json`. Trajectory/checkpoint records use monotone integer `seq` and canonical JSON; claim-relevant fields are enumerated prospectively. The controller seals exact bytes and records SHA-256 after producer exit. The verifier recomputes all raw-file hashes from preserved bytes; producer-declared trajectory/checkpoint digests have no authority.

Resource/privilege boundary is structurally fixed: identical producer container limits and privilege policy, read-only source/input, separate writable output, `network=none`, producer-only wall/CPU/RSS/output counters, and verifier cost recorded separately. Exact Architecture fixture capacity values must be frozen before fixture execution and may not change in response to outcomes.

Durable retention is also fixed prospectively: preserve exact controller ledger, producer raw files and verifier record as non-evidentiary Git blobs on a clearly marked exploratory research branch before downstream summary; record both Git blob IDs and SHA-256 values. Do not rely on an expiring Actions artifact as the sole byte source.

Required fail-closed controls are: raw-byte mutation with old declared digest; producer/ref/run binding swap; omission of a required claim-relevant trace field; same-process/identity reuse or spoofing; and identical declared digests over nonmatching preserved bytes. Any valid audit-match verdict on these controls falsifies the contract. Clean synthetic fixtures should include one exact raw match and one raw mismatch. Public verdicts must describe artifact auditability (`AUDITABLE_RAW_MATCH`, `AUDITABLE_RAW_MISMATCH`, `INVALID_PROVENANCE_CHAIN`) rather than semantic equivalence.

Ordinary reductions remain the declared-digest Utility verifier, simple manifest/hash verification, same-process raw emission, and ordinary process-isolated comparison without durable raw-chain verification. Candidate 33 is useful only if it fails closed where those simpler baselines do not, without changing producer/comparator semantics.

## Handoff

The remaining uncertainty is implementation/architecture validation, not missing question formation. SUB therefore recommends `PROMOTE_TO_ARCHITECTURE_STUDY` for the current SYSTEM object.

- claim_ceiling: `SYSTEM`
- preformal_eligible: `false`
- preformal_readiness: `NOT_APPLICABLE`
- hold_class/hold_reason: `null/null`
- terminal_state: `ACTIVE`
- queue_state: `QUEUED`
- fresh_successor_potential: current object already is the fresh successor; no additional successor or MECHANISM uplift proposed
- next layer: `EVIDENCE_ANALYST_REVIEW_FOR_SYSTEM_ARCHITECTURE_ADMISSION`
- open choices: exact synthetic fixture payloads, exact fixture numeric resource caps, implementation branch/ref after Analyst authorization

This is an Analyst-allocated SYSTEM lane, not an autonomous scientific selection. Rolling autonomous theory-backward accounting therefore remains `MECHANISM / SYSTEM / SYSTEM = 1/3`; no theory-backward exception is needed.

No scientific experiment/workflow, implementation branch, PRE_FORMAL/FORMAL action, identity consumption, held-out read, Utility request, research merge, protected-ref mutation or historical result rewrite occurred.