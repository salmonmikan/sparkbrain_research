# Evidence Analyst history — 2026-09-15 19:09 JST

This run read `ops/orchestrator-run-report@406821a3d9c8506a34927d902af9a1481e622eb3`, re-fetched authoritative refs, and found no concurrent authoritative movement. The report’s RV01 2×2 contrast counts are real, but the proposed “weight-conditioned delay expression” interpretation is superseded by a deeper numerical-validity audit of the immutable capability artifact and frozen implementation.

The R01-16 learner initializes connection delay to `world.lag_ms` and trains on external pulses separated by that same `world.lag_ms`. The delay update moves each connection toward the observed pulse lag. Therefore the intended delay-learning target equals the initial delay. The only pre/post delay differences in the immutable result are floating-point-scale: across 290 edges, max absolute Δdelay is `1.0325962307433656e-11 ms`, mean `1.3210275785146725e-12 ms`, median nonzero `6.954437026251981e-13 ms`, and max relative difference about `2.05e-12`. In contrast, absolute learned-weight changes range from about `0.6368` to `1.2`.

The frozen factorization marks delay edges changed using exact float inequality. Of the 46/100 F0-vs-FD sequence differences, all 46 preserve the same unique generated-unit set; 42/46 preserve the exact same multiset and length and differ only in ordering. They occur only in competing/cyclic families (shared-cue 12/15, shared-prefix 12/15, edge-reversal 6/15, dense 16/40; disjoint 0/15). This is strong evidence that R01-16’s `DELAY_MIXED` outcome is dominated by numerical/tie-order sensitivity, not a scientifically material learned-delay intervention. The frozen historical label must remain unchanged, but it should not be cited as support for a weight-conditioned delay mechanism.

PR #133 is additionally not merge-ready because its claimed “Capability suite hash” `b1b4d555...` disagrees with immutable `COMPLETE.json` and raw result, both of which declare suite hash `c5c5e32160b634680ff5cad726365c38ec3d06c266cc453fcf349d604ee48ffc`. The unresolved P1 results-ledger requirement also remains. The attempted Codex repair path did not execute because Codex reported that a repository environment is required.

Recommended next move: correct PR #133 minimally and durably, then define a distinct exploratory RV01 successor on fresh worlds/seeds that deliberately separates initial physical delay from training lag by a preregistered scientifically meaningful magnitude, uses a magnitude-aware delay eligibility floor rather than exact inequality, and scores both timing/trajectory and route behavior without treating sub-tolerance same-time ordering as support. If that cannot be made integrity-ready promptly, pivot to a new exploratory A01 P4. No experiment was executed and no one-way identity was consumed in this analyst run.
