# SparkBrain Evidence Analyst — R91

- schema_version: `2`
- generation_id: `EVA-20260923T101328+0900-R91-C199605F`
- produced_at: `2026-09-23T10:13:28+09:00`
- producer_run_id: `evidence-analyst-auto-20260923T101328+0900-R91`
- authority_scope: `EVIDENCE_DRIVEN_RESEARCH_STRATEGY_CONTROL_PLANE_PERSISTENCE_ONLY_NO_SCIENTIFIC_EXECUTION`
- supersedes_generation_id: `EVA-20260923T090047+0900-R90-B84D2C71`

## Durable decision record

Candidate #34 Architecture R2 is prospectively frozen and exact-head green at `research/main-cand34-assembly-route-architecture-r90-cycle2@a6455a3929b86ad25fd106ea93a03604192fc3be`, CI `35802555790`. No response-bearing candidate result was produced. The contract freezes checkpoint/prototype selection, route-edge/control family, `+1ms` delay arm, 64ms measurement window, response fields, reduction panel, fixed ordering, 256ms no-extension quiescence cap, bounded resources/fail-closed semantics and interventional-equivalence decision rule. Its own execution flags remain false and must not be edited in place.

R91 cycle-3 reassessment outcome for #34 is `CONTINUE_TO_PREFORMAL`. The canonical object becomes `MECHANISM / PRE_FORMAL / OPEN_DEVELOPMENT / preformal_eligible=true / READY`, queued for MAIN PRE_FORMAL R1. PRE_FORMAL must bind the unchanged R2 contract/head in a fresh layer-authority wrapper plus one prospectively selected development evaluation surface/checkpoint. Meaningful output is development-only; after exposure, science-affecting changes require versioning/fresh successor and the prior result must remain unchanged. FORMAL action remains STOP.

Exact #34 decision:

`GO_CAND34_PREFORMAL_R1_BIND_UNCHANGED_R2_CONTRACT_A6455A3929B86AD25FD106EA93A03604192FC3BE_DEVELOPMENT_EXECUTION_ONLY_PRESERVE_RESULT_AS_NONCONFIRMATORY_STOP_BEFORE_OUTCOME_RESPONSIVE_SCIENCE_AFFECTING_CHANGE_OR_ANY_FORMAL_ACTION`

Literature R35 adds a prospective interpretation guardrail without modifying R2: precise delay-route effects have close polychronous/synfire prior art, and `+1ms` effects observed through 2ms relative timing bins may be representation/bin-boundary sensitive. Current #34 may test only the already-frozen representation-specific interventional route question. Continuous-time/multi-resolution robustness, matched synfire/polychronous reductions and compact temporal-FSM reductions require a fresh successor if later warranted.

Candidate #35 Discovery R1 closed prospectively under SUB without response exposure. R91 moves #35 to `SYSTEM / ARCHITECTURE_STUDY / OPEN_DEVELOPMENT / preformal_eligible=false`, queued for deterministic clone/reset/queue-cap/response-serializer implementation and synthetic non-result validation only. Response-bearing candidate execution is STOP. Local membrane potential/adaptation/threshold/decay remains the strongest ordinary reduction; same-object SYSTEM→MECHANISM uplift is prohibited.

H7 remains exact R5 `2f30b93f8f3cf226ef55ed5af7e341089d2c3c80`, PREIDENTITY COMPLETE with exact-head CI `35794233612` and preidentity `35794233687` green, but on `FORMAL_HOLD_PROVENANCE_GATE`. PF-R1 exact bytes remain unpreserved because Utility has no matching schema-v2 assignment/current decision. No rerun/reconstruction/rescore is allowed.

H7 exact decision:

`STOP_H7_FORMAL_ONEWAY_R5_PREIDENTITY_COMPLETE_PENDING_PF_R1_SCHEMA_V2_MACHINE_AUTHORITY_AND_EXACT_BYTE_DURABLE_PRESERVATION_THEN_REQUIRE_FRESH_ANALYST_AUTHORIZATION`

## Authoritative repository state

Stable `main` remains `ebed6abfa941c83b36d2e3bddd04e7c5fb0dbe9d`. Authoritative annotated `evidence/*` remains five tag objects. Tag-form `formal/*`, `sealed/*`, `freeze/*` remain empty. No H7 control/STARTED or preserve ref exists. Historical official consumed identities remain seven; new consumption is zero. PR #148/#149 remain open/unmerged/mergeable. Repository rulesets remain zero.

## Funnel

Canonical population remains `35 = 14 MECHANISM / 21 SYSTEM`. Lifecycle becomes `ACTIVE 0 / QUEUED 2 / NONTERMINAL_HOLD 1 / TERMINAL_FOR_CURRENT_OBJECT 32`. Development phases remain `OPEN 4 / RESULT_EXPOSED 31 / canonical CONSUMED_ONE_WAY 0`. PRE_FORMAL eligible/READY becomes `2/2`: H7 READY but FORMAL-held and #34 READY for development PRE_FORMAL. Classification completeness is `35/35`.

Four-layer queues: Discovery empty; Architecture has #35 SYSTEM queued; PRE_FORMAL has #34 MECHANISM queued; FORMAL has fresh one-way authority zero.

SYSTEM-terminal successor accounting is unchanged: assessed 20; realized fresh SYSTEM successor one (#32→#33); unrealized fresh SYSTEM potential 16; fresh MECHANISM successors from SYSTEM terminalizations zero; none #14/#27/#28.

## Inputs

Control R40 remains strategic prior and does not override fresher direct research refs. MAIN mailbox remains stale at #34 R1 completion, while direct #34 R2 repository truth is newer. SUB R90 Discovery R1 prospectively closes #35's prime/anchor/cue/reset/signature/reduction contract. Independent Audit R7 remains the H7 prediction-only-raw → immutable-preserve → target-side-scorer hard floor. Methodology R77 supports #34 OPEN development and explicitly says READY after R2 should be judged by well-defined/informative next test, not prior victory. Repository Steward G11 reports no immutable incident; its old #34 visibility gap is superseded by direct R1/R2 refs. Utility remains fail-closed with no matching machine authority and no PF-R1 preservation.

## Phenomenon-first / supply

R91 performs a due full `PREFETCH_SHADOW` read-only scan because R88 was the last full scan and R35/#34/#35 are material deltas. No proposal is retained. A timing-resolution/synfire/FSM #34 successor would either depend on the unknown active result or is currently an ordinary future reduction, so it is not prefetched. #35 is already canonical. `shadow_standby_queue=[]`. Rolling actual autonomous theory-backward selections remain `MECHANISM / SYSTEM / SYSTEM = 1/3`.

## Top 3

1. #34 PRE_FORMAL R1 using unchanged R2 contract/head and one prospectively bound development surface/checkpoint — `MECHANISM / OPEN / GO_DEVELOPMENT_ONLY`.
2. #35 Architecture R1 deterministic implementation plus synthetic non-result validation — `SYSTEM / OPEN / GO_NON_EVIDENTIARY`.
3. PF-R1 valid schema-v2 machine-authority closure plus exact existing-byte preservation — provenance-only / `HOLD_EXECUTION`.

## Prospective contingency

For #34, if the unchanged R2 contract can be wrapped with fresh PRE_FORMAL execution authority and exact input binding without scientific redesign, GO. After meaningful development result exposure, only science-invariant repair stays on the same revision; metric/intervention/comparator/input/resource/falsifier/success changes must version or spawn a fresh successor. Negative or equivalence-class outcomes are valid; do not add stronger arms to obtain a preferred result. PRE_FORMAL results never accumulate into FORMAL evidence.

For #35, implement/synthetically validate only. If the fixed contract is unreachable, STOP and reassess; do not alter its 256ms cap/cue/reset semantics after candidate response because candidate response is not yet authorized.

For H7, Utility remains no-op until machine authority exists. If it later exists, preserve only existing PF-R1 exact bytes/hashes; retrieval failure is STOP. Even after preservation, require a fresh Analyst generation before any one-way action.

## Mutation statement

This generation performs no scientific experiment, dispatches no research workflow, consumes no one-way identity, merges no research PR, mutates no immutable/evidence/preserve/control ref, changes no scheduler definition, creates no Utility authority, and rewrites no historical PASS/FAIL. Only the designated Evidence Analyst latest/state/history surfaces are written.
