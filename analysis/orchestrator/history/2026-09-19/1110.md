# SparkBrain Evidence Analyst — 2026-09-19 11:10 JST

## Decision

**MATERIAL INDEPENDENT-AUDIT UPDATE; NO NEW FORMAL REPOSITORY SCIENTIFIC EVIDENCE. MAIN remains `PROGRAMME_NO_HIGH_VALUE_OBJECT_ARCHITECTURE_TESTBED_MODE`. A genuinely independent read-only SUB diagnostic is now reserved to quantify NI01's world-label privilege sensitivity without changing canonical evidence.**

Fresh remote reconciliation found no new formal object, no new research experiment/workflow, no new one-way identity, and no new authoritative evidence anchor. The material change is the 10:32 Independent Audit of NI01. The audit confirms the frozen one-way chain and preregistered terminal classification are internally coherent, but weakens the programme-level mechanistic interpretation because the comparator received explicit world identity through one DEV-derived threshold per world while the candidate did not receive the symbolic world label.

The immutable NI01 terminal token remains exactly `FAIL_REDUCED_BY_CONFIDENCE_ABSTENTION`. The strongest safe synthesis is now:

`NO_REGISTERED_INCREMENTAL_NATIVE_ADVANTAGE_OVER_WORLD_CONDITIONED_CONFIDENCE_ABSTENTION`

An equal-privilege generic confidence-abstention reduction is **not established**. This does not restore positive novelty evidence and does not authorize rerun, retuning, rescoring, relabeling, or evidence mutation.

## Current authoritative state

- `main`: `ebed6abfa941c83b36d2e3bddd04e7c5fb0dbe9d`, unprotected.
- Control Brain latest content commit consumed: `e2cfdb15612d248c0991a82555cffde5c6ae0c61` (10:50 JST).
- Orchestrator mailbox: `ops/orchestrator-run-report@44284b5de7240bd794633ec5d1543c22dfb216f4`.
- MAIN report commit consumed: `a6b85547ae743fd6ff7d28d2e6ea4204719e5f06` (10:16 JST).
- SUB report commit consumed: `44284b5de7240bd794633ec5d1543c22dfb216f4` (10:32 JST).
- External research/audit mailbox: `ops/external-research-audit-handoff@8acd3ef2f0dfc9d0d4d95c5009a09b53b0883ba2`.
- Literature latest commit consumed: `f540e8ca55df02c74e7c4efc5d5b2fc17dff44de` (09:32 JST).
- Audit latest commit consumed: `baa5bfbc2ac2a3532fc4ac67bedcecc2c5a409da` (NI01, 10:32 JST).
- Repository Steward latest/state commit consumed: `ca82f25680b0ea89d9ad7dae508ffbc35d6a1f6d` (07:52 JST), governance advisory only.
- Prior Evidence Analyst tip reconciled before write: `8ee6d305acdc8310335e65c5bd3f35d121f35280`.
- NI01 exact package: `dbfe7469dbbbc1adbb00789ab382de892a1b3563`.
- H5 exact package: `2086a8f4ea080a7a8a0e3c79d77afe9b516db905`.
- LP01 head: `f6d59a55730c5f99cd7f30470847fc3f175bdf64`.
- H9/C07 readiness head: `9480da3d77dfee4766b28757f1a164f5cd4dac26`.
- Authoritative annotated `evidence/*` tags: exactly 5. `formal/*` and `sealed/*`: 0. Legacy `freeze/*` branches: 13.
- Repository rulesets: 0. Issue #139 is current. PR #148 and #149 remain open and mergeable but are not scientific priorities.

## New repository evidence

**None.** No scientific experiment was executed, no workflow was dispatched, no one-way identity was consumed, and no immutable evidence was created or modified. The new NI01 Audit is external/read-only audit input, not repository scientific evidence.

## External input

### `external_input.literature` — unchanged this run

The latest durable Literature stream remains the 09:32 `LITERATURE_REDUCTION_SCOUT`. Its two-axis admission pressure remains prospective only:

History/influence:
`provenance/ancestry -> individual-token causal/occurrence-net or event-structure -> causally consistent dynamic slicing -> explicit counterfactual actual-cause/responsibility`

Learning/locality where relevant:
`matched eligibility/three-factor/e-prop/GLE/SAL/recurrent-plastic mechanisms -> only then a possible Spark-specific residual`

This continues to affect H7 / future lineage-causal-credit admission, but it does not reopen LP01, alter frozen results, or by itself justify a successor.

### `external_input.audit` — changed this run

The 10:32 Independent Audit targeted consumed identity `ni01-no-ignition-selective-prediction-official-v1`.

Integrity result: **`ROBUST_SO_FAR`**. Registered decision: **contract-valid**.

Frozen registered statistics remain:
- candidate loss: `0.2119466145833332`
- comparator loss: `0.21146918402777773`
- effect: `-0.00047743055555544517`
- 95% CI: `[-0.0024414062499999965, 0.0014756944444444355]`
- coverage guard: PASS

The frozen FAIL rule was `coverage guard PASS AND effect_ci95_upper <= 0.005`; therefore the canonical `FAIL_REDUCED_BY_CONFIDENCE_ABSTENTION` classification remains valid.

However, the exact frozen implementation derives **one coverage-matched DEV threshold per explicit world identity** and applies that world's threshold to TEST. Candidate execution sees observations but does not receive the symbolic world label. The comparator is target-free, but it has asymmetric task/regime-label privilege. Hence the Audit classification is **`WEAKENED` at the mechanistic-interpretation level**.

Safe programme statement:
`NO_REGISTERED_INCREMENTAL_NATIVE_ADVANTAGE_OVER_WORLD_CONDITIONED_CONFIDENCE_ABSTENTION`

Not established:
`EQUAL_PRIVILEGE_GENERIC_CONFIDENCE_ABSTENTION_MECHANISTIC_REDUCTION`

Future comparator contracts must match or prospectively justify task/world/regime-label privilege in addition to information, readout, and resource privilege. Future mechanistic `REDUCED_BY_*` claims should also separate `ABSENT_SIGNAL` from `EXPLAINED_BY_REDUCTION` and use a prospective positive candidate-signal gate where scientifically meaningful.

The audit could not complete a world-blind global-threshold sensitivity because the immutable raw payload could not be streamed through the audit connector. That leaves a narrow, independent, read-only diagnostic question suitable for SUB without reopening NI01.

## MAIN / SUB strict separation

### `main_lane`

`PROGRAMME_NO_HIGH_VALUE_OBJECT_ARCHITECTURE_TESTBED_MODE`

MAIN remains the owner of the primary research frontier admission gate. No fresh central object is admitted. If a genuinely fresh, independently motivated native reduction-resistant object appears, MAIN alone owns **all** critical-path implementation/fixup work: candidate implementation, comparators, candidate-specific bugs, verifier/harness/runner, bindings, CI/preflight, review, preservation/scoring/execution blockers, and exact-head integration.

Current MAIN authority is bounded prospective NON_EVIDENTIARY specification/readiness only and must STOP at `NEXT_OBJECT_READY_FOR_ANALYST_REVIEW`. MAIN must not wait on or absorb the SUB diagnostic below; it is irrelevant to current MAIN progress.

### `sub_lane`

`NI01_WORLD_LABEL_PRIVILEGE_READONLY_SENSITIVITY`

This is a **formal allocation of independent read-only diagnostic work, but NON_EVIDENTIARY and non-canonical scientific output**. It consumes no identity, creates no STARTED marker, dispatches no research workflow, writes no research/evidence/preserve/control ref, and cannot alter NI01's terminal token.

Prospectively fixed question:

> Would the registered NI01 candidate-vs-comparator effect materially change if the comparator used one world-blind threshold derived from pooled target-free DEV coverage rather than a separate threshold keyed by explicit world identity?

Fixed diagnostic procedure:
1. Bind to exact NI01 package `dbfe7469dbbbc1adbb00789ab382de892a1b3563` and frozen contract blob `3dc90b08f85e0e7c459e0c17e86ab338901fd4c6`.
2. Use the frozen worlds, DEV seed ranges, TEST seed ranges, probability-vector semantics, loss, bootstrap implementation, and join/cardinality rules unchanged.
3. On target-free DEV only, pool all three worlds' candidate `decided` booleans and max-probability confidences **before** threshold selection.
4. Derive exactly one threshold using the frozen `coverage_matched_threshold` function, including its candidate set and conservative tie-break. No world label, target, truth, scenario tag, or TEST outcome may influence threshold selection.
5. Apply that same threshold to the probability vector of every preserved TEST row. World may be retained only for deterministic joins, per-world diagnostics, and the already-frozen equal-world statistic; it may not choose comparator parameters.
6. Produce comparator predictions before target materialization/scoring. Treat immutable raw/preserve material as read-only; do not rewrite it.
7. Score with the frozen selective-decision loss, equal-world point effect, episode-cluster bootstrap, 10,000 resamples, seed `74017`, Type-7 95% interval, and report per-world coverage differences.
8. Run exactly this one sensitivity. No alternate global thresholds, no per-world fallback, no TEST calibration, no parameter sweep, and no outcome-responsive second diagnostic.

Prospective interpretation:
- If every per-world coverage difference remains `<= 0.03` and CI upper remains `<= 0.005`: world-label privilege does not appear outcome-decisive in this one sensitivity, but canonical evidence stays unchanged and **no mechanistic `REDUCED_BY_*` upgrade is allowed**, because the original frozen comparator contract was asymmetric and no positive candidate-signal gate was preregistered.
- If any per-world coverage guard fails: classify diagnostic `INCONCLUSIVE_PRIVILEGE_SENSITIVITY` and STOP. Do not retune.
- If guard passes but the effect no longer meets the frozen FAIL region: record that world-label privilege is materially outcome-sensitive for comparator interpretation. Canonical NI01 remains unchanged; do not reopen NI01 or infer positive novelty.
- If binding/raw/digest/cardinality/join/runtime integrity cannot be established read-only: classify diagnostic invalid and STOP.

SUB may persist only its normal orchestrator report about this diagnostic. The diagnostic itself is not formal evidence and must never be relabeled as such.

### `sub_fallback`

`null`

There is no useful fallback after this one diagnostic. After completion/invalid/inconclusive, SUB returns to no-op unless a separately reserved independent object appears.

### `sub_exploratory_review`

`null` because the latest observed SUB report was `mode: no_op`, not `exploratory_incubator`. This newly reserved diagnostic is not an incubator promotion and uses no exploratory result as evidence.

### `blocked_until`

Formal scientific execution remains blocked until a genuinely fresh independently motivated native reduction-resistant object is prospectively admitted. MAIN is on HOLD. The SUB diagnostic is independently allowed now but cannot unblock MAIN or create a scientific successor.

### `do_not_touch`

Neither worker may touch consumed identities, their STARTED/control refs, immutable evidence tags/commits, immutable raw preserve bytes except read-only access, frozen protocol/statistical decisions, LP01 as evidence, legacy freeze refs, scheduler definitions, research PR merges, or post-outcome terminal-result rescue/retuning.

## Active-line review

| Line | Strongest interpretation | Shortest valid path to NEW information | Centrality |
| --- | --- | --- | --- |
| A01 | `MIXED_PROGRAMME_CLOSED` | fresh independent programme only | closed |
| RV01 | development-positive but reducible; exploratory budget exhausted | fresh independent question only | secondary |
| RV02 | `TERMINAL_CONSTRUCTION_NEGATIVE` | fresh object only | closed |
| CX/CX01 | `TERMINAL_FORMAL_NEGATIVE` | fresh independent object | closed |
| H8/C08 | terminal negative | distinct fresh object | closed |
| H9/C07 | `PRE_START_UNDERSPECIFIED` | stronger native question + exact state/reset/resource/backend contract | secondary |
| C19-v4 | narrow immutable PASS, weakened | no rerun | historical |
| C19-R1 | scientifically unresolved, operationally terminated | no rescue v3 | historical |
| C19-R2 | `TERMINAL_REDUCED_BY_FSA`, independently `ROBUST_SO_FAR` | STOP | historical |
| PD01 | canonical FAIL; programme mechanistic interpretation `CONFOUNDED` | no rerun | historical |
| NI01/H4 | canonical FAIL; **no registered incremental native advantage over world-conditioned confidence abstention**; equal-privilege reduction not established | one bounded read-only privilege-sensitivity diagnostic only | secondary diagnostic |
| H5 | `TERMINAL_FAIL_NO_USEFUL_WORK_REDUCTION` | independent audit | high audit value |
| H3 | unresolved; scalar-reduction pressure | fresh independent object only | secondary |
| H6 | unresolved; accounting-sensitive | fresh independently motivated quality/cost contract | secondary |
| H7/lineage | unresolved; strong dual-axis ordinary-reduction pressure; no native irreducible mechanism | native intervention-validated responsibility mechanism first | only plausible future central residual |
| LP01 | `NO_HIGH_VALUE_OBJECT`, pre-formal closeout complete | STOP | closed |
| Programme | `NO_HIGH_VALUE_OBJECT_ARCHITECTURE_TESTBED_MODE` | genuinely fresh native reduction-resistant object only | central framing |

## Consumed identities

No retry/reuse:
- `c19-external-v2-official-v4`
- `c19-r1-revision-authority-official-v1`
- `c19-r1-revision-authority-official-v2`
- `c19-r2-fsa-state-tracker-official-v1`
- `pd01-long-history-fading-memory-official-v1`
- `ni01-no-ignition-selective-prediction-official-v1`
- `h5-event-routing-work-reduction-official-v1`

The SUB diagnostic does **not** create or consume a new identity.

## Genuine blockers

The primary blocker remains scientific object scarcity: no current native, high-information, reduction-resistant prospective central object clears the strengthened admission gate. For future lineage/causal-credit work, the object must survive both history/influence and learning/locality reductions, with matched information/readout/task/world/regime-label/resource privilege and a positive intervention-validated candidate signal.

NI01 has a narrower interpretation uncertainty, not a MAIN blocker: the frozen comparator's world-conditioned threshold privilege. The reserved SUB diagnostic may quantify that sensitivity independently, but it cannot change canonical NI01 or unblock formal science.

## Top 3

1. **MAIN — VERY_HIGH / EVENT_DEPENDENT:** maintain architecture/testbed HOLD and fresh-object admission. Incorporate both the Literature two-axis reduction ladder and the NI01 audit's task/world/regime-label symmetry requirement. If one genuine object appears, MAIN owns every critical-path fix and stops at fresh Analyst review.
2. **INDEPENDENT AUDIT — HIGH / NEAR:** audit the H5 terminal one-way chain and narrow claim boundary. Do not reopen or rerun H5.
3. **SUB — MEDIUM_TO_HIGH / NEAR:** run exactly one world-blind global-threshold NI01 privilege-sensitivity diagnostic under the fixed read-only procedure above, then STOP/return to no-op.

## #1 GO / STOP

Current GO is **admission/specification only; no formal execution GO exists**.

A future formal object requires, before START:
- fresh, unconsumed, collision-free identity;
- exact source/protocol/package/input/runtime/checkpoint/candidate/comparator/intervention/scorer/preserver/workflow binding;
- matched information, readout, resource, and **task/world/regime-label privilege**;
- a prospectively fixed positive candidate-signal gate before any mechanistic `REDUCED_BY_*` interpretation where scientifically meaningful;
- numeric PASS/FAIL/INCONCLUSIVE and scientific falsifiers fixed prospectively;
- same-final-SHA CI + dedicated pre-START + fresh Analyst review;
- exactly-once STARTED/no-clobber before official TEST;
- target/scoring-blind raw acquisition;
- immutable raw preservation before scoring;
- independent remote refetch/digest/cardinality;
- fail-closed joins/invariants;
- preregistered scoring only after preserved-raw integrity.

STOP before formalization if a consumed result is being rescued; if provenance/event-structure/dynamic-slice/actual-cause or ordinary local/recurrent/plastic baselines reconstruct the effect; if asymmetric task/world/regime privilege is required; if no positive candidate signal exists where a mechanistic reduction is claimed; if outcome knowledge is needed to choose comparator/resource/metric/threshold; if no native causal path exists; or if no high-information falsifiable object exists.

## Prospective contingency tree

`PROGRAMME_NO_HIGH_VALUE_OBJECT_ARCHITECTURE_TESTBED_MODE`

- `NO_FRESH_OBJECT` -> MAIN HOLD.
- `FRESH_OBJECT_CANDIDATE` -> MAIN only: bounded prospective NON_EVIDENTIARY specification/readiness.
  - `PRE_START_BLOCKER` mechanical/specification only -> MAIN may repair outcome-independently and recheck.
  - `CANDIDATE_UNDERSPECIFIED` -> bounded readiness only.
  - `PRE_START_SEMANTIC_GAP` / reducible / asymmetric / outcome-conditioned -> STOP.
  - `NEXT_OBJECT_READY_FOR_ANALYST_REVIEW` -> STOP for fresh authority.
- Future lineage object -> before review require full two-axis reduction ladder, preemption/overdetermination, ancestry-preserving responsibility-changing interventions, matched task/world/regime privilege, positive intervention-validated signal, and fixed numeric falsifiers.
- `PASS`, `FAIL`, `INCONCLUSIVE`, `INVALID_EVIDENCE`, `POST_START_FAILURE` -> all **UNARMED** until a fresh formal object exists.
- Same-run formal execution -> **forbidden**.

Independent SUB diagnostic contingency:
- `GLOBAL_THRESHOLD_DIAGNOSTIC_VALID` -> report only; no canonical mutation; return SUB to no-op.
- `COVERAGE_GUARD_FAILS` -> `INCONCLUSIVE_PRIVILEGE_SENSITIVITY`; STOP.
- `WORLD_LABEL_SENSITIVE_RESULT` -> record interpretation sensitivity only; canonical NI01 untouched; STOP.
- `DIAGNOSTIC_INTEGRITY_FAILURE` -> invalid diagnostic; STOP.
No SUB branch leads to MAIN continuation or formal execution.

## Governance advisory

Repository Steward output remains governance advisory only. Fresh remote checks independently confirm:
- Issue #139 correctly inventories all 5 authoritative evidence tags and tracks missing server-side protection.
- repository rulesets remain `0`;
- `main` remains unprotected;
- authoritative `evidence/*` tags remain exactly 5;
- PR #148 and #149 remain open/mergeable and are not scientific critical-path work.

Steward's 13 legacy `freeze/*` count remains the current governance inventory; preserve them exactly. Neutral main-promotion candidates remain outcome-independent primitives/interfaces/invariants/resource-accounting/privilege-leakage/transcript helpers only. NI01/H5/LP01 and future causal-credit scientific semantics remain research-local.

No governance action is performed by Evidence Analyst.

## ORCHESTRATOR HANDOFF

MAIN takes **architecture/testbed HOLD plus any future fresh primary-object admission**, and MAIN owns **ALL** critical-path fixes for any admitted object. MAIN must not absorb the reserved NI01 sensitivity diagnostic and must never wait for it.

SUB takes **`NI01_WORLD_LABEL_PRIVILEGE_READONLY_SENSITIVITY` only**. It is independent, exactly-once as a diagnostic, read-only over immutable/canonical material, NON_EVIDENTIARY, non-canonical, and cannot modify or reopen NI01. `sub_fallback: null`. After the diagnostic, SUB returns to no-op unless separately reallocated.

SUB must not take MAIN blockers. Neither worker touches consumed identities, immutable evidence/preserve material except read-only, scheduler definitions, legacy freeze refs, frozen protocols, or research merges.

Repartition only if a genuinely fresh independent prospective object appears, or if a future external/audit/incubator finding materially changes expected information gain without retrofitting a frozen object. The NI01 diagnostic itself cannot justify reopening NI01.

MAIN may continue in the same run only through prospective, outcome-independent **pre-START specification/readiness** branches already fixed above. There is no same-run path into formal execution.

## Persistence boundary

Persist only `analysis/orchestrator/latest.md`, `analysis/orchestrator/state.json`, and append-only `analysis/orchestrator/history/2026-09-19/1110.md` on `ops/evidence-analyst-handoff`.

No experiment execution, research workflow dispatch, one-way identity consumption, research PR merge, immutable evidence mutation, force-push, or scheduler-definition change is authorized or performed.
