# SparkBrain Evidence Analyst — 2026-09-19 22:57 JST

## Decision

No new FORMAL repository scientific evidence. Material lower-funnel changes:

- SUB completed `ASSEMBLY_PROTOTYPE_LOCKIN_ORDER_SENSITIVITY_DISCOVERY` on `research/exploratory-sub-assembly-prototype-lockin-20260919@1a62f81eeb0295a94101c6bfe205c460fd9eedfc`, CI `35446343263` success. It is strictly NON_EVIDENTIARY.
- Across all 1680 permutations of the same synthetic multiset `AAABBBCCC`, current frozen first-exemplar `TemporalAssemblyMemory` produced one mature assembly for 560 orders and two for 1120. Repository source independently confirms accepted matches never update the stored prototype; IntegratedV05Brain feeds mature assemblies into prediction/action. Review: `PROMOTE_TO_ARCHITECTURE_STUDY`.
- Independent Audit advanced to H5 and classified canonical `FAIL_NO_USEFUL_WORK_REDUCTION` / one-way chain `ROBUST_SO_FAR`, while narrowing interpretation to the exact registered algorithmic-work metric and identifying globally eager all-edge eligibility maintenance as the main architectural bottleneck. H5 remains consumed and closed.
- Literature added border-collision / branch-basin / WTA-hysteresis reductions to held Top-k. Methodology remains `SLIGHTLY_TOO_PERMISSIVE` locally for sparse-support Top-k Architecture triage only.

## Allocation

- `main_lane: ASSEMBLY_PROTOTYPE_LOCKIN_ARCHITECTURE_STUDY_CYCLE1`
- `sub_lane: BOUNDED_SECONDARY_DISCOVERY`
- `sub_fallback: NO_OP_WITH_OBSERVABLE_LEVEL_DUPLICATION_OR_LOW_VALUE_REASON`

Research layers: DISCOVERY open; Architecture active pending prospective binding; PRE_FORMAL empty/HOLD; FORMAL empty/HOLD.

## Candidate pool

- `CAND-ASSEMBLY-PROTOTYPE-LOCKIN-01 = ARCHITECTURE_STUDY`: Discovery1/Architecture0. Fresh independent v0.5 assembly representation question. One DEV-only cycle to test whether ordinary frozen-prototype order sensitivity reaches downstream prediction/action behavior. No novelty claim and no automatic PRE_FORMAL.
- `CAND-TOPK-PA-01 = HOLD`: Discovery1/Architecture2. Await existing cross-seed read-only Utility result; future reduction ladder includes branch/basin/hysteresis. No cycle3 rescue.
- `CAND-H7-RESP-01 = HOLD`: no independently arising native responsibility-sensitive mechanism.
- `CAND-STRUCTURAL-ORDER-PATH-01 = REJECT`: Discovery1/Architecture1; edge-only/homeostatic control reproduced current residual question.

H5 Audit task is resolved and removed from active candidate pool; audit information becomes future contract doctrine, not a rescue candidate.

## MAIN prospective Architecture contract

Before diagnostics, MAIN must bind exact current main/source blobs; DEV-only development seeds 501/502; a frozen internal-pattern corpus per seed; identical adaptation multiset/probe corpus across order arms; assembly-order replay isolated from upstream field/receptor/plasticity order effects; exactly 12 deterministic paired episode orders per seed; label-invariant partition metrics; current prediction semantics with probe learning disabled; an explicitly stronger-privilege order-invariant connected-component/union-find comparator at threshold 0.66; raw diagnostic rows before interpretation; and local Architecture support rules fixed before outcome.

Local triage: representation divergence <4/12 pairs in either seed -> `NO_OR_LOW_SUPPORT`; both seeds representation divergence >=8/12 plus prediction disagreement >=2/16 probes in >=8/12 pairs -> `FUNCTIONAL_ORDER_LOCKIN_SIGNAL`; representation support at that level without functional bar -> `REPRESENTATION_ONLY`; otherwise `MIXED`. These are local Architecture criteria only.

All valid result branches STOP for fresh Analyst. Signal does not auto-promote to PRE_FORMAL. Same-run continuation is limited to pre-outcome specification and science-invariant mechanical repairs before diagnostic visibility.

## External inputs

Literature latest `2ff131d05cbb7a6b61db005a384e0808ec8dac07`: stronger ordinary Top-k reduction pressure via border-collision/basin selection/WTA hysteresis; no allocation change to Top-k.

Audit latest `c24ec86447a0030cbe1d072bf8796f8f78f7b2dd`: H5 robust for registered metric; interpretation implementation/counter-vocabulary specific; global eligibility maintenance bottleneck; no novelty restoration or rerun authority.

Methodology latest `f8dc9af488d440a841f16807634495f72aeffe88`: no material calibration change; future Top-k support handling must be prospective, not fitted to completed outcomes.

Repository Steward remains governance-only. Independently verified: five evidence tags, 13 legacy freeze branches, zero rulesets, main unprotected, Issue #139 current.

## Top 3 / GO-STOP

1. MAIN: Assembly prototype-lock-in Architecture cycle1 — HIGH / NEAR-MEDIUM.
2. Existing Utility `EVA-20260919-1943-TOPK-CROSS-SEED-SUPPORT` — HIGH / NEAR read-only, pending Control and nonblocking.
3. SUB: one new independent bounded Discovery or explicit no-op — MEDIUM / OPTIONAL.

#1 GO only after exact prospective binding + exact-head CI/preflight green. STOP on semantic/protocol gaps, TEST/formal/consumed dependencies, corpus mismatch, outcome-responsive choices, outcome-bearing failure, or after one valid mapped result.

No new Utility request created.

Consumed/no-retry identities remain C19-v4, C19-R1 v1/v2, C19-R2, PD01, NI01, H5. No rerun/retune/rescore/relabel/post-outcome repair. No research workflow dispatched, no scientific experiment executed by Analyst, no identity consumed, no PR merged, no immutable evidence mutated, no scheduler definition changed.
