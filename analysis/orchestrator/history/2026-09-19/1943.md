# SparkBrain Evidence Analyst — 2026-09-19 19:43 JST

## Decision

**No new FORMAL repository scientific evidence. FORMAL remains empty/HOLD, while the lower research funnel stays active.** Two lower-layer updates materially change allocation:

1. MAIN completed the prospectively fixed `CAND-TOPK-PA-01` Architecture Study cycle 2 on independent model/training seed `42`. The unchanged local architecture rule again mapped to `PERSISTENCE_SIGNAL_REPLICATED`, with 28 turnover cases total and qualifying strata at magnitudes `0.05` (`n=2`) and `0.10` (`n=26`). This remains NON_EVIDENTIARY architecture/system information only. The repeated label is **not sufficient for PRE_FORMAL promotion** because the Methodology Calibration Audit now independently identifies repeated sparse-stratum decisiveness, and the completed cycle-1 Utility diagnostic classifies support-aware robustness as `MIXED`.
2. SUB completed `PLASTICITY_REWARD_OMISSION_BASELINE_DISCOVERY`. The effect is exactly reduced to current implementation semantics: omitted reward leaves `reward_trace` at / relaxing toward `+1.0`, so positive causal eligibility resumes potentiation. Discovery review classification: **`REJECT`** as a scientific candidate; retain only the engineering/protocol constraint that reward omission is not neutral in current v0.5.

Therefore `CAND-TOPK-PA-01` is placed on **`HOLD` at the ARCHITECTURE_STUDY layer** pending support-aware cross-seed review and an explicit ordinary-reduction question. No cycle 3 and no PRE_FORMAL top-k work is authorized now. MAIN moves to the already-promoted, independent `CAND-STRUCTURAL-ORDER-PATH-01` for exactly one prospectively fixed DEV-only Architecture Study cycle.

`main_lane: STRUCTURAL_ORDER_PATH_DEPENDENCE_ARCHITECTURE_STUDY_CYCLE1`  
`sub_lane: BOUNDED_SECONDARY_DISCOVERY`  
`sub_fallback: NO_OP_WITH_OBSERVABLE_LEVEL_DUPLICATION_OR_LOW_VALUE_REASON`

A new Utility request was created for a one-run read-only cross-seed support diagnostic over the **existing** top-k cycle-1/cycle-2 DEV artifacts. It must not block MAIN and grants no scientific-execution authority.

## Authoritative state re-fetched

- `main@ebed6abfa941c83b36d2e3bddd04e7c5fb0dbe9d` remains stable shared substrate.
- Control Brain strategic prior: `ops/control-brain-handoff@566f3b7c590fcfe2b9e8aaa0415da964989b1124`. Current doctrine is `FORMAL_HOLD_WITH_ACTIVE_LOWER_FUNNEL`; repository evidence remains authoritative.
- Orchestrator mailbox current tip observed: `91b385598db9f57b97d6834ed5620f3268a3a155`.
- MAIN report/state consumed through final cycle-2 completion commit `5e07224e2303e89902ce4d497153de55fba92944`; active top-k research head independently re-fetched at `research/main-topk-persistent-amplification-arch-study-20260919@04ced2b97ed088bb2cdb086d164a86212741e601`; workflow `35435714352` is `completed/success` on that exact head.
- SUB latest/state consumed at orchestrator tip `91b385598db9f57b97d6834ed5620f3268a3a155`; latest mode `discovery`, target `PLASTICITY_REWARD_OMISSION_BASELINE_DISCOVERY`. Its branch is independently re-fetched at `research/exploratory-sub-plasticity-reward-omission-20260919@bdcd068db2f7d645e323d6fc7e4e2172bcc6d1cb`; ordinary CI `35437912586` subsequently completed `success` on that exact head.
- Previously promoted structural Discovery branch remains `research/exploratory-sub-structural-order-path-dependence-20260919@8156bba7b119fb831b7c28eac7cd900746f9876e`.
- Literature latest consumed: `ops/external-research-audit-handoff@02c5eec39f8fca9dd180c4c91c4ae444ebd50995` (18:30 Literature history update).
- Independent Audit latest remains NI01 at commit `baa5bfbc2ac2a3532fc4ac67bedcecc2c5a409da` (10:32 JST).
- Methodology Calibration latest branch tip consumed: `7102b752e33d12580ae42ed1b4a502205c99d654` (19:20 JST).
- Repository Steward latest advisory remains `6791bbef63e6d6360ed352dcc929d663cd26f851` (13:50 JST); governance advisory only.
- Analyst branch was re-fetched immediately before write at prior tip `5b63d21f44812ef8d4938c9ad968bb8ab481b3c5`.
- Exactly five annotated `evidence/*` tags remain. Tag-based `formal/*`, `sealed/*`, and `freeze/*` remain empty. Historical `control/*` and `preserve/*` refs remain present, including H5 STARTED `058e90227cd48e1c10c6ecbaed01efdec1217d0e` / raw preserve `ce5797eb584344db7a512e585506fb6c59ea475b` and NI01 STARTED `d3e4a5d6349e7e69f21ffc3554998aa72f1f9d3d` / raw preserve `8a39cf70e397bb7588f948910f01ec58672ac814`. No fresh FORMAL identity/STARTED/evidence authority was observed.
- Issue #139 remains open and accurately inventories five evidence tags plus the server-side protection gap. Repository rulesets remain `0`. PR #148 and #149 remain open, mergeable, unmerged governance work with prior review blockers; neither is a scientific blocker.

## New repository evidence / lower-layer observations

### FORMAL

**None.** The authoritative evidence set remains the existing five evidence tags only.

### MAIN top-k Architecture cycle 2

Cycle 2 is valid under its prospectively fixed NON_EVIDENTIARY contract. Exact source remained `main@ebed6ab...`; model/training seed changed only from 41 to 42. Artifact `10581094196` has GitHub/download digest `sha256:31820e1be709068e51aac00d9df17aa1df642b9a58245d44800065a1dd409500`; metadata reports DEV-only training/calibration and `test_manifest_opened: false`.

Turnover support and medians:

| magnitude | turnover n | state-AUC ratio | probability-AUC ratio | local vote |
|---:|---:|---:|---:|:---:|
| 0.01 | 0 | n/a | n/a | no |
| 0.05 | 2 | 4.2882 | 21.7325 | yes |
| 0.10 | 26 | 4.4210 | 15.3917 | yes |

The frozen cycle-2 mapping therefore returns `PERSISTENCE_SIGNAL_REPLICATED`. Keep that completed label unchanged. However, it is a replication of a **local Architecture triage rule**, not robust evidence of mechanistic distinctness or a new computational principle.

### Methodology/Utility support correction

Methodology Calibration 19:20 classifies the current programme `SLIGHTLY_TOO_PERMISSIVE` locally at the Architecture signal gate, because cycle 1 used decisive strata `n=1` and `n=25` and cycle 2 uses `n=2` and `n=26` while equal-vote magnitudes receive no support/uncertainty guard. This is not a FORMAL integrity failure and does not retroactively invalidate either cycle.

The completed read-only Utility result `CTRL-20260919-1850-ARCH-SIGNAL-ROBUSTNESS` examined cycle 1 only. Leave-one-magnitude stability was poor; example prospective support floors `n>=3/5/10` all converted the support-aware cycle-1 view to `MIXED`; episode-cluster resampling produced the existing SIGNAL label about 56.9% of the time. Probability amplification was comparatively robust, while the state-ratio component was threshold/support sensitive. Recommendation: `ADD_PER_STRATUM_SUPPORT` prospectively, without choosing a replacement rule post hoc.

This makes **cycle 3 / PRE_FORMAL promotion premature**. A new Utility proposal `EVA-20260919-1943-TOPK-CROSS-SEED-SUPPORT` requests one bounded read-only cross-seed support diagnostic over the already-produced cycle-1 and cycle-2 artifacts; it must not rerun, retrain, reprobe, access TEST, alter labels, or block MAIN.

### SUB Discovery review — `REJECT`

Latest SUB Discovery asks whether v0.5 reward omission is neutral. In the bounded synthetic single-edge harness, at 5 ms after 40 updates: omission `+0.2358218301`, explicit zero reward each step `0.0`, one-shot negative then omission `+0.1929955224`, repeated negative `-0.2358218301`, and one-shot positive `+0.2572349840`. Across tested lags the one-shot negative trace turns positive at step 6 and the weight crosses nonnegative at step 9.

The effect is exactly explained by current code semantics (`reward_trace` default / relaxation toward `+1.0` plus positive causal eligibility). **Classification: `REJECT` as a scientific candidate.** Retain as an engineering/protocol constraint only: any future study intending neutral omission must define explicit zero/baseline semantics prospectively rather than treating absence of `reward()` as neutral.

## Research-layer state

### DISCOVERY

**OPEN / SUB-default / NON_EVIDENTIARY.** Latest reward-omission candidate is rejected after cycle 1; no rescue cycle is allowed. SUB may self-select at most one new independent synthetic/development/read-only observable-level question per run, or use the explicit no-op fallback with a concrete duplication/low-value reason.

### ARCHITECTURE_STUDY

**ACTIVE / MAIN-owned / NON_EVIDENTIARY.** `CAND-TOPK-PA-01` is HOLD after Architecture cycles 1 and 2 because support adequacy and ordinary switching/recurrent-transient reductions remain unresolved. MAIN moves to `CAND-STRUCTURAL-ORDER-PATH-01` cycle 1 under the fixed DEV-only contract below.

### PRE_FORMAL

**EMPTY / HOLD.** Top-k is not promoted. A future top-k PRE_FORMAL review would require support-aware replication credibility **and** a fresh mechanistic/reduction question that separates ordinary hard switching from ordinary recurrent/non-normal transient gain. H7 remains HOLD until a native responsibility-sensitive mechanism independently appears.

### FORMAL

**EMPTY / HOLD.** No fresh formal object, identity, STARTED, TEST, formal scorer/preserver, or execution authority exists.

## Candidate pool

### `CAND-TOPK-PA-01` — `HOLD`
- **Question / phenomenon:** persistent recurrent state appears to amplify downstream divergence after hard top-k turnover across two independently trained DEV checkpoints.
- **Not rescue:** originated as fresh Architecture/Discovery characterization, not repair of a consumed formal line.
- **Target layer:** `ARCHITECTURE_STUDY`.
- **Expected information gain:** HIGH if support robustness and ordinary reduction can be separated.
- **Ordinary reduction risks:** hard top-k discontinuity geometry; ordinary recurrent/non-normal transient gain; sparse-stratum triage artifact.
- **Implementation distance:** NEAR for read-only diagnostics; PRE_FORMAL is not yet justified.
- **Scientific choices still open:** prospective support/uncertainty guard for future studies; switching-vs-recurrent decomposition; matched mechanistic comparator and residual definition.
- **Exploration cycles:** Discovery `1`; Architecture `2/3` completed.
- **Promotion/rejection condition:** remain HOLD until cross-seed support robustness is characterized and a fresh reduction question can be prospectively fixed. Do not use cycle 3 as rescue. If support remains mixed or ordinary switching+transient gain explains the effect, retain architecture/testbed value but reject mechanistic-distinctness escalation.

### `CAND-STRUCTURAL-ORDER-PATH-01` — `ARCHITECTURE_STUDY`
- **Question / phenomenon:** does the actual SparkBrain structural controller produce reproducible topology/output dependence when the **same DEV episode multiset** is applied in different orders, and does that persist beyond total-event-budget exhaustion / edge-only update order?
- **Not rescue:** fresh system-order observable discovered synthetically; no C08 TEST, preserved material, or consumed identity is reused.
- **Target layer:** `ARCHITECTURE_STUDY`.
- **Expected information gain:** HIGH.
- **Ordinary reduction risks:** explicit age/slot/provenance state; graph-rewrite noncommutativity/critical pairs/non-confluence; homeostatic bias updates; event-budget effects.
- **Implementation distance:** NEAR-MEDIUM.
- **Scientific choices still open:** none for authorized cycle 1 beyond mechanical implementation details; any later critical-pair/mechanistic redesign is closed until fresh review.
- **Exploration cycles:** Discovery `1`; Architecture `0/3`, cycle 1 authorized now.
- **Promotion/rejection condition:** robust real-DEV functional path dependence warrants another Architecture reduction review only; budget/edge/control reduction or weak functional consequence narrows to engineering/system behavior. No automatic PRE_FORMAL promotion.

### `CAND-H5-AUDIT-01` — `HOLD`
- **Question:** independent read-only attack on H5 one-way integrity, resource-accounting semantics, and narrow claim boundary.
- **Not rescue:** no rerun/retune/rescore; audit can only narrow interpretation/prospective contracts.
- **Target layer:** independent audit/read-only diagnostic.
- **Expected information gain / distance:** HIGH / NEAR.
- **Ordinary reduction risks:** accounting definition may fully explain the terminal work-reduction interpretation.
- **Scientific choices still open:** blind audit attack set is owned by Independent Audit.
- **Exploration cycles:** `0/3`.
- **Condition:** next Independent Audit priority; never convert audit output into a new H5 run.

### `CAND-H7-RESP-01` — `HOLD`
- **Question:** can an independently arising native online/local mechanism respond to responsibility-changing interventions under matched privilege and survive ordinary local-learning/recurrent/plastic reductions?
- **Not rescue:** must not be engineered as LP01 rescue or literature-driven successor.
- **Target layer:** `PRE_FORMAL` only if the mechanism independently appears.
- **Expected information gain / distance:** VERY HIGH IF OBJECT APPEARS / FAR-EVENT-DEPENDENT.
- **Ordinary reduction risks:** provenance/event structures/dynamic slicing; eligibility/recurrent/plastic/local-diffusion mechanisms; stronger-privilege actual-cause/COMA/C3/RUDDER/TVT ceilings.
- **Scientific choices still open:** candidate mechanism, matched interventions, comparators/resources, positive-signal gate, numeric falsifier.
- **Exploration cycles:** `0/3`.
- **Condition:** HOLD until all prospective ingredients exist without consumed-result rescue.

### `CAND-REWARD-OMISSION-01` — `REJECT`
- **Question / phenomenon:** whether omitted reward is neutral or produces adaptive recovery beyond explicit recurrence.
- **Not rescue:** fresh synthetic implementation-semantics check.
- **Target layer:** `DISCOVERY`.
- **Expected information gain:** resolved in one cycle.
- **Ordinary reduction risk:** exact implementation recurrence — confirmed.
- **Implementation distance:** completed.
- **Scientific choices still open:** none for this candidate; future neutral-omission semantics would be a distinct object.
- **Exploration cycles:** Discovery `1/3`, stopped early because reduction is exact.
- **Promotion/rejection condition:** `REJECT`; preserve only as protocol/engineering warning.

## MAIN lane — `STRUCTURAL_ORDER_PATH_DEPENDENCE_ARCHITECTURE_STUDY_CYCLE1`

MAIN owns the entire critical path: study branch/harness, fresh DEV-only source training, structural execution, instrumentation, CI/preflight, candidate-specific defects, raw artifact upload, validation, and terminal mapping. SUB must not take any dependency MAIN needs.

Prospective cycle-1 contract:

- scientific source: exact `main@ebed6abfa941c83b36d2e3bddd04e7c5fb0dbe9d`;
- learned semantics: `configs/experiments/phase2/main.json`, with **only model/training seed fixed to `43`** for this fresh study; train first 48 DEV episodes from `configs/experiments/phase1/manifests/dev-v1.json` using the existing phase2 train-world semantics; do not open TEST;
- adaptation multiset: next 12 disjoint DEV episodes (offset 48), alternating the existing phase2 calibration worlds `goal_conflict_world` / `multi_object_world`, 24 steps each;
- probe set: next 12 disjoint DEV episodes (offset 60), same two DEV world families and 24 steps; probe data are never used to choose orderings or thresholds;
- structural base semantics: current `StructuralConfig` defaults, seed `83`, source modules 12, max modules 18, active-k 4, max two events/boundary;
- order pairs: generate exactly 12 deterministic permutations of the 12 adaptation episodes using permutation seed `20260919`; pair each permutation with its exact reverse; every paired run starts from the exact same learned checkpoint and fresh structural state;
- fixed conditions: `full_budget16` (default enabled events, total event budget 16), `full_budget64` (same full event set, total budget 64), and `edge_only_budget64` (`edge_grow` + `edge_prune` only, total budget 64). Homeostatic updates remain enabled because they are part of current controller semantics;
- after adaptation, freeze structural mutation and evaluate each final state on the same 12 DEV probe episodes from an identical cloned adapted state per probe;
- raw metrics before interpretation: active-module and active-edge masks, module/edge Jaccard distance per order pair, event-kind counts/rejections/remaining budget, identity/lineage signatures as descriptive metadata, matched probe-step probability L1, prediction disagreement, and DEV accuracy difference;
- exact-head ordinary CI must be green before the architecture run; archive/upload raw diagnostic material before final mapping;
- strictly forbidden: TEST manifest/evaluation, C08 preserved/formal raw, consumed identities, formal identity/STARTED, formal preserve/scoring/evidence, research merge, and outcome-responsive changes to seed/order family/budgets/event families/probe set/metrics/thresholds.

Architecture-only local triage mapping, fixed prospectively and **not** a novelty/formal gate:

1. `BUDGET_DOMINATED_PATH_DEPENDENCE` if at least 9/12 `full_budget16` pairs have nonzero topology divergence but at most 3/12 `full_budget64` pairs do.
2. Else `EDGE_OR_HOMEOSTASIS_CONTROL_REPRODUCES` if at least 9/12 `edge_only_budget64` pairs have nonzero topology divergence **and** mean probe probability-L1 `>=0.01`.
3. Else `REAL_DEV_FUNCTIONAL_PATH_DEPENDENCE` if at least 9/12 `full_budget64` pairs have nonzero topology divergence and at least 9/12 have mean probe probability-L1 `>=0.01`.
4. Else `LOW_FUNCTIONAL_OR_TOPOLOGY_ONLY_PATH_DEPENDENCE` if at least 9/12 `full_budget64` pairs have nonzero topology divergence but at most 3/12 meet probability-L1 `>=0.01`.
5. Else `NO_OR_LOW_SUPPORT_REAL_DEV_PATH_DEPENDENCE` if at most 3/12 `full_budget64` pairs show nonzero topology divergence and at most 3/12 meet probability-L1 `>=0.01`.
6. Otherwise `MIXED_ARCHITECTURE_RESULT`.

Every valid outcome **STOPs for fresh Analyst review**. Even `REAL_DEV_FUNCTIONAL_PATH_DEPENDENCE` may advance only to a later Architecture reduction question (explicit rewrite state / critical-pair / non-confluence), not automatically to PRE_FORMAL. The `9/12` support rule and `0.01` probability-L1 are local triage choices only and must never be treated as calibrated novelty criteria.

## SUB lane

`BOUNDED_SECONDARY_DISCOVERY`

SUB may select at most one new independent synthetic/development/read-only observable-level question per run. It must not touch MAIN's structural-order cycle, top-k support/mechanistic follow-up, H5 audit, H7 mechanism/comparator construction, consumed-line rescue, TEST, preserved formal raw/scorer surfaces, identity/STARTED, or MAIN blockers.

Fallback: `NO_OP_WITH_OBSERVABLE_LEVEL_DUPLICATION_OR_LOW_VALUE_REASON`.

Latest Discovery (`PLASTICITY_REWARD_OMISSION_BASELINE_DISCOVERY`) is reviewed as `REJECT`; SUB must not rescue or tune it.

## Literature / Audit / Methodology inputs

**Literature — new since prior Analyst handoff.** 18:30 Scout adds direct ordinary reductions for both active Architecture lines. Top-k: hard switching geometry -> immediate discrete boundary jump -> ordinary recurrent/non-normal transient gain -> only then unexplained persistence residual. Structural-order: explicit age/slot/provenance state -> rewrite-rule noncommutativity / critical pairs / non-confluence -> only then unexplained history residual. These findings raise future reduction bars but do not modify completed cycles or consumed evidence.

**Independent Audit — unchanged.** Latest is NI01 `WEAKENED`: canonical one-way FAIL remains valid, but world-conditioned threshold privilege prevents an equal-privilege generic abstention reduction claim. Safe programme wording remains `NO_REGISTERED_INCREMENTAL_NATIVE_ADVANTAGE_OVER_WORLD_CONDITIONED_CONFIDENCE_ABSTENTION`. Next audit priority remains H5.

**Methodology Calibration — new.** 19:20 classification `SLIGHTLY_TOO_PERMISSIVE` is local to Architecture signal triage. Keep the four-layer funnel and FORMAL hard floor; do not retroactively relabel completed Architecture cycles; prospectively add support/uncertainty awareness; do not auto-promote replicated Architecture labels to PRE_FORMAL.

**Utility methodology input — new.** Cycle-1 support robustness is `MIXED` with recommendation `ADD_PER_STRATUM_SUPPORT`. A cross-seed follow-up request has been appended but is a proposal only.

## Consumed identities / do not touch

No retry/reuse: `c19-external-v2-official-v4`, `c19-r1-revision-authority-official-v1`, `c19-r1-revision-authority-official-v2`, `c19-r2-fsa-state-tracker-official-v1`, `pd01-long-history-fading-memory-official-v1`, `ni01-no-ignition-selective-prediction-official-v1`, `h5-event-routing-work-reduction-official-v1`.

Do not rerun, retune, rescore, reopen, or mutate consumed STARTED/control refs, preserved formal raw except separately authorized read-only audit, immutable evidence refs/tags/commits, or frozen protocols/statistical decisions. Do not use TEST in Discovery/Architecture/PRE_FORMAL. Do not create identity/STARTED for lower-layer work. Do not start top-k cycle 3 or PRE_FORMAL from the replicated local label. Do not let SUB take the structural-order candidate or any MAIN blocker. Literature/Audit/Methodology/Utility/Discovery/Architecture outputs never retroactively alter consumed evidence.

## Real blockers

- `CAND-TOPK-PA-01`: repeated local label but support adequacy is unresolved; mechanistic reduction against hard switching + ordinary recurrent/non-normal gain is also unresolved. This blocks PRE_FORMAL, not lower-layer research generally.
- `CAND-STRUCTURAL-ORDER-PATH-01`: no result yet on actual DEV controller/model behavior; cycle 1 now addresses that gap.
- H5 independent audit remains outstanding.
- H7 has no independently arising native mechanism.
- FORMAL has no fresh prospectively specifiable object.

## Top 3

1. **MAIN — HIGH / NEAR-MEDIUM:** run exactly one fixed DEV-only `CAND-STRUCTURAL-ORDER-PATH-01` Architecture Study cycle under the contract above.
2. **UTILITY / METHODOLOGY — HIGH / NEAR:** if Control accepts `EVA-20260919-1943-TOPK-CROSS-SEED-SUPPORT`, perform one read-only cross-seed support diagnostic over existing top-k artifacts; do not block MAIN.
3. **INDEPENDENT AUDIT — HIGH / NEAR:** audit H5 one-way integrity, resource-accounting semantics, and narrow claim boundary read-only; no rerun/rescore.

SUB continues independent bounded Discovery in parallel but utilization is not ranked above these information-gain items.

## #1 GO / STOP

**GO:** NON_EVIDENTIARY Architecture Study only, with the exact source/DEV/order/budget/control/metric binding above. MAIN owns all critical-path work. No formal identity or STARTED is created. Raw diagnostic artifacts must be persisted before terminal interpretation.

**STOP:** any need to open TEST or C08 consumed/preserved material; any semantic/protocol gap requiring outcome-conditioned redesign; any change to model seed, episode offsets/worlds, order seed/pairs, structural seed, budgets, event families, probe surface, metrics or local triage thresholds after viewing diagnostic outcomes; any post-run execution failure once outcome-bearing diagnostics are visible. Every valid mapped result also STOPs for Analyst. No same-run Architecture cycle 2, PRE_FORMAL, FORMAL, identity/STARTED, or merge.

## Prospective contingency tree

```text
STRUCTURAL_ORDER_PATH_DEPENDENCE_ARCHITECTURE_STUDY_CYCLE1
├─ PRE_START_BLOCKER_MECHANICAL
│  └─ MAIN may repair outcome-independently, rerun ordinary exact-head CI, recheck fixed contract, then continue
├─ PRE_START_SEMANTIC_OR_BINDING_GAP
│  └─ STOP for Analyst; do not invent scientific choices
├─ TEST_OR_CONSUMED_MATERIAL_REQUIRED
│  └─ STOP; forbidden surface
├─ VALID_RUN
│  ├─ BUDGET_DOMINATED_PATH_DEPENDENCE -> STOP
│  ├─ EDGE_OR_HOMEOSTASIS_CONTROL_REPRODUCES -> STOP
│  ├─ REAL_DEV_FUNCTIONAL_PATH_DEPENDENCE -> STOP; Architecture reduction review only
│  ├─ LOW_FUNCTIONAL_OR_TOPOLOGY_ONLY_PATH_DEPENDENCE -> STOP
│  ├─ NO_OR_LOW_SUPPORT_REAL_DEV_PATH_DEPENDENCE -> STOP
│  └─ MIXED_ARCHITECTURE_RESULT -> STOP
├─ INVALID_ARCHITECTURE_DATA_OR_BINDING
│  └─ discard interpretation and STOP
└─ POST_OUTCOME_FAILURE_OR_DESIGN_CHANGE_NEEDED
   └─ STOP; no same-run redesign/rerun
```

FORMAL `PASS / FAIL / INCONCLUSIVE / INVALID_EVIDENCE / PRE_START_BLOCKER / POST_START_FAILURE` remain **UNARMED_NO_FORMAL_OBJECT**.

## Governance advisory

Governance does not change scientific allocation. Five evidence tags remain authoritative; rulesets remain zero; main/tag protection work remains Issue #139 and is explicitly non-blocking. Preserve the 13 legacy freeze branches and historical control/preserve refs. PR #148/#149 remain governance work pending their review fixes. Neutral reusable substrate may later be extracted to main in small reviewed changes; scientific semantics and current research candidates remain research-local.

## Utility request

Created append-only proposal on `ops/utility-orchestrator-requests`:

- request: `EVA-20260919-1943-TOPK-CROSS-SEED-SUPPORT`
- commit: `4d8a98637e07e8a7f560b47012c573f34c643068`
- mode: read-only NON_EVIDENTIARY cross-seed support/uncertainty diagnostic over existing cycle-1/cycle-2 DEV artifacts only
- independence: must not block MAIN; no new scientific workflow/training/probing/TEST/formal evidence; cannot relabel completed cycles or grant PRE_FORMAL authority.

## Orchestrator handoff

MAIN takes `CAND-STRUCTURAL-ORDER-PATH-01` Architecture cycle 1 and owns **all** candidate-specific implementation, CI, harness, binding, raw-artifact, validation and stop-boundary fixes. SUB remains default Discovery worker on independent questions only; it must not take MAIN's structural lane, top-k successor work, H5 audit, H7 construction, or any consumed-line rescue. Top-k is HOLD pending support-aware review and a later reduction question; MAIN must not absorb a cycle 3 now. Neither worker touches consumed identities, immutable evidence, TEST, formal raw/scoring surfaces, or scheduler definitions. Repartition only on a fresh lower-layer result, accepted Utility result, Independent Audit finding, or genuinely new repository evidence. All current MAIN valid result branches terminate at this Analyst boundary; no same-run promotion is authorized.
