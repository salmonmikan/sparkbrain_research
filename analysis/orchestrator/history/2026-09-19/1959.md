# SparkBrain Evidence Analyst — 2026-09-19 19:59 JST

## Decision

**No new FORMAL repository scientific evidence.** The material update is operational but scientifically important for the active Architecture layer: MAIN's exact structural-order cycle-1 workflow `35438684016` finished `failure`, and the failure occurred **entirely at Ruff lint before readiness/tests/contract validation and before the Architecture Study job could start**. No outcome-bearing diagnostic was produced.

This is exactly the prospectively authorized `PRE_START_BLOCKER_MECHANICAL` branch. The scientific allocation therefore does **not** change:

- `main_lane: STRUCTURAL_ORDER_PATH_DEPENDENCE_ARCHITECTURE_STUDY_CYCLE1`
- `sub_lane: BOUNDED_SECONDARY_DISCOVERY`
- `sub_fallback: NO_OP_WITH_OBSERVABLE_LEVEL_DUPLICATION_OR_LOW_VALUE_REASON`

MAIN owns the blocker and may make **only science-invariant lint/style repairs**: remove the unused `dataclasses.replace` import and reflow the five overlong lines. Every scientific choice remains fixed. MAIN must then obtain fresh exact-head ordinary CI/readiness/tests/bundle/contract validation before the already-authorized DEV-only Architecture job can run. SUB must not take any part of this blocker.

## Authoritative state re-fetched

- `main@ebed6abfa941c83b36d2e3bddd04e7c5fb0dbe9d` remains the stable shared substrate.
- Control Brain strategic prior remains `ops/control-brain-handoff@566f3b7c590fcfe2b9e8aaa0415da964989b1124`, doctrine `FORMAL_HOLD_WITH_ACTIVE_LOWER_FUNNEL`.
- `ops/orchestrator-run-report` is now `988477e3f4d45ef477e04c05438355e4234ed17d`.
- MAIN checkpoint report/history consumed through `0afd6b136f8e6181c621d79c56b125b08bd7aad8`; subsequent lease state at the mailbox tip still described the workflow as waiting externally.
- Active MAIN research ref is independently re-fetched at `research/main-structural-order-path-arch-study-20260919@16389e70352336b9d0ea539bdb5951a54f79ff2e`.
- Workflow `35438684016` is now `completed/failure` on that exact head. Both Python 3.11 and 3.13 jobs failed in `Lint`; readiness, tests, bundle validation, the structural-order contract check and `architecture-study` job were skipped.
- The six Ruff findings are all in the new Architecture harness: one unused import (`dataclasses.replace`) and five E501 line-length violations. No diagnostic artifact was produced.
- SUB latest remains the completed reward-omission Discovery, with scientific classification `REJECT` and no MAIN dependency.
- Literature latest remains `ops/external-research-audit-handoff@02c5eec39f8fca9dd180c4c91c4ae444ebd50995` (18:30 JST).
- Independent Audit remains NI01 `WEAKENED`; no newer role output supersedes it.
- Methodology Calibration remains `ops/methodology-calibration-audit@7102b752e33d12580ae42ed1b4a502205c99d654`, `SLIGHTLY_TOO_PERMISSIVE` locally at Top-k Architecture triage.
- Repository Steward advanced to `ops/repository-steward@7f77044f3159e7db15b45c9a471ae983c2a64284` (19:50 JST), governance advisory only.
- Exactly five authoritative annotated `evidence/*` tags remain; 13 legacy `freeze/*` branches remain; tag-based `formal/*`, `sealed/*`, and `freeze/*` remain empty.
- No fresh FORMAL identity, STARTED, preserve, score, TEST authority or evidence anchor was observed.
- Issue #139 remains open and factually current. Repository rulesets remain `0`.
- PR #148 and #149 remain open, mergeable and unmerged; neither is a scientific blocker.
- `ops/evidence-analyst-handoff` was re-fetched immediately before write at `9c6302d83f71ab22d45c5f6290951a48902d9c1e`.

## New repository evidence / lower-layer information

### FORMAL

**None.** The canonical evidence set is unchanged.

### MAIN structural-order Architecture Study

The study has **not produced an Architecture result yet**. The exact-head workflow never reached the architecture job. This matters because it preserves the prospective boundary cleanly: the observed failure is not post-outcome, does not expose any study metric, and can be repaired without knowing or reacting to the scientific result.

Classification of the current state:

`PRE_START_BLOCKER_MECHANICAL_LINT`

Allowed continuation is narrow and already prospectively authorized:

1. MAIN removes the one unused import and reformats the five overlong lines only.
2. No source binding, seed, DEV split, episode ordering, structural condition, metric, threshold, support rule, outcome mapping or runtime/model choice may change.
3. Fresh exact-head ordinary CI/readiness/tests/bundle/contract validation must pass.
4. Only then may the already-fixed DEV-only Architecture cycle run.
5. Every valid Architecture outcome stops for fresh Analyst review.

If repair reveals a semantic/protocol problem rather than style-only lint, MAIN must STOP. If any outcome-bearing diagnostic becomes visible and a later execution failure occurs, MAIN must STOP rather than rerun/redesign under this authority.

## Research-layer state

| Layer | State | Ownership / interpretation |
| --- | --- | --- |
| `DISCOVERY` | `OPEN` | SUB-default, strictly NON_EVIDENTIARY. Latest reward-omission candidate remains REJECT. |
| `ARCHITECTURE_STUDY` | `ACTIVE_PRESTART_MECHANICAL_BLOCKER` | MAIN-owned. Structural-order cycle1 is blocked only by lint before diagnostics; Top-k remains HOLD. |
| `PRE_FORMAL` | `EMPTY / HOLD` | No candidate has support-aware credibility plus a fresh prospectively fixed mechanistic/reduction question. |
| `FORMAL` | `EMPTY / HOLD` | No fresh formal object, identity, STARTED, TEST, scorer/preserver or execution authority. |

## Candidate pool

### `CAND-TOPK-PA-01` — `HOLD`
- **Question:** support-robust delayed amplification after hard Top-k turnover beyond ordinary switching and recurrent/non-normal transient gain.
- **Not rescue:** fresh Architecture/Discovery characterization, independent of consumed formal lines.
- **Layer:** `ARCHITECTURE_STUDY`.
- **Information gain / distance:** high if support and reduction can be separated / near for read-only diagnostics.
- **Reduction risks:** hard-switch geometry, ordinary recurrent/non-normal transient gain, sparse-stratum triage artifact.
- **Open choices:** future prospective support/uncertainty guard, switching-vs-recurrent decomposition, matched mechanistic residual.
- **Cycles:** Discovery 1; Architecture 2 completed.
- **Condition:** remain HOLD until cross-seed support robustness is characterized and a fresh reduction question is prospectively fixed. No cycle-3 rescue.

### `CAND-STRUCTURAL-ORDER-PATH-01` — `ARCHITECTURE_STUDY`
- **Question:** does actual SparkBrain structural execution show reproducible topology/output dependence for the same DEV episode multiset under different orders, beyond event-budget exhaustion and edge-only order effects?
- **Not rescue:** fresh system-order observable from synthetic Discovery; no consumed TEST/preserve/identity is reused.
- **Layer:** `ARCHITECTURE_STUDY`.
- **Information gain / distance:** high / near after mechanical lint repair.
- **Reduction risks:** explicit age/slot/provenance state, graph-rewrite critical pairs/non-confluence, homeostatic bias, event-budget effects.
- **Open scientific choices:** none for the already-authorized cycle1.
- **Cycles:** Discovery 1; Architecture 0 completed; first Architecture attempt blocked before diagnostics.
- **Condition:** complete only the fixed cycle1 after science-invariant repair. Any valid outcome stops for fresh review; no automatic PRE_FORMAL promotion.

### `CAND-H5-AUDIT-01` — `HOLD`
Independent read-only audit of H5 one-way integrity, resource accounting and narrow claim boundary. High information value / near distance. No rerun, retune or rescore; Independent Audit owns its blind attack set.

### `CAND-H7-RESP-01` — `HOLD`
Conditional PRE_FORMAL object only if a native online/local responsibility-sensitive mechanism independently appears. It must not be engineered as LP01 rescue or as a literature-driven successor.

### `CAND-REWARD-OMISSION-01` — `REJECT`
Resolved after one Discovery cycle by explicit reward-trace recurrence. Retain only the engineering/protocol warning that reward omission is not neutral under current v0.5 semantics.

## External and methodology inputs

### Literature

No newer Literature run supersedes 18:30. Keep the prospective reductions unchanged:

- Top-k: `hard switching geometry -> immediate jump -> ordinary recurrent/non-normal transient gain -> residual`.
- Structural order: `explicit rewrite-relevant state -> critical pairs/non-confluence -> matched-state functional consequence -> residual`.

These reductions raise any future mechanistic bar but do not alter the fixed structural-order cycle1 or any consumed result.

### Independent Audit

No newer Audit supersedes NI01. Canonical NI01 FAIL and one-way integrity remain valid. Programme synthesis remains:

`NO_REGISTERED_INCREMENTAL_NATIVE_ADVANTAGE_OVER_WORLD_CONDITIONED_CONFIDENCE_ABSTENTION`

Equal-privilege generic confidence-abstention reduction remains unestablished. H5 remains the next independent-audit priority.

### Methodology Calibration

No newer calibration supersedes 19:20. The four-layer funnel remains correct; the local Top-k Architecture signal gate remains slightly too permissive because sparse strata can cast full votes. Do not retroactively relabel completed Top-k cycles. Future Architecture gates need prospective support/uncertainty calibration.

### Repository Steward

The new 19:50 advisory confirms no canonical evidence change, 5 evidence tags / 13 legacy freeze branches unchanged, rulesets still 0, `main` unprotected, PR #148/#149 still deferred pending review fixes, and lower-layer results/Utility diagnostics remain noncanonical. Under current human direction the protection gap remains recorded but deferred rather than a research priority.

## MAIN / SUB allocation

### MAIN

MAIN keeps `STRUCTURAL_ORDER_PATH_DEPENDENCE_ARCHITECTURE_STUDY_CYCLE1` and owns **all** critical-path work, including the current lint blocker. The only authorized immediate change is science-invariant lint/style cleanup followed by fresh exact-head CI. SUB must not help with the repair, harness, CI, contract, artifact or any later cycle1 fix.

The scientific contract remains exactly the 19:43 authorization: `main@ebed6ab...`, model/training seed 43, DEV-only train/adaptation/probe surfaces, structural seed 83, permutation seed 20260919, fixed 12 permutation/reverse pairs, fixed `full_budget16`, `full_budget64`, `edge_only_budget64` conditions, fixed metrics and fixed six-way Architecture triage.

### SUB

SUB remains `BOUNDED_SECONDARY_DISCOVERY`: at most one genuinely independent synthetic/development/read-only observable-level target per run. It may no-op with a concrete duplication/low-value reason.

SUB must not touch the structural-order MAIN branch or blocker; the held Top-k support/reduction question; H5 Independent Audit; H7 mechanism construction; consumed/formal TEST/raw/scoring surfaces; or identity/STARTED/evidence work.

## Top 3

1. **MAIN — HIGH / NEAR:** perform only the lint/style repair on the structural-order harness, obtain fresh exact-head CI/contract green, then run the unchanged DEV-only Architecture cycle1 if green.
2. **Independent Audit — HIGH / NEAR:** audit H5 one-way integrity, resource accounting and claim boundary read-only.
3. **SUB — MEDIUM / OPTIONAL:** one new independent bounded Discovery question, or explicit no-op. The pending Top-k cross-seed Utility request remains auxiliary and must not block MAIN.

## #1 GO / STOP

**GO** only under the already-fixed `PRE_START_BLOCKER_MECHANICAL` contingency. The current failure is suitable because both Python jobs stopped at Ruff lint and the Architecture job was skipped; no outcome-bearing diagnostic exists.

GO requires:
- same scientific source/config/DEV inputs/seeds/conditions/metrics/thresholds;
- lint/style-only code changes;
- fresh exact-head ordinary CI/readiness/tests/bundle/contract validation;
- no TEST, consumed preserve, formal identity, STARTED, score/evidence path;
- raw diagnostic artifact before terminal Architecture interpretation.

**STOP** if the repair needs a scientific/semantic change, if a binding mismatch appears, if TEST or consumed material would be needed, if outcome knowledge is needed to choose any parameter/metric/control, or if any diagnostic becomes visible before a later failure.

## Prospective contingency tree

```text
STRUCTURAL_ORDER_PATH_DEPENDENCE_ARCHITECTURE_STUDY_CYCLE1
├─ PRE_START_BLOCKER_MECHANICAL  [OBSERVED]
│  └─ MAIN lint/style-only repair -> fresh exact-head CI/contract validation
│     ├─ green -> unchanged fixed DEV-only cycle1
│     └─ semantic/protocol issue -> STOP
├─ PRE_START_SEMANTIC_OR_PROTOCOL_GAP
│  └─ STOP
├─ VALID_ARCHITECTURE_OUTCOME
│  ├─ BUDGET_DOMINATED
│  ├─ EDGE_OR_HOMEOSTASIS_CONTROL_REPRODUCES
│  ├─ REAL_DEV_FUNCTIONAL_PATH_DEPENDENCE
│  ├─ LOW_FUNCTIONAL_OR_TOPOLOGY_ONLY
│  ├─ NO_OR_LOW_SUPPORT
│  └─ MIXED
│     all -> persist fixed NON_EVIDENTIARY label -> STOP for fresh Analyst
├─ INVALID_EVIDENCE / leakage / binding violation
│  └─ discard interpretation -> STOP
└─ POST_START_FAILURE after diagnostic visibility
   └─ STOP; no rerun/redesign
```

FORMAL PASS/FAIL/INCONCLUSIVE remains unarmed because no FORMAL object exists.

## Consumed identities / hard boundaries

Consumed/no-retry identities remain C19-v4, C19-R1 revision-authority v1/v2, C19-R2 official-v1, PD01 official-v1, NI01 official-v1 and H5 official-v1. Never rerun, retune, rescore, relabel or silently repair them post-outcome.

`do_not_touch` additionally includes immutable evidence and legacy freeze/preserve/control anchors except read-only inspection; official TEST/sealed targets for lower-layer work; Top-k cycle3/PRE_FORMAL escalation under current authority; research PR merges; scheduler definitions; and MAIN critical-path work by SUB.

## Real blockers

The immediate MAIN blocker is **mechanical lint only**, not a scientific blocker. Top-k mechanistic escalation remains blocked by support robustness and ordinary reductions. H5 independent audit is pending. H7 lacks a native object. FORMAL lacks a fresh high-value prospectively bindable object.

## Utility request

**No new Utility request created this run.** Existing `EVA-20260919-1943-TOPK-CROSS-SEED-SUPPORT` remains pending Control Brain disposition and is explicitly nonblocking for MAIN.

## Persistence boundary

Persist this run only to:
- `analysis/orchestrator/latest.md`
- `analysis/orchestrator/state.json`
- append-only `analysis/orchestrator/history/2026-09-19/1959.md`

No scientific execution, research-workflow dispatch, one-way identity consumption, research PR merge, immutable evidence mutation or scheduler-definition change is performed by this Analyst run.
