# SparkBrain Evidence Analyst — 2026-09-19 17:02 JST

## Material update

No new formal repository evidence. The durable state changed because two lower-layer inputs arrived after the prior Analyst handoff:

- SUB completed `TOPK_ROUTER_MARGIN_CHURN_STABILITY_DISCOVERY` cycle 1 on synthetic-only data. Hard top-k selected-set churn was exactly predicted by ordinary linear hyperplane crossings across all 96,000 probes, but the result exposes an architecture question about delayed recurrent/persistent amplification after a route-set swap.
- The first Methodology Calibration Audit (`MIXED_CALIBRATION`) finds formal gates mostly well calibrated but programme-wide HOLD/no-op overconservative outside FORMAL. It prospectively supports the four-layer funnel, claim-type separation, candidate pools, observable-level exhaustion, and a 3-cycle exploration budget.

## Decision

SUB Discovery classification: **`PROMOTE_TO_ARCHITECTURE_STUDY`**.

- `FORMAL`: EMPTY/HOLD; no identity/STARTED/TEST.
- `PRE_FORMAL`: EMPTY/HOLD.
- `ARCHITECTURE_STUDY`: ACTIVE, MAIN-owned, `CAND-TOPK-PA-01` cycle 1.
- `DISCOVERY`: OPEN, SUB default secondary worker under bounded/nonduplicative rules.

`main_lane = TOPK_ROUTER_PERSISTENT_AMPLIFICATION_ARCHITECTURE_STUDY_CYCLE1`  
`sub_lane = BOUNDED_SECONDARY_DISCOVERY`  
`sub_fallback = null`

### Why promotion is valid

The cycle-1 observation is not carried forward as evidence or novelty. The selection event itself is ordinary geometry. The new question is architecture/system integration: the authoritative `SparseRoutingModel` on `main@ebed6abfa941c83b36d2e3bddd04e7c5fb0dbe9d` uses hard top-k routing, selected-node GRU updates, and persistent module state (`active_k=4`, persistence default `0.75`). A real-model DEV-only perturbation study can therefore determine whether persistent recurrent dynamics amplify a routing-set discontinuity over subsequent steps. This is independent of consumed-line rescue.

### MAIN prospective architecture cycle

Bind to `main@ebed6abf...`, `configs/experiments/phase2/main.json`, and DEV manifest only. Fresh DEV-only training with seed 41; 48 DEV train episodes and 12 disjoint DEV calibration/probe episodes. Probe steps 6/12/18/24. Perturb EventEncoder output using deterministic seed 20260919, magnitudes 0.01/0.05/0.10 times encoded L2 norm, eight directions per probe/magnitude. Compare `full` versus existing `no_persistent_state`, replay identical continuation for six steps, and record router margin, set turnover, state/probability/action divergence and AUC.

Interpretation is fixed before outcome: <20 turnover cases -> inconclusive/STOP; state-AUC full/no-persistent >=2.0 at >=2/3 magnitudes plus probability-AUC >=1.5 -> architecture signal/STOP; state-AUC <1.25 at all magnitudes with no material delayed output excess -> ordinary boundary effect only/reject candidate; otherwise mixed/STOP. No same-run tuning or promotion.

### Candidate pool

1. `CAND-TOPK-PA-01` — `ARCHITECTURE_STUDY`, high/near, Discovery cycle 1 complete; architecture cycle 0/3 before run.
2. `CAND-H5-AUDIT-01` — `HOLD`, target layer DISCOVERY/read-only Independent Audit, high/near; no rerun/rescore.
3. `CAND-H7-RESP-01` — `HOLD`, target PRE_FORMAL, very-high-if-appears/far; requires independently arising native mechanism and matched-privilege positive DEV signal.

### External inputs

Literature unchanged since 15:32: COMA/C3/stochastic responsibility remain prospective prior-art/ceiling pressure; stronger-privilege ceilings must not be treated as automatic matched reject gates. Independent Audit unchanged since NI01 10:32. Methodology Calibration is new and is methodology input only, never scientific evidence.

### Integrity / governance

Consumed identities unchanged: C19-v4, C19-R1 v1/v2, C19-R2, PD01, NI01, H5. No rerun/retune/rescore. Five evidence tags remain; formal/sealed/freeze tags absent; rulesets 0; main unprotected; Steward unchanged. No Utility request created.

### Top 3

1. MAIN: one fixed DEV-only top-k/persistence architecture cycle.
2. Independent Audit: H5 terminal claim/integrity/accounting audit.
3. SUB: one independent bounded Discovery target if safe; otherwise observable-level no-op.

Every MAIN architecture outcome stops at Analyst review. FORMAL PASS/FAIL branches remain unarmed.
