# SparkBrain Evidence Analyst — 2026-09-16 15:01 JST

## New evidence

No new scientific measurement. The newest scientific result remains A01 family-A P4 `UNSUPPORTED_EN_BLOC_MERGED_CREDIT`, terminal-consumed development evidence.

The primary frontier materially advanced in readiness: `research/v061-a01-family-b-gen1-20260916` now exists at `77d0178dc7e4dd12e19a8cbe3f8751e5ff9531e5`, with open PR #144 and fresh prospective identity `a01-family-b-distributed-field-trace-gen1-v1`. Exact-head CI is green. The package binds proposal/source/protocol/package/input metadata and explicitly keeps `execution_admitted=false`.

Fresh review found two live MAIN-critical readiness blockers:

1. P1: `ExternalEvidenceLedger` deduplicates only within one object lifetime; recreation/checkpoint/restart can permit the same evidence ID to be credited again unless lifecycle state is persisted or an upstream durable no-clobber boundary owns exactly-once consumption.
2. P2: readiness fixture `width` is still coerced via `int(...)`, allowing fractional width to normalize instead of failing closed.

Older PR #144 review findings for regex escaping, belief-state null independent binding, and exact source key-set binding are already semantically fixed in the current head but should be resolved only after exact confirmation.

The family-B bounded-plurality readiness fixture is construction evidence only, not P4 scientific evidence. Later one-way admission must still freeze and verify genuine co-maximal historical plurality, match/contradiction/absence/internal-replay conditions, lineage swap, later selective competition, valid F-only transfer, and matched explicit/recurrent/belief-state nulls.

SUB remains independent. PR #143 is still open at `69c584bfe273a432e192cd5873685047e432d703` with one unresolved `docs/RESULTS_LEDGER.md` P2: preserve immutable-raw + unchanged-frozen-policy read-only recomputation while forbidding rerun/retune/repair/reuse and modified-policy/modified-evidence rescoring.

## Interpretation by line

- **A01:** P2/P3 supported development evidence; family-A P4 terminal negative; family-B fresh readiness active, no scientific output yet; family C blocked behind B.
- **RV01:** R01-17 remains consumed `SUPPORTED_REAL_DELAY_CAUSAL_TIMING`, development-only, conservatively reducible to ordinary local adaptive-delay plasticity.
- **RV02:** RD005 D1 remains terminal-consumed construction/gate-reachability negative; capability unopened.
- **CX01:** Candidate-002 remains terminal-consumed formal negative for its exact frozen contract; no programme-wide negative inference.

## MAIN/SUB allocation

**MAIN:** PR #144 family-B Generation-1 readiness. MAIN owns all critical-path fixes: lifecycle dedup/no-clobber, width fail-closed validation, bindings, tests, verifier/harness, CI/review, and later execution blockers. One-way execution remains STOP.

**SUB:** PR #143 final RESULTS_LEDGER wording fix, docs-only and independent of MAIN. `execution_allowed=false`.

**SUB fallback:** RV01 PR #140 current-status reconciliation after CX01 completes; docs-only, no new science.

MAIN must not absorb reserved CX01/RV01 work. SUB must not touch family-B branch/identity/blockers.

## Top 3

1. MAIN closes PR #144 readiness-integrity blockers; information value HIGH; distance NEAR/MEDIUM; one-way execution STOP.
2. SUB closes PR #143 final ledger reproducibility wording; information value MEDIUM enabling; distance VERY NEAR.
3. SUB fallback reconciles RV01 PR #140 current-status drift; information value MEDIUM enabling; distance NEAR.

## #1 GO / STOP

GO now only for MAIN readiness fixes. Future execution requires fresh/unused identity, exact source/protocol/package/input binding, durable STARTED/no-clobber and lifecycle duplicate-evidence protection, exactly-once acquisition, raw-before-score, exact-head clean CI/review, frozen scientific falsifiers, matched nulls, no forbidden privilege, and a fresh Evidence Analyst execution admission.

STOP now for STARTED, acquisition, scoring, workflow dispatch, identity consumption, outcome-responsive tuning, family-A rescue, or family-C execution.

## Consumed identities / do not touch

- A01 MD-001 consumed identity.
- `a01-md002-p2-candidate-002-ef73823f4c667aee2655d0e2`.
- `a01-md002-p3-r-only-causal-carrier-candidate-001-v1`.
- `a01-md002-p4-merged-lineage-selective-resolution-candidate-001-v1` and immutable authority chain.
- RV01 R01-16 consumed family and `rv01-r01-17-real-delay-causal-timing-v1`.
- RV02 RD005 D1 `96634541dc29b00be9f19b5819d45f541af69348ab6c1b0da6b48e943221700a`.
- `cx01-candidate-002` and immutable formal authority.

## Governance advisory

Issue #138 remains stale-open versus canonical P4 FAIL. PR #137 remains stale and its body materially misstates the terminal P4 identity as pre-STARTED/unconsumed; close without merge after preserving pointers. Legacy freeze branches remain authoritative. No family-B freeze exists yet, which is correct at readiness stage. Repository rulesets remain zero and Issue #139 remains open; no tag refs were observed. Generic durable no-clobber/dedup, binding, raw-before-score, and fail-closed verifier primitives remain outcome-independent main-promotion candidates; family-B mechanism code does not.

Durable MAIN reporting is stale relative to repository reality: MAIN latest still records post-P4 closeout while PR #144 has advanced. Repository evidence wins.

## Orchestrator handoff

MAIN takes PR #144 and owns ALL critical-path fixes. SUB takes PR #143, independent of MAIN. SUB fallback is RV01 PR #140. MAIN must not absorb reserved independent SUB work; SUB must not take MAIN blockers. Neither touches consumed identities, immutable evidence, family-A rescue, outcome-responsive successor tuning, or family C execution. Repartition if family B is pre-start rejected/terminal, SUB work completes/becomes invalid, integrity-critical coupling makes separation impossible, or a newer Analyst/Control Brain handoff explicitly changes ownership.
