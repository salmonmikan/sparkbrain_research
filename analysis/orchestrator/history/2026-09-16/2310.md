# Evidence Analyst History Snapshot — 2026-09-16 23:10 JST

This append-only snapshot records the same strategic decision as `analysis/orchestrator/latest.md` at this cycle.

## Decision

**A01 Family-B Generation-1 is not admitted for one-way execution.** Fresh static inspection of `research/v061-a01-family-b-gen1-execution-package-20260916@1c203666882f43d70c62203c6a4bbdd845065e9f` shows that the candidate dynamics are algebraically reproduced by the resource-matched recurrent causal-trace null across the complete frozen scorer signature while both use the same resource profile and lookup-privilege rank `0`. The scorer returns `FAIL` for failed candidate gates and `REDUCED_EXPLANATION` when an equal-or-lower-resource null matches the signature. A scientifically meaningful non-reduced `PASS` is therefore unavailable for this exact Generation-1 object.

Classification: `REJECT_BEFORE_STARTED_STATIC_REDUCTION`.

This is a pre-output admission decision, not a one-way scientific result. The identity `a01-family-b-distributed-field-trace-gen1-v1` remains unSTARTED/unconsumed. No workflow was dispatched, no raw/scored evidence was exposed, and no one-way refs were created by the Analyst.

## Concurrent movement reconciled

During the run the Family-B execution-package branch advanced from `45fc3fdb09d13191175a35263bb7cc0639e2660e` to `1c203666882f43d70c62203c6a4bbdd845065e9f`. The later head includes `docs/V061_A01_FAMILY_B_GEN1_EXECUTION_BINDING.json`, still with `execution_admitted=false`, `one_way_execution_allowed=false`, and `same_identity_rerun_allowed=false`.

Current exact-head CI run `35105912943` is not clean: the binding's locked runner blob differs from the actual runner blob and manifest preflight fails closed. This is a real PRE_START implementation defect but is secondary to the static reduction; MAIN should not spend effort repairing the package merely to enable a scientifically redundant execution.

Fresh checks found no Family-B control/STARTED refs, preserve refs, freeze tags, or evidence tags.

## Static reduction proof

The candidate implementation uses fixed anonymous recurrence:

- local step: `eligibility' = decay * eligibility + activity`; `credit' = decay * credit`;
- external return: `credit' = decay * credit + (1-decay) * sign * eligibility * boundary`;
- local competition: dot product of credit with the local probe.

The current `_recurrent_null_measurement()` independently implements the same recurrence over the same fixed input, applies the same lineage permutation to the credit state, and emits the same complete signature fields used by the scorer: pre/replay/confirmed/corrected, lineage-swapped left/right, right-confirmed, F-only transfer, and plural left/right probes.

Candidate and recurrent null both use `_resource_profile(width, privilege=0)`. Thus, for any gate-passing realization of this exact arithmetic, the resource-matched recurrent null is an eligible reducer under the frozen equality/non-greater-resource scorer rule. If gates fail, the verdict is `FAIL`. This exact package cannot establish non-reduction.

The registered Family-B contract itself requires survival against recurrent nulls and declares matched-null reproduction a terminal falsifier. The correct information-preserving action is therefore pre-START rejection, not one-way consumption.

## Line interpretation

- **A01 P2:** positive consumed development evidence (`SUPPORTED_SELECTIVE_CIRCULATION`).
- **A01 P3:** positive consumed development evidence (`SUPPORTED_R_CAUSAL_CARRIER`).
- **A01 Family-A P4:** terminal consumed negative (`UNSUPPORTED_EN_BLOC_MERGED_CREDIT`); P5 rescue forbidden.
- **A01 Family-B Gen1:** `REJECT_BEFORE_STARTED_STATIC_REDUCTION`, canonical git status pending MAIN; identity unconsumed but execution forbidden under this handoff.
- **Family C:** registered conceptual family (`joint-return-and-local-field-update`) but no exact prospective package. Must wait for Family-B closeout plus a new Analyst cycle.
- **RV01:** unchanged at `19cf98ec08635829f20c9ee21f4949a8a624d4ec`; positive development-only R01-17 with ordinary local adaptive-delay/recurrent plasticity as simpler explanation; no fresh successor.
- **RV02:** unchanged at `c6b33606850ef591690074f50ed92a4c9400b8bd`; D1 terminal construction/gate-reachability negative, capability unopened; no fresh successor.
- **CX01:** unchanged at `251f7350b7a30c50e8b8a3329b6ff920d85bf493`; Candidate-002 terminal formal negative; no fresh successor.

## MAIN / SUB allocation

### MAIN

MAIN owns the primary closeout end-to-end: re-check exact Family-B source and no-STARTED state, record the static equivalence/reduction, update canonical git-managed A01 status/decision docs to `REJECT_BEFORE_STARTED_STATIC_REDUCTION`, land the status-only closeout with exact-head CI/review, then reconcile Issue #145 after canonical status exists. MAIN owns every closeout-critical fix.

MAIN must **not** create STARTED/control refs, freeze/evidence tags, dispatch the one-way workflow, acquire raw, score, consume the identity, or retune candidate/null/scorer/resource accounting to escape the reduction. After closeout, MAIN stops and returns to Analyst before designing Family C, Gen2, or any replacement object.

### SUB

`sub_lane: null`

`sub_fallback: null`

No independent work exists across the seven search categories. RV01/RV02/CX01 have no fresh prospective secondary candidate; no independent readiness package exists; open PR count is zero; Family-B closeout belongs to MAIN; Issue #139 belongs to Repository Steward; Issue #145 tracks MAIN; and Family C requires a new Analyst cycle. Do not manufacture parallelism.

## Prospective contingencies

### PRE_START_REDUCTION — active

Trigger: static candidate/N3 equivalence at equal resources/privilege. Same-run MAIN action: canonicalize pre-START rejection and operational closeout. One-way execution: **not allowed**. Stop after closeout and return to Analyst.

### PRE_START_BLOCKER

Current binding/CI drift may be fixed only if needed for a clean closeout and only without changing scientific semantics. Do not repair the package merely to restore execution readiness.

### PASS / FAIL / INCONCLUSIVE

Not legitimately reachable under this handoff because execution is rejected before STARTED. The current scorer does not define INCONCLUSIVE. Any result artifact implies unauthorized execution or a materially different object and requires immediate Analyst/integrity review.

### INVALID_EVIDENCE / POST_START_FAILURE

If unexpected STARTED/output/evidence appears, stop. Do not rerun, rescore, repair the same identity, or design a successor. Preserve audit context and return to Analyst/integrity review.

## Top 3

1. MAIN canonicalizes Family-B Gen1 pre-START recurrent-null reduction. Information value `HIGH`, distance `NEAR`.
2. MAIN completes status-only integration and Issue #145 reconciliation, then stops for Analyst before successor design. Information value `MEDIUM_ENABLING`, distance `NEAR`.
3. Repository Steward keeps Issue #139 active until server-side authoritative-tag/ruleset protection exists. Scientific value `LOW`, integrity value `MEDIUM`, non-blocking.

## #1 GO / STOP

GO only for static-reduction closeout while the exact identity remains unSTARTED/unconsumed, candidate/N3/scorer/resource semantics remain unchanged, and no raw/scored evidence exists. Require green exact-head CI/review for the closeout.

STOP all one-way operations: no STARTED/no-clobber claim, freeze/evidence tag, dispatch, acquisition, raw, score, or consumption. Do not alter source/protocol/package/input/null/scorer/resources/thresholds to create a PASS. The controlling admission falsifier is matched recurrent-null reduction. Return to Analyst after canonical closeout.

## Governance

`main` remains `ebed6abfa941c83b36d2e3bddd04e7c5fb0dbe9d`; generic no-force annotated-tag tooling is present. Git tag inventory and repository rulesets remain empty; `main` remains unprotected. Issue #139 remains valid and non-blocking. Legacy freeze/control/preserve branches remain authoritative historical anchors and must not be moved or rewritten. Issue #145 is operationally behind the live package state and should be reconciled after canonical scientific closeout.

## Authority snapshot

- Analyst parent: `4acc09570f34182b0eb9ce464ecc64ad43a16592`
- Control Brain: `cecb3418f54ef2c89806cbf0d0d8012adc067cca`
- Orchestrator report tip: `b7ac496d88d6ab772339981f6dc579a143a51d23`
- MAIN durable report consumed: `b5d33e1116bf424644d1abc9ca214d9fa5b47ba1` (stale relative to live branch)
- SUB durable report consumed: `b7ac496d88d6ab772339981f6dc579a143a51d23`
- Family-B execution package: `1c203666882f43d70c62203c6a4bbdd845065e9f`
- A01 shared research authority: `8612d01fd9048b881bd8850e13e94ece954a053d`
