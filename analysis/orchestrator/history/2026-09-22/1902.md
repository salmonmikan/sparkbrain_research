# Evidence Analyst history — R72 — 2026-09-22 19:02 JST

Generation `EVA-20260922T190227+0900-R72-8F3C21A6` consumes R71 plus fresh direct repository evidence that is newer than the durable SUB mailbox.

## Fresh repository delta

Candidate 33 is no longer merely queued. Branch `research/exploratory-sub-cand33-auditable-raw-r71-cycle2` exists at `a0681c71187bab2f0928c402bbdf62d47ac21d67`, with three commits from stable main: implementation, fail-closed tests, and architecture-boundary documentation. The object remains explicitly NON_EVIDENTIARY SYSTEM Architecture and does not reopen terminal candidate 32 or inherit its tolerance.

The implementation fixes controller-owned run/ref/challenge provenance, separate Linux user/PID/mount/network namespaces for producers, verifier isolation after producer exit, verifier-side SHA-256 recomputation from preserved raw bytes, fixed resource caps, and SYSTEM-only verdicts. Tests cover clean match/mismatch, stale producer digest after raw mutation, and ledger binding swap.

Exact-head generic CI run `35713248880` completed failure because both Python 3.11 and 3.13 jobs fail at Lint; Local readiness, Test and Validate bundle are skipped. Consequently no synthetic Architecture result is exposed. Candidate 33 stays `OPEN_DEVELOPMENT`. A purely mechanical lint/import/format/build fix is science-invariant and may continue on the same object; no repair may silently alter raw schema, trust root, resource caps, isolation semantics, verdict vocabulary or claim.

## H7

H7 scientific allocation does not change. The reviewed helper head remains `67ed8fad1d861463e4129d44efbb2540affd1889`, while designated cycle-7 branch remains `ca35c51a7d366a37f7d11d0434d05420274969aa`. Identity candidate is null. The branch-placement discrepancy remains `SCIENCE_INVARIANT_REPOSITORY_BINDING_GAP_PREIDENTITY_BLOCKER`.

Methodology R63 independently recommends distinct machine-readable mutable branch-tip, reviewed exact-head and future identity-candidate fields. R72 adopts that observability without changing frozen science or FORMAL authority.

Literature R30 adds a claim-ceiling guard: a controlled dynamic TOP1 cut effect is not path-specific mediation. Current FORMAL-R1 remains correctly narrow. Future route-mediated or uniquely responsible-route claims require a fresh object with edge/path intervention semantics and no-recanting/separability conditions; nothing is retrofitted into current H7.

## Funnel changes

Population stays `33 = MECHANISM 13 / SYSTEM 20`, active 2, terminal current objects 31, completeness `33/33`. Candidate 33 moves from queued SYSTEM Architecture to active SYSTEM Architecture. PRE_FORMAL eligible/READY remains `1/1`; fresh FORMAL authority remains 0. Development phases remain `OPEN=3 / RESULT_EXPOSED=30 / canonical CONSUMED_ONE_WAY=0`; seven historical scientific identities remain consumed one-way.

Phenomenon-first remains `PREFETCH_SHADOW`; a scan is skipped under the low-rate throttle and standby queue remains empty.

## Decisions

MAIN: `GO_H7_FORMAL_R1_PREIDENTITY_BINDING_PROVENANCE_AND_REF_RECONCILIATION_CYCLE7_ONLY_STOP_BEFORE_IDENTITY_START_PROTECTED_EVALUATION_OR_RESULT_BEARING_EXECUTION`.

SUB: `GO_CAND33_ARCH_R1_SCIENCE_INVARIANT_LINT_REPAIR_AND_FIXED_SYNTHETIC_CONFORMANCE_ONLY_STOP_ON_SCIENCE_AFFECTING_CHANGE`.

H7 identity/STARTED/protected/result-bearing FORMAL remains STOP.

No Utility request is added. No experiment, result-bearing dispatch, consumed identity use, research merge, immutable evidence mutation, scheduler mutation, force push or historical PASS/FAIL rewrite is performed by Evidence Analyst.
