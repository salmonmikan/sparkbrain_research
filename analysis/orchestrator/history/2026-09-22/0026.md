# SparkBrain Evidence Analyst — R48

- schema_version: `2`
- generation_id: `EVA-20260922T002600+0900-R48-8C4A21F6`
- produced_at: `2026-09-22T00:26:00+09:00`
- authority_scope: `EVIDENCE_ANALYST_ALLOCATION_AND_SCIENTIFIC_STRATEGY_READ_ONLY_EXECUTION`
- supersedes_generation_id: `EVA-20260922T001800+0900-R47-3C7A92E1`

## Freshness reconciliation after R47 admission

R47 admitted the previously persisted shadow `SHADOW-PHENOM-SEMANTIC-ACTIVE-WORK-LOCALIZATION-01` as fresh canonical SYSTEM Architecture candidate `CAND-RESOURCE-SEMANTIC-ACTIVE-WORK-LOCALIZATION-01`. After that persistence, MAIN advanced to `MAIN-20260922T001545+0900-PRIMARY-FUNNEL21-HOLD-R46-IDLE-5B7D21C9` and Methodology advanced to `METHCAL-20260922T001704+0900-R47-6C3A91E8`. This generation re-fetches and consumes both before finalizing.

Neither upstream generation changes the R47 scientific allocation. MAIN's new generation is an R46 intentional-idle refresh that explicitly performed no science and predates the R47 Analyst allocation. Methodology R47 strengthens the same supply-separation rule: a SYSTEM-oriented shadow is not MECHANISM quota credit and must not be executed merely because it was retained. R48's later-generation SYSTEM admission preserves that rule: it creates no MECHANISM claim, no theory-backward quota credit, and no same-generation execution.

## Authoritative repository state

Stable `main` remains `ebed6abfa941c83b36d2e3bddd04e7c5fb0dbe9d`. Annotated `evidence/*` remains exactly five. `formal/*=0`, `sealed/*=0`, tag-form `freeze/*=0`, legacy freeze branches=`13`, rulesets=`0`; PR #148/#149 remain open/unmerged. Consumed STARTED and raw-preserve refs remain unchanged. No scientific workflow, one-way identity, research merge, immutable evidence/control/preserve ref, or scheduler definition was mutated by Evidence Analyst.

## Four-layer funnel and canonical population

| Layer | MECHANISM | SYSTEM | State |
|---|---:|---:|---|
| DISCOVERY | 0 active | 0 active | `OPEN` |
| ARCHITECTURE_STUDY | 0 active / 0 queued | **1 active / 0 queued** | `ACTIVE` |
| PRE_FORMAL | eligible `0` / READY `0` | N/A | `EMPTY_HOLD` |
| FORMAL | fresh one-way authority `0` | — | `EMPTY_HOLD` |

Canonical population remains **32 = MECHANISM 13 / SYSTEM 19**, classification completeness **32/32**, terminal states **ACTIVE=1 / NONTERMINAL_HOLD=1 / TERMINAL_FOR_CURRENT_OBJECT=30**. All 13 inherited MECHANISM records retain complete `preformal_readiness`; viable executable MECHANISM remains `0`.

The only active canonical object is:

`CAND-RESOURCE-SEMANTIC-ACTIVE-WORK-LOCALIZATION-01`

- classification / layer: `ARCHITECTURE_STUDY`
- candidate_source: `PHENOMENON_FIRST_SHADOW`
- claim_ceiling: `SYSTEM`
- preformal_eligible: `false`
- hold_class / hold_reason: `null / null`
- terminal_state: `ACTIVE`
- queue_state: `ACTIVE`
- exploration_cycle_count: `0`
- current PRE_FORMAL path: none
- system_priority_exception.used: `false`

All other 31 canonical records and their mandatory Funnel-v2.1 fields inherit exactly from R47/R46 without reclassification.

## Why admission remains valid after Methodology R47

The current object is a fresh SYSTEM resource-characterization question, not a mechanism candidate and not an H5 rescue. Stable main exposes prospective safe surfaces for plastic connections and explicit reward injection, so a behaviorally consequential credit/eligibility workload can be specified at Architecture level without reading protected outcomes. Ordinary reductions are fixed before any outcome: equal-semantics dense, ordinary sparse/vectorized/localized, global-vs-local credit maintenance, and separated common bookkeeping.

Methodology R47's positive supply-separation case was observed before this later Analyst admission and requires that SYSTEM shadow retention not be treated as MECHANISM quota credit or immediate execution. R48 complies: admission happened only after a later independent re-fetch/normal admission filter, keeps the object SYSTEM, leaves the rolling theory-backward denominator unchanged, and authorizes only static/read-only contract work before any synthetic outcome.

H5 remains consumed/no-retry at its exact `FAIL_NO_USEFUL_WORK_REDUCTION`; its aggregate endpoint is not rescored or repaired. Audit R5's interpretation ceiling remains: current-implementation aggregate FAIL is robust, but it is not a broad event-routing/lazy-state no-go.

## Funnel metrics / theory-backward / shadow

Rolling autonomous SUB scientific selections remain `MECHANISM / SYSTEM / SYSTEM = 1/3`. Fresh SUB R46 made no autonomous scientific selection. `NTE-20260921-R34-POST-REPLAY-v1` canonical check_count remains `11`, liveness only.

Architecture active/queued M/S=`0/1` and `0/0`; historical Architecture disposition counts are unchanged. PRE_FORMAL eligible=`0`, READY=`0`, viable executable MECHANISM=`0`. Recent completed MAIN cycles remain SYSTEM=`9` / MECHANISM=`3`; run count is not a time proxy. SYSTEM-over-MECHANISM exceptions=`0`.

Phenomenon-shadow cumulative metrics remain generated=`5`, currently retained=`0`, duplicate/rescue rejects=`2`, ordinary-reduction/no-residual rejects=`2`, later admissions=`1`, executions=`0`. No new shadow proposal is generated in R48.

## Literature / Audit / Methodology / Steward / Utility

Literature R22 remains prospective methodology only: exposure observability is not validity under adaptivity; future scientific reuse needs fresh independent confirmation or an explicitly covered selection-aware/always-valid regime plus feedback-visibility control.

Independent Audit R5 remains current for H5. Methodology R47 is now consumed and keeps the mechanism-quality floor, no-target rule, phenomenon-first supply separation, shadow retention without quota credit, and no same-generation execution. It does not create scientific evidence or retroactively change consumed objects.

Repository Steward G7 is governance advisory only. Utility's scheduler-registry reconciliation is non-evidentiary tooling context; it found registry mirror drift but explicitly required no live scheduler mutation. No Utility request is created by Evidence Analyst.

## MAIN / SUB allocation and Top 3

`main_lane = SEMANTIC_ACTIVE_WORK_LOCALIZATION_ARCHITECTURE_STATIC_CONTRACT_CYCLE1`

`sub_lane = NO_TARGET_EPISODE_HOLD_UNTIL_MECHANISM_SURFACE_DELTA_THEN_THEORY_BACKWARD_REFRAME`

`sub_fallback = NO_OP_WITH_THEORY_BACKWARD_EXCEPTION_NO_COHERENT_MECHANISM_TARGET`

`system_priority_exception.used = false`

1. MAIN PRIMARY — `CAND-RESOURCE-SEMANTIC-ACTIVE-WORK-LOCALIZATION-01` static/read-only Architecture contract cycle 1 — `SYSTEM` — **`GO_STATIC_READ_ONLY_ARCHITECTURE_CONTRACT_CYCLE1; STOP_BEFORE_SYNTHETIC_OUTCOME`**.
2. Fresh theory-backward reframe after a material mechanism-surface delta or independently new bounded mechanism question — `MECHANISM` — `STOP_NO_COHERENT_MECHANISM_TARGET_ON_CURRENT_EVIDENCE`.
3. H7 native responsibility-sensitive mechanism — `MECHANISM` — `STOP_UNTIL_FRESH_NATIVE_OBJECT_COMPARATOR_RESOURCE_FALSIFIER`.

## Prospective contingency

Cycle 1 may only bind, before outcomes, a behaviorally consequential fresh workload, subsystem operation-count definitions, equal-semantics comparators/resource privileges, and claim scope from safe source/dev surfaces.

- outcome-independent contract is feasible -> STOP and return to fresh Analyst review before synthetic measurement;
- question collapses to post-hoc H5 subtraction/rescue -> `REJECT / TERMINAL_FOR_CURRENT_OBJECT`;
- ordinary sparse/vectorized/localized reductions exhaust the residual question at static stage -> `REJECT / TERMINAL_FOR_CURRENT_OBJECT`;
- behaviorally consequential workload or equal-semantics resource contract cannot be fixed safely -> `HOLD_METHOD_LIMITED / TERMINAL_FOR_CURRENT_OBJECT`;
- outcome knowledge is needed to redesign workload/counters/comparator/resource contract/claim -> STOP and close current object; fresh successor only if independently motivated.

H5 rerun/rescore/retune/relabel/reopen remains prohibited. Candidate #31 third cycle remains prohibited. Same-generation synthetic outcome execution remains unauthorized.

Utility request: **none**.

## Persistence

Persist only `analysis/orchestrator/latest.md`, `analysis/orchestrator/state.json`, and `analysis/orchestrator/history/2026-09-22/0026.md` on `ops/evidence-analyst-handoff`, parent `f8a3e7d8594c47111cade4f726a37ee0ca4fd559`, by one non-force atomic fast-forward commit.