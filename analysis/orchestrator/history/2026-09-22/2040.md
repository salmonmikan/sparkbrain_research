# Evidence Analyst history — R74 — 2026-09-22 20:40 JST

Generation: `EVA-20260922T204000+0900-R74-B8C4E219`

Fresh repository truth closed the prior H7 ref-placement gap but exposed a new preidentity hard-floor defect: FORMAL-R1 never prospectively bound `Episode.split` for fit/calibration/evaluation. MAIN stopped before choosing a value, runner materialization, identity, STARTED or result exposure and persisted a diagnostic at `baaec4a7f1a1b6820d78ff9b33566bb809f74f42`.

R74 classifies the missing split binding as `SCIENCE_AFFECTING_CHANGE`, because the field contributes to exact input identity and may affect input semantics. Under the active development policy, same-candidate continuation is permitted only as explicit versioned development revision because H7 is result-exposed development but not consumed FORMAL science.

Prospective R2 binding is fixed outcome-independently from repository role semantics: `fit=train`, `calibration=dev`, `formal evaluation=test`; `smoke` is prohibited for result-bearing FORMAL input. All other FORMAL-R1 scientific fields remain unchanged. MAIN may materialize R2 and perform outcome-blind preidentity revalidation only; identity/STARTED/protected evaluation/result-bearing execution remain STOP.

Candidate 33 remains `HOLD_METHOD_LIMITED / NONTERMINAL_HOLD / QUEUED / OPEN_DEVELOPMENT` pending a bounded namespace-capable runner that preserves the frozen SYSTEM isolation/provenance contract.

Phenomenon-first remains `PREFETCH_SHADOW`; R74 skips a new scan because R73 was the latest low-rate scan and no new independent phenomenon surface appeared. Standby queue remains empty.

No experiment, result-bearing workflow dispatch, one-way identity consumption, research PR merge, immutable scientific-ref mutation, scheduler mutation, force push or historical result rewrite was performed by Evidence Analyst.
