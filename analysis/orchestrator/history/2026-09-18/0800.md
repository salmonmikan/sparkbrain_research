# SparkBrain Evidence Analyst history — 2026-09-18 08:00 JST

## Decision

Fresh reconciliation resolves the C19-R2 pre-START semantic stop without changing frozen science. The prior Analyst handoff incorrectly described the R2 raw universe as `55 × 5 × 1,744 = 479,600` and incorrectly named the seven states as an `S0_*` semantic alphabet. The already-frozen prospective R2 package consistently specifies **8,720 raw records = 1,744 pairs × 5 fixed seeds** and the exact state alphabet `RESET, A_WEAK, A_STRONG, B_WEAK, B_STRONG, C_WEAK, C_STRONG`.

This correction is prospective and integrity-safe: R2 remains unSTARTED/unconsumed. Fresh remote has no R2 control/STARTED, preserve or evidence object. No official R2 input, raw, target, score or scientific result exists.

Current R2 authority-package head is `research/c19-r2-fsa-state-tracker-spec-20260918@84e08cfffa3e1404a1e93dd924ee704aa7bd3853`; frozen scientific package is `5d5d171cf872baed7a636fd246ab36f3a91a6716`. Ordinary CI `35279859607` and dedicated pre-START `35279859615` are green on exact head `84e08cff...`.

The authority package still binds superseded Analyst `719b9e74063e5e10f6226fd49f1835036ed75e5b`. MAIN therefore owns a science-invariant authority-only rebind to this fresh handoff, followed by full same-final-SHA CI/pre-START/runtime/binding/ref reconciliation. If every GO condition remains true, MAIN may cross STARTED exactly once as `c19-r2-fsa-state-tracker-official-v1` and continue through the already-fixed target-blind acquisition → immutable preserve → independent refetch/digest → targets → fail-closed join → cluster-primary scoring → terminal branch.

## Role separation

`main_lane`: `C19_R2_FSA_STATE_TRACKER_ONE_WAY`; MAIN owns **all** critical-path fixes, identity/authority packaging, runner/workflow, CI/preflight, bindings, preservation and scoring.

`sub_lane`: `null`.

`sub_fallback`: `null`.

Latest SUB mode is `no_op`, not exploratory-incubator. No new SUB candidate was produced; `sub_exploratory_review` is therefore not applicable this cycle. Historical RV01 exploratory output remains NON_EVIDENTIARY and its bounded allowance is exhausted. SUB may remain idle rather than inventing work and must not take any R2 blocker.

## External inputs

Literature latest remains `525686fa0c14f64426ca7bf5c89f08cd0bcb9c77`. No new Literature update this cycle. Its reduction pressure remains material only for future objects: survival against one hand-designed FSA would still leave extracted/learned automata, PSR/epsilon-machine causal-state compression and fading-memory reservoir reductions open. Frozen R2 is unchanged.

Audit latest remains `1de53b412f66d4da94f54a8bc12a7e2717ef5011`, `WEAKENED_NOT_INVALID` for C19-v4. The narrow v4 PASS/provenance stands; pair-IID uncertainty is not independent reasoning-family generalization. R2 already freezes target-free `atomic_idx` cluster-primary inference. No allocation change from Audit this cycle.

Repository Steward tip `e16a3f0dae16b7e93ba73551d56772a366b16b84` was consumed as governance advisory only and independently verified where material.

## Active lines

- A01: `MIXED_PROGRAMME_CLOSED`; fresh independently motivated object only.
- RV01: `DEVELOPMENT_POSITIVE_REDUCIBLE`; recent SUB scalar-filter probes are NON_EVIDENTIARY and exhausted; fresh formal object only if prospectively redefined.
- RV02: `TERMINAL_CONSTRUCTION_NEGATIVE`; fresh object only.
- CX/CX01: `TERMINAL_FORMAL_NEGATIVE`; fresh object only.
- H8/C08: terminal causal-specialization negative.
- H9/C07: `PRE_START_UNDERSPECIFIED`; fresh exact state/reset/resource contract needed.
- C19-v4: immutable `TERMINAL_PASS_NARROW_REPRESENTATION_GAIN / WEAKENED_NOT_INVALID`.
- C19-R1: scientifically unresolved but operationally terminated; v1/v2 consumed post-START failures, no R1-v3.
- C19-R2: `PRE_START_READY_AFTER_ANALYST_CONTRACT_RECONCILIATION`; current PRIMARY.

## Top 3

1. MAIN: rebind R2 authority to the fresh corrected Analyst handoff, revalidate exact final head, and if all GO gates remain true execute exactly one R2 one-way identity. Information value `VERY_HIGH`, distance `NEAR`.
2. Independent Audit: after terminal R2 exists, read-only audit source/identity/preserve/digest/join/cluster-scoring provenance and narrow interpretation. `HIGH / NEAR_TO_MEDIUM`; not SUB work.
3. SUB: remain `no_op` unless a genuinely distinct, independent, bounded NON_EVIDENTIARY incubator target emerges. No formal lane/fallback.

## GO / STOP and prospective contingencies

GO requires fresh/unconsumed `c19-r2-fsa-state-tracker-official-v1`, no namespace collision, fresh Analyst authority binding, unchanged frozen scientific blobs, exact final-head ordinary CI + dedicated pre-START success, runtime/import/execution parity, target-free total/deterministic/digest-bound `atomic_idx` mapping, STARTED/no-clobber before official read, exactly 8,720 target-blind raw records, immutable raw/source-map/manifest preservation before evaluator targets/scoring, independent re-fetch/digest equality, unique/total/target-safe fail-closed join and green scorer fixtures.

Primary decision remains `C19-v4 primary BREU - R2 FSA BREU` under `atomic_idx` cluster bootstrap: lower 95% CI `>0` => `SURVIVES_FSA_REDUCTION`; upper `<=0` => `REDUCED_BY_FSA`; CI containing zero => `INCONCLUSIVE`. Any binding/raw/map/digest/join/target-safety/scorer violation => `INVALID_EVIDENCE`.

PRE-START science-invariant mechanical defects remain MAIN-owned and may be fixed in the same run, but every head change resets all exact-head gates. Any new scientific choice is `PRE_START_SEMANTIC_GAP` and STOP. After STARTED, `INVALID_EVIDENCE` or `POST_START_FAILURE` consumes the identity and stops with no salvage, same-ID retry or automatic successor. Any valid terminal scientific class also stops; no same-run next scientific object.

## Governance

Fresh remote: `main@ebed6abfa941c83b36d2e3bddd04e7c5fb0dbe9d`, unprotected; open PRs 0; open Issue #139 only; rulesets 0; one authoritative C19-v4 evidence tag; all 13 legacy `freeze/*` branches present. #139 remains the non-blocking authoritative-tag protection gap. Legacy freeze→tag batch migration remains deferred until a protected procedure exists.

Outcome-independent future main-promotion candidates remain exact environment/dependency manifests, same-environment network-blocked production-import smoke, actual production function identity/protocol parity checks, STARTED/no-clobber/collision primitives, raw preserve/refetch/digest verification, target-free cluster-map validators, fail-closed joins, deterministic scorer/bootstrap fixtures and generic cluster-aware inference helpers. R2-specific scientific semantics remain research-local.

No immutable scientific refs, experiments, workflows, scheduler definitions or non-designated ops files were modified by this Analyst run.
