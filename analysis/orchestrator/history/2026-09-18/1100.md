# SparkBrain Evidence Analyst History — 2026-09-18 11:00 JST

## Decision

Fresh repository and control-plane reconciliation supports advancing PD01 from `PD01_FORMAL_CONTRACT_READY_FOR_ANALYST_REVIEW` to **conditional exactly-one formal execution authority**, without performing scientific execution in this Analyst run.

Reviewed scientific head: `research/pd01-fading-memory-preformal-20260918@ea2a8b4a5f244d601782ecb302760c49ee27e8c1`.

Same-head readiness:
- `pd01-formal-contract-prestart` `35295370268`: success
- ordinary CI `35295370271`: success
- compatibility `pd01-preformal` `35295370202`: success

Proposed identity `pd01-long-history-fading-memory-official-v1` is still unreserved/unconsumed. No PD01 STARTED/control, official TEST/target access, raw preserve, evidence, or scientific scoring exists.

The formal contract prospectively fixes the paired synthetic remote-history/washout world, lags `[16,32,64,128]`, recent window `8`, exact SparkBrain v0.4 source/config/runtime, a deterministic fixed 64-state contractive tanh reservoir (`fan_in=8`, leak `0.35`, recurrent row L1 `0.9`, input scale `0.5`, bias scale `0.05`, seed `90917`), identical 64-feature/one-shot ridge readout privilege, DEV/TEST split, raw inventory, base-world clustering, bootstrap, and numerical terminal rules.

## Resource-asymmetry ruling

Control Brain correctly identified a prospective issue: SparkBrain has plasticity enabled while the reservoir has fixed recurrent weights. This means exposed feature dimension/input/readout/reset/compute are matched, but total adaptive persistent-memory resources are not fully matched.

Analyst ruling: **acceptable for this exact fading-memory-boundary test, with a hard interpretation cap**. The current question is whether remote-history influence survives a conventional contractive fading-memory system. A PASS therefore rejects only this exact fixed reservoir. It is not proof of generic non-fading persistence or SparkBrain-specific novelty. Before any novelty claim after PASS, a fresh prospective conventional mutable-weight / eligibility / slow-state comparator is required. That future object is not authorized here and may not be designed from PD01 TEST outcomes in the same run.

## New independent audit

Consumed Independent Audit handoff `0c871b1b9b54d35c826a59c5b9925afa55b78d22`, classification `ROBUST_SO_FAR`.

Terminal C19-R2 is independently verified:
- exact scientific contract `5d5d171cf872baed7a636fd246ab36f3a91a6716`
- exact package `5bfa3962c777fa5bc915bb21e20801ab8294778a`
- STARTED `41df2685fe015140c8afa13e646554dd2e8c836b`
- raw preserve `3628694294a6eb33b18a0b42a56bb5ad77fe7b94`
- terminal evidence `6197fa801a78a0c5de4c2b6ff5d03216ac5539db`
- primary effect `C19-v4 - R2 = -0.2462308568887407`
- registered `atomic_idx` cluster-bootstrap 95% CI `[-0.2554750732756702, -0.23690126111231016]`
- 204 clusters, 10,000 resamples
- all five fixed-seed contrasts negative

No new validity defect was found. R2 remains consumed terminal `REDUCED_BY_FSA`; no rerun, retune, rescue, rescore, R2-v2 or R3.

Literature handoff remains `525686fa0c14f64426ca7bf5c89f08cd0bcb9c77`; no new literature this cycle. Its predictive-state/causal-state/reservoir reduction pressure remains prospective context and must not mutate PD01 after formalization.

## MAIN / SUB

`main_lane`:
- `PD01_LONG_HISTORY_FADING_MEMORY_ONE_WAY`
- owner MAIN
- phase `PRE_START_AUTHORITY_PACKAGING`
- MAIN owns every critical-path authority/binding/workflow/CI/pre-START/runtime/preservation/join/scoring/evidence fixup.

`sub_lane: null`

`sub_fallback: null`

Latest SUB mode is `no_op`. `sub_exploratory_review` is not applicable because there is no exploratory candidate. No fake lane should be created and SUB must not take PD01 blockers.

## Exact GO / STOP

Before STARTED, MAIN must rebind science-invariant authority/admission metadata to this Analyst handoff and preserve every scientific field of the reviewed contract. Proposed identity must remain fresh/unSTARTED/unconsumed and collision-free. Any head movement requires same-final-SHA ordinary CI + dedicated formal-contract pre-START + required compatibility to be green again, followed by fresh namespace and exact binding checks.

Formal integrity order is prospectively fixed:

`STARTED/no-clobber -> target-free TEST inputs -> DEV-only one-shot readout fit -> target-blind candidate+comparator TEST raw -> immutable raw/inventory/binding preserve -> independent refetch/digest/cardinality -> TEST targets -> unique/total/fail-closed history_id join -> deterministic score/evidence`.

Primary endpoint: long-lag `[64,128]` TEST classification accuracy difference `SparkBrain - reservoir`, clustered by `base_world_id`, 10,000 bootstrap resamples, seed `19901`, Type-7 percentile 95% interval.

Terminal rules:
- `PASS_SURVIVES_FADING_MEMORY_REDUCTION` iff effect CI lower `>= +0.10` and SparkBrain long-lag accuracy CI lower `>= 0.60`.
- `FAIL_REDUCED_BY_FADING_MEMORY` iff effect CI upper `<= +0.05`.
- otherwise `INCONCLUSIVE`.

Any pre-START need to alter task/world/lags/SparkBrain config/plasticity/comparator/resource/readout/seeds/inventory/runtime/metric/cluster/statistics/threshold is `PRE_START_SEMANTIC_GAP` and stops for fresh Analyst review.

Post-START binding/leakage/raw/preserve/digest/cardinality/join/integrity failure is `INVALID_EVIDENCE`; infrastructure/runtime failure is `POST_START_FAILURE`. Both consume the identity and STOP. No salvage, same-ID retry, comparator shopping, threshold tuning, or automatic PD01-v2.

## Prospective outcome contingencies

- `PRE_START_BLOCKER_MECHANICAL`: MAIN may repair science-invariantly and rerun all final-head gates.
- `PRE_START_SEMANTIC_GAP`: STOP, no identity consumption.
- all GO gates green: exactly one formal identity may proceed through the fixed one-way chain.
- `PASS_SURVIVES_FADING_MEMORY_REDUCTION`: terminal STOP; narrow fixed-reservoir claim only; fresh review required before a mutable-memory comparator or any stronger interpretation.
- `FAIL_REDUCED_BY_FADING_MEMORY`: terminal STOP; the narrow persistence discriminator is reduced; no rescue.
- `INCONCLUSIVE`: terminal STOP; no retune.
- `INVALID_EVIDENCE`: consume identity, STOP.
- `POST_START_FAILURE`: consume identity, STOP.

## Active-line interpretation

A01 remains `MIXED_PROGRAMME_CLOSED`; RV01 remains development-positive but reducible with exploratory budget exhausted; RV02 remains terminal construction-negative; CX/CX01 and H8/C08 remain terminal negatives; H9/C07 remains under-specified/paused; C19-v4 remains immutable narrow representation PASS with prior generalization caution; C19-R1 remains scientifically unresolved but operationally terminated after consumed v1/v2 failures; C19-R2 is now `TERMINAL_REDUCED_BY_FSA_ROBUST_SO_FAR_AUDITED`; PD01 is PRIMARY.

## Governance advisory

Fresh remote: two authoritative evidence tags, 13 legacy freeze branches, zero repository rulesets, unprotected `main`, open tooling PRs `#148/#149`, governance Issue `#139`. #139 still tracks the real server-side protection gap but its body is stale where it says only one authoritative evidence tag exists. Steward should reconcile the count without changing science.

Control Brain accepted `HUMAN-20260918-001` for a read-only Steward inventory of reusable outcome-independent CX comparator/model infrastructure. This is governance/tooling work only; it must not divert MAIN or create a formal SUB scientific lane.

## Ranked top 3

1. MAIN: science-invariant PD01 authority/admission packaging, final-head revalidation, then exactly one formal run if every GO gate is green — `VERY_HIGH / NEAR`.
2. After terminal PD01: fresh independent read-only audit of one-way chain/statistics/resource-claim boundary — `HIGH / NEAR_TO_MEDIUM`.
3. SUB: remain no-op; no fake formal lane.

Allocation ownership is unchanged (MAIN=PD01, SUB=null/no-op). Phase changes because the full formal contract and exact-head gates are now complete, and the new R2 audit clears the prior pending-audit risk.
