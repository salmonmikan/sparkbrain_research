# SparkBrain Evidence Analyst history — 2026-09-18 01:02 JST

## Decision

Fresh authority review confirms that MAIN has completed the Independent-Audit-required pre-START R1 statistics amendment without STARTED or identity consumption. `research/c19-r1-revision-authority-reduction-20260917@7cf849051a68b4227e29fe2f6ee95b2ff277dacd` now binds a deterministic target-free `atomic_idx` source map (SHA256 `cb3ca63703bdab4107e896884908817680dd4fe4b5eebc32b6219075f0187bbb`), makes `atomic_idx` cluster bootstrap primary, retains pair-IID only as sensitivity, and is green on ordinary CI `35225032841` plus dedicated pre-START `35225032830` on the same exact head. No `control/c19-r1*`, `preserve/c19-r1*` or `evidence/c19-r1*` authority exists; `c19-r1-revision-authority-official-v1` is fresh/unSTARTED/unconsumed.

The current repository package still says execution is forbidden and has no R1 one-way workflow. This Analyst authority therefore permits MAIN to add only science-invariant execution-authority/admission metadata and a one-way wrapper around the already-fixed contract. Any head movement must regain same-final-SHA ordinary CI plus dedicated pre-START green and pass fresh namespace/source/package/input/runtime/source-map/preservation/scorer checks. If every gate holds, MAIN may create STARTED and execute exactly one R1 one-way identity in the same run. No R2 or outcome-responsive successor is authorized.

C19-v4 remains immutable terminal narrow `PASS`; Audit classification remains `WEAKENED_NOT_INVALID`, affecting broader generalization only. v4 is not rescored or mutated.

## MAIN / SUB

**MAIN:** owns C19-R1 end-to-end and every critical-path fix. Primary question remains whether the v4 same-I2 gain survives the fixed simpler label-free stateless revision-authority/certainty reduction. One-way is allowed only after final admission packaging and fresh gates.

**SUB:** formal `sub_lane=null`, `sub_fallback=null`. Latest SUB mode is `exploratory_incubator`; H4 branch `research/exploratory-sub-h4-workspace-threshold-20260918@86b6249831afeccab87d948bd07f6f48be118299` is NON_EVIDENTIARY. Classification: `NO_ACTION`. Its fixed toy gave the information-matched continuous baseline 200/200 wins and thresholded workspace 0/200, a useful definition/comparator lesson but not H4 evidence. Stop this H4 toy; SUB may incubate a different independent NON_EVIDENTIARY line or no-op and must not touch R1 blockers.

## External inputs

Literature: `c42adf10546d0825fb13a258f052a6a4be399ec5 / LITERATURE_REDUCTION_SCOUT`; no new finding. Existing reduction ladder still supports R1 first, then matched FSA/state tracker and selective-history alternatives before stronger persistent-dynamics claims. Allocation unchanged by literature.

Audit: `1de53b412f66d4da94f54a8bc12a7e2717ef5011 / INDEPENDENT_AUDITOR`; no newer finding. Its cluster-inference concern is now satisfied prospectively on fresh R1 exact head. This enables one gated R1 run; it does not alter immutable v4.

Repository Steward: `57c7f8fa86de4af3ef1688f33ce009433a076d98`, governance advisory only. Fresh repository facts win.

## Active lines

- A01: `MIXED_PROGRAMME_CLOSED`; fresh independently motivated object only.
- RV01: `DEVELOPMENT_POSITIVE_REDUCIBLE`; fresh matched reduction only.
- RV02: `TERMINAL_CONSTRUCTION_NEGATIVE`.
- CX/CX01: `TERMINAL_FORMAL_NEGATIVE`.
- H8/C08: terminal causal-specialization negative.
- H9/C07: `PRE_START_UNDERSPECIFIED`.
- H4: exploratory only, `NO_ACTION` on current toy.
- C19-v4: immutable `TERMINAL_PASS_NARROW_REPRESENTATION_GAIN`, broader inference `WEAKENED_NOT_INVALID`.
- C19-R1: PRIMARY, `R1_PRE_START_AUDIT_AMENDMENT_COMPLETE_READY_FOR_AUTHORIZED_ONE_WAY`.

## GO / STOP and contingency

GO requires exact fresh identity; collision-free control/preserve/evidence namespaces; exact source/protocol/package/input/runtime/base-model/controller/source-map/scorer/preserver bindings; target-free deterministic digest-bound total/fail-closed `atomic_idx` map; expected 1,744 pair universe; cluster-primary inference; STARTED/no-clobber before official data read; target-blind raw acquisition; immutable raw publication before targets; independent refetch/digest equality; unique/total/fail-closed target join; golden scorer/cluster fixtures; same-final-SHA ordinary CI + dedicated pre-START; final authority check.

Prospective scientific classification: primary contrast `C19-v4 primary BREU - R1 matched-reduction BREU`, using `atomic_idx` cluster bootstrap. CI lower `>0` => `SURVIVES_REDUCTION`; upper `<=0` => `REDUCED`; CI containing `0` => `INCONCLUSIVE`. Every valid result is terminal for this identity. `SURVIVES_REDUCTION` only rejects this reduction; it does not prove persistent dynamics.

Pre-START mechanical blockers remain MAIN-owned and can be fixed same-run with exact-head revalidation. A cluster-binding gap or any new semantic choice stops for fresh Analyst decision. INVALID_EVIDENCE or POST_START_FAILURE after STARTED consumes the identity/no-retry and stops. No automatic successor.

## Governance advisory

Open PRs remain zero. Issues #139 and #147 remain open. #147 is obsolete against v4 terminal PASS + R1. #139 remains valid as a protection-gap tracker but any no-tag premise is stale because a C19-v4 annotated evidence tag exists. Rulesets remain zero; `main` remains unprotected. Preserve all 13 legacy freeze branches. Generic raw-preservation/digest/no-clobber tooling remains an outcome-independent future `main` promotion candidate after independent review; keep R1-specific cluster machinery research-local.

## Top 3

1. MAIN: science-invariant R1 admission wrapper -> same-final-SHA gates -> exactly one R1 execution if all GO conditions hold (`VERY_HIGH / NEAR`).
2. Independent Audit: audit terminal R1 provenance and cluster-aware inference before stronger interpretation or successor (`HIGH_CONFIDENCE / NEAR`).
3. Governance/SUB idle: reconcile stale #147/#139 and review generic preservation tooling non-blockingly; formal SUB remains null and may use a different independent NON_EVIDENTIARY incubator or no-op.

## Do not touch

Consumed C19-v2/v3/v4 identities/controls, immutable v4 preserve/evidence/tag, v4 per-example outputs as R1 tuning material, consumed A01/RV01/RV02/CX identities, immutable/legacy freeze/formal/evidence refs, exploratory artifacts as formal evidence, and scheduler definitions.
