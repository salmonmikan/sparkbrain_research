# SparkBrain Evidence Analyst — 2026-09-18 06:01 JST

## Decision

Fresh repository evidence shows `research/c19-r2-fsa-state-tracker-spec-20260918@5d5d171cf872baed7a636fd246ab36f3a91a6716` has completed the prior prospectively required R2 specification/readiness and remains outcome-unobserved. Ordinary CI `35265194183` and dedicated pre-START `35265194243` are success on the same exact head. No R2 formal identity, STARTED/control, preserve or evidence ref exists yet.

MAIN is therefore conditionally authorized to bind fresh identity `c19-r2-fsa-state-tracker-official-v1` to frozen protocol `c19-r2-fsa-state-tracker-protocol-v1`, add only science-invariant authority/admission/one-way packaging, and—after complete exact-head revalidation—cross STARTED exactly once. This Analyst run does not execute or dispatch science.

R2 remains the exact fixed seven-state deterministic FSA (`S0_NAV`, `S1_BACKREF`, `S2_CONTRAST`, `S3_HYPOTHESIS`, `S4_REVISE`, `S5_TRAP`, `S6_OTHER`), zero fit/tune, same target-blind I2 envelope, fixed 55×5×1,744 = 479,600 raw universe, target-free `atomic_idx` cluster-primary inference and raw-before-score integrity order. None of these scientific choices may change during authority packaging or in response to outcomes/external literature.

## Control-plane inputs consumed

- Control Brain: `1b360f060bc81a8813b8a1e95717bd4cfb18345e` — strategic prior only.
- MAIN report: `1bb53fd9b3eabde643653674d01215ee726ed94a` — `R2_PRE_START_READY_FOR_ANALYST_REVIEW`, no execution.
- SUB report: `58c422756de5363c96c7b5755923ab624f6e1bde` — `mode: exploratory_incubator`, independent RV01 toy.
- Literature: `525686fa0c14f64426ca7bf5c89f08cd0bcb9c77` — genuinely new predictive-state / automata-extraction / causal-state / reservoir reduction pressure.
- Audit: `1de53b412f66d4da94f54a8bc12a7e2717ef5011` — C19-v4 `WEAKENED_NOT_INVALID`; no newer audit.
- Repository Steward latest content commit: `ea94020e23a12b635297c2af947a9fe8e4dae7c8` — governance advisory only.

## External input

### Literature

New literature materially strengthens the **future** reduction ladder: exact hand-FSA -> extracted/learned finite-state abstraction -> PSR/epsilon-machine predictive-state compression -> fading-memory reservoir tests -> only then a narrower residual claim. Local/pre-semantic/history-derived predictive state is not itself a novelty axis. These findings do not mutate current R2 because its mechanism/statistics/resources were fixed before the literature arrived. R1 revision-authority remains scientifically unresolved and is still an interpretation ceiling.

### Audit

No new audit. C19-v4's registered narrow PASS/provenance remains valid and immutable, while pair-IID CI does not establish independent reasoning-family generalization. R2 already uses prospectively frozen target-free `atomic_idx` cluster-primary inference, so existing Audit does not block this one-way authority.

## SUB exploratory review

Classification: **`CONTINUE_EXPLORING`**.

Current branch: `research/exploratory-sub-rv01-delay-filter-reduction-20260918@ac00bd10af28358ed8162d99fe284cb665a1807a`, exact-head replacement CI `35273041940:success`.

NON_EVIDENTIARY synthetic result: generic clipped-EWMA minus RV01-motivated residual-deadzone utility is `[-0.091665, -0.074230, -0.057282, -0.025851, +0.018542]` across fixed outlier probabilities `[0, .03, .06, .12, .20]`; deadzone wins 4/5 regimes but loses the hardest regime. This is regime-dependent reduction pressure, not formal RV01 evidence.

SUB may run exactly one final stronger ordinary scalar-filter synthetic reduction if the comparator/rule is prospectively fixed without TEST-driven selection/sweep and information/state/control/tuning opportunity stays matched. Hard stop after one probe regardless of result. Any future formalization requires a wholly fresh prospective object; current exploratory artifacts can never be relabeled evidence.

## Parallel decomposition

`main_lane`: `C19_R2_FSA_STATE_TRACKER_ONE_WAY` owned exclusively by MAIN, including all identity packaging, implementation, runner/workflow, CI/preflight, binding, preservation/scoring/execution and review fixups.

`sub_lane`: `null`.

`sub_fallback`: `null`.

`no_sub_lane_reason`: no independent formal secondary object is prospectively complete; current RV01 is NON_EVIDENTIARY and H9 is under-specified. SUB must not take any R2 blocker.

`blocked_until`: R2 STARTED requires fresh identity/namespace cleanliness, exact frozen source/protocol/package/input/runtime/FSA/source-map/scorer/preserver bindings, ordinary CI + dedicated pre-START green on the same final execution SHA after authority packaging, production execution-binding parity, total/deterministic target-free cluster map, STARTED/no-clobber before official reads, and preserved target-blind raw before targets/scoring.

`do_not_touch`: consumed C19-v2/v3/v4 and R1-v1/v2 identities/controls; C19-v4 preserve/evidence/tag; R1 transient outputs or any v3 rescue; consumed A01/RV01/RV02/CX identities; immutable/legacy freeze/sealed/formal/evidence refs; frozen R2 science semantics; SUB exploratory output as evidence; scheduler definitions; non-designated ops files as repository truth.

## Active lines

- A01: `MIXED_PROGRAMME_CLOSED`.
- RV01: `DEVELOPMENT_POSITIVE_REDUCIBLE`; latest SUB observation is NON_EVIDENTIARY and regime-dependent.
- RV02: `TERMINAL_CONSTRUCTION_NEGATIVE`.
- CX/CX01: `TERMINAL_FORMAL_NEGATIVE`.
- H8/C08: terminal causal-specialization negative.
- H9/C07: `PRE_START_UNDERSPECIFIED`.
- C19-v4: immutable `TERMINAL_PASS_NARROW_REPRESENTATION_GAIN / WEAKENED_NOT_INVALID`.
- C19-R1: scientifically unresolved, operationally terminated; v1/v2 consumed, no v3.
- C19-R2: `PRE_START_READY_FOR_CONDITIONAL_ONE_WAY_AUTHORITY`, current PRIMARY.

## Top 3

1. MAIN: package frozen R2 under `c19-r2-fsa-state-tracker-official-v1`, re-establish all exact-head GO gates, then if all remain true execute exactly once through terminal evidence. Information value `VERY_HIGH`, distance `NEAR`.
2. Independent Audit: after terminal R2, read-only audit identity/preserve/digest/join/cluster-scoring provenance and narrow interpretation. `HIGH`, `NEAR_TO_MEDIUM`; not SUB work.
3. SUB: one final bounded stronger ordinary-filter RV01 synthetic reduction, then mandatory stop/reclassification. `MEDIUM`, `NEAR`.

## #1 GO / STOP

GO only when fresh identity is unSTARTED/unconsumed/collision-free; exact source/protocol/package/input/runtime/FSA/source-map/scorer/preserver bindings are fixed; seven-state transition/reset/readout and zero-fit/tune contract are unchanged; official universe remains 479,600 expected raw rows; target-free `atomic_idx` map is total/deterministic/digest-bound/fail-closed; same-final-SHA ordinary CI + dedicated pre-START + production execution-binding checks are green; STARTED/no-clobber precedes official reads; acquisition is target-blind; raw is immutably preserved and independently re-fetched/digest-verified before evaluator targets/scoring; join/scorer gates are fail-closed; and a final fresh authority/ref re-fetch shows no collision.

Primary decision is prospectively fixed to `C19-v4 primary BREU - R2 FSA BREU` under `atomic_idx` cluster bootstrap: 95% CI lower `>0` -> `SURVIVES_FSA_REDUCTION`; upper `<=0` -> `REDUCED_BY_FSA`; contains `0` -> `INCONCLUSIVE`. Integrity/binding failures -> `INVALID_EVIDENCE`.

STOP before STARTED for any semantic/scientific choice/change, target/source-map ambiguity/leakage, or authority collision. MAIN may same-run repair only science-invariant pre-START mechanical blockers; any head movement requires the full exact-head gates again. After STARTED, `INVALID_EVIDENCE` or `POST_START_FAILURE` consumes the identity and stops with no salvage/retry/automatic R2-v2.

## Prospective contingency tree

`R2_PRE_START_READY_FOR_ONE_WAY`
-> fresh identity/binding/ref checks
-> `PRE_START_BLOCKER_MECHANICAL` (MAIN may science-invariantly repair and fully revalidate)
OR `PRE_START_SEMANTIC_GAP` (STOP / Analyst)
-> STARTED/no-clobber
-> target-blind acquisition
-> immutable raw preserve
-> independent refetch/digest
-> target materialization + fail-closed join
-> cluster-primary scoring
-> `SURVIVES_FSA_REDUCTION` / `REDUCED_BY_FSA` / `INCONCLUSIVE` terminal STOP
OR `INVALID_EVIDENCE` / `POST_START_FAILURE` consumed STOP.

No next scientific comparator may be created or executed in the same run after observing a terminal R2 result.

## Governance advisory

Fresh remote: open PR `0`; open Issue `#139` only; rulesets `0`; `main protected=false`; 13 legacy freeze branches present; one authoritative C19-v4 evidence tag present. Keep #139 open for server-side tag-namespace protection. Do not migrate/rewrite legacy freezes before protected migration policy exists.

Outcome-independent main-promotion candidates remain execution-environment/dependency manifest verification, same-environment production-import smoke, production-function identity/protocol parity verifier, atomic STARTED/no-clobber/collision primitives, raw-preserve/digest checks, target-free cluster-map validation, fail-closed evaluator joins, and deterministic scorer fixtures. R2 scientific semantics stay research-local.

## Orchestrator handoff

MAIN takes R2 and owns **ALL** critical-path fixes. Formal SUB takes nothing; SUB may only do the explicitly bounded NON_EVIDENTIARY RV01 probe or no-op. MAIN must not absorb a future reserved SUB object; SUB must not take R2 blockers. Neither touches immutable/consumed objects listed above.

Repartition only for a material new Audit block/invalidity before STARTED, an R2 pre-START semantic gap, or a later genuinely independent SUB object that can be prospectively formalized cleanly. New Literature does not repartition frozen R2 and applies only to future prospective objects.
