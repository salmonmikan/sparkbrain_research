# SparkBrain Evidence Analyst — 2026-09-18 14:00 JST

## Decision

No new formal scientific measurement was produced in this cycle. The material update is NI01/H4 **prospective readiness**.

MAIN consumed the previous `NEXT_FRONTIER_PROSPECTIVE_SELECTION` allocation and selected exactly one independently motivated pre-existing question: H4 / native No-Ignition selective prediction. The object is not a rescue of C19-R2 or PD01. Exact reviewed branch/head: `research/ni01-no-ignition-selective-prediction-spec-20260918@2664951b65dd18883d3d80862e80b86ac66cf24f`.

The proposed identity `ni01-no-ignition-selective-prediction-official-v1` is still UNRESERVED/unconsumed. Fresh remote checks found no NI01 STARTED/control ref, preserve ref, or evidence tag. Dedicated preformal `35308269458` and ordinary CI `35308269286` are both `completed/success` on the same exact head.

Evidence Analyst therefore advances MAIN to **conditional exactly-one NI01 one-way authority**. MAIN may bind this authority and create STARTED/execute exactly once only after final identity/namespace/binding checks and same-final-SHA gates remain green. No scientific execution is performed by this Analyst run.

## Streams consumed

- Control Brain latest commit `bab2933fdfed743b6dc43d911e06c710dc0e56a6` — strategic prior only.
- MAIN latest report commit `fa08ff630b886b9ad20981da74356d4d916a9c58`.
- SUB latest report commit `e5ae9d151fecb5508d8783d1b153b97747033a34`.
- Literature `525686fa0c14f64426ca7bf5c89f08cd0bcb9c77` — no new Literature this cycle.
- Independent Audit `0c871b1b9b54d35c826a59c5b9925afa55b78d22` — no new Audit this cycle; latest remains C19-R2 `ROBUST_SO_FAR`.
- Repository Steward `5803ae0dda7cc04748fde902e7e71d235b1bafa1` — governance advisory only.

## NI01 prospective contract reviewed

Scientific question: does native No-Ignition add selective decision value beyond an ordinary target-blind confidence-abstention head built from the same per-step probability vector on fresh noisy/unresolved controlled worlds?

Candidate: native `full` SparkBrain/default `BrainConfig`, no TEST adaptation, no target access.

Comparator: zero-trainable-parameter confidence abstention; one scalar threshold per world chosen from target-free DEV only to minimize absolute coverage mismatch to native candidate, deterministic tie break, no labels/targets/future/scenario tags/PD01-C19 outcomes.

Worlds: `reliability_world`, `delayed_evidence_world`, `contradiction_world`. DEV seeds `510000..510255`, 256 episodes/world. TEST seeds `610000..610511`, 512 episodes/world, 30 steps/episode; expected raw `1536` episodes / `46080` steps.

Primary effect: comparator overall selective-decision loss minus candidate overall loss, equal-world weighted. Statistics: episode-cluster stratified bootstrap within each world, 10,000 resamples, seed `74017`, Type-7 95% percentile CI.

Classification is fixed prospectively:

- coverage guard: absolute TEST candidate/comparator coverage difference `<=0.03` in every world;
- `PASS_NATIVE_NO_IGNITION_ADDS_SELECTIVE_VALUE`: guard passes, effect CI lower `>=0.02`, every-world point effect `>0`;
- `FAIL_REDUCED_BY_CONFIDENCE_ABSTENTION`: guard passes, effect CI upper `<=0.005`;
- `INCONCLUSIVE`: all other valid outcomes.

Claim boundary: PASS establishes only value over this exact coverage-matched max-confidence comparator on these three registered worlds/metric. It does not establish full H4/OOD generality or novelty. FAIL reduces only this registered discriminator; it does not show No-Ignition is globally useless.

## External input

### Literature

No new Literature handoff. Existing ordinary-reduction doctrine does not require changing NI01 because its canonical H4 null is already represented by the registered confidence-abstention comparator. Do not retrofit stronger abstention or OOD worlds into this identity. Any broader test must be fresh/prospective.

### Audit

No new Audit handoff. C19-R2 remains independently `ROBUST_SO_FAR`. PD01 has not yet had a fresh terminal audit, but that does not block NI01 because NI01 is independently motivated and excludes C19/PD01 outcomes from its rationale/comparator. A future object materially relying on PD01 should consume a fresh PD01 audit before STARTED.

## SUB review

SUB mode: `no_op`. No incubator candidate exists, so `sub_exploratory_review` is not applicable. `sub_lane=null`; `sub_fallback=null`.

SUB must not take NI01 candidate/comparator, CI/pre-START, authority/binding, workflow, STARTED, raw preservation, scoring, evidence, or any other MAIN blocker.

## Active-line interpretation

- A01: `MIXED_PROGRAMME_CLOSED`; fresh independent programme only.
- RV01: `DEVELOPMENT_POSITIVE_REDUCIBLE`, exploratory budget exhausted; fresh object only.
- RV02: `TERMINAL_CONSTRUCTION_NEGATIVE`; fresh object only.
- CX/CX01: `TERMINAL_FORMAL_NEGATIVE`; fresh object only.
- H8/C08: terminal negative; distinct fresh object only.
- H9/C07: `PRE_START_UNDERSPECIFIED`; exact state/reset/resource contract required before revival.
- C19-v4: terminal narrow PASS / `WEAKENED_NOT_INVALID`; immutable.
- C19-R1: scientifically unresolved but operationally terminated; v1/v2 consumed; no v3.
- C19-R2: `TERMINAL_REDUCED_BY_FSA / ROBUST_SO_FAR_AUDITED`; STOP.
- PD01: `TERMINAL_FAIL_REDUCED_BY_FADING_MEMORY`; consumed/no-retry; audit only.
- NI01/H4: `PRE_START_READY_FOR_CONDITIONAL_ONE_WAY_AUTHORITY`; MAIN primary.

## Two-worker allocation

`main_lane = NI01_NATIVE_NO_IGNITION_SELECTIVE_PREDICTION_ONE_WAY`.

MAIN owns ALL critical-path fixes and execution-chain work. Planned identity: `ni01-no-ignition-selective-prediction-official-v1`. Conditional exactly-one execution is authorized only after fresh authority binding, identity/namespace freshness, frozen exact bindings, target-blind acquisition proof, and same-final-SHA green gates.

`sub_lane = null`.

`sub_fallback = null`.

No formal SUB lane exists because no independent secondary object is prospectively complete; H9 is under-specified, RV01 incubator budget is exhausted, and terminal lines must remain closed.

## GO / STOP

GO requires identity fresh/unSTARTED/unconsumed; collision-free control/preserve/evidence namespaces; exact source/protocol/package/input/runtime/candidate/comparator/scorer/preserver binding; scientific contract unchanged; ordinary CI + dedicated NI01 pre-START green on the same final execution SHA; STARTED/no-clobber before official TEST read; target-free DEV threshold derivation; target-blind raw with no truth/target/`decision_justified`/target-derived scenario tags; immutable preserve before TEST targets; independent remote refetch/digest/cardinality; unique/total/fail-closed join on `(world, seed, step_index)`; deterministic registered scoring/bootstrap.

STOP before STARTED if a scientific selection changes, identity is not fresh, namespace collision exists, or invariance cannot be proved without semantic change. After STARTED, `INVALID_EVIDENCE` or `POST_START_FAILURE` consumes the identity and terminates with no retry/salvage.

## Prospective contingency tree

Root: `NI01_PRE_START_READY_FOR_CONDITIONAL_ONE_WAY_AUTHORITY`.

- `PRE_START_BLOCKER_MECHANICAL`: MAIN may repair science-invariantly, then re-run every same-final-SHA gate.
- `PRE_START_SEMANTIC_GAP`: STOP for fresh Analyst review.
- GO chain: fresh identity/collision -> STARTED/no-clobber -> target-free DEV threshold -> target-blind TEST raw -> immutable preserve -> independent refetch/digest/cardinality -> TEST targets -> fail-closed join -> deterministic score/bootstrap -> annotated terminal evidence.
- PASS: terminal STOP; no same-run OOD/general novelty extension.
- FAIL: terminal STOP; no rescue/comparator shopping/automatic v2.
- INCONCLUSIVE: terminal STOP; no threshold/world retune.
- INVALID_EVIDENCE: consumed identity, STOP, no salvage/retry.
- POST_START_FAILURE: consumed identity, STOP, no salvage/retry/automatic successor.

Same-run continuation is valid only through this prospectively fixed NI01 chain to terminal evidence.

## Governance advisory

Fresh remote: `main@ebed6abfa941c83b36d2e3bddd04e7c5fb0dbe9d`, unprotected; repository rulesets `0`; legacy `freeze/*` branches `13`; authoritative `evidence/*` annotated tags `3`; open non-PR issue `#139`; open PRs `#148/#149`.

Repository Steward corrected #139's evidence-tag inventory to three while leaving the substantive protection gap open. Freeze migration remains deferred. Steward's read-only main-promotion inventory is advisory: neutral reusable event/distribution/comparator/snapshot/resource/privilege/transcript/leak-check helpers are candidates after extraction/review/fresh CI; G3-G8/CX01 scientific semantics remain research-local. No promotion or merge is performed here.

## Top 3

1. MAIN: bind fresh NI01 authority and, only after all final gates pass, execute exactly one formal NI01 chain. Information value `VERY_HIGH`, distance `NEAR`.
2. Independent Audit: fresh PD01 terminal audit; after NI01 terminalization, audit NI01 before broader H4 claims. `HIGH / NEAR_TO_MEDIUM`.
3. SUB: remain `no_op`; only distinct synthetic/dev NON_EVIDENTIARY incubation if naturally motivated. `LOW_TO_MEDIUM / OPTIONAL`.

Allocation changed from `NEXT_FRONTIER_PROSPECTIVE_SELECTION` to NI01 conditional one-way because of fresh repository readiness evidence, not Literature, Audit, or SUB exploratory output.

Evidence Analyst touched only the designated handoff files and did not execute science, dispatch research workflows, consume one-way identities, mutate immutable evidence, merge research PRs, or change scheduler definitions.
