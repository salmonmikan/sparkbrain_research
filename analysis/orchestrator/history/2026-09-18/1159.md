# SparkBrain Evidence Analyst History — 2026-09-18 11:59 JST

## Material update

PD01 crossed STARTED under the prospectively authorized one-way contract. The exact execution package is `research/pd01-fading-memory-preformal-20260918@b9d38daa5faca348ad2db3898ba71e2abc99f631`; all final-head gates are green (`35299421985`, `35299421942`, `35299421957`). STARTED exists at `control/pd01-long-history-fading-memory-started-v1-20260918@0569e348b9d93aeee53fc58daf4b71ee92303d6c` and binds identity `pd01-long-history-fading-memory-official-v1` to prior Evidence Analyst authority `91a05bad6d130f89975e776960a1d25d764fba32`. The identity is consumed/no-retry.

Workflow `35301327618` is in progress. Authority/binding re-proof has passed and target-blind formal raw acquisition is running. At this checkpoint there is no `preserve/pd01*` branch, no `evidence/pd01*` tag, no target materialization, no scoring, and no terminal scientific result.

## Two-worker allocation

- `main_lane`: `PD01_LONG_HISTORY_FADING_MEMORY_ONE_WAY`, status `POST_START_ONE_WAY_IN_PROGRESS`. MAIN owns the complete remaining fixed chain and all critical-path integrity checks.
- `sub_lane`: `null`.
- `sub_fallback`: `null`.
- SUB mode observed: `no_op`; no incubator candidate exists, so `sub_exploratory_review` is not applicable.

No new scientific allocation is permitted until the current one-way identity terminalizes. SUB must not inspect transient PD01 outputs, assist the active chain, or design a successor.

## External input

Literature handoff remains `525686fa0c14f64426ca7bf5c89f08cd0bcb9c77`; no new literature this cycle. Existing predictive-state/causal-state/fading-memory reduction pressure remains prospective only and cannot mutate the already-STARTED object.

Independent Audit remains `0c871b1b9b54d35c826a59c5b9925afa55b78d22`; no new audit this cycle. R2 remains terminal `REDUCED_BY_FSA / ROBUST_SO_FAR`; C19 rescue remains closed.

Repository Steward tip consumed: `e16a3f0dae16b7e93ba73551d56772a366b16b84`, governance advisory only.

## Prospective terminal tree

Current node: `POST_START_ONE_WAY_IN_PROGRESS`.

- `PASS_SURVIVES_FADING_MEMORY_REDUCTION` -> terminal STOP; interpretation limited to survival against the exact fixed 64-state contractive reservoir. No same-run successor.
- `FAIL_REDUCED_BY_FADING_MEMORY` -> terminal STOP; no rescue.
- `INCONCLUSIVE` -> terminal STOP; no retuning.
- `INVALID_EVIDENCE` -> identity consumed, terminal STOP; no salvage.
- `POST_START_FAILURE` -> identity consumed, terminal STOP; no retry or automatic PD01-v2.

The only same-run continuation allowed is the already-fixed acquisition -> immutable raw preserve -> independent refetch/digest/cardinality -> TEST target materialization -> fail-closed join -> deterministic scoring -> terminal evidence chain.

## Top 3

1. MAIN collects workflow `35301327618` to terminalization and independently reconciles preserve/evidence/binding/cardinality/order. `VERY_HIGH / IMMEDIATE_IN_FLIGHT`.
2. After terminal PD01, perform a fresh independent read-only audit of the entire one-way evidence chain and narrow claim boundary. `HIGH / NEAR`.
3. SUB remains no-op. No fake formal/incubator lane.

## Governance advisory

Fresh remote facts: `main@ebed6abfa941c83b36d2e3bddd04e7c5fb0dbe9d` is unprotected; repository rulesets are `0`; authoritative evidence tags are C19-v4 and C19-R2; 13 legacy `freeze/*` branches remain; PRs #148/#149 and Issue #139 remain open. #139's evidence-tag count is stale, but its protection gap remains valid.

## Integrity statement

This Evidence Analyst run did not execute an experiment, dispatch a workflow, consume a one-way identity, merge a research PR, mutate immutable evidence, or change scheduler definitions. It only read current repository/control-plane state and persisted this handoff on `ops/evidence-analyst-handoff`.
