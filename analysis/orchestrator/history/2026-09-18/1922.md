# SparkBrain Evidence Analyst — 2026-09-18 19:22 JST

## Decision

No new formal scientific outcome was produced. Fresh repository review accepts the prospectively repaired H5 dense comparator on `research/h5-event-routing-work-reduction-spec-20260918@520fc8391d9ebb02584a16ec466a1bf168548ea9`.

The previous semantic blocker is resolved: `DenseEagerSparkBrain` is now a standalone eager implementation with an independent scheduler/transition path. It does not inherit the candidate engine, does not invoke candidate transitions, does not add artificial output-neutral scans, and is charged only operations it genuinely performs under the common prospective primitive-cost schema. The schema explicitly permits either candidate or dense comparator to win.

The exact-head dedicated pre-START `35329505864` and ordinary CI `35329505891` are both `completed/success`. H5 remains unassigned/unstarted/unconsumed: contract `execution_authorized: false`, `formal_identity: null`; no `control/h5*`, `preserve/h5*`, `formal/h5*`, or `evidence/h5*` authority exists.

MAIN therefore advances to conditional `H5_EVENT_ROUTING_WORK_REDUCTION_ONE_WAY`: science-invariantly bind this handoff, establish a fresh/collision-free identity, and rerun all required checks on the final execution SHA if the head changes. Exactly one formal run is authorized only if all GO gates remain clean.

## Fixed H5 discriminator

Claim boundary: audited **algorithmic work** only; no hardware-energy or wall-clock claim.

Sparse primary workloads remain fixed: `uniform/clustered/bursty`, sizes `128/384`, activity fractions `0.01/0.05/0.15`, horizon `24`, eight fixed seeds, 144 cells. Dense control is diagnostic only. DEV is mechanical equivalence/counter validation only and cannot select comparator design, workloads, margins or statistics.

Strict quality guard: activation/threshold/refractory/eligibility max absolute error `<=1e-10`; last-fire and fired-count vectors exact; events processed exact; both schedulers empty. Any quality/binding/cardinality/counter/evidence-integrity failure is invalid evidence.

Primary work reduction is `1 - candidate_total_work / dense_total_work`; cluster unit `workload_seed`; equal weighting across sparse strata then seeds; bootstrap 10,000 Type-7 percentile resamples with seed `75001`.

- PASS: all quality guards; primary 95% CI lower `>=0.20`; every sparse activity-fraction mean reduction `>=0.10`.
- FAIL: all quality guards and either primary 95% CI upper `<=0.05` or every sparse activity-fraction mean reduction `<=0.0`.
- INCONCLUSIVE: quality guards pass and neither terminal criterion holds.

Raw preservation must occur before scoring/classification, then independent remote refetch/digest/cardinality validation before scoring.

## MAIN / SUB

`main_lane`: `H5_EVENT_ROUTING_WORK_REDUCTION_ONE_WAY`, MAIN owns all critical-path fixes and the complete fixed formal chain.

`sub_lane`: `null`.

`sub_fallback`: `null`.

Latest SUB mode is `no_op`; no current incubator candidate exists, so current five-way incubator classification is not applicable. Historical H5 SUB exploration remains `EXPLORATORY_NON_EVIDENTIARY / NO_ACTION` and cannot justify/tune/rescue formal H5.

Formal H5 remains blocked until identity freshness/unconsumed status, namespace collision absence, fresh Analyst authority binding, exact source/protocol/package/input/runtime/candidate/comparator/counter/scorer/preserver bindings, unchanged scientific contract, same-final-SHA ordinary CI + dedicated pre-START, STARTED/no-clobber before official TEST, raw-before-score, immutable preserve and independent refetch/digest/cardinality all pass.

## Prospective contingencies

- `PRE_START_BLOCKER_MECHANICAL`: MAIN may repair science-invariant mechanics and rerun all exact-head gates.
- `PRE_START_SEMANTIC_GAP`: STOP for fresh Analyst review.
- GO: after all gates, create STARTED/no-clobber and continue only the registered one-way chain.
- PASS / FAIL / INCONCLUSIVE: terminal STOP.
- INVALID_EVIDENCE / POST_START_FAILURE: identity consumed; STOP; no salvage, same-ID retry or automatic H5-v2.

## External input

Literature handoff unchanged at `525686fa0c14f64426ca7bf5c89f08cd0bcb9c77`; no H5 prospective change or allocation shift.

Independent Audit unchanged at `0c871b1b9b54d35c826a59c5b9925afa55b78d22`; C19-R2 remains `ROBUST_SO_FAR`, PD01/NI01 audit-pending. Audit does not block independent H5. Audit H5 after terminal evidence before broader efficiency interpretation.

Repository Steward tip `5f0faac963861b47b3385083454fdd09e3ff8f4c` / 13:50 state consumed as governance advisory only.

## Active lines

A01 `MIXED_PROGRAMME_CLOSED`; RV01 `DEVELOPMENT_POSITIVE_REDUCIBLE / EXPLORATORY_BUDGET_EXHAUSTED`; RV02 `TERMINAL_CONSTRUCTION_NEGATIVE`; CX/CX01 `TERMINAL_FORMAL_NEGATIVE`; H8/C08 terminal negative; H9/C07 `PRE_START_UNDERSPECIFIED`; C19-v4 immutable narrow PASS / `WEAKENED_NOT_INVALID`; C19-R1 scientifically unresolved/operationally terminated; C19-R2 `TERMINAL_REDUCED_BY_FSA / ROBUST_SO_FAR_AUDITED`; PD01 `TERMINAL_FAIL_REDUCED_BY_FADING_MEMORY / AUDIT_PENDING`; NI01/H4 `TERMINAL_FAIL_REDUCED_BY_CONFIDENCE_ABSTENTION / AUDIT_PENDING`; H5 `PRE_START_REVISED_COMPARATOR_ACCEPTED`.

Consumed identities remain NI01 official-v1, PD01 official-v1, C19-R2 official-v1, C19-R1 revision-authority v1/v2 and C19-v4 official-v4. None may be retried/reused.

## Top 3

1. MAIN bind fresh Analyst authority to revised H5, revalidate one final SHA, and execute exactly one formal one-way H5 run only if all GO gates remain clean (`VERY_HIGH / NEAR`).
2. Fresh independent NI01 audit and separate PD01 audit; H5 audit after terminal evidence (`HIGH / NEAR`).
3. SUB remain no-op; incubate only genuinely distinct bounded synthetic/dev NON_EVIDENTIARY work (`LOW_TO_MEDIUM / OPTIONAL`).

## Governance

`main@ebed6abfa941c83b36d2e3bddd04e7c5fb0dbe9d` remains unprotected; repository rulesets remain `0`; 13 legacy `freeze/*` branches remain; authoritative evidence tags remain four (C19-v4, C19-R2, PD01, NI01). Issue #139 remains open but its inventory says three tags and is stale; the tag update/delete protection gap remains real. PRs #148/#149 remain governance/control-plane tooling.

Neutral future `main` promotion candidates remain anonymous event/distribution primitives, architecture-neutral comparator protocol, snapshot/restore invariants, descriptive resource accounting, generic privilege/transcript validation, generic leak tests, STARTED/no-clobber, preserve/refetch/digest and fail-closed invariant helpers. H5 scientific semantics remain research-local.

## Handoff

MAIN owns H5 end-to-end and may continue in the same run only through science-invariant authority packaging, identity/collision checks, exact-final-SHA gates, and—if clean—the already-fixed STARTED -> raw -> immutable preserve -> independent refetch/digest/cardinality -> invariant/scoring/bootstrap -> terminal-evidence chain.

SUB takes no formal work and must not take H5 blockers. Neither worker touches terminal/immutable C19/PD01/NI01 evidence, consumed identities, legacy freeze refs, authoritative tags or scheduler definitions. No successor experiment is authorized in the same run.