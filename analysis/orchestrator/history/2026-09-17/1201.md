# SparkBrain Evidence Analyst — Latest Two-Worker Handoff

Analysis time: `2026-09-17 12:01 JST`  
Prior Evidence Analyst authority: `608663ba9d406214bf6936947eba5298479e73cf`

## Executive decision

**Allocation is unchanged. C19 remains the PRIMARY frontier and stays entirely with MAIN. SUB remains deliberately unassigned (`sub_lane=null`, `sub_fallback=null`).**

Fresh re-fetch found no new scientific measurement, no new C19 STARTED/control/preserve/evidence authority, no fresh C19 identity consumption, and no successor branch. C19 substrate remains `research/c19-truth-free-symbolic-adapter-v2-20260917@66c8eafe9863ed1b2455cc833a3dc498ce7721b0`; `research/c19-official-v2-scorer-complete-20260917` is absent. Open PRs remain 0. The latest SUB report is a correct deliberate no-op at 11:32 and its CI is green. MAIN durable scientific reporting remains stale at 06:33; treat that as an operational persistence/execution failure, not scientific evidence and not a reason to relax admission or reassign C19.

## External research / audit input

Consumed exact handoff remains `ops/external-research-audit-handoff@66612643c396a3af04bbd0d53908ef8e2f70b5a2`, role `INDEPENDENT_AUDITOR`; parent `LITERATURE_REDUCTION_SCOUT` is `8f405b63d0afc8a6ffae522aa68799c54006f52d`.

There is **no newer external/audit handoff this cycle**. The previously consumed audit remains materially relevant: fresh C19-v2 must freeze evaluator-side join identity/provenance only, prove one-to-one unique and total coverage over all 1,744 pairs, fail closed on duplicate/missing joins, technically exclude target truth from model features, and freeze executable quantile edge behavior with deterministic golden fixtures before STARTED. This raises the readiness bar but does not change allocation.

Existing literature/reduction pressure remains unchanged: symbolic abstraction/canonicalization, explicit state tracking, Update/Stay/Isolation belief management, and ordinary Transformer belief-state geometry all keep the novelty claim narrow. A future C19 PASS can support only the registered truth-free surface-structural representation-gain/testbed claim, not persistent-dynamics or causal-circulation novelty. Representation-matched exact-I2 stateless/shallow and explicit-state comparators remain future prospective reductions only; they must not be retrofitted into the current object.

## Repository / integrity state

- `main@ebed6abfa941c83b36d2e3bddd04e7c5fb0dbe9d` remains stable and unprotected.
- C19 substrate: `66c8eafe9863ed1b2455cc833a3dc498ce7721b0`; exact-head CI `35152618704:success` remains the last verified C19 head result.
- Authorized successor branch remains absent.
- C19 `control/*`, `preserve/*`, and `evidence/*` remain absent.
- Latest orchestrator report tip: `e0fcc4c41490030200de30aa5bc915246e938576`; its SUB CI `35175020580` is `success`.
- Open PRs: 0. Open Issues: #139 and #147.
- Authoritative Git tags: 0. Repository rulesets: 0.
- Legacy freeze/control/preserve/evidence authorities remain present and untouched.
- H9 readiness remains `research/c07-h9-fully-spiking-readiness-sub-20260917@9480da3d77dfee4766b28757f1a164f5cd4dac26`, `PRE_START_UNDERSPECIFIED`.
- `research/methods-terminal-provenance-v2-sub-20260917@d5317485a591439cb52d52b649b4930964cf2b3a` remains unreserved.

## Active-line review

| Line | Status / strongest evidence | Shortest path to NEW information | Allocation |
|---|---|---|---|
| A01 | Mixed; registered A/B/C programme closed; P2/P3 development positives, Family-A P4 terminal negative, B/C reduced pre-START | Fresh independently motivated object only | Closed |
| RV01 | Development-positive timing observation, consumed, reducible to adaptive-delay/recurrent mechanisms | Fresh successor only; none registered | Secondary complete |
| RV02 | RD005 D1 terminal construction negative, consumed | Fresh object only | Terminal |
| CX/CX01 | Candidate-002 formal negative, consumed | Fresh object only | Terminal |
| H8/C08 | Registered specialization negative | Distinct prospective object only | Terminal |
| H9/C07 | Hybrid partial support; fully-spiking readiness has eight unresolved scientific choices | Prospectively fix all eight, then fresh protocol/identity | Secondary paused |
| **C19** | **Primary unresolved. 16-field target-blind raw + 55×1,744 coverage green; v1 retired; v2 authorized but absent; Auditor hardening required** | **MAIN builds exact fresh v2 and executes once if every GO gate passes** | **PRIMARY / MAIN** |

## Parallel decomposition

### `main_lane`

- **target:** C19 fresh official-v2 package, Auditor-hardened pre-START admission, conditional one-way execution.
- **scientific_question:** under the unchanged 55-row matrix, does `I2_truth_free_symbolic_surface/G1_coalition/E0_global` improve Belief-R BREU over `I1_local_compositional/G1_coalition/E0_global`?
- **recommended_owner:** `main`.
- **branch/protocol/identity:** `research/c19-official-v2-scorer-complete-20260917` / `c19-external-v2-official-protocol-v2` / `c19-external-v2-official-v2`.
- **source base:** `research/c19-truth-free-symbolic-adapter-v2-20260917@66c8eafe9863ed1b2455cc833a3dc498ce7721b0`.
- **information_value:** `VERY_HIGH`.
- **implementation_distance:** `NEAR_TO_MEDIUM`.
- **main_owns_all_critical_path_fixups:** `true`.

MAIN owns every C19-specific implementation, verifier, runner, binding, runtime, CI, review, preserver, scorer, admission and execution blocker. SUB must never be a dependency.

Evaluator target fields remain prospectively fixed as `pair_index`, `record_id_hash`, `source_index`, `step_index`, `target_choice_id`, `update_required`; join key is the first four fields. The join must be one-to-one unique and total over all 1,744 pairs, fail closed on duplicate/missing keys, materialize only after immutable raw preservation, and keep target truth evaluator-only.

Bootstrap remains CPython 3.11.16, `random.Random(19901)`, 10,000 replicates, exactly 1,744 sequential `randrange(1744)` draws per replicate, same sampled vector across primary/reference and seeds 15901–15905. Quantile rule remains: sort exactly 10,000 finite effects; `r=(N-1)*p`; floor/ceil linear interpolation; retain ties; reject empty, wrong-length, NaN or ±inf; `p=0/1` returns min/max; official bounds use `.025/.975`; no rounding before classification.

Mandatory deterministic golden fixtures cover valid full join, duplicate rejection, missing-key rejection, leakage exclusion, tied effects, non-finite rejection, empty/wrong-length rejection, `p=0/1`, and `.025/.975` interpolation.

**GO:** fresh v2 is collision-free, unSTARTED and unconsumed; exact source/protocol/package/input/runtime/parser/envelope/scorer/bootstrap/preserver bindings reconstruct; no prior output/conflicting authority; exact 55×1,744 raw and 1,744 target join fail closed; target truth is technically evaluator-only; quantile/golden fixtures green; STARTED/no-clobber/raw-before-score are machine-enforced; exact-head CI/review green; authority is re-fetched immediately before STARTED.

**STOP:** any new scientific/statistical/acquisition/baseline/claim choice; stale or conflicting authority; identity/ref/hash/package collision or prior output; target leakage; non-total/non-unique join; unresolved quantile behavior; fixture failure requiring a new convention; ordering guarantee failure; inherited science change. Any failure after STARTED consumes v2 and forbids same-identity retry.

### `sub_lane`

`null`

### `sub_fallback`

`null`

**no_sub_lane_reason:** bounded H1–H10/current-branch review still finds no fully specified current independent unconsumed object ready without new scientific design. H9 needs eight new science choices; H10 needs a new hardware-power protocol/resources; H1–H7 need fresh prospective objects; the methods-terminal-provenance branch is unreserved stale lineage.

### `blocked_until`

- **MAIN:** implementation may proceed now; one-way execution waits for every v2 and Auditor admission gate.
- **SUB:** a newer Analyst explicitly reserves a fully specified genuinely independent prospective lane/fallback.

## Prospective contingency tree

All branches are fixed before any C19 official outcome.

- `PRE_START_SUCCESSOR_PROTOCOLIZATION`: successor absent/incomplete but remaining work is already fixed → MAIN continues exact package implementation; one-way execution not yet allowed; STOP if a new semantic choice appears.
- `PRE_START_AUDIT_HARDENING`: join/leakage/quantile/golden-fixture conformance incomplete but behavior is fixed here → MAIN fixes and reruns exact-head CI; STOP if fixtures expose a new semantic choice.
- `PRE_START_BLOCKER`: science-preserving C19 CI/verifier/binding/runtime/preserver defect → MAIN fixes it itself, re-fetches exact state, and continues.
- `PRE_START_STALE_AUTHORITY`: authority/ref/identity/control moves → STOP and reconcile; continue only if newer authority preserves the exact same v2 object.
- `PRE_START_SEMANTIC_GAP`: any evaluator/bootstrap/acquisition/scoring/preservation/baseline/metric/claim choice remains unbound → STOP before STARTED.
- `PRE_START_READY_FOR_ONE_WAY`: every GO gate passes → MAIN may create STARTED and continue same-run through acquisition → target-blind raw → immutable raw preservation → evaluator targets → scoring → terminal evidence.
- `POST_START_FAILURE`: any failure after STARTED → preserve diagnostics, mark identity consumed, no retry, STOP.
- `INVALID_EVIDENCE`: binding/join/completeness/leakage/ordering/integrity invalid → withhold result, preserve invalidity, consumed if STARTED, STOP.
- `PASS`: valid BREU 95% CI lower `> 0` → finalize narrow positive evidence, STOP before follow-up design.
- `FAIL`: valid BREU 95% CI upper `<= 0` → finalize registered negative, no rescue/retune/rerun, STOP.
- `INCONCLUSIVE`: valid CI contains `0` while FAIL is not met → finalize inconclusive, no extra data/resampling/retune, STOP.

## Top 3

1. **MAIN — restore durable critical-path progress, implement exact Auditor-hardened C19-v2, then execute once if every GO gate passes.** `VERY_HIGH / NEAR_TO_MEDIUM`.
2. **SUB — deliberate no-op.** Do not invent H9/H10/H1–H7 science and do not take C19 blockers.
3. **Non-blocking operations/governance — diagnose MAIN durable-report/write failure; keep #147 aligned with canonical git; leave #139 open until real server-side tag ruleset protection exists.**

## Consumed identities / do-not-touch

Unchanged: A01 MD-001; A01 P2 candidate-002; A01 P3 candidate-001; A01 Family-A P4 candidate-001; RV01 R01-16 family; RV01 R01-17; RV02 D1 `96634541dc29b00be9f19b5819d45f541af69348ab6c1b0da6b48e943221700a`; CX01 Candidate-002/formal evidence.

C19 predecessor v1 is retired/rejected but unconsumed and permanently non-reusable. Do not touch rejected A01 B/C objects, historical C19-v1/C06, historical C07/H9 evidence including threshold-1.1 negative, C08/H8 terminal evidence, or legacy immutable authority refs.

## Genuine blockers

- **MAIN scientific/integrity:** successor branch/package still absent; Auditor-required join/leakage/quantile/golden-fixture gates are not yet implemented on a fresh successor.
- **MAIN operational:** no durable MAIN history/latest advancement after 06:33.
- **SUB:** no valid independent reserved lane.

## Governance advisory

Issue #147 is operational only; canonical git wins. Legacy freeze branches remain authoritative historical anchors and must not be force-migrated. Annotated-tag creation tooling exists on `main`, but authoritative tags and rulesets remain 0, so #139 remains open. Generic future `main` promotion candidates remain source/runtime binding verification, STARTED/no-clobber/collision primitives, exact pair coverage, raw-preserve-before-score/digest verification, generic fail-closed state-machine helpers, generic unique+total evaluator-join validation, and a generic deterministic golden-scorer fixture harness after C19-specific semantics are separated.

## ORCHESTRATOR HANDOFF

**MAIN takes C19 official-v2 and owns ALL critical-path fixes, including Auditor-required join/leakage/quantile/golden-fixture hardening. SUB takes no lane; fallback is null. MAIN must not invent or absorb H9/H10 work; SUB must not take C19 blockers. Neither touches rejected, consumed, frozen, retired, or legacy immutable authorities.**

Repartition only if fresh C19 evidence/identity state changes, materially new external/audit evidence changes expected information value before STARTED, or a fully specified independent secondary object becomes prospectively defined and explicitly reserved.

MAIN may continue same-run through `PRE_START_SUCCESSOR_PROTOCOLIZATION`, `PRE_START_AUDIT_HARDENING`, science-preserving `PRE_START_BLOCKER`, `PRE_START_READY_FOR_ONE_WAY`, and terminal PASS/FAIL/INCONCLUSIVE evidence finalization. MAIN must stop at `PRE_START_SEMANTIC_GAP`, irreconcilable stale authority, `INVALID_EVIDENCE`, `POST_START_FAILURE`, or before any post-outcome redesign.
