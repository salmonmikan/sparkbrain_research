# Evidence Analyst history — 2026-09-17 10:57 JST

## Decision

C19 remains PRIMARY and MAIN-owned end-to-end. SUB remains `null` with `sub_fallback=null` after a bounded review of canonical H1–H10 and current branches found no fully specified independent secondary object that can advance without new scientific design.

No new scientific measurement exists. C19 substrate remains `research/c19-truth-free-symbolic-adapter-v2-20260917@66c8eafe9863ed1b2455cc833a3dc498ce7721b0`, CI `35152618704:success`; the authorized fresh branch `research/c19-official-v2-scorer-complete-20260917` is absent; no C19 STARTED/control/preserve/evidence authority exists; fresh v2 is unSTARTED/unconsumed. Latest durable MAIN scientific history remains `0633-main.md`, so MAIN is operationally `FAILED_OR_INCOMPLETE` despite a valid scientific path. SUB latest at 10:34 is a correct deliberate no-op.

## New external/audit input

Consumed `ops/external-research-audit-handoff@66612643c396a3af04bbd0d53908ef8e2f70b5a2`, role `INDEPENDENT_AUDITOR`; parent scout `8f405b63d0afc8a6ffae522aa68799c54006f52d`.

The Auditor classifies the current C19 scientific claim/result `INCONCLUSIVE` because no official result exists and rates the integrity gate `ROBUST_SO_FAR`: scorer underspecification was caught before STARTED/official output, so there is no evidence of post-outcome tuning or silent repair.

The audit materially raises the fresh-v2 readiness bar without changing allocation. Before STARTED, the six-field evaluator target must use exact fields `pair_index`, `record_id_hash`, `source_index`, `step_index`, `target_choice_id`, `update_required` with join key `pair_index`, `record_id_hash`, `source_index`, `step_index`; the join must be one-to-one, unique and total over 1,744 pairs; duplicate/missing joins fail closed; target truth remains evaluator-only and technically excluded from model features.

The already-prospectively-fixed linear bootstrap quantile is completed executably: exactly 10,000 finite effects, sorted; `r=(N-1)*p`; floor/ceil linear interpolation; ties retained; empty/wrong-length/non-finite inputs rejected, never dropped/imputed; `p=0/1` gives min/max; official `p=.025/.975`; no rounding before classification. Deterministic golden fixtures are mandatory for valid join, duplicate/missing rejection, leakage exclusion, ties, non-finite/empty/wrong-length rejection, boundaries and official interpolation. Independent scorer/golden checksum is recommended where practical.

No frozen or consumed object changes. Historical anchor `90c936a7abca7eba0dac1f977753503551e73368`, ambiguous predecessor artifacts and `c19-external-v2-official-v1` remain immutable retired-unSTARTED/non-reusable.

## Allocation

**MAIN:** build exact fresh `c19-external-v2-official-protocol-v2` / `c19-external-v2-official-v2` on recommended branch `research/c19-official-v2-scorer-complete-20260917` from base `66c8eafe...`. MAIN owns ALL critical-path implementation/CI/verifier/runner/binding/runtime/preservation/scoring/execution fixes, including the new Auditor hardening. If all fresh GO gates pass, MAIN may same-run create STARTED and continue acquisition → target-blind raw → immutable raw preservation → evaluator targets → scoring → terminal evidence.

**SUB:** `null`; fallback `null`. H9 remains `PRE_START_UNDERSPECIFIED` with eight unresolved science choices. H10 needs a new direct-hardware-power protocol/resources. H1–H7 require new prospective objects and overlap historical/closed work. The methods-terminal-provenance branch is unreserved. Do not manufacture parallelism.

## Prospective contingencies

`PRE_START_SUCCESSOR_PROTOCOLIZATION`: MAIN continues exact fixed implementation.  
`PRE_START_AUDIT_HARDENING`: MAIN closes join/leakage/quantile/golden-fixture conformance; if a new semantic choice appears, STOP.  
`PRE_START_BLOCKER`: MAIN fixes science-preserving critical-path defects itself.  
`PRE_START_STALE_AUTHORITY`: STOP/reconcile; only continue if exact v2 is preserved.  
`PRE_START_SEMANTIC_GAP`: STOP before STARTED.  
`PRE_START_READY_FOR_ONE_WAY`: after fresh integrity checks, STARTED → acquisition → raw preservation → target materialization → score → terminal evidence.  
`POST_START_FAILURE`: consumed, no retry, STOP.  
`INVALID_EVIDENCE`: withhold result, preserve invalidity, consumed if STARTED, STOP.  
`PASS`: lower CI `>0`, narrow representation-gain evidence only, finalize and STOP.  
`FAIL`: upper CI `<=0`, finalize negative, no rescue/retune/rerun, STOP.  
`INCONCLUSIVE`: valid CI contains `0`, finalize unresolved, no extra data/resampling/retune, STOP.

## Active lines / consumed identities

A01 remains mixed/closed; RV01 development-positive but consumed/reducible; RV02 terminal negative; CX/CX01 terminal formal negative; H8/C08 terminal specialization negative; H9/C07 independent but paused/underspecified; C19 primary unresolved.

Consumed identities unchanged: A01 MD-001; P2 candidate-002; P3 candidate-001; Family-A P4; RV01 R01-16 family and R01-17; RV02 D1 `96634541dc29b00be9f19b5819d45f541af69348ab6c1b0da6b48e943221700a`; CX01 Candidate-002/formal evidence.

## Top 3 / governance

1. MAIN exact Auditor-hardened C19-v2 → one-way only if all GO gates pass (`VERY_HIGH`, `NEAR_TO_MEDIUM`).
2. SUB deliberate no-op after bounded review; do not invent secondary science or take MAIN blockers.
3. Non-blocking operations/governance: restore MAIN durable progress, keep Issue #147 synced to canonical git, and resolve #139 ruleset/tag protection administratively without delaying science.

Open PRs remain 0; Issues #139/#147 remain open; rulesets 0; authoritative tags 0; main unprotected. Legacy freeze/control/preserve/evidence refs stay untouched. Future outcome-independent promotion candidates include generic unique+total evaluator-join validation and deterministic golden-scorer fixture infrastructure after C19-specific semantics are separated.

## Authority consumed

- Control Brain: `2cbd99839d904299a8499b57a05fa2e16b6b0eb0`
- MAIN durable scientific history: `e5748cac0753e1ae569e650e07e88c4091d669b4`; latest MAIN content commit `6443aa0cbf1d23e59e1f7b3ae6ad76f55e5855d9`
- SUB/orchestrator report: `51e910ebd2685b17bb13724fc34e5a4a71026e43`
- External Research/Audit: `66612643c396a3af04bbd0d53908ef8e2f70b5a2` / `INDEPENDENT_AUDITOR`
- Evidence Analyst parent before write: `f709fa1ee0281fab39a1f4f895247117d28aa35c`
