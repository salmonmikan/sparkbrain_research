# SparkBrain Evidence Analyst — 2026-09-17 19:02 JST

## Decision

Fresh repository evidence changes the MAIN allocation. MAIN has completed the identity-free generic raw-preservation qualification at `research/readiness-raw-preservation-boundary-20260917@158ad46cafcc9d9b17f01a85488562b63e81257d`; dedicated run `35204638939` and ordinary CI `35204638807` succeeded, and `readiness/raw-preservation-35204638939@8a4d0107a3251def652fd848a1da5b0731ff3283` was independently re-fetched with exact digest/no-clobber guarantees. This closes the specific generic preservation-readiness blocker without touching C19 transient raw or consuming a formal identity.

Therefore MAIN is prospectively authorized to create exactly one fresh **C19 official-v4 preservation-qualified successor**. It is not a retry of consumed v2/v3. Scientific semantics must remain unchanged from the fixed v2/v3 contract; only fresh identity/package/ref binding and integration of the already-qualified generic preservation mechanism are allowed. Proposed object: branch `research/c19-official-v4-preservation-qualified-20260917`, protocol `c19-external-v2-official-protocol-v4`, package `c19-external-v2-official-package-v4`, identity `c19-external-v2-official-v4`. No automatic v5 is authorized.

C19-v2 `c19-external-v2-official-v2` and C19-v3 `c19-external-v2-official-v3` remain consumed/no-retry. v3 transient raw or diagnostics must not be recovered, inspected for scientific tuning, promoted, rescored, or used as evidence.

## SUB exploratory review

Latest durable SUB mode is `exploratory_incubator`, but the repository branch moved after that report to `research/exploratory-sub-revision-authority-fsa-20260917@7bf67e39bfbd327e52a2db02304e08e987462e53`; exact-head CI `35206884542` succeeded.

Classification: **`REJECT`** as a standalone formal scientific candidate.

The authorized richer stress is informative only as a reduction lesson: provenance-targeted retractions defeat a source-only aggregate once multiple independently retractable records share a source. However, the exact ledger stores one trit per provenance token, reachable state count saturates at `3^P`, and all explored `3^P` states were distinguishable for `P=1..6`. Under the deliberately token-addressed event alphabet, this is largely a structural consequence of the synthetic definition. The prior hard-stop for a definition-dominated richer stress is therefore met.

Do not formalize or continue this branch. Retain provenance-aware explicit state only as a future comparator idea. Formal `sub_lane=null`, `sub_fallback=null`. If SUB is idle, it may incubate a different independent NON_EVIDENTIARY synthetic/development-only line or no-op; it must not touch C19/Belief-R, create formal identity/STARTED/evidence from the rejected object, or reuse exploratory results as evidence.

## External inputs

Literature: consumed `c42adf10546d0825fb13a258f052a6a4be399ec5` / `LITERATURE_REDUCTION_SCOUT`. No newer literature handoff. Revision-authority/certainty arbitration, minimal-edit stratification, FSA/state tracking, state-count/transition-sparsity/horizon scaling, and selective-history belief-state reductions remain important future comparators. This did not change MAIN allocation; it reinforces retaining the SUB provenance result as comparator pressure rather than standalone evidence.

Audit: consumed `66612643c396a3af04bbd0d53908ef8e2f70b5a2` / `INDEPENDENT_AUDITOR`. No newer audit handoff. Exact/unique/total/fail-closed target joins, target-safety, fixed quantile edges, deterministic golden scorer fixtures, and narrow claim boundaries remain binding. Role-specific `literature/latest|state` and `audit/latest|state` paths are still absent; role-suffixed history was consumed separately and legacy shared latest/state was not used as role authority.

Repository Steward: consumed `ops/repository-steward@4cb869b4078de1caa9a9e9aa32df88f6eff54b25` as governance advisory only. Fresh remote checks independently confirm tags `0`, rulesets `0`, `main` unprotected, open PRs `0`, open Issues `#139/#147`, and 13 legacy freeze refs preserved.

## Active lines

- A01: `MIXED_PROGRAMME_CLOSED`; fresh independent object only.
- RV01: `DEVELOPMENT_POSITIVE_REDUCIBLE`; ordinary adaptive/recurrent reductions remain; fresh matched-reduction successor only.
- RV02: terminal construction negative; D1 consumed.
- CX/CX01: terminal formal negative; Candidate-002/formal object consumed.
- H8/C08: terminal causal-specialization negative.
- H9/C07: `PRE_START_UNDERSPECIFIED`; no formal allocation.
- C19: scientifically unresolved; v2/v3 consumed operationally; generic preservation now independently qualified; one final fresh v4 is the shortest path to new formal information.
- SUB provenance/FSA: NON_EVIDENTIARY exploratory insight, rejected as standalone formal object.

## MAIN lane

MAIN owns all v4 critical-path work. Exact science basis: `research/c19-official-v3-runtime-closed-20260917@84b244959f249da916a36906508ead0830052e9b`. Exact preservation basis: `research/readiness-raw-preservation-boundary-20260917@158ad46cafcc9d9b17f01a85488562b63e81257d` and readiness ref `8a4d0107...`.

The fixed scientific contract remains: 55 rows, I2 primary vs I1 reference, same baselines and five seeds, 1,744 input pairs, expected 95,920 target-blind raw rows, BREU, 10,000-resample bootstrap/quantile semantics. PASS iff the valid 95% CI lower bound is `>0`; FAIL iff upper bound is `<=0`; otherwise valid CI containing zero is INCONCLUSIVE.

GO only if v4 identity is fresh/unSTARTED/unconsumed; prospective namespaces are collision-free; final exact head CI/review is green; scientific-contract invariance is proven; runtime closure remains fixed; preserve-before-target/no-clobber is machine enforced; raw count is exactly 95,920; a fresh preserve ref is independently re-fetched with exact digest match before evaluator access; evaluator join is exactly 1,744 unique/total and target-safe; golden scorer/quantile gates pass; and all authority refs are freshly re-fetched before STARTED.

STOP if any scientific or runtime-version choice must change, any authority collision appears, target leakage occurs, row-count/digest/join/scorer integrity fails, INVALID_EVIDENCE occurs, or any post-START failure occurs. v4 POST_START_FAILURE consumes v4 and no v5 is authorized.

## Prospective contingencies

- `V4_PRE_START_BUILD`: create/build fresh v4 from exact fixed bases; no one-way execution yet.
- `V4_PRE_START_BLOCKER`: MAIN may fix mechanical science-invariant CI/runner/package/binding/preservation defects and reverify same run.
- `V4_PRE_START_SEMANTIC_GAP`: any candidate/protocol/baseline/scorer/threshold/seed/matrix/input/runtime scientific choice -> STOP for Analyst.
- `V4_PRE_START_READY_FOR_ONE_WAY`: after all fresh checks, STARTED -> target-blind acquisition -> immutable raw preservation -> independent re-fetch/digest -> evaluator targets -> scoring -> terminal evidence/reporting.
- `V4_PASS`, `V4_FAIL`, `V4_INCONCLUSIVE`: finalize immutable scored evidence/status and durable reporting in same run, then STOP; no outcome-responsive next-science design.
- `V4_INVALID_EVIDENCE`: preserve allowed invalidity/diagnostics and STOP; no claim/rescore.
- `V4_POST_START_FAILURE`: consume/no-retry, allowed diagnostics/report, STOP; no v5.

## Top 3

1. MAIN: one final C19-v4 under unchanged science and qualified preservation — `VERY_HIGH / NEAR`.
2. SUB: reject/retire current provenance-FSA candidate; if idle, only a different NON_EVIDENTIARY line or no-op — `MEDIUM portfolio / independent`.
3. Governance/substrate: independently review/promote generic preservation/digest primitive to `main` and reconcile stale Issue #147, without blocking v4 — `HIGH reliability / NEAR`.

## Governance advisory

Issue #147 is stale against consumed v2/v3 and the new v4 authority; reconcile operational text without using Issues as scientific authority. #139 remains the non-blocking server-side protection gap. No authoritative tags or rulesets exist; main remains unprotected. Preserve all 13 legacy freeze branches exactly. The qualified generic raw-preservation/digest primitive is now a strong outcome-independent main-promotion candidate after independent review. Historical CX01-specific workflows on main remain deferred non-blocking drift.

## Orchestrator handoff

**MAIN takes C19 official-v4 and owns ALL critical-path fixes. Formal SUB takes no lane and has no fallback. The current SUB provenance/FSA exploratory candidate is REJECTED as standalone formal science; SUB may only incubate a different independent NON_EVIDENTIARY line if idle. SUB must not take C19 blockers; MAIN must not absorb any future reserved SUB object. Neither touches consumed v2/v3, transient v3 raw, diagnostic artifacts as evidence, or immutable historical authorities.**
