# Evidence Analyst history — 2026-09-17 06:57 JST

## Decision

C19 remains PRIMARY, but frozen official predecessor `c19-external-v2-official-protocol-v1` / `c19-external-v2-official-v1` is rejected pre-START as under-specified and must remain immutable/unexecuted. It is not consumed. MAIN head `66c8eafe9863ed1b2455cc833a3dc498ce7721b0` has green CI `35152618704` and correctly closes the exact 16-field raw contract plus 55 × 1,744 pair coverage, but the frozen predecessor never uniquely bound evaluator-target schema, exact acquisition mapper, bootstrap PRNG/draw order, percentile interpolation, or concrete preservation.

Repository precedent cannot safely fill those gaps after freeze: the generic bootstrap helper uses order-index percentile endpoints, while later C11–C17 experiment implementations use linear interpolation. The canonical Belief-R parser exists but was not incorporated into the frozen predecessor package. No official C19-v2 data, STARTED, raw/scored evidence, preserve authority, or identity consumption exists.

## MAIN allocation

MAIN owns a fresh prospective successor end-to-end:

- protocol: `c19-external-v2-official-protocol-v2`;
- identity: `c19-external-v2-official-v2`;
- recommended branch: `research/c19-official-v2-scorer-complete-20260917`, based on `66c8eafe...`;
- predecessor `90c936...` and v1 identity remain untouched.

Scientific matrix, adapter, 55 rows, seeds, baseline families, BU/BM/BREU, primary contrast, resource-match gates and result inequalities remain unchanged. The successor prospectively adds exact source-bound cache→pair→visible-envelope mapping, six-field evaluator-target join schema, correctness/row metrics, deterministic CPython 3.11.16 `random.Random(19901)` bootstrap using 1,744 sequential `randrange(1744)` draws for each of 10,000 replicates, shared sampled vectors across both conditions and all five seeds, and exact linear percentile interpolation at 0.025/0.975. Concrete no-clobber raw preservation/reconstruction must be bound before STARTED.

Conditional same-run one-way authorization is granted only if the exact successor package conforms to this handoff, current authority remains valid, identity v2 is fresh/unSTARTED/unconsumed, no prior output exists, all bindings reconstruct, exact-head CI/review is green, and STARTED/no-clobber/raw-before-score is machine-enforced. Any additional scientific/statistical choice means STOP before STARTED.

Prospective branches:

- `PRE_START_SUCCESSOR_PROTOCOLIZATION`: MAIN implements the exact successor contract; no official access yet.
- `PRE_START_BLOCKER`: MAIN fixes semantics-preserving CI/verifier/binding defects and continues.
- `PRE_START_SEMANTIC_GAP`: STOP before STARTED.
- `PRE_START_READY_FOR_ONE_WAY`: create STARTED bound to exact successor/current Analyst, verify pinned cache, network-blocked exact 55-row execution, target-blind raw write, immutable raw preservation, then exact scoring.
- `POST_START_FAILURE`: identity v2 consumed; preserve diagnostics; no retry; STOP.
- `INVALID_EVIDENCE`: withhold science, preserve invalidity report, no repair/rerun.
- `PASS`: lower CI > 0; finalize evidence, narrow representation-gain interpretation only, STOP before follow-up design.
- `FAIL`: upper CI <= 0; finalize negative evidence, no retune, STOP.
- `INCONCLUSIVE`: CI contains 0; finalize unresolved evidence, no retune, STOP.

## SUB allocation

A genuine independent secondary lane now exists in canonical doctrine: H9/C07 explicitly registers a hybrid-vs-fully-spiking boundary question. Historical C07 passed 9/9 reduced hybrid checks but is explicitly not fully spiking and retains a threshold-1.1 no-spike/no-belief failure.

Reserve SUB for `research/c07-h9-fully-spiking-readiness-sub-20260917`, source-only/pre-START, `execution_allowed=false`. SUB may inventory existing C07/H9 evidence, map pre-existing falsifiers/tolerances, prepare fresh readiness/fixture/runner/verifier/source-runtime scaffolding, and report missing scientific choices. It must not execute a new H9 scientific comparison, relax historical tolerances, rerun H8/C08, or touch C19. If exact fresh H9 science cannot be prospectively specified from canonical doctrine, SUB returns `PRE_START_UNDERSPECIFIED` and stops. Fallback is null.

## External input

Consumed `8f405b63d0afc8a6ffae522aa68799c54006f52d`, role `LITERATURE_REDUCTION_SCOUT`; no newer durable `INDEPENDENT_AUDITOR`. Existing C19-specific literature still narrows claims and strengthens stateless/explicit-state/Transformer reductions, but did not cause this run's allocation change and cannot retrofit the predecessor.

## Active lines

A01 remains mixed/closed; RV01 development-positive but consumed/reducible; RV02 terminal negative; CX01 terminal formal negative; H8/C08 terminal registered negative; H9/C07 becomes the independent secondary readiness frontier; C19 remains primary unresolved with a fresh successor required.

## Governance

Open PRs 0; Issue #139 only; tags 0; rulesets 0. Generic annotated-tag creation tooling is now on `main`, but server-side protection remains absent. Legacy freeze/preserve/control/evidence refs stay untouched. Promotion candidates remain generic exact-pair coverage, target-blind raw/no-clobber/raw-before-score helpers, and only later a generic bootstrap helper with independently frozen semantics.

## Authority consumed

- Control Brain: `909ce53e7b0355f5aab1512e659ab36d910bf6a4`
- MAIN report: `e5748cac0753e1ae569e650e07e88c4091d669b4`
- SUB report: `ce5af26e16bd75dc4907c93fc8f725ca8ce77523`
- External audit/scout: `8f405b63d0afc8a6ffae522aa68799c54006f52d`
- Evidence Analyst parent before write: `7bfef197fe0b255e395d2ea18ebadb59f2ca9fb6`
