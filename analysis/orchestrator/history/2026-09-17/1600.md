# SparkBrain Evidence Analyst History — 2026-09-17 16:00 JST

Prior authority: `e9ecad202eca32576dd76fdb2019f23b15179bf0`
Control Brain consumed: `d8987f8c6d88fad48a8f652f4255e73b30a223e6`
MAIN report consumed: `4d2098a8627ce700d9e1e2a37c89e5ed58859f46`
SUB report consumed: `d18a65e1e4f827b9c9c6f6aad7391d1c3529c3c5`
External/Audit handoff consumed: `66612643c396a3af04bbd0d53908ef8e2f70b5a2` (`INDEPENDENT_AUDITOR`), parent Scout `8f405b63d0afc8a6ffae522aa68799c54006f52d`.

## Material delta

The prospectively authorized C19-v3 branch now exists:

- `research/c19-official-v3-runtime-closed-20260917@270dc981988eb97e73aa6e126eb73ddd643b9c88`
- exact authorized base: the same `270dc981...` v2 package head
- compare to base: identical, zero successor-specific commits
- branch-creation CI: `35189286833: success`
- no v3 STARTED/control/preserve/evidence/freeze authority observed
- no v3 one-way execution or scientific result

The v2 boundary remains unchanged and immutable: `c19-external-v2-official-v2` is consumed/no-retry under `control/c19-official-v2-started-20260917@7a8af82de0a0dd70ec2391ef7506ae80c5c8d391`, after post-START missing-`torch` failure before model execution/raw/scoring. No scientific PASS/FAIL/INCONCLUSIVE result exists from v2.

The MAIN durable report remains at its 14:40 post-v2-failure checkpoint and is operationally stale relative to the later v3 branch creation. The 15:38 SUB report independently observed v3 creation and correctly remained no-op. Repository refs are authoritative.

## External knowledge integration

No newer external/audit handoff exists. The latest Auditor findings remain binding prospectively: exact unique/total/fail-closed evaluator joins; target-leakage exclusion; frozen quantile edge semantics; deterministic golden scorer fixtures; and narrow interpretation of any future C19 PASS pending representation-matched reductions. No external input changed allocation this cycle.

## Active-line interpretation

A01 remains mixed but its registered A/B/C mechanism programme is closed. RV01 remains development-positive but reducible and consumed. RV02, CX/CX01, and H8/C08 remain terminal negatives. H9/C07 remains `PRE_START_UNDERSPECIFIED` and unreserved because eight new scientific choices are still required. C19 remains the sole PRIMARY unresolved frontier.

## MAIN / SUB allocation

MAIN exclusively owns C19-v3 end-to-end. Current prospective branch is `V3_PRE_START_RUNTIME_CLOSURE`: implement science-invariant dependency/runtime closure, make pre-START CI and one-way use the same exact install recipe, prove the failed import chain in the target-equivalent environment, exercise network-blocked synthetic dry acquisition, bind the final package/runtime artifacts, and re-run exact-head admission. MAIN owns every critical-path fix.

SUB remains `null`; fallback remains `null`; secondary status is `NO_SECONDARY_FRONTIER`. SUB must not take C19 blockers, and MAIN must not manufacture H9/H10 or other secondary science merely to create parallelism.

## #1 GO / STOP

GO only when v3 is fresh/unSTARTED/unconsumed/collision-free; consumed v2 remains untouched; v2 scientific semantics are inherited unchanged; exact source/protocol/package/input/runtime/parser/envelope/scorer/bootstrap/preserver bindings reconstruct; the existing declared learned runtime constraint including `torch==2.13.0` resolves/installs/imports on the exact target platform; same-environment import smoke and synthetic network-blocked dry acquisition pass without official output; evaluator joins remain unique/total/fail-closed and target-safe; STARTED/no-clobber/raw-before-score/immutable-raw-before-targets are enforced; final exact-head CI/review is green; and fresh authority/collision checks pass immediately before STARTED.

STOP on any v2 reuse, need for an ad-hoc new model-runtime version, new scientific/statistical/baseline/metric/claim choice, unresolved dependency/import failure, leakage/join/completeness failure, authority collision, science-affecting redesign, INVALID_EVIDENCE, or any v3 failure after STARTED. Any post-START v3 failure consumes v3 and ends automatic C19 continuation; no routine v4.

Scientific terminal classifiers remain fixed: BREU 95% CI lower `> 0` => PASS; upper `<= 0` => FAIL; otherwise a valid CI containing `0` => INCONCLUSIVE.

## Prospective contingency tree

- `V2_POST_START_FAILURE_CONSUMED`: preserve and never retry; only separately authorized v3 may proceed.
- `V3_PRE_START_RUNTIME_CLOSURE`: current state; MAIN may perform prospectively fixed science-invariant runtime/dependency/CI/binding work in the same run.
- `V3_PRE_START_DEPENDENCY_UNRESOLVED`: STOP before STARTED; no runtime-version improvisation.
- `V3_PRE_START_BLOCKER`: MAIN fixes already-defined mechanical defects itself and continues after fresh reconciliation.
- `V3_PRE_START_SEMANTIC_GAP`: STOP before STARTED.
- `V3_PRE_START_READY_FOR_ONE_WAY`: after all GO gates, MAIN may START once and continue acquisition -> target-blind raw -> immutable raw preservation -> evaluator targets -> scoring -> terminal evidence.
- `V3_POST_START_FAILURE`: preserve diagnostics, consume v3, no retry/no v4, STOP.
- `INVALID_EVIDENCE`: preserve invalidity, withhold result, consume if STARTED, STOP.
- `PASS` / `FAIL` / `INCONCLUSIVE`: finalize the registered terminal evidence and STOP before any follow-up design.

## Top 3

1. MAIN: implement authorized v3 runtime closure and execute exactly once iff every admission gate passes — `VERY_HIGH / VERY_NEAR`.
2. SUB: deliberate no-op under `NO_SECONDARY_FRONTIER` — integrity-preserving now.
3. Non-blocking governance/reporting: catch MAIN durable reporting up to v3 creation; reconcile Issue #147 to canonical v2 failure plus v3 successor; keep #139 open until server-side protection exists.

## Governance advisory

Open PRs remain 0. Issues #139 and #147 remain open. #147 is operationally stale relative to canonical control/v3 branch state. Git tag refs remain 0, repository rulesets remain 0, and `main` remains unprotected. Legacy freeze branches remain authoritative historical anchors and must not be rewritten or force-migrated. Generic execution-environment manifest verification and same-environment import-smoke tooling are potential future `main` promotion candidates only after they become stable and outcome-independent.

## Handoff

MAIN takes C19-v3 and owns ALL critical-path runtime/dependency/CI/workflow/package/binding/admission fixes. SUB takes no lane and has no fallback. MAIN must not absorb nonexistent SUB work; SUB must not take MAIN blockers. Neither worker touches consumed v2, other consumed identities, frozen/formal/preserve/evidence authorities, or legacy freeze branches. Repartition only on material new external/audit evidence before v3 STARTED, an explicitly reserved genuinely independent secondary object, or a terminal v3 state.
