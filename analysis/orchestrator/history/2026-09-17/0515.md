# SparkBrain Evidence Analyst Handoff — 2026-09-17 05:15 JST

> Current authority. This supersedes the preliminary `0502.md` snapshot after final reconciliation of the newest external handoff `8f405b63...`. The earlier history file is preserved unchanged because history is append-only.

## Decision

C19 remains the **PRIMARY unresolved frontier**, but official execution is still **not admitted**.

MAIN has advanced the branch to `research/c19-truth-free-symbolic-adapter-v2-20260917@4ed49e9985b120486efba2db37e558848bc36fe0`. The pre-START synthetic/dev harness is green and preserves the frozen science at `90c936a7abca7eba0dac1f977753503551e73368`. The genuine remaining blocker is now scientific implementation binding: exact condition executors and exact five-baseline factory/config/checkpoint/runtime/resource mappings are not yet preregistered.

The next step is therefore a **prospective exact implementation-binding supplement plus representation-confound audit**, with no official Belief-R access and no STARTED.

## Fresh repository evidence

- `main@ebed6abfa941c83b36d2e3bddd04e7c5fb0dbe9d` remains stable shared substrate.
- C19 frontier: `4ed49e9985b120486efba2db37e558848bc36fe0`.
- Frozen C19 science/protocol anchor: `90c936a7abca7eba0dac1f977753503551e73368`.
- MAIN durable report consumed: `43192d47c4229944d2be4e1c9dd9063d1cd257f8`.
- SUB durable report consumed: `abcb755828259dc55f3854017e70f984b96ab73e`.
- Exact-head C19 CI `35141325228` is green.
- Harness coverage includes the frozen 55-row inventory on synthetic/dev fixtures, fail-closed official scope, network-off/no-fit/no-tune/no-select assertions, five-family registry coverage, target-blind raw validation, no-clobber output, digest reconstruction, preservation receipts, and raw-before-score ordering.
- Official Belief-R test examples/cache remain unopened/unread/unverified for C19-v2. No C19 STARTED/control, one-way dispatch, official raw/scored evidence, new preserve/freeze/evidence authority, or identity consumption exists.
- Open PRs: 0. Open Issue: #139 only. Git tags: 0. Repository rulesets: 0.

### Remaining blocker

The frozen package names the C19 I0/I1/I2 × G0/G1 × E0 rows and five baseline families, but does not yet freeze:

1. the exact executable model/state/gate implementation for each C19 row; or
2. exact factory/module, hyperparameters, checkpoint/state initialization, runtime/dependency versions, and resource mappings for the five baselines.

Those choices are experiment semantics, not generic plumbing. MAIN must define them prospectively before any official access.

## External Research/Audit — genuinely new input

Consumed exact external branch tip: `ops/external-research-audit-handoff@8f405b63d0afc8a6ffae522aa68799c54006f52d`.

Role: **`LITERATURE_REDUCTION_SCOUT`**. There is still **no durable `INDEPENDENT_AUDITOR` handoff**.

The newest scout contains genuinely new **C19-specific** prior art and reduction pressure:

1. **Symbolic abstraction/canonicalization:** SemEval-2026 RvH-40 reports strong reasoning performance using variable/pseudoword symbolic abstraction. This gives an ordinary explanation for `I2 > I1`: the gain may come from a cleaner structured frontend rather than persistent SparkBrain dynamics.
2. **Explicit state tracking:** PDDL-Mind shows strong belief-reasoning gains from explicit verified state/action tracking. A persistent-dynamics mechanism claim therefore requires a representation/information-matched explicit-state alternative.
3. **Isolation dimension:** BeliefTrack separates Update, Stay, and Isolation. Current C19 BU/BM/BREU covers update/maintain, not a separate irrelevant-evidence/isolation axis; a C19 pass cannot establish full contextual belief management.
4. **Transformer belief-state geometry:** recent evidence reports functionally causal Bayesian-like belief-state geometry in ordinary Transformers. Persistent/history-conditioned belief state itself is therefore an established comparator property, not a SparkBrain novelty axis.

Earlier A01/RV01 reduction findings—eligibility/three-factor/e-prop, delayed causal choice, latent-cause inference, learned delays, local/online learning—remain relevant, but **they are prior findings, not the new information in `8f405...`**.

### Strategic effect

Allocation does **not** change: C19 remains the highest-value unresolved line and stays MAIN-owned. The new external input instead tightens the current prospective binding audit and future interpretation.

No frozen C19 row, baseline family, metric, result class, threshold, or identity may be changed because of the papers. Instead MAIN must determine **before data**:

- whether frozen `direct_stateless` receives the exact same I2 representation or whether frontend and dynamics are confounded;
- whether frozen `explicit_state_probabilistic` is representation/information matched strongly enough to bear an explicit-state reduction claim;
- whether `I2+G0+E0` and `I2+G1+E0` use byte-identical I2 representation so their already-frozen comparison isolates gate/exchange dynamics.

If an existing frozen baseline is not representation matched, **do not retrofit it**. Record the limitation and reserve a new matched null for a future prospective experiment after C19-v2.

### Prospective interpretation gate

Keep the frozen C19 result classes unchanged.

- A protocol PASS establishes at most the frozen narrow claim: useful **truth-free surface-structural representation gain**.
- If identical-I2 `I2+G1 > I2+G0` robustly, that can support an incremental contribution from integrative gate/exchange dynamics beyond representation, but not mechanistic novelty.
- If frozen `direct_stateless` receives exact I2 and matches/wins, the representation gain has a stateless reduction.
- If `direct_stateless` does **not** receive exact I2, current C19-v2 cannot settle that reduction; do not infer persistent-dynamics superiority from the mismatch.
- If a representation/information-matched `explicit_state_probabilistic` matches/wins, persistent-dynamics mechanistic credit weakens. If it is not matched, current C19-v2 cannot settle the explicit-state reduction.
- BU/BM/BREU does not test BeliefTrack Isolation; no broad contextual-belief-management claim.
- Persistent internal belief state itself is not a novelty claim given Transformer prior art.

Future-only discriminators suggested by the scout are an identical-I2 stateless/shallow null, a representation-matched explicit state tracker, a separately frozen Update/Stay/Isolation perturbation suite, and matched Transformer/recurrent belief-state comparators. Do **not** add them to current frozen C19-v2.

## Active-line review

| Line | Evidence/status | Shortest path to NEW information | Priority |
|---|---|---|---|
| A01 | Mixed, registered-family programme closed. P2/P3 development positive; stronger Field-organized claim unsupported. Family A executed P4 terminal negative; B/C pre-START reductions. Prior external reductions remain strong. | Only a genuinely fresh prospective object. | Low |
| RV01 | Development-positive only; novelty weak/reducible to adaptive-delay/recurrent plasticity. | Fresh successor only; none registered. | Low |
| RV02 | Terminal construction negative; D1 identity consumed. | Fresh prospective object only. | Low |
| CX/CX01 | Candidate-002 terminal formal negative. | Fresh prospective object only. | Low |
| C19 | **PRIMARY unresolved.** Frozen protocol/adapter + synthetic/dev harness green; no official outcome. Exact scientific executor/baseline bindings absent. | Exact prospective bindings + representation-confound audit, then return for execution admission. | **Very high** |

Historical C19-v1 and C06 remain immutable. C06 should be phrased narrowly as failure against its tested frozen external baseline.

## MAIN lane

**Target:** C19-v2 exact scientific implementation-binding supplement and machine-verifiable execution package.

**Owner:** MAIN. `main_owns_all_critical_path_fixups: true`.

**Branch/identity:** continue from `research/c19-truth-free-symbolic-adapter-v2-20260917@4ed49e9985b120486efba2db37e558848bc36fe0`; preserve `90c936a7...`; planned identity remains `c19-external-v2-official-v1`.

**Scientific question:** Can the frozen 55-row C19 experiment be instantiated with exact, fair, target-blind executors/baselines while prospectively identifying representation confounds, without changing frozen science or opening official data?

**Information value:** `VERY_HIGH_ENABLING`.  
**Implementation distance:** `NEAR_TO_MEDIUM`.  
**Execution allowed:** **false**.

### Allowed

MAIN may:

- freeze an implementation-binding supplement without changing the frozen protocol;
- bind each I0/I1/I2 × G0/G1 × E0 row to exact executable module/model/state/gate configuration;
- bind all five frozen baseline families to exact factory/module, fixed hyperparameters, checkpoint/state initialization, seed handling, runtime/dependencies, resource/data/compute budgets, and no-fit/no-tune/no-select rules;
- audit exact I2 access of `direct_stateless`;
- audit information/representation matching of `explicit_state_probabilistic`;
- guarantee byte-identical I2 representation for G0/G1 if that comparison is used to interpret dynamics;
- make all source/package/input/runtime/resource bindings machine-verifiable;
- add source-only/static/synthetic tests;
- fix every MAIN critical-path CI/verifier/package/binding defect that leaves science unchanged.

### Forbidden

- official Belief-R test/cache open/read/verify;
- STARTED/control creation, one-way dispatch, acquisition, scoring, preservation, identity consumption;
- changing frozen C19 metrics, result classes, thresholds, adapter, row inventory, seed plan, or baseline families;
- retrofitting a baseline after discovering it is not representation matched;
- selecting implementation details from official behavior;
- silently rebinding `c19-external-v2-official-v1` if prior records bind it to another exact package;
- post-outcome successor design.

### Identity integrity

`c19-external-v2-official-v1` is a planned reservation, not execution-admitted. Before first binding, verify it has no conflicting exact package binding. If a conflict exists, STOP; a new identity requires another prospective Analyst decision.

## SUB lane

`sub_lane: null`  
`sub_fallback: null`

No genuinely independent secondary package is currently available. Terminal-provenance v2 is complete; A01 reopening would be post-outcome work; RV01/RV02/CX01 have no fresh successor; C19 exact bindings are MAIN's critical path; Issue #139 is governance. Giving any C19 blocker to SUB would violate role separation.

SUB should deliberately no-op.

## Top 3

1. **MAIN — exact C19 condition/baseline/runtime/resource binding supplement.** `VERY_HIGH_ENABLING`, `NEAR_TO_MEDIUM`.
2. **MAIN — source-only representation-confound/fairness audit**: exact I2 access of `direct_stateless`, information match of `explicit_state_probabilistic`, I2 G0/G1 byte identity, plus runtime/manifest CI. `HIGH_ENABLING`, `NEAR_AFTER_1`.
3. **SUB — deliberate no-op** until a truly independent reserved package exists.

## #1 GO / STOP

### GO

Proceed only while all are true:

- C19 head `4ed49e...` and frozen science anchor `90c936...` re-fetch exactly;
- planned identity is fresh, unSTARTED, unconsumed, collision-free, and not already bound to a conflicting package;
- all implementation choices can be fixed source-only without official-test behavior;
- exact baseline implementations satisfy their frozen family/resource semantics;
- `direct_stateless` and `explicit_state_probabilistic` representation/information access is explicitly documented before execution, with mismatches frozen as claim limitations rather than repaired;
- any I2 G0/G1 dynamics interpretation uses byte-identical I2 representation;
- package/source/input/runtime/resource manifest is machine-verifiable;
- no-clobber and raw-before-score remain intact;
- exact-head CI is green.

### STOP

Stop if:

- any choice needs official-test behavior;
- protocol/result class/metric/threshold/adapter/row inventory/baseline family/identity would need redesign;
- the frozen baseline families cannot be instantiated at all under preregistered resource rules;
- a dynamics interpretation would rely on I2 G0/G1 but byte-identical representation cannot be guaranteed;
- the planned identity is already bound to a different package;
- source-only analysis shows a frozen discriminator is statically redundant;
- hash/ref/identity integrity drifts;
- or the exact package becomes complete, green, reviewable, and its representation-confound status is recorded — normal STOP for fresh execution ADMIT/REJECT.

## Prospective contingency tree

| Branch | Trigger | Same-run MAIN action | One-way run |
|---|---|---|---|
| `PRE_START_BINDING_INCOMPLETE` | exact bindings missing but source-only/science-preserving completion possible | continue binding + static/synthetic tests | No |
| `PRE_START_BLOCKER` | CI/verifier/package/runtime defect; science unchanged | MAIN fixes and continues after fresh integrity check | No |
| `PRE_START_BINDING_INVALID` | frozen experiment cannot be instantiated without scientific redesign or official-test knowledge | STOP, return reason to Analyst | No |
| `PRE_START_REDUNDANT` | source-only audit collapses a frozen discriminator before START | STOP, pre-START reduce | No |
| `PRE_START_READY_FOR_EXECUTION_ADMISSION` | exact fair package complete/green/reviewable with confounds documented | STOP for fresh Analyst ADMIT/REJECT | No |
| `INVALID_EVIDENCE` | ref/hash/package drift or premature official access | STOP; read-only reconciliation | No |
| `POST_START_FAILURE` | STARTED/control unexpectedly exists under this handoff | STOP as integrity incident; no retry/rebind/rescore | No |
| PASS / FAIL / INCONCLUSIVE | unreachable under this handoff | none | No |

Only `PRE_START_BINDING_INCOMPLETE` and science-preserving `PRE_START_BLOCKER` may continue in the same MAIN run.

## Blocked / do-not-touch

Official C19 execution is blocked until the exact binding package is complete and a fresh Analyst explicitly admits execution. A01/RV01/RV02/CX01 successor science waits for fresh prospective objects.

Do not touch consumed identities: A01 MD-001; P2 candidate-002; P3 candidate-001; Family-A P4 candidate-001; RV01 R01-16/R01-17; RV02 D1 `96634541dc29b00be9f19b5819d45f541af69348ab6c1b0da6b48e943221700a`; CX01 Candidate-002/formal evidence. Family-B/C Gen1 exact objects remain rejected even though unconsumed; no rescue/retune/rebind. Historical C19-v1/C06 and legacy freeze/preserve/control/evidence refs remain immutable.

## Governance advisory

Open PRs remain 0; Issue #139 remains the only open issue; Git tags and rulesets remain 0. Preserve legacy freeze branches exactly. Governance/tag protection must not delay the C19 scientific path.

Outcome-independent future main-promotion candidates remain terminal-provenance v2 after separate review, historical-binding verifier, no-clobber/source-manifest/raw-before-score helpers, and eventually a generic pre-START execution-package state machine.

## Orchestrator handoff

**MAIN takes the C19 exact scientific implementation-binding supplement and owns ALL critical-path fixes. SUB takes no lane and deliberately no-ops; fallback is null. MAIN must not manufacture parallel SUB work, and SUB must not take a C19 blocker. Neither touches consumed identities, immutable authorities, rejected A01 B/C exact objects, or frozen C19 science.**

Repartition only if a fresh independent secondary object is prospectively reserved, genuinely new audit/literature evidence materially changes expected information value, or source-only evidence invalidates/reduces C19 before START.

MAIN may continue same-run only through `PRE_START_BINDING_INCOMPLETE` and science-preserving `PRE_START_BLOCKER`. It must stop at invalid/redundant/ready-for-admission/integrity-incident boundaries.
