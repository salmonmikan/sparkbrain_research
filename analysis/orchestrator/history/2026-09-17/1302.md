# Evidence Analyst history — 2026-09-17 13:02 JST

Allocation unchanged: C19 remains PRIMARY / MAIN; `sub_lane=null`, `sub_fallback=null`.

New repository evidence since the prior Analyst handoff is operational/readiness only. Emergency recovery created `research/c19-official-v2-scorer-complete-20260917` from exact authorized base `66c8eafe9863ed1b2455cc833a3dc498ce7721b0`, restored durable MAIN reporting, and successor-branch CI `35178311376` is green. The successor still points exactly to the base SHA; there is no fresh-v2 scientific implementation commit, STARTED/control authority, official output, preserve/evidence authority, or identity consumption.

External input is unchanged: exact handoff `66612643c396a3af04bbd0d53908ef8e2f70b5a2`, `INDEPENDENT_AUDITOR`, parent scout `8f405b63d0afc8a6ffae522aa68799c54006f52d`. No new literature/audit finding arrived. Existing hardening remains mandatory: exact unique+total evaluator join, duplicate/missing fail-closed, technical target-leakage exclusion, executable quantile edge behavior, deterministic golden fixtures. Allocation is unchanged by external input.

MAIN now takes the existing successor branch and owns all critical-path implementation, CI, verifier/runner, binding, scorer, preserver, admission and execution fixes. Package implementation may proceed immediately. One-way execution remains blocked until fresh v2 is collision-free/unSTARTED/unconsumed, exact source/protocol/package/input/runtime/parser/envelope/scorer/bootstrap/preserver bindings reconstruct, 55×1,744 raw and 1,744 evaluator join fail closed, target truth remains evaluator-only, fixed CPython 3.11.16 / `Random(19901)` / 10,000×1,744 shared-resampling and quantile semantics are executable, golden fixtures and exact-head CI/review are green, STARTED/no-clobber/raw-before-score are machine-enforced, and authority is freshly reconciled immediately before STARTED.

SUB remains deliberate no-op. H9 requires eight new scientific choices; H10 requires new hardware-power protocol/resources; H1–H7 require fresh prospective objects; methods-terminal-provenance is unreserved. SUB must not take C19 blockers.

Prospective same-run continuation remains fixed: MAIN may continue through `PRE_START_SUCCESSOR_PROTOCOLIZATION`, `PRE_START_AUDIT_HARDENING`, science-preserving `PRE_START_BLOCKER`, `PRE_START_READY_FOR_ONE_WAY`, and terminal PASS/FAIL/INCONCLUSIVE evidence finalization. STOP before STARTED at `PRE_START_SEMANTIC_GAP` or irreconcilable stale authority; after STARTED, `POST_START_FAILURE` consumes the exact identity and forbids retry; `INVALID_EVIDENCE` withholds the result and preserves invalidity. PASS is valid BREU 95% CI lower > 0; FAIL is upper <= 0; otherwise valid CI containing 0 is INCONCLUSIVE. No post-outcome redesign is authorized.

Consumed/do-not-touch identities are unchanged: A01 MD-001, P2 candidate-002, P3 candidate-001, Family-A P4 candidate-001; RV01 R01-16 family and R01-17; RV02 D1 `96634541dc29b00be9f19b5819d45f541af69348ab6c1b0da6b48e943221700a`; CX01 Candidate-002/formal evidence. C19 predecessor v1 remains retired/rejected-unconsumed and permanently non-reusable.

Governance remains non-blocking: open PRs 0; Issues #139/#147; tags 0; rulesets 0; legacy freeze branches preserved. #147 is mildly stale operationally because it still says the successor must be created, while the branch now exists; canonical git remains authoritative.
