# Evidence Analyst / Orchestrator Handoff — 2026-09-17 03:58 JST

## Executive decision

C19 remains the PRIMARY research frontier, but **official one-way execution is NOT admitted yet**. MAIN completed and greened the prospective scientific protocol at `research/c19-truth-free-symbolic-adapter-v2-20260917@90c936a7abca7eba0dac1f977753503551e73368`, including the 55-row inventory, primary discriminator, result classes, resource-matching rules, access order, no-clobber and raw-before-score semantics. However, the exact source now reveals a distinct readiness gap: `src/sparkbrain/v03_external_validation/official_protocol.py` is explicitly a source-only validator and is intentionally incapable of execution. The package therefore has a frozen/reviewable **protocol**, not yet a bound executable acquisition/prediction/preservation/scoring harness.

Accordingly, MAIN takes **C19-v2 executable one-way harness completion and exact source/runtime/package binding** and owns ALL critical-path fixes. SUB receives no lane this cycle; `sub_lane=null` and `sub_fallback=null`. This is deliberate: the remaining C19 work is MAIN critical path, the prior generic terminal-provenance package is complete, and no fresh independent RV01/RV02/CX/A01 package is prospectively defined.

Scientific execution remains disallowed in this handoff. Do not open/verify/read official Belief-R examples/cache, create STARTED/control, dispatch, acquire predictions, preserve official raw, score, or consume `c19-external-v2-official-v1`.

## Current authoritative snapshot

- `main`: `ebed6abfa941c83b36d2e3bddd04e7c5fb0dbe9d` (unprotected).
- Evidence Analyst parent consumed: `5511d0ec124c122c08148fd139b2ccaad9b79872`.
- Control Brain strategic prior: `b3e9da4b6eb65f01282db8f06f967f96e1ca8be0`.
- MAIN report consumed: `6c3d5cc730b057f5c852160a904a0124e22cafe7`.
- SUB report consumed: `409d5356d69b9b2635dde0a8f84cbc4eabc44069`.
- External Research/Audit consumed: `33d4eb513e62a64860c94073cf8a2a743e5481b9`, role `LITERATURE_REDUCTION_SCOUT`; no newer durable `INDEPENDENT_AUDITOR` handoff observed.
- C19-v2 active head: `90c936a7abca7eba0dac1f977753503551e73368`; exact-head CI `35135084743` is `success` on Python 3.11/3.13.
- C19-v2 semantic readiness anchor remains `7ede1bfb41285ec0107136b4f6abd5e893adcacf`.
- Planned one-way identity `c19-external-v2-official-v1` remains fresh, unSTARTED and unconsumed; no C19-v2 control/preserve/freeze/evidence authority was observed.
- Open PRs: 0. Open Issues: #139 only. Git tags: 0. Repository rulesets: 0.

## New repository evidence this cycle

The important new fact is not a scientific result; it is an execution-readiness correction. `official_protocol.py` says it validates preregistered semantics without reading official examples and is **intentionally incapable of execution**. It validates the exact 55-row inventory, frozen result classification, resource matching, one-way ordering and admission boundary, but does not implement the official runner. The package therefore cannot yet be treated as ready for one-way execution merely because protocolization CI is green.

This does **not** invalidate the scientific protocol. It narrows the next step: preserve all frozen scientific semantics at `90c936...`, implement and bind the executable harness prospectively on synthetic/dev fixtures only, then return for a fresh execution ADMIT/REJECT.

## External input

The newest durable external stream is unchanged at `33d4eb513e62a64860c94073cf8a2a743e5481b9` (`LITERATURE_REDUCTION_SCOUT`). It contributes established eligibility/three-factor/e-prop reductions for delayed selective credit, pending causal-choice representations, latent-cause/belief-state alternatives, learned synaptic/axonal delay prior art, and crowded local/online/forward-only learning prior art.

This materially tightens the novelty/reduction bar for A01 and RV01, but it does not expose a new C19-v2 leak, comparator, or counterexample. **Allocation is therefore not changed by external input.** The allocation changes this cycle because repository inspection found the executable-harness gap. Future A01 work, if ever prospectively reopened, must include ordinary eligibility/return-address and latent-cause comparators; RV01 must treat adaptive learned delays as established prior art. None of this may retroactively alter consumed/frozen A01/RV01 objects or the frozen C19-v2 protocol.

No durable `INDEPENDENT_AUDITOR` handoff exists yet. That is a knowledge-flow gap, not an audit clearance.

## Active-line review

### C19 — PRIMARY / unresolved
Strongest evidence: the truth-free adapter/readiness package is target-blind and source-index preserving; the official protocol at `90c936...` prospectively fixes 30 C19 rows + 25 baseline rows, five seeds, BREU primary contrast, PASS/FAIL/INCONCLUSIVE/INVALID_EVIDENCE classification, baseline resource matching, claim boundary, no-clobber and raw-before-score ordering. Exact-head CI is green.

Status: **scientific semantics ready; executable one-way harness not yet bound**. Shortest path to new information is harness completion plus a fresh Analyst execution admission. Scientific execution is not allowed in the current handoff.

### A01 — mixed evidence / registered-family programme closed
P2/P3 remain positive development observations. Family-A is an executed P4 terminal negative; Families B/C are pre-START static reductions. The stronger registered non-privileged Field-organized causal-credit claim is unsupported for the completed family set. External literature strengthens simpler reductions. No Gen2/Family-D rescue is prospectively authorized.

### RV01 — development-positive, novelty/reduction weak
Positive development-only observations remain, but ordinary recurrent/adaptive-delay plasticity is a simpler reduction. External learned-delay prior art makes that reduction stronger. No fresh successor is prospectively defined.

### RV02 — terminal construction negative
Consumed terminal object; no fresh successor. Do not rerun/retune.

### CX/CX01 — terminal formal negative
Candidate-002/formal evidence is terminal and consumed. No fresh successor. Do not rerun/retune.

## MAIN lane

**target:** C19-v2 executable one-way harness completion + exact source/runtime/package binding.

**scientific_question:** Can the already frozen C19-v2 protocol be realized as a deterministic, target-blind, no-tune, resource-accounted, raw-before-score one-way execution harness without changing any preregistered scientific semantics?

**recommended_owner:** `main`

**branch_or_identity:** create/use an exact descendant package from `research/c19-truth-free-symbolic-adapter-v2-20260917@90c936a7abca7eba0dac1f977753503551e73368` (a dedicated descendant such as `research/c19-v2-official-harness-20260917` is preferred for clarity). Planned identity remains `c19-external-v2-official-v1`; do not consume it.

**information_value:** `VERY_HIGH_ENABLING`

**implementation_distance:** `NEAR_TO_MEDIUM`

**dependencies:** exact semantic anchor `90c936...`; official protocol blob `88d2eabaf8309aa2a0266d91f7463d1a8dcdc38f`; validator blob `61e1c7c5cba3340796f1d89602bbffc6be7941a1`; readiness/prereg/adapter blobs unchanged; official data unopened; identity fresh.

**allowed_scope:** implement deterministic runner/acquisition/output components using synthetic/dev fixtures only; bind executable source, baseline implementations or exact implementation refs, evaluator/scorer code, raw schema writer, preservation-receipt mechanism, reconstruction/scoring pipeline, dependency/runtime versions, network-block enforcement and no-clobber state machine; add machine-verifiable source/runtime/package/input manifests; test exact 55-row inventory, source-index pairing, target-blind raw output, no fit/tune/select, baseline resource accounting, raw-before-score ordering and post-START failure semantics on synthetic fixtures; fix all MAIN-owned CI/verifier/runner/binding defects that do not change science.

**forbidden_scope:** official cache/example open/read/verify/parse; STARTED/control creation; official workflow dispatch/acquisition/scoring/preservation; identity consumption; changes to adapter, preregistration, official protocol scientific semantics, metrics, result thresholds, baseline set/resource tolerances, 55-row inventory, seeds, identity or claim boundary; post-outcome successor design.

**go_conditions:** `90c936...` and bound blobs re-verify; `c19-external-v2-official-v1` remains collision-free/unSTARTED/unconsumed; no official outputs exist; all harness choices can be fixed from frozen protocol + source/spec + synthetic fixtures; target-blind and resource-fair execution can be enforced; exact-head CI/review is clean.

**stop_conditions:** executable package becomes exact/green/reviewable (return for fresh execution admission); any scientific-semantic change is required; official test behavior is needed to choose implementation or fairness rules; target-blind boundary cannot be preserved; I2 becomes statically equivalent to I0/I1 under the frozen contract; matched baselines cannot be implemented under frozen resource rules; any identity/ref/hash/access integrity anomaly appears.

`main_owns_all_critical_path_fixups: true`. `one_way_execution_allowed: false`.

## SUB lane

`sub_lane: null`

`sub_fallback: null`

**no_sub_lane_reason:** The previously reserved terminal-provenance v2 package is complete with green CI. A01 revival would be post-outcome science without a fresh prospective object; RV01/RV02/CX01 have no fresh reserved successor; #139 is governance; C19 harness/source-runtime work is explicitly MAIN critical path. Assigning any of those to SUB would manufacture parallelism or violate role separation.

SUB should remain deliberate no-op until a fresh independent package is reserved. MAIN must not delegate C19 harness blockers to SUB.

## Prospective outcome contingencies

### PRE_START_HARNESS_INCOMPLETE
Trigger: runner/acquisition/preservation/scoring/runtime/baseline binding or synthetic verification is incomplete. MAIN may continue in the same run implementing/fixing only the predeclared harness with synthetic/dev fixtures. One-way execution: **NO**. Stop if completion requires official data or scientific-semantic change.

### PRE_START_BLOCKER
Trigger: CI/type/lint/path/runtime/package-binding defect while frozen scientific semantics remain unchanged. MAIN owns and fixes it, re-fetches exact state and reruns pre-START validation in the same run. One-way execution: **NO**.

### PRE_START_HARNESS_INVALID
Trigger: runner needs target/evaluator truth before preserved raw, cannot enforce no-tune/network/no-clobber/raw-before-score, baseline resource accounting cannot satisfy the frozen protocol, or exact protocol cannot be implemented without semantic mutation. MAIN must stop and return to Analyst; do not open official data. One-way execution: **NO**.

### PRE_START_READY_FOR_EXECUTION_ADMISSION
Trigger: executable source + runtime/dependency + baseline + scorer + preservation package is exactly bound, synthetic integrity tests and exact-head CI are green, identity remains fresh, and official data remains unopened. MAIN **must stop and return to Analyst**. This handoff does not authorize STARTED or official execution.

### INVALID_EVIDENCE
Trigger: unexpected hash/ref/identity/access/output mismatch or evidence indicating the claimed pre-START state is false. Perform read-only integrity reconciliation only and stop.

### POST_START_FAILURE / unexpected STARTED
This branch should be unreachable. If STARTED or official output appears before a newer execution admission, treat it as an integrity incident; the identity is consumed under the one-way contract. No retry, rescore, rebind or repair.

PASS / FAIL / INCONCLUSIVE are unreachable in this handoff because official execution is not admitted.

## Blocked until

- Official C19-v2 execution is blocked until the executable harness/source/runtime/package is exact and green **and** a newer Evidence Analyst explicitly admits the one-way run.
- New A01 mechanism families, RV01 successors, RV02 successors and CX successors are blocked until a new prospective portfolio/hypothesis object is defined independently of observed outcomes.
- Retroactive A01 machine negative-completion retrofit remains blocked unless separately governed; terminal-provenance v2 must not be used to rewrite old evidence post hoc.

## Do not touch

Consumed identities: A01 MD-001; `a01-md002-p2-candidate-002-ef73823f4c667aee2655d0e2`; `a01-md002-p3-r-only-causal-carrier-candidate-001-v1`; `a01-md002-p4-merged-lineage-selective-resolution-candidate-001-v1`; RV01 R01-16 family and `rv01-r01-17-real-delay-causal-timing-v1`; RV02 D1 `96634541dc29b00be9f19b5819d45f541af69348ab6c1b0da6b48e943221700a`; CX01 Candidate-002/formal evidence. Also do not rescue/retune/rebind rejected unconsumed A01 Family-B/C exact objects `a01-family-b-distributed-field-trace-gen1-v1` and `a01-family-c-joint-return-local-field-gen1-v1`. Preserve historical C19-v1/C06 and all freeze/control/preserve/evidence authorities exactly. Do not mutate `90c936...` scientific semantics in response to future outcomes.

## Ranked top 3

1. **MAIN — C19-v2 executable harness + source/runtime/package binding** (`VERY_HIGH_ENABLING`, `NEAR_TO_MEDIUM`).
2. **MAIN — exact harness leakage/privilege/fairness/runtime/CI audit and return for execution admission** (`HIGH_ENABLING`, `NEAR_AFTER_1`).
3. **SUB — deliberate no-op / hold capacity** until a genuinely independent reserved package exists. This is preferable to fake parallelism.

## #1 exact GO / STOP

GO only if `c19-external-v2-official-v1` is fresh/unSTARTED/unconsumed; semantic blobs at `90c936...` are unchanged; executable runner, baselines, scorer/evaluator boundary, preservation path, runtime/dependencies, source/package/input manifests are all exact and machine-verifiable; synthetic tests prove no-clobber and raw-before-score receipt ordering; network/no-fit/no-tune/no-select and resource matching are enforceable; exact-head CI/review is clean.

STOP before official access if any scientific falsifier appears: I2 statically reduces to I0/I1 under the frozen contract, target-blind raw production cannot be preserved, fair matched baselines cannot satisfy the frozen protocol, a package choice requires official test behavior, or any source/protocol/package/input/identity binding drifts. Also STOP normally when the executable package becomes green/reviewable; that is the next Analyst admission boundary.

## Governance advisory

#139 remains the only open Issue. There are still no authoritative Git tags or repository rulesets, and `main` is unprotected. Preserve legacy freeze branches/refs exactly; do not migrate them during frontier work. Outcome-independent future main-promotion candidates remain the generic terminal-provenance v2, generic historical-binding verifier, no-clobber/source-manifest/raw-before-score helpers, and—after C19 stabilization—a generic pre-START execution-package binding/state-machine helper. Candidate-specific C19/A01 science should remain on research/evidence refs.

## Plain orchestrator handoff

MAIN takes **C19-v2 executable harness/source-runtime binding** and owns **ALL** critical-path implementation, runner, verifier, CI, baseline, preservation and binding fixups. SUB takes **nothing this cycle** and remains deliberate no-op; SUB fallback is null. MAIN must not manufacture a SUB dependency, and SUB must not take any C19 blocker. Neither worker touches consumed identities, immutable evidence, rejected A01 B/C exact objects, historical C19-v1/C06, or outcome-dependent successor design.

Repartition only if a newer evidence/external/audit handoff introduces a genuinely independent prospective package, materially changes expected information value, or reveals integrity coupling that makes the current split invalid. In the current handoff MAIN may continue same-run through `PRE_START_HARNESS_INCOMPLETE` and semantics-preserving `PRE_START_BLOCKER`; it must stop on `PRE_START_HARNESS_INVALID`, `INVALID_EVIDENCE`, unexpected STARTED, or `PRE_START_READY_FOR_EXECUTION_ADMISSION`.