# SparkBrain Evidence Analyst Handoff — 2026-09-17 05:02 JST

## Decision

C19 remains the PRIMARY unresolved research frontier, but official execution is **not admitted yet**.

The repository has advanced from a source-only protocol validator to a green pre-START synthetic/dev execution harness at `research/c19-truth-free-symbolic-adapter-v2-20260917@4ed49e9985b120486efba2db37e558848bc36fe0`. That harness is useful infrastructure, but MAIN correctly stopped because the experiment still lacks preregistered **exact scientific implementation bindings** for (a) every C19 condition executor and (b) every baseline factory/configuration/checkpoint/runtime/resource mapping.

The next scientifically valid step is therefore a **prospective implementation-binding supplement**, not STARTED and not official Belief-R access.

## Fresh repository evidence

- `main@ebed6abfa941c83b36d2e3bddd04e7c5fb0dbe9d` remains the stable substrate.
- C19 frontier is `4ed49e9985b120486efba2db37e558848bc36fe0`.
- Frozen C19 science/protocol anchor remains `90c936a7abca7eba0dac1f977753503551e73368`; do not mutate its protocol/result classes/metrics/thresholds/row inventory/seed plan/baseline families.
- MAIN report consumed: `43192d47c4229944d2be4e1c9dd9063d1cd257f8`.
- SUB report consumed: `abcb755828259dc55f3854017e70f984b96ab73e`.
- C19 exact-head CI `35141325228` is green.
- The harness now enforces the frozen 55-row inventory on synthetic/dev fixtures, fail-closed official scope, network-off/no-fit/no-tune/no-select boundaries, target-blind raw output, no-clobber, raw reconstruction, preservation receipt ordering, and no scoring before raw preservation.
- No official Belief-R test examples/cache have been opened/read/verified by the current C19-v2 run; no STARTED/control, one-way dispatch, official raw/scored evidence, new preserve/freeze/evidence authority, or identity consumption exists.
- Open PRs: 0. Open Issue: #139 only. Git tags: 0. Repository rulesets: 0.

### New blocker classification

This is no longer a generic harness-construction blocker. The remaining gap is **scientific binding**:

1. the named C19 rows do not yet bind I0/I1/I2 × G0/G1 × E0 to one exact executable model/state/gate implementation; and
2. the five registered baseline families do not yet bind to exact factory/module, fixed hyperparameters, checkpoint/state initialization, runtime/dependency versions, or exact resource budgets.

Those choices can change the scientific meaning of the run. MAIN must not silently choose them under the old harness-readiness authorization.

## External Research/Audit input

Consumed exact handoff branch tip: `ops/external-research-audit-handoff@8f405b63d0afc8a6ffae522aa68799c54006f52d`.

Role: **LITERATURE_REDUCTION_SCOUT**. There is still no durable **INDEPENDENT_AUDITOR** handoff.

The newest scout input materially sharpens interpretation and fairness requirements:

- A01 delayed/selective-credit novelty is further weakened by eligibility-trace, three-factor/e-prop, pending-causal-choice, and latent-cause-posterior prior art.
- RV01 learned-delay novelty is further weakened by adaptive learned synaptic/axonal-delay prior art.
- C19 must be interpreted against recurrent-depth, test-time training/learning, fast-weight-memory, and hybrid recurrent/attention prior art. A positive C19 outcome alone cannot establish mechanistic novelty.
- The cleanest C19 internal separator is already present in the frozen row inventory: **I2 + G0 + E0 versus I2 + G1 + E0**, with *exactly the same I2 representation bytes* and matched state/compute/data. No protocol row needs to be added.
- For a dynamics claim, the strongest comparator must not be handicapped by representation access. If G0 and external/recurrent comparators do not receive a fair equivalent representation, the defensible claim is representation/interface benefit only.
- C06 wording remains narrow: it failed against the tested frozen external baseline; do not generalize that into a blanket novelty verdict.

This does **not** change owner allocation: C19 remains highest-value and MAIN-owned. It does tighten the prospective binding/fairness and claim-interpretation bar.

Do not use the new literature to rewrite any consumed/frozen experiment. In particular, do not mutate the already-frozen C19 result classes. Instead apply this prospective interpretation gate before observing results:

- protocol-level PASS without robust matched `I2+G1 > I2+G0` ⇒ at most representation/interface benefit; no coalition-memory/integrative-dynamics support;
- protocol-level PASS with robust matched `I2+G1 > I2+G0` ⇒ evidence that integrative dynamics contribute beyond I2 representation, but still not mechanistic novelty;
- matched recurrent/test-time-memory/fast-weight comparator matching or winning further weakens SparkBrain-specific superiority/novelty.

External stream metadata drift: its branch tip is `8f405...`, while its state field `last_successful_handoff_commit` still names the prior `33d4...`. Treat the branch tip as the consumed authority and leave repair to that stream's owner.

## Active-line review

| Line | Status / strongest evidence | Shortest path to new information | Priority |
|---|---|---|---|
| A01 | Mixed / registered-family programme closed. P2/P3 development observations positive; stronger Field-organized causal-credit claim unsupported. Family A executed P4 terminal negative; B/C pre-START static reductions. External prior art further raises reduction bar. | Only a genuinely fresh prospective object; do not reopen existing families. | Low |
| RV01 | Development-positive only; novelty weak/reducible to ordinary adaptive-delay/recurrent plasticity. New learned-delay prior art strengthens that reduction. | Fresh successor only; none currently registered. | Low |
| RV02 | Terminal construction negative; D1 identity consumed. | Fresh prospective object only. | Low |
| CX/CX01 | Candidate-002 terminal formal negative. | Fresh prospective object only. | Low |
| C19 | PRIMARY unresolved. Frozen protocol + adapter + synthetic/dev harness are green; no official outcome. Exact scientific executor/baseline bindings remain absent. | Prospectively bind exact implementation/package/runtime/resource semantics, source-only audit, then return for execution admission. | **Very high** |

Historical C19-v1 and C06 remain immutable historical evidence. C06 should be interpreted narrowly.

## MAIN lane — reserved PRIMARY frontier

**Target:** C19-v2 exact scientific implementation-binding supplement and machine-verifiable execution package.

**Scientific question:** Can the frozen 55-row C19-v2 experiment be instantiated with exact, fair, target-blind executors and baselines such that `I2+G0` versus `I2+G1` isolates integrative dynamics from representation privilege before any official data are opened?

**Owner:** MAIN. `main_owns_all_critical_path_fixups: true`.

**Branch/refs:** continue from `research/c19-truth-free-symbolic-adapter-v2-20260917@4ed49e9985b120486efba2db37e558848bc36fe0`; preserve frozen science anchor `90c936a7abca7eba0dac1f977753503551e73368`; planned identity remains `c19-external-v2-official-v1`.

**Execution allowed:** **false**.

### Allowed scope

MAIN may, in the same run:

- create a new prospective binding supplement without changing frozen C19 scientific semantics;
- bind every I0/I1/I2 × G0/G1 × E0 condition to an exact executable module/model/state/gate configuration;
- bind all five baseline families to exact factory/module, fixed hyperparameters, checkpoint/state initialization, seeds, runtime/dependency versions, resource/data/compute budgets, and no-fit/no-tune/no-select constraints;
- guarantee byte-identical I2 adapter representation for G0 and G1 so only preregistered gate/exchange dynamics differ;
- make source/package/input/runtime bindings machine-verifiable;
- add synthetic/static/source-only tests;
- fix all candidate-specific CI/verifier/package/binding defects that do not alter scientific semantics.

### Forbidden scope

- official Belief-R test/cache open/read/verify;
- STARTED/control creation, one-way dispatch, acquisition, scoring, preservation, identity consumption;
- changing protocol/result classes/metrics/thresholds/adapter/row inventory/seed plan/baseline families;
- choosing implementation details based on official-test behavior;
- silently rebinding `c19-external-v2-official-v1` if it was already bound to another exact package;
- post-outcome successor design.

### Identity rule

`c19-external-v2-official-v1` remains a **planned reservation**, not an execution-admitted identity. The binding supplement must verify whether this identity has ever been exact-bound to a package. If a conflicting prior exact binding exists, MAIN must STOP rather than silently rebind it; a fresh identity would require a subsequent prospective Analyst decision.

## SUB lane

`sub_lane: null`

`sub_fallback: null`

Reason: terminal-provenance v2 is already complete; A01 reopening would be post-outcome science; RV01/RV02/CX01 have no fresh prospective successors; C19 implementation binding is MAIN's critical path; #139 is governance and lower priority. Any C19 binding or harness fix assigned to SUB would improperly make MAIN depend on SUB.

SUB should deliberately no-op until a genuinely independent prospective package is reserved.

## Top 3

1. **MAIN — C19 exact implementation-binding supplement.** Information value: `VERY_HIGH_ENABLING`; distance: `NEAR_TO_MEDIUM`.
2. **MAIN — source-only privilege/reduction/fairness/runtime audit + machine-verifiable exact-head CI/manifest.** Information value: `HIGH_ENABLING`; distance: `NEAR_AFTER_1`.
3. **SUB — deliberate no-op.** Avoid false parallelism until a genuinely independent lane exists.

## #1 GO / STOP

### GO only if all remain true

- `4ed49e9985b120486efba2db37e558848bc36fe0` and `90c936a7abca7eba0dac1f977753503551e73368` re-fetch exactly;
- `c19-external-v2-official-v1` is fresh, unSTARTED, unconsumed, collision-free, and not already exact-bound to a conflicting package;
- condition/baseline implementation choices can be fixed source-only without official test behavior;
- I2 representation bytes are identical for G0 and G1, with only gate/exchange dynamics changing;
- all five baselines are fixed prospectively and receive fair representation/resources for the claims tested;
- source/protocol/package/input/runtime/resource bindings are machine-verifiable;
- STARTED/no-clobber/raw-before-score contracts remain unchanged;
- exact-head CI is green and package is reviewable.

### STOP immediately if any occurs

- implementation requires inspecting official test behavior;
- scientific semantics, protocol/result class/metric/threshold, row inventory, adapter, or identity must be redesigned;
- I2+G0 versus I2+G1 cannot be made a clean same-representation comparison;
- matched baselines cannot be instantiated within frozen resource rules;
- the planned identity is already bound to a different package;
- hash/ref/identity integrity drifts;
- source-only audit reduces G1 to G0 before START;
- or the exact binding package is complete/green/reviewable — this is a **normal stop** for fresh execution ADMIT/REJECT.

Scientific falsifiers/claim limiters are prospectively fixed: static G1≡G0 collapse rejects the integrative-dynamics interpretation; representation privilege limits claims to interface/representation benefit; matched recurrent/test-time-memory baselines matching/winning weaken SparkBrain-specific superiority.

## Prospective outcome / readiness contingencies

| Branch | Trigger | Same-run MAIN action | One-way execution |
|---|---|---|---|
| `PRE_START_BINDING_INCOMPLETE` | exact binding absent but source-only/science-preserving completion is possible | continue binding + synthetic/source-only tests | No |
| `PRE_START_BLOCKER` | CI/verifier/package/runtime defect with unchanged science | MAIN fixes it, re-fetches, reruns allowed checks, continues | No |
| `PRE_START_BINDING_INVALID` | fair instantiation requires scientific redesign, official-test knowledge, or same-I2 fairness is impossible | STOP; return exact reason to Analyst | No |
| `PRE_START_REDUNDANT` | source-only audit shows G1 statically equivalent to G0/no distinct mechanism beyond I2 | STOP; pre-START reduce; do not execute | No |
| `PRE_START_READY_FOR_EXECUTION_ADMISSION` | exact fair binding complete, machine-verifiable, green, no official data | STOP; return to Analyst for execution ADMIT/REJECT | No |
| `INVALID_EVIDENCE` | hash/ref/source/package drift or premature official access | STOP; read-only integrity reconciliation | No |
| `POST_START_FAILURE` | any STARTED/control appears under this no-execution handoff | STOP as integrity incident; no retry/rescore/rebind | No |
| PASS / FAIL / INCONCLUSIVE | unreachable because official execution is not admitted | none | No |

Only `PRE_START_BINDING_INCOMPLETE` and semantics-preserving `PRE_START_BLOCKER` may continue through the same MAIN run under this handoff.

## Blocked until

- C19 official STARTED/dispatch/acquisition/scoring/preservation: exact bindings complete + fresh Analyst execution admission.
- A01/RV01/RV02/CX01 successor science: a fresh prospective object independent of consumed outcomes.
- Tag/ruleset governance: its owning lane; it must not delay C19 science.

## Do not touch

Consumed/immutable or rejected objects remain out of bounds: A01 MD-001; P2 candidate-002; P3 candidate-001; Family-A P4 candidate-001; rejected Family-B/C Gen1 exact objects; RV01 R01-16/R01-17; RV02 D1 `96634541dc29b00be9f19b5819d45f541af69348ab6c1b0da6b48e943221700a`; CX01 Candidate-002/formal evidence; historical C19-v1/C06 evidence; legacy freeze/preserve/control/evidence refs; frozen C19 science anchor `90c936...`; and outcome-dependent successor design.

## Governance advisory

Open PRs remain 0; Issue #139 is the only open issue; Git tags and repository rulesets remain 0. Legacy freeze branches must remain exactly preserved. Protected annotated tags remain a future governance target, not a reason to interrupt C19.

Outcome-independent promotion candidates remain: generic terminal-provenance v2 after separate review, historical-binding verifier, no-clobber/source-manifest/raw-before-score helpers, and eventually a generic pre-START execution-package state machine after C19 stabilizes.

## Orchestrator handoff

**MAIN takes C19 exact scientific implementation binding and owns ALL critical-path fixes. SUB takes no lane and deliberately no-ops; there is no fallback. MAIN must not manufacture or absorb nonexistent SUB work, and SUB must not take any C19 blocker. Neither worker touches consumed identities, immutable evidence/legacy authorities, rejected A01 B/C exact objects, or frozen C19 protocol semantics.**

Repartition only if a fresh independent secondary object is prospectively defined/reserved, a new external/audit finding materially changes C19's expected information value, or source-only evidence invalidates/reduces the C19 binding before START.

MAIN may continue in the same run through `PRE_START_BINDING_INCOMPLETE` and science-preserving `PRE_START_BLOCKER`. It must stop at `PRE_START_BINDING_INVALID`, `PRE_START_REDUNDANT`, `PRE_START_READY_FOR_EXECUTION_ADMISSION`, `INVALID_EVIDENCE`, or any unexpected STARTED/post-START state.
