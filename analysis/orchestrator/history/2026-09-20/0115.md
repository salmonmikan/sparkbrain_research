# SparkBrain Evidence Analyst — 2026-09-20 01:15 JST

## Executive decision

No new FORMAL scientific evidence exists. Current `main` remains `ebed6abfa941c83b36d2e3bddd04e7c5fb0dbe9d`; the five authoritative `evidence/*` tags are unchanged; no `formal/*`, `sealed/*`, or tag-based `freeze/*` refs exist; thirteen legacy `freeze/*` branches remain preserved.

The material update is lower-funnel only. MAIN's prospectively fixed Temporal Expectation Batch-Partition Architecture Study completed successfully and returned `FUNCTIONAL_BATCH_PARTITION_EFFECT`. This is valid NON_EVIDENTIARY Architecture/engineering evidence: API call partition changes omission scheduling for an identical external timeline and, in the repetition family, that changed omission schedule propagates to downstream v0.4 output. It does **not** support a new computational principle or mechanistic distinctness. The current question is therefore closed after one Architecture cycle and moved to `HOLD`; there is no cycle-2 rescue and no automatic PRE_FORMAL path.

The newest SUB Discovery, `FIELD_SOURCE_LINEAGE_CAP_PERMUTATION_SENSITIVITY_DISCOVERY`, is `REJECT`: enqueue order changes only bounded retained source-ID provenance and full serialized state hash; physical spike observables and physical unit state are unchanged, and current code has no production consumer by which source IDs change dynamics.

## New repository / lower-funnel evidence

### MAIN — Temporal batch partition Architecture cycle 1

- workflow: `35451528895`, `completed/success`
- research head: `7fa4391bbf34cf25e10b708ce64acddf07bf7f42`
- artifact: `temporal-batch-partition-cycle1` (`10587410697`)
- contract digest: `681c21e31c686be7d8beb10e927ef65510ce82689c2cc81ae5fd9fedc179c06d`
- fixed classification: `FUNCTIONAL_BATCH_PARTITION_EFFECT`
- `schedule_divergence=true`; `functional_divergence=true`

Repetition timeline: `WHOLE_BATCH` emitted no omission pulses, while `MIDPOINT_TWO_BATCH` and `EVENT_TIME_CAUSAL` emitted `A@33ms` and `A@43ms`. Replaying the resulting pulse sets on the fixed downstream v0.4 surface changed activation/probability (`WHOLE_BATCH` activation A `0.0261947583`, probability `0.4722445744`; split/causal activation A `0.0198495954`, probability `0.4699388023`). Action ignition remained true.

Noisy-motif timeline: `EVENT_TIME_CAUSAL` emitted two omissions (`A@84.5ms`, `A@119.5ms`) while whole/two-batch emitted none, but the final fixed replay outputs were identical across arms. Therefore the effect is real but task/timeline dependent.

Claim separation:
- new computational principle: **unsupported**;
- mechanistic distinctness: **unsupported**;
- architecture/system-integration value: **supported**;
- engineering/reproducibility value: **high**;
- research/testbed value: **moderate-high**.

Fresh Literature independently strengthens the ordinary reduction: discrete-event scheduling, batching semantics, and online-vs-batch stream processing explain why observing a batch before polling deadlines can make behavior call-partition dependent. The appropriate engineering direction is canonical event-time semantics invariant to API batching, not a novelty claim.

### SUB Discovery review

Latest SUB mode is `discovery`; target `FIELD_SOURCE_LINEAGE_CAP_PERMUTATION_SENSITIVITY_DISCOVERY`; review classification: **`REJECT`**.

Twenty simultaneous arrivals into one field unit with `max_sources_per_unit=16` produced identical spikes and physical state under ascending vs descending enqueue order, but retained different source-ID sets (`p04..p19` vs `p15..p00`, set Jaccard `0.6`) and therefore different full state hashes. `_source_tuple` exactly explains this by retaining the last 16 unseen IDs. Current production search found no source-ID consumer that changes physical dynamics. Keep only an engineering note about provenance/hash reproducibility; no cycle 2.

## External inputs

### Literature

Changed this run. Temporal batch partition sensitivity should first be reduced to ordinary discrete-event scheduling/batch semantics and online-vs-batch processing. This affects `CAND-TEMPORAL-BATCH-PARTITION-01`: it strengthens Architecture/engineering relevance while lowering any mechanistic-novelty interpretation. Allocation effect: close the completed Architecture question at `HOLD`; do not promote to PRE_FORMAL.

### Independent Audit

No new Audit after H5. H5 remains consumed and canonical `FAIL_NO_USEFUL_WORK_REDUCTION`; one-way integrity remains `ROBUST_SO_FAR`. The claim remains narrow: the exact registered aggregate-work implementation failed its gate, while eager all-edge eligibility maintenance dominates work, so this does not establish that event routing/lazy state can never reduce compute.

### Methodology Calibration

Latest classification remains `WELL_CALIBRATED`. Keep the hard FORMAL integrity floor, four-layer funnel, claim-type separation, stop-after-cycle review, and prospective support-aware Architecture criteria. Completed lower-layer outcomes must not be retrofitted into novelty/formal gates.

### Repository Steward

Consumed only as governance advisory. Its latest available designated state continues to report five evidence tags, thirteen legacy freeze branches, zero repository rulesets, unprotected `main`, and PR #148/#149 as governance/review maintenance rather than scientific blockers. Material facts were independently rechecked against current remote state.

## Research-layer state

| Layer | State | Owner / rule |
|---|---|---|
| `DISCOVERY` | `OPEN` | SUB default; synthetic/dev/read-only only; NON_EVIDENTIARY |
| `ARCHITECTURE_STUDY` | `HOLD_NO_ACTIVE_MAIN_OBJECT` | Temporal cycle complete; no automatic successor |
| `PRE_FORMAL` | `EMPTY_HOLD` | no fresh mechanistic/reduction object with sufficient lower-layer support |
| `FORMAL` | `EMPTY_HOLD` | no fresh identity / STARTED / TEST / scorer / preserve / evidence authority |

## Candidate pool

1. `CAND-TEMPORAL-BATCH-PARTITION-01` — **HOLD**, target `ARCHITECTURE_STUDY`, cycles `DISCOVERY=1, ARCHITECTURE=1`. Question resolved positively at Architecture/API level but strongly ordinarily reducible. Fresh future work is allowed only as a newly specified API/event-time-invariance or downstream-reliability question; not as cycle-2 rescue.
2. `CAND-TOPK-PA-01` — **HOLD**, target `ARCHITECTURE_STUDY`, cycles `DISCOVERY=1, ARCHITECTURE=2, UTILITY_READ_ONLY=1`. Narrow high-magnitude signal exists but breadth/generalization remain mixed. No cycle 3; any successor must be a fresh branch/basin/hysteresis/recurrent-transient reduction object.
3. `CAND-H7-RESP-01` — **HOLD**, target `PRE_FORMAL`. Very high value only if a genuinely native online/local responsibility-sensitive mechanism independently appears. Do not construct one to rescue LP01 or another consumed negative.
4. `CAND-FIELD-SOURCE-LINEAGE-CAP-01` — **REJECT**, target `DISCOVERY`, cycle `1/3`. Exact provenance-buffer/bookkeeping reduction; physical dynamics invariant.

Closed Assembly/Structural/Reward/Receptor candidates remain archival and are not reopened.

## MAIN / SUB allocation

`main_lane = LOWER_FUNNEL_MAIN_HOLD_PENDING_FRESH_OBJECT`.

MAIN owns every future central Architecture/PRE_FORMAL/FORMAL object's complete critical path once prospectively authorized: implementation, candidate-specific bugs, harness/runner/verifier, bindings, CI/preflight, review fixes, preservation/scoring/execution blockers, and exact-head integration. MAIN must not manufacture a Temporal cycle 2, Top-k cycle 3, H7 queue-filler, or absorb SUB Discovery merely for utilization.

`sub_lane = BOUNDED_SECONDARY_DISCOVERY`.

SUB may perform at most one genuinely independent synthetic/development/read-only Discovery question per run. It must not take MAIN blockers, Temporal successor construction, Top-k follow-up, H7 construction, consumed-line rescue, official TEST, formal raw/scoring, STARTED/control, preserve/evidence, or immutable refs.

`sub_fallback = NO_OP_WITH_OBSERVABLE_LEVEL_DUPLICATION_OR_LOW_VALUE_REASON`.

## Blocked until

- FORMAL: a fresh high-value object exists with fresh identity and complete prospective source/package/runtime/input/protocol/comparator/scorer/preserver binding plus STARTED/no-clobber and raw-before-score authority.
- PRE_FORMAL: a fresh mechanistic/reduction question with enough lower-layer support exists.
- Temporal: a **new** API/event-time invariance or reliability question is prospectively specified; current result cannot be retuned into a successor.
- Top-k: a fresh ordinary-reduction Architecture object is justified; same-object cycle 3 remains prohibited.
- H7: a native responsibility-sensitive mechanism arises independently.

## Top 3

1. **Fresh-object admission / MAIN HOLD** — `VERY_HIGH_IF_OBJECT_APPEARS`, distance `EVENT_DEPENDENT`. Do not manufacture activity; screen new lower-funnel observations for an independently valuable central Architecture/PRE_FORMAL object.
2. **Existing Temporal batching callsite audit request** `SUB-20260919-2344-TEMPORAL-EXPECTATION-BATCHING-CALLSITE-AUDIT` — `MEDIUM_HIGH / VERY_NEAR`, read-only and nonblocking. The request exists; no duplicate Utility request is created here.
3. **SUB bounded Discovery** — `MEDIUM / OPTIONAL`; one independent question or explicit reasoned no-op.

## #1 GO / STOP

**GO only for prospective admission/specification.** There is no current MAIN execution object and no FORMAL GO.

A future central object may proceed only if it is fresh, independent of consumed-result rescue, bound prospectively to exact source/package/runtime/input/protocol/comparator/resources/metrics, and has fixed falsifiers before outcome-bearing data. FORMAL additionally requires fresh collision-free identity, dedicated pre-START review, exactly-once STARTED/no-clobber before official TEST, target/scoring-blind raw, immutable preserve before read/score, independent refetch/digest/cardinality checks, fail-closed joins/invariants, and preregistered scoring.

**STOP** if admission requires looking at a held-out/formal outcome to choose candidate, comparator, metric, threshold, resource contract, runtime/model, or identity; if it is merely rescue of a consumed/closed line; or if no nonduplicative information-bearing question exists.

## Prospective contingency tree

`LOWER_FUNNEL_MAIN_HOLD_PENDING_FRESH_OBJECT`

- `NO_FRESH_CENTRAL_OBJECT` -> MAIN HOLD; SUB may independently Discover.
- `FRESH_SUB_DISCOVERY` -> fresh Analyst review only; promotion requires a new prospective definition.
- `TEMPORAL_CALLSITE_AUDIT`:
  - broad repository-supported call-partition dependence -> may motivate a **fresh** API-invariance Architecture object; never automatic PRE_FORMAL;
  - narrow/local dependence -> document engineering constraint and HOLD/close;
  - missing safe inputs -> HOLD; do not regenerate scientific evidence.
- `FRESH_CENTRAL_ARCH_OR_PRE_FORMAL_OBJECT`:
  - pre-start mechanical blocker -> MAIN may fix outcome-independently and recheck exact-head CI;
  - semantic/protocol gap -> STOP;
  - invalid/leaky input -> discard + STOP;
  - any valid outcome -> STOP for fresh Analyst review.
- FORMAL `PASS / FAIL / INCONCLUSIVE / INVALID_EVIDENCE / PRE_START_BLOCKER / POST_START_FAILURE` remain **UNARMED_NO_FORMAL_OBJECT**.

## Do not touch / consumed identities

Do not rerun, retune, rescore, relabel, or post-outcome repair:
- `c19-external-v2-official-v4`
- `c19-r1-revision-authority-official-v1`
- `c19-r1-revision-authority-official-v2`
- `c19-r2-fsa-state-tracker-official-v1`
- `pd01-long-history-fading-memory-official-v1`
- `ni01-no-ignition-selective-prediction-official-v1`
- `h5-event-routing-work-reduction-official-v1`

Neither worker may mutate immutable evidence/freeze/preserve refs, access official TEST without fresh formal authority, merge research PRs, or change scheduler definitions.

## Governance advisory

Issue #139 remains open and current for the five evidence tags/protection gap. Repository rulesets remain `0`; `main` remains unprotected. PR #148 and #149 remain open/unmerged and are governance/review work, not scientific critical-path blockers. No governance mutation is performed here.

## Utility

No new Utility request created. Existing `SUB-20260919-2344-TEMPORAL-EXPECTATION-BATCHING-CALLSITE-AUDIT` remains the relevant bounded read-only proposal; duplicating it would add no information value.

## Orchestrator handoff

MAIN: hold pending a genuinely fresh central lower-funnel object; when one is admitted, MAIN owns **all** of its critical-path fixes. SUB: continue only independent bounded NON_EVIDENTIARY Discovery, or explicit no-op. MAIN must not absorb SUB work merely to stay busy; SUB must not take MAIN blockers. Neither touches consumed identities, official TEST/formal scoring surfaces, immutable refs, research merges, or scheduler definitions. Repartition only if fresh evidence/Discovery/Literature/Audit/Methodology materially changes expected information gain or establishes a prospectively specifiable independent object.
