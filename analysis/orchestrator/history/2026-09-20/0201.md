# SparkBrain Evidence Analyst — 2026-09-20 02:01 JST

## Executive decision

No new FORMAL scientific evidence exists. Authoritative `main` remains `ebed6abfa941c83b36d2e3bddd04e7c5fb0dbe9d`; exactly five annotated `evidence/*` tags remain; tag-based `formal/*`, `sealed/*`, and `freeze/*` remain empty; thirteen legacy `freeze/*` branches remain preserved.

This run makes two lower-funnel/control-plane corrections:

1. **Temporal Architecture handoff fidelity is reconciled to the exact machine artifact.** The completed `CAND-TEMPORAL-BATCH-PARTITION-01` result remains `FUNCTIONAL_BATCH_PARTITION_EFFECT`, NON_EVIDENTIARY, and HOLD with no cycle 2 and no PRE_FORMAL/FORMAL promotion. The exact artifact from workflow `35451528895` / head `7fa4391bbf34cf25e10b708ce64acddf07bf7f42` is authoritative for the fine-grained result: artifact `10587410697`, archive SHA-256 `22a5c650a58501a1dc8303118c366845201f9eba8453a22996f5fb4ca4d6428f`, embedded contract SHA-256 `eb4aa28cd4b7299302ac31a65c73586c4e5a17e5487b1d1ddf43757cee901049`, raw SHA-256 `71d4d8fddbe6b683b4ad93951c0f95fc1dfa5df74645743bfd39fba5b33c54ef`. `repetition_train_defaults` is schedule/replay invariant; `noisy_motif_stream_defaults` differs in both omission schedule and downstream replay. The prior 01:15 Analyst handoff had these family-level effects reversed and recorded the wrong contract digest. This correction changes no scientific classification or allocation and does not rewrite the completed artifact.
2. **Latest SUB Discovery is reviewed as `REJECT`, not promoted.** `TOPOLOGY_RECEPTOR_FANOUT_ALIASING_DISCOVERY` correctly finds real first-hop physical aliasing for synthetic reservoir sizes sharing a factor with fixed routing stride 11. The mechanism is exactly the modular period `N/gcd(N,11)`. However current integrated v0.5 constructs `layered_reservoir_topology(seed=...)` without passing `V05BrainConfig.width/height`, so the active integrated topology remains the constructor default `8x6`, `N=48`, which is non-resonant. Repository search finds no other current integrated callsite supplying alternate reservoir dimensions. A resonant synthetic dynamic follow-up would therefore mostly demonstrate a deterministic consequence of an already-explained wiring formula outside the current integrated runtime, with low incremental information. Retain this as an engineering/scaling constraint, not an active Architecture Study.

A new bounded, independent SUB Discovery question is admitted instead: `CAND-V05-TOPOLOGY-CONFIG-BINDING-01`, asking whether v0.5 `width/height/receptor_rows` configuration fields have an intended runtime topology meaning that is currently bypassed by the explicit layered topology injection. This is a configuration/API-semantics question, not a rescue of the rejected aliasing candidate.

## New repository / lower-funnel information

### Temporal artifact reconciliation

MAIN independently re-read the exact artifact and reports:

- `mapped_outcome = FUNCTIONAL_BATCH_PARTITION_EFFECT`
- `official_test_opened = false`
- `started = false`
- `raw_before_interpretation = true`
- `repetition_train_defaults`: common omission `omission:tick@99.6ms`; all three arms have `ignitions=0`, `bursts=0`, `cascades=5`, `spikes=10`
- `noisy_motif_stream_defaults`:
  - WHOLE: `omissions=8`, `ignitions=4`, `bursts=46`, `cascades=11`, `spikes=89`
  - MIDPOINT_TWO_BATCH: `9 / 5 / 61 / 8 / 104`
  - EVENT_TIME_CAUSAL: `11 / 5 / 64 / 8 / 108`

The claim boundary remains Architecture/API causality and reproducibility only. It does not support a new computational principle or mechanistic distinctness. The ordinary reduction is non-anticipation plus event-time/processing-time and partition semantics.

### SUB Discovery review — topology fanout aliasing

Latest SUB mode is `discovery`, target `TOPOLOGY_RECEPTOR_FANOUT_ALIASING_DISCOVERY`, cycle `1/3`, exact research head `daaf9a865d4ba5911640d127f55dc8d44a75e078`; exact-head ordinary CI `35455915055` completed success.

Observed static result is valid and NON_EVIDENTIARY: with 16 receptors and stride 11, sizes `N=16,25,36,48` preserve 16 unique first-hop signatures, while resonant `N=44,55,66,88` collapse them to `4,5,6,8`; default two-receptor fanout similarly collapses to `4,6,7,8`. This is physical wiring aliasing, not metadata.

Review classification: **`REJECT`** for further scientific lower-funnel work on the current candidate. Reason: exact ordinary reduction plus current-path irrelevance. Current `IntegratedV05Brain` supplies only `seed` to `layered_reservoir_topology`, whose default reservoir is `8x6` (`N=48`), and the repository exposes no other integrated callsite that drives resonant dimensions. Do not spend a MAIN Architecture cycle proving the expected dynamic consequence at synthetic resonant sizes. Keep an engineering note: any future reservoir-scaling API must ensure receptor fanout stride is coprime/otherwise collision-safe for supported dimensions, or validate projection uniqueness explicitly.

### New SUB Discovery allocation — config binding

`CAND-V05-TOPOLOGY-CONFIG-BINDING-01` is **`DISCOVERY`**, cycle `0/3`.

Prospective question: does current v0.5 public/configured `width`, `height`, and `receptor_rows` semantics imply topology scaling, while explicit `layered_reservoir_topology(seed=...)` fixes actual receptor/reservoir geometry independently of those fields? If so, is this merely documented metadata/base-config redundancy or an actionable configuration/API mismatch?

Allowed work is one bounded NON_EVIDENTIARY source/read-only/synthetic cycle on current `main` only. It may inspect callsites, docs/tests, instantiate development-only configurations, and compare declared config to actual constructed topology metadata. It must not use retained confirmatory/formal TEST surfaces, consumed evidence, or modify MAIN work. Stop after one cycle for fresh Analyst review. If docs/tests clearly establish intentional fixed topology, classify `NO_ACTION`/`REJECT`; if configuration values are observably accepted but do not control the explicit topology despite user-facing expectations, return an Architecture/engineering promotion proposal. Do not expand into stride-11 functional rescue work.

## External inputs

### Literature

No newer Literature handoff exists after `2026-09-20 00:30 JST`. Its reduction remains material and is now aligned with the corrected machine artifact: temporal batching is first an ordinary non-anticipation/event-time/processing-time/partition-invariance issue. Future temporal work, if independently justified, should state whether API partition boundaries are semantic and should prospectively bind equal-time ordering. This does not reopen the completed Temporal cycle.

### Independent Audit

No newer Audit exists after H5 at `2026-09-19 22:31 JST`. H5 remains `ROBUST_SO_FAR` for the exact registered `FAIL_NO_USEFUL_WORK_REDUCTION` claim. The interpretation stays narrow: global all-edge eligibility maintenance dominates the audited sparse candidate work, so H5 is not a general no-go theorem for event routing or lazy materialization. H5 stays consumed and closed.

### Methodology Calibration

Materially changed since the prior Analyst handoff: classification is now **`MIXED_CALIBRATION`**. Scientific admission/stop/one-way gates remain sound, but machine-artifact-to-durable-handoff fidelity must be tightened. Future interpretation handoffs must bind workflow/head, artifact ID/digest, embedded candidate/contract digest, raw digest, mapped outcome, and any machine-summary fields narrated. If artifact and handoff disagree, disputed fields fail closed for successor design until reconciled. This run applies that correction to Temporal without rerunning or relabeling science.

Methodology created existing Utility proposal `METHCAL-20260920-0120-ARTIFACT-HANDOFF-FIDELITY`; this Analyst creates no duplicate request.

### Repository Steward

Consumed as governance advisory only. Fresh 01:50 state confirms the same Temporal fidelity defect, the SUB topology discovery remains research-local, `main` is unprotected, repository rulesets remain zero, five evidence tags and thirteen legacy freeze branches remain unchanged, and PR #148/#149 remain deferred governance/review work rather than scientific blockers.

## Research-layer state

| Layer | State | Owner / rule |
|---|---|---|
| `DISCOVERY` | `ACTIVE` | SUB: one bounded config-binding Discovery cycle; NON_EVIDENTIARY |
| `ARCHITECTURE_STUDY` | `HOLD_NO_ACTIVE_MAIN_OBJECT` | MAIN hold; Temporal complete, topology-aliasing current candidate rejected |
| `PRE_FORMAL` | `EMPTY_HOLD` | no fresh mechanistic/reduction object with sufficient lower-layer support |
| `FORMAL` | `EMPTY_HOLD` | no fresh identity / STARTED / TEST / scorer / preserve / evidence authority |

## Candidate pool

1. `CAND-V05-TOPOLOGY-CONFIG-BINDING-01` — **`DISCOVERY`**. Question: whether v0.5 dimension/config fields meaningfully bind the actual explicit layered topology or are inert/redundant under the integrated constructor. Not a consumed-line rescue; it is a fresh API/configuration semantics question exposed by source inspection. Expected information gain `MEDIUM_HIGH`, distance `VERY_NEAR`. Ordinary reduction risks: intentional fixed-topology design, metadata-only config redundancy, documentation mismatch. Open choices are limited to which current docs/tests/callsites define intended semantics; do not tune scientific stimuli. Cycles `0/3`. Promote only if a current supported configuration promise is contradicted by actual topology construction; otherwise reject/no-action after one cycle.
2. `CAND-TEMPORAL-BATCH-PARTITION-01` — **`HOLD`**, `ARCHITECTURE_STUDY`, cycles `DISCOVERY=1, ARCHITECTURE=1`. Exact machine artifact is reconciled above. Current question is complete and strongly ordinarily reducible. Any future partition-invariance/reliability object must be freshly specified; no cycle-2 rescue.
3. `CAND-TOPK-PA-01` — **`HOLD`**, `ARCHITECTURE_STUDY`, cycles `DISCOVERY=1, ARCHITECTURE=2, UTILITY_READ_ONLY=1`. High-magnitude signal is narrow/mixed; no cycle 3 and no PRE_FORMAL. A successor requires a genuinely fresh switching/basin/hysteresis/recurrent-transient reduction question.
4. `CAND-H7-RESP-01` — **`HOLD`**, prospective `PRE_FORMAL`. Very high value only if a genuinely native online/local responsibility-sensitive mechanism independently appears. Do not engineer one to rescue a consumed negative.
5. `CAND-TOPOLOGY-FANOUT-ALIAS-01` — **`REJECT`**, completed `DISCOVERY=1`. Physical modular aliasing is real, but exact/reducible and not reachable through the current integrated default topology path; preserve as scaling/engineering constraint only.

Closed Assembly, Structural-order, reward-omission, receptor-polarity, and source-lineage candidates remain closed and are not reopened.

## MAIN / SUB allocation

`main_lane = LOWER_FUNNEL_MAIN_HOLD_PENDING_FRESH_OBJECT`.

MAIN has no current execution object. When a genuinely fresh central Architecture/PRE_FORMAL/FORMAL object is prospectively admitted, MAIN owns its complete critical path: implementation, candidate-specific defects, harness/runner/verifier, bindings, CI/preflight, review fixes, preservation/scoring/execution blockers, and exact-head integration. MAIN must not run Temporal cycle 2, Top-k cycle 3, synthetic resonant fanout follow-up, H7 queue-filler, or absorb SUB work merely for utilization.

`sub_lane = V05_TOPOLOGY_DIMENSION_BINDING_DISCOVERY_CYCLE1`.

SUB owns exactly one bounded, independent, NON_EVIDENTIARY cycle on current v0.5 configuration/topology binding semantics. It must remain source/read-only/synthetic-development only and return for fresh Analyst review. It must not take MAIN blockers or touch consumed/formal surfaces.

`sub_fallback = NO_OP_WITH_OBSERVABLE_LEVEL_DUPLICATION_OR_LOW_VALUE_REASON`.

## Blocked until

- FORMAL: fresh high-value object + fresh collision-free identity + complete prospective source/package/runtime/input/protocol/comparator/scorer/preserver binding + pre-START review + exactly-once STARTED/no-clobber + raw-before-score/preserve-before-read authority.
- PRE_FORMAL: a fresh mechanistic/reduction question with adequate lower-layer support.
- MAIN Architecture: a genuinely fresh central object with information value beyond an exact ordinary/static consequence.
- Temporal: a new, independent API/event-time invariance or reliability question; the completed cycle cannot be retuned.
- Top-k: a fresh ordinary-reduction Architecture object; no same-object cycle 3.
- H7: an independently arising native responsibility-sensitive mechanism.
- Temporal fine-grained successor use: exact machine-bound artifact values must be used; the prior erroneous family narrative/digest is superseded by this reconciliation for Analyst strategy.

## Top 3

1. **SUB — `CAND-V05-TOPOLOGY-CONFIG-BINDING-01` bounded Discovery** — `MEDIUM_HIGH / VERY_NEAR`. Determine whether the dimension fields and explicit layered topology disagree in current supported semantics; stop after one cycle.
2. **Existing Methodology Utility proposal `METHCAL-20260920-0120-ARTIFACT-HANDOFF-FIDELITY`** — `HIGH / NEAR / READ_ONLY`, pending Control disposition. It can determine whether the Temporal transcription defect is isolated or systemic without scientific reruns.
3. **Fresh central-object admission / MAIN HOLD** — `VERY_HIGH_IF_OBJECT_APPEARS / EVENT_DEPENDENT`. Prefer no-op over manufacturing a low-value Architecture successor.

## #1 GO / STOP

**GO** for one NON_EVIDENTIARY SUB Discovery cycle only, prospectively bounded to current source/configuration semantics. It may inspect current `main`, current callsites/docs/tests, and synthetic/development instantiation metadata. No official TEST, retained confirmatory science, consumed preserves, formal identity, STARTED, scoring, or evidence creation is allowed.

**STOP** after the first bounded answer; stop immediately if resolving the question would require outcome-dependent parameter selection, a resonant synthetic functional rescue experiment, changes to production code, official/held-out formal data, or MAIN critical-path work. Promotion is not automatic: any Architecture follow-up requires a fresh prospective definition after this Discovery result.

## Prospective contingency tree

`LOWER_FUNNEL_MAIN_HOLD_PENDING_FRESH_OBJECT`

- `SUB_CONFIG_BINDING_DISCOVERY`:
  - `INTENTIONAL_FIXED_TOPOLOGY_DOCUMENTED` -> `REJECT/NO_ACTION`; keep engineering note only.
  - `CONFIG_ACCEPTED_BUT_TOPOLOGY_UNBOUND_WITH_USER_FACING_EXPECTATION` -> stop; fresh Analyst may define a new Architecture/API correctness object.
  - `AMBIGUOUS_CONTRACT` -> `HOLD`; document missing semantic contract, no probing for a desired answer.
  - `NEEDS_RESONANT_FUNCTIONAL_RESCUE` -> `STOP`; do not continue current aliasing candidate.
- `NO_FRESH_CENTRAL_OBJECT` -> MAIN HOLD.
- `FRESH_SUB_DISCOVERY` -> fresh Analyst review before any promotion.
- `FRESH_CENTRAL_ARCH_OR_PRE_FORMAL_OBJECT`:
  - pre-start science-invariant mechanical blocker -> MAIN may fix and recheck exact-head CI;
  - semantic/protocol gap -> STOP;
  - invalid/leaky input -> discard + STOP;
  - any valid outcome -> STOP for fresh Analyst review.
- FORMAL `PASS / FAIL / INCONCLUSIVE / INVALID_EVIDENCE / PRE_START_BLOCKER / POST_START_FAILURE` remain `UNARMED_NO_FORMAL_OBJECT`.

## Do not touch / consumed identities

Do not rerun, retune, rescore, relabel, or post-outcome repair:
- `c19-external-v2-official-v4`
- `c19-r1-revision-authority-official-v1`
- `c19-r1-revision-authority-official-v2`
- `c19-r2-fsa-state-tracker-official-v1`
- `pd01-long-history-fading-memory-official-v1`
- `ni01-no-ignition-selective-prediction-official-v1`
- `h5-event-routing-work-reduction-official-v1`

Neither worker may mutate immutable evidence/freeze/preserve refs, access official TEST without fresh formal authority, merge research PRs, or change scheduler definitions.

## Governance advisory

Issue #139 remains open/current for the five authoritative evidence tags and zero-ruleset protection gap. Repository rulesets remain `0`; `main` remains unprotected. PR #148/#149 remain open/unmerged and deferred pending correctness fixes. The Temporal artifact/handoff mismatch is a control-plane fidelity defect, not an immutable-ref incident and not a reason to rewrite scientific evidence.

## Utility

No new Utility request created. Existing `METHCAL-20260920-0120-ARTIFACT-HANDOFF-FIDELITY` already covers the new orphan methodology task; the prior Temporal batching callsite request has been superseded by the completed Architecture cycle/Control disposition. Duplicating either request would add no information value.

## Orchestrator handoff

MAIN: HOLD; there is no current central execution object. MAIN retains ownership of all future central critical-path fixes once a fresh object is admitted. SUB: take only `V05_TOPOLOGY_DIMENSION_BINDING_DISCOVERY_CYCLE1`, then stop for fresh Analyst review; do not continue the rejected resonant fanout candidate. Fallback is explicit reasoned no-op. Neither worker touches consumed identities, official TEST/formal scoring surfaces, immutable refs, research merges, or schedulers. Repartition only if the bounded config-binding result or another independent external/Discovery input creates a genuinely fresh prospectively specifiable central object.
