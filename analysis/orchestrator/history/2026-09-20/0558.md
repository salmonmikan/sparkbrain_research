# SparkBrain Evidence Analyst — Latest

Timestamp: 2026-09-20 05:58 JST  
Role: `EVIDENCE_ANALYST`  
Scientific execution authority: `READ_ONLY`  
Parent Evidence Analyst handoff: `d6b14d826a0847e439dfedd86e363786d84e329a`

## Executive decision

There is **new lower-funnel repository evidence but no new FORMAL evidence**.

The completed MAIN Architecture Study `CAND-ASSEMBLY-CROSSCASCADE-FALLBACK-01` reached the prospectively mapped terminal `FUNCTIONAL_CROSSCASCADE_SEGMENTATION_EFFECT`. The exact workflow completed successfully on research head `388799d9880d7b472417a680f288da13f4fd9b41`; the downloaded artifact binds workflow `35468475903`, artifact `10591704496`, archive SHA-256 `5abb9a4b12673f1ef0c50c3dcaf9b6b6f3d4ba20270f777789dc9893ea140ac5`, contract SHA-256 `902d04379f3d89df27cee490e2ba2988d0bc7b695cf951b0dcf471e2a96d64dd`, input-family digest `180ba148660e9283e4c10b68a35d8b008c185c4a5806e22f90a3c6c8068a3f5c`, and raw SHA-256 `96b5164dd8c43ea6dc94654308a15bcf35caca6b69df8f0acf38b8dd81e908d9`.

In the matched joined control, production and the boundary-preserving shadow each produce one `[101,102]` pattern, one candidate, mature activation, and prediction `target-A`. In the split-sparse condition, production's episode-global fallback merges the two cascades into one `source_cascade_id=null` pattern, forms a mature assembly, and predicts `target-A`; the boundary-preserving shadow produces no pattern, no candidate, no mature activation, and no prediction. All bound source blobs verify, and the machine decision facts are `representation_effect=true`, `assembly_effect=true`, `functional_effect=true`, `joined_control_match=true`.

This is a real **architecture/system-integration and testbed effect**, but it is ordinarily reduced to the segmentation/fallback policy. It is **not** support for a new computational principle or mechanistic distinctness. The current Assembly question is complete after one Discovery and one Architecture cycle. `CAND-ASSEMBLY-CROSSCASCADE-FALLBACK-01` is therefore moved to `HOLD`; no same-object cycle 2 is authorized.

The latest SUB Discovery `REFRACTORY_INHIBITION_CANCELLATION_DISCOVERY_CYCLE1` is classified `PROMOTE_TO_ARCHITECTURE_STUDY`. The exact research head is `7b1dff2b1b3677c4af37969364b74d119bd942a8`. Its synthetic result is completely ordinary-reduced to current `_deliver_group`: same-time positive and negative currents are summed to `net_current`, then during refractory the implementation applies `min(0.0, net_current)`. Therefore a positive arrival can cancel simultaneous inhibition before the refractory clamp, despite the adjacent source comment saying positive drive is ignored, and that retained membrane difference can alter a fixed post-refractory spike outcome. The exploratory result remains strictly `NON_EVIDENTIARY`.

A fresh candidate `CAND-REFRACTORY-CURRENT-ACCOUNTING-01` is admitted at `ARCHITECTURE_STUDY` and becomes the new MAIN lane. This is an API/state-semantics characterization question, not a novelty claim and not a rescue of a consumed line.

## Authoritative repository state

- stable `main`: `ebed6abfa941c83b36d2e3bddd04e7c5fb0dbe9d`
- authoritative annotated `evidence/*` tags: `5`
- tag-based `formal/*`: `0`
- tag-based `sealed/*`: `0`
- tag-based `freeze/*`: `0`
- legacy `freeze/*` branches: `13`
- repository rulesets: `0`
- Issue `#139`: open; still tracks five evidence tags and server-side protection gap
- PR `#148`: open/unmerged
- PR `#149`: open/unmerged

No consumed identity, STARTED/control ref, raw preserve ref, or authoritative evidence tag changed.

## Research funnel

| Layer | State | Owner | Meaning |
| --- | --- | --- | --- |
| `DISCOVERY` | `OPEN` | SUB | Independent synthetic/dev/read-only questions only; strictly NON_EVIDENTIARY |
| `ARCHITECTURE_STUDY` | `ACTIVE_PENDING_MAIN_PROSPECTIVE_BINDING` | MAIN | `CAND-REFRACTORY-CURRENT-ACCOUNTING-01` |
| `PRE_FORMAL` | `EMPTY_HOLD` | MAIN | No fresh supported mechanistic/reduction object |
| `FORMAL` | `EMPTY_HOLD` | MAIN | No fresh one-way object/identity/authority |

`NO_HIGH_VALUE_FORMAL_OBJECT` does not imply programme-wide inactivity. The new Architecture question has real information value, while PRE_FORMAL and FORMAL remain correctly empty.

## Claim-type separation

For the completed Assembly result:
- new computational principle: **no support**
- mechanistic distinctness: **no support; ordinary fallback/segmentation semantics**
- architecture/system integration value: **positive**
- engineering/API/reproducibility value: **positive**
- research/testbed value: **positive**

For the Refractory Discovery:
- new computational principle: **no support**
- mechanistic distinctness: **no support; ordinary signed-current aggregation and refractory-clamp semantics**
- architecture/API state semantics: **worth fresh characterization**
- engineering/testbed value: **potentially positive**
- formal evidentiary value: **none**

## Candidate pool

### `CAND-REFRACTORY-CURRENT-ACCOUNTING-01` — `ARCHITECTURE_STUDY`
- question / phenomenon: during absolute refractory, do same-time positive arrivals cancel inhibitory current before the clamp, and can that accounting choice change one fixed post-refractory spike observable relative to a positive-ignore shadow comparator?
- not a consumed-line rescue: fresh field-state/API semantics from synthetic development-only Discovery.
- target layer: `ARCHITECTURE_STUDY`.
- expected information gain: `HIGH_ARCHITECTURE_API_SEMANTICS_LOW_NOVELTY`.
- ordinary reductions: signed-current aggregation; refractory clamp semantics; comment/implementation contract mismatch; retained membrane state.
- implementation distance: `NEAR`.
- open choices: intended meaning of refractory; whether canonical docs/tests define netting; whether the state difference reaches the fixed downstream observable.
- exploration count: Discovery `1`, Architecture `0`.
- condition: one fresh prospectively bound Architecture cycle; every valid terminal STOPs for fresh review, with no automatic PRE_FORMAL/FORMAL.

### `CAND-ASSEMBLY-CROSSCASCADE-FALLBACK-01` — `HOLD`
- question: does episode-global fallback across separate sparse cascades change assemblies and downstream output relative to boundary-preserving extraction?
- not rescue: fresh segmentation/testbed line, distinct from the rejected assembly prototype-order line.
- target layer: `ARCHITECTURE_STUDY`.
- expected information gain: current question resolved with positive functional Architecture effect.
- ordinary reductions: segmentation policy, episode-global fallback, minimum-spike gating, cascade-provenance erasure.
- implementation distance: current object complete.
- open choices: only a **fresh** prevalence/reliability/supported-contract successor, if independently justified.
- exploration count: Discovery `1`, Architecture `1`.
- condition: HOLD; no same-object cycle 2.

### `CAND-V05-UNIT-SUPPRESSION-TRANSIENT-SEMANTICS-01` — `HOLD`
- question: explicit state-preserving vs state-neutral vs silent/ambiguous transient suppress/clear semantics.
- not rescue: fresh API/testbed line; scored persistent causal-ablation path excluded.
- target layer: `ARCHITECTURE_STUDY`.
- expected information gain: currently blocked by machine-fact extractor fidelity.
- ordinary reductions: threshold-only blockade, retained state, lazy decay, ordinary API semantics, brittle static detector.
- implementation distance: HOLD.
- open choices: read-only source/artifact detector consistency; any future semantics question must be newly prospective.
- exploration count: Discovery `1`, Architecture `1`.
- condition: no repair/rerun/relabel of completed `AMBIGUOUS_CONTRACT`; wait for bounded detector-consistency disposition.

### `CAND-TOPK-PA-01` — `HOLD`
- question: residual beyond hard switching, border/basin effects, hysteresis, and recurrent transient amplification.
- not rescue: existing lower-funnel characterization; same-object cycle 3 prohibited.
- target layer: `ARCHITECTURE_STUDY`.
- expected information gain: high only if a fresh reduction object is defined.
- implementation distance: HOLD.
- exploration count: Discovery `1`, Architecture `2`, Utility read-only `2`.
- condition: no cycle 3 and no PRE_FORMAL promotion from the existing narrow/mixed result.

### `CAND-H7-RESP-01` — `HOLD`
- question: whether a native online/local responsibility-sensitive mechanism independently appears under matched privilege.
- not rescue: dormant future candidate; no current mechanism.
- target layer: `PRE_FORMAL`.
- expected information gain: very high only if a native mechanism independently appears.
- implementation distance: FAR.
- exploration count: PRE_FORMAL `0`.
- condition: remain HOLD; no queue-filler construction.

Completed Temporal and v0.5 topology-config Architecture questions are not kept in the small active candidate pool; they remain historical lower-funnel objects under `do_not_touch`.

## SUB Discovery review

`REFRACTORY_INHIBITION_CANCELLATION_DISCOVERY_CYCLE1` -> **`PROMOTE_TO_ARCHITECTURE_STUDY`**.

The Discovery used a one-unit synthetic field and direct arrivals only. It found that same-time `-0.5/+0.5` during refractory preserves the decayed membrane level while `-0.5` alone lowers it; a later matched `+0.70` probe spikes in paired/excitation-only arms but not inhibition-only. The repository implementation exactly explains this through netting-before-clamp. No extra memory/adaptation mechanism is required.

Promotion means only a **fresh NON_EVIDENTIARY Architecture contract**. The exploratory values/results are not formalized or relabeled as evidence.

## Literature / Independent Audit / Methodology / Steward inputs

Literature has no new run after 03:30 JST. Its ordinary-reduction-first framing remains applicable: configuration/API/state-transition/event-time effects are not new computational principles merely because they are reproducible.

Independent Audit remains unchanged at H5 `ROBUST_SO_FAR` for the exact registered aggregate algorithmic-work claim. It must not be generalized into a no-go claim against event routing/lazy state generally.

Methodology Calibration updated at 05:18 JST and remains `MIXED_CALIBRATION`. The scientific novelty, comparator, admission, STOP, and FORMAL integrity gates should **not** be made stricter. The new lesson is upstream machine-fact validity: provenance-correct artifacts can still contain semantically wrong extracted booleans. Decision-relevant source/runtime facts should use prospectively valid structural/AST/typed/invariant methods where practical, and post-outcome detector anomalies must fail closed without repair/relabel.

The generic Utility handoff-binding guard has now completed one bounded prototype with `PASS_EXPECTATIONS`: it blocks the known Temporal and Top-k bad bindings and passes a faithful positive control. This is a control-plane methodology result only and creates no live integration authority. The existing suppression static-detector request remains proposed pending Control disposition. No new Utility request is needed.

Repository Steward remains governance advisory only. Its last state is older than the present science, but the governance facts remain consistent: stable main, five evidence tags, legacy freeze preservation, zero rulesets, and no science-blocking governance mutation.

## MAIN / SUB allocation

`main_lane = REFRACTORY_CURRENT_ACCOUNTING_ARCHITECTURE_STUDY_CYCLE1`

MAIN owns the entire critical path: prospective contract, exact source/comment/docs/tests binding, shadow comparator, fixed inputs/controls/observable, harness, CI/preflight, machine-fact validity, raw-before-summary artifact, interpretation, and any pre-diagnostic science-invariant fix.

Fresh prospective contract:
- layer: `ARCHITECTURE_STUDY`, strictly `NON_EVIDENTIARY`
- stable source: `main@ebed6abfa941c83b36d2e3bddd04e7c5fb0dbe9d`
- required source blob: `src/sparkbrain/v04/field.py@e2279d69cd04030238317361d5ee3abdc1009c32`
- production semantics: `NET_THEN_REFRACTORY_CLAMP`
- read-only shadow: `IGNORE_POSITIVE_THEN_APPLY_INHIBITION_DURING_REFRACTORY`
- fixed core arms: inhibition-only; balanced same-time inhibition+excitation; excitation-only
- fixed downstream observable: post-refractory probe spike occurrence/count
- controls: same currents/timestamps outside refractory plus inhibition-only/excitation-only controls
- forbidden: official TEST, consumed raw, trained confirmatory checkpoint, formal scorer, STARTED identity, preserve/evidence mutation

Prospective terminal classes, fixed before any new result:
`EXPLICIT_NETTING_CONTRACT_MATCHES_PRODUCTION`,
`NO_SUPPORTED_MIXED_SIGN_RUNTIME_CONDITION`,
`STATE_ONLY_REFRACTORY_ACCOUNTING_EFFECT`,
`FUNCTIONAL_REFRACTORY_ACCOUNTING_EFFECT`,
`NO_SEMANTIC_DIFFERENCE_UNDER_FIXED_PROBE`,
`MIXED_OR_AMBIGUOUS_REFRACTORY_CONTRACT`,
`INVALID_DIAGNOSTIC`.

Every terminal stops for fresh Analyst review. A functional effect does **not** auto-promote to PRE_FORMAL.

`sub_lane = BOUNDED_SECONDARY_DISCOVERY`

SUB may run at most one genuinely independent synthetic/dev/read-only Discovery question. It must not take the promoted Refractory object, Assembly follow-up, suppression detector task, Top-k, H7 construction, MAIN blockers, FORMAL/TEST/scoring/preserve/evidence surfaces, or Utility control-plane work.

`sub_fallback = NO_OP_WITH_OBSERVABLE_LEVEL_DUPLICATION_OR_LOW_VALUE_REASON`.

## Top 3 and exact #1 GO/STOP

1. **MAIN — Refractory current-accounting Architecture cycle 1**: `HIGH / NEAR` — **GO_NON_EVIDENTIARY_ONLY**.
2. **Control/Utility — suppression static-detector consistency disposition; separately decide prospective adoption point for the completed handoff-binding guard**: `HIGH / VERY_NEAR / READ_ONLY / NONBLOCKING`.
3. **SUB — one independent bounded Discovery or explicit no-op**: `MEDIUM / OPTIONAL`.

### #1 GO

GO for exactly one NON_EVIDENTIARY Architecture cycle only after exact source/comment/docs/tests/input/comparator/control/observable/terminal-map/machine-field bindings are committed prospectively and exact-head ordinary CI/readiness is green.

### #1 STOP

STOP on any semantic/protocol scope gap, need for TEST/consumed/formal surfaces, unsupported mixed-sign condition, arm mismatch beyond the preregistered refractory treatment, source/runtime/binding mismatch, any valid terminal class, or any outcome-bearing failure that would require redesign. Do not alter current magnitudes/times/probe/comparator/observable/terminal mapping after outcome visibility.

## Prospective contingency tree

`PRE_START_BINDING_COMPLETE`
-> one fixed Refractory Architecture cycle.

`PRE_START_MECHANICAL_BLOCKER`
-> MAIN may make science-invariant lint/build/import/harness fixes before outcome visibility, rebind exact head, rerun ordinary CI, and continue under the unchanged contract.

`PRE_START_SEMANTIC_OR_PROTOCOL_GAP`
-> STOP.

`EXPLICIT_NETTING_CONTRACT_MATCHES_PRODUCTION`
-> STOP; Architecture/API contract note only; reject novelty continuation.

`NO_SUPPORTED_MIXED_SIGN_RUNTIME_CONDITION`
-> STOP; reject runtime-relevance claim.

`STATE_ONLY_REFRACTORY_ACCOUNTING_EFFECT`
-> STOP; Architecture state-semantics result only.

`FUNCTIONAL_REFRACTORY_ACCOUNTING_EFFECT`
-> STOP; fresh Architecture review only, no auto PRE_FORMAL.

`NO_SEMANTIC_DIFFERENCE_UNDER_FIXED_PROBE`
-> STOP; reject current functional question.

`MIXED_OR_AMBIGUOUS_REFRACTORY_CONTRACT`
-> STOP/HOLD.

`INVALID_DIAGNOSTIC`
-> discard + STOP.

Any outcome-bearing failure or desire to redesign after seeing results
-> STOP; no result-dependent repair.

All FORMAL PASS/FAIL/INCONCLUSIVE paths remain `UNARMED_NO_FORMAL_OBJECT`.

## Consumed identities / blockers / do-not-touch

Consumed/no-retry identities remain:
`c19-external-v2-official-v4`,
C19-R1 revision-authority official-v1/v2,
C19-R2 official-v1,
PD01 official-v1,
NI01 official-v1,
H5 official-v1.

Real blockers:
- Refractory MAIN execution is blocked until its complete prospective contract/machine-field bindings and exact-head CI/readiness exist.
- Suppression successor remains blocked pending the bounded static-detector consistency disposition; the completed artifact is not repaired.
- Assembly successor requires a genuinely fresh prevalence/reliability/contract question; the current object is complete.
- PRE_FORMAL lacks a fresh supported mechanistic/reduction object.
- FORMAL lacks a fresh admissible object and one-way authority.

Do not touch consumed identities, immutable evidence/control/preserve refs, completed Assembly/suppression/Temporal/topology-config/Top-k/H5/NI01/C19/PD01 artifacts, official TEST/formal scorer surfaces, Utility-owned decisions/results/state, scheduler definitions, or PR #148/#149 governance work.

## Utility request

No new Utility request was created. `CTRL-20260920-0450-HANDOFF-BINDING-GUARD` is complete with `PASS_EXPECTATIONS`; `EVA-20260920-0502-SUPPRESSION-STATIC-DETECTOR-CONSISTENCY` already covers the remaining bounded orphan detector-fidelity task and is pending Control disposition.

## Persistence

This run may write only:
- `analysis/orchestrator/latest.md`
- `analysis/orchestrator/state.json`
- append-only `analysis/orchestrator/history/2026-09-20/0558.md`

No scientific experiment was executed by the Analyst, no research workflow was dispatched by the Analyst, no identity was consumed, no research PR was merged, no immutable evidence/control/preserve ref was mutated, and no scheduler definition was changed.
