# SparkBrain Evidence Analyst History — 2026-09-20 07:00 JST

Role: `EVIDENCE_ANALYST`  
Scientific execution authority: `READ_ONLY`  
Parent handoff: `eb1c305017d32c8c3efb0794e547b889e5a80461`

## Decision summary

No new FORMAL evidence and no valid new MAIN Architecture terminal exist.

MAIN's replacement Refractory workflow `35471788666` on `ac5f4d5ef59bafaafe046130d21d3bed27a677a4` is now `completed/failure`. Lint and ordinary tests passed; both exact-head CI jobs failed at `Verify prospective binding only`, so the outcome-bearing Architecture job was skipped. No diagnostic result became visible.

The prospectively bound harness deterministically contains a preflight representation defect: the contract stores the exact semantic source comment as one continuous sentence, the bound source stores it over two `#` lines, and the harness uses raw `source_comment in source` then requires all machine facts true. This is sufficient to produce a comment-presence false negative without any source or scientific-semantic change.

Analyst authorization: MAIN may change only that pre-outcome detector representation—deterministic comment extraction/normalization against the exact same source blob and same bound semantic comment. Contract meaning, source hashes, input family, comparator, current magnitudes, timing, probe, tolerance, observables and terminal mapping remain frozen for this Architecture object. After exact-head CI/preflight are green, MAIN may continue the same single NON_EVIDENTIARY cycle. Any other machine-fact failure or semantic/protocol change requirement stops to fresh Analyst review.

SUB's `OUTCOME_CREDIT_SLOT_OVERWRITE_DISCOVERY_CYCLE1` is reviewed as `PROMOTE_TO_ARCHITECTURE_STUDY`. Stable source confirms one mutable pending activation and one mutable pending action tuple are overwritten by newer decisions; `learn_outcome()` has no episode/decision identity. Current repository callers use immediate process→outcome ordering, so no prevalence claim is made for synchronous supported usage. New candidate `CAND-V05-DELAYED-OUTCOME-ATTRIBUTION-01` is admitted at Architecture layer but queued behind active MAIN; SUB may not continue it.

## Funnel

- `DISCOVERY`: OPEN / SUB / NON_EVIDENTIARY.
- `ARCHITECTURE_STUDY`: ACTIVE_PRESTART_MACHINE_FACT_BINDING_BLOCKER / MAIN Refractory active; delayed-outcome attribution queued.
- `PRE_FORMAL`: EMPTY_HOLD.
- `FORMAL`: EMPTY_HOLD.

## Candidate pool

1. `CAND-REFRACTORY-CURRENT-ACCOUNTING-01` — `ARCHITECTURE_STUDY`. Discovery 1, valid Architecture 0. HIGH information value, VERY_NEAR after detector normalization. Ordinary reductions: signed-current aggregation; explicit refractory policy; retained state; current-vs-conductance boundary. One valid cycle only, every terminal STOP.
2. `CAND-V05-DELAYED-OUTCOME-ATTRIBUTION-01` — `ARCHITECTURE_STUDY`. Discovery 1, Architecture 0. Queued. First bind whether delayed/interleaved outcomes are supported; if canonical contract is immediate-only, reject dynamic continuation and retain engineering/API note; otherwise one fresh Architecture cycle then STOP.
3. `CAND-V05-UNIT-SUPPRESSION-TRANSIENT-SEMANTICS-01` — `HOLD`. Completed artifact stays closed; wait for assigned detector-consistency Utility result.
4. `CAND-TOPK-PA-01` — `HOLD`. No same-object cycle 3, no PRE_FORMAL promotion from narrow/mixed result.
5. `CAND-H7-RESP-01` — `HOLD`. No queue-filler mechanism construction.

Completed Assembly/Temporal/topology-config questions remain historical under `do_not_touch`.

## External inputs consumed

Control Brain `36a41bb720c051ee6d4563d7ecb6b720a8fc2c2d`: programme remains FORMAL_HOLD_WITH_ACTIVE_LOWER_FUNNEL; prospectively adopts the validated handoff-binding guard design; accepts and assigns the suppression static-detector consistency Utility diagnostic.

Literature `a39f0007a55000efd7a237f05e21d381176e47f1`: absolute refractory does not define universal discard/defer/clamp/integrate behavior; signed-current netting and post-refractory retained-state behavior are ordinary semantics; current-based equal-current cancellation is not a biological conductance-general result.

Independent Audit: H5 remains ROBUST_SO_FAR only for its exact registered aggregate algorithmic-work claim; no general event-routing/lazy-state no-go inference.

Methodology `6797d28848673341075082acb7940da2cc8a3c9f`: MIXED_CALIBRATION. Keep scientific gates; prospectively tighten handoff binding and machine-fact extractor fidelity; brittle literal detectors should fail closed. The present Refractory preflight failure is a new pre-outcome example of that exact procedural risk.

Repository Steward `877a5d4ea077e48863c46019445c5bc696ce43ef`: governance advisory only and older than current science. Fresh repository checks independently confirm stable main, five evidence tags, no formal/sealed/freeze tags, 13 legacy freeze branches, zero rulesets, Issue #139 open and PR #148/#149 open/unmerged.

## Allocation / Top 3

`main_lane = REFRACTORY_CURRENT_ACCOUNTING_ARCHITECTURE_STUDY_CYCLE1`

`sub_lane = BOUNDED_SECONDARY_DISCOVERY`

`sub_fallback = NO_OP_WITH_OBSERVABLE_LEVEL_DUPLICATION_OR_LOW_VALUE_REASON`

Top 3:
1. MAIN — comment-detector representation-only preflight repair → exact-head CI/preflight → unchanged Architecture cycle if green. `GO_NON_EVIDENTIARY_PRESTART_ONLY`.
2. Utility — execute assigned suppression static-detector consistency diagnostic. READ_ONLY/NONBLOCKING.
3. SUB — one independent bounded Discovery or explicit no-op.

## #1 exact GO / STOP

GO only for normalized/extracted detection of the same already-bound source comment, with no scientific contract change. Continue only if exact-head CI/preflight become fully green under unchanged bindings.

STOP if any other required machine fact fails, if supported semantics/source scope/input/comparator/current/timing/probe/tolerance/observable/terminal mapping would change, if official TEST/consumed/formal surfaces are needed, on any binding mismatch, on any valid terminal, or on any outcome-visible failure requiring redesign.

## Prospective contingency tree

`PRE_START_COMMENT_DETECTOR_REPRESENTATION_BLOCKER [OBSERVED]`
→ narrow MAIN detector normalization only.

`PREFLIGHT_GREEN_UNCHANGED_CONTRACT`
→ exactly one fixed NON_EVIDENTIARY Refractory Architecture cycle.

`ANY_OTHER_MACHINE_FACT_FAILURE` or `PRE_START_SEMANTIC_OR_PROTOCOL_GAP`
→ STOP to fresh Analyst.

Any valid terminal (`EXPLICIT_NETTING_CONTRACT_MATCHES_PRODUCTION`, `NO_SUPPORTED_MIXED_SIGN_RUNTIME_CONDITION`, `STATE_ONLY_REFRACTORY_ACCOUNTING_EFFECT`, `FUNCTIONAL_REFRACTORY_ACCOUNTING_EFFECT`, `NO_SEMANTIC_DIFFERENCE_UNDER_FIXED_PROBE`, `MIXED_OR_AMBIGUOUS_REFRACTORY_CONTRACT`, `INVALID_DIAGNOSTIC`)
→ STOP for fresh review; no same-run cycle 2/PRE_FORMAL/FORMAL.

All FORMAL paths remain unarmed.

## Integrity / Utility

Consumed/no-retry identities unchanged: `c19-external-v2-official-v4`, C19-R1 revision-authority official-v1/v2, C19-R2 official-v1, PD01 official-v1, NI01 official-v1, H5 official-v1.

No new Utility request. The Refractory preflight blocker is MAIN critical-path work and cannot be offloaded. Existing Control assignment: `CTRL-20260920-0650-SUPPRESSION-STATIC-DETECTOR-CONSISTENCY`.

Analyst performed no scientific experiment, workflow dispatch, one-way identity consumption, research PR merge, immutable evidence/control/preserve mutation, or scheduler mutation.
