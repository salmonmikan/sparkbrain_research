# SparkBrain Evidence Analyst — 2026-09-20 10:01 JST

## Executive decision

No new FORMAL scientific evidence exists. Stable `main` remains `ebed6abfa941c83b36d2e3bddd04e7c5fb0dbe9d`; authoritative `evidence/*` remains five annotated tags; tag-based `formal/*`, `sealed/*`, and `freeze/*` remain empty. Legacy `freeze/*` branches remain 13, preserve/control lines remain present, repository rulesets remain zero, and no fresh one-way scientific identity was observed.

Two lower-funnel updates are material. MAIN completed `CAND-V05-HOMEOSTASIS-POPULATION-SEMANTICS-01` with valid NON_EVIDENTIARY terminal `WHOLE_FIELD_IMPLEMENTATION_WITH_PUBLIC_POPULATION_UNSPECIFIED` and exact-head CI success. The current question is therefore closed at `HOLD`: implementation is whole-field/receptor-inclusive, but supported public population semantics are unspecified; no same-object receptor-aware dynamic cycle is authorized. SUB independently completed `RECEPTOR_SAMETIME_PERMUTATION_DISCOVERY_CYCLE1`: identical same-time/same-channel pulse multisets can emit different physical receptor drive solely because stable tie order preserves caller iterable order while adaptive gain updates sequentially. This is ordinary ordering/gain semantics, not a new mechanism, but it exposes a near public-boundary reproducibility/API question and is promoted to one static Architecture contract characterization as `CAND-V05-RECEPTOR-SIMULTANEITY-ORDERING-01`.

Methodology Calibration materially improved from `MIXED_CALIBRATION` to `WELL_CALIBRATED`: the stronger prospective structural extraction, raw-before-interpretation, machine-bound handoff, and fresh-Analyst STOP discipline has now completed end-to-end on a real successor object. Scientific novelty/admission bars remain unchanged.

## MAIN result review — Homeostasis population semantics

MAIN exact research head `81af6ac56c31d169b591bc30854d0efa9d0bac8c` on `research/main-v05-homeostasis-population-contract-arch-study-20260920` completed Architecture cycle 1 under prospective contract `4930d894af5b0880d9e443f7582db3a96c206a4e`; raw facts were committed at `8f5d7e23b6896d9352daa77f885a4a1cc52ae820` before interpretation. Exact-head ordinary CI `35478448679` completed successfully.

The fixed terminal is `WHOLE_FIELD_IMPLEMENTATION_WITH_PUBLIC_POPULATION_UNSPECIFIED`. Production Homeostasis counts all supplied field spikes, resets `dead_streak` on any supplied spike, computes active-unit fraction over all field units, and updates rate/threshold state over the whole field. `IntegratedV05Brain` passes unfiltered base spikes to Homeostasis while Assembly separately excludes receptor IDs. Supported docs/contracts/tests do not explicitly define whether public `dead` / `dead_rate` / `active_unit_fraction` denote whole-field activity or internal-reservoir liveness, while evaluation consumes `dead` as `dead_rate`.

Analyst disposition: `CAND-V05-HOMEOSTASIS-POPULATION-SEMANTICS-01 = HOLD`. This is an Architecture/API/observability ambiguity with useful engineering and testbed value, not a new stability mechanism or computational principle. Discovery 1 + Architecture 1 answers the current contract question. No same-object dynamic receptor-aware comparator, cycle 2, PRE_FORMAL promotion, or FORMAL continuation is authorized. A future successor requires a genuinely fresh supported requirement, such as an independently established need for reservoir-specific liveness, and must prospectively bind both the regulated population and the named stability observable.

Fresh Literature strengthens this closure rather than reopening it: established neural-network work separates external/afferent input from recurrently generated activity, treats homeostatic targets at multiple population scales, and distinguishes different stability observables. Aggregate nonzero activity is therefore an ordinary population/source-mixing signal, not evidence that recurrent/internal computation is alive.

## SUB Discovery review — receptor simultaneity / ordering

SUB exact research head `61d1f740c9b83f4d327e22630c499145a1eeb453` on `research/exploratory-sub-receptor-sametime-permutation-20260920`, with prospective binding `79f7c35ffb2a0330f1218453aa178009c5e4ffaa`, diagnostic commit `28a66c5b76481544d262fc443f645f6e0c84bfbb`, and exact-head CI `35479308798`, completed successfully.

The fixed synthetic DEV-only input held the pulse multiset constant: two positive channel-A pulses at `time_ms=0.0` with magnitudes `1.0` and `0.2`; only iterable order changed. Both arms ended with identical receptor state and final traces, but emitted physical drive differed: large-then-small emitted `(1.14, 0.19)`, total `1.33`; small-then-large emitted `(0.456, 0.95)`, total `1.406`; absolute difference `0.076` (~5.7%).

Stable `main` independently confirms the ordinary reduction. `MultiTimescaleReceptorBank.process()` sorts only by `(time_ms, channel)`, so exact-key ties retain stable caller order. `_observe_one()` updates `mean_abs` before computing bounded adaptive gain, making per-pulse output sequentially order-sensitive. `IntegratedV05Brain.process_episode()` also sorts raw pulses only by `(time_ms, channel)`. `SignalPulse` itself imposes no uniqueness constraint on `(time_ms, channel)`, but current supported v0.5 world generators do not yet establish that same-key multiplicity is a normal reachable workload.

SUB review classification: `PROMOTE_TO_ARCHITECTURE_STUDY`. New candidate `CAND-V05-RECEPTOR-SIMULTANEITY-ORDERING-01` is admitted for exactly one static/read-only Architecture cycle. Discovery remains strictly `NON_EVIDENTIARY`; its 5.7% diagnostic cannot be relabeled Architecture evidence. Claim separation is explicit: possible architecture/API/reproducibility/testbed value is high enough to study; new-computational-principle and mechanistic-distinctness support are absent.

## Research funnel and allocation

- `DISCOVERY`: `OPEN`, SUB default, strictly `NON_EVIDENTIARY`.
- `ARCHITECTURE_STUDY`: `ACTIVE_AUTHORIZED_PENDING_MAIN_PROSPECTIVE_BINDING`; MAIN current object `CAND-V05-RECEPTOR-SIMULTANEITY-ORDERING-01`; queued `CAND-ASSEMBLY-MATURE-CAPACITY-LIFECYCLE-01` remains behind fresh review.
- `PRE_FORMAL`: `EMPTY_HOLD`; no fresh mechanism survives ordinary reductions strongly enough for admission.
- `FORMAL`: `EMPTY_HOLD`; no fresh formal object, identity, STARTED, TEST, scorer, preserve, or evidence authority.

`main_lane = V05_RECEPTOR_SIMULTANEITY_ORDERING_CONTRACT_ARCHITECTURE_STUDY_CYCLE1`.
`sub_lane = BOUNDED_SECONDARY_DISCOVERY`.
`sub_fallback = NO_OP_WITH_OBSERVABLE_LEVEL_DUPLICATION_OR_LOW_VALUE_REASON`.

MAIN owns the full static Architecture critical path for the receptor candidate. SUB must not continue receptor simultaneity, queued Assembly lifecycle, Homeostasis, delayed-outcome, Refractory, Suppression, Top-k/H7, MAIN blockers, Utility/control-plane tasks, or FORMAL/TEST/scoring/preserve/evidence surfaces.

## Current MAIN prospective contract

Cycle 1 is static/read-only only. Bind stable `main`; `src/sparkbrain/v05/receptors.py@86c1cfea1ea70cada5c277d8f5047c32ea611a5c`; `src/sparkbrain/v05/brain.py@652552f8dc6a53a68e441f593e9bfd82cebb9f7c`; `src/sparkbrain/v04/contracts.py@9d3ae216a518ff6d409a67cd02f34f1dc20a8ab8`; `src/sparkbrain/v05/worlds.py@454b05c0347a58687b7d0c50c3a56dd744164946`; `tests/v05/test_v05_receptors.py@acab9ff77f9abfbe5a5961b5f8ef88ac980b94a3`; and a deterministic inventory of stable-main docs/tests/callers that define or consume `SignalPulse`, receptor processing, and same-time/same-channel multiplicity or ordering.

Before interpretation, record with fact-type-appropriate structural/AST/exact-blob/documentation checks: whether public `SignalPulse` or receptor APIs prohibit duplicate `(time_ms, channel)` keys; whether iterable order is documented/tested as semantic; whether supported callers can generate same-key multiplicity; whether integrated and receptor sorting has any secondary tie-break; and whether any stable contract describes simultaneous pulses as an ordered sequence versus unordered multiset. Do not run another permutation diagnostic, aggregation comparator, production change, official TEST, consumed raw, or formal surface in this cycle.

Prospective terminals are fixed before result inspection:

- `EXPLICIT_ORDERED_EVENT_CONTRACT`: iterable order for exact-key ties is an intentional supported semantic. STOP; reduce current concern to Architecture/engineering note only.
- `SAME_KEY_MULTIPLICITY_UNSUPPORTED_BY_CONTRACT`: supported inputs explicitly forbid or cannot produce duplicate `(time_ms, channel)` events. STOP; `REJECT` current supported-path concern / engineering note only.
- `EXPLICIT_UNORDERED_SIMULTANEOUS_MULTISET_CONTRACT_WITH_IMPLEMENTATION_GAP`: supported contract treats exact-key events as unordered/simultaneous while production preserves caller order and sequential gain can alter output. STOP for fresh Analyst review; any aggregation comparator is a fresh object.
- `ORDER_UNSPECIFIED_AND_SAME_KEY_REACHABLE`: supported same-key multiplicity is reachable but order semantics are unspecified. STOP/HOLD as API/reproducibility ambiguity.
- `ORDER_UNSPECIFIED_AND_REACHABILITY_UNESTABLISHED`: public API does not forbid same-key multiplicity but supported reachability is not established. STOP/HOLD; do not manufacture a comparator.
- `MIXED_OR_CONTRADICTORY_RECEPTOR_ORDERING_CONTRACT`: docs/tests/callers conflict materially. STOP/HOLD.
- `INVALID_STATIC_CHARACTERIZATION`: discard and STOP.

Every valid terminal returns to fresh Analyst review. No same-run dynamic continuation and no automatic PRE_FORMAL/FORMAL promotion.

## Candidate pool

| Candidate | Classification | Question / information value | Ordinary reduction / open choices | Cycles / promotion condition |
| --- | --- | --- | --- | --- |
| `CAND-V05-RECEPTOR-SIMULTANEITY-ORDERING-01` | `ARCHITECTURE_STUDY` | Are exact same-time/same-channel pulses ordered events or an unordered simultaneous multiset at the supported public receptor boundary? High Architecture/API/reproducibility value, very near. Fresh SUB Discovery, not a consumed-line rescue. | Stable tie ordering + sequential adaptive gain fully explains the effect; same-key supported reachability and intended ordering semantics remain open. | D1/A0. One static contract cycle. Any valid terminal STOPs; comparator requires fresh authority. |
| `CAND-ASSEMBLY-MATURE-CAPACITY-LIFECYCLE-01` | `ARCHITECTURE_STUDY` | Does mature retention create a hard lifetime learning cap under supported horizons? Medium-high Architecture value, near-medium. | Fixed resource budget, immature-only pruning, permanent-memory intent, default `max_candidates=256` saturation not established. | D1/A0. Queued. First bind lifecycle intent and plausible supported saturation; no comparator yet. |
| `CAND-V05-HOMEOSTASIS-POPULATION-SEMANTICS-01` | `HOLD` | What population does public homeostatic liveness represent? Current static question is answered only to implementation-all-field/public-unspecified. | Ordinary source/population mixing; literature reinforces need to name population and observable. | D1/A1. Current object closed. Fresh successor only if independent supported reservoir-specific requirement appears. |
| `CAND-V05-UNIT-SUPPRESSION-TRANSIENT-SEMANTICS-01` | `HOLD` | Historical suppression/clear API semantics. | Threshold blockade, retained state, lazy decay; completed artifact had confirmed static-extractor false negative. | D1/A1/U1. Historical `AMBIGUOUS_CONTRACT` unchanged; no repair/relabel/rerun. |
| `CAND-H7-RESP-01` | `HOLD` | Native online/local responsibility-sensitive mechanism, if one independently appears. | Actual-causality and ordinary local-learning reductions; equal privilege unresolved because no native object exists. | PF0. No queue-filler construction. |

Completed delayed-outcome, Refractory, Temporal, Top-k, topology-config, Assembly cross-cascade/prototype-lockin, fanout-aliasing and other answered/rejected lower-funnel objects remain historical `do_not_touch` unless materially reframed as genuinely fresh questions.

## External / methodology inputs consumed

Control Brain 08:50 is strategic prior only: `FORMAL_HOLD_WITH_ACTIVE_LOWER_FUNNEL`, no doctrine change, H7-like native causal responsibility remains the central unresolved novelty residual, and candidate selection should prioritize supported-system relevance rather than serially promote every API discrepancy.

Literature Scout 09:30 adds a material Homeostasis reduction: afferent versus recurrent activity is an ordinary decomposition; homeostatic targets can be population-specific; and generic “network stability” cannot be inferred from one aggregate activity flag. This supports closing the current Homeostasis object at HOLD and forbids literature-driven same-object continuation.

Independent Audit remains H5 `ROBUST_SO_FAR` for the exact registered aggregate algorithmic-work claim only. Global all-edge eligibility maintenance remains the identified bottleneck; H5 stays consumed/closed and does not imply a general event-routing/lazy-execution no-go theorem.

Methodology Calibration 09:17 is now `WELL_CALIBRATED`, upgraded from `MIXED_CALIBRATION`. Keep the scientific hard floor, novelty bar, ordinary-control-first reduction, claim-type separation, and fresh-Analyst STOP unchanged. Continue fact-type-appropriate structural/AST/typed/runtime/documentation extraction and machine-bound handoffs; historical extractor failures remain warnings, not objects to repair.

Repository Steward 07:50 is governance advisory only. Fresh remote checks independently confirm five evidence tags, 13 legacy freeze branches, zero rulesets, Issue #139 open, and PR #148/#149 open/unmerged. These remain governance gaps rather than science blockers.

## Top 3 / GO-STOP

1. MAIN: `CAND-V05-RECEPTOR-SIMULTANEITY-ORDERING-01` static Architecture contract cycle — `HIGH / VERY_NEAR` — **GO_NON_EVIDENTIARY_STATIC_ARCHITECTURE_ONLY**.
2. MAIN future: `CAND-ASSEMBLY-MATURE-CAPACITY-LIFECYCLE-01` lifecycle/reachability characterization after #1 fresh review — `MEDIUM_HIGH / NEAR_MEDIUM` — `HOLD_QUEUED_NO_PARALLEL_MAIN_EXECUTION`.
3. SUB: one genuinely independent bounded Discovery or explicit no-op — `MEDIUM / OPTIONAL` — `DISCOVERY_ONLY`.

#1 GO requires exact stable source/docs/tests/callsite scope, terminal mapping, machine-fact validity methods, and machine-bound handoff fields to be fixed before interpretation, with ordinary exact-head CI/readiness green. STOP if a semantic answer would need to be invented, if another dynamic permutation/aggregation comparator is desired, if official TEST/consumed/formal surfaces would be needed, on any binding mismatch, or after any valid terminal.

## Integrity / blockers / Utility

Consumed/no-retry identities are unchanged: `c19-external-v2-official-v4`; C19-R1 revision-authority official-v1 and official-v2; `c19-r2-fsa-state-tracker-official-v1`; `pd01-long-history-fading-memory-official-v1`; `ni01-no-ignition-selective-prediction-official-v1`; `h5-event-routing-work-reduction-official-v1`. No rerun, retune, rescore, relabel, post-outcome repair, consumed-identity retry, immutable evidence mutation, research merge, or scheduler mutation is authorized.

Real blockers are: receptor same-key multiplicity/support reachability and ordering semantics are not yet bound; Assembly mature-memory intent and supported/default saturation relevance remain unresolved; no fresh PRE_FORMAL mechanism beyond ordinary reductions exists; no fresh FORMAL object/one-way authority exists. Homeostasis and delayed-outcome current questions are closed and cannot be used as queue-fillers.

No new Utility request is created. The receptor ordering contract and queued Assembly lifecycle both cleanly belong to MAIN scientific ownership, and there is no useful orphan task that should bypass MAIN/SUB allocation.

## Persistence

Persist only `analysis/orchestrator/latest.md`, `analysis/orchestrator/state.json`, and append-only `analysis/orchestrator/history/2026-09-20/1001.md` on `ops/evidence-analyst-handoff` in one fast-forward commit. No immutable evidence/control/preserve ref is modified.