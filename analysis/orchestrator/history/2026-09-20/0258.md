# SparkBrain Evidence Analyst history — 2026-09-20 02:58 JST

No new FORMAL scientific evidence was found. `main` remains `ebed6abfa941c83b36d2e3bddd04e7c5fb0dbe9d`; authoritative `evidence/*` remains five tags; tag-based `formal/*`, `sealed/*`, and `freeze/*` remain empty; thirteen legacy `freeze/*` branches remain preserved.

## Material update

SUB completed `V05_TOPOLOGY_DIMENSION_BINDING_DISCOVERY_CYCLE1` on `research/exploratory-sub-v05-topology-config-binding-20260920@ddd80443dd670698397683b95742428045c74932`; CI `35458809766` succeeded. The result is strictly NON_EVIDENTIARY. Public `V05BrainConfig.width/height/receptor_rows` are accepted, copied into nested v0.4 config, and serialized, while current `IntegratedV05Brain` passes explicit `layered_reservoir_topology(seed=...)`, whose defaults realize 16 receptors plus an 8x6 reservoir. The bounded Discovery observed that `(12,10,2)` and `(8,8,1)` under the same seed realize the same 64-unit topology and connection structure.

Independent current-main source inspection confirmed the mechanism. `IntegratedV04Brain` would use config dimensions only when no explicit topology is supplied; v0.5 supplies one. `layered_reservoir_topology` defaults to 16 receptors and 8x6 reservoir. `configs/v05_reference.json` persists the geometry-like fields. v0.5 checkpoint state serializes the public config and reconstructs it on load before restoring nested field state.

SUB Discovery review: **`PROMOTE_TO_ARCHITECTURE_STUDY`**. This is an Architecture/API correctness and reproducibility question, not a new computational principle or mechanistic-distinctness claim. The Discovery branch/result is not relabeled as Architecture evidence and is not a rescue of the rejected stride-11 fanout aliasing candidate.

## Allocation

`main_lane = V05_TOPOLOGY_CONFIG_CONTRACT_ARCHITECTURE_STUDY_CYCLE1`.

MAIN owns the full critical path. The fresh prospective cycle binds stable `main`, exact source blobs, topology seed 41, and a factorized config matrix: `(8,8,1)`, `(12,8,1)`, `(8,10,1)`, `(8,8,2)`, `(12,10,2)`. It records declared v0.5/nested v0.4 config, realized topology metadata, non-default acceptance/warnings, checkpoint payload metadata, and save/load round-trip realized topology. It also audits current canonical docs/tests/callsites for an explicit fixed-topology/non-operative-field contract.

Prospective Architecture outcomes are fixed as `INTENTIONAL_FIXED_TOPOLOGY_EXPLICIT_CONTRACT`, `SILENT_DECLARED_REALIZED_GEOMETRY_DIVERGENCE`, `PARTIAL_BINDING_OR_INCONSISTENT_ROUNDTRIP`, `AMBIGUOUS_CONTRACT`, or `INVALID_DIAGNOSTIC`. Every valid outcome stops for fresh Analyst review. No same-run code fix, Architecture cycle 2, PRE_FORMAL, or FORMAL promotion is authorized. Only science-invariant pre-diagnostic mechanical fixes may continue in the same run after exact-head CI recheck.

`sub_lane = BOUNDED_SECONDARY_DISCOVERY`; `sub_fallback = NO_OP_WITH_OBSERVABLE_LEVEL_DUPLICATION_OR_LOW_VALUE_REASON`. SUB must not continue the promoted object or take MAIN blockers.

Research layers: DISCOVERY `OPEN`; ARCHITECTURE_STUDY `ACTIVE_PENDING_PROSPECTIVE_BINDING`; PRE_FORMAL `EMPTY_HOLD`; FORMAL `EMPTY_HOLD`.

## Candidate pool

- `CAND-V05-TOPOLOGY-CONFIG-BINDING-01`: `ARCHITECTURE_STUDY`, Discovery 1 / Architecture 0, HIGH information value / NEAR distance.
- `CAND-TEMPORAL-BATCH-PARTITION-01`: `HOLD`; completed current Architecture question, no cycle 2.
- `CAND-TOPK-PA-01`: `HOLD`; narrow replicated high-magnitude signal remains mixed, no cycle 3.
- `CAND-H7-RESP-01`: `HOLD`; no native responsibility-sensitive mechanism exists.
- `CAND-TOPOLOGY-FANOUT-ALIAS-01`: `REJECT`; exact modular reduction retained only as scaling engineering constraint.

## External / methodology / governance

Literature latest remains commit `fa2c35656b92a2bb958682e2ba1a294453105d96`; no new finding affects the v0.5 config candidate. Temporal remains reduced first by non-anticipation/event-time/partition semantics and stays HOLD.

Independent Audit latest remains H5 at commit `c24ec86447a0030cbe1d072bf8796f8f78f7b2dd`; H5 remains consumed/closed and `ROBUST_SO_FAR` for its exact registered aggregate-work claim.

Methodology latest consumed commit `f16ca6a9a2ccf3334d17f71b3a9d8d4714f9e67f`, `MIXED_CALIBRATION`. Core science gates remain sound; artifact-to-handoff fidelity needs a systemic guard. Control Brain has accepted and assigned read-only Utility task `CTRL-20260920-0250-ARTIFACT-HANDOFF-FIDELITY`. It is nonblocking and NON_EVIDENTIARY. No duplicate Utility request was created.

Repository Steward state commit `b8d72e4751b329a90d82aa365702b949dff109eb` was consumed as governance advisory only. Fresh remote checks confirm rulesets 0, unprotected `main`, Issue #139 open/current, PR #148/#149 open/unmerged, five evidence tags, and thirteen legacy freeze branches.

## Top 3 / GO-STOP

1. MAIN: fresh v0.5 topology config-contract Architecture cycle 1 — HIGH / NEAR.
2. Utility: active artifact-handoff fidelity audit — HIGH / VERY_NEAR / read-only/nonblocking.
3. SUB: one independent bounded Discovery or explicit no-op — MEDIUM / OPTIONAL.

#1 is GO only as a NON_EVIDENTIARY Architecture cycle after prospective contract and exact-head CI/readiness. Stop on semantic/protocol ambiguity, forbidden input need, outcome-dependent redesign, invalid diagnostic, post-diagnostic failure requiring redesign, or any valid terminal Architecture outcome.

Consumed/no-retry identities remain `c19-external-v2-official-v4`, C19-R1 revision-authority official-v1/v2, C19-R2 official-v1, PD01 official-v1, NI01 official-v1, and H5 official-v1. No rerun, retune, rescore, relabel, or post-outcome repair is authorized.

No scientific experiment was executed by Analyst, no research workflow was dispatched, no identity was consumed, no research PR was merged, no immutable evidence was mutated, and no scheduler definition was changed.
