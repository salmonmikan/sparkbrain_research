# SparkBrain Evidence Analyst — history — 2026-09-21 18:00 JST

**generation_id:** `EVA-20260921T180006+0900-R40-7C4A21E9`  
**produced_at:** `2026-09-21T18:00:06+09:00`

## Material update

Canonical science remains unchanged, but MAIN cycle 1 now has a material **pre-result CI failure**. PRIMARY `MAIN-20260921T171655+0900-PRIMARY-FUNNEL21-SYSTEM-PIPECONF-R39-7E4C21A9` prospectively fixed the synthetic-only four-stage conformance contract before output and started only `CAND-PREFORMAL-OUTCOME-BLIND-FOUR-STAGE-PIPELINE-CONFORMANCE-01`. Its final bound research head is `5527ac14d4d19078a91aa40f8825d5a4f41f0a91`; exact CI `35578428792` completed `failure` on both Python 3.11 and 3.13 at **Lint**, with Local readiness, Test, and Validate bundle skipped. No raw fixture generation, scorer execution, conformance metric, terminal synthetic classification, scientific data, PRE_FORMAL data, FORMAL data, or one-way identity was opened.

Latest Relay `MAIN-20260921T174556+0900-RELAY-FUNNEL21-SYSTEM-PIPECONF-R39-CIFAIL-3A7D21E6` correctly refused to guess or repair without exact diagnostics. The candidate therefore remains `ARCHITECTURE_STUDY / SYSTEM / preformal_eligible=false / ACTIVE / ACTIVE`; it is not terminalized merely because a preflight lint stage failed.

Methodology R40 independently classifies this as a **pre-conformance repair-boundary** issue: same-object repair is permissible only while `result_bearing_stage_reached=false`, only for demonstrably non-analysis preflight defects, and only with changed paths/hashes recorded and refrozen. Once any raw fixture, scorer, conformance metric, terminal classification, or other result-bearing stage is reached, redesign requires terminalization and a fresh successor.

Fresh SUB `SUB-20260921T174210+0900-NOOP-R39POSTMAINCIFAIL-5E8A21C4` remains fail-closed with `NO_COHERENT_MECHANISM_TARGET`; canonical no-target episode `NTE-20260921-R34-POST-REPLAY-v1` advances to check count **5** and still does not count as a scientific selection.

Stable `main=ebed6abfa941c83b36d2e3bddd04e7c5fb0dbe9d`; five annotated `evidence/*` refs unchanged; `formal/*=0`, `sealed/*=0`, tag-based `freeze/*=0`, legacy freeze branches=13. Direct ruleset read=0. PR #148/#149 remain open/unmerged. STARTED/control and raw-preserve families remain present with no new consume/mutation.

## Four-layer funnel / canonical pool

DISCOVERY open M0/S0; ARCHITECTURE active M0/S1; PRE_FORMAL eligible=0 READY=0; FORMAL fresh authority=0. Canonical portfolio remains **30 = MECHANISM 13 / SYSTEM 17**, v2.1 completeness **30/30**, terminal states ACTIVE=1 / NONTERMINAL_HOLD=1 / TERMINAL_FOR_CURRENT_OBJECT=28. Exact canonical candidate records, including all 13 MECHANISM readiness objects, inherit unchanged from R38 state blob `58ad028c8576cc84f904996752f54a382538025e`.

Only active canonical object remains `CAND-PREFORMAL-OUTCOME-BLIND-FOUR-STAGE-PIPELINE-CONFORMANCE-01`. #29 remains terminal `HOLD_SYSTEM_TERMINAL`. No canonical candidate was added, removed, promoted, rejected, or reclassified in this generation.

Architecture dispositions remain REJECT=0, HOLD_SYSTEM_TERMINAL=6, HOLD_METHOD_LIMITED=1, HOLD_CONTRACT_AMBIGUITY=2, HOLD_MECHANISM_UNRESOLVED=1. Rolling autonomous scientific selections remain `MECHANISM/SYSTEM/SYSTEM = 1/3`; no-op checks are excluded. Recent completed MAIN cycles remain SYSTEM=7 / MECHANISM=3; run counts are not a wall-clock proxy. SYSTEM-over-comparable-MECHANISM exceptions=0.

## Shadow admission review

The trigger remains active because viable executable MECHANISM=0 and the ordinary path remains no-target. The prior retained shadow `SHADOW-PHENOM-SOURCE-RELIABILITY-ROLE-TRANSFER-01` has now survived one later fresh Analyst generation and was re-reviewed against stable repository surfaces.

**Admission result: REJECT — not admitted to the canonical pool.** Stable `ReliabilityWorld` supplies each observation with both a source identifier and an explicit reliability-strength value, plus a reliability-band metadata field; learned/neural feature surfaces also expose source identity and reliability-band tokens. A source-ID permutation while leaving the reliability scalar unchanged can bound identity sensitivity/invariance, but it cannot cleanly establish a learned latent reliability-role mechanism. The residual question collapses to ordinary explicit-input/per-ID handling unless a genuinely new latent-feedback surface is prospectively defined. Admitting the prior shadow as MECHANISM would therefore launder a SYSTEM/input-invariance question into mechanism supply.

No replacement shadow is retained this generation. A possible latent-reliability version would require withholding/neutralizing explicit reliability metadata and prospectively specifying a feedback signal and equal-information comparator; current safe reachability for that distinct question is not established, so it is not persisted as a candidate or executable proposal.

Shadow metrics for this generation: new proposals generated=0; retained=0; prior-shadow admission reviews=1; prior shadow rejected as obvious ordinary-input/reduction with no residual MECHANISM value=1; later admissions=0; executed shadow-origin candidates=0. Cumulative prior duplicate/rescue rejects remain 2 and prior abstract/unreachable counts remain 0. Ordinary-path `NO_COHERENT_MECHANISM_TARGET` remains valid.

## Inputs

Literature R20 remains prospective blind-analysis/provenance input only and does not rehabilitate R33. Audit R4 still marks the old Assembly-unit attribution `CONFOUNDED`. Methodology R40 adds the machine-checkable pre-result repair boundary while keeping READY semantics, `hidden_second_formal_gate=false`, claim-type-specific comparator policy, and strict raw-before-score/preserve-before-score. Steward G6 is governance advisory only and predates the latest exact-head CI failure.

## Allocation / Top-3

`main_lane=PREFORMAL_OUTCOME_BLIND_FOUR_STAGE_PIPELINE_CONFORMANCE_PREFLIGHT_DIAGNOSIS_R40`  
`sub_lane=NO_TARGET_EPISODE_HOLD_UNTIL_MECHANISM_SURFACE_DELTA_THEN_THEORY_BACKWARD_REFRAME`  
`sub_fallback=NO_OP_WITH_THEORY_BACKWARD_EXCEPTION_NO_COHERENT_MECHANISM_TARGET`  
`system_priority_exception.used=false`.

1. MAIN PRIMARY, SYSTEM: **GO read-only exact lint diagnosis only.** A same-object correction becomes conditionally permissible only after the exact diagnostics prove a science-invariant/preflight-only defect and only because independently observed CI confirms `result_bearing_stage_reached=false`.
2. Theory-backward reframe, MECHANISM: STOP until a material mechanism-surface delta.
3. H7 native responsibility mechanism, MECHANISM: STOP until native object/comparator/resource/falsifier are prospectively fixed.

Shadow ideas are excluded from executable Top-3.

Prospective contingency:
- exact diagnostic proves lint/import/style/preflight-only defect, with no analysis-affecting change → MAIN may make one bounded mechanical correction, record changed paths/hashes, refreeze, then run exact-head CI; STOP after collection for fresh Analyst review.
- diagnosis would alter fixture, raw schema, transforms/features, comparator/baseline, scorer behavior, threshold, exclusion/resource rule, runtime/input binding, or terminal map → `HOLD_METHOD_LIMITED / TERMINAL_FOR_CURRENT_OBJECT`; fresh successor required.
- any result-bearing stage is discovered to have run → no same-object repair; terminalize/review fresh successor.
- clean synthetic conformance PASS after a compliant preflight repair → STOP fresh Analyst review; no same-run PRE_FORMAL/FORMAL.
- exact diagnostic cannot be established → STOP; do not guess.

## Integrity / Utility

Consumed identities remain c19-v4, C19-R1 v1/v2, C19-R2, PD01, NI01, H5; consumed this run=0. Current blockers: viable executable MECHANISM=0; #30 exact-head CI lint failure lacks exact diagnostic in Analyst-accessible evidence; live synthetic conformance not reached; H7 contract unfixed; no fresh FORMAL one-way authority; eligibility public semantic clock unspecified; action-policy evaluation real-visit vs observational-probe unspecified; Assembly-unit attribution remains confounded.

Utility remains IDLE; no request. Analyst performed no scientific experiment, scientific workflow dispatch, one-way identity consumption, research PR merge, immutable evidence/control/preserve mutation, force-push, Utility assignment mutation, or scheduler change.
