# SparkBrain Evidence Analyst — Latest Two-Worker Handoff

Analysis time: `2026-09-18 11:00 JST`
Prior Evidence Analyst authority: `28108fffc2e4e346bff79bf3e38d1cac27d3265c`

## Executive decision

Two material updates change the execution gate, but not MAIN/SUB ownership.

First, MAIN has completed the prospectively requested PD01 matched fading-memory formal contract on `research/pd01-fading-memory-preformal-20260918@ea2a8b4a5f244d601782ecb302760c49ee27e8c1`. The exact task/world, intervention, lag grid, SparkBrain source/config/runtime, one stronger fixed contractive reservoir comparator, resource/readout privileges, DEV/TEST split, target-free inventory, primary cluster unit, bootstrap, numerical PASS/FAIL/INCONCLUSIVE thresholds, and raw-before-score integrity order are now fixed. The same exact head has green `pd01-formal-contract-prestart` `35295370268`, ordinary CI `35295370271`, and compatibility `pd01-preformal` `35295370202`. Proposed identity `pd01-long-history-fading-memory-official-v1` remains unreserved and unconsumed; no PD01 STARTED/control, preserve, evidence, TEST access, target access, scoring, or formal result exists.

Second, the first fresh independent audit of terminal C19-R2 is now complete. Audit commit `0c871b1b9b54d35c826a59c5b9925afa55b78d22` classifies R2 `ROBUST_SO_FAR`, independently reproduces the aggregate `C19-v4 - R2 FSA` contrast `-0.2462308568887407`, confirms the registered `atomic_idx` cluster-bootstrap 95% CI `[-0.2554750732756702, -0.23690126111231016]`, and finds no authority, leakage, statistics, seed-fragility, or claim-boundary defect. R2 remains terminal `REDUCED_BY_FSA`, consumed/no-retry.

The PD01 contract is scientifically admissible **as an exact fading-memory-boundary test**, with an important prospective interpretation cap. SparkBrain has plasticity enabled while the formal comparator is a fixed-weight contractive 64-state reservoir. The contract matches observable inputs, exposed feature dimension, linear-readout privilege, reset semantics, and bounded compute, but it does **not** match every possible persistent-memory resource because SparkBrain may write history into mutable internal/synaptic state while the reservoir cannot. That asymmetry is intentional for the current question—whether a remote lineage effect survives an ordinary contractive fading-memory system—not evidence that SparkBrain beats all conventional memory mechanisms.

Therefore MAIN may proceed to science-invariant authority/admission packaging for exactly one fresh identity, `pd01-long-history-fading-memory-official-v1`. If and only if the final execution head preserves the reviewed scientific contract exactly, rebinds authority to this handoff, remains namespace-fresh, and again passes all required exact-head gates, MAIN may perform exactly one one-way PD01 execution. A PASS is capped to `SURVIVES_THIS_FIXED_FADING_MEMORY_REDUCTION`; before any novelty claim, a fresh prospective mutable-weight / eligibility / slow-state comparator would still be required. FAIL, INCONCLUSIVE, INVALID_EVIDENCE, or POST_START_FAILURE are terminal for this identity. No automatic PD01-v2, comparator shopping, threshold tuning, or same-ID salvage is authorized.

Formal `sub_lane` and `sub_fallback` remain `null`. SUB is `no_op`; there is no incubator candidate to classify this cycle.

## Fresh repository state

- `main@ebed6abfa941c83b36d2e3bddd04e7c5fb0dbe9d`, still unprotected.
- PD01 exact head: `research/pd01-fading-memory-preformal-20260918@ea2a8b4a5f244d601782ecb302760c49ee27e8c1`.
- Exact-head gates: `35295370268` pre-START success; `35295370271` ordinary CI success; `35295370202` compatibility success.
- Proposed PD01 identity: `pd01-long-history-fading-memory-official-v1`, unreserved/unconsumed.
- Matching PD01 control/preserve/evidence/formal authority objects: absent.
- PD01 new formal scientific measurement: none.
- Authoritative evidence tags: two — C19-v4 and C19-R2.
- Legacy `freeze/*` branches: 13, preserved.
- Open PRs: `#148`, `#149`, both control-plane/tooling.
- Open non-PR governance Issue: `#139`.
- Repository rulesets: 0; server-side authoritative-tag protection remains absent.

## Control-plane streams consumed

`ops/*` branches were treated only as designated mailboxes; current repository refs/workflows/evidence independently re-fetched above take precedence.

- Control Brain branch tip: `3bcd000f7e31bc8df8d59ca5ddec88ff5b847e22`; current state/history at `2026-09-18 10:51 JST` consumed as strategic prior. It explicitly flags the plastic-SparkBrain versus fixed-reservoir memory-resource asymmetry and asks Analyst for a prospective ruling.
- MAIN report commit consumed: `2563b208e22e46b4d55b815ad25e0a4e3dbf3b6a`; newest relevant history `reports/orchestrator/history/2026-09-18/1045-main.md`.
- SUB report commit consumed: `ae22b121f603476a6449d86817d14a76d1908fcc`; newest relevant history `reports/orchestrator/history/2026-09-18/1035-sub.md`.
- Literature handoff commit consumed: `525686fa0c14f64426ca7bf5c89f08cd0bcb9c77`; newest relevant role history `0430-LITERATURE_REDUCTION_SCOUT.md`.
- Independent Audit handoff commit consumed: `0c871b1b9b54d35c826a59c5b9925afa55b78d22`; newest relevant role history `1030-INDEPENDENT_AUDITOR.md`.
- Repository Steward tip/state consumed: `e16a3f0dae16b7e93ba73551d56772a366b16b84`; governance advisory only and stale versus terminal R2/current PD01/current evidence-tag count.

## External knowledge integration

### `external_input.literature`

No newer literature handoff than `525686fa...`. Its reduction pressure is unchanged: predictive-state/epsilon-machine/local-causal-state and reservoir/fading-memory families mean local, pre-semantic, history-derived persistence is not novel by itself. The current PD01 contract directly implements the literature-motivated remote-history/washout discriminator using one fixed conventional contractive reservoir.

Affected lines: `PD01`, programme novelty, persistent-dynamics residual, future external validation, C19-R2 interpretation.

Prospective impact: no change to the now-frozen PD01 scientific contract. If PD01 passes, do not interpret that as general non-fading novelty; a fresh ordinary mutable-weight / eligibility / slow-state reduction is required before any stronger claim. PSR/epsilon-machine and related reductions remain future context, not post-hoc additions to this identity.

Allocation impact: no worker-owner change.

### `external_input.audit`

This cycle has genuinely new independent-audit information. Audit `0c871b...` independently verifies terminal C19-R2 `REDUCED_BY_FSA` as `ROBUST_SO_FAR`: exact authority/package/STARTED binding, 8,720 target-blind raw records preserved before target materialization, target-free source map, registered cluster-primary inference over 204 clusters, all five seed contrasts negative, and independently recomputed aggregate matching terminal evidence.

Affected lines: `C19_R2_FSA_STATE_TRACKER`, `C19_V4`, `PD01`, programme novelty, statistical integrity.

Prospective impact: the previous pending-R2-audit blocker is cleared. Keep R2 closed and do not reopen C19 rescue work. Carry base-world/source-family cluster-aware inference forward; PD01 already prospectively clusters by `base_world_id`.

Allocation impact: MAIN remains PD01 and SUB remains null, but the formal-execution admission gate is strengthened because downstream planning no longer depends on an unaudited R2 terminal result.

## SUB operating mode / exploratory review

Latest SUB mode: `no_op`.

`sub_exploratory_review`: not applicable. There is no current exploratory candidate and therefore nothing to classify as `FORMALIZE_AS_SUB`, `FORMALIZE_AS_MAIN_FUTURE`, `CONTINUE_EXPLORING`, `REJECT`, or `NO_ACTION`.

`sub_lane: null`  
`sub_fallback: null`

`no_sub_lane_reason`: no independent formal secondary object is prospectively complete; recent RV01/incubator themes are exhausted or hard-stopped, H9/C07 remains under-specified, and PD01 is wholly MAIN-owned. SUB should remain `no_op` unless a genuinely distinct bounded NON_EVIDENTIARY question appears. SUB must not debug, tune, implement, audit, or unblock PD01.

## Active-line review

| Line | Current status | Strongest evidence / shortest path to NEW information | Value / distance | Centrality |
|---|---|---|---|---|
| A01 | `MIXED_PROGRAMME_CLOSED` | consumed mixed development observations; fresh independently motivated programme only | LOW / FAR | closed |
| RV01 | `DEVELOPMENT_POSITIVE_REDUCIBLE`; exploratory budget exhausted | ordinary scalar delay/filter reductions remain live; fresh object only | LOW-MED / MEDIUM | secondary |
| RV02 | `TERMINAL_CONSTRUCTION_NEGATIVE` | consumed; fresh object only | LOW / FAR | terminal |
| CX/CX01 | `TERMINAL_FORMAL_NEGATIVE` | immutable formal negative; fresh object only | LOW / FAR | terminal |
| H8/C08 | terminal causal-specialization negative | consumed/closed; distinct fresh object only | LOW / FAR | terminal |
| H9/C07 | `PRE_START_UNDERSPECIFIED` | exact state/reset/resource contract required if independently revived | MEDIUM / MEDIUM | paused secondary |
| C19-v4 | `TERMINAL_PASS_NARROW_REPRESENTATION_GAIN / WEAKENED_NOT_INVALID` | immutable representation result; no rerun | completed | completed |
| C19-R1 | scientifically unresolved / operationally terminated | v1/v2 consumed POST_START_FAILURE; no R1-v3 | LOW / FAR | interpretation ceiling |
| C19-R2 | `TERMINAL_REDUCED_BY_FSA / ROBUST_SO_FAR_AUDITED` | consumed terminal evidence; no retry/successor | realized HIGH | closed primary |
| **PD01** | **`FORMAL_CONTRACT_READY_FOR_ANALYST_REVIEW -> PRE_START_AUTHORITY_PACKAGING`** | **exact fixed reservoir/washout contract + exact-head green gates; package authority then one formal run if all GO gates re-pass** | **VERY_HIGH / NEAR** | **PRIMARY** |

## Parallel decomposition

### `main_lane`

- target: `PD01_LONG_HISTORY_FADING_MEMORY_ONE_WAY`
- owner: `MAIN`
- status: `PRE_START_AUTHORITY_PACKAGING`
- reviewed exact scientific head: `research/pd01-fading-memory-preformal-20260918@ea2a8b4a5f244d601782ecb302760c49ee27e8c1`
- proposed identity: `pd01-long-history-fading-memory-official-v1`
- exactly-one execution: conditionally authorized only after final-head revalidation
- MAIN owns **all** critical-path work: authority/admission metadata, exact binding, workflow/runner, CI/pre-START, source/runtime/package checks, candidate/comparator execution, preservation, join, scorer, evidence terminalization, and every science-invariant fixup required by this object.
- SUB must not take any MAIN blocker.

### `sub_lane`

`null`

### `sub_fallback`

`null`

## Prospective interpretation boundary for PD01

The current asymmetry is accepted **for this exact question**: SparkBrain is plasticity-enabled while the comparator is a fixed-weight contractive reservoir. This tests whether the candidate exhibits recoverable remote-history influence beyond a conventional fading-memory state system under equal observable-input and readout privilege. It does not fully match all adaptive/persistent-memory resources.

Therefore:

- `PASS_SURVIVES_FADING_MEMORY_REDUCTION` means only survival against **this exact fixed 64-state contractive reservoir** under the frozen contract. It is not proof of generic non-fading persistence, lineage-specific novelty, or superiority to conventional plastic/eligibility/slow-state memory.
- `FAIL_REDUCED_BY_FADING_MEMORY` is strong reduction evidence for this discriminator: the ordinary fixed fading-memory comparator closes the long-lag advantage under the registered criterion.
- `INCONCLUSIVE` is terminal for this identity.
- Before any novelty claim after PASS, a fresh prospectively defined conventional mutable-weight / eligibility / slow-state comparator is required. That future object is not authorized here and must not be designed from PD01 TEST outcomes in the same run.

## `blocked_until`

PD01 cannot cross STARTED until MAIN has:

1. rebound authority/admission metadata to this exact Analyst handoff without changing the reviewed scientific contract;
2. preserved the exact task/world, seeds/splits, lag grid `[16,32,64,128]`, recent window `8`, SparkBrain config, fixed 64-state reservoir (`fan_in=8`, `leak=0.35`, recurrent row L1 `0.9`, input scale `0.5`, bias scale `0.05`, seed `90917`), ridge alpha `0.001`, resource ceilings, inventory, primary lags `[64,128]`, base-world cluster unit, bootstrap and numerical terminal rules;
3. bound exact source/protocol/package/input/runtime/candidate/comparator/scorer/preserver identities;
4. re-fetched namespaces and confirmed `pd01-long-history-fading-memory-official-v1` is fresh/unSTARTED/unconsumed and no formal/control/preserve/evidence collision exists;
5. passed ordinary CI, dedicated formal-contract pre-START, and required compatibility checks on the **same final execution SHA** after any authority packaging/head movement;
6. confirmed STARTED/no-clobber will occur before any formal TEST read, and TEST targets remain unavailable until immutable raw preservation plus independent refetch/digest verification.

Any scientific-semantic change needed to task, world, lags, candidate, comparator, plasticity, resource rules, readout, seeds, TEST inventory, metric, cluster unit, bootstrap, thresholds, runtime or source binding is a `PRE_START_SEMANTIC_GAP` and requires a new Analyst review.

## `do_not_touch`

- C19-R2 identity, STARTED, package, raw preserve, statistics, terminal evidence/tag: consumed immutable `REDUCED_BY_FSA`; no rerun/retune/rescore/rescue/R2-v2/R3.
- C19-v4 immutable package/preserve/evidence/tag and narrow PASS.
- R1-v1/v2 consumed identities, transient outputs, and any R1-v3 rescue.
- consumed C19-v2/v3 and A01/RV01/RV02/CX identities.
- legacy/immutable freeze/sealed/formal/evidence refs.
- R2/C19 per-example outcomes as PD01 design/tuning data.
- SUB exploratory material as formal evidence.
- scheduler definitions.
- non-designated files on `ops/*` branches as repository truth.

## Top 3

1. **MAIN — perform science-invariant PD01 authority/admission packaging, revalidate all exact-head GO gates, then execute exactly one formal identity only if all gates remain green.** Information value `VERY_HIGH`; distance `NEAR`.
2. **After terminal PD01 — perform a fresh independent read-only audit of identity/STARTED/raw-preserve/refetch/join/scorer/bootstrap chain and the narrow resource/claim boundary.** Information value `HIGH`; distance `NEAR_TO_MEDIUM`.
3. **SUB — remain `no_op`; do not manufacture a formal lane.** A distinct bounded NON_EVIDENTIARY incubator question may be selected only if it is genuinely independent of PD01 and does not revive exhausted/terminal lines.

## #1 GO / STOP

### GO

The proposed identity must be fresh/unSTARTED/unconsumed and collision-free. Exact source/protocol/package/input/runtime/candidate/comparator/scorer/preserver binding must match the reviewed contract. The final execution SHA must have green ordinary CI + dedicated formal-contract pre-START + required compatibility checks. STARTED/no-clobber must precede TEST access. Readouts are fitted once using DEV only; no TEST target may influence fit/tune/select. TEST raw scores for both candidate and comparator are generated target-blind, then immutably preserved with inventory/binding manifest and independently re-fetched/digest-verified before TEST targets are materialized. The `history_id` join must be unique, total and fail-closed before deterministic scoring.

The primary endpoint remains long-lag `[64,128]` classification accuracy difference `SparkBrain - reservoir`, clustered by `base_world_id`, 10,000 bootstrap resamples, seed `19901`, Type-7 95% interval.

Prospectively fixed scientific terminal rules:

- `PASS_SURVIVES_FADING_MEMORY_REDUCTION` iff effect CI lower `>= +0.10` **and** SparkBrain long-lag accuracy CI lower `>= 0.60`.
- `FAIL_REDUCED_BY_FADING_MEMORY` iff effect CI upper `<= +0.05`.
- otherwise `INCONCLUSIVE`.

### STOP

Before STARTED, any need to alter scientific semantics is `PRE_START_SEMANTIC_GAP` and stops for Analyst. Science-invariant mechanical/authority defects remain MAIN-owned and may be repaired same-run only with complete exact-head revalidation after each movement.

After STARTED, binding mismatch, premature target access, raw/preserve/digest/cardinality/join violation, DEV/TEST leakage, post-START repair, rerun, retune, threshold change or comparator shopping is `INVALID_EVIDENCE`; infrastructure/runtime failure is `POST_START_FAILURE`. Either consumes the identity and stops. No salvage, same-ID retry, or automatic PD01-v2.

## Prospective contingency tree

- `PRE_START_BLOCKER_MECHANICAL` -> MAIN owns repair; science contract unchanged; move head if needed; rerun all exact-head gates; continue only if fresh integrity checks pass.
- `PRE_START_SEMANTIC_GAP` -> STOP for fresh Analyst review; no identity consumption.
- all GO gates green -> reserve/create exactly one authorized identity and STARTED/no-clobber -> target-free TEST input -> DEV-only readout fit -> target-blind TEST raw -> immutable preserve -> independent refetch/digest -> TEST targets -> fail-closed join -> deterministic score/evidence.
- `PASS_SURVIVES_FADING_MEMORY_REDUCTION` -> terminal STOP; narrow claim only; no same-run novelty upgrade or mutable-memory successor. Fresh review required for any next comparator.
- `FAIL_REDUCED_BY_FADING_MEMORY` -> terminal STOP; narrow persistence hypothesis reduced under this exact discriminator; no rescue tuning.
- `INCONCLUSIVE` -> terminal STOP; no threshold/lag/comparator retuning.
- `INVALID_EVIDENCE` -> consume identity; STOP; no salvage/retry.
- `POST_START_FAILURE` -> consume identity; STOP; no same-ID retry.

## Governance advisory

Fresh remote governance remains: two authoritative evidence tags, 13 legacy freeze branches, zero repository rulesets, and unprotected `main`. Issue `#139` still correctly tracks the server-side protection gap, but its body is stale where it says there is only one authoritative evidence tag; Steward should reconcile that count without changing scientific state. PRs `#148` and `#149` remain control-plane/tooling work and are not scientific evidence.

Repository Steward's older 07:50 state is governance advisory only and is stale versus terminal R2 and PD01. Outcome-independent future `main` promotion candidates remain exact environment/source/runtime binding checks, STARTED/no-clobber/collision primitives, raw-preserve/refetch/digest verification, fail-closed joins, target-free cluster-map/base-world validators, deterministic scorer/bootstrap fixtures, and other hypothesis-independent execution-integrity helpers. PD01 world/comparator/threshold semantics remain research-local.

Control Brain accepted a human directive to inventory reusable result-independent CX comparator/model infrastructure for possible future `main` promotion. That work belongs to governance/tooling review and must not divert MAIN or become a formal SUB scientific lane.

## Orchestrator handoff

MAIN takes PD01 and owns **all** critical-path fixes from authority packaging through terminal evidence. Formal SUB takes nothing; `sub_lane=null`, `sub_fallback=null`, and SUB remains no-op unless a genuinely independent NON_EVIDENTIARY incubator target appears. MAIN must not absorb future work explicitly reserved to a later independent lane; none is currently reserved. SUB must not take PD01 blockers. Neither worker touches consumed C19-R2/R1/C19-v4 or other immutable evidence, nor scheduler definitions.

Repartition only if a fresh material audit invalidates the current foundation, a PD01 semantic gap prevents valid execution, or a genuinely independent prospectively complete SUB object appears. Under this handoff MAIN may continue in the same run only through prospectively fixed science-invariant PRE-START repair/revalidation and, once all GO gates are green, the exactly-one PD01 one-way path through its terminal classification.
