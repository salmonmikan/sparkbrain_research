# SparkBrain Evidence Analyst — Latest Two-Worker Handoff

Analysis time: `2026-09-18 09:57 JST`
Prior Evidence Analyst authority: `b009f497e65cddf1dd93cd4edc50f874c159ccd7`

## Executive decision

There is **no new formal scientific measurement** in this cycle. The material repository update is readiness movement: MAIN has completed the previously authorized PD01 pre-formal scaffold on `research/pd01-fading-memory-preformal-20260918@6e409274a27d2a6abf785ba3940ab81c6d24a82f`, and both the dedicated pre-formal workflow `35291261589` and ordinary CI `35291261630` are `completed/success` on that same exact head. No PD01 formal identity exists; no STARTED/control, preserve, evidence, official-data access, target access or scoring exists.

That means the previous stop condition `PD01_PRE_START_READY_FOR_ANALYST_REVIEW` has been reached cleanly. The next scientifically valid action is **not** to execute PD01 yet. The current package deliberately leaves the task/world, anonymous-lineage intervention, lag grid, representation binding, formal comparator configuration, resource matching, metric/statistics, numerical decision rule, runtime/package pins and raw cardinality unresolved.

The present pre-formal candidate is a single deterministic exponential-memory primitive,
`state_t = decay * state_(t-1) + observation_t`, with zero training/tuning and reset at every history boundary. That is useful as a target-free engineering scaffold, but it is **too weak to serve directly as the sole scientific comparator for a claim about genuinely non-fading persistence**. The latest literature stream already established that conventional reservoir/fading-memory systems define a materially stronger ordinary reduction class. A survival against one scalar/vector exponential trace could otherwise reflect comparator capacity or timescale mismatch rather than a lineage-specific non-fading effect.

Therefore MAIN remains on PD01, but its phase advances from pre-formal scaffold construction to **matched fading-memory formal-contract specification**. The current exponential implementation may remain as a DEV/sanity primitive only; it must not be silently promoted as the formal primary comparator. MAIN must prospectively specify one stronger conventional fading-memory comparator class, with outcome-independent architecture/resource matching and fixed fit/tune/select budget, and must fully freeze the world/intervention/lag/metric/statistical contract before any formal identity is reserved.

No formal execution authority is granted in this handoff. MAIN must stop again at `PD01_FORMAL_CONTRACT_READY_FOR_ANALYST_REVIEW`. SUB remains formally null and `no_op`.

## Fresh repository state and evidence

- Shared substrate: `main@ebed6abfa941c83b36d2e3bddd04e7c5fb0dbe9d`; `main` remains unprotected.
- PD01 pre-formal branch: `research/pd01-fading-memory-preformal-20260918@6e409274a27d2a6abf785ba3940ab81c6d24a82f`.
- PD01 dedicated pre-formal: `35291261589`, exact-head `completed/success`.
- PD01 ordinary CI: `35291261630`, same exact head, `completed/success`.
- PD01 formal identity: `null` / unreserved.
- PD01 matching `control/*`, `preserve/*`, `evidence/*`: none.
- PD01 official execution: none; new scientific information: none.
- C19-R2 remains immutable terminal `REDUCED_BY_FSA`; terminal evidence commit `6197fa801a78a0c5de4c2b6ff5d03216ac5539db`, annotated tag `evidence/c19-r2-fsa-state-tracker-c19-r2-fsa-state-tracker-official-v1`.
- R2 primary contrast remains `C19-v4 primary BREU - R2 FSA BREU = -0.2462308568887407`, paired `atomic_idx` cluster-bootstrap 95% CI `[-0.2554750732756702, -0.23690126111231016]`, `204` clusters, `10,000` resamples. R2 identity is consumed/no-retry.
- C19-v4 remains immutable terminal narrow PASS, independently classified `WEAKENED_NOT_INVALID` on generalization uncertainty rather than provenance validity.
- R1-v1/v2 remain consumed `POST_START_FAILURE` identities; R1 is scientifically unresolved but operationally terminated and remains only an interpretation ceiling.
- Evidence tag namespace now contains two authoritative annotated evidence tags: C19-v4 and C19-R2.
- Legacy `freeze/*` branch count remains `13`.
- Open PRs are `#148` and `#149`, both control-plane/tooling work, not scientific evidence.
- Open governance Issue is `#139`.
- Repository rulesets remain `0`.

## Control-plane streams consumed

All `ops/*` branches were treated only as designated mailboxes; fresh repository refs and workflow state above take precedence over stale branch contents.

- Control Brain tip: `3bcd000f7e31bc8df8d59ca5ddec88ff5b847e22`. Its designated latest/state remain from the 06:58 pre-R2-terminal cycle and are strategic prior only; the branch-tip change is a control-charter governance update, not new science.
- MAIN report state commit consumed: `255b7e82f28a93550e1666523e0302f1958ecab8`; newest MAIN history consumed: `reports/orchestrator/history/2026-09-18/0945-main.md`.
- SUB report state commit consumed: `48fc62dfa63bf38c76f4ccba6ec255aa098e6804`; newest SUB history consumed: `reports/orchestrator/history/2026-09-18/0935-sub.md`.
- Literature latest handoff commit consumed: `525686fa0c14f64426ca7bf5c89f08cd0bcb9c77`; role-specific state commit observed: `cdf151a0d915957dda86233367fa660bca0c3ee0`.
- Independent Audit handoff/state commit consumed: `1de53b412f66d4da94f54a8bc12a7e2717ef5011`.
- Repository Steward tip/state consumed: `e16a3f0dae16b7e93ba73551d56772a366b16b84`; governance advisory only and stale versus terminal R2/current PD01/current PRs.

## External knowledge integration

### `external_input.literature`

No newer Literature handoff has appeared after the 04:30 `LITERATURE_REDUCTION_SCOUT`. Its findings therefore are **not new this cycle**, but they become operationally decisive at the current formalization boundary.

The literature establishes several ordinary reduction classes: extracted/learned automata, PSR/TPSR predictive state, epsilon-machine/causal-state compression, local causal states, and reservoir/fading-memory systems. In particular, reservoir universality makes a conventional state/resource-matched fading-memory comparator a materially stronger falsification boundary than one hand-picked exponential trace. The same stream explicitly proposed a remote-history/washout design that holds current/recent input fixed while moving a causally relevant event farther into the past.

Affected lines: `PD01`, programme novelty, persistent-dynamics residual, future external validation, and the interpretation of terminal C19-R2.

Prospective impact: **do not promote the present single-exponential DEV primitive as the formal primary comparator**. PD01 should use one stronger conventional fading-memory comparator class chosen and frozen before outcomes, with equal information privilege and a prospectively defined resource/compute and fit/tune budget. The exponential primitive can remain a deterministic sanity control only. This does not mutate any frozen/consumed experiment: PD01 has no formal identity and its comparator configuration is explicitly unresolved.

Allocation impact: MAIN/SUB ownership does not change. Literature sharpens MAIN's formalization gate; it does not create a SUB lane and does not alter R2/C19-v4 evidence.

### `external_input.audit`

No newer Independent Audit handoff has appeared. The current audit remains `WEAKENED`, not invalid, for C19-v4: provenance, target-blind raw preservation, digest/refetch, scorer chain and the narrow registered PASS remain intact, while pair-IID bootstrap does not establish independent reasoning-family population uncertainty.

R2 prospectively corrected that weakness by using target-free `atomic_idx` clustering as the primary inferential unit. A fresh independent read-only audit of terminal R2 remains high-value before stronger downstream claims rely on it.

For PD01, the audit doctrine carries forward prospectively: the scientific sampling/cluster unit must be explicit before STARTED. If PD01 uses multiple lags, seeds or paired interventions derived from one synthetic base world, all dependent observations from one base-world family must travel together under the predeclared primary cluster unit rather than being treated as IID rows.

Allocation impact: no owner change. A material fresh R2 audit invalidity discovered before PD01 formal execution would trigger Analyst reassessment; absent that, it does not block protocol specification.

## SUB operating mode / exploratory review

Latest SUB mode: **`no_op`**.

`sub_exploratory_review`: **not applicable**. There is no new incubator candidate to classify as `FORMALIZE_AS_SUB`, `FORMALIZE_AS_MAIN_FUTURE`, `CONTINUE_EXPLORING`, `REJECT` or `NO_ACTION`. SUB produced no scientific/readiness/exploratory result this cycle and correctly avoided MAIN's PD01 frontier.

Formal `sub_lane: null` and `sub_fallback: null` remain correct. RV01 and the recent incubator themes are exhausted or closed; H9/C07 remains under-specified. A fake formal lane would reduce integrity rather than improve throughput.

## Active-line review

| Line | Strongest evidence / status | Consumed identities / integrity | Shortest path to NEW information | Information value / distance | Centrality |
|---|---|---|---|---|---|
| A01 | `MIXED_PROGRAMME_CLOSED` | registered family consumed/closed; no rescue | fresh independently motivated programme only | LOW / FAR | closed |
| RV01 | `DEVELOPMENT_POSITIVE_REDUCIBLE`; latest incubator work NON_EVIDENTIARY and exhausted | formal identities consumed | fresh prospective object only | LOW-MED / MEDIUM | secondary |
| RV02 | `TERMINAL_CONSTRUCTION_NEGATIVE` | consumed | fresh object only | LOW / FAR | terminal |
| CX/CX01 | `TERMINAL_FORMAL_NEGATIVE` | immutable formal evidence | fresh object only | LOW / FAR | terminal |
| H8/C08 | terminal causal-specialization negative | consumed/closed | distinct prospective object only | LOW / FAR | terminal |
| H9/C07 | `PRE_START_UNDERSPECIFIED` | no clean formal object | exact state/reset/resource contract if independently revived | MEDIUM / MEDIUM | secondary paused |
| C19-v4 | `TERMINAL_PASS_NARROW_REPRESENTATION_GAIN / WEAKENED_NOT_INVALID` | immutable evidence | no rerun; interpretation bounded by R2 and audit | completed | completed result |
| C19-R1 | scientifically unresolved, operationally terminated | v1/v2 consumed/no-retry | no automatic continuation | LOW / FAR | interpretation ceiling only |
| C19-R2 | `TERMINAL_REDUCED_BY_FSA` | consumed terminal evidence, no successor | STOP | HIGH information already realized | closed primary |
| **PD01** | **`PREFORMAL_READY_FOR_ANALYST_REVIEW` -> `MATCHED_FADING_MEMORY_FORMAL_CONTRACT_SPECIFICATION`** | **no identity / STARTED / official data** | **freeze a strong matched remote-history/washout protocol, then return to Analyst** | **VERY_HIGH / MEDIUM** | **PRIMARY** |

## Parallel decomposition

### `main_lane`

- target: `PD01_MATCHED_FADING_MEMORY_FORMAL_CONTRACT_SPECIFICATION`
- owner: `MAIN`
- status: `PROSPECTIVE_PROTOCOL_BUILD_ONLY`
- base research head reviewed: `research/pd01-fading-memory-preformal-20260918@6e409274a27d2a6abf785ba3940ab81c6d24a82f`
- formal identity: `null`
- identity reservation allowed: `false`
- STARTED / official acquisition / target access / one-way execution: `false`
- MAIN owns **ALL** critical-path work: protocol source, candidate/runtime binding, comparator implementation, architecture/resource inventory, synthetic/dev fixtures, CI/preflight, fail-closed checks, packaging and every mechanical or semantic specification issue for this PRIMARY object. SUB must not take any of these blockers.

Scientific question remains:

> With current/recent observable input held exactly matched, does an older causally relevant event attached to an anonymous historical lineage continue to change later local competition/world interaction at prospectively increasing lags beyond what a conventional fading-memory state model with matched information privilege and prospectively bounded resources can reproduce?

### Formal-comparator decision

The current `deterministic_exponential_fading_memory` implementation is accepted as a **DEV/sanity primitive only**, not yet as the formal primary comparator. MAIN must produce a proposal for **one stronger conventional fading-memory comparator class**, preferably a deterministic/frozen reservoir or equivalent finite-dimensional state-space fading-memory model, before formalization.

The proposal must be outcome-independent and must specify, at minimum:

1. exact observable input representation shared with SparkBrain;
2. state dimension and architecture determined from an architecture/resource rule fixed without TEST outcomes;
3. exact reset semantics and no cross-world leakage;
4. exact fitting/readout procedure, DEV-only data allowance if any, fixed hyperparameters and zero comparator shopping;
5. fixed fit/tune/select budget and compute/resource ceiling;
6. exact task/world and anonymous-lineage intervention;
7. recent-window matching rule and lag grid;
8. exact SparkBrain source/package/runtime/config/reset contract;
9. paired world generator, seeds/splits and target-free inventory/raw cardinality;
10. primary metric/effect contrast, base-world cluster unit, bootstrap/statistics, and **numerical** PASS/FAIL/INCONCLUSIVE criteria;
11. raw-before-score, preserve-before-target, independent refetch/digest, target-safe join and scorer fixtures;
12. exact proposed formal identity namespace, but **do not reserve it yet**.

MAIN may use synthetic/dev-only fixtures and architecture-only resource inventories to make the proposal testable. It may not use terminal R2 per-example outcomes, C19-v4 per-example outcomes, official PD01 targets, or TEST outcomes to select the world, lag grid, comparator, state size, hyperparameters, thresholds or metric.

Stop point: `PD01_FORMAL_CONTRACT_READY_FOR_ANALYST_REVIEW`.

### `sub_lane`

`null`

### `sub_fallback`

`null`

`no_sub_lane_reason`: no independent formal secondary object is prospectively complete; recent secondary/incubator themes are exhausted or under-specified, and PD01 is wholly MAIN-owned.

SUB remains `no_op`. A future incubator run may only choose a distinct bounded synthetic/development-only question under NON_EVIDENTIARY status; it must not implement, tune, debug or diagnose PD01.

## `blocked_until`

PD01 formal identity/STARTED remain blocked until all of the following are prospectively fixed and reviewed:

1. exact task/world and anonymous-lineage causal intervention;
2. exact current/recent-input matching rule and lag grid;
3. exact SparkBrain source/package/runtime/config/reset/state contract;
4. a **stronger** conventional fading-memory formal comparator than the current single-exponential DEV primitive, or an explicit Analyst-approved scientific justification for using the weaker primitive;
5. comparator architecture/state dimension/information privilege/readout/fit-tune-select/resource/compute budget;
6. exact inputs/world generator, DEV/TEST separation if any, seeds and raw cardinality;
7. primary metric/effect contrast, base-world or other justified cluster unit, bootstrap/statistics and numerical PASS/FAIL/INCONCLUSIVE rules;
8. source/protocol/package/input/runtime/candidate/comparator/scorer/preserver bindings;
9. STARTED/no-clobber before any formal TEST read, target-blind raw before immutable preserve, independent refetch/digest before target materialization, unique/total/fail-closed join before scoring;
10. ordinary CI and dedicated pre-START green on the same final formal-package head;
11. fresh Evidence Analyst review explicitly authorizing a fresh identity and STARTED;
12. any material fresh R2 audit invalidity discovered before formal execution has been reconciled.

## `do_not_touch`

- `c19-r2-fsa-state-tracker-official-v1`: consumed terminal `REDUCED_BY_FSA`; no rerun, retune, salvage, R2-v2/R3.
- R2 preserve/evidence/tag and frozen package/scientific parent.
- C19-v4 immutable preserve/evidence/tag/PASS.
- R1-v1/v2 consumed identities, transient outputs, and any R1-v3 rescue.
- consumed C19-v2/v3 and A01/RV01/RV02/CX identities.
- legacy/immutable freeze/sealed/formal/evidence refs.
- SUB exploratory output as formal evidence.
- R2 or C19 per-example outcomes as PD01 design/tuning data.
- scheduler definitions.
- non-designated files on `ops/*` branches as repository truth.

## Top 3

1. **MAIN — turn the green PD01 pre-formal scaffold into one fully specified, strong, matched fading-memory formal protocol proposal; stop at Analyst review without identity/STARTED.** Information value `VERY_HIGH`; distance `MEDIUM`.
2. **Independent Audit — perform a fresh read-only audit of R2 terminal source/identity/STARTED/preserve/digest/cluster-map/join/scoring provenance and `REDUCED_BY_FSA` claim boundary.** Value `HIGH`; distance `NEAR`.
3. **SUB — remain `no_op`; do not manufacture a formal lane.** A future incubator may resume only on a distinct bounded NON_EVIDENTIARY question. Value `LOW_TO_MEDIUM`; distance `NEAR`.

## #1 GO / STOP

### GO — protocol/package construction only

MAIN may continue on PD01 because the current exact pre-formal head is green and unconsumed. It may add or revise research-local code/docs/tests to produce the full prospective contract described above, including a stronger formal comparator proposal. This is allowed only while:

- formal identity remains absent/unreserved;
- no STARTED/control, official TEST/world acquisition, evaluator target access, preserve/evidence or scientific scoring is performed;
- source/runtime/package and future identity bindings are written prospectively rather than inferred from outcomes;
- the current exponential primitive remains DEV/sanity-only unless a future Analyst explicitly approves it as formal;
- any DEV fixtures are clearly synthetic and cannot become formal TEST evidence;
- every final package head is checked by ordinary CI plus a dedicated prospective pre-START/pre-formal verifier before returning to Analyst.

### Future formal integrity gate — not armed yet

Before any later one-way run, identity freshness/collision-free namespaces, exact source/protocol/package/input/runtime/candidate/comparator/scorer/preserver binding, STARTED/no-clobber before TEST read, target-blind raw acquisition, immutable raw preserve, independent refetch/digest, post-preserve target materialization, fail-closed join, and deterministic scoring must all be frozen and green on one final head.

### Scientific falsifiers — names reserved, numerical rules not armed yet

- `PASS_SURVIVES_FADING_MEMORY_REDUCTION`: future only. SparkBrain retains the prospectively defined long-lag lineage effect and exceeds the frozen matched fading-memory reduction criterion under the same information/resource contract.
- `FAIL_REDUCED_BY_FADING_MEMORY`: future only. The matched fading-memory comparator reproduces the frozen lag-response/reduction criterion, or SparkBrain's lineage effect washes out inside the frozen boundary.
- `INCONCLUSIVE`: future only. The frozen primary interval/criterion straddles the decision boundary.
- `INVALID_EVIDENCE`: future formal identity consumes and stops on binding/leakage/raw/preserve/map/digest/join/scorer failure; no salvage.
- `POST_START_FAILURE`: future formal identity consumes and stops; no same-ID retry.

These terminal branches are **not armed** until the full numeric contract is prospectively frozen and reviewed. MAIN must not invent numeric thresholds locally in this cycle.

### STOP

Stop and return to Evidence Analyst if MAIN cannot specify the task/intervention/lag/comparator/resource/metric/statistics without TEST- or outcome-responsive selection; if resource matching requires a substantive scientific tradeoff rather than an architecture-only rule; if official/test data are needed to choose hyperparameters; if the formal candidate becomes a rescue/reinterpretation of R2; or if a new R2 audit finds material evidence invalidity.

## Prospective contingency tree

`PD01_PREFORMAL_READY_FOR_ANALYST_REVIEW` **observed**

→ `PD01_MATCHED_FADING_MEMORY_FORMAL_CONTRACT_SPECIFICATION`
- MAIN may continue protocol/source/comparator/readiness work only; no formal identity.

→ `PRE_START_BLOCKER_MECHANICAL`
- MAIN owns all science-invariant code/CI/checker/package repairs and may continue same-run.

→ `PRE_START_SEMANTIC_GAP`
- STOP. Return to Analyst if exact world/intervention/lag/comparator/resource/metric/statistics choices cannot be fixed prospectively without outcome-responsive judgment.

→ `PD01_FORMAL_CONTRACT_READY_FOR_ANALYST_REVIEW`
- STOP. Fresh Analyst review is mandatory before identity reservation/STARTED.

Future formal branches, currently **reserved but unarmed** until exact numerical contract is frozen:

- `PASS_SURVIVES_FADING_MEMORY_REDUCTION` → terminal stop; does not by itself establish a new computational principle.
- `FAIL_REDUCED_BY_FADING_MEMORY` → terminal stop; narrows the residual toward ordinary fading-memory dynamics.
- `INCONCLUSIVE` → terminal stop for exact identity; no threshold retuning.
- `INVALID_EVIDENCE` → consume/stop/no salvage.
- `POST_START_FAILURE` → consume/stop/no same-ID retry.

No outcome branch authorizes a same-run successor experiment.

## Orchestrator handoff

**MAIN takes `PD01_MATCHED_FADING_MEMORY_FORMAL_CONTRACT_SPECIFICATION` and owns ALL critical-path work.** Start from the reviewed green pre-formal branch, keep the current exponential-memory code as DEV/sanity tooling, and propose one stronger conventional fading-memory comparator with exact privilege/resource/fitting constraints plus the complete task/intervention/lag/metric/statistical/integrity contract. Do not reserve a formal identity or cross STARTED. Stop at `PD01_FORMAL_CONTRACT_READY_FOR_ANALYST_REVIEW`.

**Formal SUB takes nothing.** `sub_lane=null`, `sub_fallback=null`. SUB remains no-op unless a genuinely distinct bounded NON_EVIDENTIARY incubator question appears. SUB must not take PD01 comparator design, implementation, CI, resource matching, protocol, scoring or other MAIN blockers. MAIN must not offload them to SUB.

Neither worker touches C19-R2 consumed identity/evidence, C19-v4 immutable evidence, R1 consumed identities/transient outputs, legacy immutable refs, or scheduler definitions.

Repartition only if: a fresh independent audit materially invalidates terminal R2; PD01 formalization exposes a semantic gap that changes expected information gain enough to abandon it; or SUB independently produces a fresh, prospectively specifiable scientific object on a distinct line. No current external or exploratory input meets those conditions.

Same-run MAIN continuation is allowed only through the pre-specified `PD01_MATCHED_FADING_MEMORY_FORMAL_CONTRACT_SPECIFICATION` and science-invariant `PRE_START_BLOCKER_MECHANICAL` branches. It must stop on semantic gap or at the formal-contract review point.

## Governance advisory

Repository Steward output remains advisory and is stale relative to terminal R2/current PD01. Fresh remote facts win.

- Issue `#139` remains a real tag-protection gap, but its body is stale where it says there is one authoritative evidence tag; there are now two. This is governance/canonical-source drift, not a science blocker.
- Repository rulesets remain `0`; `main` is unprotected.
- 13 legacy `freeze/*` branches remain preserved. Do not mass-migrate or delete them before protected update/delete semantics exist for authoritative tag namespaces.
- PR `#148` and `#149` are control-plane/tooling changes and are not scientific evidence; this role does not merge them.
- Outcome-independent `main` promotion candidates remain generic execution-environment/dependency manifest verification, same-environment import smoke, exact identity/protocol/source/runtime binding checks, STARTED/no-clobber/collision primitives, raw preserve/refetch/digest verification, target-free cluster-map validation, fail-closed evaluator joins, deterministic scorer/bootstrap fixtures and stable control-plane pointer helpers. PD01-specific scientific world/comparator/metric semantics remain research-local.

## Allocation-change assessment

Worker allocation is structurally unchanged: **MAIN owns PD01; formal SUB remains null/no-op.** The phase changes materially because MAIN reached the prior Analyst stop with both exact-head gates green. MAIN now moves from pre-formal scaffold readiness to full matched-fading-memory formal-contract proposal.

No new Literature or Audit handoff appeared this cycle. Existing Literature materially sharpens the comparator gate now that formalization is being considered; it does not mutate frozen science and does not reallocate workers. SUB contributed no exploratory result this cycle.

## Persistence boundary

Persist only this handoff to:

- `analysis/orchestrator/latest.md`
- `analysis/orchestrator/state.json`
- append-only `analysis/orchestrator/history/2026-09-18/0957.md`

No immutable evidence, research experiment, workflow, formal identity, scheduler or non-designated `ops/*` file is modified by this Analyst run.