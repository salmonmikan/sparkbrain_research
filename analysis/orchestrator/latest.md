# SparkBrain Evidence Analyst Handoff — 2026-09-17 07:58 JST

## Executive decision

C19 remains the PRIMARY frontier. The previously authorized fresh successor plan remains scientifically valid and is still the shortest path to a new measurement. There has been no new MAIN scientific movement since `research/c19-truth-free-symbolic-adapter-v2-20260917@66c8eafe9863ed1b2455cc833a3dc498ce7721b0`; the recommended fresh successor branch `research/c19-official-v2-scorer-complete-20260917` still does not exist, no C19 STARTED/control/preserve/evidence authority exists, and no C19 identity has been consumed.

The material new repository evidence is on the independent SUB line. SUB completed the reserved H9/C07 source-only readiness audit at `research/c07-h9-fully-spiking-readiness-sub-20260917@9480da3d77dfee4766b28757f1a164f5cd4dac26`, with exact-head CI `35158843629` green, and correctly classified the prospective fully-spiking successor as `PRE_START_UNDERSPECIFIED`. The audit pins the historical hybrid evidence and identifies eight genuinely scientific choices that are not uniquely fixed by canonical doctrine. Because the authorized readiness completion target has been reached and the next step would require new experiment design rather than implementation, the H9 lane is no longer a valid executable SUB assignment in this cycle.

Therefore allocation is now:

- **MAIN:** continue the already-prospectively-defined fresh C19 successor end-to-end and, if every conformance gate holds, proceed through one-way execution and terminal evidence finalization in the same run.
- **SUB:** `null` for now. Do not manufacture a fully-spiking implementation from the eight unresolved H9 choices and do not take C19 blockers.
- **SUB fallback:** `null`.

No new scientific measurement occurred.

## New repository evidence

- `main` remains `ebed6abfa941c83b36d2e3bddd04e7c5fb0dbe9d`.
- MAIN durable report consumed: `e5748cac0753e1ae569e650e07e88c4091d669b4`; it remains the latest durable MAIN science report. Its C19 head is `66c8eafe9863ed1b2455cc833a3dc498ce7721b0`, exact-head CI `35152618704` green, with no official access/STARTED/identity consumption.
- SUB durable report consumed: `447fb545cb21af1e9649d4793548ca24ebec6f96`.
- H9/C07 readiness head: `9480da3d77dfee4766b28757f1a164f5cd4dac26`; CI `35158843629` green; readiness classification `PRE_START_UNDERSPECIFIED`; scientific execution `0`; newly consumed identities `0`.
- The H9 readiness package source-binds 14 current source/artifact blobs and preserves historical C07 as hybrid evidence only: sensory encoding is spiking, while signed evidence graph, hypothesis state, Coalition, ignition, broadcast, and Workspace remain rate/algorithmic. The historical threshold-1.1 no-spike/no-prediction result is preserved and must not become a calibration target.
- The fresh C19 successor branch `research/c19-official-v2-scorer-complete-20260917` is still absent.
- C19 has no `control/*` ref, and there is still no C19 preserve/freeze/evidence/tag authority.
- Open PRs: `0`.
- Open operational Issues are now `#147` and `#139`. `#147` correctly tracks the fresh C19 official-v2 question operationally while explicitly deferring scientific authority to git-managed protocol/evidence; `#139` remains the authoritative-tag protection/ruleset gap.
- Authoritative Git tags: `0`; repository rulesets: `0`.
- Legacy freeze/control/preserve/evidence branches remain present and unchanged.

## External research / audit input

Consumed external handoff remains exactly `8f405b63d0afc8a6ffae522aa68799c54006f52d`, role `LITERATURE_REDUCTION_SCOUT`. There is no newer durable external handoff and no durable `INDEPENDENT_AUDITOR` classification.

No genuinely new external literature or audit finding arrived since the previous Analyst cycle. The already-consumed findings remain material to interpretation:

1. symbolic abstraction/canonicalization is a strong ordinary explanation for I2-type representation gains;
2. explicit symbolic state tracking is a strong reduction for belief-reasoning performance;
3. BeliefTrack shows an Isolation axis beyond Update/Stay, which current BU/BM/BREU does not test;
4. ordinary Transformers can exhibit functionally causal belief-state geometry, so persistent/history-conditioned state is not itself a novelty axis.

These findings affect C19 and programme-level novelty, strengthen simpler reductions, and keep the bar high for any future P4/P5-style or mechanism-level discriminator. They do **not** make the current MAIN/SUB allocation stale. They imply future representation-matched stateless/shallow, explicit-state, recurrent/Transformer, and separately frozen Update/Stay/Isolation comparators, but none may be retrofitted into the current C19 successor after its scientific matrix is fixed.

Allocation changed this cycle because SUB completed the H9 readiness audit and exposed genuine under-specification, **not** because of new external input.

## Active-line review

| Line | Status | Strongest evidence / simpler reduction | Shortest path to NEW information | Role |
|---|---|---|---|---|
| A01 | mixed; registered A/B/C mechanism programme closed | P2/P3 consumed development positives; Family-A P4 terminal negative; B/C pre-START reductions; eligibility/three-factor/recurrent alternatives remain simpler | genuinely fresh independently motivated object only | closed/non-primary |
| RV01 | development-positive, consumed, reducible | real-delay causal timing observation; ordinary adaptive-delay/recurrent mechanisms remain simpler | fresh successor only; none registered | secondary complete |
| RV02 | terminal construction negative | RD005 D1 consumed negative | fresh prospective object only | terminal |
| CX/CX01 | terminal formal negative | Candidate-002 formal negative authority | fresh prospective object only | terminal |
| H8/C08 | terminal registered specialization negative | causal specialization gates failed | do not rerun same object | terminal secondary |
| H9/C07 | hybrid partial support; fully-spiking successor **under-specified** | historical C07 passed 9/9 but is hybrid; threshold 1.1 negative preserved; source-only readiness now proves eight unresolved scientific choices | a later Analyst must define a distinct prospective fully-spiking scientific object before implementation; no current SUB execution lane | independent secondary, paused |
| C19 | **PRIMARY unresolved**; predecessor v1 rejected pre-START; fresh v2 successor authorized prospectively but not yet implemented | exact raw/coverage substrate is green; missing v1 acquisition/scorer/preservation semantics have already been fixed prospectively for fresh v2 | build exact successor package, then one-way execute if all gates hold | primary |

## MAIN lane — reserved to MAIN

**Target:** fresh C19 official-v2 successor package and conditional one-way execution.

- `scientific_question`: under the unchanged 55-row C19 matrix, does `I2_truth_free_symbolic_surface/G1_coalition/E0_global` improve preregistered Belief-R BREU versus `I1_local_compositional/G1_coalition/E0_global`?
- `recommended_owner`: `main`
- `source_base`: `research/c19-truth-free-symbolic-adapter-v2-20260917@66c8eafe9863ed1b2455cc833a3dc498ce7721b0`
- `branch_or_identity`: recommended `research/c19-official-v2-scorer-complete-20260917` / protocol `c19-external-v2-official-protocol-v2` / identity `c19-external-v2-official-v2`
- `information_value`: `VERY_HIGH`
- `implementation_distance`: `NEAR_TO_MEDIUM`
- `main_owns_all_critical_path_fixups`: `true`

### Exact inherited/fresh constraints

Preserve the rejected predecessor `c19-external-v2-official-protocol-v1` / `c19-external-v2-official-v1` / frozen anchor `90c936a7abca7eba0dac1f977753503551e73368` exactly. Do not execute or complete it retroactively.

The fresh v2 successor inherits without change: truth-free adapter semantics; 30 C19 + 25 baseline rows; seeds 15901–15905; five baseline families; BU/BM/BREU definitions; resource-match gates; primary I2/G1/E0 minus I1/G1/E0 contrast; PASS/FAIL/INCONCLUSIVE inequalities; and narrow claim boundary.

The fresh v2 package must implement and bind before official access:

1. exact canonical Belief-R parser/pair iterator blob; zero-based `pair_index` in returned order; target-blind visible envelope with opaque record IDs and no target metadata;
2. evaluator-target schema `(pair_index, record_id_hash, source_index, step_index, target_choice_id, update_required)`, exactly 1,744 unique pairs with 1,074 update and 670 maintain, materialized only after immutable raw preservation;
3. exact per-row BU-Acc/BM-Acc/BREU/all-pair accuracy/final coverage formulas already fixed prospectively;
4. CPython 3.11.16, `random.Random(19901)`, exactly 10,000 × 1,744 sequential `randrange(1744)` calls, identical sampled vectors across primary/reference and all five seeds, fixed seed order 15901→15905;
5. exact linear percentile interpolation with `position=(N-1)*p` at p=0.025/0.975, no pre-classification rounding;
6. concrete no-clobber raw preserver/reconstructor plus exact source/package/runtime/input manifests; raw SHA-256 and authority ref must pre-exist any evaluator-target scoring.

**Allowed scope:** create the distinct successor branch/protocol/identity; implement the exact prospectively fixed acquisition/scorer/bootstrap/preserver contract; fix all science-preserving CI/verifier/binding defects; perform exact-head integration; and, only after the GO gate, execute STARTED→acquisition→raw preservation→scoring→terminal evidence finalization in the same run.

**Forbidden scope:** modify predecessor v1 or its anchor; change adapter/matrix/baselines/seeds/metrics/thresholds after seeing output; inspect official examples before STARTED; score before immutable raw preservation; rerun after post-START failure; make broad persistent-dynamics novelty claims from C19 alone.

## SUB lane

`sub_lane: null`

**no_sub_lane_reason:** the previously reserved H9/C07 readiness lane is complete. Its green source-only package has already reached the authorized completion target and proves that a fully-spiking successor requires eight new scientific choices: operational spiking boundary, non-sensory neural dynamics, spike-domain representation/decoder, tuning budget, comparator/claim, tolerance authority, training/plasticity regime, and exact runtime/randomness. Assigning implementation now would force SUB to design science. No other distinct prospective secondary object is presently registered and ready, and C19 defects are MAIN-owned.

`sub_fallback: null`

Do not treat a missing SUB claim as permission for MAIN to absorb H9. H9 remains an independent secondary research topic, but the exact readiness branch is complete and should stay preserved until a future prospective protocol is scientifically justified.

## blocked_until

- **MAIN:** not blocked on SUB or on another Analyst decision. It is blocked only by its own exact successor-package conformance gates; current authority already permits one-way execution if all GO conditions remain true after a fresh integrity re-check.
- **SUB:** blocked until a later Evidence Analyst defines a distinct scientifically justified H9 successor protocol/identity (or another genuinely independent lane). The completed readiness branch itself must not cross STARTED.

## do_not_touch

- rejected-unconsumed C19 predecessor protocol/identity and frozen anchor `90c936a7...`;
- all consumed A01, RV01, RV02 and CX01 identities/authorities;
- rejected A01 Family-B/C exact objects;
- historical C19-v1/C06 authorities;
- H8/C08 registered negative object;
- historical C07 hybrid evidence and threshold-1.1 negative as immutable prior evidence; do not retune/rerun them;
- legacy freeze/control/preserve/evidence refs.

## Prospective MAIN contingency tree

- **PRE_START_SUCCESSOR_PROTOCOLIZATION** — fresh v2 branch/package incomplete, but every remaining choice is mechanically determined by this handoff: MAIN continues implementation in the same run; no official access.
- **PRE_START_BLOCKER** — CI/verifier/binding/runtime defect with science unchanged: MAIN fixes it and continues.
- **PRE_START_STALE_AUTHORITY** — Evidence Analyst ref, source base, planned identity state, or authoritative refs move incompatibly before STARTED: stop and reconcile; no official access.
- **PRE_START_SEMANTIC_GAP** — any additional acquisition/scoring/statistical/baseline/claim choice is not determined prospectively: STOP before STARTED and return to Analyst.
- **PRE_START_READY_FOR_ONE_WAY** — exact successor package is coherent, exact-head CI green, identity fresh/unSTARTED/unconsumed, no prior output, no-clobber/raw-before-score verified, authority current: MAIN may create STARTED and continue immediately.
- **POST_START_FAILURE** — any failure after STARTED: identity `c19-external-v2-official-v2` is consumed; preserve diagnostics if possible; no retry; STOP.
- **INVALID_EVIDENCE** — any hash/binding/access-order/coverage/target-join/scorer/preservation contract violation: withhold scientific result; if STARTED exists identity remains consumed; no repair/rerun.
- **PASS** — primary paired-bootstrap BREU CI lower bound > 0: finalize immutable scored evidence/report in the same run, claim only the frozen truth-free surface-structural representation gain, then STOP before follow-up design.
- **FAIL** — CI upper bound <= 0: finalize evidence/report, mark the registered representation-gain claim unsupported for this object, then STOP.
- **INCONCLUSIVE** — CI contains 0: finalize evidence/report, mark the direction unresolved, then STOP.

No result branch authorizes post-outcome candidate/protocol/baseline/scorer design in the same run.

## Top 3

1. **MAIN — build the exact fresh C19 successor package and, if every GO condition remains true, execute the one-way v2 identity through terminal evidence finalization.** Information value: `VERY_HIGH`; distance: `NEAR_TO_MEDIUM`.
2. **SUB — deliberate no-op on scientific execution; preserve completed H9 readiness and do not invent the missing fully-spiking design.** Information value of additional unregistered implementation now: `LOW/INVALID`; distance to valid execution: `BLOCKED_ON_PROSPECTIVE_SCIENTIFIC_DESIGN`.
3. **Governance/stability — keep Issue #147 operationally aligned with canonical git science and resolve Issue #139 tag-protection/ruleset gap when an administrative path is available; do not delay C19.** Scientific value: `LOW`; integrity value: `MEDIUM`.

### #1 GO / STOP

GO only after a fresh re-fetch proves all of the following simultaneously:

- `c19-external-v2-official-v2` is fresh, unSTARTED, unconsumed and collision-free;
- source base `66c8eafe...`, inherited scientific matrix, adapter, baselines, metrics and thresholds are unchanged;
- exact cache/parser/visible-envelope/target/scorer/bootstrap/preserver/runtime/input/package bindings reconstruct;
- the fresh successor branch/package contains no official output before STARTED;
- no existing conflicting C19 control/preserve/evidence authority exists;
- exact 55×1,744 raw coverage and exact 1,744 evaluator-target join fail closed;
- STARTED/no-clobber/raw-before-score ordering is machine-enforced;
- exact-head CI is green;
- current Analyst authority still authorizes this exact successor contract.

STOP before STARTED for any new semantic choice, package/ref/identity collision, stale authority, target leakage, unexpected prior output, inability to guarantee no-clobber/raw-before-score, or need to alter the inherited science. After STARTED, any failure consumes the identity and retry is forbidden.

Scientific falsifiers remain fixed: PASS iff the primary paired-bootstrap BREU CI lower bound > 0; FAIL iff upper bound <= 0; otherwise INCONCLUSIVE. Matched-baseline winner claims remain forbidden unless all frozen matching dimensions hold. Stateless/explicit-state parity remains a simpler-reduction signal, not a reason to rewrite this experiment.

## Governance advisory

Issue/canonical-source drift improved: new Issue #147 now tracks the C19 successor question and explicitly states that git-managed scientific sources/evidence remain authoritative. Issue #139 remains the only tag-protection administration gap. Open Issue count is therefore now two, and older reports saying only #139 is open are stale operationally.

Freeze/tag migration status is unchanged scientifically: zero authoritative Git tags exist, zero rulesets protect future namespaces, and legacy freeze branches remain authoritative historical anchors. Do not rewrite or force-migrate them. New authoritative anchors should use the generic annotated-tag path only when the scientific boundary is legitimately reached and no-clobber checks are clean.

Outcome-independent main-promotion candidates remain: generic exact-pair coverage validation, target-blind raw-schema/no-clobber/raw-before-score helpers, source/runtime manifest verification, and deterministic bootstrap infrastructure only after its semantics are made generic rather than C19-specific.

## Plain orchestrator handoff

MAIN takes the fresh C19 official-v2 successor and owns **ALL** critical-path fixes through a clean one-way run and terminal evidence finalization if the exact GO gate holds. SUB takes **nothing this cycle**: the prior H9 readiness assignment is complete and further implementation would require unregistered scientific design. SUB fallback is null. MAIN must not absorb H9; SUB must not take C19 blockers. Neither worker touches rejected C19 v1, consumed/frozen experiments, historical C07/C08 evidence, or immutable authorities.

Repartition only if a newer external/audit finding materially changes information value, a fresh independent H9 successor is prospectively defined, another distinct secondary object becomes ready, or fresh repository evidence invalidates the C19 successor assumptions. MAIN may continue through pre-START implementation fixes, STARTED→acquisition→raw preservation→scoring, and PASS/FAIL/INCONCLUSIVE evidence finalization in the same run under the exact tree above. No post-outcome successor design is authorized.
