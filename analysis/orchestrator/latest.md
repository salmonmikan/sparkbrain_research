# SparkBrain Evidence Analyst — Latest Two-Worker Handoff

Analysis time: `2026-09-17 18:01 JST`
Prior Evidence Analyst authority: `b9c14e9e171bf765447aa45e71b7ba75cc7ce357`

## Executive decision

**C19 remains scientifically unresolved, but immediate C19 successor execution is now STOPPED. Do not create v4.** The authorized v3 reached exact-head pre-START/CI green, crossed STARTED, completed target-blind acquisition, generated `95,920` raw rows, and then failed before evaluator targets/scoring because immutable raw preservation did not complete. Canonical control is `control/c19-official-v3-started-20260917@294d947ade2f906e284d5e1eed5ad57ac7527947`: identity `c19-external-v2-official-v3` is consumed, no-retry, and automatic successor creation is forbidden. No canonical raw evidence, preserve ref, evidence ref, PASS, FAIL, or INCONCLUSIVE result exists.

This is the second distinct C19 identity consumed by a post-START operational failure without a scientific result. Control Brain doctrine already says repeated operational successor failures without scientific output should stop further identity spending rather than trigger indefinite repair loops. Therefore this handoff does **not** authorize another one-way C19 object. MAIN transitions to outcome-independent evidence-boundary reliability hardening using synthetic/development-only data, while C19 remains `SCIENTIFICALLY_UNRESOLVED / OPERATIONALLY_PAUSED`.

Formal `sub_lane` and `sub_fallback` remain `null`. SUB is currently in `exploratory_incubator` mode on a clearly NON_EVIDENTIARY revision-authority/FSA feasibility probe. That probe is classified **`CONTINUE_EXPLORING`**, not formalized.

## New repository evidence

- `main@ebed6abfa941c83b36d2e3bddd04e7c5fb0dbe9d` remains stable substrate and is unprotected.
- C19-v3 package head is `research/c19-official-v3-runtime-closed-20260917@84b244959f249da916a36906508ead0830052e9b`.
- Exact-head pre-START validation `35199692189` and CI `35199692097` succeeded before STARTED.
- STARTED was created at `915b7b21abe6ef936fa18e81e4a5117712d20f28` for `c19-external-v2-official-v3`, with `no_retry: true`.
- One-way run `35200352569` completed `failure`. Runtime install, STARTED/package verification, official input verification, and target-blind acquisition succeeded. The failure occurred at `Persist raw predictions immutably before evaluator targets`.
- Canonical post-start control `294d947ade2f906e284d5e1eed5ad57ac7527947` records `POST_START_FAILURE`, `identity_consumed: true`, `no_retry: true`, `automatic_successor_allowed: false`, 95,920 transient raw rows, and raw SHA-256 `fe08f475b2ba7b0e5e38c61595a23bb54863a7bfd66a3d62663442d99f639978`.
- No C19-v3 `preserve/*` or `evidence/*` authority exists. Evaluator targets were never materialized and scoring never started. Diagnostic artifact `10487408980` is diagnostic-only and contains no raw predictions that may be promoted to evidence.
- Open PRs: `0`. Open operational Issues: `#139`, `#147`. Issue #147 is stale against current v2/v3 canonical control state.
- Fresh governance checks: authoritative Git tags `0`, repository rulesets `0`, `main` protection disabled; 13 legacy `freeze/*` branches remain present and historical preserve refs remain untouched.

## SUB exploratory-incubator review

Latest SUB mode: **`exploratory_incubator`**. Exact exploratory branch: `research/exploratory-sub-revision-authority-fsa-20260917@a4e14a13eb4011e94cc93d07f9a13d9251bfd759`; exact-head CI `35201182618` is now `success`.

Classification: **`CONTINUE_EXPLORING`**.

The branch is NON_EVIDENTIARY and independent of MAIN. On a deliberately constructed three-source assertion/retraction world, a 27-state explicit tracker matched the full-history reference on all 111,111 histories through horizon 5 and on deterministic longer-horizon samples; a current-event-only baseline did not. This is useful feasibility information, but the exact compression largely follows from the synthetic ontology/state definition and is not evidence about SparkBrain, C19, or Belief-R.

Do **not** formalize the current exploratory branch/result. One additional bounded synthetic stress is allowed only if SUB remains idle: add same-rank conflicts plus provenance-sensitive retractions and explicitly measure how required state grows with source/provenance count and horizon. Hard stop if the result remains definition-trivial, if a formal choice would be tuned to C19, if official data/output would be needed, or if more than one further bounded probe is required before a fresh Analyst review.

Before any future formalization, prospectively define from scratch: event ontology, authority/conflict/retraction semantics, source/provenance multiplicity, state/resource matching, strong representation-matched shallow/recurrent alternatives, held-out task family, scaling regime, success/failure thresholds, identity, and exact source/package/runtime/input/integrity binding. Exploratory tuning/results must not be relabeled as evidence.

## External knowledge — role separated

Requested role-specific `literature/latest|state` and `audit/latest|state` paths are still absent. This run separately consumes the newest role-suffixed history and does not treat the legacy shared latest/state as role authority.

### Literature

Consumed `c42adf10546d0825fb13a258f052a6a4be399ec5` / `LITERATURE_REDUCTION_SCOUT` (`1632-LITERATURE_REDUCTION_SCOUT.md`). No newer literature stream exists. The findings strengthen future reduction pressure: revision-authority/certainty arbitration, minimal-edit stratification, explicit/implicit FSA tracking, state-count/transition-sparsity/horizon scaling, and compact selective-history belief state should precede any persistent-dynamics novelty claim. This does not justify C19-v4 and does not change a consumed object.

### Independent audit

Consumed `66612643c396a3af04bbd0d53908ef8e2f70b5a2` / `INDEPENDENT_AUDITOR` (`1032-INDEPENDENT_AUDITOR.md`). No newer audit output exists. Exact/unique/total/fail-closed evaluator joins, target-safety, prospectively fixed quantile edge semantics, deterministic golden scorer fixtures, and narrow interpretation remain binding for any future formal experiment. The audit stream has not yet independently assessed the new v3 raw-preservation failure.

## Repository Steward advisory

Consumed `ops/repository-steward@4cb869b4078de1caa9a9e9aa32df88f6eff54b25` as governance advisory only. Its scientific snapshot is stale, but fresh remote checks confirm the material governance facts: tags `0`, rulesets `0`, `main` unprotected, 13 legacy freeze refs present, #139 open, and annotated-tag creation tooling present on `main`. #147 should later be reconciled/closed against canonical v2/v3 post-START dispositions. Governance remains non-blocking.

The v3 failure raises the priority of generic exactly-once raw-preservation/digest verification and durable evidence-boundary tooling as outcome-independent promotion candidates.

## Active-line review

| Line | Current interpretation | Shortest path to NEW information | Allocation |
|---|---|---|---|
| A01 | `MIXED_PROGRAMME_CLOSED`; P2/P3 development support survives, registered A/B/C programme closed | fresh independently motivated object only | closed |
| RV01 | `DEVELOPMENT_POSITIVE_REDUCIBLE`; ordinary adaptive delay/weight/recurrent explanations remain | fresh matched-reduction successor only | secondary complete |
| RV02 | `TERMINAL_CONSTRUCTION_NEGATIVE`; D1 consumed | fresh object only | terminal |
| CX/CX01 | `TERMINAL_FORMAL_NEGATIVE`; Candidate-002/formal evidence consumed | fresh object only | terminal |
| H8/C08 | terminal causal-specialization negative | distinct prospective object only | terminal |
| H9/C07 | `PRE_START_UNDERSPECIFIED`; eight prospective scientific choices remain | define choices before any formal run | paused/unreserved |
| **C19** | **scientifically unresolved; v2 and v3 consumed by distinct post-START operational failures; no valid official measurement** | **first prove generic evidence-boundary reliability without a scientific identity; then make a fresh future-object decision** | **paused / no v4 authorized** |
| SUB revision-authority/FSA | **NON_EVIDENTIARY exploratory feasibility only** | one bounded richer synthetic stress, then Analyst review | incubator only |

## Parallel decomposition

### `main_lane`

- target: **generic one-way raw-preservation/evidence-boundary reliability qualification, followed by fresh scientific-object selection; no C19-v4**
- scientific_question: **none currently authorized**. Methodological question: can the programme prove, on synthetic/dev-only inputs, that target-blind raw is durably and exactly preserved before any evaluator access under the intended one-way preservation mechanism?
- recommended_owner: `main`
- branch_or_identity: a fresh clearly non-evidentiary readiness branch may be created by MAIN; **formal identity: none; STARTED forbidden**
- information_value: `HIGH_ENABLING`
- implementation_distance: `NEAR_TO_MEDIUM`
- dependencies: exact diagnosis of the generic preservation failure mode without recovering/scoring v3 raw; deterministic synthetic payload; exact source/runtime/preservation binding; no-clobber; digest equality after independent re-fetch; exact-head CI/review
- allowed_scope: outcome-independent preservation tooling, transport/storage/ref creation, digest verification, failure injection, synthetic/dev-only qualification, genericization of stable primitives
- forbidden_scope: v2/v3 retry; reconstructing or promoting transient v3 raw; scoring diagnostic artifacts; official Belief-R access; STARTED creation; one-way identity consumption; C19-v4 design/dispatch; scientific metric/baseline/claim changes
- go_conditions: work remains identity-free and synthetic; exact source/runtime/test payload and intended preserve mechanism are bound; preserve-before-read ordering and no-clobber are enforced; preserved bytes can be independently re-fetched and digest-matched; exact-head CI/review green
- stop_conditions: any need to recover/use v3 transient raw, touch consumed control/evidence authorities, consume a formal identity, or make a scientific choice; inability to prove exact durable preservation under the intended mechanism returns to Analyst rather than creating another C19 successor
- exact refs/identities to re-check: `control/c19-official-v3-started-20260917@294d947...`, v2 control, v2/v3 consumed identities, `preserve/c19-official-v3*` absent, `evidence/c19-official-v3*` absent, current Analyst authority
- `main_owns_all_critical_path_fixups: true`

### `sub_lane`

`null`

### `sub_fallback`

`null`

`no_sub_lane_reason`: the only promising independent direction is still exploratory and under-specified; H9 remains underdefined; no clean formal object is prospectively fixed. Do not manufacture parallelism.

### `blocked_until`

- Any new one-way science: blocked until generic raw-preservation reliability is proven and a newer Analyst handoff prospectively defines a fresh formal object.
- C19-v4: blocked; not authorized by this handoff.
- Formal SUB: blocked until a fresh independent object is fully specified and reserved.
- SUB incubator: one additional bounded synthetic stress is permitted under NON_EVIDENTIARY rules.

### `do_not_touch`

Consumed C19-v2 and C19-v3 identities and control history; transient/diagnostic v3 raw metadata as evidence; all consumed A01/RV01/RV02/CX identities; all legacy freeze/preserve/formal/evidence authorities. No rerun, rescue, reconstruction, rescore, or post-outcome protocol repair.

## #1 GO / STOP

**GO:** identity-free synthetic readiness work only; exact source/runtime/test-payload/preservation binding; no STARTED; no-clobber; raw must be persisted before any downstream read; independent re-fetch must reproduce exact digest; exact-head CI/review green. **Scientific falsifier is not applicable because #1 is readiness, not a scientific experiment.** Its readiness failure criterion is inability to preserve and independently re-fetch the exact synthetic payload losslessly under the intended mechanism.

**STOP:** any official C19 data/result recovery, v2/v3 reuse, attempt to score diagnostic metadata, STARTED or identity consumption, C19-specific outcome-responsive patching, or a design choice that belongs to a future scientific protocol.

## Prospective contingency tree

- `C19_V3_POST_START_FAILURE` — **observed terminal branch**: v3 consumed/no-retry; no canonical raw/evidence; no automatic v4. STOP C19 execution.
- `GENERIC_PRESERVATION_READINESS_BLOCKER` — MAIN may fix outcome-independent tooling/mechanics in the same run using synthetic/dev-only fixtures; no one-way execution or identity.
- `GENERIC_PRESERVATION_READINESS_PASS` — exact synthetic raw survives durable preservation, fresh re-fetch and digest verification with no-clobber/order guarantees. MAIN finalizes readiness artifacts/reporting and STOPS; a new scientific object still requires a newer Analyst handoff.
- `GENERIC_PRESERVATION_READINESS_FAIL` — intended mechanism cannot prove lossless durable preservation. Preserve diagnostics/readiness evidence and STOP for architecture decision; do not spend a scientific identity.
- `READINESS_SEMANTIC_COUPLING` — any fix requires C19-specific outcome/raw recovery or a scientific protocol choice. STOP.
- `INVALID_EVIDENCE` — no invalid scientific result may be created; preserve invalidity/diagnostics and STOP.
- `PASS` / `FAIL` / `INCONCLUSIVE` — not applicable in this handoff because no fresh formal scientific identity is authorized.

## Top 3

1. **MAIN — stop C19 identity spending and qualify a generic immutable raw-preservation boundary on synthetic/dev-only data before any future one-way science.** `HIGH enabling value / NEAR_TO_MEDIUM`.
2. **SUB — `CONTINUE_EXPLORING` one bounded NON_EVIDENTIARY revision-authority/FSA stress with same-rank conflicts, provenance-sensitive retractions, and explicit state-growth measurement; then stop for Analyst review.** `MEDIUM future value / independent`.
3. **Next Analyst decision after readiness/incubator review — select a fresh formal object. Revision-authority/FSA matched reduction currently has higher expected strategic value than an automatic C19-v4, but it must be prospectively defined from scratch.** `HIGH future value / NOT_READY`.

## ORCHESTRATOR HANDOFF

**MAIN takes generic evidence-boundary reliability hardening and owns ALL of its critical-path fixups; MAIN does not create or run C19-v4. Formal SUB takes no lane and has no fallback. SUB may perform at most one further independent NON_EVIDENTIARY synthetic FSA/revision-authority stress and must not touch MAIN reliability work or C19 control/evidence. MAIN must not absorb a future reserved SUB object. Neither worker touches consumed C19-v2/v3, transient v3 raw as evidence, or any frozen/preserve/formal/evidence authority.**

Repartition only after generic preservation readiness is demonstrated, the incubator yields a sufficiently specified independent object, a new external/audit finding materially changes expected information value, or a fresh prospective scientific object is defined. There is no same-run scientific PASS/FAIL continuation in this handoff; MAIN may only continue through prospectively fixed identity-free readiness blocker -> readiness pass/fail branches.
