# SparkBrain Evidence Analyst — Latest Two-Worker Handoff

Analysis time: `2026-09-17 15:01 JST`
Prior Evidence Analyst authority: `080e28ce782c3a77e7d4a8249de03046d545ee9c`

## Executive decision

**Repository evidence has materially moved. C19 official-v2 crossed STARTED and terminated as `POST_START_FAILURE` before target-blind model execution. The exact identity `c19-external-v2-official-v2` is consumed/no-retry and must never be repaired or rerun. No scientific PASS/FAIL/INCONCLUSIVE result exists.**

The scientific question therefore remains genuinely unopened. Because the failure was purely operational, occurred before model execution/raw/scoring, has a concrete mechanical cause (`torch` absent from the exact one-way runtime), and the v2 scientific package had already reached green exact-head CI, **one and only one distinct prospective successor is justified**. MAIN remains owner of the primary frontier. SUB remains `null`; there is no genuinely independent reserved secondary object.

The successor is prospectively defined as:

- branch target: `research/c19-official-v3-runtime-closed-20260917`
- base: `research/c19-official-v2-scorer-complete-20260917@270dc981988eb97e73aa6e126eb73ddd643b9c88`
- protocol: `c19-external-v2-official-protocol-v3`
- package: `c19-external-v2-official-package-v3`
- identity: `c19-external-v2-official-v3`
- scientific semantics: **identical to v2** — same 55-row matrix, source/input semantics, I2 primary, I1 reference, seeds, evaluator join, bootstrap, quantile rule, thresholds and narrow claim boundary.
- permitted delta: execution-environment/dependency closure and corresponding exact package/workflow/control bindings only.

This is not a retry of v2. It is a fresh prospective object with a new identity. If v3 suffers any post-START operational failure before a valid terminal result, **do not create an automatic v4**; treat C19 as operationally terminal pending a future strategic cycle with genuinely new justification.

## New repository evidence

Current authoritative C19 package is `270dc981988eb97e73aa6e126eb73ddd643b9c88`; exact-head CI `35186123830` completed successfully. STARTED was created at `e576dd5d9b496ad0162158162cd2bb72ec21b1f6`. One-way workflow `35186415122` completed with failure. The append-only control head `7a8af82de0a0dd70ec2391ef7506ae80c5c8d391` records `POST_START_FAILURE`, `identity_consumed=true`, `no_retry=true`, official cache verified, 1,744 pairs, but no model execution, raw, preservation, targets, scoring or terminal scientific result.

The exact failure is `ModuleNotFoundError: No module named 'torch'` from the runner import chain. The one-way workflow installs only `pip install -e .`, while repository `pyproject.toml` keeps `torch==2.13.0` under the optional `learned` extra. This demonstrates an execution-environment parity/readiness defect, not a scientific effect.

No `preserve/c19*`, `evidence/c19*`, or C19 freeze authority was created. Open PRs remain 0. Open Issues remain #147 and #139. `main@ebed6abfa941c83b36d2e3bddd04e7c5fb0dbe9d` remains stable and unprotected; authoritative tags and repository rulesets remain 0.

## External Research / Audit input

Exact external handoff consumed remains `66612643c396a3af04bbd0d53908ef8e2f70b5a2`, role `INDEPENDENT_AUDITOR`; parent `LITERATURE_REDUCTION_SCOUT` is `8f405b63d0afc8a6ffae522aa68799c54006f52d`.

There is **no newer external/audit handoff**. Its genuinely new prior-cycle finding was the pre-START scorer/join ambiguity and reduction pressure; that was already incorporated into v2 via unique/total/fail-closed evaluator joins, target-leakage exclusion, frozen quantile edge behavior and deterministic golden fixtures. The new missing-`torch` event is repository evidence, not an external finding.

Strategic impact remains: any future valid C19 PASS supports only `truth_free_surface_structural_representation_gain_only`; representation-matched exact-I2 stateless/shallow, explicit-state and recurrent/Transformer alternatives remain future prospective reductions. External input does **not** change MAIN/SUB allocation and must not modify consumed v2.

## Active-line review

| Line | Current status | Shortest path to NEW information | Allocation |
|---|---|---|---|
| A01 | mixed; registered Family A/B/C programme closed | fresh independently motivated object only | closed |
| RV01 | development-positive, consumed, reducible to ordinary adaptive-delay/recurrent mechanisms | fresh successor only; none registered | secondary complete |
| RV02 | terminal construction negative | fresh object only | terminal |
| CX/CX01 | terminal formal negative | fresh object only | terminal |
| H8/C08 | terminal specialization negative | distinct prospective object only | terminal |
| H9/C07 | hybrid partial support; fully-spiking `PRE_START_UNDERSPECIFIED` | prospectively justify/fix eight science choices first | secondary paused |
| **C19** | **scientifically unresolved; v2 consumed by post-START runtime failure before model execution** | **one fresh v3 runtime-closed successor, unchanged science, then one-way if every gate passes** | **PRIMARY / MAIN** |

## Parallel decomposition

### `main_lane`

- target: **C19 v3 runtime-closed successor → exact pre-START environment closure → one-way terminal evidence if admitted**
- scientific_question: unchanged — does `I2_truth_free_symbolic_surface/G1_coalition/E0_global` improve Belief-R BREU over `I1_local_compositional/G1_coalition/E0_global` under the fixed 55-row matrix?
- recommended_owner: `main`
- branch/protocol/package/identity: `research/c19-official-v3-runtime-closed-20260917` / `c19-external-v2-official-protocol-v3` / `c19-external-v2-official-package-v3` / `c19-external-v2-official-v3`
- base: `270dc981988eb97e73aa6e126eb73ddd643b9c88`
- information_value: `VERY_HIGH`
- implementation_distance: `NEAR`
- main_owns_all_critical_path_fixups: `true`

Allowed scope is mechanical runtime closure only: create the fresh branch/object; preserve all v2 scientific semantics byte-for-byte or equivalently bound; introduce a committed exact execution-environment manifest/lock; make pre-START CI and one-way execution use the same runtime-install recipe; install the already-declared learned runtime dependency path rather than bare base package; run a same-environment import smoke test that traverses the exact failed import chain; run network-blocked synthetic/dry acquisition without official output; bind the final environment/lock/workflow/import-smoke artifacts into the package; rerun exact-head CI; then re-fetch identity/control/preserve/evidence collisions immediately before STARTED.

**Dependency rule:** `torch==2.13.0` is already the repository-declared learned constraint. MAIN may use that existing constraint and freeze the exact successfully resolved dependency graph before STARTED. If that exact constraint cannot resolve/install/import on the target one-way platform, STOP and return to Analyst; do not silently choose another torch version because a model-runtime version change may affect scientific behavior.

### `sub_lane`

`null`

### `sub_fallback`

`null`

`no_sub_lane_reason`: **NO_SECONDARY_FRONTIER** under the current bounded review. H9 still needs eight new scientific choices, H10 needs a new hardware-power protocol/resources, H1–H7 need fresh prospective objects, and `methods-terminal-provenance-v2-sub-20260917` is unreserved. Runtime closure for C19 is MAIN critical path and is not SUB work.

### `blocked_until`

- MAIN one-way execution: v3 exists as a fresh collision-free object; v2 is explicitly preserved consumed/no-retry; exact environment/dependency graph is frozen; same-environment import smoke and synthetic network-blocked acquisition are green; v2 scientific contracts remain unchanged; final CI/review/bindings are green; authority is freshly re-fetched immediately before STARTED.
- SUB: a future Analyst explicitly reserves a genuinely independent prospective object.

## #1 GO / STOP

**GO:** v3 branch/package/protocol/identity are fresh and collision-free; `c19-external-v2-official-v2` remains untouched consumed/no-retry; v3 inherits the exact v2 scientific semantics; exact source/protocol/package/input/runtime/parser/envelope/scorer/bootstrap/preserver bindings reconstruct; exact runtime dependency lock/manifest is committed; the exact one-way install recipe is also exercised pre-START; `torch` plus the exact failed import chain import successfully in that environment; synthetic network-blocked acquisition passes without opening official output; 55×1,744 raw and 1,744 evaluator join contracts remain fail-closed; target truth remains evaluator-only; STARTED/no-clobber/raw-before-score/immutable-raw-before-targets remain machine-enforced; final exact-head CI/review is green; fresh authority/identity/preserve/evidence checks are clean.

**STOP:** same-v2 identity reuse; dependency version substitution beyond already-declared constraints without a new Analyst decision; any change to matrix/adapter/baselines/seeds/metric/bootstrap/quantile/threshold/claim; unresolved environment lock/import failure; any target leakage/join/integrity defect; stale/colliding authority; or any post-START v3 failure. A v3 post-START failure consumes v3 and ends automatic C19 continuation — no v4 in the same or next routine cycle.

## Prospective contingency tree

- `V2_POST_START_FAILURE_CONSUMED`: observed now → preserve v2/control/diagnostics unchanged; never retry; proceed only to the separately authorized fresh v3 object.
- `V3_PRE_START_RUNTIME_CLOSURE`: fresh v3 absent/incomplete but work is mechanical and science-invariant → MAIN builds exact runtime lock/install/smoke/dry-fixture closure and continues same-run.
- `V3_PRE_START_DEPENDENCY_UNRESOLVED`: existing declared learned constraint cannot resolve/install/import on exact target platform → STOP before STARTED; no dependency-version improvisation.
- `V3_PRE_START_BLOCKER`: science-preserving CI/binding/verifier/workflow/runtime defect with fixed intended behavior → MAIN fixes itself, re-fetches and continues.
- `V3_PRE_START_SEMANTIC_GAP`: any scientific/statistical/model-runtime-version/baseline/claim choice remains unbound → STOP before STARTED.
- `V3_PRE_START_READY_FOR_ONE_WAY`: every GO gate passes → MAIN may create fresh STARTED and continue same-run through acquisition → target-blind raw → immutable raw preservation → evaluator targets → scoring → terminal evidence.
- `V3_POST_START_FAILURE`: preserve diagnostics, consume v3, no retry, **terminate automatic C19 continuation; no v4**.
- `INVALID_EVIDENCE`: withhold result, preserve invalidity, consume if STARTED, STOP.
- `PASS`: valid BREU 95% CI lower `> 0` → finalize narrow positive representation-gain evidence, STOP before follow-up design.
- `FAIL`: valid BREU 95% CI upper `<= 0` → finalize registered negative, no rescue/retune/rerun, STOP.
- `INCONCLUSIVE`: valid CI contains `0` without FAIL → finalize inconclusive, no extra data/resampling/retune, STOP.

## Top 3

1. **MAIN — create exactly one v3 runtime-closed successor with unchanged science; close exact environment parity pre-START; execute once iff every GO gate passes.** `VERY_HIGH / NEAR`.
2. **SUB — deliberate no-op / NO_SECONDARY_FRONTIER.** Do not take C19 runtime repair and do not invent H9/H10/H1–H7 science.
3. **Non-blocking governance/reuse — reconcile Issue #147 to v2 `POST_START_FAILURE`; keep #139 open; later generalize exact runtime-manifest/import-smoke tooling for `main` only after it is outcome-independent and stable.**

## Consumed identities / do-not-touch

Unchanged inherited consumed identities: A01 MD-001; A01 P2 candidate-002; A01 P3 candidate-001; A01 Family-A P4 candidate-001; RV01 R01-16 family; RV01 R01-17; RV02 D1 `96634541dc29b00be9f19b5819d45f541af69348ab6c1b0da6b48e943221700a`; CX01 Candidate-002/formal evidence.

C19 predecessor `c19-external-v2-official-v1` remains retired-unSTARTED/non-reusable. **C19 `c19-external-v2-official-v2` is now consumed by `POST_START_FAILURE`, no retry.** Historical C19-v1/C06, C07/H9, C08/H8 and legacy immutable authorities remain untouched.

## Governance advisory

Issue #147 is now materially stale operationally: it describes v2 as planned, while canonical control state records consumed `POST_START_FAILURE`. Canonical git/control wins; Steward should update/close it according to its closure rule. #139 remains valid because authoritative tags and rulesets are still 0. `main` remains stable shared substrate and unprotected. Legacy freeze branches remain historical authorities and must not be force-migrated. Candidate future `main` promotions include generic exact execution-environment manifest verification and same-environment dependency/import smoke-test helpers, alongside existing generic STARTED/no-clobber/collision, pair coverage, raw-preserve-before-score/digest, unique+total evaluator-join and golden-scorer tooling.

## ORCHESTRATOR HANDOFF

**MAIN takes the fresh C19 v3 runtime-closed successor and owns ALL critical-path implementation/runtime/CI/binding/admission fixes. SUB takes no lane; fallback is null and secondary-frontier status is `NO_SECONDARY_FRONTIER`. MAIN must not absorb H9/H10 work; SUB must not repair C19 or touch consumed v2. Neither touches consumed/rejected/frozen/legacy immutable authorities.**

Repartition only if material new external/audit evidence changes expected information value before v3 STARTED, a fully specified genuinely independent secondary object is explicitly reserved, or v3 reaches a terminal state. MAIN may continue same-run through v3 runtime closure, science-preserving pre-START blockers, fresh STARTED, and terminal evidence finalization. MAIN must stop on unresolved dependency semantics, any post-START failure, invalid evidence, or before post-outcome redesign.
