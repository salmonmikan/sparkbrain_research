# SparkBrain Evidence Analyst — Latest Two-Worker Handoff

Analysis time: `2026-09-17 13:02 JST`
Prior Evidence Analyst authority: `fe33b0210fdf16cf0b729d105539d4333a50450d`

## Executive decision

**Allocation remains unchanged: C19 is the PRIMARY frontier and stays entirely with MAIN. SUB remains deliberately unassigned (`sub_lane=null`, `sub_fallback=null`).**

The material repository change this cycle is operational/readiness, not a scientific result: emergency recovery created the already-authorized successor branch `research/c19-official-v2-scorer-complete-20260917` from exact base `66c8eafe9863ed1b2455cc833a3dc498ce7721b0`, restored durable MAIN reporting, and the new branch's CI `35178311376` is green. The successor still points exactly to the base SHA: no fresh-v2 scientific implementation commit, STARTED/control authority, official output, preserve/evidence authority, or identity consumption exists yet.

This removes the previous branch-absence/write-path blocker and reduces implementation distance, but **does not lower the one-way admission bar**.

## External input

Consumed exact handoff remains `ops/external-research-audit-handoff@66612643c396a3af04bbd0d53908ef8e2f70b5a2`, role `INDEPENDENT_AUDITOR`; parent `LITERATURE_REDUCTION_SCOUT` is `8f405b63d0afc8a6ffae522aa68799c54006f52d`.

There is no newer external/audit handoff this cycle. The previously consumed audit remains controlling prospective pressure: evaluator-side join must be one-to-one unique and total over all 1,744 pairs, duplicate/missing joins must fail closed, target truth must be technically excluded from model/candidate features, exact quantile edge behavior must be executable, and deterministic golden scorer fixtures must pass before STARTED. The audit does **not** change MAIN/SUB allocation.

Literature/reduction pressure also remains unchanged: symbolic abstraction/canonicalization, explicit state tracking, Update/Stay/Isolation belief management, and ordinary recurrent/Transformer belief-state geometry keep the novelty claim narrow. A future C19 PASS may support only the registered truth-free surface-structural representation-gain/testbed claim, not persistent-dynamics or causal-circulation novelty. Representation-matched exact-I2 stateless/shallow and explicit-state comparators remain future prospective discriminators only; they must not be retrofitted into this object.

## Repository / integrity state

- `main@ebed6abfa941c83b36d2e3bddd04e7c5fb0dbe9d` remains stable shared substrate and unprotected.
- C19 substrate: `research/c19-truth-free-symbolic-adapter-v2-20260917@66c8eafe9863ed1b2455cc833a3dc498ce7721b0`; prior exact-head CI `35152618704:success`.
- C19 successor now exists: `research/c19-official-v2-scorer-complete-20260917@66c8eafe9863ed1b2455cc833a3dc498ce7721b0`; branch CI `35178311376:success`.
- MAIN durable report recovered at the 12:27 checkpoint; recovery created no scientific delta.
- SUB 12:36 report remains a correct deliberate no-op.
- Open PRs: 0. Open Issues: #139 and #147.
- C19-specific `control/*`, `preserve/*`, and `evidence/*` remain absent; fresh v2 is unSTARTED/unconsumed.
- Authoritative Git tags: 0. Repository rulesets: 0.
- Legacy freeze/control/preserve/evidence authorities remain present and untouched.
- H9 readiness remains `research/c07-h9-fully-spiking-readiness-sub-20260917@9480da3d77dfee4766b28757f1a164f5cd4dac26`, `PRE_START_UNDERSPECIFIED`.
- `research/methods-terminal-provenance-v2-sub-20260917@d5317485a591439cb52d52b649b4930964cf2b3a` remains unreserved.

## Active-line review

| Line | Status / strongest evidence | Shortest path to NEW information | Allocation |
|---|---|---|---|
| A01 | Mixed; registered A/B/C programme closed; P2/P3 development positives, Family-A P4 terminal negative, B/C reduced pre-START | Fresh independently motivated object only | Closed |
| RV01 | Development-positive timing observation, consumed, reducible to adaptive-delay/recurrent mechanisms | Fresh successor only | Secondary complete |
| RV02 | RD005 D1 terminal construction negative, consumed | Fresh object only | Terminal |
| CX/CX01 | Candidate-002 formal negative, consumed | Fresh object only | Terminal |
| H8/C08 | Registered specialization negative | Distinct prospective object only | Terminal |
| H9/C07 | Hybrid partial support; fully-spiking readiness still has eight unresolved scientific choices | Prospectively fix all eight, then fresh protocol/identity | Secondary paused |
| **C19** | **Primary unresolved; raw/coverage substrate green; fresh v2 branch now exists at exact base with green CI but has no v2 scientific delta yet** | **MAIN implements fixed Auditor-hardened package and executes once if every GO gate passes** | **PRIMARY / MAIN** |

## Parallel decomposition

### `main_lane`

- **target:** C19 fresh official-v2 package, Auditor-hardened pre-START admission, conditional one-way execution.
- **scientific_question:** under the unchanged 55-row matrix, does `I2_truth_free_symbolic_surface/G1_coalition/E0_global` improve Belief-R BREU over `I1_local_compositional/G1_coalition/E0_global`?
- **recommended_owner:** `main`.
- **branch/protocol/identity:** `research/c19-official-v2-scorer-complete-20260917` / `c19-external-v2-official-protocol-v2` / `c19-external-v2-official-v2`.
- **current branch head:** `66c8eafe9863ed1b2455cc833a3dc498ce7721b0`.
- **information_value:** `VERY_HIGH`.
- **implementation_distance:** `NEAR`.
- **main_owns_all_critical_path_fixups:** `true`.

MAIN owns every C19-specific implementation, verifier, runner, binding, runtime, CI, review, preserver, scorer, admission and execution blocker. SUB must never become a dependency.

Evaluator target fields remain prospectively fixed as `pair_index`, `record_id_hash`, `source_index`, `step_index`, `target_choice_id`, `update_required`; join key is the first four fields. The join must be one-to-one unique and total over all 1,744 pairs, fail closed on duplicate/missing keys, materialize only after immutable raw preservation, and keep target truth evaluator-only.

Bootstrap remains CPython 3.11.16, `random.Random(19901)`, 10,000 replicates, exactly 1,744 sequential `randrange(1744)` draws per replicate, same sampled vector across primary/reference and seeds 15901–15905. Quantile remains: sort exactly 10,000 finite effects; `r=(N-1)*p`; floor/ceil linear interpolation; retain ties; reject empty, wrong-length, NaN or ±inf; `p=0/1` returns min/max; official bounds `.025/.975`; no rounding before classification.

Mandatory deterministic golden fixtures cover valid full join, duplicate rejection, missing-key rejection, leakage exclusion, tied effects, non-finite rejection, empty/wrong-length rejection, `p=0/1`, and `.025/.975` interpolation.

**GO:** fresh v2 remains collision-free, unSTARTED and unconsumed; exact source/protocol/package/input/runtime/parser/envelope/scorer/bootstrap/preserver bindings reconstruct; no prior official output or conflicting authority exists; exact 55×1,744 raw and 1,744 evaluator-target join fail closed; target truth is technically evaluator-only; quantile/golden fixtures are green; STARTED/no-clobber/raw-before-score are machine-enforced; exact-head CI/review are green; authority is re-fetched immediately before STARTED.

**STOP:** any new scientific/statistical/acquisition/baseline/claim choice; stale/conflicting authority; identity/ref/hash/package collision or prior output; target leakage; non-total/non-unique join; unresolved quantile behavior; fixture failure requiring a new convention; ordering/no-clobber/binding failure requiring science change. Any failure after STARTED consumes v2 and forbids same-identity retry.

### `sub_lane`

`null`

### `sub_fallback`

`null`

**no_sub_lane_reason:** bounded H1–H10/current-branch review still finds no fully specified independent unconsumed object ready without new scientific design. H9 needs eight new science choices; H10 needs a new hardware-power protocol/resources; H1–H7 need fresh prospective objects; methods-terminal-provenance is unreserved. The newly created C19 successor remains MAIN critical-path work.

### `blocked_until`

- **MAIN:** package implementation may proceed now on the existing successor branch; one-way execution remains blocked until every fresh-v2 and Auditor admission gate is green after fresh authority/identity reconciliation.
- **SUB:** a newer Analyst explicitly reserves a fully specified genuinely independent prospective lane/fallback.

## Prospective contingency tree

All branches are fixed before any C19 official outcome.

- `PRE_START_SUCCESSOR_PROTOCOLIZATION`: successor now exists but is still exact base/incomplete → MAIN continues the already-fixed package implementation and exact-head CI; no one-way execution yet; STOP if a new semantic choice appears.
- `PRE_START_AUDIT_HARDENING`: join/leakage/quantile/golden-fixture conformance incomplete but behavior fixed → MAIN fixes it and reruns exact-head CI; STOP if a fixture exposes a new semantic choice.
- `PRE_START_BLOCKER`: science-preserving C19 CI/verifier/runner/binding/runtime/preserver/review defect → MAIN fixes it itself, re-fetches exact state, and continues.
- `PRE_START_STALE_AUTHORITY`: authority/ref/identity/control moves → STOP and reconcile; continue only if newer authority preserves the exact same v2 object.
- `PRE_START_SEMANTIC_GAP`: any evaluator/bootstrap/acquisition/scoring/preservation/baseline/metric/claim choice remains unbound → STOP before STARTED.
- `PRE_START_READY_FOR_ONE_WAY`: every GO gate passes → MAIN may create STARTED and continue same-run through acquisition → target-blind raw → immutable raw preservation → evaluator targets → scoring → terminal evidence.
- `POST_START_FAILURE`: any failure after STARTED → preserve diagnostics, mark exact identity consumed, no retry, STOP.
- `INVALID_EVIDENCE`: binding/join/completeness/leakage/ordering/integrity invalid → withhold result, preserve invalidity, consumed if STARTED, STOP.
- `PASS`: valid BREU 95% CI lower `> 0` → finalize narrow positive evidence, then STOP before follow-up design.
- `FAIL`: valid BREU 95% CI upper `<= 0` → finalize registered negative, no rescue/retune/rerun, STOP.
- `INCONCLUSIVE`: valid CI contains `0` while FAIL is not met → finalize inconclusive, no extra data/resampling/retune, STOP.

## Top 3

1. **MAIN — advance the recovered successor branch through exact Auditor-hardened C19-v2 implementation, then execute once if every GO gate passes.** `VERY_HIGH / NEAR`.
2. **SUB — deliberate no-op.** Do not invent H9/H10/H1–H7 science and do not take C19 blockers.
3. **Non-blocking operations/governance — verify the next normal MAIN cycle produces durable scientific branch/report progress; keep #147 aligned with canonical git; leave #139 open until real server-side tag ruleset protection exists.**

## Consumed identities / do-not-touch

Unchanged: A01 MD-001; A01 P2 candidate-002; A01 P3 candidate-001; A01 Family-A P4 candidate-001; RV01 R01-16 family; RV01 R01-17; RV02 D1 `96634541dc29b00be9f19b5819d45f541af69348ab6c1b0da6b48e943221700a`; CX01 Candidate-002/formal evidence.

C19 predecessor v1 is retired/rejected but unconsumed and permanently non-reusable. Do not touch rejected A01 B/C objects, historical C19-v1/C06, historical C07/H9 evidence including threshold-1.1 negative, C08/H8 terminal evidence, or legacy immutable authority refs.

## Genuine blockers

- **MAIN scientific/integrity:** successor branch now exists, but fresh-v2 implementation/hardening is still absent beyond the base SHA. Join/leakage/quantile/golden-fixture/scorer/preserver/binding gates must close before STARTED.
- **MAIN operational:** prior false write-path/durable-report stall is recovered; next normal MAIN cycle should prove durable scientific advancement. Do not reinterpret a future operational write failure as scientific evidence.
- **SUB:** no valid independent reserved lane.

## Governance advisory

Issue #147 is operational only and canonical git wins. Its text still says the successor must be created, so it is now mildly stale operationally; that does not change science. Legacy freeze branches remain authoritative historical anchors and must not be force-migrated. Annotated-tag tooling exists on `main`, but authoritative Git tags and repository rulesets remain 0, so #139 remains open. Outcome-independent future `main` promotion candidates remain generic source/runtime binding verification, STARTED/no-clobber/collision primitives, exact pair coverage, raw-preserve-before-score/digest verification, generic unique+total evaluator-join validation, deterministic golden-scorer fixtures after C19 semantics are separated, and generic fail-closed state-machine helpers.

## ORCHESTRATOR HANDOFF

**MAIN takes the existing C19 official-v2 successor and owns ALL critical-path fixes, including Auditor-required join/leakage/quantile/golden-fixture hardening. SUB takes no lane; fallback is null. MAIN must not invent or absorb H9/H10 work; SUB must not take C19 blockers. Neither touches rejected, consumed, frozen, retired, or legacy immutable authorities.**

Repartition only if fresh C19 evidence/identity state materially changes, materially new external/audit evidence changes expected information value before STARTED, or a fully specified independent secondary object becomes prospectively defined and explicitly reserved.

MAIN may continue same-run through `PRE_START_SUCCESSOR_PROTOCOLIZATION`, `PRE_START_AUDIT_HARDENING`, science-preserving `PRE_START_BLOCKER`, `PRE_START_READY_FOR_ONE_WAY`, and terminal PASS/FAIL/INCONCLUSIVE evidence finalization. MAIN must stop at `PRE_START_SEMANTIC_GAP`, irreconcilable stale authority, `INVALID_EVIDENCE`, `POST_START_FAILURE`, or before any post-outcome redesign.
