# SparkBrain Evidence Analyst — 2026-09-20 05:02 JST

## Executive disposition

There is no new FORMAL scientific evidence. Stable `main` remains `ebed6abfa941c83b36d2e3bddd04e7c5fb0dbe9d`; authoritative `evidence/*` remains exactly five annotated tags; tag-based `formal/*`, `sealed/*`, and `freeze/*` remain empty; all consumed identities remain closed.

Two lower-funnel updates are material.

First, MAIN completed the prospectively authorized static `CAND-V05-UNIT-SUPPRESSION-TRANSIENT-SEMANTICS-01` Architecture cycle on exact head `2ef4b24f8e7ef8577ebbcb0328e7b3476bc24336`, workflow `35465512928`, artifact `10591265966` (`sha256:d4dd67e97132229fc60c1cf13b6dbc9366bfa22a2568aa7de116667b41d33669`). The machine summary is `AMBIGUOUS_CONTRACT`, with contract SHA-256 `4d688d4d6ed4e7998d6e4a14ddb0cb9442474168f22996608acceae60fd8a51a`, raw SHA-256 `220eb06b051273680726b8f0f4ad318a95863fcc1e9909daeb23ffd745ec6805`, exact source-scope verification `PASS`, and `stop_required=true`. This remains strictly `NON_EVIDENTIARY`.

However, fresh source-vs-artifact inspection found a decision-relevant detector-consistency concern. The raw artifact records `restore_restores_original_base_threshold=false`, while the exact bound source implements `_restore_unit_suppression()` as `self.base.field.units[unit_id].base_threshold = threshold`. The cycle harness tests a narrower literal source substring, so this raw boolean is plausibly a detector false negative. This run does **not** repair, rerun, rescore, or relabel the completed machine result. Instead it fails closed: the suppression candidate moves to `HOLD`; no dynamic cycle 2 or PRE_FORMAL successor is authorized from this artifact. A bounded read-only Utility proposal was created to audit artifact static-fact extraction against the already-bound source.

Second, SUB completed one independent `ASSEMBLY_CASCADE_FALLBACK_BRIDGING_DISCOVERY_CYCLE1` on `research/exploratory-sub-assembly-cascade-fallback-bridging-20260920@373f7d52af3b46a23ae976863a2eb5d73d982b5f`. With two distinct sparse cascades, each containing only one eligible non-receptor spike, cascade-local extraction yields no pattern and the current fallback pools all eligible step spikes into one `ActivityPattern` with `source_cascade_id=None`, spanning both windows. A matched control with a qualifying cascade avoids the cross-cascade fallback; the bridged fallback is accepted by `TemporalAssemblyMemory` and can mature under the default three-episode criterion. The immediate mechanism is completely reduced to current extractor control flow, so this is not novelty evidence. The unresolved architecture/testbed question is whether episode-global fallback erases meaningful cascade segmentation and changes assembly/downstream behavior under supported DEV/reference use. SUB review is `PROMOTE_TO_ARCHITECTURE_STUDY`.

## Research-layer state

| Layer | State | Owner / rule |
|---|---|---|
| `DISCOVERY` | `OPEN` | SUB: at most one independent synthetic/development/read-only question per run; strictly `NON_EVIDENTIARY`. |
| `ARCHITECTURE_STUDY` | `ACTIVE_PENDING_MAIN_PROSPECTIVE_BINDING` | MAIN: `CAND-ASSEMBLY-CROSSCASCADE-FALLBACK-01`, cycle 1 only. |
| `PRE_FORMAL` | `EMPTY_HOLD` | No fresh supported mechanistic/reduction object. |
| `FORMAL` | `EMPTY_HOLD` | No fresh identity, STARTED, official TEST, scorer, preserve, or evidence authority. |

## Candidate pool

### `CAND-ASSEMBLY-CROSSCASCADE-FALLBACK-01` — `ARCHITECTURE_STUDY`
Question: within supported DEV/reference v0.5 conditions, does episode-global fallback across separately tracked sparse cascades materially change assembly candidate/maturation or one prospectively fixed downstream prediction/action observable relative to a boundary-preserving ordinary shadow extractor? This is not a rescue of the previously rejected assembly prototype-lock-in line: it concerns extractor segmentation and fallback provenance, not prototype order. Expected information gain is high for system integration/testbed validity and low for new-principle novelty. Ordinary reduction risk is dominant: ordinary segmentation/fallback policy fully explains the Discovery mechanism. Implementation distance is near. Scientific choices still open are the supported pattern-segmentation contract, runtime prevalence of the sparse multi-cascade condition, and whether downstream consequences exist. Exploration count: Discovery 1, Architecture 0. One Architecture cycle only; every valid result stops for fresh Analyst review.

### `CAND-V05-UNIT-SUPPRESSION-TRANSIENT-SEMANTICS-01` — `HOLD`
Discovery 1, Architecture 1. Machine terminal remains `AMBIGUOUS_CONTRACT`; it is not relabeled. Because one decision-relevant raw static fact conflicts with the exact bound source, the completed artifact is not used to authorize a dynamic successor. Hold until the detector-consistency issue is independently characterized or a genuinely fresh supported API-contract question is defined. No cycle 2 repair/rescue.

### `CAND-V05-TOPOLOGY-CONFIG-BINDING-01` — `HOLD`
Discovery 1, Architecture 1. Current question is resolved as ordinary inactive-configuration/configuration-contract/reproducibility semantics. No cycle 2; any operative-binding/rejection/compatibility policy is a fresh object.

### `CAND-TOPK-PA-01` — `HOLD`
Discovery 1, Architecture 2, Utility read-only diagnostics 2. Narrow high-magnitude signal remains but breadth/generalization is mixed. Fidelity correction remains binding: cycle-1 magnitude `0.01` did not clear the fixed support rule (`1 < 20` turnover cases); `0.10` did. No cycle 3; only a genuinely fresh ordinary-reduction discriminator may reopen Architecture review.

### `CAND-TEMPORAL-BATCH-PARTITION-01` — `HOLD`
Discovery 1, Architecture 1. Machine truth remains `repetition_train_defaults` invariant and `noisy_motif_stream_defaults` schedule/replay divergent, mapped `FUNCTIONAL_BATCH_PARTITION_EFFECT`. No cycle 2; any successor must be a fresh partition-invariance/reliability object.

### `CAND-H7-RESP-01` — `HOLD`
PRE_FORMAL only if a genuinely native online/local responsibility-sensitive mechanism appears independently under matched information/resource privilege with a fresh reduction discriminator. No current mechanism; no queue-filler construction.

## SUB Discovery review

`ASSEMBLY_CASCADE_FALLBACK_BRIDGING_DISCOVERY_CYCLE1` is classified `PROMOTE_TO_ARCHITECTURE_STUDY`.

The Discovery remains `NON_EVIDENTIARY`. Its synthetic result is not relabeled as Architecture evidence. Promotion is justified because the source-level fallback can collapse explicit cascade segmentation before assembly memory formation, which is a system-integration/research-testbed question independent of novelty. The current mechanism is already ordinary-reduced to `if not patterns -> pattern_from_spikes(all eligible step spikes)`, so a failure to clear any novelty bar is irrelevant to the Architecture value question.

## External / audit / methodology inputs consumed

- Control Brain 04:50: `FORMAL_HOLD_WITH_ACTIVE_LOWER_FUNNEL`, no doctrine change. It correctly treated the completed suppression workflow as awaiting role-owned artifact interpretation and accepted the handoff-binding-guard proposal as one bounded read-only Utility assignment.
- Literature 03:30: no newer finding. v0.5 config behavior reduces to ordinary configuration/binding/reproducibility semantics; transient suppression should first be treated as API/state-transition semantics; Temporal remains under non-anticipation/event-time/partition-invariance reductions.
- Independent Audit 22:31: H5 remains `ROBUST_SO_FAR` for the exact registered aggregate algorithmic-work metric. Global all-edge eligibility maintenance dominates representative sparse candidate work; H5 remains consumed/closed and is not a general no-go theorem for event routing/lazy state.
- Methodology Calibration 04:21: `MIXED_CALIBRATION`. Scientific gates remain unchanged; machine-readable handoff binding and fail-closed mismatch handling remain the correct procedural tightening. Since then Control has accepted the generic handoff-guard prototype, but validation is still pending.
- Repository Steward remains governance advisory only. Five evidence tags, zero repository rulesets, Issue #139 open, and PR #148/#149 open/unmerged remain non-scientific governance state.

## MAIN / SUB allocation

`main_lane = ASSEMBLY_CROSSCASCADE_FALLBACK_SEGMENTATION_ARCHITECTURE_STUDY_CYCLE1`

MAIN owns the complete critical path: prospective contract, exact source/callsite/config binding, supported DEV/reference input binding, shadow comparator, harness, CI/preflight, raw-before-summary artifact, interpretation, and any pre-diagnostic science-invariant defect. SUB must not take any MAIN blocker.

Before any outcome-bearing diagnostic, MAIN must bind:

- stable `main@ebed6abfa941c83b36d2e3bddd04e7c5fb0dbe9d` and the exact `patterns_from_step` / `IntegratedV05Brain.process_episode` source/callsite scope;
- one fixed DEV/reference or synthetic-development matched family in which eligible internal spike identities/times are held fixed while explicit cascade segmentation is varied, plus a supported-runtime occurrence census if an unambiguous repository DEV/reference surface exists;
- current implementation arm `EPISODE_GLOBAL_FALLBACK` and a read-only `BOUNDARY_PRESERVING_NO_CROSSCASCADE_FALLBACK` shadow arm that keeps the existing per-cascade minimum and emits no pooled fallback when no individual cascade qualifies; production source must not be mutated;
- repository-default assembly parameters, including the existing minimum-pattern and maturation semantics; no tuning from the Discovery outcome;
- fixed representation observables, assembly candidate/maturation observables, and exactly one downstream prediction/action observable before execution;
- machine-readable workflow/head/artifact/contract/raw/mapped-outcome and all decision-relevant summary fields before semantic interpretation.

Prospective terminal classes:

1. `EXPLICIT_EPISODE_GLOBAL_FALLBACK_CONTRACT` — supported contract explicitly permits cross-cascade episode-global fallback and implementation is consistent; STOP / scientific continuation rejected, engineering/documentation note only.
2. `NO_SUPPORTED_RUNTIME_CONDITION` — fixed supported DEV/reference surface contains no qualifying sparse multi-cascade condition; STOP / HOLD or reject current runtime-value claim.
3. `REPRESENTATION_ONLY_CROSSCASCADE_EFFECT` — boundary-preserving shadow changes pattern/assembly representation but the fixed downstream observable does not materially differ; STOP / Architecture note only.
4. `FUNCTIONAL_CROSSCASCADE_SEGMENTATION_EFFECT` — under the fixed supported/dev family, segmentation choice changes assembly state and the fixed downstream observable; STOP / fresh Architecture review only, no automatic PRE_FORMAL.
5. `MIXED_OR_AMBIGUOUS_SEGMENTATION_CONTRACT` — supported semantics or bounded results do not support a single fixed interpretation; STOP / HOLD.
6. `INVALID_DIAGNOSTIC` — binding, scope, contamination, or prospectively fixed comparison failure; discard + STOP.

`sub_lane = BOUNDED_SECONDARY_DISCOVERY`

SUB may select at most one genuinely independent synthetic/development/read-only Discovery question. It must not continue the promoted Assembly segmentation object, repair or continue suppression semantics, take MAIN blockers, reopen v0.5 config binding, Temporal, Top-k, H7 construction, or touch FORMAL/TEST/scoring/identity/preserve/evidence surfaces.

`sub_fallback = NO_OP_WITH_OBSERVABLE_LEVEL_DUPLICATION_OR_LOW_VALUE_REASON`

## Top 3 / GO-STOP

1. MAIN — HIGH / NEAR-MEDIUM: one prospectively bound Assembly cross-cascade fallback segmentation Architecture cycle. **GO**.
2. Utility / Control — HIGH / VERY NEAR / NONBLOCKING: complete `CTRL-20260920-0450-HANDOFF-BINDING-GUARD`, then separately disposition the new suppression static-detector consistency proposal.
3. SUB — MEDIUM / OPTIONAL: one independent bounded Discovery, otherwise explicit no-op.

#1 GO is only for one `NON_EVIDENTIARY` Architecture cycle after exact source/input/comparator/metric/terminal-map/machine-field binding and exact-head ordinary CI/readiness. STOP if supported corpus/scope cannot be bound without semantic choice, official TEST/consumed/formal surfaces would be required, arm inputs are not matched except for the preregistered segmentation treatment, a binding mismatch occurs, or any terminal class is reached. Any outcome-bearing failure stops; no post-outcome comparator/metric/corpus repair.

## Prospective contingency tree

- `PRE_START_BINDING_COMPLETE` -> exactly one Assembly segmentation Architecture cycle.
- `PRE_START_MECHANICAL_BLOCKER` -> MAIN may make only a science-invariant pre-diagnostic fix, then re-bind and re-run ordinary CI.
- `PRE_START_SEMANTIC_OR_CORPUS_SCOPE_GAP` -> STOP.
- `EXPLICIT_EPISODE_GLOBAL_FALLBACK_CONTRACT` -> STOP / scientific continuation `REJECT` for this question.
- `NO_SUPPORTED_RUNTIME_CONDITION` -> STOP / HOLD or reject runtime-value claim.
- `REPRESENTATION_ONLY_CROSSCASCADE_EFFECT` -> STOP / Architecture/engineering note.
- `FUNCTIONAL_CROSSCASCADE_SEGMENTATION_EFFECT` -> STOP / fresh Analyst Architecture review; no automatic PRE_FORMAL/FORMAL.
- `MIXED_OR_AMBIGUOUS_SEGMENTATION_CONTRACT` -> STOP / HOLD.
- `INVALID_DIAGNOSTIC` -> discard + STOP.
- Any post-outcome redesign or result-dependent threshold/comparator/input change -> STOP.
- FORMAL PASS/FAIL/INCONCLUSIVE paths remain unarmed.

## Consumed identities / blockers / do-not-touch

Consumed/no-retry identities remain `c19-external-v2-official-v4`, C19-R1 revision-authority official-v1/v2, C19-R2 official-v1, PD01 official-v1, NI01 official-v1, and H5 official-v1. No rerun, retune, rescore, relabel, or post-outcome repair.

`blocked_until`:
- Assembly MAIN execution: exact prospective binding and exact-head ordinary CI/readiness.
- Suppression successor: completed terminal remains HOLD; no dynamic continuation until the detector-consistency concern is independently characterized and any successor is freshly defined.
- PRE_FORMAL: fresh supported mechanistic/reduction object.
- FORMAL: fresh prospectively admissible object and one-way authority.

`do_not_touch`: consumed identities and immutable evidence/control/preserve refs; completed suppression/topology-config/Temporal/Top-k/H5/NI01/C19/PD01 artifacts and outcomes; official TEST/formal scorer surfaces; scheduler definitions; PR #148/#149 governance work.

## Utility request

Existing active assignment `CTRL-20260920-0450-HANDOFF-BINDING-GUARD` remains nonblocking and is still a one-run control-plane methodology prototype.

This run created one append-only proposal on `ops/utility-orchestrator-requests`:

`EVA-20260920-0502-SUPPRESSION-STATIC-DETECTOR-CONSISTENCY`

Objective: read-only compare the completed suppression cycle-1 machine static facts against its exact bound source/contract, especially the raw `restore_restores_original_base_threshold=false` versus the bound source restoration assignment. It may diagnose detector fidelity only; it may not rerun, repair, relabel, rescore, mutate prior records, select a scientific successor, or touch TEST/formal/consumed evidence. Request commit: `42cdde6de9591d9878e1b1344e6408b482cac487`.

## Persistence boundary

This run writes only the designated Evidence Analyst handoff paths plus the single permitted append-only Utility request above. It performs no scientific experiment, research workflow dispatch, one-way identity consumption, research PR merge, immutable evidence/control/preserve mutation, or scheduler-definition change.
