# SparkBrain Evidence Analyst — R91

- schema_version: `2`
- generation_id: `EVA-20260923T101328+0900-R91-C199605F`
- produced_at: `2026-09-23T10:13:28+09:00`
- producer_run_id: `evidence-analyst-auto-20260923T101328+0900-R91`
- authority_scope: `EVIDENCE_DRIVEN_RESEARCH_STRATEGY_CONTROL_PLANE_PERSISTENCE_ONLY_NO_SCIENTIFIC_EXECUTION`
- supersedes_generation_id: `EVA-20260923T090047+0900-R90-B84D2C71`

## Executive decision

Candidate #34 Architecture R2 has now closed the exact prospective scientific contract that R90 required before any response-bearing development. Direct repository truth is `research/main-cand34-assembly-route-architecture-r90-cycle2@a6455a3929b86ad25fd106ea93a03604192fc3be`; exact-head generic CI run `35802555790` is green on Python 3.11 and 3.13 through Install, Lint, Local readiness, Test and Validate bundle. The commit adds the frozen R2 contract and synthetic validation only; no candidate response-bearing execution, PRE_FORMAL result, FORMAL identity, evidence, score or preserve ref was produced.

The R2 contract prospectively fixes checkpoint/prototype selection, exact checkpoint state-hash binding, `K<=12` deterministic route-edge ranking, matched non-target controls, target sham/transmission-null/`+1ms` delay arms, prior assembly/unit reductions, a 64ms measurement window, complete response fields, fixed execution order, independent deep clones of one quiescent anchor, the 256ms no-extension quiescence cap, bounded resources, fail-closed reasons and an interventional-equivalence decision rule. It explicitly sets `response_bearing_execution_allowed=false`, `preformal_execution_allowed=false` and `formal_action_allowed=false`, so the frozen Architecture object itself must not be rewritten to enable later execution.

R91 therefore performs the required cycle-3 REASSESS and promotes candidate #34 to **`MECHANISM / PRE_FORMAL / OPEN_DEVELOPMENT / preformal_eligible=true / READY`**. READY means only that the next development test is now prospectively well-defined and informative. It does not mean the candidate has beaten reductions or is likely to PASS. PRE_FORMAL R1 must use a fresh execution-authority wrapper that binds the R2 contract/head byte-for-byte rather than mutating R2. PRE_FORMAL observations remain development-only and can never be accumulated into FORMAL evidence.

Exact #1 decision:

`GO_CAND34_PREFORMAL_R1_BIND_UNCHANGED_R2_CONTRACT_A6455A3929B86AD25FD106EA93A03604192FC3BE_DEVELOPMENT_EXECUTION_ONLY_PRESERVE_RESULT_AS_NONCONFIRMATORY_STOP_BEFORE_OUTCOME_RESPONSIVE_SCIENCE_AFFECTING_CHANGE_OR_ANY_FORMAL_ACTION`

Fresh Literature R35 does **not** justify editing R2. It establishes a stronger interpretation guardrail: precise delay-sensitive routes have ordinary synfire/polychronous prior art, and a `+1ms` perturbation observed through a representation that uses 2ms relative spike bins can be bin-boundary-sensitive rather than a robust temporal mechanism. Therefore the current #34 object may test its already-frozen representation-specific interventional route question, but a positive result cannot by itself establish robust timing, unique topology, computational irreducibility or novelty. Continuous-time/multi-resolution robustness, matched synfire/polychronous reductions and compact temporal-FSM reductions belong only to a fresh prospective successor.

Candidate #35 Discovery R1 also closed prospectively without response exposure. SUB fixed its prime, queue-empty anchor/cap, cue bytes/timing, potential/adaptation reset arms, 32ms response window, complete downstream response signature, ordinary local threshold/decay reductions and falsifier, then stopped. R91 promotes #35 to **`SYSTEM / ARCHITECTURE_STUDY / OPEN_DEVELOPMENT / preformal_eligible=false`** for deterministic clone/reset/queue-cap/serializer implementation and synthetic non-result tests only. Candidate-response execution remains STOP. Literature R35 further strengthens the ordinary local membrane/adaptation reduction; any later MECHANISM residual still requires a fresh candidate ID.

H7 is unchanged. Exact R5 head `2f30b93f8f3cf226ef55ed5af7e341089d2c3c80` remains PREIDENTITY COMPLETE with both exact-head CI `35794233612` and dedicated preidentity `35794233687` green, but FORMAL one-way authority remains zero. PF-R1 exact-byte preservation still lacks matching schema-v2 Utility machine authority and remains unexecuted.

H7 exact decision remains:

`STOP_H7_FORMAL_ONEWAY_R5_PREIDENTITY_COMPLETE_PENDING_PF_R1_SCHEMA_V2_MACHINE_AUTHORITY_AND_EXACT_BYTE_DURABLE_PRESERVATION_THEN_REQUIRE_FRESH_ANALYST_AUTHORIZATION`

## Repository evidence

Stable `main` remains `ebed6abfa941c83b36d2e3bddd04e7c5fb0dbe9d`. Authoritative `evidence/*` remains exactly five annotated tag objects: C19-v4 `4d6c0bd9...`, C19-R2 `82b88f3e...`, H5 `e7d99cc8...`, NI01 `185b741e...`, PD01 `e4c4e642...`. Tag-form `formal/*`, `sealed/*`, and `freeze/*` remain empty. No `control/h7*` or `preserve/h7*` ref exists. Historical preserve/control refs for consumed identities remain present and unchanged. Historical official consumed identities remain seven; no new one-way consumption is observed.

PR #148 and #149 remain open, unmerged and mergeable. Repository rulesets remain zero; this remains a governance fact rather than a scientific promotion prerequisite under the active human policy.

## Four-layer funnel

| Layer | MECHANISM | SYSTEM | State |
|---|---:|---:|---|
| DISCOVERY | 0 / 0 | 0 / 0 | supply surface open |
| ARCHITECTURE_STUDY | 0 / 0 | 0 active / **1 queued** | #35 Architecture R1 tooling/synthetic closure |
| PRE_FORMAL | **1 queued**; eligible **2** / READY **2** | N/A | #34 READY for development PRE_FORMAL; H7 READY but FORMAL-held |
| FORMAL | fresh one-way authority **0** | — | H7 provenance hold |

Canonical population remains **35 = MECHANISM 14 / SYSTEM 21**. Lifecycle is **ACTIVE 0 / QUEUED 2 / NONTERMINAL_HOLD 1 / TERMINAL_FOR_CURRENT_OBJECT 32**. Development phases remain **OPEN_DEVELOPMENT 4 / RESULT_EXPOSED_DEVELOPMENT 31 / canonical CONSUMED_ONE_WAY 0**. Classification completeness remains **35/35**.

Candidates #1-6 and #8-33 inherit R90 Funnel v2.1 fields unchanged. #7 remains H7 FORMAL-held. #34 moves from Architecture R2 to PRE_FORMAL READY after explicit cycle-3 reassessment. #35 moves from Discovery contract closure to queued Architecture R1. SYSTEM-terminal successor accounting remains unchanged: 20 assessed; one realized fresh SYSTEM successor (#32→#33); 16 unrealized fresh SYSTEM potentials; zero fresh MECHANISM successors from SYSTEM terminalizations; none for #14/#27/#28.

## Candidate #34 — PRE_FORMAL entry boundary

R2 exact source is frozen at `a6455a3929b86ad25fd106ea93a03604192fc3be` and CI `35802555790` is green. R2's contract hash binds its scientific fields and separately binds the evaluation surface/checkpoint when an execution plan is constructed. PRE_FORMAL R1 may create only a fresh layer-authority/execution wrapper that references the unchanged R2 contract/head and prospectively binds one development evaluation surface/checkpoint before observing responses. It must not flip the R2 contract's false execution flags in place or change the scientific fields after exposure.

PRE_FORMAL may be response-bearing because it is genuine development. The first meaningful response moves #34 to `RESULT_EXPOSED_DEVELOPMENT`. Preserve that development result durably. Thereafter a science-invariant repair may continue on the same revision, but any change to metric/response meaning, intervention family, delay amount, 64ms window, comparator/reduction panel, target/prototype rule, seed/input selection, resource/scientific privilege, threshold/tolerance, falsifier/equivalence rule or success criterion requires an explicit versioned development revision or fresh successor. The prior result must remain unchanged.

Literature R35 caps interpretation prospectively. Within this current object, `+1ms` sensitivity or changes in integer relative spike bins can support only the frozen representation/intervention-specific causal statement. Do not retrofit continuous-time metrics, alternate bin phases, synfire comparators or FSM comparators into R2 after seeing outcomes. If such a residual is worth testing, create a fresh successor with a new prospective contract.

## Candidate #35 — Architecture entry boundary

SUB's Discovery R1 contract now prospectively fixes: motif-only no-learning prime; first queue-empty +32ms boundary with 256ms hard cap; cue at anchor+4ms; sham, potential-null, adaptation-null, joint-null and delayed-sham arms; fixed 32ms no-learning response window; canonical downstream spike/cascade/ignition response; local threshold-margin/18ms membrane-decay/90ms adaptation-decay ledger; and exact negative/reduction conditions.

Architecture R1 may implement deterministic clone/reset/cap/signature machinery and synthetic non-result tests. STOP before response-bearing candidate execution. If the source contract cannot be implemented without changing the prime, cue, reset scope, cap, measurement semantics or ordinary reductions, return to Analyst rather than silently redesign. #35 remains SYSTEM; ordinary local adaptive-integrate-and-fire semantics are the strongest explanation. A later independent mechanistic residual requires a fresh MECHANISM successor ID.

## Literature / Audit / Methodology / Steward / Utility

Literature R35 is material but non-authorizing. It adds the polychronous/synfire ordinary-reduction floor, timing-resolution/bin-phase robustness concern and compact temporal-FSM reduction for **future** broader #34 claims; it explicitly says not to rewrite frozen R2. It also strengthens #35's local membrane/adaptation reduction.

Independent Audit R7 remains the H7 hard floor: literal prediction-only raw → immutable preserve → protected target-side scorer. H7 R2 remains confirmatory-confounded by reconstructible targets and pre-preserve correctness computation; no H7 FORMAL evidence exists to rewrite.

Methodology R77 independently says #34 R1 tooling did not create result exposure, R2 may prospectively freeze its scientific contract, and after R2 closure READY must be judged by whether the next test is well-defined/informative rather than prior victory. R91 follows that calibration. It also keeps #35 SYSTEM-scoped and H7 on the PF-R1 provenance gate.

Repository Steward G11 reports no immutable-ref incident and rulesets=0. Its earlier #34 materialization visibility gap is superseded by the directly re-fetched live R1/R2 research branches. Utility remains fail-closed: assignment pointer clean/idle, no matching assignment, no matching Utility-side Control decision, request `PROPOSED_NOT_APPROVED`, PF-R1 preservation unexecuted, no duplicate request.

## MAIN / SUB allocation and candidate supply

MAIN next priority:

`CAND34_ASSEMBLY_TEMPORAL_ROUTE_IDENTIFIABILITY_PREFORMAL_R1_UNCHANGED_R2_CONTRACT_DEVELOPMENT_ONLY`

SUB next priority:

`CAND35_QUEUE_FREE_SUBTHRESHOLD_STATE_CAUSAL_PRIMING_ARCHITECTURE_R1_NON_EVIDENTIARY`

SUB is not a MAIN dependency. `system_priority_exception.used=false`: #35 does not outrank or block the comparably executable #34 MECHANISM lane. Rolling actual autonomous theory-backward selections remain `MECHANISM / SYSTEM / SYSTEM = 1/3`; #35 remains Analyst-allocated and denominator-excluded.

Phenomenon-first performs its due full read-only `PREFETCH_SHADOW` scan in R91 because R88 was the last full scan and R35 is a material delta. No standby proposal is retained. The obvious timing-resolution / synfire / FSM successor surfaces for #34 either depend on the unknown #34 PRE_FORMAL outcome or are ordinary-reduction requirements for a future broader claim, so retaining them now would precompute an outcome-dependent successor. #35 is already canonical and would be an ownership collision. `shadow_standby_queue=[]`; no same-generation admission occurs.

## Funnel metrics

| Metric | R91 |
|---|---:|
| canonical candidates | **35** |
| MECHANISM / SYSTEM | **14 / 21** |
| ACTIVE / QUEUED / HOLD / terminal | **0 / 2 / 1 / 32** |
| Discovery queued M/S | **0 / 0** |
| Architecture queued M/S | **0 / 1** |
| PRE_FORMAL queued M/S | **1 / 0** |
| PRE_FORMAL eligible / READY | **2 / 2** |
| viable executable MECHANISM | **1** |
| fresh FORMAL authority | **0** |
| recent completed MAIN endpoint proxy S/M | **11 / 11** |
| SYSTEM-over-MECHANISM exceptions | **0** |
| classification completeness | **35/35** |
| OPEN / RESULT_EXPOSED / canonical CONSUMED | **4 / 31 / 0** |
| official consumed identities | **7** |
| fresh successor generated/admitted | **0 / 0** |
| cycle-3 reassess outcomes this generation | **#34 CONTINUE_TO_PREFORMAL** |
| rolling theory-backward selections | **1/3** |
| shadow standby | **0** |

## Top 3 / GO-STOP

| Rank | Action | Ceiling | Development | Type | Decision |
|---:|---|---|---|---|---|
| **1** | #34 bind unchanged R2 contract/head + one dev evaluation surface/checkpoint, then run one bounded PRE_FORMAL development cycle | MECHANISM | OPEN | new PRE_FORMAL cycle | **GO development-only** |
| **2** | #35 deterministic clone/reset/queue-cap/signature Architecture implementation + synthetic tests | SYSTEM | OPEN | new Architecture cycle | **GO NON_EVIDENTIARY** |
| **3** | PF-R1 schema-v2 machine-authority closure + exact existing-byte preserve | provenance only | RESULT_EXPOSED provenance | FORMAL gate | **HOLD execution** |

H7 FORMAL one-way remains explicit **STOP**.

## Prospective contingency tree

For #34, if a fresh PRE_FORMAL wrapper can bind the unchanged R2 head/contract, one prospectively selected development evaluation surface/checkpoint and the exact execution plan without changing science, GO. If R2's scientific fields must change before exposure, return to Analyst for an explicit revision. After meaningful response exposure, only `SCIENCE_INVARIANT_REPAIR` may stay on the same revision; any metric/intervention/comparator/input/resource/falsifier/success change is science-affecting and must version. Negative or equivalence-class outcomes are valid information; do not add stronger arms merely to obtain a preferred result. No PRE_FORMAL observation becomes FORMAL evidence. Any future FORMAL attempt requires a fresh untouched evaluation identity/surface and fresh one-way authority.

For #35, Architecture tooling/synthetic validation only. If the fixed contract is unreachable, STOP/REASSESS; never extend the 256ms cap or alter cue/reset semantics after observing candidate response because response execution is not authorized yet.

For H7, Utility remains no-op while machine authority is absent. If matching authority later materializes, preserve only existing PF-R1 exact bytes/hashes. Retrieval failure is STOP, never rerun/reconstruct/rescore. Even after preservation, require a later fresh Analyst exact-head re-fetch before any one-way authorization.

## Persistence / mutation statement

This generation writes only the designated Evidence Analyst latest/state/history control-plane surfaces. It executes no scientific experiment, dispatches no research workflow, consumes no identity, merges no research PR, mutates no immutable/evidence/preserve/control ref, changes no scheduler definition, creates no Utility authority, and rewrites no historical PASS/FAIL.
