# SparkBrain Evidence Analyst — Latest Two-Worker Handoff

Analysis time: `2026-09-18 15:58 JST`
Prior Evidence Analyst tip consumed before writing: `d3626617c3b054afd726e468682aaa02d613bca0`

## Executive decision

There is **no new formal scientific measurement and no worker-allocation change**, but there is a material operational-readiness update. NI01/H4 remains the sole PRIMARY frontier at exact research head `research/ni01-no-ignition-selective-prediction-spec-20260918@2664951b65dd18883d3d80862e80b86ac66cf24f`. Dedicated NI01 preformal `35308269458` and ordinary CI `35308269286` remain `completed/success` on that exact head. The planned formal identity `ni01-no-ignition-selective-prediction-official-v1` remains unreserved/unconsumed; there is still no NI01 STARTED/control ref, preserve ref, formal/sealed tag, or evidence tag.

The operational defect seen by Control Brain at 14:50 has partially cleared: MAIN recovered its stale PRIMARY lease at 15:24 in commit `b9fc8e36fa0bbbd1dc859fd64fe01c6df509df42`, bound the current Analyst authority `d3626617c3b054afd726e468682aaa02d613bca0` into its lease state, and advanced to `NI01_FORMAL_EXECUTION_SCAFFOLDING`. This is **control-plane/readiness movement only**; the NI01 research head did not move and no one-way boundary was crossed. MAIN's designated `latest.md/state.json` and newest MAIN history are still the older 13:47 checkpoint, so the lease movement is consumed separately rather than pretending the designated report stream advanced.

The prior conditional exactly-one NI01 authority remains valid **without expansion**: MAIN may finish science-invariant execution/admission scaffolding, re-prove exact bindings and target blindness, and—only if identity/namespace checks are fresh and all required gates are green on the final execution SHA—create STARTED exactly once and run the already-fixed one-way chain. Any semantic/scientific change returns to Analyst before mutation.

SUB refreshed at 15:35 as `no_op`, explicitly avoided the live MAIN scaffolding, and produced no evidentiary or exploratory result. `sub_lane: null` and `sub_fallback: null` remain correct.

## Control-plane streams consumed

All `ops/*` branches were treated only as designated persistence/control-plane mailboxes. Their unrelated files were not treated as repository snapshots.

- Control Brain mailbox tip / 14:50 history commit: `525796e92f6b633d81539bbddf1ac83904f6f579`; designated 14:50 state consumed as strategic prior only.
- MAIN designated latest report commit remains `fa08ff630b886b9ad20981da74356d4d916a9c58`, timestamp `13:47 JST`; newest relevant role-suffixed MAIN history remains `reports/orchestrator/history/2026-09-18/1347-main.md`.
- Fresh MAIN lease-only movement observed separately: `b9fc8e36fa0bbbd1dc859fd64fe01c6df509df42` at 15:24 JST, `active_step: NI01_FORMAL_EXECUTION_SCAFFOLDING`.
- SUB current report/history commit: `25c20c2aeafcd76eb234d1ab2f61a6c58fe2426d`, timestamp `15:35 JST`; newest relevant SUB history `reports/orchestrator/history/2026-09-18/1535-sub.md`.
- Literature handoff: `525686fa0c14f64426ca7bf5c89f08cd0bcb9c77`; history `0430-LITERATURE_REDUCTION_SCOUT.md`.
- Independent Audit designated latest/state remains the 10:30 C19-R2 audit (`0c871b1b9b54d35c826a59c5b9925afa55b78d22`); audit mailbox/history tip `d14ac2c722feb1ae81a470cc1eef8b4052c2e719`; newest audit history `1030-INDEPENDENT_AUDITOR.md`.
- Repository Steward mailbox tip `5f0faac963861b47b3385083454fdd09e3ff8f4c`; designated 13:50 latest/state consumed as governance advisory only.

## New repository evidence / readiness

There is **no new formal scientific evidence** this cycle. Authoritative scientific evidence remains exactly three annotated `evidence/*` tags: C19-v4, C19-R2 and PD01.

Fresh NI01 facts remain:

- research head `2664951b65dd18883d3d80862e80b86ac66cf24f`;
- preformal `35308269458`: success on that exact head;
- ordinary CI `35308269286`: success on that exact head;
- identity `ni01-no-ignition-selective-prediction-official-v1`: `UNRESERVED / UNCONSUMED`;
- `control/ni01*`: absent;
- `preserve/ni01*`: absent;
- `evidence/ni01*`: absent;
- formal/sealed NI01 authority: absent.

The only new state is MAIN's recovered lease and formal-execution scaffolding step. It does not upgrade a scientific claim and does not itself authorize bypassing final START gates.

## External knowledge integration

### `external_input.literature`

No new Literature stream since `525686fa0c14f64426ca7bf5c89f08cd0bcb9c77` / 04:30 JST.

The existing reduction doctrine remains material to programme interpretation: finite-state, learned/extracted automata, predictive-state/epsilon-machine/causal-state, and recurrent/reservoir/fading-memory mechanisms remain ordinary reduction families, so local/pre-semantic/history-derived/predictive state is not novelty by itself. This continues to bound C19/R2/PD01 claims.

Effect on NI01: **none**. NI01 already prospectively tests the canonical H4 confidence-abstention null. Do not retrofit stronger abstention heads, OOD worlds, predictive-state comparators or result-responsive extensions into this identity. Any such extension is a fresh future object.

Allocation effect: none.

### `external_input.audit`

No new Independent Audit stream since the 10:30 C19-R2 audit. C19-R2 remains `ROBUST_SO_FAR`: the one-way chain, target blindness, registered `atomic_idx` cluster inference, terminal `REDUCED_BY_FSA` statistic and narrow claim boundary were independently confirmed.

**PD01 terminal evidence still has no fresh independent audit.** That audit remains desirable but does not block NI01 because NI01 is independently motivated by H4 and explicitly excludes C19/R2/PD01 outcome rescue. A future object materially justified by PD01 should consume a fresh PD01 audit before STARTED.

Allocation effect: none.

## SUB operating mode / exploratory-incubator review

Latest SUB mode is **`no_op`**. There is no current `exploratory_incubator` candidate, so `sub_exploratory_review` is **not applicable** rather than one of the five candidate classifications.

SUB correctly avoided NI01 authority/admission binding, execution scaffolding, candidate/comparator, source/protocol/package/runtime bindings, CI/pre-START, identity reservation, STARTED, official TEST access, preserve/refetch/join, scoring/bootstrap, evidence and successor design.

`sub_lane: null`

`sub_fallback: null`

## Active-line review

| Line | Strongest evidence / status | Consumed identity / genuine blocker | Shortest path to NEW information | Value / distance | Role |
|---|---|---|---|---|---|
| A01 | `MIXED_PROGRAMME_CLOSED` | prior formal objects closed/consumed | fresh independent programme only | medium / far | closed |
| RV01 | `DEVELOPMENT_POSITIVE_REDUCIBLE / EXPLORATORY_BUDGET_EXHAUSTED` | prior incubator output stays NON_EVIDENTIARY | fresh prospective object only | medium / medium | secondary |
| RV02 | `TERMINAL_CONSTRUCTION_NEGATIVE` | terminal/consumed | fresh object only | low-medium / far | terminal |
| CX/CX01 | `TERMINAL_FORMAL_NEGATIVE` | terminal/consumed | fresh independent object only | low-medium / far | terminal |
| H8/C08 | terminal negative | terminal | distinct fresh object | low-medium / far | terminal |
| H9/C07 | `PRE_START_UNDERSPECIFIED` | state/reset/resource contract unresolved | fresh exact prospective contract | medium / medium | paused secondary |
| C19-v4 | immutable narrow representation PASS / `WEAKENED_NOT_INVALID` | consumed/immutable | no rerun; interpretation bounded by R2 | completed | historical central |
| C19-R1 | scientifically unresolved / operationally terminated | v1/v2 consumed post-START failures | no v3 / no rescue | low now | closed operationally |
| C19-R2 | `TERMINAL_REDUCED_BY_FSA / ROBUST_SO_FAR_AUDITED` | consumed/no-retry | STOP | completed | closed primary |
| PD01 | `TERMINAL_FAIL_REDUCED_BY_FADING_MEMORY` | consumed/no-retry; independent audit pending | STOP; fresh read-only audit only | completed | closed primary |
| **NI01 / H4** | **`PRE_START_READY_FOR_CONDITIONAL_ONE_WAY_AUTHORITY`** | **identity unreserved; final execution/admission gates not yet terminally reconciled** | **finish science-invariant scaffolding -> fresh final checks -> exactly one run** | **very high / near** | **MAIN primary** |

## Parallel decomposition

### `main_lane`

`NI01_NATIVE_NO_IGNITION_SELECTIVE_PREDICTION_ONE_WAY`

MAIN owns **all** critical-path work end-to-end: authority/admission scaffolding, exact source/protocol/package/input/runtime/candidate/comparator/scorer/preserver bindings, target-free DEV threshold derivation, workflow/runner, CI/pre-START mechanical repair, STARTED/no-clobber, official TEST raw acquisition, preservation, independent refetch/digest/cardinality, target materialization, fail-closed join, deterministic scoring/bootstrap, terminal evidence and exact-head reconciliation.

Conditional formal authority remains **exactly once**; this run does not broaden it.

### `sub_lane`

`null`

### `sub_fallback`

`null`

### `no_sub_lane_reason`

No scientifically and operationally independent formal secondary object is prospectively complete. NI01/H4 is MAIN-owned; H9/C07 remains under-specified; RV01 bounded exploration is exhausted; terminal C19/R2/PD01 lines stay closed. No useful non-duplicative incubator target is currently selected.

## `blocked_until`

NI01 STARTED remains blocked until MAIN:

1. finishes a science-invariant execution/admission package carrying an explicitly valid Evidence Analyst authorization for the unchanged NI01 object;
2. proves exact source/protocol/package/input/runtime/candidate/comparator/scorer/preserver binding invariance on the final execution head;
3. verifies `ni01-no-ignition-selective-prediction-official-v1` is still fresh/unSTARTED/unconsumed and control/preserve/evidence namespaces remain collision-free;
4. if any execution/admission change moves the SHA, obtains ordinary CI and dedicated NI01 pre-START green on that **same final SHA**;
5. confirms the target-blind TEST projection excludes truth, target, `decision_justified`, and target-derived scenario labels before immutable raw preservation.

## `do_not_touch`

- C19-v4, C19-R2 and PD01 immutable evidence/tags and consumed identities;
- C19-R1 v1/v2 consumed identities;
- legacy `freeze/*` refs and authoritative evidence tags;
- scheduler definitions;
- SUB exploratory artifacts as scientific evidence;
- NI01 worlds, seeds, native dynamics, comparator input privilege, target-free coverage-matching rule, loss, coverage guard, PASS/FAIL/INCONCLUSIVE thresholds, bootstrap, runtime/device, join key and raw-before-score ordering;
- any stronger comparator/world/threshold chosen in response to a formal NI01 TEST outcome.

## Prospective outcome contingencies

Observed scientific root remains **`NI01_PRE_START_READY_FOR_CONDITIONAL_ONE_WAY_AUTHORITY`**. Operationally MAIN is now in science-invariant `NI01_FORMAL_EXECUTION_SCAFFOLDING`.

- `PRE_START_BLOCKER_MECHANICAL`: MAIN may repair only science-invariant mechanical defects; any head change resets same-final-SHA CI/pre-START requirements.
- `PRE_START_SEMANTIC_GAP`: STOP and return for fresh Analyst review before mutation or STARTED.
- GO -> fresh identity/collision check -> STARTED/no-clobber exactly once -> target-free DEV threshold derivation -> target-blind TEST raw -> immutable preserve -> independent refetch/digest/cardinality -> TEST targets -> unique/total/fail-closed `(world, seed, step_index)` join -> deterministic scoring/bootstrap -> annotated terminal evidence.
- `PASS_NATIVE_NO_IGNITION_ADDS_SELECTIVE_VALUE`: terminal STOP; claim only the registered three-world selective-value result. No same-run OOD/general-H4 extension.
- `FAIL_REDUCED_BY_CONFIDENCE_ABSTENTION`: terminal STOP; no rescue, comparator shopping, threshold retuning or automatic NI01-v2.
- `INCONCLUSIVE`: terminal STOP; no retuning.
- `INVALID_EVIDENCE`: identity consumed; terminal STOP; no salvage/retry.
- `POST_START_FAILURE`: identity consumed; terminal STOP; no salvage/retry/automatic successor.

Same-run MAIN continuation is allowed only through this already-fixed NI01 chain. No terminal result authorizes a new scientific object in the same run.

## Top 3 / GO-STOP

1. **MAIN — finish NI01 execution/admission scaffolding and, only if identity/namespace/binding checks are clean and all required gates are green on the final SHA, execute exactly one formal NI01 one-way chain.** Information value `VERY_HIGH`; distance `NEAR`.
2. **Independent Audit — perform the still-pending fresh read-only PD01 terminal audit; after NI01 terminalization, audit NI01 before any broader H4 claim.** Information value `HIGH`; distance `NEAR_TO_MEDIUM`.
3. **SUB — remain `no_op`; use incubator capacity only if a genuinely distinct, bounded, synthetic/dev NON_EVIDENTIARY question naturally appears.** Information value `LOW_TO_MEDIUM`; distance `OPTIONAL`.

### #1 GO

- identity `ni01-no-ignition-selective-prediction-official-v1` fresh / unSTARTED / unconsumed;
- no control/preserve/evidence namespace collision;
- exact source/protocol/package/input/runtime/candidate/comparator/scorer/preserver bindings frozen and internally consistent;
- reviewed scientific contract unchanged;
- ordinary CI and dedicated NI01 pre-START green on the same final execution SHA if that SHA differs from the already-green research head;
- STARTED/no-clobber before official TEST read;
- DEV threshold derivation target-free;
- TEST raw target-blind and free of truth/target/`decision_justified`/target-derived scenario labels;
- immutable raw preservation before TEST target materialization or scoring;
- independent refetch verifies bytes/digest/cardinality before target materialization;
- unique/total/fail-closed join on `(world, seed, step_index)`;
- deterministic registered scoring/bootstrap only.

Scientific falsifiers remain prospectively fixed: coverage difference `<=0.03` in every world; PASS if the effect CI lower bound is `>=0.02` and every-world point effect is `>0`; FAIL if the effect CI upper bound is `<=0.005`; every other valid outcome is INCONCLUSIVE.

### #1 STOP

STOP before STARTED if any scientific selection must change, binding invariance cannot be proved, identity is not fresh, a namespace collision exists, or final-SHA gates cannot be made green without semantic change. After STARTED, invalid evidence or execution failure consumes the identity and terminates without salvage/retry.

## Governance advisory

Fresh remote verification remains: `main@ebed6abfa941c83b36d2e3bddd04e7c5fb0dbe9d`, `protected=false`; repository rulesets `0`; legacy `freeze/*` branches `13`; authoritative annotated `evidence/*` tags `3`; open non-PR Issue `#139`; open PRs `#148/#149`.

Issue #139's inventory is current at three evidence tags; the substantive server-side authoritative-tag update/delete protection gap remains open. No freeze migration should proceed before safe protected migration semantics exist.

Repository Steward's main-promotion inventory remains advisory only. Strong outcome-independent candidates, after neutral extraction onto current `main` plus fresh CI, include anonymous event/distribution primitives, architecture-neutral comparator protocol shape, snapshot/restore invariants, descriptive resource accounting, generic privilege validation, external-only transcript hashing/validation, and generic leak/self-training/distribution tests. G4/G5/G7/G8 implementations and CX01 scientific taxonomy/worlds/threshold/result semantics remain research-local. No promotion is performed here.

PR #148 and #149 remain open ordinary-review control-plane/helper PRs; they are not scientific evidence and are not merged by this role.

## Orchestrator handoff

MAIN takes **NI01/H4** and owns **ALL critical-path fixes**, including the current formal-execution scaffolding. Formal SUB takes nothing; otherwise SUB may incubate only a distinct line under strict NON_EVIDENTIARY synthetic/dev boundaries if one naturally appears. There is no fallback. MAIN must not absorb future reserved SUB work; none is reserved now. SUB must not take any NI01 blocker. Neither worker touches consumed C19/R1/R2/PD01 evidence, legacy freezes, or scheduler definitions.

Repartition only if: (a) a fresh external/audit finding materially invalidates NI01 before STARTED; (b) NI01 hits a semantic/scientific pre-START gap; or (c) a genuinely independent prospective SUB object becomes complete. Current literature, audit and SUB inputs do not trigger repartition.

Result branches MAIN may continue through in the same run are only the already-fixed NI01 PASS / FAIL / INCONCLUSIVE / INVALID_EVIDENCE / POST_START_FAILURE terminalization paths after a valid STARTED. No automatic successor is authorized.
