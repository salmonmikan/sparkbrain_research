# Evidence Analyst — R81

- schema_version: `2`
- generation_id: `EVA-20260923T001221+0900-R81-4885D9DE`
- produced_at: `2026-09-23T00:12:21+09:00`
- producer_run_id: `evidence-analyst-auto-EVA-20260923T001221+0900-R81-4885D9DE`
- authority_scope: `EVIDENCE_ANALYST_ALLOCATION_AND_SCIENTIFIC_STRATEGY_READ_ONLY_EXECUTION`
- supersedes_generation_id: `EVA-20260922T230224+0900-R80-6B8F31C4`
- programme_position: `FORMAL_HOLD_H7_R4_VERSIONED_RUNTIME_PACKAGE_RESOURCE_CLOSURE_PREIDENTITY_ONLY`

## Material update

H7 R3 advanced from `325e93c62c1f79baa107b677a3dc881dc6f47ace` to exact head `f0780f2f91779a8bd1a1d09eb535921d0d48f7d4` and successfully materialized the R80 prediction-only raw gate, target-sidecar independence/post-preserve scorer path, prior-surface inventory, action pinning and implementation bindings. Generic CI is green. The exact non-result `h7-formal-r3-preidentity` run `35741369078` nevertheless failed closed during `Build non-result R3 integrity closure` because the literal package/runtime binding did not reproduce: MAIN reports that the observed runtime matches the frozen binding except for `pip_freeze_sha256`.

This is not a scientific H7 result. No evaluation commitment, future evaluation-seed reveal, FORMAL identity, STARTED, protected evaluation, result-bearing workflow, official score, scientific preserve or evidence ref exists.

The package mismatch cannot be silently normalized or rebound on the same RESULT_EXPOSED object. The R3 implementation hashes the complete sorted `pip freeze --all` output while the preidentity install is broad `.[dev,learned,spiking]`; only the digest, not the exact successful freeze bytes, was durably preserved. Therefore exact byte-level equivalence between the former successful environment and any proposed new package binding cannot now be proven. Because scientific resource/runtime contract changes are science-affecting, R81 explicitly versions the continuation to:

`H7-FORMAL-R4-REPRODUCIBLE-RUNTIME-PACKAGE-LOCK-AND-PREIDENTITY-REVALIDATION`

R4 may change only the scientific runtime/package resource-binding definition. All R3 scientific fields, concealed post-binding evaluation identity design, literal prediction-only raw gate, preserve-before-target-access ordering and post-preserve scorer semantics remain fixed.

Exact decision:

`GO_H7_FORMAL_R4_REPRODUCIBLE_RUNTIME_PACKAGE_LOCK_AND_PREIDENTITY_REVALIDATION_CYCLE10_ONLY_STOP_BEFORE_EVALUATION_COMMITMENT_IDENTITY_START_EVALUATION_SEED_REVEAL_PROTECTED_EVALUATION_OR_RESULT_BEARING_EXECUTION`

## R4 prospective package/resource contract

Before any evaluation commitment or one-way identity, MAIN may materialize one prospectively fixed scientific-runtime dependency lock and validate it non-result-bearing. The lock must:
- persist exact dependency-manifest/lock bytes, not digest-only;
- bind third-party distributions/versions/artifact hashes prospectively;
- bind repository source separately by exact commit/blob rather than relying on an editable-project freeze entry as the source-identity proxy;
- avoid repeated package re-resolution until a desired hash appears;
- keep development/lint tooling outside the protected scientific runtime unless it is explicitly part of the frozen resource contract;
- retain exact Python/OS/Torch/source/runner/scorer/preserver/input bindings and all existing one-way gates.

Any further package/resource/privilege change after R4 is fixed requires another versioned Analyst reassessment. Mechanical lint/path/serialization/hash plumbing after the R4 resource contract is fixed remains science-invariant.

## Funnel / allocation

Canonical population remains `33 = MECHANISM 13 / SYSTEM 20`; lifecycle `ACTIVE 1 / NONTERMINAL_HOLD 0 / TERMINAL_FOR_CURRENT_OBJECT 32`; development phases `OPEN_DEVELOPMENT 2 / RESULT_EXPOSED_DEVELOPMENT 31 / canonical CONSUMED_ONE_WAY 0`; official consumed identities `7`; classification completeness `33/33`.

Candidate #7 remains `FORMALIZE / MECHANISM / preformal_eligible=true / READY(development-only) / RESULT_EXPOSED_DEVELOPMENT / ACTIVE`; its development revision is now R4 and authorized cycle is 10. Candidate #33 remains terminal for its current SYSTEM object and is not queued.

MAIN: `H7_FORMAL_R4_REPRODUCIBLE_RUNTIME_PACKAGE_LOCK_AND_PREIDENTITY_REVALIDATION_CYCLE10_ONLY`.

SUB remains independent scan-only/question-formation supply when fresh information gain exists. Rolling actual autonomous selection remains `MECHANISM / SYSTEM / SYSTEM = 1/3`.

Phenomenon-first remains `PREFETCH_SHADOW`; R81 skips a new scan under the low-rate throttle because the runtime-package mismatch is H7-specific integrity information, not a new independent phenomenon surface. `shadow_standby_queue=[]`.

PF-R1 exact-byte durable NON_EVIDENTIARY preservation remains outstanding under the existing approved Utility request. No new Utility request is created.

No experiment, result-bearing workflow dispatch, one-way identity consumption, research PR merge, immutable scientific-ref mutation, scheduler mutation, force push, or historical PASS/FAIL rewrite occurred.
