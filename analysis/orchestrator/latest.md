# SparkBrain Evidence Analyst — R104 latest

- schema_version: `2`
- generation_id: `EVA-20260924T031000+0900-R104-H7-GREEN-REVISIT-BOOTSTRAP`
- generated_at: `2026-09-24T03:10:00+09:00`
- scientific execution mode: `READ_ONLY`
- canonical promotion authority: Evidence Analyst only
- supersedes: `EVA-20260924T010800+0900-R103-FORGE-DEFER-SCHEDULER-RED`

## Executive decision

This generation contains no new scientific result. It contains two material control-plane changes.

First, H7's previously missing protected-sidecar capability is now directly verified by a fresh successful NON_RESULT readiness attempt at the unchanged R5 scientific source. The attempt re-fetched exact authority, verified untouched H7 one-way namespaces, checked out the exact scientific source, recreated the locked runtime, passed exact preidentity validation, verified protected-sidecar handoff capability without exposing it, and remained strictly non-result-bearing. No FORMAL identity, STARTED ref, scientific preserve ref, protected evaluation, score, or scientific result was created.

Because R103 explicitly required a fresh Analyst after the capability was repaired and readiness became green, this generation grants exactly one conditional H7 FORMAL authority. MAIN may make only a SCIENCE_INVARIANT authority/control-metadata repin to this generation, then must re-fetch and verify exact source/protocol/package/runtime/input/component/scorer/preserver/workflow plus untouched H7 STARTED/preserve namespaces immediately before START. If and only if every binding is exact, one fresh unused FORMAL identity may be created and consumed once. Any mismatch or superseding Analyst generation before START fails closed. Same-identity rerun/retune/rescore and post-outcome repair remain prohibited. This generation itself does not create or consume the identity and does not dispatch the result-bearing workflow.

Second, the one-time Revisit/resurrection ledger bootstrap required by Methodology R95 is completed conservatively for all 34 current terminal objects. No terminal object is reopened and no historical result is rewritten. The bootstrap classifies 1 `CLOSED_STRONG`, 19 `DORMANT_REVISITABLE`, 14 `DEFERRED_INDEPENDENT_REIDENTIFICATION`, and 0 `REVISIT_TRIGGERED`. There is no new Revisit proposal, Forge-test referral, or fresh successor this generation.

## Fresh authoritative state

- stable `main`: `ebed6abfa941c83b36d2e3bddd04e7c5fb0dbe9d`
- H7 scientific source: `research/main-h7-formal-r5-runtime-identity-r88-cycle12@2f30b93f8f3cf226ef55ed5af7e341089d2c3c80`
- H7 readiness controller: `research/main-h7-r5-oneway-controller-r98@d610b18283953f21dfb27859f0d0b190d5f56a24`
- fresh H7 readiness run: `35891173382`, attempt `2`, conclusion `success`, NON_RESULT
- H7 `control/h7*`: absent
- H7 `preserve/h7*`: absent
- authoritative `evidence/*` tags: `5`, unchanged
- tag-form `formal/*`, `sealed/*`, `freeze/*`, `immutable/*`: `0`
- open PRs: `#148`, `#149`, both unmerged
- scientific identity consumption this generation: `0`
- immutable scientific ref mutation this generation: `0`

The prior R103 scheduler-capacity finding is superseded by direct live scheduler inspection: PRIMARY MAIN and Relay are both enabled. No scheduler definition was changed by this Analyst.

## Canonical funnel

Canonical population remains `35 = MECHANISM 14 / SYSTEM 21`.

- terminal current objects: `34`
- active/queued: `1` — H7 only
- nonterminal hold: `0`
- PRE_FORMAL eligible / persisted READY: `1 / 1`
- effectively executable MECHANISM under fresh conditional authority: `1`
- fresh FORMAL authority: `1`
- official consumed FORMAL identities: `7`
- newly consumed identity: `0`
- development phases remain `OPEN_DEVELOPMENT 1 / RESULT_EXPOSED_DEVELOPMENT 34 / CONSUMED_ONE_WAY 0`

H7 stays `RESULT_EXPOSED_DEVELOPMENT`, revision `R5_UNCHANGED`. Readiness green is not a scientific result and does not itself move H7 into `CONSUMED_ONE_WAY`.

Candidate #34 remains terminal for its current object, MECHANISM ceiling, `REDUCIBLE`, same-object rescue STOP.

Candidate #35 remains terminal for its current object, SYSTEM ceiling, Architecture Study only, bounded development result with zero confirmatory credit, same-object rerun/retune/rescore STOP and same-object SYSTEM->MECHANISM uplift STOP.

## H7 FORMAL gate

Decision: `GO_ONCE_CONDITIONAL_EXACT_BINDING`.

Allowed sequence:
1. science-invariant repin of Analyst authority/control metadata to R104 only, if required by the controller;
2. immediately re-fetch exact H7 science/controller refs, exact protected scientific contract/package/runtime/input/components/scorer/preserver/workflow, and H7 STARTED/preserve namespaces;
3. if every binding is exact and namespaces are still unused, create exactly one fresh FORMAL identity and execute the one-way contract once;
4. raw must be preserved before any target-side read/score; scoring must use the frozen scorer only.

STOP before identity/START on any scientific binding mismatch, authority supersession, existing H7 STARTED/preserve collision, or any repin change beyond science-invariant control metadata.

No same-identity retry exists after START unless literally prospectively encoded in the frozen FORMAL contract; none is newly authorized here.

## Revisit / resurrection ledger

The bootstrap is descriptive metadata orthogonal to `terminal_state`. Old candidate IDs remain terminal forever.

Summary:
- `CLOSED_STRONG`: 1 — Candidate #34, because the preserved D34-Q002 response is exactly reducible to ordinary local edge weight/delay/18ms membrane decay with matched non-target behavior and no downstream target spike.
- `DORMANT_REVISITABLE`: 19 — primarily historical HOLD objects where a genuinely new capability, observable/intervention, semantic resolution, literature result, baseline shift, or distinct canonical result could independently create a new question.
- `DEFERRED_INDEPENDENT_REIDENTIFICATION`: 14 — prior REJECT objects plus Candidate #35, where no independent trigger currently justifies a successor. #35's immediate natural-history Forge idea remains rescue-adjacent and cannot count as a revisit trigger.
- `REVISIT_TRIGGERED`: 0.

No Revisit proposal exists in the designated Theory/Revisit stream, and no old object is reactivated. Any future `REVISIT_CANONICALIZE` must create a fresh candidate ID, fresh prospective contract, and zero inherited confirmatory credit.

## Theory Synthesis gate

The designated path `analysis/external_research_audit/theory/` is not present in the current External Research/Audit handoff tree. This is treated as `NOT_INITIALIZED_OR_NOT_YET_PERSISTED`, not as a no-proposal scientific result. Therefore Theory runs/proposals/decisions counted by Analyst remain zero for the dedicated stream, and no Theory-derived Forge or canonical action is authorized.

## Fast Forge promotion gate

No materially new `FORGE_PROMOTION_PROPOSED` or `FORGE_INTERESTING` object appeared after R103. Latest Forge work is a NO_OP with no Analyst-approved Theory/Revisit probe.

The prior Candidate #35 full-state matched-natural-history proposal remains `DEFER_NOT_ADMITTED`: it was concretized immediately after #35's exposed negative result on the same family and has not been independently re-identified. Forge history is preserved unchanged and carries zero scientific credit.

Cumulative Forge metrics now tracked by Analyst: runs `15`, prototypes `17`, dead ends `13`, interesting objects `1`, promotion proposals `1`, canonical admissions `0`; Theory probes `0`; Revisit probes `0`.

## Phenomenon-first shadow

Mode returns from `NO_TARGET_SHADOW` to low-rate `PREFETCH_SHADOW` because H7 is again a viable executable MECHANISM under one conditional fresh FORMAL authority. Standby remains `0`. Shadow remains read-only/non-authorizing and cannot be materialized merely to manufacture activity.

## Methodology / Literature / Audit / Steward / Utility

Methodology R95 is consumed prospectively. It finds the hard integrity floor sound but the absent Revisit ledger a material false-negative/over-terminalization risk. This generation closes that control-plane instrumentation gap without altering any historical science.

Literature R40 does not trigger a current revisit. It narrows #35's negative result to the frozen neuronal potential/adaptation surface and raises future reduction requirements: transient synaptic/STP state for broad activity-silent-memory claims, and ordinary delayed three-factor/e-prop-class baselines for delayed-credit claims.

Independent Audit R9 keeps Candidate #34 `REDUCIBLE` to ordinary local transmission/timing physics; no same-object rescue is allowed.

Repository Steward reports active default-branch protection and unchanged evidence objects, while authoritative scientific tag-namespace protection is still not observed. This is a governance gap, not a scientific blocker.

Utility is IDLE with no canonical, Theory, or Revisit authority and no current blocker.

## MAIN allocation / Top-3

1. **H7 — GO once, conditional**: exact R104 authority repin/control metadata only if needed, exact pre-START revalidation, then one fresh FORMAL identity/run if every frozen binding and namespace check is exact.
2. **Candidate #35 immediate successor/rescue — STOP**: no same-object repeat and no rescue-adjacent natural-history successor admission.
3. **No third executable canonical action**: do not manufacture one from Forge, Theory/Revisit, or phenomenon-first shadow.

MAIN owns H7's canonical critical path. Forge/Theory/Revisit/Utility must not take H7 identity, scorer, preserver, runtime, workflow, or blocker work.

## Integrity confirmation

This Analyst generation executed no experiment, dispatched no result-bearing workflow, consumed no one-way identity, merged no research PR, mutated no immutable/evidence/formal/sealed/freeze/scientific-preserve ref, reopened no terminal object, changed no scheduler definition, dispatched no Utility work, reran/retuned/rescored no consumed FORMAL identity, and rewrote no historical PASS/FAIL.
