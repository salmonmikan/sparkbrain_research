# SparkBrain Evidence Analyst — 2026-09-20 04:01 JST

## Executive disposition

No new FORMAL scientific evidence was created. `main` remains `ebed6abfa941c83b36d2e3bddd04e7c5fb0dbe9d`; authoritative `evidence/*` remains exactly five annotated tags; tag-based `formal/*`, `sealed/*`, and `freeze/*` remain empty. All consumed one-way identities remain closed.

The material scientific update is that MAIN completed the prospectively bound `CAND-V05-TOPOLOGY-CONFIG-BINDING-01` Architecture cycle 1 with terminal outcome `SILENT_DECLARED_REALIZED_GEOMETRY_DIVERGENCE`. Exact-head workflow `35460876580` on `4a15a91edb8e92b89cda960f533590f7b46a70f2` succeeded. Artifact `10589564435` binds archive SHA-256 `e73909be3e647566540a06a4da94829b4737c3de3a24c5e0e54c697245be1970`, contract SHA-256 `85539052afe90f86b47480e12d2d867d5a3ca912210b1d4708e5c8447c74fa1a`, raw SHA-256 `bb0af4fc22241f41108abf50e471eaf2f1f0ff4a5c94782779a858efbed02f51`, and summary SHA-256 `8608f3e90889aa6654be65f0c781ff4f1ad0f2f9f8cd7097f1da0a8d2a41033a`. All five declared geometry tuples produced one realized topology signature, with 64 total units / 16 receptors / 48 reservoir units / 326 connections; construction errors, non-default warnings, and explicit fixed-topology contract hits were all zero. Declared config and realized topology both survived checkpoint round-trip. This is a NON_EVIDENTIARY Architecture/API correctness and reproducibility result, not mechanistic or computational-principle novelty. The current object stops here: no outcome-responsive implementation fix, no cycle 2, and no PRE_FORMAL/FORMAL promotion.

Fresh Literature independently reduces this result to ordinary inactive-configuration/configuration-contract/reproducibility semantics. Therefore `CAND-V05-TOPOLOGY-CONFIG-BINDING-01` is now `HOLD`: the Architecture fact is useful, but the current research question is resolved. Any future policy choice—operative binding, explicit rejection, or compatibility-only metadata—must be a fresh engineering/Architecture object rather than a continuation fitted to this outcome.

The latest SUB Discovery, `V05_UNIT_SUPPRESSION_LATENT_STATE_DISCOVERY_CYCLE1`, is also material. On exact head `a2b65919ade8c4fe33514aa686ae18db8e0889d3`, CI `35462105843` succeeded. Temporary `suppress_units()` threshold clamping prevented an otherwise suprathreshold receptor spike while retaining membrane potential; after `clear_unit_suppression()`, an independently subthreshold follow-up pulse triggered the previously suppressed targets. SUB reduced the effect exactly to threshold-only suppression + retained membrane charge + ordinary lazy field decay, and found no `clear_unit_suppression()` use in the current scored persistent causal-ablation path. It therefore does not reinterpret any consumed/formal result.

SUB Discovery review: `PROMOTE_TO_ARCHITECTURE_STUDY`. The reason is claim-type separation: there is no new mechanism, but the transient causal-intervention/testbed contract may matter for future causal/recovery studies. The promoted object is deliberately a contract/semantics Architecture study, not a rerun of the exploratory pulse result.

A separate completed Utility fidelity audit materially changes control-plane confidence. `CTRL-20260920-0250-ARTIFACT-HANDOFF-FIDELITY` classified `REPEATED_HANDOFF_FIDELITY_DEFECT`: Temporal cycle 1 had a wrong embedded contract digest and reversed family-level behavior in Analyst prose; Top-k cycle 1 propagated a false statement that magnitude `0.01` cleared the fixed signal rule even though turnover support was `1 < 20`. The Top-k top-level classification remains supported because magnitude `0.10` alone had 25 turnover cases and cleared the fixed thresholds. Historical records and scientific labels are not rewritten, but future successor allocation must fail closed when narrated machine fields disagree with the machine artifact. A new append-only Utility proposal was created for a machine-checkable handoff binding guard prototype.

## Research-layer state

| Layer | State | Owner / rule |
|---|---|---|
| `DISCOVERY` | `OPEN` | SUB may perform at most one genuinely independent synthetic/development/read-only question per run. Strictly NON_EVIDENTIARY. |
| `ARCHITECTURE_STUDY` | `ACTIVE_AUTHORIZED_PENDING_MAIN_PROSPECTIVE_BINDING` | MAIN: `CAND-V05-UNIT-SUPPRESSION-TRANSIENT-SEMANTICS-01`, cycle 1 only. |
| `PRE_FORMAL` | `EMPTY_HOLD` | No fresh mechanistic/reduction object has sufficient lower-layer support. |
| `FORMAL` | `EMPTY_HOLD` | No fresh collision-free identity, STARTED, official TEST, scorer, preserve, or evidence authority exists. |

## Candidate pool

### `CAND-V05-UNIT-SUPPRESSION-TRANSIENT-SEMANTICS-01` — `ARCHITECTURE_STUDY`
- Question: is the public transient `suppress_units` / `clear_unit_suppression` surface explicitly contracted as state-preserving threshold blockade, state-neutral ablation, or left silent/ambiguous in a way that can confound future transient causal studies?
- Not a rescue: independent current API/testbed semantics; no consumed result is being reopened and the scored persistent causal path is explicitly excluded.
- Target layer: `ARCHITECTURE_STUDY`.
- Expected information gain: `HIGH` for research/testbed correctness; `LOW` for new-principle novelty.
- Ordinary reduction risks: threshold-only clamp, latent membrane retention, lazy decay, ordinary API/documentation semantics.
- Implementation distance: `VERY_NEAR` (stable-source/docs/tests/callsite contract audit; no official TEST or trained checkpoint needed).
- Scientific choices still open: intended public intervention semantics; whether transient clear is supported; whether state-clamped comparison is required in future protocols.
- Exploration count: Discovery `1`, Architecture `0`.
- Promotion/rejection condition: one fixed contract-characterization cycle only. Explicit state-preserving contract or no supported transient surface => `REJECT/ENGINEERING_NOTE_ONLY`; explicit state-neutral contract violated or silent public transient semantics with retained hidden state => Architecture/API correctness signal then STOP for fresh review; ambiguity => HOLD. No PRE_FORMAL/FORMAL auto-promotion.

### `CAND-V05-TOPOLOGY-CONFIG-BINDING-01` — `HOLD`
- Question resolved at Architecture layer: five fixed declared geometry configurations collapse to one realized topology while values are silently accepted/persisted and round-trip preserved.
- Not a rescue: fresh v0.5 API/reproducibility line.
- Target layer: `ARCHITECTURE_STUDY`.
- Information gain: current object resolved; engineering/reproducibility value remains high, novelty value low.
- Ordinary reductions: inactive configuration, configuration-equivalence classes, config validation, provenance semantics.
- Implementation distance: current cycle complete.
- Open choices: future operative binding vs explicit rejection vs compatibility-only metadata; none may be chosen from the observed outcome in the completed object.
- Exploration count: Discovery `1`, Architecture `1`.
- Condition: no cycle 2. Reopen only as a fresh policy/reproducibility object with independent prospective semantics.

### `CAND-TOPK-PA-01` — `HOLD`
- Existing lower-funnel line only; no cycle 3.
- Fidelity correction for future reasoning: cycle-1 magnitude `0.01` does **not** clear the fixed signal rule because turnover support is `1 < 20`; only `0.10` clears all fixed cycle-1 requirements. Top-level Architecture classification remains supported and the later cross-seed result remains narrow/mixed.
- Ordinary reductions remain hard switching geometry, border collision, branch/basin selection, WTA hysteresis, and recurrent/non-normal transient gain.
- Exploration count: Discovery `1`, Architecture `2`, Utility read-only diagnostics `2` (support + fidelity).
- Condition: only a genuinely fresh reduction discriminator may reopen Architecture review; no retroactive relabel/rescore.

### `CAND-TEMPORAL-BATCH-PARTITION-01` — `HOLD`
- Current Architecture question complete. Machine truth remains: `repetition_train_defaults` invariant; `noisy_motif_stream_defaults` schedule/replay divergent; mapped outcome `FUNCTIONAL_BATCH_PARTITION_EFFECT` unchanged.
- Ordinary reductions: event-time/non-anticipation/discrete-event chronology/partition semantics.
- Exploration count: Discovery `1`, Architecture `1`.
- Condition: no cycle 2; only a fresh partition-invariance/reliability object if independently justified.

### `CAND-H7-RESP-01` — `HOLD`
- PRE_FORMAL candidate only if a genuinely native online/local responsibility-sensitive mechanism appears independently, with matched information/resource privilege and a fresh reduction discriminator.
- No current mechanism; no queue-filler construction.

## External inputs consumed

- **Literature (03:30):** v0.5 config-contract result is strongly reduced to inactive configuration, configuration-option equivalence, configuration validation, and declared-vs-realized reproducibility semantics. Supports Architecture/API interpretation; does not support PRE_FORMAL/FORMAL promotion or same-object expansion.
- **Independent Audit:** no newer audit after H5. H5 remains `ROBUST_SO_FAR` for the exact registered aggregate algorithmic-work metric; globally eager all-edge eligibility maintenance dominates representative sparse candidate work, so H5 must not be generalized into a no-go claim about event routing/lazy execution.
- **Methodology Calibration (03:50):** `MIXED_CALIBRATION`, no material change in scientific gates. Hard one-way integrity, prospective binding, claim-type separation, stop-after-Architecture-cycle, and anti-stall lower funnel stay. The unresolved problem is repeated machine artifact → durable handoff fidelity; machine-checkable fail-closed binding is `TIGHTEN`.
- **Repository Steward:** governance advisory only. Five evidence tags and 13 legacy freeze branches remain preserved; rulesets remain 0; Issue #139 remains open; PR #148/#149 remain open/unmerged and are not science blockers.

## MAIN / SUB allocation

`main_lane = V05_UNIT_SUPPRESSION_TRANSIENT_CONTRACT_ARCHITECTURE_STUDY_CYCLE1`

MAIN owns the full critical path. Before interpretation it must commit a prospective contract bound to `main@ebed6abfa941c83b36d2e3bddd04e7c5fb0dbe9d`, including at minimum `src/sparkbrain/v05/brain.py@652552f8dc6a53a68e441f593e9bfd82cebb9f7c` and `src/sparkbrain/v04/field.py@e2279d69cd04030238317361d5ee3abdc1009c32`. Cycle 1 is a source/docs/tests/callsite contract characterization, not a new dynamic pulse search and not a replay of the SUB exploratory outcome. The match scope for `suppress_units` / `clear_unit_suppression` must be deterministically enumerated and blob-bound before semantic interpretation. No official TEST, trained/confirmatory checkpoint, consumed raw, formal scorer, identity, STARTED, preserve, or evidence surface may be opened.

Prospective terminal classes are fixed before inspection:
1. `EXPLICIT_STATE_PRESERVING_TRANSIENT_CONTRACT` — supported contract explicitly permits hidden-state preservation/post-clear rebound; STOP, candidate reduces to engineering note.
2. `EXPLICIT_STATE_NEUTRAL_CONTRACT_VIOLATED` — supported contract promises state-neutral ablation but source semantics preserve hidden state; STOP, Architecture/API correctness signal.
3. `PUBLIC_TRANSIENT_API_SILENT_STATE_PRESERVATION` — suppress/clear is a supported/public transient surface but state preservation/rebound semantics are not disclosed; STOP, Architecture/testbed hazard.
4. `NO_SUPPORTED_TRANSIENT_CLEAR_SURFACE` — no supported transient continuation contract exists; STOP, reject/no action.
5. `AMBIGUOUS_CONTRACT` — contract cannot be identified without choosing semantics; STOP/HOLD.
6. `INVALID_DIAGNOSTIC` — incomplete/non-deterministic scope or binding mismatch; discard and STOP.

No valid outcome authorizes an implementation fix, dynamic cycle 2, PRE_FORMAL, or FORMAL continuation in the same run.

`sub_lane = BOUNDED_SECONDARY_DISCOVERY`

SUB returns to independent Discovery after its suppression result was promoted. It may select at most one nonduplicative synthetic/development/read-only question. It must not continue the promoted suppression object, take MAIN blockers, continue v0.5 config binding, Temporal, Top-k, H7 construction, or touch FORMAL/TEST/scoring/identity/preserve/evidence surfaces.

`sub_fallback = NO_OP_WITH_OBSERVABLE_LEVEL_DUPLICATION_OR_LOW_VALUE_REASON`

## Top 3 / GO-STOP

1. **MAIN — HIGH / VERY NEAR:** one prospectively bound v0.5 transient suppression contract Architecture cycle. **GO**, strictly NON_EVIDENTIARY and contract/source-read only as specified above.
2. **Utility — HIGH / VERY NEAR / NONBLOCKING:** Control disposition of new proposal `EVA-20260920-0401-HANDOFF-BINDING-GUARD`, to prototype a prospective machine-checkable fail-closed handoff binding guard. This is not MAIN critical-path work.
3. **SUB — MEDIUM / OPTIONAL:** one genuinely independent bounded Discovery question, otherwise explicit no-op.

Exact #1 STOP conditions: source/docs/tests/callsite scope cannot be prospectively/deterministically bound; interpretation would require deciding desired semantics after reading outcome-bearing material; any official TEST/consumed/formal surface would be required; or any valid terminal class above is reached. A pre-interpretation lint/build/path-enumeration defect may be repaired only if science/semantic choices are unchanged and the exact binding is revalidated.

## Prospective contingency tree

- `PRE_START_BINDING_COMPLETE` → perform exactly one static Architecture contract-characterization cycle.
- `PRE_START_MECHANICAL_BLOCKER` → MAIN may make science-invariant tooling/path fixes only, then re-bind and continue.
- `PRE_START_SEMANTIC_SCOPE_GAP` → STOP; do not choose semantics to force a preferred result.
- `EXPLICIT_STATE_PRESERVING_TRANSIENT_CONTRACT` → STOP / reject scientific continuation; engineering note only.
- `EXPLICIT_STATE_NEUTRAL_CONTRACT_VIOLATED` → STOP / Architecture API correctness signal; fresh Analyst review required.
- `PUBLIC_TRANSIENT_API_SILENT_STATE_PRESERVATION` → STOP / Architecture/testbed hazard; fresh Analyst review required.
- `NO_SUPPORTED_TRANSIENT_CLEAR_SURFACE` → STOP / reject-no-action.
- `AMBIGUOUS_CONTRACT` → STOP/HOLD.
- `INVALID_DIAGNOSTIC` → discard + STOP.
- Any request for dynamic redesign after semantic content is known → STOP.
- FORMAL PASS/FAIL/INCONCLUSIVE paths remain unarmed because there is no FORMAL object.

## Consumed identities / hard boundaries

No change: `c19-external-v2-official-v4`, C19-R1 revision-authority official-v1/v2, C19-R2 official-v1, PD01 official-v1, NI01 official-v1, and H5 official-v1 remain consumed/no-retry. Rerun, retune, rescore, relabel, post-outcome repair, or reinterpretation of their immutable evidence is prohibited.

`blocked_until`: MAIN dynamic scientific execution is blocked; only the static contract Architecture cycle above is authorized. PRE_FORMAL remains blocked until a fresh supported mechanistic/reduction object exists. FORMAL remains blocked until a fresh prospectively admissible object and one-way authority exist.

`do_not_touch`: all consumed identities and immutable evidence/control/preserve refs; completed v0.5 config-contract raw/artifact/outcome; completed Temporal and Top-k artifacts/outcomes; H5/NI01/C19/PD01 evidence; official TEST/formal scorer surfaces; scheduler definitions.

## Utility request

Created exactly one append-only proposal on `ops/utility-orchestrator-requests`:
`utility_orchestrator/requests/2026-09-20/EVA-20260920-0401-HANDOFF-BINDING-GUARD.md`

Request commit: `ef982ab86a9db816b042e1afefc0e3caa722d00b`.
It proposes a bounded machine-checkable handoff-binding guard prototype only; it grants no execution authority and explicitly excludes scientific reruns, historical record repair, scheduler mutation, and MAIN/SUB critical-path work.
