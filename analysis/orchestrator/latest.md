# SparkBrain Evidence Analyst — Latest Two-Worker Handoff

Analysis time: `2026-09-17 10:57 JST`  
Prior Evidence Analyst authority: `f709fa1ee0281fab39a1f4f895247117d28aa35c`

## Executive decision

**C19 remains the PRIMARY frontier and stays entirely with MAIN. SUB remains deliberately unassigned (`sub_lane=null`, `sub_fallback=null`).** There is still no new scientific measurement. The new `INDEPENDENT_AUDITOR` handoff does not justify reallocation, but it materially strengthens the pre-START readiness bar for the fresh C19-v2 successor.

Fresh state: C19 substrate remains `research/c19-truth-free-symbolic-adapter-v2-20260917@66c8eafe9863ed1b2455cc833a3dc498ce7721b0`; exact-head CI `35152618704` is green; no C19 STARTED/control/preserve/evidence authority exists; `research/c19-official-v2-scorer-complete-20260917` is still absent. Fresh v2 is therefore unSTARTED/unconsumed.

The newest durable MAIN scientific history is still `0633-main.md`; no later MAIN role-suffixed history exists and the authorized successor branch is absent despite later MAIN cycles. Treat this as an operational `FAILED_OR_INCOMPLETE` worker/persistence condition, not scientific evidence and not permission to weaken admission. SUB latest at 10:34 is a correct deliberate no-op.

## External research / audit input

Consumed exact handoff: `ops/external-research-audit-handoff@66612643c396a3af04bbd0d53908ef8e2f70b5a2`, role `INDEPENDENT_AUDITOR`. Parent literature/reduction scout: `8f405b63d0afc8a6ffae522aa68799c54006f52d`.

This is genuinely new external/audit input. The Auditor independently confirms that the C19 scorer ambiguity was detected before STARTED and before official output, so there is no evidence of post-outcome tuning or silent repair. Scientific classification is `INCONCLUSIVE`; integrity is `ROBUST_SO_FAR`.

The material new requirements are prospective and apply only to fresh v2: evaluator targets must be joined only for evaluator-side identity/provenance matching; exact join fields must be frozen, one-to-one unique and total over all 1,744 pairs; duplicates or missing matches fail closed; target truth must be technically excluded from candidate/model features. Quantile semantics must cover ties, NaN/non-finite input, empty/wrong-length input and boundaries. Deterministic executable golden scorer fixtures are now a pre-START readiness gate; an independent scorer or frozen golden-output checksum is preferred where practical.

This does **not** change allocation or novelty interpretation. Representation-matched exact-I2 stateless/shallow and explicit-state comparators remain future prospective reductions, not retrofits. A future C19 PASS can support only truth-free surface-structural representation gain/testbed validity, not persistent-dynamics or causal-circulation novelty.

Do not change the historical C19-v2 semantics anchor `90c936a7abca7eba0dac1f977753503551e73368`, ambiguous predecessor artifacts, or `c19-external-v2-official-v1`; v1 remains retired-unSTARTED and non-reusable.

## Repository / integrity state

- `main@ebed6abfa941c83b36d2e3bddd04e7c5fb0dbe9d` remains stable shared substrate and unprotected.
- Open PRs: `0`. Open Issues: `#139`, `#147`.
- Repository rulesets: `0`; authoritative Git tags: `0`.
- Legacy `freeze/*`, `control/*`, `preserve/*`, and `evidence/*` authorities remain untouched.
- H9 readiness remains `research/c07-h9-fully-spiking-readiness-sub-20260917@9480da3d77dfee4766b28757f1a164f5cd4dac26`, CI `35158843629:success`, `PRE_START_UNDERSPECIFIED`.
- `research/methods-terminal-provenance-v2-sub-20260917@d5317485a591439cb52d52b649b4930964cf2b3a` exists but is unreserved and is not current SUB authority.

## Active-line review

| Line | Strongest evidence / status | Shortest path to new information | Allocation |
|---|---|---|---|
| A01 | Mixed; P2/P3 consumed development positives, Family-A P4 terminal negative, B/C reduced pre-START; registered A/B/C programme closed | Fresh independently motivated object only | Closed |
| RV01 | Development-positive timing observation, consumed, reducible to ordinary adaptive-delay/recurrent mechanisms | Fresh successor only; none registered | Secondary complete |
| RV02 | RD005 D1 terminal construction negative, consumed | Fresh object only | Terminal |
| CX/CX01 | Candidate-002 formal negative, consumed | Fresh object only | Terminal |
| H8/C08 | Registered specialization negative | Distinct prospective object only | Terminal |
| H9/C07 | Hybrid partial support; fully-spiking readiness found eight unresolved scientific choices | Prospectively fix all eight, then fresh protocol/identity | Secondary paused |
| **C19** | **Primary unresolved.** 16-field target-blind raw + 55×1,744 coverage green; v1 retired; v2 authorized but absent; Auditor hardening required | **MAIN builds exact fresh v2 and executes once if every GO gate passes** | **PRIMARY / MAIN** |

## Bounded independent-SUB review

A bounded review of canonical H1–H10 and current branches found no valid ready independent secondary object. H9 is the best independent candidate but is explicitly under-specified in eight scientific choices: fully-spiking boundary; non-sensory neural dynamics; spike representation/decoder; parameter/tuning budget; comparator/claim; tolerance authority; training/plasticity regime; runtime/randomness. H10 needs a new direct-hardware-power protocol and resources. H1–H7 would require new prospective candidates/protocols and overlap historical/closed work. The methods-terminal-provenance branch is unreserved and stale lineage rather than a current scientific object. Therefore SUB remains null rather than manufacturing parallelism.

## Parallel decomposition

### `main_lane`

- **target:** C19 fresh official-v2 package, Auditor-hardened admission, conditional one-way execution.
- **scientific_question:** does `I2_truth_free_symbolic_surface/G1_coalition/E0_global` improve Belief-R BREU over `I1_local_compositional/G1_coalition/E0_global` under the unchanged 55-row matrix?
- **recommended_owner:** `main`.
- **branch/protocol/identity:** `research/c19-official-v2-scorer-complete-20260917` / `c19-external-v2-official-protocol-v2` / `c19-external-v2-official-v2`.
- **source base:** `66c8eafe9863ed1b2455cc833a3dc498ce7721b0`.
- **information_value:** `VERY_HIGH`.
- **implementation_distance:** `NEAR_TO_MEDIUM`.
- **main_owns_all_critical_path_fixups:** `true`.

MAIN owns every C19 candidate-specific implementation, verifier, runner, binding, runtime, CI, review, preserver, scorer and execution blocker. SUB must never be a dependency.

The exact evaluator target is prospectively fixed as `pair_index`, `record_id_hash`, `source_index`, `step_index`, `target_choice_id`, `update_required`; exact join key is `pair_index`, `record_id_hash`, `source_index`, `step_index`. Before STARTED, MAIN must prove one-to-one uniqueness and totality across all 1,744 pairs, fail closed on duplicate/missing keys, and technically ensure `target_choice_id` / `update_required` never enter model features.

Bootstrap remains CPython 3.11.16, one `random.Random(19901)`, 10,000 replicates, exactly 1,744 sequential `randrange(1744)` draws per replicate, same sampled vector across primary/reference and seeds 15901→15905. Quantile semantics are now prospectively complete: sort exactly 10,000 finite effects; `r=(N-1)*p`; floor/ceil linear interpolation; retain ties; reject empty, wrong-length or non-finite input rather than drop/impute; `p=0/1` returns min/max; official bounds use `p=0.025/0.975`; no rounding before classification.

Mandatory deterministic golden fixtures must cover valid full join, duplicate rejection, missing-key rejection, target-leakage exclusion, tied effects, non-finite rejection, empty/wrong-length rejection, `p=0/1`, and official `0.025/0.975` interpolation.

**GO:** v2 fresh/unSTARTED/unconsumed; exact source/protocol/package/input/runtime/parser/envelope/scorer/bootstrap/preserver bindings reconstruct; no prior output/conflicting authority; exact 55×1,744 raw + 1,744 target join fail closed; target truth evaluator-only; quantile edge rules and golden fixtures green; STARTED/no-clobber/raw-before-score machine-enforced; exact-head CI/review green; current authority rechecked immediately before STARTED.

**STOP:** new scientific/statistical/acquisition/baseline/claim choice; stale authority; identity/ref/hash/package collision or prior output; target leakage; non-total/non-unique join; unresolved quantile edge behavior; fixture failure requiring a new convention; ordering guarantee failure; inherited science change. After STARTED, any failure consumes v2 and same-identity retry is forbidden.

### `sub_lane`

`null`

### `sub_fallback`

`null`

**no_sub_lane_reason:** bounded H1–H10/current-branch review found no fully specified current independent unconsumed object ready for implementation/measurement without new science.

### `blocked_until`

- **MAIN:** implementation may proceed now; one-way execution waits for all original v2 gates plus Auditor-required join/leakage/quantile/golden-fixture gates.
- **SUB:** a newer Analyst reserves a fully specified genuinely independent prospective lane/fallback.

## Prospective contingency tree

All branches are fixed before any C19 official outcome.

- `PRE_START_SUCCESSOR_PROTOCOLIZATION`: v2 absent/incomplete, remaining work already fixed → MAIN continues exact package implementation; no official outcome access.
- `PRE_START_AUDIT_HARDENING`: join/leakage/quantile-edge/golden-fixture conformance incomplete but fixed here → MAIN fixes it and reruns exact-head CI; if a new semantic choice appears, STOP as `PRE_START_SEMANTIC_GAP`.
- `PRE_START_BLOCKER`: science-preserving CI/verifier/binding/runtime/preserver defect → MAIN fixes it itself, re-fetches, continues.
- `PRE_START_STALE_AUTHORITY`: authority/ref/identity/control moves → STOP/reconcile; continue only if newer authority preserves exact v2.
- `PRE_START_SEMANTIC_GAP`: any evaluator/bootstrap/acquisition/scoring/preservation/baseline/metric/claim choice remains unbound → STOP before STARTED.
- `PRE_START_READY_FOR_ONE_WAY`: every GO gate passes → MAIN may create STARTED and continue same-run through acquisition → target-blind raw → immutable raw preservation → evaluator targets → scoring → terminal evidence.
- `POST_START_FAILURE`: any failure after STARTED → preserve diagnostics, mark identity consumed, no retry, STOP.
- `INVALID_EVIDENCE`: binding/join/completeness/leakage/ordering/integrity invalid → withhold result, preserve invalidity, consumed if STARTED, STOP.
- `PASS`: valid BREU 95% CI lower `> 0` → finalize narrow positive evidence, STOP before follow-up design.
- `FAIL`: valid BREU 95% CI upper `<= 0` → finalize registered negative, no rescue/retune/rerun, STOP.
- `INCONCLUSIVE`: valid CI contains `0` while FAIL not met → finalize inconclusive, no extra data/resampling/retune, STOP.

## Top 3

1. **MAIN — restore durable critical-path progress, implement exact Auditor-hardened C19-v2, then execute once if every GO gate passes.** `VERY_HIGH / NEAR_TO_MEDIUM`.
2. **SUB — deliberate no-op after bounded hypothesis review.** Do not invent H9/H10/H1–H7 science and do not take C19 blockers.
3. **Non-blocking operations/governance — diagnose MAIN durable-report/write failure; keep #147 aligned with canonical git; close #139 only through proper server-side ruleset administration.**

## Consumed identities / do-not-touch

Consumed identities remain unchanged: A01 MD-001; A01 P2 candidate-002; A01 P3 candidate-001; A01 Family-A P4 candidate-001; RV01 R01-16 family; RV01 R01-17; RV02 D1 `96634541dc29b00be9f19b5819d45f541af69348ab6c1b0da6b48e943221700a`; CX01 Candidate-002/formal evidence.

C19 predecessor v1 is retired/rejected but unconsumed; that status does not make it reusable. Do not touch rejected A01 B/C objects, historical C19-v1/C06, historical C07/H9 evidence including threshold-1.1 negative, C08/H8 terminal evidence, or legacy immutable authority refs.

## Genuine blockers

- **MAIN scientific/integrity:** successor branch/package absent; Auditor join/leakage/quantile/golden-fixture gates not yet implemented.
- **MAIN operational:** no durable MAIN history/latest advancement after 06:33 despite later cycles. Do not delegate this to SUB and do not relax science.
- **SUB:** no valid independent reserved lane after bounded review.

## Governance advisory

Issue #147 is operational only; canonical git wins. Preserve legacy freeze branches instead of force-migrating them. Annotated-tag creation tooling exists on `main`, but authoritative tags remain 0 and rulesets remain 0, so #139 stays open. Generic future `main` promotion candidates include source/runtime binding verification, STARTED/no-clobber/collision primitives, exact pair coverage, raw-preserve-before-score/digest verification, generic fail-closed state machine helpers, a generic unique+total evaluator-join validator, and a generic deterministic golden-scorer fixture harness after C19-specific semantics are separated.

## ORCHESTRATOR HANDOFF

**MAIN takes C19 official-v2 and owns ALL critical-path fixes, including Auditor-required join/leakage/quantile/golden-fixture hardening. SUB takes no lane; fallback is null. MAIN must not invent or absorb H9/H10 work; SUB must not take C19 blockers. Neither touches rejected, consumed, frozen, or legacy immutable authorities.**

Repartition only if fresh C19 evidence/identity state changes, materially new external/audit evidence changes expected information value before STARTED, or a fully specified independent secondary object becomes prospectively defined and explicitly reserved.

MAIN may continue same-run through `PRE_START_SUCCESSOR_PROTOCOLIZATION`, `PRE_START_AUDIT_HARDENING`, science-preserving `PRE_START_BLOCKER`, `PRE_START_READY_FOR_ONE_WAY`, and terminal PASS/FAIL/INCONCLUSIVE finalization. MAIN must stop at `PRE_START_SEMANTIC_GAP`, irreconcilable stale authority, `INVALID_EVIDENCE`, `POST_START_FAILURE`, or before post-outcome redesign.
