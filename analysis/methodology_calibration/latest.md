# SparkBrain Methodology Calibration Audit — 2026-09-20 10:20 JST

## Run disposition

**`MATERIAL_CALIBRATION_UPDATE`**

## Overall classification

**`WELL_CALIBRATED`** — unchanged.

There is no new scientific gate drift. The material new calibration evidence is that two fresh lower-funnel closures exercise the current bar on opposite failure modes without either novelty inflation or overconservative rejection: Homeostasis closes an ordinary implementation effect as an Architecture/API population-semantics ambiguity and stops; Receptor simultaneity shows a real positive synthetic effect, but the static Architecture pass distinguishes public permissiveness from supported-workload reachability and refuses to manufacture a dynamic comparator when reachability is unestablished.

## What changed since the 09:17 audit

### 1. Homeostasis completed the pending terminal and fresh Analyst STOP

`CAND-V05-HOMEOSTASIS-POPULATION-SEMANTICS-01` completed at exact research head `81af6ac56c31d169b591bc30854d0efa9d0bac8c` with fixed terminal `WHOLE_FIELD_IMPLEMENTATION_WITH_PUBLIC_POPULATION_UNSPECIFIED`. The implementation clearly accounts the whole supplied field, including receptor/input activity, while the supported public docs/contracts/tests do not explicitly define whether `dead`, `dead_rate`, or `active_unit_fraction` are whole-field versus reservoir/internal-computation observables.

Fresh Evidence Analyst commit `639a5f7baba926502965bc9fea4cdcfb9f749068` then set the candidate to `HOLD`, preserved the Architecture/API/observability interpretation, and prohibited same-object receptor-aware dynamic continuation, cycle 2, PRE_FORMAL, or FORMAL promotion. A future successor requires a genuinely fresh supported requirement and must bind the regulated population plus named stability observable prospectively.

This is good calibration: an ordinary source/population-mixing explanation blocks novelty credit, but the API/testbed value is preserved.

### 2. Receptor simultaneity provides a strong false-positive / false-negative calibration case

SUB's bounded DEV-only Discovery held the exact pulse multiset fixed and changed only iterable order. Final receptor state was identical, but emitted drive differed by about 5.7%. The effect is fully reduced to stable exact-key tie ordering plus sequential adaptive-gain updates, so no new mechanism or computational principle is supported.

Evidence Analyst admitted exactly one static/read-only Architecture object, `CAND-V05-RECEPTOR-SIMULTANEITY-ORDERING-01`, rather than treating the positive diagnostic as Architecture evidence or authorizing a dynamic comparator.

MAIN prospectively fixed source blobs, deterministic inventory queries, decision-relevant machine facts, `AST_STRUCTURAL_PLUS_EXACT_BLOB_HASHES_AND_DETERMINISTIC_REPOSITORY_TERM_INVENTORY`, invalid-on-mismatch, a seven-way terminal map, and mandatory STOP before interpretation. Raw facts were committed separately at `6dbc696e4e8b2b4bab62067c3297edb02da9605e`; summary head `705b652f0eb426c7e39a75a9f901be10483d0e63` mapped once to `ORDER_UNSPECIFIED_AND_REACHABILITY_UNESTABLISHED`.

The facts are balanced: the public `SignalPulse`/receptor boundary does not forbid duplicate exact `(time_ms, channel)` keys; exact-key ties retain caller order; receptor adaptation is sequential; but supported canonical callers/worlds do not intentionally establish exact-key multiplicity as a normal reachable workload. The object therefore remains an API/reproducibility corner at `HOLD` rather than being rejected as nonexistent or escalated to a dynamic aggregation/permutation comparison. Exact-head ordinary CI run `35481067945` independently completed successfully after MAIN persisted its waiting state.

This is the strongest new methodology-calibration evidence in this run. It demonstrates that a real positive signal is not automatically treated as supported-path prevalence, while lack of prevalence evidence does not erase the Architecture/API question.

## Authoritative state independently re-fetched

- `main`: `ebed6abfa941c83b36d2e3bddd04e7c5fb0dbe9d`
- annotated `evidence/*` tags: `5`
- tag-based `formal/*`: `0`
- tag-based `sealed/*`: `0`
- tag-based `freeze/*`: `0`
- preserve/control refs remain present; no new consumed/formal identity observed
- Homeostasis Architecture head: `81af6ac56c31d169b591bc30854d0efa9d0bac8c`
- Receptor ordering Architecture contract: `00ab02d9b9acd9a68090858cac21f1ea2fa5e244`
- Receptor ordering raw facts: `6dbc696e4e8b2b4bab62067c3297edb02da9605e`
- Receptor ordering summary/head: `705b652f0eb426c7e39a75a9f901be10483d0e63`
- Receptor ordering exact-head CI `35481067945`: `completed/success`
- Control Brain: `b35068cbee426c99c3cc1f6dd23053778a2bd88a`
- Evidence Analyst: `639a5f7baba926502965bc9fea4cdcfb9f749068`
- MAIN/SUB report branch: `f0fd040b252c8b9f0ffac62913c192397d22f7c1`
- Literature/Audit mailbox: `f399e9d14ef6d491135d19161b8c1d7986b94e5b`
- Repository Steward: `e53df976b98d56b8e37a5cbfc20f1aeb84caadeb`
- Utility bus: `7321568e67b1c17ef37e028e342ae0ace08c5f94`
- PR `#148`: open, unmerged, mergeable
- PR `#149`: open, unmerged, mergeable

No new FORMAL evidence exists.

## Gate calibration

| Gate / rule | Classification | Current finding |
| --- | --- | --- |
| Hard one-way integrity floor | `KEEP` | Necessary and unchanged. |
| Prospective protocol / exact source-runtime-input binding | `KEEP` | Receptor contract again fixed exact source and validity method before interpretation. |
| Raw facts before interpretation | `KEEP` | Receptor raw-facts commit precedes terminal mapping. |
| Positive signal before strong mechanistic claims | `KEEP` | Positive Discovery signal existed; it was then ordinarily reduced and not inflated. |
| Equal-privilege comparator/resource matching | `KEEP` | Still appropriate when a comparator is scientifically warranted. |
| Supported reachability before stronger dynamic comparator allocation | `KEEP` | Receptor case correctly stops when supported same-key reachability is unestablished. |
| Public-API permissiveness versus supported-workload reachability | `CLARIFY` | Permissive API supports an Architecture/API question but is not itself prevalence evidence; lack of canonical reachability should HOLD, not auto-REJECT. |
| Comparator semantics matched to actual claim/resource policy | `CLARIFY` | Do not invent aggregation or receptor-aware comparators before the underlying contract is bound. |
| New-computational-principle novelty bar | `KEEP` | Appropriate for strongest claim type. |
| Applying the same novelty bar to architecture/system/engineering/testbed value | `SPLIT_BY_CLAIM_TYPE` | Homeostasis and receptor ordering preserve value without novelty credit. |
| `NO_HIGH_VALUE_FORMAL_OBJECT => programme-wide HOLD` | `RELAX` | FORMAL can remain empty while useful lower-funnel work proceeds. |
| Fresh independently motivated object requirement | `KEEP` | Continues to prevent post-outcome rescue. |
| Stop after each Architecture cycle for fresh Analyst review | `KEEP` | Homeostasis demonstrates closure; receptor ordering is now stopped pending fresh review. |
| Legacy Top-k sparse-stratum support gate | `TIGHTEN` | Historical local weakness; do not reuse. |
| Programme-wide replacement support threshold | `INSUFFICIENT_EVIDENCE` | Do not infer universal support thresholds from local objects. |
| Prospective support-aware Architecture triage | `KEEP` | Receptor reachability handling is a good example. |
| Local Architecture thresholds as FORMAL/novelty thresholds | `CLARIFY` | Keep local triage local. |
| Ordinary-control-first reduction | `KEEP` | Stable tie ordering + sequential gain explains receptor effect without novelty inflation. |
| Negative-evidence claim-boundary narrowing | `KEEP` | No change. |
| Artifact-to-durable-handoff binding requirement | `KEEP` | No new fidelity mismatch observed. |
| Decision-relevant machine-fact extractor fidelity | `KEEP` | Receptor uses the strengthened fact-type-appropriate policy prospectively. |
| Brittle literal/format-sensitive semantic detectors | `TIGHTEN` | Historical warning remains valid. |
| One universal extractor technology for all facts | `CLARIFY` | Match validation method to fact type. |
| Post-outcome invalid-diagnostic fail-closed handling | `KEEP` | Historical rule remains appropriate. |
| Automatic PRE_FORMAL promotion after Architecture signal | `KEEP` | Must remain absent. |

## Mandatory calibration dimensions

- `gate_drift`: **none in scientific criteria** since 09:17. New work exercises existing support/reachability and STOP rules rather than moving them.
- `justification_trace`: strong. Receptor reachability handling directly addresses the risk of turning a synthetic positive into a supported-path claim; Homeostasis closure traces to ordinary population/source-mixing reduction plus explicit public-contract ambiguity.
- `false_positive_control`: strong. A 5.7% synthetic positive did not become novelty, prevalence, or dynamic-comparator authority.
- `false_negative_risk`: controlled. Receptor ordering was not discarded merely because canonical reachability is unestablished; it remains a bounded Architecture/API ambiguity. Homeostasis similarly preserves engineering/testbed value.
- `duplicate_guards`: no merge. Provenance, machine-fact validity, reachability/support characterization, handoff fidelity, and fresh Analyst review protect distinct stages.
- `moving_goalposts`: **`LOW`**. Receptor terminals and validity method were fixed before raw facts/interpretation; Homeostasis was closed without same-object rescue.
- `pass_reachability`: **`REACHABLE_BUT_NARROW`**.
- `comparator_calibration`: healthy. Both new cases withhold a stronger comparator until claim semantics and supported relevance justify one.
- `signal_before_reduction`: healthy. Receptor Discovery establishes the effect before ordinary reduction; reduction then constrains the claim.
- `claim_type_separation`: healthy across new principle, mechanistic distinctness, Architecture/API, engineering/reproducibility, and testbed value.
- `research_worthiness_vs_novelty`: separated. Homeostasis and receptor ordering remain worth characterizing without novelty credit.
- `external_calibration`: Literature supports afferent/recurrent separation, population-specific homeostatic targets, and named stability observables; this reinforces claim-scope discipline rather than changing admission thresholds.
- `opportunity_cost`: the receptor case shows the current method avoids a low-information dynamic comparator when supported reachability is unestablished while preserving the unresolved API question. Additional global strictness would have low information gain.

## PASS reachability

**`REACHABLE_BUT_NARROW`**. A realistic prospective path remains:

positive native signal -> fresh exact-bound object -> claim-local adequate support/reachability -> matched equal-privilege comparator where needed -> survive ordinary reductions -> semantically valid fact-type-appropriate machine extraction -> faithful machine-bound handoff -> fixed intervention/falsifier -> one-way preserved FORMAL evidence.

The reachability check is claim-local: it should prevent prevalence inflation, not erase a public-API/Architecture ambiguity.

## Prospective recommendations

1. Keep the scientific admission, novelty, reduction, comparator and FORMAL bars unchanged.
2. Explicitly preserve the distinction between **public API permissiveness** and **supported/canonical workload reachability**. Use the former for API-contract worthiness and the latter for prevalence/resource-allocation claims.
3. Keep positive synthetic diagnostics NON_EVIDENTIARY until an independently bound Architecture question is admitted; do not relabel Discovery output as Architecture evidence.
4. When supported reachability is unestablished, prefer `HOLD` plus a bounded static contract characterization over either automatic `REJECT` or a manufactured dynamic comparator.
5. Continue fresh Analyst STOP after every Architecture cycle. Receptor ordering has now reached that boundary; any aggregation/permutation comparator must be a fresh prospective object.
6. Keep historical Suppression/Refractory/Temporal/Top-k results unchanged and use them only as calibration examples.
7. Do not add programme-wide replication or support thresholds from these local Architecture cases.

## Utility request

No new Methodology Calibration Utility request created. The current receptor static object already answered the calibration question about positive-signal versus supported-reachability handling; a new request would duplicate MAIN's completed characterization.

## Hard-integrity-floor confirmation

Confirmed unchanged: no rerun/retune/rescore of consumed identities; frozen/prospective protocols; raw-before-score; preserve-before-read; exact identity/source/package/runtime/input binding; immutable evidence; no evaluator/target leakage; no silent post-outcome repair.

## Bottom line

The current methodology remains **`WELL_CALIBRATED`**. The strongest new evidence is not another stricter rule; it is the programme correctly refusing both kinds of error on a real positive edge-case signal: it neither promotes a synthetic effect into novelty/prevalence nor discards the unresolved public-boundary question merely because canonical reachability is not established.