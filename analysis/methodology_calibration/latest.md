# SparkBrain Methodology Calibration Audit — 2026-09-20 00:22 JST

## Overall classification

**`WELL_CALIBRATED`** — unchanged, with materially stronger confirmation.

This run does not loosen or tighten the FORMAL bar. The material new evidence is that the prospectively improved lower-funnel methodology behaved correctly under two adverse cases: a fresh support-aware Architecture object failed cleanly and was rejected without rescue, while the previously problematic Top-k line returned a read-only `MIXED` support diagnosis and remained HOLD without cycle 3 or PRE_FORMAL escalation.

## Material methodology evidence since the previous audit

1. **Fresh support-aware Architecture gating rejected a candidate cleanly.** `CAND-ASSEMBLY-PROTOTYPE-LOCKIN-01` completed the prospectively bound Assembly Architecture cycle 1 on both fixed DEV seeds. Both seeds produced `0/12` representation-divergent order pairs and no downstream functional disagreement, mapping exactly to the preregistered `NO_OR_LOW_SUPPORT` branch. Evidence Analyst classified the current stronger candidate `REJECT` and authorized no cycle-2 rescue.
2. **The historical Top-k permissiveness stayed quarantined after the cross-seed diagnostic.** Utility completed the predeclared read-only two-seed support analysis as `MIXED`: the `0.10` magnitude replicates and survives support sensitivity, but lower magnitudes are under-supported and replicated support is concentrated in the high-magnitude stratum. Evidence Analyst kept `CAND-TOPK-PA-01` at HOLD, with no cycle 3 and no PRE_FORMAL promotion.
3. **Claim-type separation continues to work.** SUB found temporal-expectation batch-partition sensitivity whose mechanism is already explainable by ordinary API/event-time ordering. Evidence Analyst did not treat it as novelty support, but promoted one fresh Architecture study because the question has engineering/reproducibility value and current repository callers use both per-pulse and multi-time ingestion patterns.
4. **The new Temporal Architecture object is prospectively bounded before outcome visibility.** MAIN committed a fresh DEV-only contract before running the diagnostic. It fixes source blobs, two repository reference timeline families, tracker defaults, three partition arms, a fixed final horizon, an event-time-causal ceiling, isolated downstream replay, raw-before-interpretation outputs, and a four-way local Architecture outcome map. At audit time its exact-head workflow is still running; there is no outcome-bearing result available to tune against.

## Calibration interpretation

These events directly test the programme's current research gates rather than merely restating them.

- The Assembly case shows that support-aware local thresholds do not force positive outcomes: the fresh object failed and was stopped.
- The Top-k case shows that a historical/local permissive label no longer has automatic promotion authority: a mixed robustness diagnosis produced HOLD, not threshold-shopping or rescue experimentation.
- The temporal batching case shows that ordinary implementation semantics can still motivate Architecture/engineering research without being laundered into a new-computational-principle claim.

This is the intended balance between false-positive control and false-negative avoidance.

## Gate calibration

| Gate / rule | Classification | Current finding |
| --- | --- | --- |
| No rerun/retune/rescore; immutable evidence; raw-before-score / preserve-before-read | `KEEP` | Hard integrity floor remains necessary and unchanged. |
| Prospective protocol / exact binding / STARTED no-clobber for FORMAL | `KEEP` | No weakening warranted. |
| Positive candidate signal before strong mechanistic `REDUCED_BY_*` language | `KEEP` | Preserves absent-signal versus explanatory-reduction distinction. |
| Equal-privilege comparator matching for mechanistic claims | `KEEP` | Stronger-privilege methods remain ceilings unless matched. |
| Stronger-privilege reduction ceilings explicitly labeled | `KEEP` | Temporal event-time-causal arm is correctly a ceiling/reference, not a mechanistic peer comparator. |
| New-computational-principle novelty bar | `KEEP` | Appropriate for the strongest claim. |
| Same novelty bar applied to architecture/system/testbed value | `SPLIT_BY_CLAIM_TYPE` | Temporal batching demonstrates the distinction is operational. |
| `NO_HIGH_VALUE_FORMAL_OBJECT => programme-wide HOLD` | `RELAX` | Corrected doctrine remains appropriate. |
| Observable/question-level exhaustion rather than family-level exhaustion | `CLARIFY` | Fresh independently motivated bounded questions remain permissible. |
| Default three-cycle exploration budget | `KEEP` | Upper bound only; exact ordinary reductions and low support may stop earlier. |
| Stop after each Architecture cycle for fresh Analyst review | `KEEP` | Assembly rejection and Top-k HOLD demonstrate this guard is working. |
| Legacy Top-k sparse-stratum local gate | `TIGHTEN` | Do not reuse its full-vote sparse-stratum semantics prospectively. |
| Top-k exact replacement support/uncertainty rule | `INSUFFICIENT_EVIDENCE` | `MIXED` cross-seed result argues against fitting one universal threshold from completed outcomes. |
| Prospective support-aware Architecture triage | `KEEP` | Assembly failed cleanly under this pattern, which is evidence against a positivity-biased gate. |
| Porting local Architecture numeric thresholds across lines | `CLARIFY` | Assembly cutoffs are not a general programme standard. |
| Local Architecture cutoffs as novelty/FORMAL thresholds | `CLARIFY` | Keep local triage local. |
| Automatic PRE_FORMAL promotion after Architecture signal/replication | `KEEP` | Explicitly absent for both Top-k and Assembly. |
| Outcome-informed selection of future support rule | `CLARIFY` | Future support guards need general prospective rationale. |
| Ordinary-control-first Architecture reduction | `KEEP` | Current funnel continues to stop/reject on ordinary explanations. |
| Architecture value surviving novelty rejection | `KEEP` | Temporal batching is a valid reproducibility question despite ordinary mechanism. |
| Fresh Architecture contract before outcome visibility | `KEEP` | Temporal batching contract was fixed before its workflow outcome. |

## Mandatory calibration dimensions

- `gate_drift`: positive and now behaviorally validated. The programme has moved from binary formal-or-stop and thin local Architecture votes to layered research, support-aware fresh objects, explicit HOLD/MIXED states, and mandatory fresh review.
- `justification_trace`: strong for the hard integrity floor, comparator privilege, reduction gates, and stop/review boundaries. Local Architecture numeric thresholds remain object-specific and must not become universal defaults without separate justification.
- `false_positive_control`: strong. Assembly produced zero supported DEV effect and was rejected; Top-k mixed support did not escalate; no lower-layer output became formal evidence.
- `false_negative_risk`: controlled. Temporal batching can proceed as Architecture/engineering work despite being reducible to ordinary API/event-time semantics; `MIXED`/HOLD remains distinct from REJECT.
- `duplicate_guards`: no scientific-purpose guard should be removed. Operational consolidation remains separate from methodological purpose.
- `moving_goalposts`: `LOW`. Completed Top-k labels remain unchanged; Assembly was judged under its pre-outcome mapping; Temporal batching has a pre-outcome contract and no result yet.
- `pass_reachability`: `REACHABLE_BUT_NARROW`. The lower funnel can generate objects while FORMAL admission still requires a fresh exact-bound, support-adequate, matched-comparator object that survives ordinary reductions.
- `comparator_calibration`: healthy. Stronger-reference/ceiling comparators are labeled as such rather than used as equal-resource mechanistic peers.
- `signal_before_reduction`: healthy. Assembly low support stops the stronger question; ordinary mechanism explanations in Discovery do not become novelty claims.
- `claim_type_separation`: healthy and demonstrated by Temporal batching promotion for reproducibility value without mechanistic novelty support.
- `research_worthiness_vs_novelty`: no material conflation observed.
- `external_calibration`: current literature strengthens prospective Top-k ordinary reduction pressure (switching/basin/hysteresis/transient dynamics) but does not rewrite completed results; H5 audit preserves its terminal negative while narrowing interpretation to the registered work metric.
- `opportunity_cost`: balanced. Lower-funnel research remains active without reopening consumed lines or weakening FORMAL standards.

## Representative checks

- C19-R2 remains anchored by immutable annotated evidence tag `evidence/c19-r2-fsa-state-tracker-c19-r2-fsa-state-tracker-official-v1` to terminal commit `6197fa801a78a0c5de4c2b6ff5d03216ac5539db`; ordinary reduction remains a justified stop condition.
- H5 remains anchored by immutable annotated evidence tag `evidence/h5-event-routing-work-reduction-h5-event-routing-work-reduction-official-v1` to terminal commit `61aff6d74b82b68a326f3d90505d70bcd4071fd5`; interpretation remains claim-bounded rather than generalized.
- Assembly Architecture cycle 1 completed exact-head CI successfully and mapped prospectively to `NO_OR_LOW_SUPPORT`; the current candidate was rejected with no rescue cycle.
- Top-k cross-seed read-only diagnosis is `MIXED`, and the line remains HOLD rather than being threshold-fit into promotion.
- Temporal batching is a fresh NON_EVIDENTIARY Architecture object; its workflow is running under a fixed contract and has no result yet.

## Prospective recommendations

1. Keep the four-layer funnel and `FORMAL_HOLD_WITH_ACTIVE_LOWER_FUNNEL` doctrine.
2. Keep Assembly `REJECT` for the current object; do not add seeds, permutations, thresholds, or corpora to rescue it.
3. Keep Top-k at HOLD. Do not create cycle 3 or PRE_FORMAL from the current `MIXED` support diagnosis. Any successor must be a fresh prospectively defined ordinary-reduction Architecture question.
4. Preserve the Temporal batch-partition contract exactly through its first diagnostic. Every terminal mapped result must stop for fresh Analyst review; no same-run redesign or promotion.
5. Keep event-time-causal scheduling explicitly labeled as an Architecture/API reference ceiling rather than an equal-resource mechanistic comparator.
6. Do not generalize local Architecture thresholds across candidate families without an independent prospective rationale.
7. Preserve external-literature reduction ladders as prospective comparator/discriminator input only.
8. Preserve the complete one-way integrity floor.

## Utility request

**No new Utility request created.** The remaining Top-k methodology uncertainty has already been answered at the currently authorized level (`MIXED`), and Temporal batching is already prospectively bound as MAIN work. A new methodology request would duplicate active or completed work rather than answer a distinct calibration question.

## Hard-integrity-floor confirmation

Confirmed unchanged: no rerun/retune/rescore of consumed identities; frozen/prospective protocols; raw-before-score; preserve-before-read; exact identity/source/package/runtime/input binding; immutable evidence; no evaluator/target leakage; no silent post-outcome repair.

## Bottom line

**`WELL_CALIBRATED` remains the best current classification, now with stronger empirical support from gate behavior.** The programme rejected a fresh support-aware Architecture candidate when support was absent, held a historically permissive Top-k line after a mixed robustness result, and still allowed an ordinary API/event-time effect to proceed as a bounded reproducibility Architecture question without inflating it into novelty evidence.