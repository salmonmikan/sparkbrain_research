# SparkBrain Methodology Calibration Audit — 2026-09-21 19:22 JST

schema_version: `2`  
generation_id: `METHCAL-20260921T192258+0900-R42-7F3A9C21`  
produced_at: `2026-09-21T19:22:58+09:00`  
authority_scope: `METHODOLOGY_ADVISORY_ONLY`  
supersedes_generation_id: `METHCAL-20260921T182131+0900-R41-C7A4D291`

## Result

`MATERIAL_CALIBRATION_UPDATE`

Overall classification remains **`MIXED_CALIBRATION`**.

The strongest positive update is that the fresh outcome-blind four-stage synthetic conformance object has now completed successfully and been canonically terminalized as SYSTEM-only integrity work. Direct repository inspection confirms the exact pre-result repair at `168883bd316985537e404c6aad3a7ac03202e28a` changed only one blank line in the raw generator plus the bound frozen hash. Exact-head CI `35583071628` passed Python 3.11 and 3.13 through Install/Lint/Local readiness/Test/Validate. The conformance implementation durably writes raw-only bytes and a raw manifest before invoking the fixed scorer, binds the exact raw digest into the scored preserve, and the tests fail closed on mutated preserved raw. This validates the prospective preflight-repair boundary and the four-stage architecture on a non-scientific synthetic surface without repairing or reusing R33.

Accordingly `pre_conformance_preflight_repair_boundary` improves from `CLARIFY` to **`KEEP`**: same-object repair is calibrated when and only when `result_bearing_stage_reached=false`, the diagnostic is demonstrably science/analysis-invariant, changed paths/hashes are recorded, and all affected bindings are refrozen before any result-bearing stage. This evidence does not justify relaxing raw-before-score or preserve-before-score for live science; the first scientific PRE_FORMAL remains historically nonconforming and terminal, so those programme-level execution gates remain `TIGHTEN` pending a fresh scientific use of the conforming pipeline.

Fresh Evidence Analyst R41 closes the synthetic conformance object as `HOLD / SYSTEM / HOLD_SYSTEM_TERMINAL / TERMINAL_FOR_CURRENT_OBJECT / NOT_QUEUED` and opens a fresh SYSTEM Architecture object, `CAND-PREFORMAL-CROSS-GENERATION-HOLDOUT-EXPOSURE-INTEGRITY-01`. Canonical Funnel v2.1 is now classification-complete at **31/31**, with `MECHANISM=13 / SYSTEM=18`, Architecture `M0/S1`, PRE_FORMAL eligible=`0`, READY=`0`, and viable executable MECHANISM=`0`. No same-object SYSTEM→MECHANISM upgrade occurred and no SYSTEM-over-comparable-MECHANISM exception is in use.

The cross-generation holdout concern is scientifically calibrated rather than speculative. External adaptive-data-analysis literature shows that repeated holdout reuse after prior feedback can induce overfitting, while reusable/limited-feedback mechanisms can preserve validity; empirical work also shows reuse does not automatically imply substantial overfitting in every regime. Therefore the calibrated policy is **not** a universal ban or numeric reuse threshold. Instead future PRE_FORMAL/FORMAL work should prospectively account for protected-set identity/exposure and select a fresh/rotated set or a bounded-feedback/reusable-holdout policy appropriate to the reuse regime before outcome inspection.

The newest MAIN strategy attempt on that object produced another useful integrity calibration event. It requested commit metadata/file inventory but the GitHub commit endpoint over-returned result-bearing patch content. MAIN excluded those fields from inference and failed closed with `R41_SCOPE_INVALID_PROTECTED_OUTCOME_ENDPOINT_OVERRETURN_FAIL_CLOSED_NO_TERMINAL_CLASSIFICATION`; no science, identity consumption, or terminal disposition followed. This is positive evidence for stop discipline (`scope_invalid_protected_outcome_overread_fail_closed=KEEP`) but reveals a real tool/observability boundary: `metadata_only_holdout_exposure_endpoint_boundary=TIGHTEN`. Future metadata-only exposure audits should use endpoint/path allowlists or payload validation that rejects any response carrying protected outcome/result content before the payload is made available for inference.

The current strategy result is post-Analyst and therefore does **not** alter the canonical 31/31 funnel. `CAND-PREFORMAL-CROSS-GENERATION-HOLDOUT-EXPOSURE-INTEGRITY-01` remains ACTIVE pending fresh Analyst review. Cross-generation exposure observability is therefore `INSUFFICIENT_EVIDENCE`, not a proven repository defect and not a basis for retroactive invalidation of existing immutable evidence.

The open `NO_COHERENT_MECHANISM_TARGET` episode remains one episode, now at canonical check_count=`6`; it consumes no candidate or scientific identity and is excluded from selection/conversion denominators. Rolling scientific selection remains `MECHANISM / SYSTEM / SYSTEM = 1/3`. Mechanism supply is still stalled (`viable executable MECHANISM=0`), so false-negative risk remains a moderate watch even though the quality floor is behaving correctly and no shadow is being laundered into MECHANISM.

Mandatory Funnel-v2.1 findings remain stable: current-object-only `claim_ceiling=KEEP`; no completed SYSTEM object is upgraded in place; `preformal_eligible` remains distinct from READY; READY remains development/test readiness and `HIDDEN_SECOND_FORMAL_GATE=false`; multidimensional HOLD and classification-completeness gating remain intact; SYSTEM retains genuine integrity/testbed value; and no universal readiness/support threshold is warranted.

PASS reachability improves to **`PRE_FORMAL_REACHED; FOUR_STAGE_SYNTHETIC_CONFORMANCE_PASSED_AND_CANONICALIZED; CLEAN_SCIENTIFIC_PASS_REACHABLE_ONLY_VIA_FRESH_PROSPECTIVE_IDENTITY_WITH_CONFORMING_PIPELINE_AND_PROSPECTIVELY_RESOLVED_PROTECTED_SET_EXPOSURE_POLICY`**. R33 remains terminal and may not be rerun, repaired, retuned, rescored, or upgraded.

Authoritative repository truth was independently refreshed before strategy summaries: stable `main=ebed6abfa941c83b36d2e3bddd04e7c5fb0dbe9d`; exactly five annotated `evidence/*` refs with unchanged tag-object SHAs; `formal/*=0`, `sealed/*=0`, and tag-level `freeze/*=0`. Fresh Analyst is `EVA-20260921T185902+0900-R41-8D3A21C7@ffc8672c01ed0f45b2d0fd998c36706b8a343a91`; current Control is `CTRL-20260921T185206+0900-R24-6B4D21C8@5f9348022b3af8ef6ac6c1bd9d15cb77380d7d92`; newest strategy mailbox tip is `3dc7e4b0e2fb97038247778730cb9a93c9e915c8` and is non-authoritative.

No Utility request was created. Hard floor remains **`CONFIRMED / DO NOT RELAX`**.
