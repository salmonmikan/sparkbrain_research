# SparkBrain Methodology Calibration Audit — 2026-09-20 08:20 JST

## Run disposition

**`MATERIAL_CALIBRATION_UPDATE`**

## Overall classification

**`MIXED_CALIBRATION`** — unchanged, but the remaining mixture is now more sharply localized. The substantive scientific admission, novelty, reduction, comparator, STOP/reframe, and FORMAL evidence bars remain broadly well calibrated. The material weakness is the validity of decision-relevant machine-fact extraction before an artifact is produced; this weakness is now independently confirmed rather than merely suspected, and the newest Architecture object prospectively uses a stronger structural extraction policy.

No evidence supports globally tightening or relaxing the scientific novelty/admission bar.

## What changed since the 07:21 audit

### 1. Suppression static-extractor false negative is independently confirmed

Utility completed `CTRL-20260920-0650-SUPPRESSION-STATIC-DETECTOR-CONSISTENCY` with classification `STATIC_EXTRACTOR_FALSE_NEGATIVE_CONFIRMED`. The completed suppression artifact recorded `restore_restores_original_base_threshold=false`, but exact bound source semantics restore the saved threshold. The derived artifact field `implementation_state_preserving=false` is therefore also not trustworthy as source-semantic authority. Other decision-relevant fields matched; this was an extractor mismatch, not an artifact-to-handoff mismatch.

The audit was read-only and NON_EVIDENTIARY: no scientific rerun, dynamic probe, artifact/contract/harness repair, historical relabel, TEST/FORMAL access, or successor selection occurred. The historical `AMBIGUOUS_CONTRACT` terminal remains unchanged.

This materially strengthens `TIGHTEN` for decision-relevant machine-fact extractor fidelity. It does **not** justify a stricter scientific novelty threshold.

### 2. The next Architecture object prospectively adopted structural machine-fact extraction

Evidence Analyst `850b4502cf15d3f6118a487c5ab4ba81fbf7b75e` admitted `CAND-V05-DELAYED-OUTCOME-ATTRIBUTION-01` only as a static/read-only Architecture/API characterization. MAIN prospectively bound exact source blobs, a fixed repository callsite surface, a fixed terminal map, and a machine-fact policy of `AST_STRUCTURAL_PLUS_EXACT_BLOB_HASHES`, with invalid-on-mismatch and post-outcome repair forbidden.

On research head `c79c5434222a6d018931dc1752e7f0074c23854b`, raw facts were committed before interpretation at `d226f5993b69f0d0c98c1e1cd7b049e2ba3ded49`. All exact source blobs matched. The fixed callsite inventory found canonical/runtime callers always use `learn_outcome` after the corresponding `process_episode`, with no intervening `process_episode`; the public/source surface does not explicitly define immediate-only or delayed/interleaved semantics; the implementation keeps mutable pending activation/action state and `learn_outcome` carries no episode/decision identity.

The prospectively mapped static terminal is `IMMEDIATE_ONLY_USAGE_BUT_PUBLIC_CONTRACT_UNSPECIFIED`. The summary explicitly says this is API-contract ambiguity, **not** a demonstrated delayed-credit failure, executes no dynamic probe, creates no consumed identity, and STOPs for fresh Evidence Analyst review. No cycle 2, PRE_FORMAL, or FORMAL continuation is authorized.

This is the first fresh operational evidence that the extractor lesson is being applied prospectively to a real object. However, a fresh Evidence Analyst successor decision has not yet consumed this terminal, so programme-wide end-to-end closure is not yet demonstrated.

### 3. Refractory completed a real positive Architecture terminal without novelty inflation

The previously repaired Refractory object completed exact-head workflow `35472786687` successfully. Machine output maps to `FUNCTIONAL_REFRACTORY_ACCOUNTING_EFFECT` and sets `stop_required=true`; artifact `10593866036` has raw SHA-256 `4587b7e0ee1404f3f78f8a0c83c8224fea3d632e8a25162cb0ae586dc4a5712e`.

Evidence Analyst classified the result as architecture/API-state semantics, engineering/reproducibility value, and testbed value while explicitly withholding new-computational-principle, mechanistic-distinctness, biological-novelty, or prevalence claims. The candidate is `HOLD`; same-object cycle 2 and automatic PRE_FORMAL promotion are prohibited.

This is strong positive evidence for current claim-type separation and STOP calibration: a real functional effect can survive as useful Architecture evidence without being laundered into novelty.

### 4. SUB mature-capacity lockout shows healthy admission with relevance caveats

SUB completed `ASSEMBLY_MATURE_CAPACITY_LOCKOUT_DISCOVERY_CYCLE1`. In a bounded synthetic `max_candidates=2` setting, mature candidates occupy capacity indefinitely under the current prune policy while a stale immature candidate is reclaimable. The effect is fully reduced to ordinary resource/lifecycle policy; default/supported workload saturation of the normal `max_candidates=256` capacity is not established.

Evidence Analyst admitted one queued fresh Architecture characterization only. Before any turnover comparator is meaningful, the object must bind whether mature assemblies are intended as permanent lifetime memory or a reclaimable working set and whether supported DEV horizons plausibly approach saturation. This preserves research-worthiness without treating a synthetic edge case as established architectural harm or novelty.

## Authoritative repository/evidence state independently re-fetched

- `main`: `ebed6abfa941c83b36d2e3bddd04e7c5fb0dbe9d`
- annotated `evidence/*` tags: `5`
- tag-based `formal/*`: `0`
- tag-based `sealed/*`: `0`
- tag-based `freeze/*`: `0`
- historical `control/*` and `preserve/*` refs remain present; no new FORMAL identity observed
- Refractory Architecture head: `4589192d927e40cf7a05cf8a94207efb42ebbc65`
- delayed-outcome Architecture head: `c79c5434222a6d018931dc1752e7f0074c23854b`
- SUB mature-capacity Discovery head: `3cac844ca6a80725c87d7e681c23b703f35150c2`
- Control Brain: `36a41bb720c051ee6d4563d7ecb6b720a8fc2c2d`
- Evidence Analyst: `850b4502cf15d3f6118a487c5ab4ba81fbf7b75e`
- MAIN mailbox: `682a940bd1c58d5dad2f201b3a65d87297327f17`
- Literature/Audit mailbox: `a39f0007a55000efd7a237f05e21d381176e47f1`
- Repository Steward: `e53df976b98d56b8e37a5cbfc20f1aeb84caadeb`
- Utility bus: `38a5a715b3d5afec73e76fea49b56647c095d8fb`
- PR `#148`: open, unmerged, mergeable
- PR `#149`: open, unmerged, mergeable

No new FORMAL evidence exists.

## Gate calibration

| Gate / rule | Classification | Current finding |
| --- | --- | --- |
| No rerun/retune/rescore; immutable evidence; raw-before-score / preserve-before-read | `KEEP` | Hard integrity floor remains necessary. |
| Prospective protocol / exact identity-source-runtime-input binding | `KEEP` | Necessary and functioning. |
| Raw facts before interpretation in lower-funnel characterization | `KEEP` | Delayed-outcome object committed raw facts before its mapped summary. |
| Pre-outcome science-invariant mechanical/representation repair under unchanged bindings | `KEEP` | Refractory repair boundary behaved correctly. |
| Any post-outcome semantic/protocol repair of the same consumed object | `KEEP` | Remains prohibited; suppression terminal was not repaired. |
| Positive signal before strong mechanistic claims | `KEEP` | No reason to change. |
| Equal-privilege comparator/resource matching | `KEEP` | No current systematic over/undermatching evidence. |
| Comparator semantics matched to actual claim and intended resource policy | `CLARIFY` | Refractory comparators must match the API/model claim; Assembly turnover comparison must first bind lifetime-memory vs working-set intent and saturation relevance. |
| New-computational-principle novelty bar | `KEEP` | Appropriate for strongest claim type. |
| Applying that novelty bar to architecture/system/testbed value | `SPLIT_BY_CLAIM_TYPE` | Current lower funnel is handling this correctly. |
| `NO_HIGH_VALUE_FORMAL_OBJECT => programme-wide HOLD` | `RELAX` | Productive Discovery/Architecture work continues while FORMAL remains empty. |
| Fresh independently motivated object requirement | `KEEP` | Prevents post-outcome rescue. |
| Stop after each Architecture cycle for fresh Analyst review | `KEEP` | Refractory and delayed-outcome both stop. |
| Legacy Top-k sparse-stratum support gate | `TIGHTEN` | Historical local weakness remains; do not reuse it. |
| Programme-wide replacement support/uncertainty threshold | `INSUFFICIENT_EVIDENCE` | Do not fit a universal threshold to completed outcomes. |
| Ordinary-control-first Architecture reduction | `KEEP` | Refractory and Assembly-capacity effects reduce through ordinary semantics/policy before novelty. |
| Negative-evidence claim-boundary narrowing | `KEEP` | H5 remains narrow to registered algorithmic-work claim. |
| Generic artifact-to-handoff binding requirement | `KEEP` | Prospectively adopted by Control. |
| Automated/live handoff-guard tooling coverage | `CLARIFY` | Needs one real successor-decision demonstration; not an extra scientific hurdle. |
| Decision-relevant machine-fact extractor fidelity | `TIGHTEN` | Suppression false negative is now independently confirmed. |
| Literal/format-sensitive semantic detectors | `TIGHTEN` | Suppression/Refractory justify avoiding brittle representation tests. |
| Structural/AST/exact-blob extraction for source-semantic facts | `KEEP` | Appropriate prospectively when the fact is structurally representable; delayed-outcome object is a positive operational example. |
| One universal extractor technology for all fact types | `CLARIFY` | Method should match the fact: structural/AST, typed invariant, runtime invariant, or documentation check as appropriate. |
| Documentation/comment facts versus executable semantic facts | `CLARIFY` | Keep separate unless documented API contract is the claim itself. |
| Post-outcome invalid-diagnostic fail-closed handling | `KEEP` | Preserve historical artifact/terminal; block only dependent interpretation. |
| Automatic PRE_FORMAL promotion after Architecture signal | `KEEP` | Must remain absent. |
| Local Architecture thresholds as FORMAL/novelty thresholds | `CLARIFY` | Keep local triage claim-local. |

## Mandatory calibration dimensions

- `gate_drift`: evidence-driven procedural maturation, not accumulated caution. Temporal/Top-k produced handoff-binding safeguards; suppression/Refractory produced extractor-validity safeguards; delayed-outcome is the first fresh object to bind a stronger structural method prospectively. Scientific novelty thresholds have not moved.
- `justification_trace`: strong for the material gates. Every recent procedural tightening traces to a concrete integrity/fidelity failure. No important new scientific hurdle lacks a traceable failure mode because no new scientific hurdle was added.
- `false_positive_control`: strong scientifically. Refractory produced a genuine functional effect yet remained Architecture-only; delayed-outcome remained an API ambiguity rather than a manufactured failure claim. Remaining procedural risk is any semantically wrong extracted fact that happens to pass provenance/handoff consistency.
- `false_negative_risk`: still concentrated in extractor validity. Suppression confirms a source-semantic false negative; Refractory showed a representation false negative. The new structural policy is promising, but not yet a programme-wide demonstrated closure.
- `duplicate_guards`: no merge recommended. Exact provenance binding, semantic extractor validation, artifact-to-handoff fidelity, and fresh Analyst review protect distinct failure stages.
- `moving_goalposts`: **`LOW`**. Refractory repair was pre-outcome and science-invariant; suppression history was not repaired; delayed-outcome contract, fact method, terminal map, and stop rule were fixed before raw facts and interpretation.
- `pass_reachability`: **`REACHABLE_BUT_NARROW`**.
- `comparator_calibration`: broadly appropriate. Do not overmatch an engineering/API claim to an unrelated biological comparator; do not compare Assembly permanent-memory semantics with a turnover policy until intended lifetime/resource semantics and plausible saturation are bound.
- `signal_before_reduction`: healthy. Real effects are first established locally, then ordinary semantics/policy reductions are applied; static ambiguity is not converted into an unearned dynamic failure claim.
- `claim_type_separation`: healthy across new computational principle, mechanistic distinctness, architecture/system integration, engineering efficiency, and testbed value.
- `research_worthiness_vs_novelty`: separated. Refractory, delayed-outcome, and mature-capacity lines can remain worthwhile Architecture questions without receiving novelty credit.
- `external_calibration`: current refractory literature supports model/API-specific comparator semantics and ordinary-state reductions. Independent H5 audit remains `ROBUST_SO_FAR` only for the registered metric and reinforces narrow claim boundaries. Neither justifies changing scientific admission thresholds.
- `opportunity_cost`: the highest information gain is now from observing one real successor decision under both the adopted handoff binding and stronger machine-fact extraction. Adding global replication/novelty strictness would target the wrong failure mode and suppress useful lower-funnel work.

## PASS reachability

**`REACHABLE_BUT_NARROW`**. A realistic prospective path remains:

positive native signal -> fresh exact-bound object -> claim-local adequate support -> matched equal-privilege comparator where the claim requires one -> survive ordinary implementation/dynamical reductions -> valid fact-type-appropriate machine extraction -> faithful machine-bound handoff -> fixed intervention/falsifier -> one-way preserved FORMAL evidence.

Nothing in the new calibration evidence makes PASS logically unreachable. The new extractor safeguards are integrity-transfer requirements, not novelty hurdles.

## Prospective recommendations

1. Keep the four-layer funnel, strict FORMAL novelty bar, comparator discipline, ordinary-reduction-first interpretation, and hard integrity floor unchanged.
2. Keep `decision_relevant_machine_fact_extractor_fidelity = TIGHTEN` until at least one real successor decision completes under the stronger prospective extraction plus adopted handoff-binding path.
3. For each decision-relevant machine fact, prospectively bind an appropriate validity method. Prefer AST/structural/typed/runtime invariants over raw substring/format matching when they express the fact faithfully; do not mandate AST where it is the wrong representation.
4. Treat the completed suppression diagnostic as calibration evidence only. Never repair, rerun, rescore, or relabel the historical `AMBIGUOUS_CONTRACT` result.
5. On the delayed-outcome object, honor its valid static terminal and fresh-review STOP. Do not create a dynamic interleaving experiment unless fresh prospective authority first establishes a supported delayed/interleaved claim worth testing.
6. On Assembly mature-capacity, bind intended memory-lifecycle semantics and plausible supported saturation before choosing a turnover comparator or downstream learnability threshold.
7. Continue separating useful Architecture/system/engineering/testbed findings from computational-principle novelty.
8. Fail closed only for successor decisions that depend on a disputed machine fact; do not automatically reject the candidate or inflate programme-wide support thresholds.
9. Never retroactively rescore, relabel, rerun, invalidate, or upgrade consumed/frozen experiments because methodology safeguards improve.

## Utility request

No new Methodology Calibration Utility request created. The suppression extractor diagnostic is now complete, and the newest delayed-outcome object already prospectively exercises the higher-value structural extraction approach. A duplicate request would add little information.

## Hard-integrity-floor confirmation

Confirmed unchanged: no rerun/retune/rescore of consumed identities; frozen/prospective protocols; raw-before-score; preserve-before-read; exact identity/source/package/runtime/input binding; immutable evidence; no evaluator/target leakage; no silent post-outcome repair.

## Bottom line

The current methodology remains **`MIXED_CALIBRATION`**, but not because the scientific bar is too high or too low. The remaining mixed element is procedural: machine-fact extractors have produced confirmed false negatives, while the newest real Architecture object shows a prospective path toward fixing that layer with structural checks and raw-before-interpretation discipline. Keep tightening that transfer layer; do not tighten the scientific novelty/admission bar.