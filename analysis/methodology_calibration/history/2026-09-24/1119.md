# SparkBrain Methodology Calibration Audit — R104

- schema_version: `2`
- generation_id: `METHCAL-20260924T111949+0900-R104-6B8E2D41`
- generated_at: `2026-09-24T11:19:49+09:00`
- authority_scope: `METHODOLOGY_ADVISORY_ONLY`
- supersedes: `METHCAL-20260924T103400+0900-R103-C4F19B72`
- overall_classification: `MIXED_CALIBRATION`
- scientific_execution: `NONE`
- Utility request: `none`

## Executive assessment

R103's H7 defect has been repaired correctly at the controller/launch-contract layer without changing scientific semantics: the repaired controller now binds the current result-workflow blob, Relay stopped before identity/START, and Control correctly withdrew the stale R111 execution authority pending a fresh Analyst rebind. FORMAL integrity remains unchanged.

Two material calibration issues remain prospectively.

First, the dormant H7 dispatch bridge and its request file still pin the pre-repair controller `bac7402...`, while the target controller branch now points at repaired `af3aa975...`. The bridge therefore cannot dispatch the repaired controller and will fail closed before dispatch if armed. This is safe, but Control R54's next-step description is incomplete if interpreted as “fresh Analyst rebind, then arm the existing bridge”: bridge/request exact pins must be repaired and validated before the fresh Analyst rebind that authorizes one-shot FORMAL execution.

Second, Independent Audit R10 produced the first plausible candidate-specific live Revisit signal for terminal Candidate #35. The audit was fixed blind before current control-plane summaries, used frozen source and preserved raw only, and performed no rerun/retune/rescore. It found that the R100 null treatment modified non-receptors while the observed declared response consisted only of directly cued untreated receptor spikes, so treatment-to-readout causal opportunity was not demonstrated. This narrows the old negative; it does not reopen #35, create confirmatory credit, or by itself justify a fresh canonical successor. Because Analyst R111 predates R10, the canonical Revisit ledger has not yet adjudicated this signal.

## Authoritative reconstruction

Repository/evidence were independently re-fetched. Stable `main` is `d16403414fc7abebd23075fc401240971b8eb91d`. The five authoritative `evidence/*` annotated tags are unchanged. Tag-form `formal/*`, `sealed/*`, `freeze/*`, and `immutable/*` remain absent. H7 `control/*`, `preserve/*`, and `launch/h7-r5-*` namespaces are empty.

H7 frozen science remains `2f30b93f8f3cf226ef55ed5af7e341089d2c3c80`, with only a science-invariant whitespace repair at that head. The repaired operational controller is `af3aa97574c365e3e918c3d4d012faa4886760d0`; its launch contract now binds result workflow blob `1c4e2199740397c1afbbcc66e89d83f41fe54b21`, resolving R103's controller-internal mismatch.

Evidence Analyst R111 remains the latest canonical Analyst generation and still records GO_ONCE/effectively-executable against pre-repair controller `bac7402...`. That authority does not bind the repaired controller. Relay and Control both recognize this and stop before FORMAL start pending fresh Analyst authority.

The dormant bridge branch is still `1e12e73b8faa806ac07c88d4cb95875093439c7a`. Both `ops/h7_launch_request.json` and `.github/workflows/h7-r5-launch-bridge.yml` pin `bac7402...`; the target branch now resolves to `af3aa975...`. The request remains `armed=false`.

Canonical funnel remains 35 candidates = 14 MECHANISM / 21 SYSTEM, 34 terminal and H7 the sole active/queued/READY object. Official consumed FORMAL identities remain 7. No new identity was consumed.

## Gate findings

| Gate | Classification | Finding |
|---|---|---|
| Hard one-way integrity | KEEP | No H7 identity, START, protected target access, score, preserve or new evidence exists. |
| Development-phase monotonicity | KEEP | H7 remains `RESULT_EXPOSED_DEVELOPMENT / R5_UNCHANGED`; terminal objects remain terminal. |
| Cycle-3 policy | KEEP / RELAX automatic-cap interpretation | Cycle 3 is reassessment, not terminal cap; no hard-cap regression observed. |
| Science-invariant vs science-affecting repair | KEEP | R103 controller repair is exact-binding plumbing only; no metric/comparator/threshold/intervention/falsifier/resource semantics changed. |
| PRE_FORMAL development | KEEP | READY remains distinct from success and from current FORMAL executability. |
| Controller launch-contract exact binding | KEEP | R103 mismatch is fixed at repaired controller head. |
| End-to-end controller/bridge/request exact binding | TIGHTEN | Bridge and dormant request still pin old controller while target branch moved to repaired controller. |
| Effective executability | TIGHTEN | Must include controller + launch contract + bridge + request + target-ref consistency, not controller self-consistency alone. |
| Fresh Analyst rebind ordering | TIGHTEN | Repair/validate all operational pins first; then Analyst binds the stable end-to-end bundle. |
| Fresh FORMAL transition | INSUFFICIENT_EVIDENCE | No H7 FORMAL identity has started yet. |
| Revisit axis vs terminal state | KEEP | Old IDs remain terminal; Revisit metadata is orthogonal. |
| Revisit bootstrap | KEEP | Last Analyst has complete 34/34 conservative coverage, no old-ID reopening. |
| New candidate-specific Revisit input ingestion | CLARIFY | R10 arose after R111; it now requires explicit fresh Analyst adjudication. |
| Rescue-laundering guard | KEEP | R10 cannot directly become a successor or repair R100. |
| REVISIT_FORGE_TEST new-trigger-only behavior | INSUFFICIENT_EVIDENCE | There is still no live Revisit Forge case. R10 could become the first if accepted. |
| Theory / Forge canonical separation | KEEP | TH-001 remains reduced/rejected; Forge remains zero-credit and no canonical admission occurred. |
| Claim ceilings / successor rules | KEEP | No same-object SYSTEM→MECHANISM uplift or manufactured fresh successor observed. |
| PASS reachability | KEEP scientifically; operationally blocked | PASS remains reachable without weaker standards, but current bridge path is not executable. |

## Development calibration

Development semantics are consistent end-to-end on scientific objects. OPEN development remains flexible; H7 is RESULT_EXPOSED and only science-invariant same-object repair is being used. The current controller and required bridge-pin repair are correctly classifiable as science-invariant because they change only exact execution plumbing and do not alter scientific meaning. No development observation has been laundered into independent confirmatory evidence.

Cycle 3 is not being treated as a hard cap. No evidence of automatic terminalization solely due to cycle count was found.

## FORMAL calibration

FORMAL one-way integrity is unchanged. The repaired controller itself is now internally self-consistent, but the full dispatch path is not. If the current bridge were armed, it would compare the target branch head against hardcoded `bac7402...`; the branch is now `af3aa975...`, so it should fail closed before workflow dispatch. This is a safe failure, not a scientific-integrity incident.

Prospective order should be:
1. repair only the bridge/request controller pin to the already-repaired controller, with request remaining dormant and no result;
2. validate the exact bridge/controller/request/target-ref checks in a non-result path;
3. obtain a fresh Analyst generation that independently re-fetches and exact-binds the now-stable science/controller/bridge/generation+commit and empty one-way namespaces;
4. only then allow one arm, one dispatch, one fresh identity.

R111 history must remain unchanged.

## Revisit / resurrection calibration

Bootstrap remains conservative and complete at the last Analyst snapshot: 34/34 terminal candidates classified, no old ID reopened, no historical outcome rewritten, no inherited confirmatory credit.

Independent Audit R10 materially changes the current Revisit calibration picture for Candidate #35. It does not produce new scientific evidence; it produces candidate-specific methodological information about the old negative's diagnostic support. The frozen R100 treatment reset non-receptor state, while the observed declared response contained only directly cued untreated receptor spikes. No treated unit was shown to have causal opportunity to affect the declared spike/cascade/ignition signature. The delayed sham also changed latent state while leaving that high-level signature unchanged. The robust historical statement remains only that the fixed arms produced the same declared signature.

This is strong enough that the next Analyst/Revisit scan must explicitly adjudicate it. It is not strong enough to reopen Candidate #35 or automatically canonicalize a successor.

If independence/new-question screening accepts it as a Revisit trigger, `REVISIT_TRIGGERED` is appropriate as metadata only. A reasonable immediate decision is `REVISIT_DORMANT` unless there is a bounded way to test the new rationale itself. A `REVISIT_FORGE_TEST` would be legitimate only if it tries to kill the new treatment-to-readout causal-opportunity/readout-sensitivity rationale cheaply. It must not rerun or retune R100, search parameter space for a positive priming effect, or inherit any confirmatory credit.

A later `REVISIT_CANONICALIZE` remains valid only with a fresh candidate ID, independently motivated and meaningfully distinct question, fresh reduction/comparator/falsifier contract, informative negative outcome and zero inherited credit from R100/Audit/Theory/Forge/Revisit observations.

## False-positive / false-negative balance

- Zombie/rescue false-positive risk: low now. No terminal ID reopened and no fresh successor was manufactured.
- Moving-goalpost risk: low if R10 remains a Revisit signal only. It becomes material if the audit is used to redraw R100's intervention/readout and immediately retry the old hypothesis.
- False-negative / over-terminalization risk: now moderate for Candidate #35 if R10 is simply ignored. This is the first concrete candidate-specific signal that an old closure surface may have been non-diagnostic beyond its narrow signature.
- FORMAL contamination risk: low; all current H7 failures remain before identity/START.

The system is therefore not reviving too many weak old lines. The more immediate calibration concern is potentially missing one valuable fresh question adjacent to #35 while still needing to prevent that question from becoming rescue laundering.

## Mechanism supply and pass reachability

Supply remains fragile: one canonical object is scientifically queued and READY, but zero are currently end-to-end executable. Candidate #35 contributes a plausible Revisit signal, not a candidate. This throughput pressure does not justify weaker claim ceilings, comparator/falsifier rules or FORMAL integrity.

PASS is realistically reachable without weakening standards. H7 requires only science-invariant operational completion and a fresh exact-binding Analyst gate; no hidden second scientific success gate is required.

## Mandatory questions

1. Development-phase semantics consistent end-to-end? **Yes for current scientific objects.**
2. Cycle 3 mistaken for hard cap? **No.**
3. Science-invariant vs science-affecting distinguished? **Yes.**
4. Development observations kept out of independent evidence credit? **Yes.**
5. FORMAL one-way integrity unchanged? **Yes.**
6. Legitimate fresh SYSTEM→MECHANISM successors suppressed/manufactured? **No manufactured successor; #35 now has a fresh-question signal requiring adjudication, not automatic promotion.**
7. PRE_FORMAL genuine development? **Yes.**
8. Terminal semantics calibrated? **Yes for old IDs; #35 must remain terminal regardless of Revisit disposition.**
9. Revisit catches genuinely changed conditions? **A first plausible candidate-specific signal now exists after the last Analyst; end-to-end handling is not yet demonstrated.**
10. Revisit avoids rescue/zombies? **Yes so far.**
11. REVISIT_FORGE_TEST tests new triggers? **Insufficient evidence; no live Revisit Forge case yet.**
12. Bootstrap complete/conservative? **Yes at last Analyst snapshot.**
13. PASS reachable without weaker standards? **Yes scientifically; not operationally on the current stale bridge pin.**

## Prospective recommendations

Keep all H7 science/runtime/input/scorer/preserver/comparator/threshold/intervention semantics unchanged. Do not arm the current bridge. Repair only its dormant exact controller pin and request pin to the repaired controller, validate without results, then require a fresh Analyst exact rebind before GO_ONCE.

Feed Independent Audit R10 into the next Revisit differential scan with zero confirmatory credit. Do not reopen or rewrite Candidate #35. If R10 passes trigger-independence/new-question screening, use Revisit metadata and at most a bounded new-trigger Forge test; do not rerun/retune the old R100 experiment. Canonicalize only a genuinely fresh object satisfying the full fresh-successor contract.

No Utility request is warranted; existing Analyst/Revisit and H7 operational lanes can resolve both issues without a new parallel assignment.

## Hard-floor confirmation

No consumed FORMAL identity was rerun/retuned/rescored. No terminal current object was reactivated. No frozen scientific protocol or historical PASS/FAIL was rewritten. No evaluator/held-out target was accessed. No H7 identity/START/raw/score/preserve/new evidence exists. This methodology audit changes only its designated methodology latest/state/history records.

## Confidence

High for H7 controller repair, stale bridge pin and current FORMAL safety. High for the narrow Candidate #35 treatment/readout causal-opportunity finding because it is directly supported by frozen source/preserved raw audit. Moderate for the final Revisit disposition because that requires fresh Analyst assessment of independence and whether a meaningfully distinct new question is available.

## Questions for Control / Analyst

- Before any H7 GO_ONCE, does the exact bundle include repaired controller, launch contract, dormant request, bridge workflow and target branch together rather than only the controller?
- Does R10 qualify as a candidate-specific `REVISIT_TRIGGERED` signal or remain dormant narrowing? Either way, keep Candidate #35 terminal and zero-credit.
- If R10 is referred to Forge, can the probe be written to attack only treatment-to-readout causal opportunity/readout sensitivity without rerunning, retuning or positive-searching the old R100 priming hypothesis?
