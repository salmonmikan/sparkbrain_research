# Methodology Calibration History — R66 — 2026-09-22 21:32 JST

Generation `METHCAL-20260922T213200+0900-R66-2C8F7A41` supersedes R65.

Overall classification: `MIXED_CALIBRATION`.

## Strongest new evidence

Candidate 33's exact CI failure is now diagnosed from the runner path: the GitHub-hosted environment reaches the candidate's fixed test path but fails at required user-namespace creation with `unshare: write failed /proc/self/uid_map: Operation not permitted`, before the intended synthetic Architecture observable is produced. No post-failure candidate-33 research commit is observed. Fresh Evidence Analyst R75 correctly keeps the object `OPEN_DEVELOPMENT`, reclassifies it `HOLD_METHOD_LIMITED / NONTERMINAL_HOLD / QUEUED`, and allows same-revision continuation only on an independently verified bounded runner that satisfies the already-fixed isolation contract unchanged.

This refines the development exposure boundary: a test-step failure is not automatically `RESULT_EXPOSED_DEVELOPMENT`. Pre-observable environment-capability failures may remain OPEN when durably logged and when the scientific contract is unchanged. Weakening or replacing the isolation/backend/trust/resource/raw/verdict/claim contract to escape the failure is science-affecting and must be explicitly versioned before execution.

H7 supplies the first live compliant post-result science-affecting revision under HUMAN-20260922-005. Cycle-7 preidentity validation found the prospective `Episode.split` field unfixed even though it participates in exact `episode_id`. MAIN failed closed before choosing it or creating an identity. R75 then prospectively versioned `H7-FORMAL-R2-INPUT-SPLIT-BINDING-AND-PREIDENTITY-REVALIDATION` with fit=`train`, calibration=`dev`, evaluation=`test`, and result-bearing `smoke` forbidden.

The R2 branch now exists at `4d898b150caede1cbe0e57a8d12a37b9e71d18a0`. It adds a versioned R2 contract/runner while leaving R1 artifacts unchanged. The R2 contract explicitly records `SCIENCE_AFFECTING_CHANGE_VERSIONED_BEFORE_FORMAL_IDENTITY`, preserves all other R1 scientific fields, and keeps identity, STARTED, protected evaluation, result-bearing execution, scoring and scientific preserve/evidence false. This is positive live evidence that result-exposed development can accept a science-affecting prospective correction without rewriting prior development or weakening the one-way boundary.

## Remaining calibration defects

- Candidate-33 environment readiness needs a functional capability probe; binary/tool presence was insufficient to establish that the frozen namespace contract was runnable.
- PF-R1 durable exact raw/summary byte preservation remains unresolved; the existing bounded Utility request is not observed completed.
- H7 still has no `identity_candidate_sha` and no fresh consumed-FORMAL end-to-end case. The exact final R2 head still requires fresh review and complete binding/preserve/protected-evaluation closure before identity.
- Genuine fresh SYSTEM→MECHANISM successor admission remains unobserved.

## Canonical funnel / risks

Fresh Analyst R75: `33 = MECHANISM 13 / SYSTEM 20`, classification completeness `33/33`, `ACTIVE=1 / NONTERMINAL_HOLD=1 / TERMINAL_FOR_CURRENT_OBJECT=31`, PRE_FORMAL eligible/READY `1/1`, fresh FORMAL authority `0`, development phase counts `OPEN=3 / RESULT_EXPOSED=30 / canonical CONSUMED_ONE_WAY=0`, historical official consumed identities `7`.

False-positive risk: `LOW_TO_MODERATE_WATCH_IMPROVING`.
False-negative/opportunity-cost risk: `MODERATE_WATCH`.
Moving-goalpost/rescue risk: `LOW_TO_MODERATE_WATCH_IMPROVING`.
Over-terminalization risk: `LOW_TO_MODERATE_WATCH_IMPROVING`.

PASS remains realistically reachable without weakening evidence standards.

No new Utility request was created. Hard floor remains `CONFIRMED_DO_NOT_RELAX`.
