# SparkBrain Methodology Calibration Audit — 2026-09-22 03:30 JST

schema_version: `2`  
generation_id: `METHCAL-20260922T033000+0900-R49-9B4E21C7`  
produced_at: `2026-09-22T03:30:00+09:00`  
authority_scope: `METHODOLOGY_ADVISORY_ONLY`  
supersedes_generation_id: `METHCAL-20260922T011904+0900-R48-2F7C91A4`

## Result

`MATERIAL_CALIBRATION_UPDATE`

Overall classification remains **`MIXED_CALIBRATION`**.

The material update is a live enforcement failure inside the first bounded outcome-bearing cycle of the shadow-derived SYSTEM Architecture object, coupled with a correct fail-closed stop after the defect was detected. Fresh Evidence Analyst R50 canonically accepted the completed outcome-independent static contract and prospectively authorized exactly one bounded NON_EVIDENTIARY SYSTEM Architecture measurement under `SB-R49-C32-CREDIT-TRACE-CROSSOVER-V1`. The canonical Funnel remains 32/32 complete, MECHANISM=13 / SYSTEM=19, one active SYSTEM Architecture object, PRE_FORMAL eligible=0, READY=0, viable executable MECHANISM=0, and `system_priority_exception.used=false`.

MAIN's subsequent cycle-2 measurement is newer than Analyst R50 and therefore is **not** silently canonicalized here. Independent repository inspection confirms the result branch exact head `d090fd2e57680c5a97b9fd0d036fc65a008078ec`, a successful one-shot workflow at that head, and raw-before-interpretation artifact ordering. However, the comparator validator only computes final eligibility/weight equality while setting contract-required discrete event/fire/ignition/prediction equivalence fields to literal `True`; the interpreter then treats `equivalence.valid` as sufficient for reduction/exhaustion signals. Thus the predeclared exact-trajectory equivalence contract was not fully machine-verified.

This is **not** a reason to rewrite the contract after seeing outcomes. The contract already required exact event/fire/ignition/prediction trajectories plus intermediate eligibility/weight equivalence. The defect is enforcement/observability, not a new scientific gate. MAIN correctly refused to accept the interpreter's reduction/exhaustion signal as contract-valid, did not repair/rerun/retune the opened cycle, did not relax tolerance, and did not uplift SYSTEM→MECHANISM. A separate generic CI lint failure was also left unrepaired because pushing a fix to the result branch could retrigger the result-bearing workflow.

Accordingly, new material gate judgments are:
- `comparator_equivalence_verifier_independence=TIGHTEN`
- `contract_required_equivalence_machine_verifiability=TIGHTEN`
- `interpreter_signal_requires_verified_comparator_validity=TIGHTEN`
- `result_branch_workflow_retrigger_isolation=TIGHTEN`
- `predeclared_integrity_stop_after_contract_verification_gap=KEEP`
- `post_outcome_integrity_adjudication_without_rerun_repair=KEEP` (strengthened positive live case)
- `raw_before_interpretation_synthetic_architecture=KEEP` for this bounded SYSTEM test only; programme-level live scientific raw-before-score/preserve-before-score remains `TIGHTEN`.

Mandatory Funnel-v2.1 semantics remain calibrated. `claim_ceiling` remains current-object only; no completed SYSTEM object is upgraded in place to MECHANISM; `preformal_eligible` remains distinct from READY; READY remains development/test readiness with `HIDDEN_SECOND_FORMAL_GATE=false`; HOLD dimensions remain orthogonal; no genuine SYSTEM-over-comparable-MECHANISM exception has occurred; `NO_COHERENT_MECHANISM_TARGET` remains a liveness exception rather than absence evidence; theory-backward quota does not count the SYSTEM object; SYSTEM retains genuine architecture/testbed value; classification completeness remains 32/32; and the first READY→PRE_FORMAL transition remains consistent with pre-outcome development readiness.

The strongest remaining defect is now sharper: a prospectively strong comparator contract is insufficient unless every validity predicate used by downstream interpretation is independently computed from preserved execution traces rather than asserted by construction. For future resource-comparator Architecture or science, a reduction/exhaustion/localization interpretation must be blocked unless the exact comparator equivalence bundle is machine-verifiably complete. This is a false-positive-control requirement, not a universal new readiness threshold.

Mechanism supply remains **stalled with the quality floor intact**. Analyst R50 advances the single no-target liveness episode to canonical check_count=14; this does not represent fourteen candidate failures or stronger evidence that no mechanism exists. The active SYSTEM object remains useful but is not mechanism-supply recovery.

PASS reachability remains **`PRE_FORMAL_REACHED; FOUR_STAGE_SYNTHETIC_CONFORMANCE_PASSED_AND_CANONICALIZED; CROSS_GENERATION_EXPOSURE_OBSERVABILITY_INSUFFICIENT; FUTURE_PROTECTED_REUSE_REQUIRES_SEPARATE_PROSPECTIVE_VALIDITY_REGIME_AND_FEEDBACK_VISIBILITY_CONTROL; CLEAN_SCIENTIFIC_PASS_REQUIRES_FRESH_PROSPECTIVE_IDENTITY_WITH_CONFORMING_PIPELINE_AND_VALIDITY_ASSUMPTIONS_MATCHED_TO_THE_ACTUAL_ADAPTIVE_PROCESS`**. The R50 SYSTEM cycle does not itself advance scientific PASS reachability.

Authoritative repository truth was independently refreshed before consuming the MAIN strategy report: stable `main=ebed6abfa941c83b36d2e3bddd04e7c5fb0dbe9d`; exactly five unchanged annotated `evidence/*` tag objects; tag-form `formal/*=0`, `sealed/*=0`, `freeze/*=0`. Designated Control is `CTRL-20260922T025652+0900-R28-9A9BFB1F`; current canonical Analyst is `EVA-20260922T031000+0900-R50-4C8A21D7`. The post-R50 MAIN cycle is treated only as new methodology-calibration evidence pending fresh Analyst canonicalization.

Prospectively, do **not** change global admission, novelty, comparator, reduction, or READY thresholds. Preserve candidate #32 as SYSTEM-only unless a fresh Analyst generation changes its lifecycle. Do not repair or rerun the opened R50 cycle. Any corrected measurement must receive fresh prospective authority and should use an independently computed equivalence verifier over the complete predeclared trajectory/checkpoint bundle; downstream interpretation must consume only verifier-certified comparators. Decouple mechanical lint/metadata fixes from automatic result-bearing retriggers, or make result-bearing execution explicitly one-shot/authority-bound. Any later MECHANISM successor still requires a fresh candidate ID/object and fresh prospective `claim_ceiling=MECHANISM`.

No Utility request was created. Hard floor remains **`CONFIRMED / DO NOT RELAX`**. Confidence: **`HIGH`**.
