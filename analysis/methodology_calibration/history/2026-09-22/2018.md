# Methodology Calibration History — R65 — 2026-09-22 20:18 JST

Generation `METHCAL-20260922T201843+0900-R65-4B9E71C2` supersedes R64.

Overall classification: `MIXED_CALIBRATION`.

## Strongest new evidence

Candidate 33 advanced after canonical Evidence Analyst R72. The branch `research/exploratory-sub-cand33-auditable-raw-r71-cycle2` now points to `71af7a36862eeb2b5d08c2a8f37462ed21acc972`. The intervening commit is formatting/lint-only and is classified `SCIENCE_INVARIANT_REPAIR`.

Exact-head CI `35715729340` passes Lint and Local readiness under Python 3.11 and 3.13, then fails the Test step in both jobs; Validate bundle is skipped. The tracked tests exercise SYSTEM-Architecture semantics including verifier recomputation of preserved raw bytes, SYSTEM mismatch verdicts, rejection of stale declared digests, and fail-closed ledger binding. This is a meaningful synthetic development/conformance result, though not scientific or confirmatory evidence.

R72's canonical `OPEN_DEVELOPMENT` status predates this test execution. Fresh authority should therefore explicitly reassess the development phase before any science-facing repair. Default calibration is `RESULT_EXPOSED_DEVELOPMENT` once the failing semantic conformance result is acknowledged, unless the exact failure is demonstrated to be purely non-semantic harness plumbing.

The exact failing assertion is not available from structured job metadata, so the next repair remains unclassified. If the fix only corrects implementation behavior to already-fixed Architecture semantics, same-object `SCIENCE_INVARIANT_REPAIR` is acceptable. If it changes raw schema, trust root, namespace/isolation semantics, resource caps, verdict vocabulary, comparator/provenance contract or claim meaning, it is `SCIENCE_AFFECTING_CHANGE` and requires an explicit versioned revision or fresh successor preserving the failed result.

No later candidate-33 commit is observed, so no post-result rescue/manipulation has occurred yet.

## H7 / hard floor

H7 designated cycle-7 ref remains `67ed8fad1d861463e4129d44efbb2540affd1889`. Latest MAIN recovery records that exact active/reviewed head, `identity_candidate_sha=null`, no FORMAL identity/result consumption, and no authority for identity, STARTED, protected evaluation or result-bearing execution.

Stable `main=ebed6abfa941c83b36d2e3bddd04e7c5fb0dbe9d`; authoritative `evidence/*` remains exactly five tag objects; tag-form `formal/*`, `sealed/*`, `freeze/*` remain empty. PF-R1 byte durability remains open; the existing bounded Utility request remains approved but not observed executed.

Hard floor remains `CONFIRMED_DO_NOT_RELAX`.

## Calibration delta

- `development_phase_transition_after_science_facing_synthetic_conformance_result=TIGHTEN`
- `candidate33_next_repair_classification_after_test_failure=CLARIFY`
- `candidate33_lint_only_repair_before_tests=KEEP`
- `post_result_science_affecting_change_requires_explicit_version_or_fresh_successor=KEEP`
- `durable_result_exposed_development_raw_result_preservation=TIGHTEN`
- all FORMAL one-way, READY/PRE_FORMAL, claim-ceiling, terminal-scope, mechanism-priority and classification-completeness protections remain unchanged.

Canonical Funnel metrics remain R72's `33 = MECHANISM 13 / SYSTEM 20`, completeness `33/33`, ACTIVE 2, terminal current objects 31, PRE_FORMAL eligible/READY `1/1`, fresh FORMAL authority 0. These counts are not silently rewritten from post-R72 CI state.

False-positive risk: `LOW_TO_MODERATE_WATCH`, localized to candidate-33 post-failure repair classification. False-negative/opportunity-cost risk: `MODERATE_WATCH_IMPROVING`. Moving-goalpost/rescue risk: `LOW_TO_MODERATE_WATCH_LOCALIZED_CAND33`. Over-terminalization risk: `LOW_TO_MODERATE_WATCH_IMPROVING`.

PASS remains realistically reachable without weakening standards.

No new Utility request was created.
