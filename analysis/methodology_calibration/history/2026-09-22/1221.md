# SparkBrain Methodology Calibration Audit History — 2026-09-22 12:21 JST

schema_version: `2`  
generation_id: `METHCAL-20260922T122122+0900-R58-B84F2D17`  
produced_at: `2026-09-22T12:21:22+09:00`  
authority_scope: `METHODOLOGY_ADVISORY_ONLY`  
supersedes_generation_id: `METHCAL-20260922T111918+0900-R57-CE3EB4CC`

## Result

`MATERIAL_CALIBRATION_UPDATE`

Overall classification remains **`MIXED_CALIBRATION`**.

Prior Methodology R57 was read first. Stable repository/evidence were then independently refreshed before designated Control/Analyst history and only afterward current MAIN/SUB strategy mailboxes were consumed. Stable `main=ebed6abfa941c83b36d2e3bddd04e7c5fb0dbe9d`; annotated `evidence/*` remains exactly five at unchanged tag-object SHAs; tag-form `formal/*=0`, `sealed/*=0`, `freeze/*=0`.

The material update is the human-originated process directive `HUMAN-20260922-005`, consumed as **process guidance, not scientific evidence**. Methodology accepts its central calibration: development must be flexible while confirmatory evidence remains one-way. The required `OPEN_DEVELOPMENT / RESULT_EXPOSED_DEVELOPMENT / CONSUMED_ONE_WAY` axis is scientifically useful only if it remains orthogonal to Funnel stage, `claim_ceiling`, `preformal_eligible`, READY, HOLD dimensions, terminal state and queue state.

There is not yet an end-to-end live implementation to audit. `HUMAN-20260922-005@2f655f3c32a19263260a5154138a95b35612d7b9` postdates Control R32, Analyst R59, MAIN R59 and SUB R59. Their current records therefore represent the pre-directive policy, not a failed post-directive rollout.

That pre-directive policy is nevertheless a strong calibration example of **over-conservative development semantics**. Analyst R59/SUB R59 explicitly keep candidate #32 `SYSTEM / HOLD_METHOD_LIMITED / TERMINAL_FOR_CURRENT_OBJECT / NOT_QUEUED`, cycle 2, and prohibit same-object repair, rerun, retune and cycle 3. The historical R50 verifier implementation contains equivalence predicates for event schedule, fire order, ignition order and prediction trajectory that are hard-coded `True` while validity is computed only from eligibility and weights. Because the scientific equivalence requirement was already fixed before that result, correcting those predicates is prospectively a `SCIENCE_INVARIANT_REPAIR` under HUMAN-005, not a new scientific criterion. This historical object is calibration evidence only: **do not reopen, rerun, reinterpret or alter candidate #32 solely because methodology changed**.

Accordingly:
- `open_development_bounded_iteration=RELAX`;
- `result_exposed_same_object_science_invariant_repair=RELAX`;
- `cycle3_as_automatic_terminal_cap=RELAX`;
- `preformal_as_real_development_surface=RELAX`;
- `fresh_successor_suppression_after_terminal_current_object=RELAX`;
- `development_phase_axis_end_to_end=CLARIFY`;
- `repair_classification_science_invariant_vs_science_affecting=CLARIFY`;
- `terminal_for_current_object_topic_scope=CLARIFY`.

The protective side remains strict:
- `consumed_one_way_formal_integrity=KEEP`;
- science-affecting changes after meaningful development-result exposure require a versioned development revision or fresh successor preserving prior results: `KEEP`;
- repeated PRE_FORMAL observations are not independent confirmatory evidence: `KEEP`;
- same-object post-outcome SYSTEM→MECHANISM upgrade remains prohibited: `KEEP`;
- cycle 3 remains a mandatory reassessment point, with continuation only when a distinct prospective information-gain question is recorded: `KEEP`.

Current canonical funnel remains `32 = MECHANISM 13 / SYSTEM 19`, classification completeness `32/32`, `ACTIVE=0 / NONTERMINAL_HOLD=1 / TERMINAL_FOR_CURRENT_OBJECT=31`, Architecture active/queued M/S=`0/0`, PRE_FORMAL eligible/READY=`0/0`, viable executable MECHANISM=`0`. The no-target episode is canonical check_count `22` and SUB-local `23`; it remains liveness-only, denominator-excluded and non-evidentiary. The exception itself remains calibrated, but repeated no-target liveness must not become an implicit reason to suppress fresh versioned development revisions or legitimate fresh successors.

`HIDDEN_SECOND_FORMAL_GATE=false` remains the correct READY interpretation. However, future PRE_FORMAL must no longer inherit a blanket no-rerun/no-repair posture from FORMAL. READY means only that the next development test is well-defined and informative. Iterative PRE_FORMAL tuning/testing is permitted with durable provenance, but none of those repeated observations become independent confirmation.

Fresh SYSTEM→MECHANISM successors are legitimate only with a new candidate ID, an independently motivated mechanistic residual/question, fresh comparator/reduction/falsifier contracts and a fresh development-phase state. “Independently motivated” must not mean “unrelated to prior results”: development results may motivate a successor. Rescue laundering occurs when the same object/identity or exposed contract is silently rewritten to make the prior outcome succeed.

The strongest current false-negative risk is over-terminalization/candidate starvation, not a weak evidence floor. Thirty-one of 32 canonical candidates are terminal and there are zero active/queued/READY objects. That does not prove those closures were wrong, but it makes `TERMINAL_FOR_CURRENT_OBJECT` semantics and fresh-successor admission a high-priority throughput calibration surface. False-positive risk rises during rollout if development flexibility is implemented without explicit `change_class`, durable prior-result preservation and nonconfirmatory accounting.

External methodology calibration is consistent with this split: preregistration is valuable because it distinguishes prediction from postdiction, not because exploratory iteration is illegitimate. Iteration and discovery remain useful so long as they are not presented as prospectively confirmatory evidence.

PASS remains realistically reachable without weakening standards. PRE_FORMAL may iterate as nonconfirmatory development; a clean confirmatory PASS still requires a fresh `CONSUMED_ONE_WAY` FORMAL identity with a prospectively frozen protocol, raw-before-score, preserve-before-read, exact source/package/runtime/input binding, immutable evidence and a valid protected/adaptive-evaluation regime.

Prospectively:
1. add `development_phase` as an orthogonal canonical field;
2. add `dev_revision_id`, `change_class`, `prior_result_refs` and prospective `information_gain` for repeated development;
3. at cycle 3, record reassessment rather than auto-terminalization;
4. allow same-object `SCIENCE_INVARIANT_REPAIR` in OPEN/RESULT_EXPOSED development;
5. require science-affecting post-exposure changes to preserve old results and move to a versioned development revision or fresh successor;
6. keep all consumed FORMAL identities immutable;
7. interpret `TERMINAL_FOR_CURRENT_OBJECT` literally, not as topic death;
8. keep `NO_COHERENT_MECHANISM_TARGET` non-evidentiary while preventing it from becoming a successor-suppression norm.

No Utility request was created: first post-directive Control/Analyst rollout is the higher-information calibration path. Hard floor remains **`CONFIRMED / DO NOT RELAX`**. Confidence is **`HIGH`** on the calibration mismatch and **`MEDIUM`** on rollout quality because no post-directive Control/Analyst generation exists yet.
