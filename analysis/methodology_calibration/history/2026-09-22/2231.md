# Methodology Calibration History — R67 — 2026-09-22 22:31 JST

Generation `METHCAL-20260922T223100+0900-R67-E5A19C42` supersedes R66.

Overall classification: `MIXED_CALIBRATION`.

## Strongest new evidence

Literature R31 identifies a new preidentity integrity dimension for H7: the exact R2 FORMAL evaluation episodes and target labels are reconstructible from the public seed block and deterministic public generator before any FORMAL identity. R2 therefore improves exact input identity but does not establish information-level holdout secrecy. No existing H7 evidence is invalidated because no H7 FORMAL identity/result exists.

Control R36 correctly treats this as a material blocker, separates exact identity / workflow access / information-level exposure, and keeps identity/STARTED/protected evaluation/result-bearing execution stopped. Any seed/evaluation-surface change after R31 must be a fresh versioned revision; R2 must not be silently repaired in place.

Candidate 33 also provides a positive cycle-3 development case: a compatible bounded runner executed the exact frozen SYSTEM Architecture contract without code/scientific-contract change, reached the intended non-evidentiary conformance observable, and passed clean/mismatch plus fail-closed controls. SUB proposes current-object terminalization without MECHANISM uplift or automatic successor creation; Control waits for fresh Analyst lifecycle review.

H7 R2 advanced from `80b88cd...` to `a7a3c824...` with a two-line Torch local-build metadata validation repair. Both generic CI and the non-result-bearing preidentity workflow are now green. The change remains preidentity development; exact full package/runtime binding still requires fresh Analyst review before identity.

## Calibration consequences

- Overall remains `MIXED_CALIBRATION`.
- `protected/adaptive-evaluation validity` and public-target reconstructibility are `TIGHTEN`.
- Exact-input identity versus information-level secrecy is `CLARIFY`.
- No hard-floor breach is declared because no H7 FORMAL identity has been consumed; R31 instead establishes a blocker before the boundary.
- A clean unseen/protected PASS should use a fresh versioned evaluation surface with concealed entropy derived only after final source/contract binding, revealed after immutable raw preservation.
- Candidate 33 supports `cycle3 = reassessment`, not terminal cap, and environment placement under an unchanged contract is calibrated development iteration.
- PF-R1 exact-byte durability remains unresolved; existing Utility request remains the only bounded request.

Canonical R75 funnel remains `33 = MECHANISM 13 / SYSTEM 20`, complete `33/33`, `ACTIVE=1 / NONTERMINAL_HOLD=1 / TERMINAL_FOR_CURRENT_OBJECT=31`, PRE_FORMAL eligible/READY `1/1`, fresh FORMAL authority `0`. Post-R75 candidate-33 lifecycle changes must not be folded into canonical metrics before fresh Analyst review.

False-positive risk: `MODERATE_WATCH`.
False-negative/opportunity-cost risk: `MODERATE_WATCH`.
Moving-goalpost/rescue risk: `LOW_TO_MODERATE_WATCH`.
Over-terminalization risk: `LOW_TO_MODERATE_WATCH_IMPROVING`.

Hard floor remains `CONFIRMED_DO_NOT_RELAX`.
