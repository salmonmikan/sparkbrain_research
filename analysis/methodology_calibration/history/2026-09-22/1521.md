# SparkBrain Methodology Calibration Audit — 2026-09-22 15:21 JST

schema_version: `2`  
generation_id: `METHCAL-20260922T152143+0900-R60-B7E4C219`  
produced_at: `2026-09-22T15:21:43+09:00`  
authority_scope: `METHODOLOGY_ADVISORY_ONLY`  
supersedes_generation_id: `METHCAL-20260922T132125+0900-R59-F4C7A21D`

## Result

`MATERIAL_CALIBRATION_UPDATE`

Overall classification remains **`MIXED_CALIBRATION`**.

Prior Methodology R59 was read first. Stable repository/evidence were then independently refreshed before current designated Control/Analyst history; only afterward was the current MAIN mailbox consulted. Stable `main=ebed6abfa941c83b36d2e3bddd04e7c5fb0dbe9d`; annotated `evidence/*` is still exactly five tag objects; tag-form `formal/*=0`, `sealed/*=0`, `freeze/*=0`. `ops/*` was treated only as control/history/mailbox material, not scientific source of truth.

The material update is the **first canonical READY -> PRE_FORMAL transition under HUMAN-20260922-005**. Evidence Analyst R64 completed H7 DEV-R2 implementation/conformance without scientific outcome exposure in that cycle and prospectively promoted candidate 7 to `PRE_FORMAL`, `preformal_eligible=true`, `preformal_readiness.status=READY` under versioned revision `H7-PF-R1-FROZEN-PANEL-DEVELOPMENT-EVALUATION`. R64 explicitly states that READY is justified because the next test is well-defined and informative **without requiring H7 already to defeat its reductions or survive its falsifier**. This is the strongest live evidence so far that READY is development readiness rather than a hidden second FORMAL gate.

The transition also validates the cycle policy. H7 was not terminalized at cycle 3: the cycle-3 reassessment versioned the underspecified comparator protocol into DEV-R2, DEV-R2 then completed implementation/conformance, and R64 advanced the same MECHANISM object to a fixed cycle-4 PRE_FORMAL development evaluation. `cycle3_as_automatic_terminal_cap` therefore remains `RELAX`; `cycle3_mandatory_reassessment_with_prospective_information_gain=KEEP`.

A second important positive signal is that versioning does **not** erase exposure state. H7 remains `RESULT_EXPOSED_DEVELOPMENT` while moving from DEV-R2 to PF-R1. Same-object versioning does not reset the lineage to `OPEN_DEVELOPMENT`. R64 requires science-affecting redesign after meaningful result exposure to use explicit versioning or a fresh successor preserving the prior result unchanged.

The first PRE_FORMAL run is not yet an observed result. At audit cutoff the MAIN mailbox had only acquired the R64 H7 PF-R1 cycle-4 lease; no completed result report was available. Actual iterative PRE_FORMAL rerun/retune behavior, non-independent evidence accounting across repeated result-bearing PRE_FORMAL observations, and the first science-affecting revision after a meaningful PF result remain `INSUFFICIENT_EVIDENCE`.

Gate changes/evidence: hard FORMAL floor `KEEP`; development axis active path `KEEP`; full rollout `INSUFFICIENT_EVIDENCE`; exposure-phase persistence across same-object revision `KEEP`; invariant repair `KEEP`; science-affecting change versioning `KEEP`; cycle3 auto-terminal `RELAX`; cycle3 reassessment `KEEP`; first READY->PRE_FORMAL development semantics `KEEP`; READY semantics `KEEP`; PRE_FORMAL allocation without prior scientific victory `KEEP`; iterative PRE_FORMAL live execution `INSUFFICIENT_EVIDENCE`; same-object SYSTEM->MECHANISM ban `KEEP`; fresh SYSTEM->MECHANISM admission `INSUFFICIENT_EVIDENCE`; terminal current-object scope `KEEP`; terminal-topic fresh-successor question formation `KEEP`; comparator verifier/raw-preserve/protected-adaptive plumbing remain `TIGHTEN`. `HIDDEN_SECOND_FORMAL_GATE=false`.

Canonical funnel: `32 = MECHANISM 13 / SYSTEM 19`, classification completeness `32/32`, `ACTIVE=1 / TERMINAL_FOR_CURRENT_OBJECT=31`, Architecture active/queued `0/0`, PRE_FORMAL eligible=`1`, READY=`1`, active MECHANISM=`1`, fresh FORMAL authority=`0`. Development phases remain `OPEN_DEVELOPMENT=2 / RESULT_EXPOSED_DEVELOPMENT=30 / canonical CONSUMED_ONE_WAY=0`, with seven official consumed identities separate and immutable.

Candidate 32 remains terminal but can motivate a noncanonical fresh SYSTEM-successor question without reopening the predecessor. The old no-target episode remains closed at liveness count 23 and is not mechanism-absence evidence.

Mechanism supply health is `RECOVERING_BUT_THIN_SINGLE_READY_MECHANISM_WITH_QUALITY_FLOOR_INTACT`. False-positive risk `LOW_TO_MODERATE_WATCH`; false-negative/opportunity-cost `MODERATE_WATCH_IMPROVING`; moving-goalpost/rescue `LOW_TO_MODERATE_WATCH`; over-terminalization `MODERATE_WATCH_IMPROVING`.

PASS remains realistically reachable without weakening evidence standards: PF-R1 is nonconfirmatory development, while clean confirmation still requires a fresh `CONSUMED_ONE_WAY` FORMAL identity with frozen prospective protocol, raw-before-score, preserve-before-read, exact source/protocol/package/runtime/input binding, immutable evidence and valid protected/adaptive-evaluation controls.

Prospectively, at first PF-R1 completion persist exact result refs/bindings/exposure/NONCONFIRMATORY status before redesign. Any subsequent science-affecting change must preserve prior raw/result/contract and version or create a fresh successor before a new run; same-lineage exposure state must not reset. Keep PRE_FORMAL run count separate from confirmatory evidence count. Continue fresh successor question formation from terminal topics only with new ID/contract.

Utility request: `none`. Hard floor: **`CONFIRMED / DO NOT RELAX`**. Confidence: `HIGH` through READY allocation; `MEDIUM_LOW` for completed iterative PRE_FORMAL/post-result revision because those cases remain unobserved.
