# SparkBrain Methodology Calibration Audit — 2026-09-21 12:19 JST

schema_version: `2`  
generation_id: `METHCAL-20260921T121915+0900-R35-6D2A8F41`  
produced_at: `2026-09-21T12:19:15+09:00`  
producer_run_id: `methodology-calibration-auto-20260921T121915+0900-R35-6D2A8F41`  
authority_scope: `METHODOLOGY_ADVISORY_ONLY`  
supersedes_generation_id: `METHCAL-20260921T112242+0900-R34-9C4E2A71`

## Run disposition

**`MATERIAL_CALIBRATION_UPDATE`**

## Overall classification

**`MIXED_CALIBRATION`**.

The material change is fresh canonical adjudication of the first live PRE_FORMAL execution. Evidence Analyst R34 independently confirms the previously identified `raw-before-score` defect and canonicalizes `CAND-V05-ASSEMBLY-SET-CAUSAL-NECESSITY-DISTRIBUTIONAL-CONTROLS-01` as `HOLD / MECHANISM / preformal_eligible=false / HOLD_METHOD_LIMITED / TERMINAL_FOR_CURRENT_OBJECT / NOT_QUEUED`. It explicitly forbids rerun, repair, rescoring, or redesign under the same identity merely to obtain a cleaner pipeline. This resolves the prior pending-adjudication uncertainty without rewriting the consumed object.

The same Analyst generation opens a fresh SYSTEM Architecture object, `CAND-PREFORMAL-RAW-PRESERVE-SCORER-PIPELINE-INTEGRITY-01`, solely to determine prospectively whether future PRE_FORMAL can enforce `raw generator -> durable raw preserve/digest -> fixed scorer -> scored preserve`. Its first cycle is static/read-only feasibility only; it does not authorize same-run implementation, new scientific outcome, or any repair of R33.

## Fresh independent reconstruction

Prior Methodology R34 was read first. Stable `main` was independently re-fetched at `ebed6abfa941c83b36d2e3bddd04e7c5fb0dbe9d`. Authoritative annotated refs remain `evidence/*=5`, `formal/*=0`, `sealed/*=0`, and tag-based `freeze/*=0`.

Designated Control remains `CTRL-20260921T085000+0900-R21-4F7C2A91@49ac783b5640b8250c19133f8842f0bf49867e8f`. Fresh designated Evidence Analyst is `EVA-20260921T120207+0900-R34-2E7C91A4@d5554d45a62d0ef0a20d0443e40b0f4d11c0eea8`.

The fresh Analyst canonical funnel is `29/29` complete, `MECHANISM=13 / SYSTEM=16`, PRE_FORMAL eligible=`0`, READY=`0`, with two active SYSTEM objects: one Discovery object and one Architecture integrity object. No fresh FORMAL identity or authoritative FORMAL evidence was created.

## Funnel v2.1 mandatory audit

1. `claim_ceiling` current-object semantics: **KEEP**. Current objects have object-local SYSTEM ceilings; no prestige/topic relabeling observed.
2. completed SYSTEM -> same-object MECHANISM upgrade: **KEEP**. No such upgrade observed.
3. `preformal_eligible` vs READY: **KEEP**. The historical first transition remains a valid example of eligible-before-READY; the integrity defect occurred after admission.
4. READY semantics: **KEEP**; `HIDDEN_SECOND_FORMAL_GATE=false`. R33 READY was development/test readiness before intervention outcome, not prior success.
5. HOLD multidimensional model: **KEEP**. Canonical Analyst state preserves hold class, terminal state, and queue state separately. Producer enum conformance now warrants **TIGHTEN** because a fresh SUB dependency HOLD again used a non-v2.1 enum before Analyst normalization.
6. MAIN MECHANISM priority / SYSTEM-priority exception: **KEEP**. Fresh Analyst reports viable executable MECHANISM=`0`; the integrity SYSTEM object does not displace a comparable MECHANISM, so `system_priority_exception.used=false` is correct. First genuine exception remains **INSUFFICIENT_EVIDENCE**.
7. `NO_COHERENT_MECHANISM_TARGET`: **KEEP**. No current exception; the next new autonomous selection must reassess theory-backward supply rather than manufacture a weak MECHANISM.
8. theory-backward quality: **KEEP**. No SYSTEM object is being relabeled as MECHANISM.
9. SYSTEM value: **KEEP**. The fresh pipeline-integrity Architecture object has genuine reproducibility/evidence-ordering value under MECHANISM priority.
10. PRE_FORMAL/PASS reachability: **PRE_FORMAL_REACHED; CLEAN_PASS_REACHABILITY_REQUIRES_PROSPECTIVE_PIPELINE_FIX_AND_LIVE_CONFORMANCE**.
11. classification-completeness gating: **KEEP**. Canonical policy conclusions now use `29/29`; the nonconforming PRE_FORMAL result is represented explicitly as a terminal method-limited row rather than omitted.
12. first READY -> PRE_FORMAL empirical semantics: **KEEP**. It remains consistent with development readiness rather than prior success, even though the subsequent execution pipeline was nonconforming.

## Material gate classifications

- hard-integrity policy itself: **`KEEP`**; do not relax.
- `preformal_raw_before_score_execution_order`: **`TIGHTEN`**; no conforming future live execution yet exists.
- `preformal_preserve_then_score_pipeline`: **`TIGHTEN`**; the fresh SYSTEM object is only architecture feasibility so far.
- `preformal_immutable_evidence_durability`: **`TIGHTEN`**; durable raw-preserve-before-score remains to be demonstrated prospectively.
- post-outcome integrity adjudication without rerun/repair: **`KEEP`**; first live defect was canonicalized as method-limited terminal without same-object rescue.
- no silent post-outcome repair: **`KEEP`**, now empirically stronger.
- exact source/package/runtime/input binding: **`KEEP`**.
- first Analyst-authoritative READY -> PRE_FORMAL transition: **`KEEP`**.
- `preformal_eligible` / READY separation: **`KEEP`**.
- READY development-readiness semantics: **`KEEP`**.
- HOLD multidimensional canonical model: **`KEEP`**.
- producer Funnel-v2.1 HOLD enum conformance: **`TIGHTEN`** (was `CLARIFY`); a fresh producer again emitted a temporary dependency HOLD outside the canonical v2.1 enum and required Analyst normalization.
- classification-completeness gating: **`KEEP`**.
- MAIN MECHANISM priority: **`KEEP`**.
- prospective SYSTEM-priority exception: **`KEEP`**; first genuine use **`INSUFFICIENT_EVIDENCE`**.
- rolling one-in-three theory-backward supply: **`KEEP`**; observed selection window `MECHANISM / SYSTEM / SYSTEM = 1/3`.
- theory-backward quality floor: **`KEEP`**.
- `NO_COHERENT_MECHANISM_TARGET`: **`KEEP`**.
- SYSTEM architecture/testbed/reproducibility value: **`KEEP`**.
- causal comparator equivalence standard: **`SPLIT_BY_CLAIM_TYPE`**.
- equal-privilege comparator / ordinary-reduction-first / claim-type separation: **`KEEP`**.
- external-validation competent reference, relevant simple state-machine reduction, shared-source clustered inference, and legacy Top-k sparse-support: **`TIGHTEN`**, unchanged.

## Calibration dimensions

`gate_drift`: low in admission/scientific semantics; known PRE_FORMAL execution-order nonconformance is now canonically acknowledged rather than normalized away.  
`justification_trace`: strong.  
`false_positive_control`: improved by explicit method-limited terminalization; future scientific inference still requires a conforming raw-preserve-before-score pipeline.  
`false_negative_risk`: moderate if the integrity defect is incorrectly converted into a stricter READY prior-success gate or if the consumed object is rerun; neither is currently authorized.  
`duplicate_guards`: none material.  
`moving_goalposts`: `LOW`; outcome was not used to alter the existing object's comparator/terminal map or to create a same-object rescue.  
`pass_reachability`: `PRE_FORMAL_REACHED_CLEAN_PASS_PENDING_PROSPECTIVE_PIPELINE_CONFORMANCE`.  
`comparator_calibration`: `HEALTHY_CLAIM_TYPE_SPLIT_WITH_PROSPECTIVE_EQUAL_PRIVILEGE`.  
`signal_before_reduction`: healthy.  
`claim_type_separation`: healthy.  
`research_worthiness_vs_novelty`: healthy.  
`external_calibration`: unchanged.  
`opportunity_cost`: acceptable; bounded SYSTEM integrity work is justified while viable executable MECHANISM=`0`.  
`mechanism_supply_health`: `QUALITY_FLOOR_HEALTHY_M13_S16_NO_VIABLE_EXECUTABLE_MECHANISM_CURRENTLY_SMALL_N`.  
`funnel_observability`: `GOOD_29_OF_29_CANONICAL_WITH_PREFORMAL_NONCONFORMANCE_EXPLICITLY_CLASSIFIED`.  
`preformal_gate_calibration`: `READY_SEMANTICS_VALIDATED_EXECUTION_NONCONFORMANCE_CANONICALLY_TERMINAL_FUTURE_PIPELINE_FIX_PENDING`.

## Remaining semantic / observability defects

The central defect remains execution plumbing, not admission. A future PRE_FORMAL must demonstrate raw-only generation, durable immutable preservation and digesting before any scoring/classification, then a prospectively bound scorer consuming exactly that preserved raw digest, followed by separate scored preservation. The R33 object must remain terminal historical calibration evidence and must not be rerun or repaired.

Producer-side HOLD enum conformance is now a demonstrated recurring handoff defect rather than merely ambiguous. Canonical downstream state is protected by Analyst normalization, but producer handoff should prospectively validate/reject/map noncanonical HOLD enums before publication so queue/method/terminal semantics cannot fragment upstream observability.

## Prospective recommendations

Keep READY semantics unchanged. Complete the fresh pipeline-integrity Architecture feasibility cycle as static/read-only work; if a four-stage design can be fixed, stop for fresh Analyst review before implementation or any new science. Require producer-side Funnel-v2.1 HOLD enum schema validation prospectively. Do not rerun, retune, repair, rescore, invalidate, or upgrade the consumed R33 object solely because the methodology defect was discovered.

## Utility request

None created. The live integrity-architecture path is higher-information than a synthetic calibration request.

## Hard-integrity-floor confirmation

**POLICY CONFIRMED / HISTORICAL IMPLEMENTATION NONCONFORMANCE CANONICALLY ACKNOWLEDGED / DO NOT RELAX.** No rerun, repair, rescoring, or same-object rescue is authorized. Future conformance remains unproven until a fresh object exercises the corrected pipeline prospectively.

## Current inputs / authoritative refs

- stable `main@ebed6abfa941c83b36d2e3bddd04e7c5fb0dbe9d`
- previous Methodology `METHCAL-20260921T112242+0900-R34-9C4E2A71@7fa1c49edd81ec2e61affa4a2214fa2e377a465c`
- Control `CTRL-20260921T085000+0900-R21-4F7C2A91@49ac783b5640b8250c19133f8842f0bf49867e8f`
- Evidence Analyst `EVA-20260921T120207+0900-R34-2E7C91A4@d5554d45a62d0ef0a20d0443e40b0f4d11c0eea8`
- latest canonical PRE_FORMAL disposition: `CAND-V05-ASSEMBLY-SET-CAUSAL-NECESSITY-DISTRIBUTIONAL-CONTROLS-01 = HOLD_METHOD_LIMITED / TERMINAL_FOR_CURRENT_OBJECT / NOT_QUEUED`
- integrity Architecture object: `CAND-PREFORMAL-RAW-PRESERVE-SCORER-PIPELINE-INTEGRITY-01 = SYSTEM / ARCHITECTURE_STUDY / ACTIVE`
- authoritative tags independently re-fetched: `evidence/*=5`; `formal/*=0`; `sealed/*=0`; tag-based `freeze/*=0`

## Confidence

**HIGH** on canonical integrity adjudication, READY semantics, raw-before-score nonconformance, and classification completeness. **MODERATE** on future clean PASS reachability until the fresh pipeline design is implemented and exercised prospectively.

## Questions for Control / Analyst

- Keep the R33 PRE_FORMAL object terminal and explicitly nonconforming; no rerun/repair/rescore under the same identity.
- Require a machine-checkable `raw generator -> durable immutable preserve/digest -> fixed scorer -> scored preserve` sequence before any future clean PRE_FORMAL/PASS inference.
- Keep READY as development/test readiness; do not add prior-success requirements.
- Add producer-side schema validation so noncanonical HOLD enums are rejected or mapped before handoff, not repaired only at Analyst ingestion.
- Preserve `29/29` classification-completeness gating and keep no-target events outside candidate/conversion denominators.
- Keep SYSTEM priority exceptions prospective and machine-readable; first genuine use remains unobserved.
