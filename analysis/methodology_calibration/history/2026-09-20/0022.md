# Methodology Calibration History — 2026-09-20 00:22 JST

Overall classification: `WELL_CALIBRATED` (`MATERIAL_CALIBRATION_CONFIRMATION`).

Material new calibration evidence since 23:21:

- `CAND-ASSEMBLY-PROTOTYPE-LOCKIN-01` completed its fresh prospectively bound Architecture cycle 1 and returned `NO_OR_LOW_SUPPORT` on both DEV seeds (`0/12` representation-divergent order pairs on each; no downstream functional disagreement). The candidate was prospectively mapped to REJECT and Evidence Analyst authorized no cycle-2 rescue. This is positive evidence that the new support-aware Architecture gate is not positivity-biased.
- `CTRL-20260919-2250-TOPK-CROSS-SEED-SUPPORT` completed read-only as `MIXED`. The 0.10 magnitude replicates across both DEV seeds, but support breadth is narrow and lower magnitudes are under-supported. Evidence Analyst kept Top-k HOLD, with no cycle 3 and no PRE_FORMAL promotion. This confirms containment of the historical sparse-stratum permissiveness without retroactive relabeling.
- SUB discovered temporal-expectation batch-partition sensitivity that is exactly explainable by ordinary API/event-time ordering. Evidence Analyst nevertheless promoted one fresh Architecture study for engineering/reproducibility value, not novelty. This validates claim-type separation and false-negative control.
- MAIN committed the Temporal Architecture contract before outcome visibility. It fixes source blobs, timeline families, tracker defaults, WHOLE_BATCH/MIDPOINT_TWO_BATCH/EVENT_TIME_CAUSAL arms, horizon, isolated replay, raw schema and local outcome mapping. Workflow `35451528895` is running; no outcome-bearing result was available at audit time.

Gate conclusions:

- `KEEP`: one-way integrity floor; prospective binding; positive signal before strong mechanistic reduction; equal-privilege comparator matching; stronger-reference ceiling labeling; new-principle novelty bar; three-cycle upper bound; fresh review after Architecture cycles; prospective support-aware Architecture triage; no automatic PRE_FORMAL promotion; ordinary-control-first reduction; Architecture/engineering value surviving novelty rejection.
- `SPLIT_BY_CLAIM_TYPE`: do not use new-principle novelty as the threshold for architecture/system/testbed value.
- `RELAX`: reject programme-wide HOLD from absence of a high-value FORMAL object; current active lower funnel remains appropriate.
- `TIGHTEN`: legacy Top-k sparse-stratum voting semantics must not be reused prospectively.
- `CLARIFY`: local Architecture thresholds are not portable universal rules; outcome-fitted replacement support rules are prohibited; question-level exhaustion is distinct from family-level exhaustion.
- `INSUFFICIENT_EVIDENCE`: no universal replacement support/uncertainty threshold should be inferred from completed Top-k outcomes.

Moving-goalpost risk: `LOW`. No completed result was reclassified. Assembly was rejected under its pre-outcome map, Top-k labels stayed unchanged and HOLD followed the read-only MIXED diagnostic, and Temporal batching has no result yet.

PASS reachability: `REACHABLE_BUT_NARROW` via fresh positive native signal -> exact-bound object/protocol -> support adequate to local question -> equal-privilege comparator -> survive ordinary reductions -> fixed intervention/falsifier -> one-way preserved FORMAL evidence.

Utility request: none. Existing Top-k methodology work is complete at the currently authorized level; Temporal batching is already active MAIN Architecture work. No distinct new methodology check justifies another request.

Hard integrity floor confirmed unchanged. No consumed identity was rerun/retuned/rescored and no immutable evidence was modified.