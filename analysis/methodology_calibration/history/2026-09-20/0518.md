# SparkBrain Methodology Calibration History — 2026-09-20 05:18 JST

- run_disposition: `MATERIAL_CALIBRATION_UPDATE`
- overall_classification: `MIXED_CALIBRATION`
- previous_classification: `MIXED_CALIBRATION`
- pass_reachability: `REACHABLE_BUT_NARROW`
- moving_goalposts: `LOW`
- utility_request_created: `NONE`

## Material update

Control Brain has now accepted the generic artifact-to-handoff binding guard proposal and issued bounded assignment `CTRL-20260920-0450-HANDOFF-BINDING-GUARD`. This is a valid evidence-driven procedural tightening and does not change scientific novelty/comparator/FORMAL thresholds.

A distinct upstream fidelity defect was independently verified in `CAND-V05-UNIT-SUPPRESSION-TRANSIENT-SEMANTICS-01`. The prospectively bound harness computed `restore_restores_original_base_threshold` using the literal detector `"unit.base_threshold = threshold" in restore_source`, while the exact bound source implements restoration as `self.base.field.units[unit_id].base_threshold = threshold`. Thus a provenance-correct machine artifact can contain a semantically wrong decision-relevant static fact.

The integrity response was correct. MAIN blocked on `INVALID_DIAGNOSTIC_POST_OUTCOME_STATIC_HARNESS_FALSE_NEGATIVE`, refused in-run patch/rerun/rescore/relabel, and did not authorize a successor. Evidence Analyst likewise placed the candidate on HOLD and created bounded read-only proposal `EVA-20260920-0502-SUPPRESSION-STATIC-DETECTOR-CONSISTENCY` instead of repairing the completed result.

## Calibration consequence

The programme is not substantively too strict or too loose at the scientific admission/novelty/comparator level. It remains `MIXED_CALIBRATION` because two separate fidelity layers are not yet end-to-end enforced:

1. artifact -> durable handoff transfer fidelity;
2. source/runtime -> decision-relevant machine-fact extraction validity.

The second cannot be solved by the first: a handoff checker can faithfully transmit an already-wrong machine fact.

## Gate changes

- hard one-way integrity: `KEEP`
- prospective exact binding: `KEEP`
- equal-privilege comparator: `KEEP`
- new-principle novelty bar: `KEEP`
- architecture/testbed worthiness vs novelty: `SPLIT_BY_CLAIM_TYPE`
- programme-wide HOLD from no FORMAL object: `RELAX`
- legacy Top-k sparse-support gate: `TIGHTEN`
- universal replacement support threshold: `INSUFFICIENT_EVIDENCE`
- artifact->handoff fidelity: `TIGHTEN`
- machine threshold/support-rule application: `TIGHTEN`
- decision-relevant machine-fact extractor fidelity: `TIGHTEN`
- brittle literal-string semantic detectors: `TIGHTEN`
- fail-closed post-outcome invalid-diagnostic handling: `KEEP`
- automatic PRE_FORMAL promotion after Architecture signal: `KEEP` (remain absent)

## False-positive / false-negative symmetry

False-positive risk: a wrong machine fact can be treated as authoritative because provenance/digests are correct. False-negative risk: brittle syntax-specific extraction can mark a real property absent, as observed in suppression restoration. The proportionate response is semantic detector validation, not universal replication or novelty inflation.

## Claim-type separation

Healthy. SUB's cross-cascade fallback result is ordinarily reduced to fallback/control-flow semantics and was promoted only as a fresh Architecture/testbed segmentation question, not computational-principle novelty.

## Prospective recommendations

1. Complete the bounded handoff-binding guard prototype.
2. For decision-relevant facts derived from source/runtime inspection, bind a prospective validation method appropriate to the semantic claim; prefer structural/AST/typed checks or explicit invariants to brittle literal substring matching where practical.
3. On a post-outcome detector mismatch, preserve the artifact and fail closed for interpretation-dependent continuation. Any later diagnostic must have fresh prospective authority; do not silently repair and reinterpret the completed attempt.
4. Do not increase scientific replication, comparator, novelty, or FORMAL thresholds because of this procedural defect.
5. Do not retroactively rescore, relabel, rerun, invalidate, or upgrade consumed/frozen experiments.

## Utility

No new Methodology request. `EVA-20260920-0502-SUPPRESSION-STATIC-DETECTOR-CONSISTENCY` already directly addresses the new bounded calibration question, so another request would be duplicate.

## Hard integrity floor

Confirmed unchanged and not recommended for relaxation.
