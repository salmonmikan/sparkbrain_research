# SparkBrain Methodology Calibration Audit — R76

- schema_version: `2`
- generation_id: `METHCAL-20260923T081804+0900-R76-6F2A91D4`
- produced_at: `2026-09-23T08:18:04+09:00`
- authority_scope: `METHODOLOGY_ADVISORY_ONLY`
- supersedes_generation_id: `METHCAL-20260923T073000+0900-R75-C8D41B6E`
- material_change: `true`
- audit_result: `MATERIAL_CALIBRATION_UPDATE`
- overall_classification: `MIXED_CALIBRATION`

## Executive decision

A material update is present.

H7 R5 Cycle 12 is now **PREIDENTITY COMPLETE** at exact research head `2f30b93f8f3cf226ef55ed5af7e341089d2c3c80`. The post-repair dedicated non-result preidentity run `35794233687` and generic CI `35794233612` both succeeded on that same SHA. The previously permitted import-whitespace repair therefore has a closed exact-head review trail. This is development closure only: no FORMAL identity, STARTED marker, evaluation payload/seed reveal, protected result execution, official scoring, or scientific evidence/preserve ref exists.

This materially strengthens the HUMAN-005 development semantics. A RESULT_EXPOSED object was versioned prospectively when its resource identity changed (R4→R5), science-invariant repairs were then allowed on R5, and the final repaired head was independently re-reviewed without converting preidentity success into evidence. The programme also correctly stops further H7 development merely to maintain activity. H7 is now a provenance-gated FORMAL hold, not an invitation to retune.

The remaining H7 programme gate is PF-R1 exact-byte development-provenance preservation. Utility remains fail-closed because the existing request has no matching schema-v2 assignment/current decision. This is not a scientific reason to rerun or reconstruct PF-R1. The existing artifact must be preserved exactly if and when machine authority is materialized, followed by a fresh Analyst re-fetch of the unchanged R5 head before any one-way authority.

Candidate #34 has advanced from Discovery R1 to a prospective NON_EVIDENTIARY Architecture R1 contract while remaining `OPEN_DEVELOPMENT`, `preformal_eligible=true`, and `NOT_READY`. The contract fixes a bounded edge family, transmission-null and +1ms timing interventions, sham/matched controls, a <=256ms quiescent anchor, and an equivalence-class falsifier. A dedicated research branch now exists from stable `main`, but at audit time it still points to the unchanged stable-main SHA; the live MAIN lease is operational provenance, not scientific result. This is good evidence that PRE_FORMAL is not a hidden second FORMAL gate and that terminal prior objects need not imply topic death.

## Gate-by-gate calibration

| Gate | Classification |
|---|---|
| hard FORMAL integrity floor | `KEEP` |
| development-phase orthogonal axis | `KEEP` |
| fresh OPEN→RESULT_EXPOSED→CONSUMED_ONE_WAY end-to-end | `INSUFFICIENT_EVIDENCE` |
| RESULT_EXPOSED same-object SCIENCE_INVARIANT_REPAIR | `KEEP` |
| post-result SCIENCE_AFFECTING change → explicit version/fresh successor | `KEEP` |
| R4→R5 explicit science-affecting versioning | `KEEP` |
| exact post-repair R5 head review / both non-result gates green | `KEEP` |
| PREIDENTITY COMPLETE distinct from FORMAL authority/evidence | `KEEP` |
| no extra H7 iteration merely to maintain activity | `KEEP` |
| PF-R1 exact-byte preservation requirement | `KEEP` |
| PF-R1 preservation execution state | `TIGHTEN` |
| Utility default-deny without schema-v2 assignment | `KEEP` |
| duplicate request or rerun to bypass authority gap | `TIGHTEN` |
| exact R5 content-addressed runtime identity | `KEEP` |
| hosted-runner/kernel/provisioning dimensions | `SPLIT_BY_CLAIM_TYPE` |
| omitted claim-capable native/runtime properties | `TIGHTEN` |
| resolver/package/runner-image shopping | `TIGHTEN` |
| final exact source/runner/scorer/preserver/runtime/input binding at one-way entry | `TIGHTEN` |
| cycle 3 as automatic terminal cap | `RELAX` |
| cycle 3 as mandatory prospective reassessment | `KEEP` |
| additional cycles with distinct stated information gain | `KEEP` |
| development rerun/retune non-independent evidence accounting | `KEEP` |
| repeated result-bearing PRE_FORMAL stress case | `INSUFFICIENT_EVIDENCE` |
| raw-before-score as file ordering only | `CLARIFY` |
| literal target-blind prediction raw | `TIGHTEN` |
| immutable preserve before target-side scoring | `TIGHTEN` |
| post-preserve scorer recomputation | `TIGHTEN` |
| target-sidecar independence | `TIGHTEN` |
| fresh concealed evaluation surface | `TIGHTEN` |
| PRE_FORMAL as genuine development | `KEEP` |
| preformal_eligible distinct from READY | `KEEP` |
| READY = informative next test, not prior success | `KEEP` |
| HIDDEN_SECOND_FORMAL_GATE | `KEEP` — false |
| candidate #34 bounded Architecture R1 before readiness | `KEEP` |
| candidate #34 quiescence cap failure => reassess, not extend | `KEEP` |
| current-object claim ceiling | `KEEP` |
| same-object SYSTEM→MECHANISM uplift ban | `KEEP` |
| fresh MECHANISM successor distinctness/non-rescue | `KEEP` |
| fresh SYSTEM→MECHANISM successor specifically | `INSUFFICIENT_EVIDENCE` |
| TERMINAL_FOR_CURRENT_OBJECT topic scope | `KEEP` |
| classification completeness | `KEEP` |
| MAIN MECHANISM priority | `KEEP` |
| prospective SYSTEM-priority exception | `KEEP` |
| genuine SYSTEM-over-comparable-MECHANISM exception | `INSUFFICIENT_EVIDENCE` |
| theory-backward quality floor | `KEEP` |
| NO_COHERENT_MECHANISM_TARGET liveness semantics | `KEEP` |
| protected/adaptive-evaluation validity | `TIGHTEN` |

## Mandatory questions

1. **Are development-phase semantics implemented consistently end-to-end?** Consistent through preidentity and stronger than R75: exact repaired R5 is now independently green/reviewed while still non-evidentiary. A fresh post-HUMAN-005 `CONSUMED_ONE_WAY` completion remains unobserved.
2. **Is cycle 3 being mistaken for a hard cap?** No. R5 reached cycle 12 only through prospectively stated information-gain steps, then stopped once preidentity closure was complete. #34 starts a new Architecture cycle with its own bounded question.
3. **Are SCIENCE_INVARIANT_REPAIR and SCIENCE_AFFECTING_CHANGE distinguished correctly?** Yes in the live H7 case. R4 resource-identity reclassification was versioned to R5; subsequent import/test-plumbing repairs stayed within fixed R5 semantics and were re-reviewed at the final SHA.
4. **Are development rerun/retune/tolerance revisions logged without evidence laundering?** No laundering is observed. R5 workflows are explicitly NON_EVIDENTIARY and add zero confirmatory credit. Repeated result-bearing PRE_FORMAL remains insufficiently stress-tested.
5. **Does RESULT_EXPOSED development preserve prior results when revised?** Yes for R4→R5. Programme-wide closure remains incomplete because PF-R1 exact bytes are still not durably preserved.
6. **Is FORMAL one-way integrity unchanged?** Yes. Stable `main` and five authoritative evidence tags are unchanged; tag-form `formal/*`, `sealed/*`, and `freeze/*` remain empty; H7 has no FORMAL identity or result consumption.
7. **Are legitimate fresh SYSTEM→MECHANISM successors suppressed or manufactured?** No manufacture is observed. #34 is a legitimate fresh MECHANISM successor question, but canonical accounting still records zero fresh MECHANISM successors specifically from SYSTEM terminalizations.
8. **Is PRE_FORMAL behaving as development rather than hidden FORMAL?** Yes. #34 is preformal-eligible but NOT_READY and may perform bounded Architecture work without prior comparator victory. H7 is READY yet has zero FORMAL authority.
9. **Are terminal semantics and candidate-supply controls calibrated?** Improved. H7 can sit on a FORMAL provenance hold while MAIN moves to #34 Architecture work; prior Assembly terminal objects do not suppress a fresh residual question.
10. **Is PASS realistically reachable without weakening evidence standards?** Yes, `REALISTIC_NEAR_TERM_CONDITIONAL`. R5 preidentity is complete. PF-R1 preservation, fresh Analyst one-way authorization, and the already-required raw/preserve/scorer/concealed-evaluation/final-binding checks remain mandatory.

## Risk calibration

- false-positive risk: `MODERATE_WATCH` — the main misuse risk is treating PREIDENTITY COMPLETE or green synthetic workflows as scientific evidence/identity authority, or silently omitting a host/native property that is actually claim-capable.
- false-negative/opportunity-cost risk: `LOW_TO_MODERATE_WATCH_IMPROVING` — two MECHANISM lines now exist and MAIN can work on #34 while H7 is held. The remaining avoidable friction is PF-R1 machine-authority wiring, not evidence quality.
- moving-goalpost/rescue risk: `LOW_WATCH_IMPROVING` — R4 was preserved, R5 was explicit, final repairs were science-invariant, and H7 is now frozen from further activity-driven development.
- over-terminalization risk: `LOW_WATCH_IMPROVING` — #34 demonstrates topic continuation from an independently motivated residual without reopening or rewriting prior objects.

## Development-iteration calibration

H7 is the strongest positive HUMAN-005 case so far. The sequence is: meaningful development result exposure → explicit R5 version for a science-affecting resource-identity change → bounded science-invariant repairs → exact repaired-head non-result closure → STOP before one-way actions. Iteration itself was permitted; outcome-responsive scientific mutation was not.

The next H7 transition is not another development cycle. It is a provenance closure followed by a fresh one-way authorization decision. Any source/runtime/scorer/preserver/input change after exact head `2f30b93...` requires fresh exact-head review; any scientific criterion/resource meaning change requires explicit versioning.

Candidate #34 is also well calibrated. Discovery R1 defined the question without demanding prior success. Architecture R1 may test only reachability/instrumentation under the fixed bounded design. Quiescence failure within the fixed 256ms cap, inability to construct matched controls without outcome knowledge, or unresolved route alternatives must trigger reassessment/equivalence-class reporting rather than cap extension or post-outcome topology strengthening.

## Funnel observability / mechanism supply

Canonical R89 population is `34 = 14 MECHANISM / 20 SYSTEM`, lifecycle `ACTIVE 0 / QUEUED 1 / NONTERMINAL_HOLD 1 / TERMINAL_FOR_CURRENT_OBJECT 32`, development phases `OPEN 3 / RESULT_EXPOSED 31 / canonical CONSUMED_ONE_WAY 0`, PRE_FORMAL eligible/READY `2/1`, fresh FORMAL authority `0`, historical official consumed identities `7`, classification completeness `34/34`.

The live MAIN mailbox has acquired a NON_EVIDENTIARY #34 Architecture lease and created its research branch from stable main, but this is operational execution state and does not rewrite canonical Analyst counts until a fresh Analyst generation says so.

Mechanism-supply health: `TWO_VIABLE_MECHANISM_LINEAGES_H7_FORMAL_HOLD_PLUS_CAND34_ARCHITECTURE_QUEUE_QUALITY_FLOOR_INTACT`.

## Preformal calibration / pass reachability

PF-R1 remains one nonconfirmatory development observation with confirmatory count zero. `HIDDEN_SECOND_FORMAL_GATE=false`.

R5 PREIDENTITY COMPLETE materially improves PASS reachability, but it is not a PASS and not FORMAL authority. The prospective path is now narrow: preserve the existing PF-R1 bytes under valid machine authority without rerun/reconstruction; re-fetch unchanged R5 exact head in a fresh Analyst generation; authorize one-way only if all raw-before-score, preserve-before-read, target-side scoring, concealed-evaluation, collision/no-clobber and exact binding gates remain satisfied.

No evidence standard should be weakened.

## Utility request

No new Utility request is created. Existing `UTIL-20260922-1648-PFR1-DEVELOPMENT-PROVENANCE-PRESERVE` remains the only request. Execution stays fail-closed until matching schema-v2 authority exists. Do not duplicate, rerun, reconstruct, regenerate, or rescore PF-R1.

## Prospective recommendations

1. Materialize matching schema-v2 Utility authority for the existing PF-R1 preservation request if Control still intends execution; preserve only the existing artifact bytes and hashes.
2. After preservation, require a later fresh Analyst generation to independently re-fetch R5 exact head `2f30b93...` before any evaluation commitment or FORMAL identity.
3. Treat `PREIDENTITY COMPLETE` as a development/provenance state only, never as confirmatory evidence.
4. Keep #34 Architecture R1 non-evidentiary and bounded. Do not make READY contingent on already winning its comparator/reduction/falsifier tests.
5. If #34's fixed quiescence/control construction is unreachable, reassess the object; do not extend the cap or strengthen the scientific contract after observing outcomes.

## Hard-floor confirmation / confidence

`CONFIRMED_DO_NOT_RELAX`.

Confidence: FORMAL non-consumption `HIGH`; exact post-repair R5 preidentity completion `HIGH`; R4→R5 version discipline `HIGH`; PF-R1 authority/preservation blocker `HIGH`; #34 prospective Architecture contract and NOT_READY semantics `HIGH`; claim-capable host/native runtime sufficiency `MEDIUM_HIGH`; fresh consumed-FORMAL end-to-end `UNOBSERVED`; fresh specifically SYSTEM→MECHANISM successor `UNOBSERVED`.

## Questions for Control / Analyst

- Can the existing PF-R1 request receive matching schema-v2 machine authority without creating a duplicate request or rerunning/reconstructing the artifact?
- After PF-R1 preservation, will a fresh Analyst generation re-fetch exact R5 head `2f30b93...` and keep PREIDENTITY COMPLETE distinct from one-way authority?
- Are any host/ABI/CPU properties still claim-capable but absent from R5 scientific identity/equivalence coverage?
- For #34, will Architecture R1 remain non-evidentiary and NOT_READY until reachability/instrumentation closure, without requiring prior scientific victory?
- If #34 fails the fixed 256ms quiescence boundary or bounded intervention family cannot separate alternatives, will the programme reassess/collapse to an equivalence class rather than extend the contract?

## Authoritative refs used

- prior Methodology: `METHCAL-20260923T073000+0900-R75-C8D41B6E@4e608a2dd73059d8302dcfa353e47cabe08797e4`
- Human directive: `HUMAN-20260922-005` (process directive, not scientific evidence)
- Control: `CTRL-20260923T055000+0900-R39-C31A7D52@c93f870e47bdb5b1613475e079c0a6dee2aa1753`
- Evidence Analyst: `EVA-20260923T080115+0900-R89-9A4C2E71@4ce972b38cf925ec5d447546339ead57830ada7c`
- live MAIN lease: `MAIN-20260923T081600+0900-PRIMARY-CAND34-ARCHR1-R89-9A4C2E71@682ad984a1d9d3f820ee137e01fd0229724c771d`
- stable main: `ebed6abfa941c83b36d2e3bddd04e7c5fb0dbe9d`
- H7 R5 exact head: `research/main-h7-formal-r5-runtime-identity-r88-cycle12@2f30b93f8f3cf226ef55ed5af7e341089d2c3c80`
- R5 exact-head preidentity run: `35794233687` = success
- R5 exact-head generic CI: `35794233612` = success
- candidate #34 branch: `research/main-cand34-assembly-route-architecture-r89-cycle1@ebed6abfa941c83b36d2e3bddd04e7c5fb0dbe9d`
- Utility mailbox: `ops/utility-orchestrator-requests@b18a1d76c05efbbfacb9618ac1ca1e9941b17555`
