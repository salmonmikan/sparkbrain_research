# SparkBrain Methodology Calibration Audit — R72

- schema_version: `2`
- generation_id: `METHCAL-20260923T042037+0900-R72-21A0646D`
- produced_at: `2026-09-23T04:20:37+09:00`
- authority_scope: `METHODOLOGY_ADVISORY_ONLY`
- overall_classification: `MIXED_CALIBRATION`
- material_change: `true`
- supersedes: `METHCAL-20260923T031749+0900-R71-B73D5A2C`

## Material change

R71's R4 durability blocker is materially improved: the exact one-shot R4 scientific runtime lock and manifest are now durably committed at `f84ba35e17c24fcdcfa6920ef23972d4a81567a5` and exact-head preidentity / generic CI are green. Fresh FORMAL authority remains zero.

The new calibration gap is narrower but important: **durably binding the exact lock bytes is not the same as functionally realizing and validating the protected scientific runtime represented by that lock.** The current R4 preidentity workflow installs `.[dev,learned,spiking]` editable into the validation runner and then validates the committed lock/manifest bytes plus inherited synthetic tests. It does not create a clean isolated environment from `scientific-runtime.lock` and execute the claim-capable protected source/executor there. Therefore current green preidentity CI proves static binding/integrity of the lock artifacts and inherited non-result code, but not yet exact-runtime functional reproducibility.

Evidence Analyst R85 independently identifies the adjacent source/executor gap and authorizes only a bounded `NON_RESULT` cycle-11 implementation/binding closure under already-frozen R3/R4 scientific semantics. The current cycle-11 branch exists at the same `f84ba35e17c24fcdcfa6920ef23972d4a81567a5` and no R3/R4 protected result runner is yet present. This continuation is calibrated: it has specific prospective information gain and remains before identity/evaluation/result-bearing execution.

The calibrated next boundary is: materialize the already-frozen protected executor semantics, realize the committed scientific lock in a clean non-result environment without resolver shopping, assert the frozen runtime/resource conditions there, run only synthetic/non-protected integrity checks, bind final runner/scorer/preserver/source blobs externally, then STOP for fresh Analyst review. If any claim, estimand, comparator, intervention, seed/exclusion, threshold/tolerance, resource/privilege contract, hypothesis, falsifier, success criterion, or decision rule must change, this is not implementation closure and must return to explicit versioned SCIENCE_AFFECTING reassessment.

PF-R1 exact-byte preservation remains separately blocked by the existing request-level Utility authority divergence; no duplicate request is warranted and no rerun/reconstruction is allowed.

## Input generations / authoritative refs

- prior methodology: `METHCAL-20260923T031749+0900-R71-B73D5A2C` @ `ops/methodology-calibration-audit@c49c2077c034e077a214e224eea928daf89edeb4`
- human directive: `HUMAN-20260922-005` — process directive only, not scientific evidence
- Control: `CTRL-20260922T235000+0900-R37-7A3E9C51` @ `ops/control-brain-handoff@23676a55c9473d1cfd22d982ffffd5d2ba18b557`
- Evidence Analyst: `EVA-20260923T035826+0900-R85-1C7E4A92` @ `ops/evidence-analyst-handoff@8c0564d9acad7b074de66c4f902134a58d31e3f6`
- Utility reconciliation: `UTILITY-20260923T023100+0900-AUTO-PFR1-AUTH-CONSISTENCY-COMPLETED-4E7A2C91` @ `ops/utility-orchestrator-requests@b18a1d76c05efbbfacb9618ac1ca1e9941b17555`
- stable main: `ebed6abfa941c83b36d2e3bddd04e7c5fb0dbe9d`
- H7 R4 exact head: `research/main-h7-formal-r4-source-executor-r85-cycle11@f84ba35e17c24fcdcfa6920ef23972d4a81567a5`
- committed R4 lock SHA-256: `d2053e13bad626b70e044b0da384f547b8e85718343b57d0ac653df01eec0583`
- committed R4 manifest SHA-256: `cf4fd0a568b501242ec65cacc8741d1a743a2f0a911873c77a8581a63c855223`
- committed R4 versions SHA-256: `91fb0b9f7e7a18d855ba8e605bc4533fbf9793a595948ae4ff2dd55859c18575`
- exact-head R4 preidentity workflow: `35768073646` — success
- PF-R1 development artifact remains workflow `35695286240` / artifact `10680620448` / archive digest `sha256:db43c7557b55760ddd182afe836405c2e2f261c60261504723182ef9240e908d`
- annotated `evidence/*`: five unchanged refs; tag-form `formal/*`, `sealed/*`, `freeze/*`: empty

## Gate-by-gate classification

| Material gate | Classification | Finding |
|---|---|---|
| hard FORMAL integrity floor | `KEEP` | No fresh H7 identity/result/evidence; one-way authority remains zero. |
| orthogonal development-phase axis | `KEEP` | H7 remains RESULT_EXPOSED development; cycle-11 is non-result implementation closure only. |
| full OPEN→RESULT_EXPOSED→CONSUMED_ONE_WAY live path | `INSUFFICIENT_EVIDENCE` | Fresh post-directive consumed FORMAL remains unobserved. |
| RESULT_EXPOSED same-object SCIENCE_INVARIANT_REPAIR | `KEEP` | Frozen-semantic executor/path/hash/runtime plumbing may be repaired without changing science. |
| post-result SCIENCE_AFFECTING change → explicit version/fresh successor | `KEEP` | R3→R4 discipline remains the correct boundary. |
| exact R4 lock/manifest durable capture before identity | `KEEP` | Previously open R71 blocker is now durably closed at exact head. |
| exact scientific runtime functional realization from committed lock | `TIGHTEN` | Static hash/manifest validation is not yet an isolated locked-environment execution proof. |
| dev/editable validation environment as proof of protected runtime equivalence | `CLARIFY` | It validates artifacts/code but is not sufficient by itself for the scientific runtime identity. |
| protected result executor materialization under already-frozen semantics | `KEEP` | R85 cycle-11 scope is bounded, non-result and science-invariant if no scientific choice changes. |
| final claim-capable source/runner/scorer/preserver blob binding | `TIGHTEN` | Exact protected executor is still absent/unbound. |
| frozen deterministic/resource runtime assertions in protected executor | `TIGHTEN` | Assert already-frozen runtime conditions in the exact locked environment before identity. |
| resolver/materializer shopping for preferred runtime bytes | `TIGHTEN` | Reproduce from the committed lock; do not regenerate until a convenient environment appears. |
| request-scoped Utility execution authority machine-resolvable | `TIGHTEN` | PF-R1 preservation machine authority remains unresolved. |
| Utility default-deny without matching decision/assignment | `KEEP` | No prose-derived self-authorization. |
| durable RESULT_EXPOSED PF-R1 bytes | `TIGHTEN` | Existing bytes remain unpreserved; rerun is not an acceptable substitute. |
| cycle 3 as automatic terminal cap | `RELAX` | Cycle 11 remains justified by explicit preidentity information gain. |
| cycle 3 mandatory reassessment | `KEEP` | Continuation is bounded and prospectively motivated. |
| raw-before-score as file ordering only | `CLARIFY` | Semantic prediction-only raw remains required. |
| literal target-blind prediction raw | `TIGHTEN` | R68 gate remains live. |
| scorer-side recomputation after immutable preserve | `TIGHTEN` | Still required before one-way entry. |
| target-sidecar independence | `TIGHTEN` | Still required before one-way entry. |
| preserve-before-read / immutable FORMAL evidence path | `TIGHTEN` | Fresh H7 end-to-end path remains unproven. |
| concealed fresh evaluation surface | `TIGHTEN` | R2 remains quarantined; concealed post-binding evaluation remains required. |
| development reruns ≠ independent evidence | `KEEP` | Preidentity/tooling checks carry zero confirmatory credit. |
| repeated result-bearing PRE_FORMAL live stress case | `INSUFFICIENT_EVIDENCE` | Still not adequately observed. |
| PRE_FORMAL as genuine development | `KEEP` | Iteration remains allowed without confirmatory credit. |
| preformal_eligible distinct from READY | `KEEP` | Distinction remains live. |
| READY = informative next test, not prior success | `KEEP` | `HIDDEN_SECOND_FORMAL_GATE=false`. |
| current-object claim ceiling | `KEEP` | Ceiling remains object-specific. |
| same-object SYSTEM→MECHANISM uplift ban | `KEEP` | No laundering observed. |
| fresh terminal SYSTEM successor discipline | `KEEP` | Candidate 33 remains SYSTEM-only/current-object terminal. |
| fresh SYSTEM→MECHANISM successor | `INSUFFICIENT_EVIDENCE` | Genuine live case remains absent. |
| TERMINAL_FOR_CURRENT_OBJECT scope | `KEEP` | Topic death is not implied. |
| classification completeness | `KEEP` | R85 retains the canonical population fully classified. |
| MAIN MECHANISM priority | `KEEP` | H7 remains the viable MECHANISM lane. |
| prospective SYSTEM-priority exception | `KEEP` | No improper comparable-MECHANISM override observed. |
| genuine SYSTEM-over-MECHANISM exception | `INSUFFICIENT_EVIDENCE` | Still unobserved. |
| theory-backward quality floor / no-target liveness | `KEEP` | No candidate manufacturing to preserve activity. |
| protected/adaptive-evaluation validity | `TIGHTEN` | Concealed-evaluation/raw/runtime execution gates remain unresolved. |

## Mandatory questions

1. Development-phase semantics remain coherent through R4 preidentity work; a fresh `CONSUMED_ONE_WAY` post-directive case is still unobserved.
2. Cycle 3 is not being mistaken for a hard cap. Cycle 11 has a distinct prospective question: whether the frozen claim-capable source/executor can be materialized and validated under the fixed R4 runtime without changing science.
3. Repair classification remains calibrated. Implementing the already-fixed protected execution path and exact locked-runtime plumbing is science-invariant; changing any scientific/resource meaning is science-affecting and must version.
4. Development reruns/retunes are not being laundered into independent evidence. Current workflow/lock checks have confirmatory credit zero.
5. RESULT_EXPOSED semantic lineage is preserved, but PF-R1 exact-byte durability remains unresolved because request-level Utility authority is still absent.
6. FORMAL one-way integrity is unchanged: no H7 FORMAL identity, STARTED, evaluation commitment/reveal, protected result, official score or scientific evidence exists.
7. Candidate 33 remains a legitimate fresh SYSTEM successor without MECHANISM uplift; a genuine fresh SYSTEM→MECHANISM successor remains unobserved.
8. PRE_FORMAL remains development, not hidden FORMAL; READY remains development readiness.
9. Terminal semantics remain calibrated, but mechanism supply remains thin. Cycle-11 source/executor closure should not be used to manufacture additional candidates.
10. PASS remains realistically reachable without weakening evidence standards; exact locked-runtime realization and exact executor binding are engineering/integrity closures, not new success criteria.

## Risk / development calibration

False-positive risk: `MODERATE_WATCH`. The new local risk is treating static lock/manifest validation in a dev/editable environment as if it proved the exact scientific runtime will execute the protected source identically. Existing concealed-evaluation and semantic raw/preserve risks also remain.

False-negative/opportunity-cost risk: `MODERATE_WATCH`. Mechanism supply remains one H7 lineage, and excessive insistence on irrelevant dev-tool reproducibility would be overconservative; however, one clean realization of the already-committed scientific lock is directly relevant and not excess conservatism.

Moving-goalpost/rescue risk: `LOW_TO_MODERATE_WATCH_IMPROVING`. The runtime package set is already frozen and durably committed, the next work is implementation under that frozen surface, and R85 explicitly requires versioning if scientific choices change. Over-terminalization remains `LOW_TO_MODERATE_WATCH_IMPROVING`.

Development iteration remains calibrated: cycle-11 is allowed because it asks a new, prospectively stated implementation-integrity question. Its output must remain non-confirmatory and cannot erase or upgrade prior development observations.

## Funnel / mechanism supply / PRE_FORMAL

R85 retains the canonical programme at `33 = MECHANISM 13 / SYSTEM 20`, classification complete; lifecycle remains one active H7 lineage with 32 `TERMINAL_FOR_CURRENT_OBJECT`; canonical `CONSUMED_ONE_WAY=0`, PRE_FORMAL eligible/READY `1/1`, fresh FORMAL authority `0`, historical official consumed identities `7`. Candidate 7/H7 remains the active MECHANISM formalization lineage; candidate 33 remains SYSTEM / RESULT_EXPOSED / current-object terminal.

Mechanism supply remains `ONE_ACTIVE_MECHANISM_FORMALIZATION_LINEAGE; QUALITY_FLOOR_INTACT_BUT_MECHANISM_SUPPLY_THIN`.

PRE_FORMAL remains a nonconfirmatory development surface. PF-R1 contributes confirmatory count zero and has not been rerun/rescored. `HIDDEN_SECOND_FORMAL_GATE=false`.

## PASS reachability / prospective recommendations

PASS remains `REALISTIC_CONDITIONAL` without relaxing any evidence standard.

1. Preserve the committed R4 lock/manifest exactly; do not rematerialize or shop resolver outcomes.
2. Materialize the already-frozen protected source/executor only under R85's non-result scope; no scientific choice changes.
3. Before one-way identity, create one clean isolated non-result environment from the committed scientific lock/manifest, without dependency resolution or editable scientific install, and run the synthetic/integrity executor path there.
4. In that environment assert the already-frozen runtime/resource conditions, including deterministic-mode/resource settings that the protected executor is expected to enforce; bind the observed runtime manifest separately from dev tooling.
5. Bind final claim-capable source SHA and exact runner/scorer/preserver/source blobs via a non-self-referential control/identity-input record.
6. Preserve literal prediction-only raw → immutable preserve → target-side scoring, target-sidecar independence, concealed evaluation, no-clobber/collision and exact source/input/evaluator gates.
7. Separately resolve PF-R1's existing request-scoped Utility assignment and preserve the existing bytes only; no rerun/reconstruction/rescoring.
8. Even after all preidentity closures are green, STOP and require a fresh Analyst generation before one-way FORMAL authority.

## Utility / hard floor / confidence

No new Utility request. Existing `UTIL-20260922-1648-PFR1-DEVELOPMENT-PROVENANCE-PRESERVE` remains unresolved at request-level machine authority; the auditor does not dispatch or mutate Utility authority.

Hard floor: `CONFIRMED / DO NOT RELAX`.

Confidence: `HIGH` that R4 lock/manifest durability is now closed, `HIGH` that no H7 FORMAL consumption occurred, `HIGH` that no R3/R4 protected result runner is yet present at the current head, `MEDIUM_HIGH` that exact scientific-runtime functional realization remains unproven because the current preidentity workflow executes inherited tests after an editable development install rather than from the committed lock. Fresh consumed-FORMAL end-to-end and fresh SYSTEM→MECHANISM successor remain `UNOBSERVED`.

Questions for future Control/Analyst: require a clean no-resolution realization of the committed R4 scientific lock before identity; require the cycle-11 executor to assert already-frozen runtime/resource conditions and preserve R3 prediction-only/preserve-before-score semantics; keep the cycle-11 STOP-after-non-result boundary; and make the existing PF-R1 Utility authorization machine-resolvable without rerun or duplicate request.
