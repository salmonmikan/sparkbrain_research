# SparkBrain Methodology Calibration Audit — R75

- schema_version: `2`
- generation_id: `METHCAL-20260923T073000+0900-R75-C8D41B6E`
- produced_at: `2026-09-23T07:30:00+09:00`
- authority_scope: `METHODOLOGY_ADVISORY_ONLY`
- supersedes_generation_id: `METHCAL-20260923T062034+0900-R74-A3F7C912`
- material_change: `true`
- audit_result: `MATERIAL_CALIBRATION_UPDATE`
- overall_classification: `MIXED_CALIBRATION`

## Executive decision

A material calibration update is present on two independent axes.

First, R74's unresolved H7 R4 runtime mismatch was diagnosed exactly and handled correctly under the HUMAN-005 development policy. R4 is preserved unchanged as `RESULT_EXPOSED_DEVELOPMENT`; because R4 itself classified the installed torch `RECORD` hash and hosted-runner metadata as scientific runtime, reclassifying them in place would have been science-affecting. Evidence Analyst R88 instead authorized explicit revision `H7-FORMAL-R5-CONTENT-ADDRESSED-RUNTIME-IDENTITY-AND-PROVISIONING-PROVENANCE-SPLIT`. R5 preserves byte-identical R4 package/version/wheel-lock bytes and all non-resource scientific fields, while prospectively classifying installer-generated `RECORD` and outer hosted-runner image/kernel metadata as provisioning provenance. This is a strong positive live case of RESULT_EXPOSED development versioning rather than rescue laundering.

The authoritative R5 branch is `research/main-h7-formal-r5-runtime-identity-r88-cycle12@a8f4b4aed20dfa11a31aa7a3cfdff6635cb37e3b`. Dedicated preidentity workflow `35792249736` completed successfully: R88 authority and inherited R4 bytes were verified, a clean environment was constructed from the byte-identical R4 hash lock, the R5 content-addressed runtime/non-result executor validation passed, and absence of one-way material was asserted. This remains NON_EVIDENTIARY. Generic CI `35792249682` fails only at Ruff import ordering in the new R5 preflight; this is a confirmed `SCIENCE_INVARIANT_REPAIR` and does not reopen scientific/runtime criteria.

Second, mechanism supply materially improved. Fresh literature R34 identified a distinct identifiability residual: a finite intervention family generally supports an interventional equivalence class, not automatically a unique causal route graph. SUB R87 independently applied that reduction to the stable v0.5 Assembly lineage, not to the live H7 object, and proposed a fresh temporal-route identifiability question with edge/timing interventions, ordinary reductions, and an explicit equivalence-class falsifier. Evidence Analyst R88 admitted canonical candidate #34 `CAND-34-ASSEMBLY-TEMPORAL-ROUTE-IDENTIFIABILITY` as `MECHANISM / OPEN_DEVELOPMENT / DISCOVERY-R1 / PRE_FORMAL eligible / NOT_READY / QUEUED_FOR_SUB_DISCOVERY`. Prior Assembly objects are not reopened or rewritten. This is legitimate successor generation, not candidate manufacture or rescue laundering.

## Gate-by-gate calibration

| Gate | Classification |
|---|---|
| hard FORMAL integrity floor | `KEEP` |
| development-phase orthogonal axis | `KEEP` |
| fresh OPEN→RESULT_EXPOSED→CONSUMED_ONE_WAY end-to-end | `INSUFFICIENT_EVIDENCE` |
| RESULT_EXPOSED same-object SCIENCE_INVARIANT_REPAIR | `KEEP` |
| post-result SCIENCE_AFFECTING change → explicit version/fresh successor | `KEEP` |
| R4 preserved unchanged after runtime-binding diagnosis | `KEEP` |
| R4→R5 explicit science-affecting revision | `KEEP` |
| R5 byte-identical R4 package/version/wheel-lock inheritance | `KEEP` |
| installed torch RECORD reclassified prospectively as provisioning provenance | `KEEP` |
| hosted-runner image/kernel metadata scientific-vs-provisioning status | `SPLIT_BY_CLAIM_TYPE` |
| claim-capable native/runtime dimensions omitted from scientific identity | `TIGHTEN` |
| dedicated R5 content-addressed non-result realization | `KEEP` |
| generic R5 import-order lint repair | `KEEP` |
| silent runtime/hash normalization or in-place R4 rebinding | `TIGHTEN` |
| resolver/package/runner-image shopping | `TIGHTEN` |
| final exact source/runner/scorer/preserver/runtime/input binding | `TIGHTEN` |
| cycle 3 as automatic terminal cap | `RELAX` |
| cycle 3 as mandatory prospective reassessment | `KEEP` |
| cycle 12 continuation with stated information gain | `KEEP` |
| development rerun/retune non-independent evidence accounting | `KEEP` |
| repeated result-bearing PRE_FORMAL stress case | `INSUFFICIENT_EVIDENCE` |
| durable RESULT_EXPOSED PF-R1 bytes | `TIGHTEN` |
| Utility default-deny without matching machine authority | `KEEP` |
| raw-before-score as file-order only | `CLARIFY` |
| literal target-blind prediction raw | `TIGHTEN` |
| post-preserve scorer-side recomputation | `TIGHTEN` |
| target-sidecar independence | `TIGHTEN` |
| preserve-before-read / immutable FORMAL evidence | `TIGHTEN` |
| fresh concealed post-binding evaluation surface | `TIGHTEN` |
| PRE_FORMAL as genuine development | `KEEP` |
| preformal_eligible distinct from READY | `KEEP` |
| READY = informative next test, not prior success | `KEEP` |
| HIDDEN_SECOND_FORMAL_GATE | `KEEP` — false |
| current-object claim ceiling | `KEEP` |
| same-object SYSTEM→MECHANISM uplift ban | `KEEP` |
| fresh MECHANISM successor distinctness/non-rescue | `KEEP` |
| fresh SYSTEM→MECHANISM successor specifically | `INSUFFICIENT_EVIDENCE` |
| TERMINAL_FOR_CURRENT_OBJECT topic scope | `KEEP` |
| candidate #34 admission without reopening #23/#24/#26 | `KEEP` |
| classification completeness | `KEEP` |
| MAIN MECHANISM priority | `KEEP` |
| prospective SYSTEM-priority exception | `KEEP` |
| genuine SYSTEM-over-comparable-MECHANISM exception | `INSUFFICIENT_EVIDENCE` |
| theory-backward quality floor | `KEEP` |
| NO_COHERENT_MECHANISM_TARGET liveness semantics | `KEEP` |
| protected/adaptive-evaluation validity | `TIGHTEN` |

## Mandatory questions

1. **Development-phase semantics end-to-end?** Consistent through preidentity. R4 result exposure followed by an explicitly versioned science-affecting R5 revision is the clearest positive case so far. A fresh post-HUMAN-005 `CONSUMED_ONE_WAY` completion remains unobserved.
2. **Cycle 3 mistaken for a hard cap?** No. Cycle 12 had explicit information gain: determine whether the byte-identical locked scientific runtime can be realized under the prospectively split identity/provisioning contract. The dedicated non-result preidentity run succeeded.
3. **Repair classification correct?** Yes in current live cases. R4 resource-identity reclassification was treated as science-affecting and versioned into R5. Generic R5 import ordering and the previously diagnosed test error-message precedence are science-invariant because they do not alter validator/scientific semantics.
4. **Development rerun/retune laundered into independent evidence?** No. R4/R5 preidentity workflows and synthetic probes remain non-evidentiary with confirmatory credit zero. Repeated result-bearing PRE_FORMAL remains insufficiently stress-tested.
5. **RESULT_EXPOSED prior results preserved when revised?** Yes for the H7 R4→R5 lineage: R4 is preserved and R5 is additive. PF-R1 exact-byte durable preservation remains open due Utility authority wiring, so programme-wide closure is incomplete.
6. **FORMAL one-way integrity unchanged?** Yes. Stable main and evidence tags remain unchanged; tag-form `formal/*`, `sealed/*`, and `freeze/*` are empty; no H7 FORMAL identity, STARTED, protected result execution, official score, scientific preserve/evidence ref, or consumed result exists.
7. **Legitimate fresh SYSTEM→MECHANISM successors suppressed or manufactured?** No manufacture is observed. Candidate #34 is a legitimate fresh MECHANISM successor/admission based on an independent identifiability residual and fresh intervention/falsifier contract, but it is not established here as the specific SYSTEM→MECHANISM transition case requested by that guard. That narrower case remains unobserved.
8. **PRE_FORMAL behaving as development rather than hidden FORMAL?** Yes. Candidate #34 is already `preformal_eligible=true` while `NOT_READY`, directly demonstrating eligibility does not require prior success. H7 remains READY while FORMAL authority is still zero. `HIDDEN_SECOND_FORMAL_GATE=false`.
9. **Terminal semantics and candidate-supply controls calibrated?** Improved. Prior current-object terminalizations did not suppress the fresh Assembly identifiability question. Canonical supply is now two viable MECHANISM lines: H7 active and #34 queued discovery. Quality floor remains intact.
10. **PASS realistically reachable without weakening standards?** Yes, `REALISTIC_CONDITIONAL`. R5 materially improves reachability by closing the content-addressed runtime realization, but exact final-head binding after invariant lint repair, PF-R1 preservation, raw/preserve/scorer/held-out gates, collision/no-clobber checks, and fresh one-way Analyst authorization remain mandatory.

## False-positive / false-negative calibration

- false-positive risk: `MODERATE_WATCH` — R5 correctly removes known installation/transport artifacts from equality gates, but claim-capable host/native runtime properties must not disappear merely because they are inconvenient to reproduce. Target-blind raw, concealed evaluation, preserve-before-read, and final exact-binding gates remain open.
- false-negative/opportunity-cost risk: `LOW_TO_MODERATE_WATCH_IMPROVING` — R5 reduces artificial failures caused by installer/transport metadata, and candidate #34 improves mechanism supply. PF-R1 authority friction and remaining FORMAL integrity gates still impose real throughput cost.
- moving-goalpost/rescue risk: `LOW_TO_MODERATE_WATCH_IMPROVING` — R4 was not rewritten; R5 is explicit, outcome-independent in its declared resource reclassification, and preserves non-resource science and exact package bytes.
- over-terminalization risk: `LOW_WATCH_IMPROVING` — candidate #34 is direct evidence that current-object terminal states are not being treated as topic death when an independently motivated residual appears.

## Development-iteration calibration

H7 remains `MECHANISM / RESULT_EXPOSED_DEVELOPMENT / ACTIVE / READY`. R5 is a strong positive implementation of HUMAN-005: a meaningful development result/runtime mismatch was exposed; the science-affecting resource-identity change was not silently repaired in R4; R4 was preserved; a new version was prospectively authorized; and the first R5 realization remained non-evidentiary and stopped before every one-way action.

The dedicated R5 workflow's success is development closure only. It must not be converted into scientific evidence or identity authority. Generic CI's only current defect is import ordering in the new preflight, so same-R5 lint repair is permitted. Because that repair changes the branch SHA, final source/component binding must be refreshed at the exact post-repair head before any one-way authorization.

## Runtime/package calibration

R5 cleanly separates content-addressed scientific identity from provisioning provenance. Exact package lock, manifest, versions, wheel hashes, Python version/implementation, torch distribution/module/git/CUDA semantic versions, CPU/device/thread/determinism constraints, and bound scientific component blobs remain scientific identity. Installed torch `RECORD`, hosted image metadata, and outer kernel/platform observations are provenance-only under R5.

This split is calibrated only so far as omitted provisioning dimensions are genuinely non-claim-capable. If a native host/ABI/CPU/kernel property can alter the protected scientific output despite the frozen package and deterministic constraints, that property belongs in scientific identity or must be covered by a prospectively defined equivalence/fingerprint gate. Do not discover such a dependency after protected results and then redefine it in place. Any further science-affecting resource-identity change requires a fresh version preserving R5.

## Claim-type / successor findings

Candidate #34 is a legitimate fresh MECHANISM admission. Its motivation is not 'the prior candidate failed, try a stronger version'; it is the distinct identifiability residual that coarse assembly/unit lesions can leave multiple temporal route structures interventionally equivalent. The proposed successor uses a new temporal/lagged causal object, a prospectively bounded edge/timing separating family, matched random/sham and prior-lesion reductions, and an explicit falsifier that collapses claims to the equivalence class when alternatives are not separated.

This does not retroactively upgrade prior Assembly objects, does not reopen #23/#24/#26, and is intentionally `NOT_READY` pending Discovery R1 contract formation. That is calibrated successor generation rather than rescue laundering.

Candidate 33 remains a SYSTEM current-object terminal example; no same-object SYSTEM→MECHANISM uplift is observed.

## Funnel observability / mechanism supply

Canonical R88 population is `34 = 14 MECHANISM / 20 SYSTEM`, lifecycle `ACTIVE 1 / QUEUED 1 / NONTERMINAL_HOLD 0 / TERMINAL_FOR_CURRENT_OBJECT 32`, development phases `OPEN 3 / RESULT_EXPOSED 31 / canonical CONSUMED_ONE_WAY 0`, PRE_FORMAL eligible/READY `2/1`, viable MECHANISM `2`, fresh FORMAL authority `0`, official historical consumed identities `7`, classification completeness `34/34`.

Mechanism-supply health improves to `TWO_VIABLE_MECHANISM_LINEAGES_ONE_ACTIVE_FORMALIZATION_ONE_QUEUED_DISCOVERY_QUALITY_FLOOR_INTACT`.

## Preformal calibration / pass reachability

PF-R1 remains one nonconfirmatory development observation with confirmatory count zero. `HIDDEN_SECOND_FORMAL_GATE=false`.

Candidate #34 is particularly informative for calibration: `preformal_eligible=true` while `NOT_READY`, so eligibility is clearly a development-admission property rather than hidden confirmatory success.

PASS remains `REALISTIC_CONDITIONAL`. Prospective sequence: apply only the confirmed import-order science-invariant repair; rebind/review the exact final R5 head; verify all claim-capable native/runtime dimensions are either frozen or prospectively justified as equivalent/provenance-only; resolve PF-R1 exact-byte preservation authority; close prediction-only raw → immutable preserve → post-preserve target-side scoring, target-sidecar independence, concealed evaluation, collision/no-clobber and final component/input bindings; then STOP for fresh Evidence Analyst one-way authorization. No evidence standard needs weakening.

## Utility request

No new Utility request is created. Existing `UTIL-20260922-1648-PFR1-DEVELOPMENT-PROVENANCE-PRESERVE` remains the sole request and execution remains fail-closed until matching schema-v2 machine authority exists. Do not duplicate, rerun, reconstruct, regenerate, or rescore PF-R1.

## Prospective recommendation

Keep R5 preidentity-only. Permit the import-order lint fix as science-invariant, then require exact-head review/binding rather than treating the already-green dedicated run on the prior SHA as identity authority. Preserve R4 and R5 revision history.

For candidate #34, keep Discovery R1 genuinely developmental: first define the temporal causal object, separating intervention family, ordinary reductions, reachability, and equivalence-class falsifier. Do not require it to have already beaten those reductions in order to become READY, and do not count discovery observations as independent confirmatory evidence.

Do not create successors merely to increase throughput. The presence of #34 means candidate-starvation pressure has eased; quality floor should dominate quantity.

## Hard-floor confirmation / confidence

`CONFIRMED_DO_NOT_RELAX`.

Confidence: FORMAL non-consumption `HIGH`; R4 exact mismatch diagnosis `HIGH`; R4→R5 explicit versioning `HIGH`; R5 dedicated content-addressed preidentity realization `HIGH`; generic CI defect as import-order-only `HIGH`; candidate #34 independent motivation and fresh-contract structure `HIGH`; claim-capable host/native runtime sufficiency under the R5 split `MEDIUM_HIGH`; fresh consumed-FORMAL end-to-end `UNOBSERVED`; fresh specifically SYSTEM→MECHANISM successor `UNOBSERVED`.

## Questions for Control / Analyst

- After the invariant import-order repair, will the exact post-repair R5 head be independently reviewed and rebound before any identity authorization?
- Which native host/ABI/CPU properties, if any, can alter claim-bearing output despite R5's exact wheel/Python/torch/device/thread/determinism identity, and how are those properties prospectively covered?
- Will R5 dedicated preidentity success remain development-only and still STOP for a fresh Analyst one-way authorization?
- Can PF-R1 exact-byte preservation receive matching schema-v2 machine authority without rerun or reconstruction?
- For #34, can Discovery R1 define a bounded separating intervention family and explicit temporal causal object without requiring prior route-identification success as a READY prerequisite?

## Authoritative refs used

- prior Methodology: `METHCAL-20260923T062034+0900-R74-A3F7C912@077d282bf9a3d3f730550ad23edda4835f73c2d8`
- Human directive: `HUMAN-20260922-005` (process directive, not scientific evidence)
- Control: `CTRL-20260923T055000+0900-R39-C31A7D52@c93f870e47bdb5b1613475e079c0a6dee2aa1753`
- Evidence Analyst: `EVA-20260923T065834+0900-R88-D5A7C219@3341ad00ade796d9a3b80dd61c7386fc8b3e07b7`
- SUB: `SUB-20260923T064336+0900-QFD-ASSEMBLY-INTERVENTIONAL-EQUIV-R87-8F4C21A7` from `ops/orchestrator-run-report`
- Literature: `LIT-20260923T063434+0900-R34-INTERVENTIONAL-EQUIVALENCE-8F4C21A7@027fa9fc45648269c2ebe0ae6408162d155fe840`
- MAIN lease: `MAIN-20260923T071545+0900-PRIMARY-H7-FORMALR5-C12-R88-PREIDENTITY-3A7C5D21@74c5500f49f2d867115496f7997fe2d4e4d447b1`
- stable main: `ebed6abfa941c83b36d2e3bddd04e7c5fb0dbe9d`
- evidence tags: five authoritative annotated `evidence/*` refs; tag-form `formal/*`, `sealed/*`, `freeze/*` empty
- R4 history: `research/main-h7-formal-r4-source-executor-r85-cycle11@02382fbc7d3838159598015e488c6ce49ac34efc`
- R5 current: `research/main-h7-formal-r5-runtime-identity-r88-cycle12@a8f4b4aed20dfa11a31aa7a3cfdff6635cb37e3b`
- R5 dedicated preidentity workflow: `35792249736` = success
- R5 generic CI: `35792249682` = lint-only failure on current head
