# SparkBrain Methodology Calibration Audit — R74

- schema_version: `2`
- generation_id: `METHCAL-20260923T062034+0900-R74-A3F7C912`
- produced_at: `2026-09-23T06:20:34+09:00`
- authority_scope: `METHODOLOGY_ADVISORY_ONLY`
- supersedes_generation_id: `METHCAL-20260923T051912+0900-R73-6B1E94D3`
- material_change: `true`
- audit_result: `MATERIAL_CALIBRATION_UPDATE`
- overall_classification: `MIXED_CALIBRATION`

## Executive decision

A material methodology-calibration update is present.

Since R73, authoritative H7 Cycle-11 moved from `f84ba35e17c24fcdcfa6920ef23972d4a81567a5` to `02382fbc7d3838159598015e488c6ce49ac34efc` (six commits ahead). The branch now contains the frozen protected-executor implementation, non-result preflight/tests, and an exact committed-lock realization workflow. Exact wheel installation succeeds, but the clean-runtime assertion fails before the synthetic protected-executor probe; generic CI now passes lint/readiness on Python 3.11/3.13 and fails at Test. These are NON_EVIDENTIARY preidentity observations only.

The development-iteration policy is working as intended: implementation, lint repair, diagnostic instrumentation, and bounded rerun remain permissible because no FORMAL identity/result has been consumed and no scientific meaning has been changed. The remaining defects are not yet automatically classifiable as science-invariant. In particular, a runtime/hash assertion is not science-invariant merely because it is expressed as a hash check.

The new calibration target is to separate **scientific artifact/runtime identity** from **installation/host realization metadata** without weakening exact binding. R4 already pins the exact wheel artifacts by hash, while separately pinning `torch_distribution_record_sha256` and a GitHub-hosted `runner_image_version`. Current failures show that these dimensions can diverge. Do not silently normalize, delete, or rebind either field inside R4. First obtain exact diagnostics. If only installer/transport metadata differs while the prospectively fixed wheel artifacts and claim-capable scientific runtime semantics are unchanged, that distinction should be handled explicitly and prospectively; because R4 currently treats these fields as scientific runtime, any relaxation/reclassification requires a fresh versioned development revision.

A second material risk is **runner-image shopping**. R4 binds `runner_image_version=20260907.300.1`, but the workflow requests only the moving `ubuntu-24.04` hosted-runner label. Repeated dispatches until a matching hosted image happens to be allocated would be an outcome-independent but provenance-undermining form of environment shopping. Exact runtime integrity should be achieved through a reproducibly provisioned substrate when the host image itself is scientifically material, not by repeated retries for a transient hosted image.

## Gate-by-gate calibration

| Gate | Classification |
|---|---|
| hard FORMAL integrity floor | `KEEP` |
| development-phase orthogonal axis | `KEEP` |
| fresh OPEN→RESULT_EXPOSED→CONSUMED_ONE_WAY end-to-end | `INSUFFICIENT_EVIDENCE` |
| RESULT_EXPOSED same-object SCIENCE_INVARIANT_REPAIR | `KEEP` |
| post-result SCIENCE_AFFECTING change → explicit version/fresh successor | `KEEP` |
| cycle 3 as automatic terminal cap | `RELAX` |
| cycle 3 as mandatory prospective reassessment | `KEEP` |
| frozen protected-executor source materialization under fixed semantics | `KEEP` |
| clean exact-lock package installation | `KEEP` |
| exact scientific-runtime functional realization | `TIGHTEN` |
| generic Test failure classification before repair | `INSUFFICIENT_EVIDENCE` |
| diagnostic-only instrumentation after failed preidentity validation | `KEEP` |
| silent expected-hash/runtime rebind | `TIGHTEN` |
| exact wheel artifact hashes as package identity | `KEEP` |
| installed-distribution RECORD hash as scientific-vs-installation provenance | `CLARIFY` |
| hosted-runner image metadata as scientific-vs-transport provenance | `SPLIT_BY_CLAIM_TYPE` |
| rerun until desired hosted image/runtime happens to appear | `TIGHTEN` |
| reproducibly provisioned substrate when host image is scientifically material | `TIGHTEN` |
| final claim-capable source/runner/scorer/preserver binding | `TIGHTEN` |
| frozen deterministic/resource/runtime assertions | `TIGHTEN` |
| PF-R1 Utility request-level machine authority | `TIGHTEN` |
| Utility default-deny without matching decision/assignment | `KEEP` |
| durable RESULT_EXPOSED development bytes | `TIGHTEN` |
| raw-before-score as file-order only | `CLARIFY` |
| literal target-blind prediction raw | `TIGHTEN` |
| post-preserve scorer-side recomputation | `TIGHTEN` |
| target-sidecar independence | `TIGHTEN` |
| preserve-before-read / immutable FORMAL evidence | `TIGHTEN` |
| fresh concealed post-binding evaluation surface | `TIGHTEN` |
| development rerun/retune non-independent accounting | `KEEP` |
| repeated result-bearing PRE_FORMAL stress case | `INSUFFICIENT_EVIDENCE` |
| PRE_FORMAL as genuine development | `KEEP` |
| preformal_eligible distinct from READY | `KEEP` |
| READY = informative next test, not prior success | `KEEP` |
| HIDDEN_SECOND_FORMAL_GATE | `KEEP` — false |
| current-object claim ceiling | `KEEP` |
| same-object SYSTEM→MECHANISM uplift ban | `KEEP` |
| fresh SYSTEM successor distinctness/non-rescue | `KEEP` |
| fresh SYSTEM→MECHANISM successor admission | `INSUFFICIENT_EVIDENCE` |
| TERMINAL_FOR_CURRENT_OBJECT topic scope | `KEEP` |
| classification completeness | `KEEP` |
| MAIN MECHANISM priority | `KEEP` |
| prospective SYSTEM-priority exception | `KEEP` |
| genuine SYSTEM-over-comparable-MECHANISM exception | `INSUFFICIENT_EVIDENCE` |
| theory-backward quality floor | `KEEP` |
| NO_COHERENT_MECHANISM_TARGET liveness semantics | `KEEP` |
| protected/adaptive-evaluation validity | `TIGHTEN` |

## Mandatory questions

1. **Development-phase semantics end-to-end?** Consistent through current preidentity work. Cycle-11 implementation and diagnostic retries remain NON_EVIDENTIARY; a fresh post-HUMAN-005 `CONSUMED_ONE_WAY` completion remains unobserved.
2. **Cycle 3 mistaken for a hard cap?** No. Cycle 11 has distinct prospective information gain: executor/source reachability and exact-runtime realization.
3. **Repair classification correct?** Confirmed lint-format and diagnostic-reporting changes are science-invariant. The remaining generic-Test and runtime-assertion failures are unclassified pending exact diagnostics; hash failure alone does not justify same-revision rebinding.
4. **Development rerun/retune laundered into independent evidence?** No. Current CI/preflight activity receives confirmatory credit zero. Repeated result-bearing PRE_FORMAL remains insufficiently stress-tested.
5. **RESULT_EXPOSED prior results preserved?** Revision lineage remains preserved. PF-R1 exact-byte durable preservation is still open because request-level Utility authority is unresolved.
6. **FORMAL one-way integrity unchanged?** Yes. Stable main/evidence are unchanged, fresh FORMAL authority is zero, and no FORMAL/sealed/freeze tag or H7 consumed result exists.
7. **Fresh SYSTEM→MECHANISM successors suppressed or manufactured?** No manufacturing observed; genuine fresh SYSTEM→MECHANISM successor remains unobserved.
8. **PRE_FORMAL behaving as development rather than hidden FORMAL?** Yes. READY remains development readiness, not prior comparator/falsifier victory.
9. **Terminal semantics and candidate supply calibrated?** Terminal semantics remain calibrated; mechanism supply remains thin at one active MECHANISM lineage.
10. **PASS reachable without weakening standards?** Yes, conditionally. Exact diagnostics and a reproducible scientific execution substrate are required; repeated runner/image shopping is not an acceptable route to PASS.

## False-positive / false-negative calibration

- false-positive risk: `MODERATE_WATCH` — a green retry on a transient hosted runner must not be mistaken for a reproducibly bound scientific runtime; target-blind raw, concealed evaluation, and preserve-before-read gates also remain open.
- false-negative/opportunity-cost risk: `MODERATE_WATCH` — requiring scientifically irrelevant installer/transport metadata to match exactly can create artificial version churn or make a valid claim surface practically unreachable. This should be distinguished prospectively without silently weakening R4.
- moving-goalpost/rescue risk: `LOW_TO_MODERATE_WATCH_IMPROVING` — current repairs are preidentity and diagnostic; expected values have not been rewritten to fit observed failures.
- over-terminalization risk: `LOW_TO_MODERATE_WATCH_IMPROVING`.

## Development-iteration calibration

H7 remains `MECHANISM / RESULT_EXPOSED_DEVELOPMENT / ACTIVE / READY`. Cycle 11 is a positive test of HUMAN-005 semantics: six implementation commits were allowed after prior development-result exposure because they target frozen execution closure, not scientific outcome rescue. The branch now materially exercises the exact lock and executor surface without consuming FORMAL evidence.

The current failed validations must remain part of durable development provenance. Same-R4 repair is allowed only after exact diagnostics show implementation/path/serialization/logging/hash-plumbing or already-fixed behavior. Changing the expected runtime artifact, package/resource/privilege contract, runner scientific semantics, metric/scorer, threshold/tolerance, comparator, seed/exclusion, intervention, hypothesis, falsifier, claim, or success criteria requires explicit versioning.

## Runtime/package calibration

The committed lock pins exact wheel hashes, including `torch==2.13.0` wheel SHA `60fcdcb2f3876e21146cb4524ef06397d727ca9ad5f020818547e25075fe3cb7`. The current workflow successfully installs the exact lock in a clean venv and then fails while asserting the frozen runtime/source surface before the synthetic executor probe.

R4 also requires `torch_distribution_record_sha256=407832ba9a2275aa30a1263f65dc9cc05fc04d74e81895e1ec8f74518eb45cf0`. Treat this as an unresolved provenance layer, not an excuse for silent rebinding. Exact wheel artifact identity, imported module/build identity, installer-generated/installed-distribution metadata, and transport-host identity should be explicit separate dimensions. If a dimension is scientifically claim-capable, make it reproducibly provisioned and exact; if it is only engineering provenance, do not let it become a hidden scientific gate. Because R4 currently classifies the RECORD and hosted image fields as scientific runtime, changing that classification requires a fresh revision.

## Claim-type / successor findings

Candidate 33 remains `SYSTEM / RESULT_EXPOSED_DEVELOPMENT / TERMINAL_FOR_CURRENT_OBJECT / NOT_QUEUED`; no same-object SYSTEM→MECHANISM uplift. H7 remains the sole active MECHANISM lineage. No fresh SYSTEM→MECHANISM successor is observed, and no successor is manufactured merely to increase throughput.

## Funnel observability / mechanism supply

Canonical R87 population remains `33 = 13 MECHANISM / 20 SYSTEM`, lifecycle `ACTIVE 1 / HOLD 0 / TERMINAL_FOR_CURRENT_OBJECT 32`, development phases `OPEN 2 / RESULT_EXPOSED 31 / canonical CONSUMED_ONE_WAY 0`, PRE_FORMAL eligible/READY `1/1`, fresh FORMAL authority `0`, official historical consumed identities `7`, classification completeness `33/33`.

Mechanism-supply health remains `ONE_ACTIVE_MECHANISM_FORMALIZATION_LINEAGE_QUALITY_FLOOR_INTACT_BUT_MECHANISM_SUPPLY_THIN`.

## Preformal calibration / pass reachability

PF-R1 remains one nonconfirmatory development observation with confirmatory count zero. `HIDDEN_SECOND_FORMAL_GATE=false`.

PASS remains `REALISTIC_CONDITIONAL`. Required prospective sequence: diagnose the exact generic-Test and runtime assertion failures; permit only confirmed science-invariant same-R4 repair; avoid resolver/materializer/runner-image shopping; complete one reproducible exact scientific-runtime synthetic realization; bind final claim-capable source plus runner/scorer/preserver blobs; resolve PF-R1 exact-byte preservation authority; close prediction-only raw → immutable preserve → target-side scorer and concealed-evaluation/no-clobber/collision gates; then STOP for a fresh Analyst one-way authorization.

## Utility request

No new Utility request is created. Existing `UTIL-20260922-1648-PFR1-DEVELOPMENT-PROVENANCE-PRESERVE` remains blocked by request-level machine-authority divergence. Do not duplicate, rerun, reconstruct, regenerate, or rescore PF-R1.

## Prospective recommendation

Keep Cycle 11 preidentity-only. Obtain exact diagnostics before changing any expected value. In particular, do not rerun repeatedly merely to obtain a hosted runner whose moving image happens to equal the frozen `runner_image_version`.

If exact diagnosis shows that R4 bound a transient hosted-runner/install-metadata property as scientifically material but that property is not reproducibly provisionable, preserve R4 unchanged and use an explicit prospective revision to define a reproducible execution substrate and cleanly separate scientific runtime identity from transport/installer provenance. If the mismatch instead shows actual scientific artifact/resource drift, stop and version the change; do not normalize or rebind.

PF-R1 preservation should continue to use only the existing artifact and existing request after matching schema-v2 Utility authority exists.

## Hard-floor confirmation / confidence

`CONFIRMED_DO_NOT_RELAX`.

Confidence: FORMAL non-consumption `HIGH`; exact research-ref advance and six-commit Cycle-11 materialization `HIGH`; clean exact-lock install success and pre-probe runtime assertion failure `HIGH`; generic CI failure after lint/readiness `HIGH`; science-invariant lint/diagnostic classification `HIGH`; exact cause of runtime/Test failure `UNRESOLVED`; installed-RECORD scientific relevance `MEDIUM_HIGH`; hosted-runner reproducibility risk `HIGH`; fresh consumed-FORMAL end-to-end `UNOBSERVED`; fresh SYSTEM→MECHANISM successor `UNOBSERVED`.

## Questions for Control / Analyst

- What exact observed/expected tuple caused the locked-runtime assertion failure, and does it alter scientific artifact/resource semantics or only installation provenance?
- Are `torch_distribution_record_sha256` and `runner_image_version` intended as scientifically claim-capable runtime identity, or as engineering provenance? Do not change R4 to answer this; classify prospectively.
- If the hosted image itself is scientifically material, what reproducible substrate can realize the exact image without repeated allocation retries?
- Can the generic-Test failure be classified from exact diagnostics before any repair?
- After all preidentity gates turn green, will the programme still stop for fresh Analyst one-way authorization?

## Authoritative refs used

- prior Methodology: `METHCAL-20260923T051912+0900-R73-6B1E94D3@ba241a77bc897d6facfc77cf2c26a898501e24f9`
- Control: `CTRL-20260923T055000+0900-R39-C31A7D52@c93f870e47bdb5b1613475e079c0a6dee2aa1753`
- Evidence Analyst: `EVA-20260923T061000+0900-R87-7B3D91E4@f03769227e888a70ea674eb627d4d311a1229c95`
- Utility: `UTILITY-20260923T023100+0900-AUTO-PFR1-AUTH-CONSISTENCY-COMPLETED-4E7A2C91@b18a1d76c05efbbfacb9618ac1ca1e9941b17555`
- stable main: `ebed6abfa941c83b36d2e3bddd04e7c5fb0dbe9d`
- H7 Cycle-11: `research/main-h7-formal-r4-source-executor-r85-cycle11@02382fbc7d3838159598015e488c6ce49ac34efc`
- exact-lock realization workflow: `35784686838`
- generic CI: `35784686727`
