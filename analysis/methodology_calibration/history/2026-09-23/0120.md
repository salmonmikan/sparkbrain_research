# SparkBrain Methodology Calibration Audit — R70

- schema_version: `2`
- generation_id: `METHCAL-20260923T012000+0900-R70-4C8A1E73`
- produced_at: `2026-09-23T01:20:00+09:00`
- authority_scope: `METHODOLOGY_ADVISORY_ONLY`
- overall_classification: `MIXED_CALIBRATION`
- material_change: `true`
- supersedes: `METHCAL-20260923T002000+0900-R69-7D2B4C91`

## Material change

H7 R4 has advanced from authorization-only to a materialized preidentity resource contract. The authoritative research branch is
`research/main-h7-formal-r4-runtime-lock-r81-cycle10@647253f4c0128ff09d47fdfce80dabf006863af1`.
Its `scientific-runtime.versions` prospectively fixes a 31-package protected runtime and explicitly excludes
pytest/ruff/jsonschema/FastAPI/uvicorn/httpx from protected scientific identity. The one-shot lock materialization workflow
`35748064227` succeeded at that exact head; the run-scoped artifacts endpoint returns exactly one artifact:
id `10703822275`, name `h7-formal-r4-lock-647253f4c0128ff09d47fdfce80dabf006863af1`,
size `5231` bytes, archive digest `sha256:54334fcb7ceb962e46f82712a36722a4a687d87e7d83dc32d86c61564e58e9d1`.

The artifact is retrievable and contains exact `scientific-runtime.lock` and `scientific-runtime-manifest.json` bytes.
The branch still does not contain those final files, so durable commit/capture remains open and no one-way identity may follow yet.

Fresh independent reconciliation exposes a control-plane observability defect in Evidence Analyst R82. Its mailbox says workflow
`35748064227` produced artifact id `10697560883`, size `4763`, while the authoritative run-scoped artifacts endpoint for that exact
workflow/head reports id `10703822275`, size `5231`. R82 also describes an "exact scientific package versions" set containing
numpy 2.2.6, scipy, scikit-learn, matplotlib, safetensors, platformdirs, jsonschema and cloudpickle, whereas the authoritative
R4 `scientific-runtime.versions` fixes a different 31-package set (including numpy 2.4.6 and the Torch/CUDA closure), and the
resource contract explicitly excludes jsonschema from the protected scientific runtime.

This is not scientific contamination. `ops/*` is a mailbox, fresh FORMAL authority remains zero, and no identity/result has been
consumed. It is an observability/provenance defect at the FORMAL boundary: artifact recovery and package-equivalence decisions must
bind to the authoritative workflow run + exact head + artifact name/digest + exact branch contract/version blobs, not copied mailbox
IDs or prose. The authoritative R4 contract must not be altered merely to match the stale/inconsistent R82 summary; if the fixed R4
resource set itself proves scientifically inadequate, STOP and version again.

## Input generations / refs

- prior methodology: `METHCAL-20260923T002000+0900-R69-7D2B4C91` @ `ops/methodology-calibration-audit@4211f373d39fda357b5176ed2732c4923c0c876a`
- human directive: `HUMAN-20260922-005` (process directive, not scientific evidence)
- Control: `CTRL-20260922T235000+0900-R37-7A3E9C51` @ `ops/control-brain-handoff@23676a55c9473d1cfd22d982ffffd5d2ba18b557`
- Evidence Analyst: `EVA-20260923T011030+0900-R82-7C4E19B2` @ `ops/evidence-analyst-handoff@000895a72f6cf54eeb3489a42f63f42b70281223`
- stable main: `ebed6abfa941c83b36d2e3bddd04e7c5fb0dbe9d`
- R4 research: `research/main-h7-formal-r4-runtime-lock-r81-cycle10@647253f4c0128ff09d47fdfce80dabf006863af1`
- R4 lock materialization run: `35748064227`
- authoritative run-scoped artifact: `10703822275`, digest `sha256:54334fcb7ceb962e46f82712a36722a4a687d87e7d83dc32d86c61564e58e9d1`
- existing PF-R1 utility request: `UTIL-20260922-1648-PFR1-DEVELOPMENT-PROVENANCE-PRESERVE@46a2b6735c4df221b28ee02a252993ffb7cbaa06`, incomplete

## Gate-by-gate classification

| Material gate | Classification | Finding |
|---|---|---|
| hard FORMAL integrity floor | `KEEP` | No fresh H7 identity/result; historical scientific refs unchanged. |
| orthogonal development-phase axis | `KEEP` | H7 remains RESULT_EXPOSED development; R4 is preidentity only. |
| full OPEN→RESULT_EXPOSED→CONSUMED_ONE_WAY live path | `INSUFFICIENT_EVIDENCE` | Fresh post-directive consumed FORMAL still unobserved. |
| RESULT_EXPOSED same-object invariant repair | `KEEP` | Parser/lint-only closure may remain same revision if fixed package/resource semantics are untouched. |
| post-result science-affecting change → version/fresh successor | `KEEP` | R3→R4 remains explicit/prospective. |
| R4 one-shot prospectively fixed package-set materialization | `KEEP` | Successful exact-head materialization exists without scientific execution. |
| exact lock/manifest bytes durable capture before identity | `TIGHTEN` | Artifact exists but branch has not yet durably committed final lock/manifest. |
| resolver/materializer shopping for preferred bytes | `TIGHTEN` | Do not rerun merely because mailbox pointer is wrong or branch lacks final bytes. |
| protected scientific runtime vs dev-tooling separation | `KEEP` | Authoritative contract/version file implements the R69 split. |
| authoritative run-scoped artifact discovery/binding | `TIGHTEN` | R82 copied artifact id/size conflicts with run-scoped authoritative metadata. |
| copied ops-mailbox artifact id/size as provenance authority | `CLARIFY` | Mailbox pointers are hints, not scientific/runtime source of truth. |
| Analyst package-contract echo vs authoritative branch contract | `TIGHTEN` | R82 package list conflicts materially with authoritative R4 versions/resource contract. |
| package/resource equivalence inferred from mailbox prose | `TIGHTEN` | Equivalence must use exact authoritative version/lock/manifest/source bindings. |
| cycle 3 as automatic terminal cap | `RELAX` | H7 cycle 10 remains justified by prospective integrity information gain. |
| cycle 3 mandatory reassessment | `KEEP` | Continuation is bounded and prospectively motivated. |
| raw-before-score as file ordering only | `CLARIFY` | Semantic prediction-only raw remains required. |
| literal target-blind prediction raw | `TIGHTEN` | R68 gate remains live. |
| scorer-side recomputation after immutable preserve | `TIGHTEN` | Remains a preidentity requirement. |
| target-sidecar independence | `TIGHTEN` | Remains a preidentity requirement. |
| preserve-before-read / immutable FORMAL evidence path | `TIGHTEN` | Remains unproven end-to-end for fresh H7 FORMAL. |
| concealed fresh evaluation surface | `TIGHTEN` | R2 remains quarantined; concealed post-binding evaluation is still required. |
| development reruns ≠ independent evidence | `KEEP` | R4 materialization/tooling runs carry zero confirmatory credit. |
| repeated result-bearing PRE_FORMAL live stress case | `INSUFFICIENT_EVIDENCE` | Still not adequately observed. |
| durable RESULT_EXPOSED development bytes | `TIGHTEN` | PF-R1 exact-byte preservation remains incomplete. |
| PRE_FORMAL as real development | `KEEP` | Iteration remains allowed without confirmatory credit. |
| preformal_eligible distinct from READY | `KEEP` | Distinction remains live. |
| READY = informative next test | `KEEP` | `HIDDEN_SECOND_FORMAL_GATE=false`. |
| current-object claim ceiling | `KEEP` | Ceiling remains object-specific. |
| same-object SYSTEM→MECHANISM uplift ban | `KEEP` | No laundering observed. |
| fresh terminal SYSTEM successor discipline | `KEEP` | Candidate 33 remains SYSTEM-only and terminal for current object. |
| fresh SYSTEM→MECHANISM successor | `INSUFFICIENT_EVIDENCE` | Genuine live case remains absent. |
| TERMINAL_FOR_CURRENT_OBJECT scope | `KEEP` | Topic death is not implied. |
| classification completeness | `KEEP` | R82 retains 33/33 classified. |
| MAIN MECHANISM priority | `KEEP` | H7 remains the viable MECHANISM lane. |
| prospective SYSTEM-priority exception | `KEEP` | No comparable-MECHANISM override observed. |
| genuine SYSTEM-over-MECHANISM exception | `INSUFFICIENT_EVIDENCE` | Still unobserved. |
| theory-backward quality floor / no-target liveness | `KEEP` | No candidate manufacturing to preserve activity. |
| protected/adaptive-evaluation validity | `TIGHTEN` | Prior concealed-evaluation/raw findings remain unresolved FORMAL gates. |

## Mandatory questions

1. Development semantics remain coherent through R4 preidentity materialization, but fresh `CONSUMED_ONE_WAY` remains unobserved.
2. Cycle 3 is not a hard cap; cycle 10 has explicit integrity/reproducibility information gain.
3. Repair classification remains calibrated: the parser fix keeps the exact fixed package set; resource/package semantic changes still require versioning.
4. No rerun/retune/tolerance observation is laundered into independent evidence. The one-shot materialization is non-evidentiary.
5. RESULT_EXPOSED revision lineage is preserved; PF-R1 exact-byte durability remains the open preservation gap.
6. FORMAL one-way integrity is unchanged: stable main/evidence unchanged and no H7 FORMAL identity/STARTED/result exists.
7. Candidate 33 is a legitimate fresh SYSTEM successor without MECHANISM uplift; a genuine fresh SYSTEM→MECHANISM successor remains unobserved.
8. PRE_FORMAL remains development, not hidden FORMAL; READY remains development readiness.
9. Terminal semantics remain calibrated, but mechanism supply remains thin: one active H7 lineage and 32 current-object terminals.
10. PASS remains reachable without weakening evidence standards if authoritative artifact/package bindings are reconciled and the remaining raw/holdout/preserve gates close.

## Risks / development calibration

False-positive risk: `MODERATE_WATCH`. The main scientific risks remain concealed-evaluation and semantic raw/preserve integrity. A new control-plane risk is accidentally treating R82's conflicting package summary or artifact pointer as authoritative resource evidence.

False-negative/opportunity-cost risk: `MODERATE_WATCH`. Mechanism supply remains one H7 lineage; stale artifact pointers or stale package summaries can also create unnecessary fail-closed churn even when authoritative bytes are recoverable.

Moving-goalpost/rescue risk: `LOW_TO_MODERATE_WATCH_IMPROVING`. R3 is preserved, R4 is explicit, the package set is fixed prospectively, and the parser repair preserves exact version bytes. Over-terminalization remains `LOW_TO_MODERATE_WATCH_IMPROVING`.

Development iteration remains calibrated: this is tooling/resource closure before one-way consumption, not evidence generation. R4 materialization attempts have zero scientific credit. The key new rule is that an ops summary mismatch must trigger authoritative reconciliation, not contract mutation or rematerialization.

## Funnel / mechanism supply / PRE_FORMAL

R82 canonical state: `33 = MECHANISM 13 / SYSTEM 20`; classification `33/33`; lifecycle `ACTIVE 1 / NONTERMINAL_HOLD 0 / TERMINAL_FOR_CURRENT_OBJECT 32`; development phases `OPEN_DEVELOPMENT 2 / RESULT_EXPOSED_DEVELOPMENT 31 / canonical CONSUMED_ONE_WAY 0`; PRE_FORMAL eligible/READY `1/1`; fresh FORMAL authority `0`; historical official consumed identities `7`.

Candidate 7/H7 remains `MECHANISM / FORMALIZE / READY(development-only) / RESULT_EXPOSED_DEVELOPMENT / ACTIVE`. Candidate 33 remains `SYSTEM / RESULT_EXPOSED_DEVELOPMENT / TERMINAL_FOR_CURRENT_OBJECT / NOT_QUEUED`. Mechanism supply remains `ONE_ACTIVE_MECHANISM_FORMALIZATION_LINEAGE; QUALITY_FLOOR_INTACT_BUT_MECHANISM_SUPPLY_THIN`.

PRE_FORMAL remains a nonconfirmatory development surface. PF-R1 contributes confirmatory count zero and has not been rerun/rescored. `HIDDEN_SECOND_FORMAL_GATE=false`.

## Pass reachability / prospective recommendations

PASS remains `REALISTIC_CONDITIONAL` without relaxing any evidence standard.

1. Treat authoritative R4 `resource_contract.json` + `scientific-runtime.versions` and exact repository/source refs as the package/resource contract; do not rewrite them to match R82 prose.
2. Recover the existing lock artifact from exact workflow run `35748064227` and head `647253f4...`; require run/head/name/digest agreement before durable capture. The current authoritative artifact is `10703822275`. Do not rematerialize merely because R82 cites another id.
3. Commit/preserve the exact recovered `scientific-runtime.lock` and manifest bytes unchanged, then bind their hashes and exact source commit/blob identities. If those exact bytes cannot be verified, STOP.
4. Fresh Control/Analyst should reconcile the R82 artifact-id/size and package-list discrepancies before any next preidentity authorization.
5. Keep separate machine-readable fields for workflow run id, exact head SHA, artifact name, observed artifact id, archive digest, lock digest, manifest digest, resource-contract blob and versions-file blob. A copied mailbox summary must never collapse these identities.
6. If the authoritative fixed R4 package/resource scope itself must change, version R5; do not reinterpret the mailbox summary as authorization to mutate R4.
7. Preserve all R68 literal prediction-only raw → immutable preserve → target-side scoring, concealed-evaluation, no-clobber/collision gates.
8. Complete the existing PF-R1 exact-byte NON_EVIDENTIARY preservation without rerun or reconstruction.

## Utility / hard floor / confidence

No new Utility request. Existing `UTIL-20260922-1648-PFR1-DEVELOPMENT-PROVENANCE-PRESERVE` remains outstanding.

Hard floor: `CONFIRMED / DO NOT RELAX`.

Confidence: `HIGH` on the authoritative R4 branch/run/artifact mismatch finding, `HIGH` that FORMAL one-way integrity remains unconsumed, `HIGH` that R4 package/runtime separation is calibrated, `MEDIUM_HIGH` on final preidentity closure because final lock/manifest bytes are not yet committed. Fresh consumed-FORMAL end-to-end and fresh SYSTEM→MECHANISM successor remain `UNOBSERVED`.

Questions for future Control/Analyst: reconcile R82 against the exact run-scoped artifact and authoritative R4 version file; confirm durable capture uses artifact `10703822275` (or another artifact only if exact run/head/name/digest prove it authoritative); record exact lock/manifest/source bindings machine-readably; keep R4 scientific semantics fixed; preserve R68 raw/holdout gates; complete PF-R1 byte preservation; and require a genuinely independent mechanistic residual plus fresh reduction/comparator/falsifier contract before any future SYSTEM→MECHANISM successor admission.
