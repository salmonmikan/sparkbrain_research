# SparkBrain Methodology Calibration Audit — 2026-09-19 20:22 JST

Overall classification: **`SLIGHTLY_TOO_PERMISSIVE`**.

## Material update

The imbalance remains local to lower-layer Architecture triage, not FORMAL integrity or the programme as a whole.

The previously accepted read-only Utility diagnostic `CTRL-20260919-1850-ARCH-SIGNAL-ROBUSTNESS` is now complete. It did not rerun training/probes, access TEST or formal/consumed evidence, mutate research refs, change scheduler state, or relabel the canonical Top-k cycle-1 result. Its methodology classification is `MIXED` with recommendation `ADD_PER_STRATUM_SUPPORT`.

For Top-k cycle 1, turnover support is `1 / 11 / 25` by magnitude. The canonical signal votes come from the `n=1` and `n=25` strata. Leave-one-magnitude-out analysis retains SIGNAL in only one of three views, and that retained view still relies on the single-case stratum. Diagnostic support eligibility at `n>=3`, `n>=5`, or `n>=10` yields MIXED in all three examples. Episode-cluster resampling under the current gate yields about `56.9% SIGNAL`, `9.7% low-turnover/inconclusive`, and `33.4% mixed/other`. These are descriptive robustness checks, not formal sampling claims.

The denominator-floor hypothesis is not supported: no turnover-case denominator is clipped by `1e-12`; minimum control state AUC is about `0.3485` and minimum control probability AUC about `4.60e-4`. Probability/output amplification remains well above the local cutoff under much larger diagnostic floors. The weak component is state-amplification support/threshold stability. Support-aware interpretation is therefore: **strong output amplification with mixed / threshold-sensitive state amplification**.

Evidence Analyst has already created `EVA-20260919-1943-TOPK-CROSS-SEED-SUPPORT`, a read-only cycle-1/cycle-2 diagnostic that forbids rerun/retraining/reprobe, threshold optimization, retroactive relabeling, or PRE_FORMAL authority. No duplicate Methodology request is created.

A separate MAIN Architecture line supplies important counterbalance against global tightening. `CAND-STRUCTURAL-ORDER-PATH-01` cycle 1 completed after a purely pre-diagnostic Ruff lint blocker was fixed by removing one unused import and line wrapping only. Exact-head workflow `35438868911` succeeded on `6a53ee36259bca31b77024245e5ab0a9ef5ec405`. The prospectively fixed mapping returned `EDGE_OR_HOMEOSTASIS_CONTROL_REPRODUCES`: the edge-only control diverged topologically in `11/12` pairs and had mean probability-L1 `0.02658`, satisfying the predeclared ordinary-control branch. MAIN stopped for fresh review; no cycle 2, PRE_FORMAL/FORMAL promotion, TEST access, identity creation, or rescue tuning occurred.

SUB also rejected reward-omission Discovery after explicit reward-trace recurrence fully explained the effect. These lower-layer reductions show that the four-layer funnel is not broadly too permissive: it admits bounded questions, learns from them, reduces ordinary mechanisms early, and stops.

No new FORMAL evidence was observed. The authoritative evidence set remains five annotated `evidence/*` tags. C19-R2 remains a representative terminal ordinary reduction: `REDUCED_BY_FSA` with exact package/raw-preserve/STARTED binding and clustered bootstrap evidence.

## Gate classifications

- one-way integrity / immutable evidence / no rerun-retune-rescore: `KEEP`
- prospective protocol / exact binding: `KEEP`
- positive signal before strong mechanistic reduction language: `KEEP`
- equal-privilege comparator matching: `KEEP`
- stronger-privilege counterfactual ceilings: `CLARIFY`
- new-computational-principle novelty bar: `KEEP`
- same novelty bar for architecture/testbed research: `SPLIT_BY_CLAIM_TYPE`
- programme-wide HOLD from no FORMAL object: `RELAX`
- observable-level versus family-level exhaustion: `CLARIFY`
- fresh independently motivated object requirement: `CLARIFY`
- three-cycle exploration budget: `KEEP`
- stop novelty claim after ordinary reduction: `SPLIT_BY_CLAIM_TYPE`
- stop each Architecture cycle for fresh Analyst review: `KEEP`
- Architecture per-stratum support adequacy: `TIGHTEN`
- exact replacement support/uncertainty rule: `INSUFFICIENT_EVIDENCE`
- local Architecture cutoffs as FORMAL/novelty thresholds: `CLARIFY`
- automatic PRE_FORMAL promotion from replicated Architecture labels: `KEEP` (prohibition retained)
- pre-start science-invariant repair before diagnostic visibility: `KEEP`
- ordinary-control-first Architecture reduction: `KEEP`
- selecting a future support rule after seeing completed-line outcomes: `CLARIFY`

## Calibration dimensions

`gate_drift`: healthy allocation drift to `FORMAL_HOLD_WITH_ACTIVE_LOWER_FUNNEL` remains intact; the current issue is local confidence calibration.

`justification_trace`: the need for some support/uncertainty guard is now strongly justified by actual robustness analysis. The exact replacement rule is not yet justified.

`false_positive_control`: FORMAL remains strong; lower-layer categorical labels can overstate support, but fresh-review boundaries contain the risk. Structural-order ordinary-control reproduction demonstrates healthy reduction.

`false_negative_risk`: global tightening would recreate prior stagnation. Fresh lower-layer work continues to produce informative rejection/reduction.

`duplicate_guards`: preserve distinct integrity purposes; only implementation consolidation is potentially appropriate.

`moving_goalposts`: `LOW_RETROACTIVE_RISK_WITH_PROSPECTIVE_RULE_SELECTION_WARNING`. Completed labels remain untouched. A future support rule must be justified generally and frozen prospectively, not optimized solely to flip or preserve Top-k outcomes.

`pass_reachability`: `REACHABLE_BUT_NARROW`; active lower layers improve object discovery without lowering FORMAL admission.

`comparator_calibration`: keep equal privilege. Top-k future reduction should test hard switching then recurrent/non-normal transient gain. Structural-order demonstrates ordinary-control-first value directly.

`signal_before_reduction`: Architecture signal wording remains local triage, not mechanistic support. Top-k state signal is support-sensitive; structural-order is already ordinary-control-reproduced.

`claim_type_separation` / `research_worthiness_vs_novelty`: currently healthy. Reduced lower-layer phenomena retain architecture/engineering information without becoming novelty claims.

`external_calibration`: Literature ordinary-reduction ladders and NI01 Audit privilege findings support current strict comparator semantics; neither rewrites consumed evidence.

`opportunity_cost`: targeted support calibration has higher value than global gating.

## Prospective recommendations

1. Keep the four-layer funnel and `FORMAL_HOLD_WITH_ACTIVE_LOWER_FUNNEL`.
2. Never retroactively relabel completed Top-k cycles.
3. Treat the completed Utility result as sufficient evidence that future Top-k-like Architecture gates need per-stratum support or uncertainty handling.
4. Do not choose an exact support floor/interval/weight rule yet; consume the existing cross-seed diagnostic and adopt a general prospective rationale rather than outcome-fitting completed cycles.
5. Do not advance Top-k to cycle 3/PRE_FORMAL solely from `PERSISTENCE_SIGNAL_REPLICATED`.
6. Keep mandatory fresh Analyst review after each Architecture cycle.
7. Treat structural-order `EDGE_OR_HOMEOSTASIS_CONTROL_REPRODUCES` as a successful ordinary-control reduction at this layer; do not mechanically consume remaining exploration budget.
8. Keep science-invariant pre-start mechanical repair authority only before any diagnostic visibility and with the scientific contract unchanged.
9. Keep local Architecture thresholds local; do not promote them into novelty/FORMAL gates without independent calibration.
10. Preserve equal-privilege reductions, stronger-privilege ceilings, and the entire one-way hard integrity floor.

Utility request created this run: **none**. The next required support question is already represented by Evidence Analyst request `EVA-20260919-1943-TOPK-CROSS-SEED-SUPPORT`.

Hard-integrity floor confirmed unchanged.