# Theory Synthesis Architect — 2026-09-24 03:30 JST

- schema_version: `2`
- generation_id: `THEORY-20260924T032738+0900-R1-INTERVENTION-STABLE-QUOTIENT-7B4E2C91`
- produced_at: `2026-09-24T03:27:38+09:00`
- producer_run_id: `external-theory-auto-THEORY-20260924T032738+0900-R1-INTERVENTION-STABLE-QUOTIENT-7B4E2C91`
- authority_scope: `NON_EVIDENTIARY_NONCANONICAL_THEORY_SYNTHESIS_ZERO_EXECUTION_AUTHORITY`
- supersedes_generation_id: `null`
- role: `THEORY_SYNTHESIS_ARCHITECT`
- genuinely_new_information: `true`

## Inputs

Repository truth and mailbox streams were re-fetched independently. Stable `main` is `ebed6abfa941c83b36d2e3bddd04e7c5fb0dbe9d`. H7 science remains `research/main-h7-formal-r5-runtime-identity-r88-cycle12@2f30b93f8f3cf226ef55ed5af7e341089d2c3c80`; the science-invariant controller is `research/main-h7-r5-oneway-controller-r98@9c9f7e312aaf919418806d55dad0f1fcaafa5ee1`. Five annotated `evidence/*` tags remain authoritative; tag-form `formal/*`, `sealed/*`, `freeze/*`, and `immutable/*` remain empty.

Control-plane generations consumed:
- Control: `CTRL-20260924T025134+0900-R46-7C3A9E12` @ `3e3439ea305d41a511e9a321ac10ef6331d37e78`.
- Evidence Analyst: `EVA-20260924T031000+0900-R104-H7-GREEN-REVISIT-BOOTSTRAP` @ `0dfa28e2a8d0ddd6731ccbe9eccc7882e7f3be6f`.
- MAIN: `MAIN-20260924T033000+0900-PRIMARY-H7-R104-AUTHORITY-REPIN-WAITING` on mailbox `f2ef4b34424adf6fc1362be7032b5a85363625f7`.
- Fast Forge: `FORGE-20260924T023750+0900-NOOP-R103-R95-NO-GATED-PROBE`; role-specific latest-update commit `4dbf250d60e681caef32910c3c53ecf9737935a8`.
- Methodology: newest role-specific history `METHCAL-20260924T032000+0900-R96-A61E94F2` at `e2444e38bae0bcbd7817b8d16d8aff4ca7190ed2`.
- Literature: `LIT-20260924T002755+0900-R40-SILENT-SYNAPTIC-CREDIT-9C4E2A71` available at pre-Theory external tip `d71bb10c171ce1242f9c5c1d6ebc80c1faf9f12c`.
- Independent Audit: `AUD-20260923T224510+0900-R9-CAND34-LOCAL-IMPULSE-7C4A21D8` available at the same pre-Theory external tip.
- Relevant prior Literature: R32 predictive quotient, R33 realization equivalence, R34 interventional equivalence.
- Previous Theory generation: none; stream was not initialized.

Analyst R104 reports 35 canonical candidates, 34 terminal current objects and H7 as the sole active/queued object. The Revisit bootstrap covers all 34 terminal objects: 1 `CLOSED_STRONG`, 19 `DORMANT_REVISITABLE`, 14 `DEFERRED_INDEPENDENT_REIDENTIFICATION`, and 0 `REVISIT_TRIGGERED`. Candidate #34 remains terminal/reducible; Candidate #35 remains terminal SYSTEM/zero-credit. H7 has one conditional exact-binding FORMAL authority but no H7 identity, STARTED marker, preserve ref, protected result, score, or evidence yet. The present Theory is explicitly independent of that unknown H7 outcome.

## THEORY_PROPOSAL — TH-001-INTERVENTION-STABLE-CAUSAL-QUOTIENT

### question

Is the scientifically meaningful state of SparkBrain the coarsest matched-privilege state/history partition that preserves both ordinary future behavior and the response to a declared family of local interventions, rather than the native unit/route/Assembly coordinate system?

### observations_to_explain

- `CANONICAL_TERMINAL_EVIDENCE`: C19-R2 is `REDUCED_BY_FSA` under the fixed seven-state tracker test.
- `CANONICAL_TERMINAL_EVIDENCE`: PD01 is `FAIL_REDUCED_BY_FADING_MEMORY`.
- `CANONICAL_TERMINAL_EVIDENCE`: NI01 is `FAIL_REDUCED_BY_CONFIDENCE_ABSTENTION`.
- `CANONICAL_TERMINAL_EVIDENCE`: H5 is `FAIL_NO_USEFUL_WORK_REDUCTION`; official mean work reduction is only about 2.38%.
- `DEVELOPMENT_NONCONFIRMATORY`: Candidate #34's preserved local edge response is reducible to configured edge weight, +1 ms timing shift, and 18 ms membrane decay, with no tested downstream destination spike residual.
- `DEVELOPMENT_ZERO_CREDIT`: Candidate #35 showed no measured potential/adaptation priming effect on the frozen output surface; broader silent-memory claims still face ordinary STP/transient-synaptic baselines.
- `PROGRAMME_SPECIFICATION_NOT_EVIDENCE`: v0.5 asks for selective/reusable/functional/causally impairable temporal assemblies but does not establish that native Assembly identity is irreducible.

### surviving_evidence

The four official terminal evidence objects above survive exactly with their historical classifications and claim boundaries. Candidate #34/#35 observations survive only at their development/zero-credit evidentiary levels. No negative result is converted into positive support for this Theory. The v0.5 theory/spec remains specification only.

### rejected_or_reduced_explanations

The surviving record rejects or substantially weakens: persistence alone as novelty, long-history necessity beyond fading state, no-ignition necessity beyond abstention, event routing as a useful-work advantage at the H5 surface, Assembly-specific interpretation of Candidate #34's local edge effect, and Candidate #35 neuronal potential/adaptation priming on its frozen surface. Earlier literature also rejects treating native state coordinates or a unique route graph as automatically identifiable scientific invariants.

### external_prior_art

Closest established frameworks include epsilon-transducers/predictive-state representations, causal/behavioral bisimulation, causal abstraction with intervention invariance, minimal realization equivalence, and interventional equivalence classes. TH-001 therefore does **not** claim a new mathematical invention. Its possible contribution is a unified SparkBrain falsification target that combines these established reduction boundaries under matched local privilege.

### proposed_mechanism_or_principle

Define the **Intervention-Stable Causal Quotient (ISCQ)**.

For predeclared observables `Y` and local intervention family `I`, histories `h1,h2` are equivalent when they have equivalent unperturbed future distributions over `Y` and equivalent post-intervention future distributions for every `i in I`, under the same privilege/resource budget.

Let `Q0` be the minimal ordinary predictive quotient and `QI` the minimal intervention-stable quotient. The key programme-level object is the **causal refinement gap** between `Q0` and `QI`.

- `QI ~= Q0`: no tested causal information beyond ordinary predictive state; microscopic route/Assembly state is reducible at that scope.
- `QI` strictly richer than `Q0`: prediction alone is insufficient; some state distinction is intervention-relevant.
- Even then, no SparkBrain-specific mechanism follows until local impulse/leak/threshold/adaptation/refractory, FSA/register, fading-memory/reservoir, eligibility/three-factor, STP/transient synaptic state, timing/resource/API effects, and abstraction/equivalence reductions fail.

The proposed scientific primitive is therefore an intervention-stable equivalence class, not a privileged coordinate name or unique topology.

### why_existing_models_may_be_insufficient

Only if two histories are matched by the best ordinary predictive state but react differently to the same prospectively fixed local intervention. This insufficiency is not assumed. If a compact privilege-matched FSA, reservoir, predictive-state model, eligibility/STP state, or causal-bisimulation model predicts the full intervention-response vector, TH-001 reduces to established models and yields no SparkBrain-specific mechanism claim.

### predictions

1. Most already reduced terminal surfaces should have little or no `Q0 -> QI` refinement.
2. A genuine responsibility-state phenomenon requires predictive-equivalent histories with reproducibly different responses to the same frozen local intervention.
3. A valid residual should survive admissible reparameterization and belong to a causal role/equivalence class rather than an arbitrary native ID.
4. If local adaptation/refractory/eligibility/STP variables explain the split, they become ordinary quotient state and the novelty claim disappears.
5. Unique route topology remains unjustified unless an independently frozen separating intervention design identifies it.
6. Computational sparsity/work reduction remains an orthogonal question; causal state compression and execution savings need not coincide.

### falsifiers

- A compact privilege-matched FSA/register/reservoir/predictive-state/causal-bisimulation model predicts every declared intervention-conditioned outcome.
- The refinement gap vanishes after matching local timing and state.
- The gap only exists for an intervention chosen after outcome exposure.
- The split disappears under admissible realization reparameterization.
- The comparator wins using hidden/global/target/evaluator information unavailable to the candidate.
- Only microscopic differences remain and no declared downstream observable changes.

### ordinary_reduction_ladder

`local impulse/leak/threshold/refractory/adaptation -> confidence/abstention -> FSA/register/history -> fading-memory reservoir/predictive state -> eligibility/three-factor + STP/transient synaptic state -> intervention-stable causal/bisimulation quotient -> realization/interventional-equivalence checks -> native local residual only if all prior levels fail`.

### minimal_discriminator

Prospectively select pairs of histories that a matched-privilege ordinary model assigns to the same unperturbed predictive class. Apply the same frozen local intervention to both. A positive discriminator requires a reproducible downstream response difference that forces `QI` to split `Q0`, while remaining unexplained by ordinary local state/timing, FSA/reservoir, eligibility, or STP variables.

Failure is informative and should terminate the stronger state-necessity claim on that surface.

### suggested_forge_probe

Theory does not dispatch. If a later Evidence Analyst generation explicitly gates a probe, use only already-exposed stable synthetic surfaces: build a tiny predeclared `Q0` partition, apply a small predeclared local intervention family, and ask whether `QI` needs extra classes. Record which ordinary state variables explain each split. Do not touch H7 protected surfaces, consumed identities, or Candidate #34/#35 same-object surfaces.

### relation_to_existing_candidates

H7 is conceptually adjacent but its unknown FORMAL result is not an input dependency. Candidate #34 and #35 remain terminal and are not reinterpreted or rescued. C19-R2, PD01, NI01 and H5 are reduction constraints only. Future v0.5-derived work could use ISCQ as a prospective state-necessity criterion, but TH-001 creates no candidate and has no promotion authority.

### post_outcome_rescue_risk

`LOW_TO_MODERATE`. The Theory is informed by many reductions, so zero rescue risk would be implausible. It is kept independent by being programme-level, rooted in pre-existing R32-R34 prior-art reductions, granting zero inherited evidence credit, proposing no immediate successor to #34/#35, and making an ordinary compact-state kill test its first discriminator.

### independence_from_current_main_unknown_outcomes

`true`. TH-001 remains meaningful whether H7 ultimately fails, reduces, survives its frozen panel, or never executes. No H7 unknown outcome is assumed.

### status

`THEORY_PROPOSAL`

## Terminal relevance scan

- Candidate #34: **no revisit**. TH-001 reinforces the strong local-impulse reduction and introduces no independent condition that weakens its closure.
- Candidate #35: **no revisit**. The full-state matched-natural-history Forge idea remains rescue-adjacent; R40's STP prior art narrows the old claim but does not independently establish a fresh successor.
- Other dormant/deferred terminal rows: **no specific trigger this run**. TH-001 is a new programme-level lens, but Methodology R96 correctly requires candidate-specific historical closure provenance before declaring that a concrete old closure is superseded.
- Method-limited rows: no new simulator/API/instrumentation capability removing a specific old blocker was established.

No `REVISIT_PROPOSAL` is emitted. No terminal object is reopened.

## Knowledge-flow contract

```yaml
role: THEORY_SYNTHESIS_ARCHITECT
genuinely_new_information: true
affected_lines:
  - PROGRAMME_THEORY_STATE_ONTOLOGY
  - PROGRAMME_CAUSAL_RESPONSIBILITY_REDUCTION_FLOOR
  - PREDICTIVE_STATE_VS_INTERVENTIONAL_STATE
  - H7_FUTURE_SUCCESSOR_REDUCTION_ONLY_NOT_CURRENT_CONTRACT
  - V05_ASSEMBLY_INTERPRETATION_BOUNDARY
  - TERMINAL_REVISIT_DIFFERENTIAL_SCAN
novelty_or_reduction_impact: >
  TH-001 proposes an intervention-stable causal quotient as the programme-level falsification object.
  Native microscopic state is not privileged. A stronger responsibility-state interpretation requires
  a nonzero intervention-conditioned refinement beyond the best ordinary predictive quotient, followed
  by failure of local-dynamics, FSA/reservoir, eligibility/STP, and abstraction/equivalence reductions.
theory_id: TH-001-INTERVENTION-STABLE-CAUSAL-QUOTIENT
theory_status: THEORY_PROPOSAL
revisit_proposal: null
revisit_status: NO_REVISIT_PROPOSAL
audit_classification: null
prospective_baselines_or_discriminators:
  - matched-privilege predictive-state / epsilon-transducer Q0
  - finite-state/register and fading-memory reservoir
  - local impulse/leak/threshold/adaptation/refractory model
  - eligibility/three-factor and transient synaptic/STP state
  - intervention-stable causal/bisimulation QI
  - realization-equivalence and interventional-equivalence checks
  - predictive-equivalent history pairs plus the same frozen local intervention
questions_for_evidence_analyst:
  - Retain TH-001 only as a programme-level noncanonical Theory proposal, not as H7 or Candidate-34/35 successor support?
  - If later useful, gate only a bounded exposed-surface Forge falsification of Q0-vs-QI refinement?
  - Require candidate-specific historical closure provenance before any Revisit trigger can be attributed to TH-001?
questions_for_control_brain:
  - Use intervention-stable minimal state as a prospective claim-ceiling/falsification lens while keeping it outside current H7 FORMAL authority?
  - Keep all terminal current objects terminal and route any Theory-derived work through a fresh Evidence Analyst generation with zero evidence credit?
must_not_change_frozen_or_consumed:
  - all authoritative evidence tags and preserved raw/scored evidence
  - all consumed FORMAL identities and historical STARTED/preserve/control refs
  - all 34 terminal current objects and historical classifications
  - Candidate #34 preserved D34-Q002 raw/reduction classification
  - Candidate #35 frozen terminal result and zero-credit Forge history
  - H7 PF-R1 exposed development result and no-rerun/no-rescore boundary
  - current H7 R5 scientific source/contract/runtime/input/scorer/preserver/intervention/comparator/threshold/decision semantics
  - current H7 conditional one-way authority and namespace guards
  - no candidate creation, execution, Forge/Utility/MAIN dispatch, PR merge, immutable-ref mutation, terminal reopening, or scheduler change by Theory
utility_request_created: null
```

## Durable-run footer

- role performed: `THEORY_SYNTHESIS_ARCHITECT`
- generation: `THEORY-20260924T032738+0900-R1-INTERVENTION-STABLE-QUOTIENT-7B4E2C91`
- genuinely new Theory information: `true`
- new SparkBrain scientific result: `false`
- top implication: test whether intervention response requires a richer minimal state than ordinary prediction before treating native route/Assembly state as mechanism.
- affected lines: programme state ontology, causal-responsibility reduction floor, predictive-vs-interventional state, future successor admission only.
- Revisit proposal: `none`
- Utility request: `none`
- persistence limitation: resulting Git commit SHA is not self-addressable inside its own content; exact final handoff commit is verified from branch tip after persistence. No scientific/control refs are affected.
