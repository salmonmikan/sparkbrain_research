# SparkBrain Control Brain — 2026-09-18 14:50 JST

## Current strategic position

**No programme-level doctrine change.** SparkBrain remains a **persistent dynamical cognitive architecture / experimental testbed**, not a demonstrated new computational principle. The novelty bar remains narrow: locality, pre-semantic organization, history dependence, persistent state, predictive state, or self-organization are not sufficient. A stronger claim must survive ordinary finite-state, learned/extracted automata, predictive-state / causal-state, recurrent/reservoir/fading-memory, and conventional plasticity / eligibility / slow-state reductions, and must isolate a residual tied to actual historical lineage / causal credit rather than generic memory.

The evidence has narrowed the central thesis further. C19's registered temporal/state story is terminally reduced by an explicit seven-state FSA and independently audited `ROBUST_SO_FAR`; PD01 is terminal `FAIL_REDUCED_BY_FADING_MEMORY`. The live high-information frontier is now NI01/H4: whether **native No-Ignition** adds selective predictive value beyond an ordinary target-blind, coverage-matched max-confidence abstention comparator under a prospectively fixed controlled protocol.

## Strongest support, counterevidence, and reductions

- **C19-v4:** preserve only as the narrow truth-free representation PASS. Do not infer a novel temporal/state mechanism from it.
- **C19-R2:** terminal `REDUCED_BY_FSA`, evidence tag `evidence/c19-r2-fsa-state-tracker-c19-r2-fsa-state-tracker-official-v1`. Primary effect `C19-v4 BREU - R2 FSA BREU = -0.2462308568887407`, cluster-bootstrap 95% CI `[-0.2554750732756702, -0.23690126111231016]`. Independent audit recomputed the aggregate and found all five seed-level contrasts negative.
- **PD01:** terminal `FAIL_REDUCED_BY_FADING_MEMORY`; identity consumed/no-retry and authoritative evidence tag present. This weakens the idea that long-history persistence, by itself, exposes a SparkBrain-specific mechanism.
- These reductions strengthen the programme's reduction-first doctrine and weaken any claim whose content is only “persistent/local/history-derived state exists.”

## Primary frontier — NI01/H4

Current exact research object: `research/ni01-no-ignition-selective-prediction-spec-20260918@2664951b65dd18883d3d80862e80b86ac66cf24f`.

Fresh repository state at this run:
- dedicated NI01 preformal run `35308269458`: `completed/success`;
- ordinary CI `35308269286`: `completed/success` on the same exact head;
- proposed identity `ni01-no-ignition-selective-prediction-official-v1`: **unreserved / unconsumed**;
- no NI01 `control/*` / STARTED ref;
- no NI01 `preserve/*` ref;
- no NI01 `evidence/*` tag.

Evidence Analyst handoff `e22b401013b01517890d48e33debdd79bb522e65` prospectively authorizes **exactly one** NI01 one-way chain only after fresh binding/integrity checks remain green. The registered comparator is a zero-trainable-parameter, target-blind max-confidence abstention rule with one DEV-derived scalar threshold per world, matched to native coverage. PASS requires every-world coverage difference `<=0.03`, overall effect CI lower `>=0.02`, and every-world point effect `>0`; FAIL requires the coverage guard and effect CI upper `<=0.005`; otherwise valid outcomes are INCONCLUSIVE.

Control Brain adds no new semantic condition to that frozen prospective object. A PASS would support only native No-Ignition selective value over this exact comparator/world/metric set; it would **not** establish full H4, OOD generality, or a new computational principle. A FAIL terminates this registered discriminator as reduced by confidence abstention; no rescue/comparator shopping/automatic NI01-v2. POST_START_FAILURE or INVALID_EVIDENCE consumes the identity and terminates with no retry.

### Current operational blocker

The scientific object itself is ready, but MAIN's durable control-plane stream is not current. The last durable MAIN latest/history remains the 13:47 RELAY checkpoint, while the 14:00 Analyst handoff granted fresh conditional authority and the exact-head ordinary CI subsequently completed green. A PRIMARY lease was written for 14:13 with status `RUNNING` and authority `e22b401...`, but its heartbeat did not advance and no newer MAIN latest/history had persisted by this snapshot. Fresh repository refs still show no STARTED/preserve/evidence, so there is **no observed scientific-integrity breach or accidental identity consumption**; this is an operational persistence/progress defect, not a scientific result.

## External input — literature

Consumed role: `LITERATURE_REDUCTION_SCOUT`.
Exact handoff commit: `525686fa0c14f64426ca7bf5c89f08cd0bcb9c77`.

Material findings remain:
- PSR/TPSR and epsilon-machine / causal-state models are stronger ordinary reductions than one hand-designed FSA;
- local causal-state work removes “local + pre-semantic + emergent predictive organization” as a standalone novelty axis;
- reservoir universality increases pressure on fading-memory claims;
- learned/extracted automata are preferable to an endless hand-FSA rescue ladder.

Strategic impact: retain the reduction ladder **exact FSA → learned/extracted finite-state → PSR/epsilon-machine → recurrent/reservoir/fading-memory → residual lineage-specific causal credit**. These findings do not require any mutation of NI01; its canonical null is already the prospectively registered confidence-abstention comparator. Any stronger abstention/OOD test must be a fresh future object.

Questions sent to Evidence Analyst: keep NI01 interpretation narrow; after any positive result, distinguish the registered H4 discriminator from broader uncertainty/OOD mechanisms; do not retrofit stronger baselines into the current identity.

## External input — independent audit

Consumed role: `INDEPENDENT_AUDITOR`.
Exact handoff commit: `0c871b1b9b54d35c826a59c5b9925afa55b78d22`.
Classification: **`ROBUST_SO_FAR`** for terminal C19-R2.

The audit independently confirms one-way integrity, target blindness, the registered cluster bootstrap, the terminal FSA reduction, and the narrow claim boundary. It materially strengthens confidence that C19-specific temporal/state rescue work should remain closed. Evidence Analyst has now explicitly consumed this audit, so **Literature → Analyst → Control and Audit → Analyst → Control knowledge flow are both healthy**.

A fresh independent read-only audit of terminal PD01 remains strategically useful but does not block NI01 because NI01 is independently motivated and excludes PD01/C19 outcomes from its comparator rationale.

## SUB incubator assessment

Current SUB mode: `no_op`. Classification: **`NO_OP_BY_DESIGN`**.

SUB latest at 14:40 independently re-fetched NI01 readiness, confirmed both gates green and the identity unconsumed, then correctly touched nothing. No formal `sub_lane`, no fallback, and no safe non-duplicative incubator candidate currently exist. This is scientifically preferable to fake parallelism. Previous bounded exploratory work is exhausted; there is no current incubator observation to formalize, reject, or continue.

## Programme-drift assessment

Discipline remains good. Terminal reductions are preserved instead of rescue-tuned; NI01 was selected from a pre-existing independent H4 question rather than invented to save C19/PD01; SUB has not invaded MAIN; and current readiness remains pre-START. Complexity is **not** presently being added to rescue a failed hypothesis.

The immediate drift risk is operational: a stale MAIN lease / missing durable MAIN checkpoint can obscure whether a prospectively authorized transition has actually advanced. Repository refs currently resolve the ambiguity safely—NI01 is still unstarted—but the MAIN worker should repair its durable checkpoint/lease flow before relying on relay continuation.

## Repository doctrine status

Status: **`PARTIAL_COMPLIANCE_IMPROVING`**.

- `main@ebed6abfa941c83b36d2e3bddd04e7c5fb0dbe9d` remains stable shared substrate.
- unresolved NI01 remains under `research/*`.
- authoritative annotated evidence tags now total **3**: C19-v4, C19-R2, PD01.
- all **13** legacy `freeze/*` branches remain preserved.
- repository rulesets remain **0** and `main` remains unprotected; server-side update/delete protection for authoritative tag namespaces remains the principal governance debt tracked by Issue #139.
- open PRs remain #148 (human-directives skill) and #149 (scheduler-registry skill). They are outcome-independent and structurally main-eligible for ordinary review, but Control Brain does not merge them.

### HUMAN-20260918-001 promotion direction

Directive remains **ACCEPT**, with implementation now refined by the completed Steward inventory. Do **not** wholesale merge/cherry-pick the old CX comparator branches. The worthwhile promotion path is a small neutral extraction on current `main`, with fresh CI, containing only outcome-independent substrate such as anonymous event / prediction-distribution primitives, architecture-neutral comparator protocol shape, snapshot/restore invariants, descriptive resource accounting, generic privilege guards, transcript validation, and generic leak/distribution checks. Pure G3 recurrent or G6 VOMM cores are conditional candidates only after clean extraction. G4/G5/G7/G8 mechanisms, CX01 worlds/thresholds, comparator taxonomy/privilege mapping, frozen bindings, and result-coupled semantics remain research-local. This work must not delay NI01.

## Ranked priorities for the next 12–48 hours

1. **MAIN:** reconcile the stale PRIMARY lease/durable report against current remote refs, then execute the prospectively authorized exact NI01 chain only if fresh Analyst authority, identity freshness, namespace checks, exact bindings, and same-head green gates all remain valid. No Control-Brain-created successor or semantic repair.
2. **Evidence / audit:** after NI01 terminalizes, independently audit it before any broader H4 claim; separately obtain a fresh read-only PD01 terminal audit because PD01 now carries substantial reduction weight in programme theory.
3. **Reduction strategy after NI01:** PASS stays narrow and motivates a fresh stronger uncertainty/OOD reduction object; FAIL closes the registered H4 discriminator. Either way, do not automatically create NI01-v2.
4. **Repository:** keep the neutral CX-substrate extraction as non-blocking secondary governance work; ordinary review only.

## Stop / reframe conditions

- Clean NI01 FAIL → terminate the registered No-Ignition selective-value discriminator; no rescue tuning.
- NI01 PASS → preserve only the narrow registered effect; broader H4/novelty requires fresh stronger ordinary comparators and prospective tests.
- NI01 INCONCLUSIVE → terminal for this identity; do not threshold/world tune.
- NI01 INVALID_EVIDENCE or POST_START_FAILURE → consumed identity, no salvage/retry.
- If remaining non-privileged residual families also reduce under ordinary predictive/recurrent/plasticity baselines, retain the architecture/testbed framing and stop computational-principle novelty claims.

## Scheduler fleet health

`fleet_status: YELLOW`

- **Control / Repository Steward combined:** `HEALTHY` — role-specific durable records continue advancing; minor runtime delay only.
- **Evidence Analyst:** `DELAYED` — latest 14:00 analysis persisted around 14:09, but it is current and scientifically coherent.
- **MAIN / PRIMARY:** `FAILED_OR_INCOMPLETE` — 14:13 lease entered `RUNNING`, no later heartbeat, and no durable MAIN latest/history after 13:47 despite fresh Analyst authority and completed green CI. No scientific mutation is observed.
- **MAIN / RELAY:** `DELAYED` — prior relay was useful, but the current downstream continuation had not durably reconciled the stale PRIMARY lease by this snapshot.
- **SUB:** `NO_OP_BY_DESIGN` — correct; no independent object exists.
- **External Research / Audit:** `HEALTHY` — both role-specific streams persist and both are consumed by Analyst and Control Brain.
- **Current State Brief:** `DELAYED`, non-critical read-only output.

`critical_failures`: none in scientific integrity or evidence preservation.

`operational_defects`: stale MAIN PRIMARY heartbeat/durable report; continued runtime slippage; dense `:35 → :45 → :50` downstream window.

`efficiency_findings`: RELAY remains useful as a recovery mechanism; SUB no-op prevents fake parallelism; external knowledge flow is healthy; no justification exists for higher experiment cadence.

Scheduler recommendation: **no scheduler-definition change in this role**. First diagnose MAIN PRIMARY duration/persistence. If another 2–3 cycles repeatedly leave Control/Steward sampling during an unfinished RELAY, then consider shifting only the downstream read-only Control/Steward slot 5–10 minutes later. Do not increase research frequency to compensate for persistence latency.

## Direction to Evidence Analyst

1. Keep NI01 as the sole MAIN frontier and its current exact prospective semantics unchanged.
2. On any NI01 terminal outcome, enforce the already-fixed narrow interpretation and no automatic successor rule.
3. After NI01, prioritize an independent audit before broader H4 interpretation; obtain a fresh PD01 audit as a separate read-only confidence check.
4. Do not reopen C19 rescue work; C19-R2 remains terminal and independently robust.
5. Keep SUB null/no-op unless a genuinely distinct independent prospective question appears.
6. Treat the Steward's CX promotion inventory as repository substrate advice only, not scientific evidence or a reason to interrupt NI01.
