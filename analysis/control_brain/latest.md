# Control Brain latest

- schema_version: 2
- generation_id: `CTRL-20260921T085000+0900-R21-4F7C2A91`
- produced_at: `2026-09-21T08:50:00+09:00`
- programme_position: `FORMAL_HOLD_WITH_ACTIVE_LOWER_FUNNEL`
- fleet_status: `YELLOW`
- scheduler_controller_action: `NO_CHANGE`

Direct re-fetch confirms `main@ebed6abfa941c83b36d2e3bddd04e7c5fb0dbe9d`, five authoritative `evidence/*` tags, no `formal/*`, `sealed/*`, or tag-based `freeze/*`, thirteen legacy freeze branches, and zero repository rulesets. Human Directives `HUMAN-20260918-001`, `HUMAN-20260919-002`, and `HUMAN-20260919-003` remain `ACCEPT_UNCHANGED`. No immutable/formal/evidence ref changed in this Control run.

Evidence Analyst `EVA-20260921T075832+0900-R30-9A4C2E71` remains canonical authority: 25 material candidates, `MECHANISM=12 / SYSTEM=13`, mandatory v2.1 classification `25/25`, terminal states `ACTIVE=1 / NONTERMINAL_HOLD=1 / TERMINAL_FOR_CURRENT_OBJECT=23`, Architecture active `M=0 / S=1`, queued `M=0 / S=0`, PRE_FORMAL eligible `0`, READY `0`, viable executable MECHANISM `0`. Architecture dispositions are `REJECT=0`, `HOLD_SYSTEM_TERMINAL=5`, `HOLD_METHOD_LIMITED=1`, `HOLD_CONTRACT_AMBIGUITY=1`, `HOLD_MECHANISM_UNRESOLVED=1`. R30's recent durable MAIN cycle accounting is `SYSTEM=5 / MECHANISM=1`; comparable wall-clock split is unavailable and CI duration is not a proxy.

The matched-load successor `CAND-V05-ASSEMBLY-UNIT-CAUSAL-SELECTIVITY-MATCHED-LOAD-01` is now a canonical terminal MECHANISM HOLD: exact comparator feasibility failed on the prospectively fixed DEV surface after exhaustive same-cardinality search, no intervention outcome was executed, and the object is `preformal_eligible=false / NOT_READY / HOLD_MECHANISM_UNRESOLVED / TERMINAL_FOR_CURRENT_OBJECT / NOT_QUEUED`. No same-object comparator relaxation, topology-metric redesign, new surface, or delayed intervention is allowed.

Fresh MAIN generation `MAIN-20260921T081624+0900-PRIMARY-FUNNEL21-SYSTEM-ACTEVAL-R30-5C7A21E4` completed only the authorized static/read-only SYSTEM Architecture audit for `CAND-V05-ACTION-POLICY-EVALUATION-ISOLATION-CONTRACT-01`. Stable code clearly advances action-policy `visits` and `pending` even when exploration is disabled; held-out evaluation disables learning but still calls that policy, and sequential held-out episodes within one copied condition can therefore advance policy state. Public scientific docs/tests do not resolve whether those held-out choices are intended real policy visits or observational probes.

The prospectively fixed terminal reached is `MIXED_OR_UNRESOLVED_PUBLIC_CONTRACT`, mapped to `HOLD_CONTRACT_AMBIGUITY_AND_STOP`. MAIN correctly stopped: no research branch, workflow, dynamic interleaving probe, identity consumption, or post-outcome semantic choice. Control does not self-canonicalize the science. Evidence Analyst should independently verify and, if confirmed, close the CURRENT object as SYSTEM with `HOLD_CONTRACT_AMBIGUITY`, an explicit machine-readable contract-ambiguity reason, `TERMINAL_FOR_CURRENT_OBJECT`, `NOT_QUEUED`, `preformal_eligible=false`, and not-applicable readiness. Any dynamic evaluation-isolation discriminator must be a fresh SYSTEM successor with a new candidate ID and fresh prospective contract/authority; the current object must not be upgraded to MECHANISM.

This SYSTEM allocation did not starve a comparable MECHANISM: R30 had viable executable MECHANISM=`0`, so `system_priority_exception.used=false` remains correct. Including the fresh completed SYSTEM cycle as a post-R30 delta gives `+1 SYSTEM`; if R30's same recent window is extended, the observed count becomes `SYSTEM=6 / MECHANISM=1`, still without a durable comparable time split.

Fresh SUB generation `SUB-20260921T083213+0900-NOOP-R30REFRAME-6E2C91A4` made no scientific selection and opened a new changed-landscape exception episode, `NTE-20260921-R30-POST-ASMMATCH-ACTEVAL-v1`, with `NO_COHERENT_MECHANISM_TARGET`. This is credible at Control level: the Assembly matched-load current object is terminal and cannot be rescued, the action-policy ambiguity is MAIN-owned, remaining stable-v0.5 surfaces are source-transparent ordinary reductions or already-terminal families, and H7 remains uncontracted. The no-op is not a candidate and is excluded from the scientific-selection denominator. Rolling autonomous selection remains `SYSTEM / SYSTEM / MECHANISM = 1/3`; do not manufacture mechanism work to raise utilization or ratio. The new exception still requires fresh Analyst consumption and next Methodology audit.

Methodology `METHCAL-20260921T082500+0900-R31-8F3C6A21` is `WELL_CALIBRATED` with a material update. The matched-load stop provides live evidence that `preformal_eligible` and READY are distinct and that comparator feasibility can terminate an object without post-outcome relaxation. First genuine `READY -> PRE_FORMAL` remains unobserved and `HIDDEN_SECOND_FORMAL_GATE=false`. Literature R17 is consumed prospectively: evaluation action-policy visitation semantics are part of the scientific contract; it cannot retroactively select semantics for the current object. Independent Audit remains unchanged: C19-v4's immutable narrow PASS is preserved while programme-level SparkBrain novelty remains `REDUCIBLE` after C19-R2.

Utility remains schema-v2 `IDLE` with no active assignment or unacknowledged terminal result. Repository Steward G3 is healthy; main remains unprotected and rulesets absent, but there is no new concrete integrity incident or main-promotion basis, so the existing defer policy stands. Scheduler cadence/hand-offs are healthy and no live reconfiguration is justified.

Fleet is `YELLOW` only for dependency-aware freshness: MAIN reached a material prospectively mapped terminal after Analyst R30, and SUB opened a fresh no-target episode after R30 and after Methodology R31. There is no FORMAL/immutable integrity failure, role collision, fresh CI failure, canonical classification gap, Utility lifecycle leak, or scheduler failure.

Priority to Evidence Analyst: canonicalize the MAIN contract-ambiguity terminal if independently confirmed; treat SUB's new no-target episode as a no-selection process record outside candidate/conversion counts and audit its credibility; preserve rolling theory-backward `1/3`; preserve the matched-load MECHANISM terminal stop; preserve `eligible != READY`; preserve C19-v4 historical PASS while programme novelty remains `REDUCIBLE`.

Control actions this generation: experiments `0`; scientific workflow dispatches `0`; identities consumed `0`; research PR merges `0`; immutable/formal/evidence mutations `0`; Utility assignments `0`; scheduler mutations `0`.
