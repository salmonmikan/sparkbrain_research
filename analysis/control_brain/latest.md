# SparkBrain Control Brain — 2026-09-19 22:50 JST

- role: `CONTROL_BRAIN`
- selected_slot_jst: `22:50`
- role_inference_required: `false`
- foundational_doctrine_change: `NO`
- operating_doctrine_change: `NO_MATERIAL_CHANGE`
- programme_position: `FORMAL_HOLD_WITH_ACTIVE_LOWER_FUNNEL`
- scheduler_controller_action: `NO_CHANGE`
- fleet_status: `YELLOW`

## Human Directive review

No new or materially changed active Human Directive was found. Existing dispositions remain:

- `HUMAN-20260918-001`: `ACCEPT` — promote only stable, outcome-independent reusable substrate; never wholesale-merge consumed scientific state/result-dependent machinery.
- `HUMAN-20260919-002`: `ACCEPT` — ruleset/protection deployment remains deferred absent a materially changed concrete integrity risk; the absence of protection remains a factual governance gap.
- `HUMAN-20260919-003`: `ACCEPT` — research stagnation is a real concern, but the response is active DISCOVERY/ARCHITECTURE_STUDY/PRE_FORMAL under strict FORMAL integrity, not weaker evidence standards or low-value cleanup.

## Central theory / reductions / residual

The strongest central novelty claim remains on FORMAL HOLD. A central SparkBrain novelty claim is admissible only for a native, prospectively positive, intervention-validated lineage-specific causal-responsibility effect that survives ordinary history/provenance/actual-causality and local-learning/recurrent/plastic reductions under matched information, readout, task/world/regime-label, and resource privilege. No current object clears that bar.

The lower funnel is nevertheless producing substantive information rather than governance-only activity:

- `CAND-TOPK-PA-01`: two NON_EVIDENTIARY Architecture cycles reproduced the fixed local label, but support robustness is unresolved. The completed cycle-1 Utility diagnostic found `MIXED / ADD_PER_STRATUM_SUPPORT`; one signal-casting magnitude had only one turnover case. Keep the candidate at `ARCHITECTURE_STUDY/HOLD` and do not run cycle 3 or promote to PRE_FORMAL yet.
- `CAND-STRUCTURAL-ORDER-PATH-01`: current Architecture question is `REJECT`; edge-only/homeostatic control reproduced the functional order dependence. Preserve order sensitivity as architecture information, but do not rescue the current mechanistic-distinctness question.
- SUB has continued to generate independent bounded information and stop early on ordinary reductions. The newest Discovery, `ASSEMBLY_PROTOTYPE_LOCKIN_ORDER_SENSITIVITY_DISCOVERY`, found that the same `AAABBBCCC` episode multiset yields one mature assembly in `560/1680` orders and two in `1120/1680`, under greedy matching to frozen first-exemplar prototypes. This is strictly NON_EVIDENTIARY. SUB recommends fresh `ARCHITECTURE_STUDY`; Evidence Analyst must decide promotion prospectively.

## H5 Independent Audit implication

The 22:31 Independent Audit classifies consumed H5 as `ROBUST_SO_FAR`: authority/package/STARTED/raw-preserve/evidence binding and the canonical `FAIL_NO_USEFUL_WORK_REDUCTION` remain valid. The interpretation must remain narrow. H5 does not establish that event routing or lazy execution can never save compute; under the registered accounting, global all-edge eligibility maintenance dominates the sparse workload and becomes the bottleneck. Any future localized-eligibility or component-wise efficiency question must be a fresh independently justified object, not an H5 rerun, retune, rescue, or rescore.

## External literature implication

The 21:30 Literature stream sharpens the prospective ordinary-reduction ladder for held Top-k persistence:

`hard routing boundary -> border-collision / branch-basin selection -> recurrent WTA hysteresis -> within-branch recurrent/non-normal transient gain -> unexplained residual`.

Local contraction within routing regions is not enough to claim a distinct persistence mechanism because switching discontinuities can sustain globally different long-lived behavior. This literature is strategy/reduction input only and does not alter completed Architecture labels.

## Methodology Calibration implication

The 22:18 Methodology Calibration remains `SLIGHTLY_TOO_PERMISSIVE` with `NO_MATERIAL_CALIBRATION_CHANGE`. The permissiveness is local, not programme-wide: Architecture triage should prospectively tighten per-stratum support adequacy, while the exact replacement threshold/weighting rule must not be fit from completed Top-k outcomes. The hard one-way integrity floor, positive-signal-before-strong-reduction language, matched comparator privilege, stop-after-Architecture-cycle review, and claim-type separation remain appropriate. PASS remains reachable but narrow.

## Research funnel health

- `DISCOVERY`: `ACTIVE_PRODUCTIVE` — SUB is generating independent observables and rejecting ordinary implementation effects early.
- `ARCHITECTURE_STUDY`: `ACTIVE/HOLD` — Top-k is held pending cross-seed support; Structural current question is rejected; fresh assembly-prototype lock-in Discovery awaits Analyst promotion review.
- `PRE_FORMAL`: `EMPTY_HOLD` — no support-aware candidate with a freshly fixed reduction/comparator question.
- `FORMAL`: `EMPTY_HOLD` — no fresh identity, STARTED, TEST, formal preserver/scorer, or evidence authority.

The anti-stall doctrine is working: FORMAL HOLD no longer means programme-wide inactivity, and lower-layer work is producing promotion/rejection/reduction information without weakening FORMAL integrity.

## Utility request bus / assignment

The prior one-run assignment `CTRL-20260919-1850-ARCH-SIGNAL-ROBUSTNESS` is complete at max-runs with `MIXED / ADD_PER_STRATUM_SUPPORT` and no integrity breach.

This run dispositions the unhandled requests as follows:

- `EVA-20260919-1943-TOPK-CROSS-SEED-SUPPORT`: `ACCEPT`. It is the highest-value bounded read-only question and directly resolves whether the replicated Top-k Architecture label is broadly supported or repeatedly sparse-stratum sensitive.
- `LIT-20260919-1830-TOPK-HYBRID-TRANSIENT`: `DEFER`. The mechanistic decomposition remains useful, especially after the 21:30 literature update, but should wait for the cross-seed support diagnostic and fresh Analyst review rather than run concurrently.
- `METHCAL-20260919-1818-ARCH-SIGNAL-SUPPORT`: prior `ACCEPT`, now `COMPLETED`; no extension.

New assignment: `CTRL-20260919-2250-TOPK-CROSS-SEED-SUPPORT`, max one Utility run, strictly read-only and NON_EVIDENTIARY over the existing two DEV Architecture artifacts. MAIN must not wait for it. It may report only `SUPPORT_ROBUST`, `MIXED`, `UNDER_SUPPORTED`, or `BLOCKED_MISSING_SAFE_INPUT`; every outcome returns to fresh Evidence Analyst review and authorizes neither cycle 3 nor PRE_FORMAL in the same run.

Control decision commit: `ffe5b7b5f363802270d90ec1af65e8c8d265423e`. Final corrected assignment commit: `3dffa7212e054fd5ca8ca5ddf1991ad2a69affbc`.

## MAIN / SUB / Relay / Utility assessment

- MAIN: `NO_OP_BY_DESIGN` / healthy explicit hold at 22:19. Structural is rejected, Top-k is held, and no fresh central object is authorized. No experiment/workflow/identity/formal mutation occurred.
- SUB: `EXPLORATORY_PRODUCTIVE` at 22:41. `ASSEMBLY_PROTOTYPE_LOCKIN_ORDER_SENSITIVITY_DISCOVERY` completed cycle `1/3`, exact-head ordinary CI succeeded, and a prospective Architecture promotion recommendation was returned without touching MAIN or formal surfaces.
- Relay: `NO_OP_BY_DESIGN`. MAIN has no live continuation object. The relay scheduler remains active, but no durable MAIN overwrite is expected merely to record a no-op.
- Utility: `HEALTHY / ASSIGNED`. The previous assignment completed cleanly; the new cross-seed read-only assignment is now authoritative on the request bus. Git assignment is sufficient; live Utility scheduler semantics/cadence were not changed.

## Knowledge-flow gaps / Evidence Analyst direction

Evidence Analyst latest is 21:02 and therefore predates four material inputs: 21:30 Literature, 22:18 Methodology, 22:31 H5 Audit, and 22:41 SUB Discovery. This is the main current control-plane lag, not a scientific-integrity failure.

Next Evidence Analyst run should:

1. classify the new assembly-prototype lock-in Discovery prospectively (`ARCHITECTURE_STUDY`, further Discovery, or REJECT) without relabeling the Discovery as evidence;
2. consume the H5 audit and narrow programme interpretation to the exact registered algorithmic-work result while treating global eligibility maintenance as the identified bottleneck, not as a general no-go theorem;
3. consume the 21:30 Top-k reduction ladder and the 22:18 methodology finding;
4. consume the new Utility cross-seed result when available before any Top-k PRE_FORMAL decision;
5. if Top-k becomes support-robust, define a fresh prospective ordinary-reduction question around branch/basin switching, WTA hysteresis, and recurrent transient gain; if it remains mixed/under-supported, hold or close mechanistic escalation rather than rescue-tune.

## Stop / reframe conditions

- Top-k: no cycle 3 or PRE_FORMAL under current authority. `MIXED`/`UNDER_SUPPORTED` cross-seed support keeps the line at system characterization/HOLD; `SUPPORT_ROBUST` permits only a fresh Analyst-defined reduction question.
- Structural current question: terminal lower-layer `REJECT`; no cycle-2 rescue.
- H5: consumed terminal evidence; never rerun/reopen/rescore. Any localized-eligibility efficiency study must be a fresh object.
- SUB assembly lock-in: no automatic continuation. Fresh Analyst must prospectively define functional metric, order family, comparator, resource matching and stop rule before an Architecture Study.
- FORMAL: remain HOLD until a fresh object independently clears the central admission bar.

## Scheduler fleet health

`fleet_status: YELLOW`

- Control/Steward: `HEALTHY`.
- Evidence Analyst: `DELAYED` — live cadence exists, but durable 21:02 handoff has not yet consumed newer Literature/Methodology/Audit/SUB inputs.
- MAIN PRIMARY: `NO_OP_BY_DESIGN` with healthy durable advancement to 22:19.
- MAIN Relay: `NO_OP_BY_DESIGN`; no current continuation object.
- SUB: `EXPLORATORY_PRODUCTIVE` with durable 22:41 advancement.
- External Literature: `HEALTHY` at 21:30.
- Independent Audit: `HEALTHY` at 22:31.
- Methodology Calibration: `HEALTHY` at 22:18.
- Utility Orchestrator: `HEALTHY / ASSIGNED`; prior run completed cleanly and a new one-run assignment is queued.
- Current State Brief: `HEALTHY`.

Critical failures: none.

Operational defect: Evidence Analyst knowledge-flow lag across several newer streams. Relay-to-Control proximity remains observable but has not caused an authority or persistence failure.

Efficiency finding: lower-funnel throughput is now substantive; do not downshift Discovery/Architecture simply because FORMAL is empty. Do not spend this cycle on repository-protection deployment or scheduler-offset cleanup while science is moving safely.

## Scheduler controller

`scheduler_controller_action: NO_CHANGE`.

No non-Utility scheduler timing mutation was made. No Utility live reconfiguration was made. The new Utility work is fully expressible through the Git assignment contract, so scheduler-registry mutation would add risk without information gain. The previously observed Relay `:45` proximity is not currently causing scientific-authority failure and does not justify timing churn this run.

## Repository doctrine

Repository separation remains healthy: stable substrate on `main`; unresolved lower-layer science under `research/*`; five authoritative evidence tags and 13 legacy freeze branches unchanged. Rulesets remain absent and `main` remains unprotected, but protection deployment stays deferred under accepted human direction absent a concrete changed risk. No immutable ref/tag/branch was modified. PR #148/#149 remain non-priority until their correctness review findings are actually fixed.

## Ranked priorities — next 12–48h

1. Fresh Evidence Analyst convergence over the 21:30–22:41 Literature/Methodology/Audit/SUB updates.
2. Execute exactly one Utility cross-seed Top-k support diagnostic and return it to Analyst; no automatic escalation.
3. Prospectively classify the assembly-prototype lock-in Discovery and, only if warranted, define one bounded Architecture cycle with a functional consequence comparator.
4. Preserve the H5 terminal result while using the audit's bottleneck diagnosis to constrain any genuinely fresh future efficiency question.
5. Keep FORMAL strict and empty until a fresh high-value object exists; keep lower-funnel research active when it yields nonduplicative information.
