# SparkBrain Control Brain — 2026-09-19 18:50 JST

- role: `CONTROL_BRAIN`
- selected_slot_jst: `18:50`
- foundational_doctrine_change: `NO`
- operating_doctrine_change: `YES — FORMAL_HOLD_WITH_ACTIVE_LOWER_FUNNEL`
- programme_position: `experimental_cognitive_architecture_testbed_with_active_discovery_and_architecture_study`
- central_theory_status: `NO_HIGH_VALUE_FORMAL_OBJECT`
- scheduler_controller_action: `NO_CHANGE`

## Strategic doctrine

The foundational SparkBrain scientific frame is unchanged: a genuinely new computational-principle claim still requires a native, intervention-validated, lineage-specific causal-responsibility effect that is prospectively positive and survives ordinary history/influence and local-learning/recurrent/plastic reductions under matched information, readout, task/world/regime-label and resource privilege.

The **programme operating doctrine changes materially** from the stale 14:50 global-HOLD wording. `NO_HIGH_VALUE_FORMAL_OBJECT` means **FORMAL HOLD only**. It must not freeze DISCOVERY, ARCHITECTURE_STUDY or PRE_FORMAL when safe, bounded, nonduplicative work exists. The lower-layer funnel has now produced real information twice and is functioning as intended. SparkBrain remains a rigorous experimental cognitive architecture/testbed even when a lower-layer phenomenon ultimately reduces to an ordinary mechanism.

No fresh FORMAL object, identity, STARTED authority, official TEST access, formal preserve/scoring/evidence ref or consumed identity is authorized by this Control run.

## Human Directive dispositions

### HUMAN-20260918-001 — `ACCEPT` (unchanged)

Continue the prior disposition: outcome-independent CX comparator/runtime assets may be promoted toward `main` only after small, reviewed extraction. Do not wholesale-merge a research branch and do not promote consumed-result semantics, frozen thresholds/scorers, identity state or one-way evidence machinery merely because it is polished.

### HUMAN-20260919-002 — `ACCEPT`

Defer repository protection rules/rulesets for now. Continue reporting `main`/tag protection absence as read-only governance state, but do not make protection deployment an action item, prerequisite, or competing project unless a materially changed concrete integrity risk appears or a new human directive requests reconsideration. This does not weaken scientific immutability/no-rerun discipline.

### HUMAN-20260919-003 — `ACCEPT`

Treat research stagnation as a genuine throughput defect **when safe lower-layer work exists**, while preserving the full FORMAL integrity floor. The correct response is the four-layer funnel, bounded candidate pool and explicit promotion/rejection conditions—not weaker formal standards. Prioritize substantive Discovery/Architecture/Pre-formal work over low-value governance cleanup when the latter has negligible safety gain.

## Research funnel health

### DISCOVERY — `ACTIVE / PRODUCTIVE`

SUB completed a new bounded synthetic Discovery probe, `PLASTICITY_UPDATE_CAP_SCALING_BIAS_DISCOVERY`, on `research/exploratory-sub-plasticity-update-cap-bias-20260919@6444848f3cf9c4a92bfd6317591ac4e39ffea44f`. The result was correctly **REJECTED as a scientific candidate** because the effect reduces to ordinary implementation behavior: deterministic numeric edge ordering plus a fixed successful-update cap. It still yields useful architecture/engineering information about scaling bias. Exact-head CI `35435596401` has now completed successfully. No cycle-2 rescue is warranted.

This is positive funnel behavior: the programme generated a distinct question, learned something concrete, reduced it to an ordinary mechanism, and stopped rather than manufacturing novelty.

### ARCHITECTURE_STUDY — `ACTIVE / MAIN-owned`

`CAND-TOPK-PA-01` cycle 1 remains NON_EVIDENTIARY `PERSISTENCE_COUPLED_DELAYED_AMPLIFICATION_SIGNAL`. Evidence Analyst authorized exactly one unchanged independent-seed replication, seed `41 -> 42`, with all other DEV surfaces, perturbations, horizons, comparators and decision thresholds fixed.

Relay has now fast-forwarded the authorized research branch to `04ced2b97ed088bb2cdb086d164a86212741e601` and dispatched exact-head workflow `35435714352`. The durable Relay report stopped while the workflow was in progress. Control independently re-read the workflow after that checkpoint and observed `completed/success` on the same exact head. **Control does not inspect or interpret the scientific artifact.** MAIN/Relay must collect the already-produced artifact under the existing authority, apply only the prospectively fixed cycle-2 mapping, and stop for fresh Evidence Analyst review.

`CAND-STRUCTURAL-ORDER-PATH-01` remains queued behind this MAIN line for Architecture Study. Its Discovery result is useful system characterization, but external reduction pressure is high and must be built into any future architecture contract.

### PRE_FORMAL — `EMPTY / HOLD`

No candidate is promoted automatically from a lower-layer signal. Before PRE_FORMAL, require a fresh mechanistic/reduction question, matched privileges/resources, development-only criteria, and—where relevant—support/uncertainty adequacy. Architecture triage thresholds are not novelty thresholds.

### FORMAL — `EMPTY / HOLD`

No formal object currently clears the central admission bar. The lineage-responsibility residual remains conditional and must not be engineered as an LP01 rescue or literature-driven successor.

## Candidate pool

1. **`CAND-TOPK-PA-01` — ARCHITECTURE_STUDY, HIGH / NEAR.** Cycle 2 is the only authorized continuation. Promotion condition: replication plus fresh review; then, before any mechanistic claim, survive support robustness and ordinary hard-switch/recurrent-transient reductions. Failure/mixed/under-supported result stops or reframes without retuning.
2. **`CAND-STRUCTURAL-ORDER-PATH-01` — ARCHITECTURE_STUDY QUEUED, MEDIUM-HIGH.** First ordinary baseline is explicit age/slot/provenance state plus graph-rewrite noncommutativity/critical-pair analysis. Only matched-current-explicit-state residual functional divergence would justify a stronger question.
3. **`CAND-H7-RESP-01` — HOLD / conditional central residual.** Admit only if an independently arising native online/local responsibility-sensitive mechanism appears and can be prospectively bound against both history/influence and learning/locality reductions. Do not construct it merely to fill the queue.

Keep the normal three-cycle exploration budget. Early ordinary reduction or low-value information is a reason to stop before budget exhaustion, not to consume all three cycles mechanically.

## Strongest support / counterevidence

Strongest current support is for **architecture research value and process quality**, not a new computational principle:

- MAIN cycle 1 found a repeatable-looking DEV architecture interaction worth one independent replication.
- SUB structural-order Discovery and the new update-cap Discovery show that the lower layers can generate and reject concrete system questions instead of idling.
- stop-after-cycle Analyst review, exact-head workflow binding and no-TEST/no-identity boundaries remain intact.

Counterevidence/reductions remain substantial:

- C19-R2 is reduced by an explicit finite-state tracker.
- PD01 remains safest as `NO_REGISTERED_LONG_LAG_SIGNAL + COMPARATOR_NOT_WORSE`.
- NI01 remains terminal under its frozen contract, with programme wording limited to `NO_REGISTERED_INCREMENTAL_NATIVE_ADVANTAGE_OVER_WORLD_CONDITIONED_CONFIDENCE_ABSTENTION` because of comparator world-label privilege.
- H5 remains `FAIL_NO_USEFUL_WORK_REDUCTION`.
- LP01 remains closed as a formal/central candidate.
- The new SUB update-cap effect reduces to ordinary deterministic ordering plus a hard cap.

## Methodology Calibration — consumed

Newest Methodology Calibration is `MIXED_CALIBRATION`.

The four-layer funnel has materially corrected the old false-negative/overconservative allocation problem. The new local risk is the opposite: cycle-1 Top-k architecture triage counted a full positive magnitude vote from only one turnover case (`n=1 / 11 / 25` across magnitudes). This is **not** a formal-integrity failure and must not retroactively change cycle 1. Prospectively, future Architecture Study triage needs support adequacy per stratum or a robust aggregate/uncertainty-aware rule, and the existing `2.0 / 1.5` ratios must not become novelty or FORMAL gates.

Control accepts that calibration conclusion. Stop-after-cycle Analyst review remains mandatory.

## External input — Literature

Consumed the newest Literature Reduction Scout, 18:30 JST.

Material new reductions:

- hard Top-k turnover is ordinary discontinuous switching/piecewise geometry;
- large delayed amplification can arise from ordinary non-normal recurrent transient gain even in stable recurrent systems;
- same-multiset/different-order structural outcomes are ordinary graph-rewrite non-confluence/conflict territory, with critical-pair analysis as a sharper baseline.

Prospective implication for `CAND-TOPK-PA-01`: after replication/support review, the reduction ladder is `hard Top-k switching geometry -> immediate boundary jump -> ordinary recurrent/non-normal transient gain -> only then any unexplained persistence residual`.

Prospective implication for `CAND-STRUCTURAL-ORDER-PATH-01`: `explicit age/slot/provenance state -> rewrite-rule noncommutativity / critical pairs / non-confluence -> matched-state functional consequence -> only then any unexplained history residual`.

These findings raise the mechanistic bar but **do not cancel architecture research value** and do not alter the fixed cycle-2 contract.

## External input — Independent Audit

No newer Audit supersedes the 10:32 NI01 audit. Its chain remains `ROBUST_SO_FAR`, while equal-privilege generic confidence-abstention reduction remains unestablished because the comparator uses explicit world identity/world-specific DEV thresholds. Canonical NI01 evidence remains untouched. The previously blocked world-blind sensitivity stays closed.

Audit priority remains H5 read-only claim-boundary/resource-accounting review when the Audit stream reaches it; no consumed experiment is to be rerun or rescored.

## Utility Orchestrator request bus

Two previously unhandled requests were reviewed independently:

- `METHCAL-20260919-1818-ARCH-SIGNAL-SUPPORT` -> **`ACCEPT`**. Highest immediate information gain: read-only robustness/support/denominator analysis of the existing cycle-1 artifact before lower-layer triage hardens into doctrine.
- `LIT-20260919-1830-TOPK-HYBRID-TRANSIENT` -> **`DEFER`**. Valuable and nonduplicative, but deeper mechanism decomposition should wait until replication/support status shows the signal remains worth reducing; do not spend Utility capacity or risk collision with MAIN prematurely.

Control created assignment `CTRL-20260919-1850-ARCH-SIGNAL-ROBUSTNESS`, max 1 run, expiring 22:30 JST. It is strictly read-only, NON_EVIDENTIARY, cycle-1-only, and **MAIN must not wait for it**. If safe existing artifact material is insufficient, Utility must return `BLOCKED_MISSING_SAFE_INPUT`. No live Utility scheduler mutation was needed; the existing hourly scheduler can pick up the Git assignment.

## SUB assessment

SUB is now **`EXPLORATORY_PRODUCTIVE`**, not blanket no-op. Its latest self-selected update-cap candidate was genuinely independent of MAIN, used synthetic/development-safe resources, remained NON_EVIDENTIARY, produced a concrete ordinary-reduction result, and was rejected without rescue iteration. That is exactly the desired anti-stall behavior.

The promoted structural-order candidate is no longer SUB-owned; SUB must not continue it after promotion. MAIN must not depend on SUB Discovery work.

## Programme drift assessment

The previous programme-wide HOLD language is now stale and is explicitly retired. There is **no evidence-integrity drift**: lower-layer work remains NON_EVIDENTIARY; consumed identities/evidence are untouched; TEST remains forbidden; formal admission remains strict.

The desirable drift is allocation-level: more safe lower-layer information generation, explicit candidate budgets, early ordinary reduction and disciplined stop/review. The undesirable drift to guard against is lower-layer signal labels becoming de facto novelty gates without support calibration.

## Repository doctrine / promotion

Repository separation is healthy enough for the current research phase:

- `main@ebed6abfa941c83b36d2e3bddd04e7c5fb0dbe9d` remains stable substrate;
- active Top-k and SUB exploratory work are correctly off-main under `research/*`;
- five authoritative annotated `evidence/*` tags remain the formal evidence set; no fresh formal/sealed/tag-freeze authority was observed;
- immutable/consumed evidence remains untouched.

`main` and authoritative namespaces remain unprotected, but under accepted `HUMAN-20260919-002` this is **read-only deferred governance state**, not a current implementation priority or research prerequisite absent a new concrete risk.

PR #148 and #149 remain open, mergeable and unmerged. They are not current research priorities. Outcome-independent neutral substrate remains eligible for small reviewed promotion; current Top-k/structural-order/update-cap scientific semantics and diagnostics remain research-scoped.

## Ranked priorities — next 12–48h

1. **Complete the already-authorized `CAND-TOPK-PA-01` cycle-2 collection/classification through MAIN/Relay, then STOP for fresh Evidence Analyst review.** Control observed exact-head workflow success but does not pre-interpret the artifact.
2. **Consume the one-run Utility support-robustness result** before any PRE_FORMAL consideration of the Top-k line; keep it methodology-only and nonblocking for MAIN.
3. **If Top-k replication remains informative, require ordinary hard-switch + recurrent transient-gain reduction before mechanistic-distinctness claims.** Reconsider the deferred Literature Utility request only after fresh Analyst review/support diagnostics.
4. **Advance `CAND-STRUCTURAL-ORDER-PATH-01` to its queued Architecture Study only after MAIN cycle 2 reaches the Analyst boundary**, with explicit-state/critical-pair/non-confluence baselines prospectively fixed.
5. **Keep SUB on one bounded independent Discovery question per run, or no-op with a concrete observable-level reason.** Reject early when ordinary implementation mechanics explain the observation.
6. Keep FORMAL/H7 on HOLD until a truly independent native responsibility-sensitive object appears.

## Stop / reframe conditions

- Never rerun, retune, rescore or reopen consumed C19/PD01/NI01/H5 identities.
- Do not alter cycle-1 classification retroactively because of the new support-calibration concern.
- Do not alter cycle-2 contract from its outcome; every valid cycle-2 outcome stops for Analyst.
- If Top-k cycle 2 fails to replicate, is mixed, or robustness shows the signal is sparse/denominator-driven, retain only bounded architecture value and do not rescue-tune.
- If Top-k amplification is largely explained by hard-switch geometry plus ordinary recurrent transient gain, close mechanistic novelty for that phenomenon while preserving architecture characterization.
- If structural path dependence is explained by explicit rewrite-relevant state and critical pairs/non-confluence with no residual functional consequence, keep it as architecture behavior, not new mechanism.
- No lower-layer result becomes FORMAL evidence without a fresh prospectively bound formal object and full one-way integrity path.

## SCHEDULER FLEET HEALTH

`fleet_status: YELLOW`

| Active SparkBrain stream | Classification | Finding |
| --- | --- | --- |
| Control & Repository Steward | `HEALTHY` | Durable role-specific governance handoffs continue; this run is Control only. |
| Evidence Analyst | `DELAYED` | 18:20 handoff is scientifically current and authorized cycle 2, but execution/persistence is materially later than its nominal hourly boundary. No downstream authority error resulted. |
| MAIN PRIMARY | `HEALTHY` | Productive Architecture work; correctly stopped after cycle 1 and obeyed fresh Analyst authority. |
| MAIN Relay | `DELAYED` | Correctly dispatched fixed cycle 2, but its run again spilled into the Control window; workflow has since completed successfully. |
| SUB | `EXPLORATORY_PRODUCTIVE` | New independent Discovery produced a useful reduction/rejection; exact-head CI completed successfully. Timing was late, but scientific boundaries held. |
| External Literature | `HEALTHY` | 18:30 stream advanced with genuinely new reduction baselines and was consumed by Control. |
| External Audit | `HEALTHY` | Latest NI01 audit remains current by role cadence; no contradictory newer Audit is present. |
| Methodology Calibration Auditor | `HEALTHY` | First substantive calibration output exists and identified a concrete lower-layer support defect without weakening FORMAL integrity. |
| Utility Orchestrator | `HEALTHY` | Previously IDLE; one bounded read-only assignment is now active for the next normal run. No live scheduler reconfiguration. |
| Current State Brief | `HEALTHY` | Read-only state reporting continues; no material integrity defect observed. |

Critical failures: none.

Operational defects:
- Relay `:45` continues to crowd/overlap the `:50` Control/Steward observation window.
- Evidence Analyst and SUB have shown meaningful schedule latency, although durable handoffs still arrived before dependent scientific decisions.
- Utility assignment is newly issued and not yet executed; it is not a MAIN dependency.

Efficiency findings:
- the main prior efficiency defect—programme-wide no-op under FORMAL HOLD—is materially improved;
- lower-layer work is now producing promotion, rejection and ordinary reduction rather than utilization-only activity;
- ruleset/protection cleanup is explicitly deprioritized under accepted human direction absent new concrete integrity risk;
- Utility is now used for a narrowly bounded read-only methodology diagnostic rather than duplicating MAIN/SUB work.

### Scheduler controller

`scheduler_controller_action: NO_CHANGE`

The Relay `:45 -> :40` offset remains a plausible collision improvement, but this is not the right run to mutate it: the lower-layer research pipeline has just become active, Relay has an active cycle-2 collection boundary, the overlap has not caused a scientific-authority failure, and accepted human direction prioritizes meaningful research throughput over low-value control-plane cleanup. No non-Utility scheduler definition was changed.

The Utility scheduler also remains unchanged. Git assignment is sufficient, so exceptional live Utility reconfiguration is unnecessary.

## Direction to Evidence Analyst

- Treat the old programme-wide HOLD as retired; keep FORMAL HOLD but actively manage the lower-layer candidate pool.
- After MAIN/Relay persists cycle 2, review it fresh under the already-fixed outcome mapping. Do not permit same-run redesign or retuning.
- Incorporate Methodology Calibration prospectively: preserve cycle-1 classification, but require support/uncertainty adequacy before any PRE_FORMAL promotion and do not reuse architecture cutoffs as novelty thresholds.
- Incorporate Literature reductions prospectively: hard-switch geometry + recurrent/non-normal transient gain for Top-k; explicit rewrite state + critical-pair/non-confluence for structural order.
- Keep candidate count small and apply the three-cycle budget as a ceiling, with early rejection when ordinary reductions explain the observation.
- Treat Utility result `CTRL-20260919-1850-ARCH-SIGNAL-ROBUSTNESS` as NON_EVIDENTIARY methodology input only; MAIN must not wait for it.
- Reconsider `LIT-20260919-1830-TOPK-HYBRID-TRANSIENT` only if replication/support review leaves a reduction question worth pursuing.
- Preserve all consumed C19/PD01/NI01/H5 evidence/identity boundaries and the blocked NI01 post-terminal sensitivity closure.

## Persistence scope

This Control run updates only designated Control Brain latest/state/history on `ops/control-brain-handoff`, plus the explicitly authorized Utility request-bus decision and assignment records on `ops/utility-orchestrator-requests`. No experiment, consumed identity, immutable evidence ref, research PR merge, research result, scheduler definition, scheduler registry, ruleset, or protection setting is changed.
