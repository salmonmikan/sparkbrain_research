# SparkBrain Control Brain History — 2026-09-20 06:50 JST

- role: `CONTROL_BRAIN`
- selected_slot_jst: `06:50`
- role_inference_required: `false`
- foundational_doctrine_change: `NO`
- operating_doctrine_change: `PROSPECTIVE_HANDOFF_BINDING_GUARD_ADOPTED`
- programme_position: `FORMAL_HOLD_WITH_ACTIVE_LOWER_FUNNEL`
- scheduler_controller_action: `NO_CHANGE`
- fleet_status: `YELLOW`

## Human Directive disposition

No directive is new or materially changed. `HUMAN-20260918-001`, `HUMAN-20260919-002`, and `HUMAN-20260919-003` remain `ACCEPT / UNCHANGED`: promote only stable outcome-independent reusable substrate; defer rulesets absent a materially changed concrete immutable-ref risk; and prioritize substantive Discovery/Architecture/Pre-formal throughput while preserving the strict FORMAL hard floor.

## Scientific position

Central novelty doctrine is unchanged. No current object clears the FORMAL bar for a native prospectively positive intervention-validated lineage-specific causal-responsibility effect surviving ordinary history/provenance/actual-causality and local-learning/recurrent/plastic reductions under matched privilege/resources.

The latest lower-funnel evidence continues to support architecture/testbed value without novelty inflation. The active Refractory question reduces first through ordinary signed-current aggregation, explicit refractory discard/defer/clamp/integrate policy, retained state, and post-refractory sensitivity. The fresh SUB delayed-outcome observation reduces to one mutable pending activation plus one mutable pending action slot overwritten by an intervening decision; `learn_outcome` carries no episode/decision identity.

## Funnel state

`DISCOVERY`: `EXPLORATORY_PRODUCTIVE`. SUB completed `OUTCOME_CREDIT_SLOT_OVERWRITE_DISCOVERY_CYCLE1` on `83eb00212eb9c877e8217c44dfb35a5563f04626`; workflow `35471236612` succeeded. Immediate A outcome credits A, but after A->B pending overwrite the delayed A-labelled outcome credits B. This is strictly NON_EVIDENTIARY and ordinary-reduced to mutable single-slot pending state. SUB stopped and returned a promotion proposal only. Evidence Analyst must decide whether a fresh delayed/asynchronous API-attribution Architecture contract is worthwhile.

`ARCHITECTURE_STUDY`: MAIN owns `CAND-REFRACTORY-CURRENT-ACCOUNTING-01` on `4ac9aeead78ec8d053291f922096fab7e31f6070`. Exact-head Architecture workflow `35470259892` and ordinary CI `35470259884` both completed failure before outcome; compile/lint failed and the outcome-bearing architecture-study job did not run. There is no valid terminal result. MAIN may apply only a prospectively allowed science-invariant mechanical fix, exact-head rebind, and CI retry under the unchanged contract. Any semantic/protocol/current/timing/probe/comparator/observable/terminal-map change requirement stops to Evidence Analyst. SUB/Utility must not take this blocker.

Assembly `CAND-ASSEMBLY-CROSSCASCADE-FALLBACK-01` is HOLD after a real functional Architecture effect ordinary-reduced to segmentation/fallback policy; no same-object cycle 2. Suppression remains HOLD with completed `AMBIGUOUS_CONTRACT`; no relabel. Top-k, Temporal, topology-config remain HOLD. `PRE_FORMAL` and `FORMAL` remain empty.

## External knowledge

Fresh Literature at 06:30 is consumed by Control. Established spiking simulators expose multiple legitimate refractory input/state policies, so `absolute refractory` alone is insufficient to infer discard/defer/clamp/integrate behavior. Same-time E/I current netting and retained post-refractory state are ordinary model semantics; current-based equal-current cancellation must not be generalized to conductance-based biology. Add explicit refractory input/state policy to the prospective ordinary-reduction checklist; do not alter the already-bound cycle.

Independent Audit remains H5 `ROBUST_SO_FAR` for its exact registered aggregate algorithmic-work contract. Preserve the canonical FAIL and narrow claim boundary; no rerun or general event-routing no-go interpretation.

Methodology Calibration at 06:20 remains `MIXED_CALIBRATION`. The completed Utility handoff-binding guard passed expectations on known bad Temporal/Top-k fixtures and a faithful positive control. Control adopts the design principle prospectively for future outcome-bearing lower-funnel closures: machine-bind/check provenance, canonical machine-summary digest, exact mapped classification, and narrated decision-relevant fields before successor decisions depend on them. This is a procedural control-plane change only; no retroactive rewrite and no live scheduler/workflow integration authority.

## Utility

`EVA-20260920-0502-SUPPRESSION-STATIC-DETECTOR-CONSISTENCY` -> `ACCEPT`. The completed suppression artifact's `restore_restores_original_base_threshold=false` appears inconsistent with the exact bound source and may reflect a brittle literal detector. This is a bounded read-only methodology question distinct from handoff binding and independent of active MAIN/SUB work.

Assignment `CTRL-20260920-0650-SUPPRESSION-STATIC-DETECTOR-CONSISTENCY` issued for exactly one run, expiring 09:00 JST, evidentiary status `NON_EVIDENTIARY_METHODOLOGY_DIAGNOSTIC`. It may inspect only completed suppression cycle-1 artifact/contract/harness/workflow metadata and exact bound source. It may not rerun, probe, repair, relabel `AMBIGUOUS_CONTRACT`, access TEST/formal/consumed raw, mutate research/main/immutable refs, choose a successor, or mutate schedulers.

Utility decision commit: `5d72eca7f3011cee1cc3b5971a7f19f2e2881c17`.
Utility assignment commit: `dcb7dc021b09c7ec0713d99b8c2f8168169ac8af`.

## Fleet health

`fleet_status: YELLOW`; critical scientific/integrity failures: none.

- Control/Steward: `HEALTHY`.
- Evidence Analyst: `HEALTHY`; fresh Literature/SUB/MAIN-failure inputs await its next review.
- MAIN PRIMARY: `FAILED_OR_INCOMPLETE` operationally due the pre-outcome compile/lint blocker; no scientific terminal exists.
- MAIN Relay: `DELAYED`; durable state remains `WAITING_EXTERNAL` after workflow failure, pending post-failure reconciliation.
- SUB: `EXPLORATORY_PRODUCTIVE`.
- Literature: `HEALTHY`.
- Independent Audit: `HEALTHY`.
- Methodology Calibration: `HEALTHY`.
- Utility: `HEALTHY / ASSIGNED`.
- Current State Brief: `HEALTHY` based on the previously observed read-only stream.

Operational defects: MAIN exact-head pre-outcome compile/lint failure; Relay post-failure durable reconciliation pending; suppression machine-fact extractor fidelity unresolved under one bounded Utility audit. These are not cadence failures.

`scheduler_controller_action: NO_CHANGE`. No scheduler or registry mutation occurred. Increasing cadence would not resolve the object-local blocker; Utility can consume the Git assignment at its existing cadence.

## Repository doctrine

Fresh `main` remains `ebed6abfa941c83b36d2e3bddd04e7c5fb0dbe9d`, unprotected. Authoritative annotated `evidence/*` tags remain exactly five. Legacy `freeze/*` branches remain thirteen; tag-based `freeze/*`, `formal/*`, and `sealed/*` remain zero. No consumed identity, STARTED/control ref, raw preserve ref, or authoritative evidence tag changed. Ruleset deployment remains deferred because no new immutable-ref incident occurred.

## Ranked priorities

1. MAIN/Relay reconcile the Refractory exact-head pre-outcome blocker; only science-invariant mechanical repair is allowed, otherwise STOP to Analyst.
2. Evidence Analyst consumes the 06:30 Literature and the fresh SUB delayed-outcome Discovery, and decides whether the latter merits one fresh Architecture/API contract.
3. Utility performs exactly one suppression static-detector consistency audit; its result may inform future successor interpretation but cannot repair/relabel the completed outcome.
4. Use the validated handoff-binding design prospectively for future decision-relevant lower-funnel closures, keeping upstream extractor semantic validation distinct.
5. Preserve FORMAL HOLD and all consumed/immutable evidence until a genuinely admissible fresh object appears.

## Direction to Evidence Analyst

Treat the failed Refractory runs as pre-outcome mechanical blockers, not a scientific terminal. Consume the new refractory Literature reduction before any successor decision. Review `OUTCOME_CREDIT_SLOT_OVERWRITE_DISCOVERY_CYCLE1` strictly as NON_EVIDENTIARY and, if promoted, define intended immediate-only versus delayed-outcome semantics, supported interleaving scope, identity-bound comparator, and fixed downstream observable prospectively. Consume the suppression detector audit without relabeling `AMBIGUOUS_CONTRACT`. Use the prospectively adopted handoff-binding design for future successor decisions without rewriting historical outcomes. Keep substantive scientific gates unchanged and preserve consumed/immutable evidence exactly.
