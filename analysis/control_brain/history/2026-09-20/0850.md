# SparkBrain Control Brain History — 2026-09-20 08:50 JST

- role: `CONTROL_BRAIN`
- selected_slot_jst: `08:50`
- role_inference_required: `false`
- foundational_doctrine_change: `NO`
- operating_doctrine_change: `NO_NEW_CHANGE`
- programme_position: `FORMAL_HOLD_WITH_ACTIVE_LOWER_FUNNEL`
- fleet_status: `YELLOW`
- scheduler_controller_action: `NO_CHANGE`

## Human Directives

No directive changed materially. `HUMAN-20260918-001`, `HUMAN-20260919-002`, and `HUMAN-20260919-003` remain `ACCEPT / UNCHANGED`: promote only outcome-independent reusable substrate, defer protection/rulesets absent a changed concrete immutable-ref risk, and actively use lower research layers without weakening FORMAL integrity.

## Strategic result

The central novelty bar is unchanged and no current object qualifies for FORMAL admission. The unresolved residual remains a native, prospectively positive, intervention-validated lineage-specific causal-responsibility effect surviving ordinary history/provenance/actual-causality and local-learning/recurrent/plastic reductions under matched privilege/resources.

MAIN/Relay completed `CAND-V05-DELAYED-OUTCOME-ATTRIBUTION-01` Architecture cycle 1 at exact head `c79c5434222a6d018931dc1752e7f0074c23854b`; CI `35475862604` succeeded. Prospectively fixed terminal: `IMMEDIATE_ONLY_USAGE_BUT_PUBLIC_CONTRACT_UNSPECIFIED`. All bound observed callers use `learn_outcome` immediately after the relevant `process_episode`; pending credit state is mutable/single-slot; no episode/decision identity is carried; no explicit immediate-only or delayed/interleaved public contract was found. This is NON_EVIDENTIARY Architecture/API information only. No dynamic interleaving continuation, cycle 2, PRE_FORMAL or FORMAL authority is created.

SUB independently completed `HOMEOSTASIS_RECEPTOR_DEAD_MASK_DISCOVERY_CYCLE1` at exact head `8a2fa2efa7ac13ea7a1a835c14e8ea0f8013d71f`; CI `35476878641` succeeded. Receptor-only spikes kept production `dead=false` while the internal reservoir was silent; empty and receptor-filtered controls became `dead=true`. This reduces to all-field homeostatic spike accounting and absent receptor filtering. It is NON_EVIDENTIARY observability/population semantics, not novelty. SUB stopped after one cycle and returned only a promotion proposal.

Evidence Analyst should first close the delayed-outcome static terminal, then classify the homeostasis Discovery and re-rank it against queued `CAND-ASSEMBLY-MATURE-CAPACITY-LIFECYCLE-01`. Do not automatically execute the queue or parallelize MAIN Architecture objects. Any homeostasis Architecture successor must prospectively bind intended population semantics, supported consumer relevance, and a matched receptor-aware comparator. Any delayed-outcome dynamic successor requires a genuinely fresh prospective question establishing supported/scientifically meaningful delayed/interleaved use.

Refractory remains HOLD after `FUNCTIONAL_REFRACTORY_ACCOUNTING_EFFECT`; ordinary refractory policy/state-retention reductions remain sufficient. Suppression remains historical `AMBIGUOUS_CONTRACT`; Utility confirmed a static machine-fact extractor false negative but did not alter the historical terminal. Top-k, Temporal, H5, NI01 and other answered/consumed lines remain HOLD/do-not-touch absent fresh objects.

## External / Methodology inputs

Literature 06:30 is consumed by Control and Evidence Analyst. It reinforces explicit refractory discard/defer/clamp/integrate policy, retained-state behavior, signed-current aggregation, and the boundary between current-based engineering abstraction and conductance-based biological interpretation. No novelty threshold changes.

Independent Audit remains H5 `ROBUST_SO_FAR`; canonical H5 FAIL remains valid for the exact aggregate algorithmic-work metric, with global all-edge eligibility maintenance an architecture bottleneck rather than a general event-routing no-go theorem.

Methodology Calibration 08:20 remains `MIXED_CALIBRATION`: scientific hard floor and novelty/comparator thresholds stay unchanged; the remaining defect is procedural machine-fact extractor fidelity. Continue prospective structural/AST/typed/runtime validation and machine-bound artifact-to-handoff fidelity. Historical outcomes remain immutable.

## Utility

No unhandled Utility request exists. All three 2026-09-20 requests have prior Control dispositions and completed assignments. The suppression assignment result already recorded `COMPLETED / run_count=1`, while `assignment/current.md` remained stale at `ASSIGNED / active_assignment=true / run_count=0`. Control normalized only that lifecycle pointer to `COMPLETED / active_assignment=false / run_count=1` at commit `7321568e67b1c17ef37e028e342ae0ace08c5f94`. No scientific content, historical result, scheduler, or assignment authority was changed. No new Utility assignment was created; Utility is idle.

## Fleet

`fleet_status: YELLOW`; critical scientific/integrity failures: none.

- Control/Steward: `HEALTHY`.
- Evidence Analyst: `HEALTHY`; fresh MAIN/SUB terminals await next review normally.
- MAIN/Relay: `HEALTHY`; delayed-outcome cycle completed and durable STOP persisted.
- SUB: `EXPLORATORY_PRODUCTIVE`.
- Literature: `HEALTHY`.
- Independent Audit: `HEALTHY`.
- Methodology Calibration: `HEALTHY`.
- Utility: `HEALTHY / IDLE` after pointer normalization.
- Current State Brief: `UNKNOWN` in this run because its live handoff was not independently read; it is not an observed dependency of the active path.

Remaining operational concern: decision-relevant machine-fact extraction has a confirmed historical false-negative mode, so fact-type-appropriate prospective checks remain required. Fresh Analyst review latency is normal and not stale.

No scheduler mutation is justified. Current throughput is substantive and ownership-clean; cadence churn would not increase information gain.

## Repository doctrine

Fresh `main` remains `ebed6abfa941c83b36d2e3bddd04e7c5fb0dbe9d`, unprotected. Annotated `evidence/*` tags remain 5; legacy `freeze/*` branches remain 13. No new FORMAL/sealed/evidence identity appeared and no consumed/immutable ref changed. PR #148/#149 remain open and unmerged. Protection/ruleset deployment remains deferred under accepted human direction because no concrete immutable-ref incident occurred.

## Top priorities

1. Evidence Analyst closes delayed-outcome cycle 1 and consumes the new homeostasis Discovery.
2. Re-rank queued Assembly lifecycle versus homeostasis observability by supported-system relevance/information gain; one MAIN Architecture object at a time.
3. No delayed-outcome dynamic probe without fresh prospective authority and supported contract relevance.
4. Continue structural/typed extractor and handoff-binding discipline prospectively; never rewrite historical outcomes.
5. Keep Utility idle until a new high-information request exists and keep PRE_FORMAL/FORMAL empty until a genuinely admissible object appears.

## Integrity

No experiment, scientific workflow dispatch, identity consumption, research merge, immutable evidence mutation, consumed-candidate retuning, or scheduler mutation occurred. The sole write outside the Control handoff was the authorized Utility lifecycle-pointer normalization above.
