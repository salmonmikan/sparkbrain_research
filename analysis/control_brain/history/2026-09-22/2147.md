# SparkBrain Control Brain — R36

- schema_version: `2`
- generation_id: `CTRL-20260922T214728+0900-R36-C8F31D72`
- produced_at: `2026-09-22T21:47:28+09:00`
- producer_run_id: `control-brain-auto-20260922T2147JST-R36`
- authority_scope: `CONTROL_BRAIN_STRATEGY_GOVERNANCE_ONLY_NO_SCIENTIFIC_EXECUTION`
- supersedes_generation_id: `CTRL-20260922T175240+0900-R35-A6C4E219`
- programme_position: `FORMAL_HOLD_WITH_H7_R2_INFORMATION_LEVEL_HOLDOUT_EXPOSURE_PENDING_FRESH_ANALYST_REVIEW`
- fleet_status: `YELLOW`
- scheduler_controller_action: `NO_CHANGE`

## Authoritative refresh and one-way floor

Independent refresh keeps stable `main` at `ebed6abfa941c83b36d2e3bddd04e7c5fb0dbe9d`. The five authoritative annotated `evidence/*` tag objects remain unchanged and no immutable-ref incident is observed. Human Directives through `HUMAN-20260922-008` remain active/current on `ops/human-directives@d9efed2d58939f9cc8675b0a35392ff6f2134900`.

The hard one-way floor remains intact. Control authorizes no consumed-FORMAL rerun/retune/rescore, no evaluator/held-out leakage, no immutable/frozen/formal/sealed/evidence mutation or retargeting, no post-FORMAL rescue change, and no historical PASS/FAIL rewrite.

## H7 — R2 improves exact binding but does not supply an unexposed holdout

Control adopts Evidence Analyst `EVA-20260922T210223+0900-R75-D7F2A9C1@0e4780840dda4e1483645f442ec055025b55c333` as the current lifecycle authority. H7 remains `MECHANISM / RESULT_EXPOSED_DEVELOPMENT / ACTIVE`, `preformal_eligible=true`, readiness `READY`, with fresh FORMAL authority still **0**. The current science revision is `H7-FORMAL-R2-INPUT-SPLIT-BINDING-AND-PREIDENTITY-REVALIDATION`: fit=`train`, calibration=`dev`, FORMAL evaluation=`test`, and `smoke` is forbidden as a result-bearing FORMAL input.

MAIN cycle 8 materialized the R2 contract and implementation/preidentity machinery on `research/main-h7-formal-r2-input-split-r75-cycle8@80b88cd49fa5b9f5535feba27a75ebd3d4912406`; generic CI succeeded while the dedicated preidentity-manifest workflow failed. The exact failure cause has not been established by Control and must not be guessed or silently repaired. No FORMAL identity, `STARTED`, protected evaluation, result-bearing FORMAL execution, official scoring, or scientific preserve/evidence mutation occurred.

Literature R31 introduces a material preidentity FORMAL-integrity blocker. The public R2 contract fixes the proposed evaluation seeds, world assignment, step count, and split, while the public deterministic generator can reconstruct both evaluation observations and target labels before any FORMAL identity exists. Therefore exact source/input binding and protected workflow access do **not** establish an information-level unexposed holdout. There is no evidence that anyone actually adapted to those target labels and no consumed H7 FORMAL result is invalidated, but the current surface cannot be described as an unseen/hidden holdout.

Control therefore tracks three distinct integrity dimensions:

- exact input identity: `BOUND/IMPROVED_BY_R2`
- workflow access control: `OFFICIAL_PROTECTED_EVALUATION_NOT_ACCESSED`
- information-level holdout exposure: `PUBLICLY_RECONSTRUCTIBLE_NOT_UNEXPOSED`

FORMAL identity creation, `STARTED`, protected evaluation, and result-bearing dispatch remain **STOP**. R2 must remain unchanged as a development/preidentity artifact. Seed/evaluation-surface policy is science-affecting; it may not be repaired in-place after this information became known. Before any H7 FORMAL identity, Evidence Analyst must freshly adjudicate R31 and, if the intended claim requires genuinely unseen evaluation, create an explicit versioned R3/fresh evaluation surface whose evaluation entropy is concealed until after source/contract binding and whose key is not exposed before immutable raw preservation. A public fixed surface could only be considered under an explicitly different, sharply capped claim with fresh Analyst/Methodology justification; Control does not authorize that as FORMAL here.

Post-R31 work on R2 is restricted to clearly science-invariant plumbing/diagnosis that cannot change scientific outputs or evaluation semantics. Any output-affecting semantic repair requires fresh versioning and Analyst authority.

PF-R1 `raw.json` and `summary.json` also remain a hard preidentity provenance gate: the already-produced exact bytes must be durably preserved from the existing artifact without rerun, with known hashes verified. Retrieval failure must fail closed; PF-R1 may not be reconstructed by rerunning it.

## Candidate 33 — fresh SYSTEM Architecture result pending Analyst lifecycle review

Evidence Analyst R75 still records candidate 33 `CAND-EQUIV-AUDITABLE-RAW-PROVENANCE-01` as `SYSTEM / OPEN_DEVELOPMENT / HOLD_METHOD_LIMITED / NONTERMINAL_HOLD / QUEUED`, because the GitHub-hosted environment could not satisfy the frozen namespace contract.

After R75, SUB generation `SUB-20260922T214353+0900-ARCH-C33-CONFORM-R75-D7F2A9C1` produced a material non-evidentiary Architecture result on a newly verified compatible bounded Linux runtime without changing the frozen candidate code/controller. Clean-pair verification reached `AUDITABLE_RAW_MATCH / VERIFIER_RECOMPUTED_RAW_BYTES`; the intended mismatch and fail-closed provenance controls also triggered as designed. This is SYSTEM characterization only, not scientific/semantic equivalence evidence and not PRE_FORMAL/FORMAL authority.

Control does **not** silently change the canonical lifecycle before fresh Analyst review. The official R75 funnel therefore remains authoritative for counts. Analyst should independently validate the SUB provenance/reproduction and, if sound, normally lift the method hold and terminalize the **current SYSTEM object** as `TERMINAL_FOR_CURRENT_OBJECT / NOT_QUEUED`. Do not create a low-value runner-placement successor merely to manufacture activity; a fresh SYSTEM successor would require independent information gain. Candidate 32 remains terminal throughout.

## Funnel / development state

Official R75 metrics remain:

- canonical candidates: `33 = MECHANISM 13 / SYSTEM 20`
- classification completeness: `33/33`
- terminal state: `ACTIVE=1 / NONTERMINAL_HOLD=1 / TERMINAL_FOR_CURRENT_OBJECT=31`
- PRE_FORMAL eligible: `1`; READY: `1`
- result-bearing PRE_FORMAL active: `0`
- fresh FORMAL authority: `0`
- development phases: `OPEN_DEVELOPMENT=3 / RESULT_EXPOSED_DEVELOPMENT=30 / canonical CONSUMED_ONE_WAY=0`
- official consumed one-way identities: `7`
- rolling autonomous scientific selections: `MECHANISM / SYSTEM / SYSTEM = 1/3`

`READY` remains test-readiness, not prior scientific victory; no `HIDDEN_SECOND_FORMAL_GATE` is detected. Cycle 3 remains reassessment rather than automatic terminalization. H7 R2 is a compliant result-exposed science-affecting revision, but R31 now requires another prospective adjudication before FORMAL identity. Candidate 33 remains a distinct SYSTEM successor, not same-object rescue or SYSTEM→MECHANISM laundering.

## Phenomenon-first shadow

Mode remains `PREFETCH_SHADOW`; the last scan was R73. R75 correctly skipped scanning under throttle and the standby queue is `0/3`. A normal scan is eligible at R76 or later, but it may retain at most one independent proposal and must not distract from the H7 integrity review. R31 is H7-specific and is not itself a new independent phenomenon surface. Shadow remains noncanonical/non-authorizing and excluded from candidate/quota/readiness metrics.

## Utility

`UTIL-20260922-1648-PFR1-DEVELOPMENT-PROVENANCE-PRESERVE` remains approved as bounded NON_EVIDENTIARY provenance preservation, but no completion is observed on `ops/utility-orchestrator-requests@3628f4edb9938bc906e8cd4afa34bf3301641ca2`. Allowed scope remains retrieval of the existing PF-R1 artifact without rerun, exact hash verification, and durable storage of exact `raw.json`, `summary.json`, and provenance manifest in an explicitly non-evidentiary location. This is a hard gate before FORMAL identity, but not a hidden dependency for non-result-bearing implementation diagnosis.

## Methodology / Literature implications

Methodology R66 remains `MIXED_CALIBRATION` and predates the R31 reconstructibility finding. Its positive points remain: development-phase calibration, R2 split-versioning discipline, cycle-3 reassessment, and no hidden second FORMAL gate. Its tightening points remain durable development bytes, exact future FORMAL binding, preserve-before-read, comparator verifier boundaries, and protected/adaptive evaluation validity.

Next Methodology must explicitly consume R31 and audit information-level reconstructibility, post-exposure repair/manipulation risk, and the minimum prospective concealed-entropy design required for any claim that relies on an unseen holdout. No R31 requirement may be retroactively grafted onto already-observed PF-R1 or R2 as a rescue.

## Human directive dispositions

- `HUMAN-20260922-005`: accepted/applied prospectively; development iteration remains flexible with provenance and versioning while consumed FORMAL remains one-way.
- `HUMAN-20260922-006`: documentation-review issue only; no isolated root-version bump by Control.
- `HUMAN-20260922-007`: accepted; SUB question-formation remains noncanonical until normal admission.
- `HUMAN-20260922-008`: `MODIFY`; Fast Forge concept remains acceptable only as NON_EVIDENTIARY/NONCANONICAL work with fresh later canonical admission. `SB-SCHED-FORGE-001` remains `USER_APPROVAL_REQUIRED`.

## Scheduler health

Fleet is `YELLOW`. Live definitions remain authoritative with `159` active SparkBrain run opportunities/day, exact deterministic collision count `0`, role/ownership collision count `0`, and definition drift `true` against the stale repository registry. Relay remains disabled and Current State Brief retains the approved `01:55/09:55/13:55/17:55/21:55 JST` phase.

Broad recent start delays are now material enough to classify `scheduler_cadence_failure_detected=true`: approximately Control 10m, Analyst 17.5m, MAIN 20.8m, Methodology 13.4m, Utility 12m, SUB 13.3m, External 13.5m, Brief 15.5m. Because delays are fleet-wide rather than one deterministic collision, a small timing offset is not a justified SAFE_AUTO_FIX. Useful scientific generations continue, so RED is not warranted.

`SB-SCHED-LATENCY-001` remains `USER_APPROVAL_REQUIRED / RECOMMEND`: if approved, reduce cadence density only for lower-information roles while preserving the explicit hourly Control/Steward mapping and Brief `:55`, then observe latency. `SB-SCHED-FORGE-001` remains open/unchanged. No scheduler mutation is performed in this run.

## Direction to Evidence Analyst / MAIN / Methodology / Utility

1. Before any H7 FORMAL identity, consume Literature R31 and explicitly adjudicate exact input identity, workflow access control, and information-level exposure separately.
2. Preserve H7 R2 unchanged. If unseen/protected evaluation is required, design a fresh/versioned R3 surface with concealed post-binding entropy; do not repair seeds/evaluation semantics in-place.
3. MAIN remains STOP before identity/result-bearing work. Diagnose the failed preidentity workflow only; do not make output/science-affecting repairs without Analyst versioning.
4. Verify PF-R1 exact-byte preservation before any FORMAL identity; never rerun PF-R1 to reconstruct missing bytes.
5. Freshly lifecycle-review candidate 33 using the new SUB Architecture result. Terminalize only after independent validation; do not manufacture a runner-placement successor.
6. Methodology must consume R31 and audit reconstructible-target leakage and outcome-responsive manipulation boundaries.
7. Preserve shadow throttle and non-authorizing separation; a normal R76+ scan may occur only within the stated quality floor.
8. Keep every consumed identity and immutable evidence ref untouched.

R36 executes no experiment, no FORMAL identity consumption, no result-bearing scientific workflow dispatch, no research PR merge, no immutable/formal/sealed/evidence mutation, no scheduler mutation, no ruleset/protection mutation, and no historical scientific-result rewrite.