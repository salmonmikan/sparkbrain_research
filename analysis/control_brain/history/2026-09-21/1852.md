# SparkBrain Control Brain — 2026-09-21 18:52 JST

- schema_version: `2`
- generation_id: `CTRL-20260921T185206+0900-R24-6B4D21C8`
- supersedes_generation_id: `CTRL-20260921T145000+0900-R23-4F07AFBF`
- selected_role: `CONTROL_BRAIN`
- programme_position: `FORMAL_HOLD_WITH_ACTIVE_LOWER_FUNNEL`
- fleet_status: `YELLOW`
- scheduler_controller_action: `USER_APPROVAL_REQUIRED`
- authoritative main: `ebed6abfa941c83b36d2e3bddd04e7c5fb0dbe9d`

## Human Directive dispositions

- `HUMAN-20260918-001`: `ACCEPT`. Continue auditing stable outcome-independent comparator infrastructure for eventual main promotion; no current promotion candidate is directed.
- `HUMAN-20260919-002`: `ACCEPT`. Repository protection/ruleset deployment remains deferred absent a materially changed integrity risk; the live repository still has zero rulesets and an unprotected `main`.
- `HUMAN-20260919-003`: `ACCEPT`. Treat lower-layer research throughput as a real programme concern while preserving all one-way and prospective integrity floors. NO_OP and repeated no-delta scans do not count as scientific progress.
- `HUMAN-20260921-004`: `MODIFY`. Accept bounded Utility autonomy when no Control assignment exists, but require a fresh autonomous task record, conflict/ownership check, one task at a time, NON_EVIDENTIARY status for candidate-like outputs until Analyst/Control consumption, no use of consumed/protected identities, no claim-ceiling/readiness/Formal authority, no theory-backward quota credit, no PRE_FORMAL/FORMAL execution, and fail-closed behavior on conflict. This modification addresses hidden-MAIN-dependency, role-collision, protected-identity and post-outcome-authority risks without returning Utility to assignment-only idling.

## Authoritative repository / evidence refresh

`ops/*` mailboxes were treated only as control-plane inputs. Stable `main` remains `ebed6abfa941c83b36d2e3bddd04e7c5fb0dbe9d`. Authoritative evidence remains exactly five annotated `evidence/*` tags:

- `evidence/c19-official-v4-c19-external-v2-official-v4` @ tag object `4d6c0bd9a6c06c17352941d3fa730502e72b8540`
- `evidence/c19-r2-fsa-state-tracker-c19-r2-fsa-state-tracker-official-v1` @ `82b88f3e2ad524fed8b72300dcba46053c1f2c7e`
- `evidence/h5-event-routing-work-reduction-h5-event-routing-work-reduction-official-v1` @ `e7d99cc806206ac27ced225d4779c9fc5bb67ff5`
- `evidence/ni01-no-ignition-selective-prediction-ni01-no-ignition-selective-prediction-official-v1` @ `185b741e69ea8a0ce0d076153d36e9296a748765`
- `evidence/pd01-long-history-fading-memory-pd01-long-history-fading-memory-official-v1` @ `e4c4e6428d8ef9e09e92cae231041de0788162e2`

`formal/*=0`, `sealed/*=0`, tag-form `freeze/*=0`, legacy `freeze/*` branches=`13`, repository rulesets=`0`. No immutable evidence movement or formal/evidence mutation was observed.

## Canonical Funnel v2.1 state

Fresh Evidence Analyst R40 remains the canonical scientific authority pending review of the newer MAIN result. Canonical portfolio: `30` material candidates = `MECHANISM 13 / SYSTEM 17`; mandatory v2.1 classification is complete `30/30`. Terminal-state counts are `ACTIVE=1 / NONTERMINAL_HOLD=1 / TERMINAL_FOR_CURRENT_OBJECT=28`.

Discovery dispositions: `REJECT=2 / HOLD_SYSTEM_TERMINAL=1 / HOLD_MECHANISM_UNRESOLVED=1`; no canonical active Discovery object. Rolling safe autonomous scientific selections remain `MECHANISM / SYSTEM / SYSTEM = 1/3`. The open no-target episode `NTE-20260921-R34-POST-REPLAY-v1` has canonical check count `5`; subsequent SUB no-delta checks are not scientific selections or throughput.

Architecture is active `MECHANISM=0 / SYSTEM=1`, queued `MECHANISM=0 / SYSTEM=0`. Architecture dispositions are `REJECT=0 / HOLD_SYSTEM_TERMINAL=6 / HOLD_METHOD_LIMITED=1 / HOLD_CONTRACT_AMBIGUITY=2 / HOLD_MECHANISM_UNRESOLVED=1`. PRE_FORMAL eligible=`0`, READY=`0`, viable executable MECHANISM=`0`. Recent MAIN scientific-cycle counts are `SYSTEM=7 / MECHANISM=3`; comparable time split is unavailable and run count is not used as a time proxy. SYSTEM-over-comparable-MECHANISM exceptions remain `0`, so `SYSTEM_QUEUE_STARVING_MECHANISM=false`.

Eligibility and READY remain semantically separated. The first real READY->PRE_FORMAL transition (R33) was admitted as prospective test-readiness before outcome, not because the object had already defeated comparator/reduction/falsifier. `HIDDEN_SECOND_FORMAL_GATE=false`. Its execution remains terminal `HOLD_METHOD_LIMITED / TERMINAL_FOR_CURRENT_OBJECT / NOT_QUEUED / preformal_eligible=false` because it violated raw-before-score. No same-ID rerun, repair, rescore, redesign, or unopened-surface continuation is authorized.

## MAIN / synthetic pipeline conformance

The current canonical object is `CAND-PREFORMAL-OUTCOME-BLIND-FOUR-STAGE-PIPELINE-CONFORMANCE-01`, an `ARCHITECTURE_STUDY / SYSTEM` integrity object. Analyst R40 observed a pre-result Ruff `I001` lint failure at research head `5527ac14d4d19078a91aa40f8825d5a4f41f0a91`; all result-bearing stages were skipped. R40 prospectively allowed exactly one semantics-invariant mechanical preflight repair.

MAIN generation `MAIN-20260921T181649+0900-PRIMARY-FUNNEL21-SYSTEM-PIPECONF-R40-LINTFIX-6A3D21C8` diagnosed that exact preflight-only import-order defect, applied one import-formatting correction, re-froze the raw-generator digest before any result, and ran exact-head CI `35583071628@168883bd316985537e404c6aad3a7ac03202e28a`, which succeeded on Python 3.11 and 3.13 across install/lint/local-readiness/test/validate. Producer observation is `SYNTHETIC_LIVE_CONFORMANCE_PASS`.

Control does not canonicalize that observation scientifically. It remains NON_EVIDENTIARY SYSTEM Architecture pending fresh Analyst review. It does not upgrade the current object to MECHANISM, eligible, READY, PRE_FORMAL or evidence. MAIN stopped correctly with zero identity consumption and no same-run PRE_FORMAL/FORMAL continuation.

## SUB / anti-stall

Latest SUB `SUB-20260921T183349+0900-NOOP-R40POSTMAINPASS-6D4A21C9` failed closed with `NO_COHERENT_MECHANISM_TARGET`. This is credible: the new MAIN delta is pipeline-integrity SYSTEM work, not a mechanism-surface delta. The no-op is excluded from the 1-in-3 denominator. Repeated no-delta scans must not be presented as progress or used to manufacture a MECHANISM object.

## Methodology, Literature and Audit

Methodology R41 is `MIXED_CALIBRATION / MATERIAL_CALIBRATION_UPDATE`. It confirms that the negative shadow->mechanism admission guard is operating correctly and that repeated no-target scans are not independent scientific evidence. It predates the fresh MAIN conformance pass, so it is current on method and stale only on the newest conformance outcome.

Literature R21 contributes genuinely new prospective integrity information: within-run outcome blinding and raw-before-score are not sufficient if successive candidate generations adapt to repeated disclosure from the same protected scientific holdout. Cross-generation holdout exposure / benchmark exhaustion must be treated as a separate prospective failure mode. This does not retroactively invalidate R33 or any immutable evidence and does not affect the current synthetic conformance fixture. Future clean PRE_FORMAL/FORMAL designs should track protected-set exposure and use bounded-feedback reusable-holdout or fresh/rotated confirmatory-set controls when adaptive reuse is material.

Independent Audit R4 remains binding as a ceiling on the historical Assembly interpretation: the original targeted-loss observation is valid within its narrow scope but Assembly-specific causal attribution remains `CONFOUNDED` by active-versus-inactive comparator/load mismatch. It must not be counted as resolved Assembly-specific mechanism or H7 uplift.

## Utility governance

Control-owned Utility assignment pointer is schema-v2 `IDLE`, `active_assignment_id=null`; the prior terminal BLOCKED generation was compare-and-swap acknowledged and no replacement assignment exists. Utility's own historical state file still records its last BLOCKED run, but it does not override the Control-owned current assignment pointer.

The new Human Directive changes desired Utility semantics: assignment absence alone must no longer force IDLE. However, the currently active Utility scheduler definition remains assignment-oriented and therefore does not yet implement the directive. This is a scheduler-definition/semantic drift, not a cadence failure.

## Scheduler health

`fleet_status=YELLOW`.

- `scheduler_definition_drift_detected=true`: Utility live definition remains assignment-only while `HUMAN-20260921-004` authorizes bounded no-assignment autonomy.
- `scheduler_cadence_failure_detected=false`.
- Exact deterministic start-time overlaps detected: `0`; the Current State Brief :55 phase offset remains correctly separated from Relay :45.
- Observed recent starts where directly inferable: Analyst ~`+6s`; MAIN ~`+1m49s`; Methodology ~`+1m31s`; Literature ~`+8m`. This is not yet a persistent cadence failure.
- Role/ownership collision count: `0`.
- NO_OP/IDLE pattern: MAIN produced a useful SYSTEM conformance result; SUB repeatedly NO_OPs under the same no-target episode; Relay commonly stops at authority boundaries; Utility is IDLE under the assignment-only live definition.
- Useful-result cadence vs polling: dense polling persists, but the current window produced both a useful MAIN conformance observation and genuinely new Literature. There is insufficient evidence to reduce core cadence without risking responsiveness.
- Generation lag: expected phase lag exists. Analyst R40 and Methodology R41 predate the fresh MAIN conformance pass; fresh Analyst review is required. Literature R21 already consumed the fresh MAIN result.
- Active SparkBrain run opportunities/day across the registered fleet: `175`.
- `scheduler_change_warranted=true` only for Utility semantic alignment.
- `scheduler_controller_action=USER_APPROVAL_REQUIRED`.

### Open scheduler proposal

`proposal_id=SCHED-UTILITY-AUTONOMY-20260921-01`

- severity: `RECOMMEND`
- change_authority: `USER_APPROVAL_REQUIRED`
- problem: active Utility scheduler prompt remains assignment-only despite the new Human Directive authorizing bounded autonomous complementary work when no assignment exists.
- evidence_window: current Human Directive `HUMAN-20260921-004`, current live Utility scheduler definition, Control-owned Utility pointer IDLE, repeated Utility idle/no-assignment operation.
- proposed_change: keep the current hourly timing unchanged; update only Utility scheduler semantics so that, when no Control assignment exists, Utility may select exactly one bounded complementary NON_EVIDENTIARY task after a durable conflict/ownership check; assignment takes precedence; it must publish autonomous selection/state/evidence footprint and fail closed on conflict. It receives no candidate classification, claim-ceiling/readiness, PRE_FORMAL/FORMAL, theory-backward quota or protected-identity authority.
- expected_effect: convert some assignment-empty Utility windows into tooling/reproducibility/diagnostic/Discovery/Architecture support without becoming a hidden MAIN dependency or competing with MAIN/SUB.
- scientific_risk: scope creep, duplicate scientific outcome generation, post-outcome repair, protected-identity reuse, or de-facto scientific authority if constraints are not enforced.
- rollback: restore the current assignment-only Utility scheduler prompt; timing remains unchanged.
- user_decision_needed: approve or reject the semantic Utility prompt update.

No SAFE_AUTO_FIX is applied because changing Utility's scientific/operational prompt semantics is explicitly outside the safe timing-only authority boundary.

## Direction to Evidence Analyst

1. Independently verify MAIN head `168883bd316985537e404c6aad3a7ac03202e28a`, CI `35583071628`, and that the predecessor failure was pre-result/pre-analysis with exactly one import-formatting repair and a new prospective raw-generator digest frozen before any result-bearing stage.
2. Verify the synthetic four-stage sequence is genuinely `raw generator -> durable raw preserve/digest -> fixed scorer consuming the exact preserved blob -> scored preserve`, with no cross-stage outcome leakage.
3. If verified, close the current SYSTEM object as a terminal conformance characterization. Do not convert the same current object into MECHANISM, eligible, READY, PRE_FORMAL or evidence.
4. Preserve R33 as consumed terminal `HOLD_METHOD_LIMITED / NONCONFORMING_RAW_BEFORE_SCORE`; no rerun/repair/rescore/continuation.
5. Consume Literature R21 prospectively as a distinct cross-generation holdout-reuse / benchmark-exhaustion integrity dimension. Do not retroactively alter consumed evidence absent repository proof.
6. Preserve Audit R4's `CONFOUNDED` Assembly-specific attribution ceiling.
7. Treat Utility autonomous candidate-like outputs, if scheduler semantics are later approved, only as NON_EVIDENTIARY proposals until independent Analyst classification. They do not satisfy theory-backward quota and cannot alter claim ceiling/readiness/Formal authority.

Control mutations this run: experiments=`0`, scientific workflow dispatch=`0`, identities consumed=`0`, research PR merges=`0`, immutable/formal/evidence mutations=`0`, Utility assignments=`0`, scheduler mutations=`0`.
