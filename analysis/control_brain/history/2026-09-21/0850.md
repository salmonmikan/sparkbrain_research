# Control Brain history — 2026-09-21 08:50 JST

- schema_version: 2
- generation_id: `CTRL-20260921T085000+0900-R21-4F7C2A91`
- produced_at: `2026-09-21T08:50:00+09:00`
- producer_run_id: `control-brain-auto-20260921T085000+0900-R21-4F7C2A91`
- authority_scope: `CONTROL_BRAIN_STRATEGIC_GOVERNANCE_AND_ALLOCATION_ONLY_NO_SCIENTIFIC_EXECUTION`
- supersedes_generation_id: `CTRL-20260921T065000+0900-R20-8E4C2A71`
- programme_position: `FORMAL_HOLD_WITH_ACTIVE_LOWER_FUNNEL`
- fleet_status: `YELLOW`
- scheduler_controller_action: `NO_CHANGE`

## Human directives

All three current Human Directives remain `ACCEPT_UNCHANGED`.

- `HUMAN-20260918-001`: main promotion remains limited to stable, outcome-independent, reusable assets after independent review. No promotion is directed.
- `HUMAN-20260919-002`: repository protection/rulesets remain deferred absent a materially changed concrete integrity incident.
- `HUMAN-20260919-003`: strict FORMAL integrity remains non-negotiable; prefer meaningful lower-funnel information over low-value cleanup, but do not manufacture activity.

## Authoritative repository / immutable evidence

Direct re-fetch confirms stable `main@ebed6abfa941c83b36d2e3bddd04e7c5fb0dbe9d`, five authoritative `evidence/*` annotated tags, zero `formal/*`, `sealed/*`, or tag-based `freeze/*` refs, thirteen preserved legacy `freeze/*` branches, and zero repository rulesets. No immutable, formal, evidence, preserve, or research refs were changed by Control.

The C19-v4 immutable registered PASS remains historically valid within its registered truth-free structural-representation claim. Programme-level SparkBrain-specific novelty remains `REDUCIBLE` under the authoritative C19-R2 fixed seven-state FSA reduction. No frozen result is reopened, relabelled, rerun, or rescored.

## Evidence Analyst R30 — canonical Funnel v2.1 state

Current Analyst authority is `EVA-20260921T075832+0900-R30-9A4C2E71@773c88edcaf799140830f3d204d0f0ff82c9bcad`.

Canonical portfolio:
- material candidates: `25`
- claim ceiling split: `MECHANISM=12`, `SYSTEM=13`
- mandatory v2.1 classification completeness: `25/25`
- terminal states: `ACTIVE=1`, `NONTERMINAL_HOLD=1`, `TERMINAL_FOR_CURRENT_OBJECT=23`
- Architecture active: `MECHANISM=0`, `SYSTEM=1`
- Architecture queued: `MECHANISM=0`, `SYSTEM=0`
- Architecture dispositions: `REJECT=0`, `HOLD_SYSTEM_TERMINAL=5`, `HOLD_METHOD_LIMITED=1`, `HOLD_CONTRACT_AMBIGUITY=1`, `HOLD_MECHANISM_UNRESOLVED=1`
- PRE_FORMAL eligible: `0`
- PRE_FORMAL READY: `0`
- viable executable MECHANISM candidates: `0`
- SYSTEM-over-comparable-MECHANISM exceptions: `0`
- recent canonical MAIN Architecture cycles: `SYSTEM=5`, `MECHANISM=1`; durable comparable wall-clock split unavailable and CI duration must not be used as a proxy.

The matched-load MECHANISM successor `CAND-V05-ASSEMBLY-UNIT-CAUSAL-SELECTIVITY-MATCHED-LOAD-01` is now canonical terminal HOLD: `claim_ceiling=MECHANISM`, `preformal_eligible=false`, `preformal_readiness=NOT_READY`, `hold_class=HOLD_MECHANISM_UNRESOLVED`, `terminal_state=TERMINAL_FOR_CURRENT_OBJECT`, `queue_state=NOT_QUEUED`. Exact aggregate activity/topology matching was infeasible on the prospectively fixed supported DEV surface after exhaustive same-cardinality comparator search, and no intervention outcome was executed. Comparator relaxation, surface change, or same-object rescue remains forbidden.

R30 therefore prospectively allocated one fresh SYSTEM Architecture object, `CAND-V05-ACTION-POLICY-EVALUATION-ISOLATION-CONTRACT-01`, with `claim_ceiling=SYSTEM`, `preformal_eligible=false`, `terminal_state=ACTIVE`, `queue_state=ACTIVE`, and `system_priority_exception.used=false`. This was valid because viable executable MECHANISM count was zero; there is no `SYSTEM_QUEUE_STARVING_MECHANISM` event.

## Fresh MAIN result after R30

MAIN generation `MAIN-20260921T081624+0900-PRIMARY-FUNNEL21-SYSTEM-ACTEVAL-R30-5C7A21E4` executed only the prospectively authorized static/read-only source/docs/tests/callsite audit. It created no research branch, dispatched no workflow, ran no dynamic scientific diagnostic, and consumed no identity.

The implementation is clear: `AssemblyActionPolicy.choose()` increments visit state and sets pending state even when `explore=false`; non-learning held-out paths disable learning and exploration but still call the action policy. Top-level held-out conditions are isolated by deep-copy, while sequential episodes within a condition can advance visit/pending state. The public scientific contract does not clearly state whether held-out action choices are real policy visits allowed to mutate state or observational probes requiring policy-state immutability.

Prospectively fixed terminal reached: `MIXED_OR_UNRESOLVED_PUBLIC_CONTRACT`, mapped by the R30 contingency tree to `HOLD_CONTRACT_AMBIGUITY_AND_STOP`.

Control does not self-canonicalize this science. Direction to Evidence Analyst: independently verify the static result and, if confirmed, canonicalize the CURRENT SYSTEM object as `HOLD_CONTRACT_AMBIGUITY` with an explicit machine-readable contract-ambiguity hold reason, `TERMINAL_FOR_CURRENT_OBJECT`, `NOT_QUEUED`, `preformal_eligible=false`, and SYSTEM/not-applicable readiness. Do not run dynamic evaluation-interleaving on the same object. If a dynamic isolation discriminator is still scientifically useful, it requires a fresh candidate ID, fresh prospective contract, and fresh Analyst authority. It must not be a post-outcome SYSTEM->MECHANISM upgrade.

With this fresh completed MAIN SYSTEM cycle, the observed post-R30 delta is `+1 SYSTEM`; relative to R30's durable recent-cycle accounting that is `SYSTEM=6`, `MECHANISM=1` if the same window is extended. Comparable wall-clock split remains unavailable and must not be inferred from CI duration.

## Fresh SUB reframe / theory-backward supply

SUB generation `SUB-20260921T083213+0900-NOOP-R30REFRAME-6E2C91A4` performed no scientific selection or execution. After re-fetching the changed landscape it found no safe, bounded, nonduplicative independent MECHANISM target outside MAIN ownership and recorded:

`theory_backward_exception.code=NO_COHERENT_MECHANISM_TARGET`

with a new landscape episode `NTE-20260921-R30-POST-ASMMATCH-ACTEVAL-v1`.

The exception is credible at Control level because the Assembly matched-load current object is terminal and cannot be rescued, the action-policy semantic object is MAIN-owned, remaining stable-v0.5 surfaces are source-transparent ordinary reductions or already-terminal families, and H7 still lacks a prospectively fixed native object/comparator/resource/falsifier contract. This no-op is not a candidate and does not enter the scientific-selection denominator.

Rolling autonomous scientific selection therefore remains `SYSTEM / SYSTEM / MECHANISM = 1/3`. The 1-in-3 rule is a minimum supply floor, not a target ratio. Do not manufacture a mechanism object to increase utilization. The new no-target episode is distinct from the earlier historical episode because the matched-load landscape changed; it should be independently consumed by the next Analyst and Methodology generations rather than mechanically inherited.

## Methodology / Literature / Audit

Methodology generation `METHCAL-20260921T082500+0900-R31-8F3C6A21` remains `WELL_CALIBRATED` with a material calibration update. It positively validates the comparator-feasibility stop discipline: the matched-load object tested feasibility only, did not execute an intervention after infeasibility, and was terminalized without relaxing comparator/surface/metric. This is strong implementation evidence that `preformal_eligible` and `READY` are not duplicate guards. The first genuine `READY -> PRE_FORMAL` transition is still unobserved and `HIDDEN_SECOND_FORMAL_GATE=false`.

Literature R17's action-policy visitation result is consumed prospectively: visit counters are ordinary policy state, not passive telemetry, and evaluation semantics are part of the scientific contract. It may motivate a fresh SYSTEM successor after Analyst review, but it cannot retroactively choose semantics for the current object. Independent Audit remains unchanged: C19-v4 immutable PASS preserved, programme novelty `REDUCIBLE` after C19-R2.

## Utility / Repository Steward / scheduler fleet

Utility remains schema-v2 `IDLE`, `active_assignment_id=null`, with no new request and no unacknowledged terminal assignment. No Utility work is needed on the MAIN critical path.

Repository Steward `STEWARD-20260921T075030+0900-G3-A6C4E291` is healthy. `main` remains unprotected and repository rulesets remain absent, but no new immutable-ref incident or stable main-promotion basis exists, so the Human Directive defer policy remains unchanged.

Live scheduler/cadence verification shows Evidence Analyst, MAIN, Methodology, Utility, SUB, Relay, Control/Steward, Literature/Audit, and Brief roles enabled at their configured cadences. No scheduler collision or timing failure warrants intervention. `scheduler_controller_action=NO_CHANGE`.

## Fleet health

`YELLOW`, not RED. There is no FORMAL/immutable integrity failure, no role collision, no fresh CI failure, no canonical classification gap, and no Utility lifecycle leak. Yellow reflects dependency-aware freshness only: MAIN reached a material prospectively mapped terminal after Analyst R30 and awaits fresh Analyst canonicalization; SUB opened a fresh no-target episode after R30 and after Methodology R31, so that episode also awaits independent Analyst/Methodology consumption.

Worker assessment:
- Evidence Analyst: `HEALTHY_FRESH_DEPENDENCIES_PENDING`
- MAIN PRIMARY: `HEALTHY_BOUNDED_SYSTEM_TERMINAL_PENDING_ANALYST`
- Relay: `HEALTHY_NO_AUTHORIZED_CONTINUATION`
- SUB: `HEALTHY_CREDIBLE_NO_TARGET_NO_OP`
- Literature: `HEALTHY`
- Independent Audit: `HEALTHY_UNCHANGED`
- Methodology: `HEALTHY_MATERIAL_CALIBRATION_UPDATE`
- Repository Steward: `HEALTHY`
- Utility: `IDLE`

## Direction to Evidence Analyst

1. Independently verify MAIN R30 static audit against stable source/docs/tests/callsites and the fixed contingency tree. If confirmed, terminalize the current SYSTEM object as contract-ambiguous and not queued; do not infer either semantic contract post hoc.
2. Any dynamic evaluation-isolation test must be a fresh SYSTEM successor with new candidate ID and fresh prospective contract/authority. Do not upgrade the current SYSTEM object to MECHANISM.
3. Consume SUB's new `NTE-20260921-R30-POST-ASMMATCH-ACTEVAL-v1` no-target exception as a no-selection process record, not a candidate or denominator event; independently audit its credibility.
4. Preserve rolling theory-backward accounting at `1/3`; do not require artificial selections to raise the ratio.
5. Preserve the matched-load MECHANISM terminal stop. No same-object comparator relaxation, topology-metric redesign, new surface, or delayed intervention.
6. Preserve `eligible != READY`, with zero current eligible/READY candidates. READY remains test-readiness, not prior comparator/reduction/falsifier victory.
7. Preserve C19-v4's immutable historical PASS while programme-level novelty remains `REDUCIBLE`.

## Control integrity actions

- experiments executed: `0`
- scientific workflows dispatched: `0`
- identities consumed: `0`
- research PRs merged: `0`
- immutable refs mutated: `0`
- formal refs mutated: `0`
- evidence refs mutated: `0`
- Utility assignments created: `0`
- Utility live scheduler reconfigurations: `0`
- schedulers changed: `0`
