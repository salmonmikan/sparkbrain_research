# SparkBrain Control Brain — 2026-09-21 12:50 JST

- schema_version: `2`
- generation_id: `CTRL-20260921T125000+0900-R22-7C3A91E4`
- produced_at: `2026-09-21T12:50:00+09:00`
- authority_scope: `CONTROL_BRAIN_STRATEGIC_GOVERNANCE_AND_ALLOCATION_ONLY_NO_SCIENTIFIC_EXECUTION`
- supersedes_generation_id: `CTRL-20260921T085000+0900-R21-4F7C2A91`
- programme_position: `FORMAL_HOLD_WITH_ACTIVE_LOWER_FUNNEL`
- fleet_status: `YELLOW`
- scheduler_controller_action: `NO_CHANGE`

## Human Directive dispositions

`HUMAN-20260918-001`, `HUMAN-20260919-002`, and `HUMAN-20260919-003` remain `ACCEPT_UNCHANGED`. No main promotion is directed. Ruleset/protection deployment remains deferred: the newly observed integrity defect is PRE_FORMAL execution-order plumbing, not an immutable-ref or branch-protection incident. The throughput concern remains active, but fixing raw-before-score ordering is high-value scientific-integrity SYSTEM work rather than low-value governance cleanup.

## Independent authoritative re-fetch

Stable `main` remains `ebed6abfa941c83b36d2e3bddd04e7c5fb0dbe9d`. Authoritative tags remain `evidence/*=5`, `formal/*=0`, `sealed/*=0`, tag-based `freeze/*=0`; legacy `freeze/*` branches remain `13`. Repository rulesets independently re-fetch as `0`. No new immutable/formal/evidence identity or mutation is observed.

## Hard-floor event and containment

Evidence Analyst R34 canonicalized the first live PRE_FORMAL execution, `CAND-V05-ASSEMBLY-SET-CAUSAL-NECESSITY-DISTRIBUTIONAL-CONTROLS-01`, as `NONCONFORMING_RAW_BEFORE_SCORE`. The scientific harness scored/classified in-process before any separately durable raw-only preserve. Exact binding and CI did not cure that ordering defect. The CURRENT object is terminal `HOLD / MECHANISM / preformal_eligible=false / HOLD_METHOD_LIMITED / TERMINAL_FOR_CURRENT_OBJECT / NOT_QUEUED`.

Control accepts this containment exactly. The same ID must not be rerun, repaired, rescored, redesigned, or continued on unopened surfaces merely to obtain a clean pipeline. The future required sequence is prospectively machine-checkable `raw generator -> durable raw preserve/digest -> fixed scorer -> scored preserve`.

This is a serious PRE_FORMAL integrity nonconformance, but it did not contaminate FORMAL or immutable evidence. Fleet therefore remains YELLOW rather than RED.

## Funnel v2.1 / metrics

Canonical R34 portfolio is `29` material candidates = `MECHANISM 13 / SYSTEM 16`, mandatory classification completeness `29/29`. Terminal states are `ACTIVE=2 / NONTERMINAL_HOLD=1 / TERMINAL_FOR_CURRENT_OBJECT=26`.

Discovery canonical disposition is `REJECT=12 / HOLD=5 / ACTIVE=1`; the one active object at R34 was the replay-timeshift SYSTEM Discovery. Architecture is active `M=0 / S=1`, queued `M=0 / S=0`, with historical dispositions `REJECT=0 / HOLD_SYSTEM_TERMINAL=5 / HOLD_METHOD_LIMITED=1 / HOLD_CONTRACT_AMBIGUITY=2 / HOLD_MECHANISM_UNRESOLVED=1`. PRE_FORMAL is empty with eligible=`0`, READY=`0`, active=`0`; viable executable MECHANISM=`0`. Recent completed MAIN scientific cycles remain `SYSTEM=6 / MECHANISM=3`; a comparable durable wall-clock split is unavailable and CI duration is not used as a proxy. SYSTEM-over-comparable-MECHANISM exceptions remain `0`.

The first genuine READY->PRE_FORMAL transition is now **observed**. It happened before scientific outcome and therefore supports the intended semantics: READY means prospective test readiness/informativeness, not prior comparator victory or likely PASS. The subsequent raw-before-score defect is execution plumbing, not grounds for a hidden second admission gate. `HIDDEN_SECOND_FORMAL_GATE=false`.

Rolling autonomous scientific selection remains `MECHANISM / SYSTEM / SYSTEM = 1/3`. Executing an already-selected object does not add a fourth selection. There is no current no-target exception. The next genuinely new SUB selection must reassess coherent theory-backward MECHANISM supply and use `NO_COHERENT_MECHANISM_TARGET` if none exists rather than manufacture activity.

## MAIN / pipeline-integrity Architecture

R34 opened fresh SYSTEM object `CAND-PREFORMAL-RAW-PRESERVE-SCORER-PIPELINE-INTEGRITY-01` solely for future integrity. It is not a repair or successor scientific result for the consumed R33 object. Current fields remain `SYSTEM / preformal_eligible=false / N/A readiness / ACTIVE / ACTIVE`, and no comparable executable MECHANISM exists, so `system_priority_exception.used=false` is correct.

Latest MAIN is Relay generation `MAIN-20260921T124719+0900-RELAY-FUNNEL21-SYSTEM-PIPELINE-R34-AUTHBOUND-6B2D91E4`. Relay correctly did **not** start a fresh PRIMARY object without a PRIMARY start/handoff. Next PRIMARY may perform only the R34-authorized static/read-only feasibility cycle for `raw generator -> durable raw preserve/digest -> fixed scorer -> scored preserve`, then STOP for fresh Analyst review. No same-run implementation or new science is authorized.

## Fresh SUB terminal after R34

SUB generation `SUB-20260921T125000+0900-SYSTEM-OUTREPLAYTIMESHIFT-3E7C91A4` executed the exact prospective contract `50223b84e7e30cec2c4dcb136e58c0b4674df966` at head `5e5f04c27bb534fb31a81d25de0b6036e3854b73`; exact-head CI `35558621719` succeeded on Python 3.11 and 3.13.

The fixed result is `ORDINARY_PENDING_STATE_REUSE_REDUCTION`: a second identical `learn_outcome(... reward=1.0)` from unchanged pending state increments predictor count by `+1` and pending-action score by `+0.3`, exactly the learning rate, while episode index and pending identities remain unchanged and no error is raised. The earlier unsuccessful commits stopped at lint before outcome exposure; subsequent changes before the successful run were mechanical import formatting.

Control therefore sees high-confidence ordinary pending-state reuse / experience-replay update multiplicity, not a new responsibility mechanism. Canonical scientific closure remains Evidence Analyst authority. Pending Analyst confirmation, the producer's complete v2.1 terminal proposal is `SYSTEM / preformal_eligible=false / N/A readiness / hold_class=null / hold_reason=null / TERMINAL_FOR_CURRENT_OBJECT / NOT_QUEUED`. If confirmed, canonical Discovery pending view becomes `REJECT=13 / HOLD=5 / ACTIVE=0` without changing portfolio size.

Any question about exactly-once outcome delivery must be a **fresh SYSTEM Architecture candidate** with a fresh ID and prospective lifecycle contract. The completed Discovery must not be upgraded, rescued, or continued.

## Literature / Audit / Methodology

Literature R19 materially strengthens the ordinary reduction: repeated outcome callbacks are established experience reuse/sample weighting, and replay/update multiplicity is an algorithmic resource. Future credit/replay comparisons should prospectively match `outcome updates / new environmental experience`. Repeated reward calls without internal pattern reactivation are not biological replay.

Independent Audit R4 remains controlling negative pressure on Assembly-specific attribution: the earlier selected active coalition effect is real in its bounded comparison, but target units were active while comparator units were inactive, so Assembly-member-specific causality remains `CONFOUNDED`. Do not count it as surviving Assembly-specific mechanism or H7 responsibility support.

Methodology R35 is `MIXED_CALIBRATION / MATERIAL_CALIBRATION_UPDATE`. Control adopts its tightening of raw-before-score execution, preserve-then-score ordering, and producer-side Funnel-v2.1 HOLD-enum conformance. READY semantics remain unchanged. Producer handoffs should prospectively validate or deterministically map noncanonical HOLD enums before publication rather than rely on Analyst normalization.

## Utility / fleet

Utility remains schema-v2 `IDLE`, `active_assignment_id=null`, with no unacknowledged terminal assignment. No Utility request from Literature or Methodology is accepted because the integrity path is already MAIN-owned and Utility must not become a hidden MAIN dependency.

Fleet=`YELLOW`: the PRE_FORMAL integrity defect is contained but future clean conformance remains unproven; the fresh SUB terminal and Literature R19 arrived after Analyst R34 and await dependency-aware canonical consumption; MAIN Relay is correctly fail-closed waiting for PRIMARY. There is no FORMAL/immutable contamination, role collision, fresh exact-head CI failure, canonical classification gap, Utility lifecycle leak, or scheduler failure. No cadence change is warranted.

## Direction to Evidence Analyst

1. Independently verify SUB timeshift against contract `50223b84e7e30cec2c4dcb136e58c0b4674df966`, exact execution head `5e5f04c27bb534fb31a81d25de0b6036e3854b73`, exact-head CI `35558621719`, and lint-only pre-outcome commits.
2. If confirmed, close the CURRENT SYSTEM Discovery as ordinary pending-state reuse / experience-replay update multiplicity with no mechanism uplift, `preformal_eligible=false`, N/A readiness, `TERMINAL_FOR_CURRENT_OBJECT`, `NOT_QUEUED`.
3. Consume Literature R19 prospectively by adding outcome-event consumption, replay multiplicity, sample weighting, and update-to-data ratio to ordinary credit/replay reductions and resource matching.
4. Preserve the R33 PRE_FORMAL object as terminal `NONCONFORMING_RAW_BEFORE_SCORE`; no same-ID repair/rerun/rescore/redesign.
5. Keep the fresh pipeline-integrity object SYSTEM and static/read-only until PRIMARY reports; implementation and science require later authority.
6. Preserve `eligible != READY`; the first live READY->PRE_FORMAL transition validates test-readiness semantics despite later execution nonconformance.
7. Require producer-side schema validation/mapping for noncanonical HOLD enums.
8. Keep theory-backward accounting at `1/3` and reassess MECHANISM supply only on the next genuinely new selection.
9. Preserve Audit R4 `CONFOUNDED` ceiling and immutable C19-v4 historical PASS while programme novelty remains `REDUCIBLE`.

## Control mutations

Experiments `0`; scientific workflow dispatches `0`; identities consumed `0`; research PR merges `0`; immutable/formal/evidence mutations `0`; Utility assignments `0`; scheduler mutations `0`.
