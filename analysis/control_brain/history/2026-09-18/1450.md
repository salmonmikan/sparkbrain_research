# SparkBrain Control Brain — 2026-09-18 14:50 JST

## Strategic decision

**No programme-level doctrine change.** SparkBrain remains a **persistent dynamical cognitive architecture / experimental testbed**, not a demonstrated new computational principle. The novelty bar remains narrow: local, pre-semantic, history-dependent, persistent, predictive, or self-organizing state is insufficient by itself. A stronger claim must survive finite-state, learned/extracted automata, predictive-state/causal-state, recurrent/reservoir/fading-memory, and conventional plasticity/eligibility/slow-state reductions, with a residual specifically tied to actual historical lineage / causal credit.

C19-v4 is retained only as a narrow truth-free representation PASS. C19-R2 is terminal `REDUCED_BY_FSA` and independently `ROBUST_SO_FAR`, with `C19-v4 BREU - R2 FSA BREU = -0.2462308568887407` and cluster-bootstrap 95% CI `[-0.2554750732756702, -0.23690126111231016]`. PD01 is terminal `FAIL_REDUCED_BY_FADING_MEMORY`. These results further weaken generic persistent/local/history-derived-state novelty and strengthen reduction-first doctrine.

## NI01/H4 frontier

Exact object: `research/ni01-no-ignition-selective-prediction-spec-20260918@2664951b65dd18883d3d80862e80b86ac66cf24f`.

Fresh checks:
- NI01 preformal run `35308269458`: `completed/success`.
- ordinary CI `35308269286`: `completed/success`.
- identity `ni01-no-ignition-selective-prediction-official-v1`: unreserved/unconsumed.
- no NI01 STARTED/control ref, preserve ref, or evidence tag.

Evidence Analyst `e22b401013b01517890d48e33debdd79bb522e65` prospectively authorizes exactly one one-way chain only while fresh identity/binding/integrity checks remain green. The registered comparator is a zero-trainable-parameter target-blind max-confidence abstention rule with one DEV-derived threshold per world, coverage-matched to native No-Ignition. PASS remains narrow; FAIL closes the registered discriminator; INCONCLUSIVE is terminal for this identity; POST_START_FAILURE/INVALID_EVIDENCE consumes the identity with no retry. Control Brain adds no semantic condition and authorizes no successor.

### Operational defect

The scientific object is ready, but MAIN's durable stream is stale. The last durable MAIN latest/history is the 13:47 RELAY checkpoint. A PRIMARY lease created at 14:13 under authority `e22b401...` remains `RUNNING` with no heartbeat advancement and no newer durable MAIN report at this snapshot. Fresh repository refs nevertheless show NI01 still unstarted and unconsumed, so there is **no observed scientific-integrity breach**. Treat this as a MAIN persistence/progress defect and reconcile the lease/report before further continuation.

## External input — literature

Consumed `LITERATURE_REDUCTION_SCOUT` commit `525686fa0c14f64426ca7bf5c89f08cd0bcb9c77`.

Impact: preserve the reduction ladder **exact FSA → learned/extracted finite-state → PSR/epsilon-machine → recurrent/reservoir/fading-memory → residual lineage-specific causal credit**. Local causal-state, PSR/TPSR, epsilon-machine, and reservoir results keep ordinary reductions ahead of novelty claims. NI01 is not mutated; stronger uncertainty/OOD comparators, if warranted after a PASS, require a fresh prospective object.

## External input — audit

Consumed `INDEPENDENT_AUDITOR` commit `0c871b1b9b54d35c826a59c5b9925afa55b78d22`, classification `ROBUST_SO_FAR` for C19-R2. It independently confirms integrity, target blindness, registered cluster inference, and terminal FSA reduction. Evidence Analyst has now consumed it explicitly, so both literature and audit knowledge flows through Analyst to Control are healthy. A fresh read-only PD01 audit remains desirable but does not block NI01.

## SUB incubator

Current SUB mode `no_op`; scheduler classification **`NO_OP_BY_DESIGN`**. SUB's 14:40 report independently confirmed NI01's green gates and unconsumed identity and correctly touched nothing. No independent formal/incubator lane currently exists. Previous bounded incubator work is exhausted; do not manufacture parallelism or disguise MAIN assistance as exploration.

## Programme drift

Scientific discipline remains controlled: terminal reductions are preserved rather than rescue-tuned, NI01 is a pre-existing independent H4 question, SUB has not invaded MAIN, and NI01 remains pre-START. The immediate risk is operational opacity from the stale MAIN lease/durable checkpoint, not scientific complexity or post-outcome tuning.

## Repository doctrine

Status **`PARTIAL_COMPLIANCE_IMPROVING`**.

- `main@ebed6abfa941c83b36d2e3bddd04e7c5fb0dbe9d` remains stable substrate.
- NI01 remains under `research/*`.
- 3 authoritative annotated evidence tags exist: C19-v4, C19-R2, PD01.
- all 13 legacy `freeze/*` branches remain preserved.
- rulesets remain 0 and `main` is unprotected; Issue #139 tracks the server-side tag/main protection gap.
- open PRs #148/#149 remain ordinary-review candidates only; Control Brain does not merge them.

`HUMAN-20260918-001` remains **ACCEPT**. The Steward inventory narrows promotion to small neutral extraction on current `main` with fresh CI: anonymous event/prediction-distribution primitives, architecture-neutral comparator protocol shape, snapshot/restore invariants, descriptive resource accounting, generic privilege guards, transcript validation, and generic leak/distribution checks. Do not wholesale cherry-pick old CX branches. G4/G5/G7/G8 mechanisms, CX01 worlds/thresholds/taxonomy/frozen bindings remain research-local; pure G3/G6 cores are conditional only after clean extraction. This work must not delay NI01.

## Priorities — next 12–48h

1. MAIN reconciles the stale PRIMARY lease/durable report against current refs, then executes the exact NI01 chain only if fresh Analyst authority, identity freshness, namespace checks, exact bindings, and same-head green gates remain valid.
2. After NI01 terminalization, obtain independent audit before broader H4 interpretation; obtain a fresh read-only PD01 audit separately.
3. NI01 PASS remains narrow and may motivate a fresh stronger uncertainty/OOD reduction object; FAIL closes the registered discriminator. No automatic NI01-v2.
4. Keep neutral CX-substrate extraction non-blocking and under ordinary review.

## Stop / reframe conditions

- clean NI01 FAIL: terminate registered No-Ignition selective-value discriminator, no rescue tuning;
- NI01 PASS: preserve only the narrow registered effect; broader H4/novelty needs fresh prospective ordinary comparators;
- NI01 INCONCLUSIVE: terminal for identity, no threshold/world tuning;
- NI01 INVALID_EVIDENCE or POST_START_FAILURE: consumed identity, no salvage/retry;
- if remaining non-privileged residual families reduce under ordinary predictive/recurrent/plasticity baselines, retain the architecture/testbed framing and stop computational-principle novelty claims.

## Scheduler fleet health

`fleet_status: YELLOW`

- Control/Repository Steward combined: `HEALTHY`.
- Evidence Analyst: `DELAYED` but current/coherent.
- MAIN/PRIMARY: `FAILED_OR_INCOMPLETE` operationally — stale `RUNNING` lease, no heartbeat/durable report advancement after 13:47; no scientific mutation observed.
- MAIN/RELAY: `DELAYED` at this snapshot.
- SUB: `NO_OP_BY_DESIGN`.
- External literature/audit: `HEALTHY`, both consumed by Analyst and Control.
- Current State Brief: `DELAYED`, non-critical.

No critical scientific-integrity/evidence-preservation failure. Defects are MAIN persistence/runtime slippage and the dense `:35 → :45 → :50` downstream window. RELAY remains useful; SUB no-op prevents fake parallelism. **No scheduler-definition change.** Diagnose MAIN persistence first. Only if 2–3 further cycles repeatedly leave Control/Steward sampling unfinished RELAY should the downstream read-only control/governance slot be considered for a 5–10 minute delay.

## Direction to Evidence Analyst

Keep NI01 as the sole MAIN frontier with current exact prospective semantics unchanged; enforce narrow terminal interpretation and no automatic successor; audit NI01 after terminalization and obtain fresh PD01 audit separately; do not reopen C19 rescue work; keep SUB null/no-op unless a genuinely distinct independent prospective question appears; treat the CX promotion inventory as repository-substrate advice, not scientific evidence.
