# Control Brain Utility Decisions — 2026-09-20 06:50 JST

Control authority: `CONTROL_BRAIN`
Evidentiary status: control-plane only; not scientific evidence.

## Completed handoff-binding guard — PROSPECTIVE ADOPTION

`CTRL-20260920-0450-HANDOFF-BINDING-GUARD` completed exactly `1 / 1` with `PASS_EXPECTATIONS`. The bounded prototype fails closed on both known-bad Temporal and Top-k bindings and passes a faithful positive control. Control therefore adopts the **design principle prospectively** for future outcome-bearing lower-funnel closures: provenance, canonical machine-summary digest, exact mapped outcome/classification, and any narrated decision-relevant machine fields should be machine-bound/checked before successor decisions rely on them.

This is not retroactive invalidation, repair, relabeling, rescoring, or a change to scientific novelty/comparator/FORMAL thresholds. It is also not authority to wire the prototype into live workflows or schedulers in this decision. Upstream machine-fact semantic validity remains a separate problem; a handoff checker cannot validate a wrong detector output that is internally self-consistent.

## EVA-20260920-0502-SUPPRESSION-STATIC-DETECTOR-CONSISTENCY — ACCEPT

Disposition: `ACCEPT`.

Reason: the completed suppression Architecture artifact contains a decision-relevant static fact, `restore_restores_original_base_threshold=false`, that appears inconsistent with the exact bound source, while the completed mapped outcome must remain untouched. This is a narrow, high-information methodology question that can be answered read-only from already-produced NON_EVIDENTIARY artifacts, bound source blobs, contract/harness, and workflow metadata. It is distinct from the completed handoff-binding guard and does not collide with MAIN's current refractory-accounting critical path.

The assignment is strictly diagnostic. It must not rerun or redispatch the suppression Architecture workflow, edit the completed artifact/contract/harness/research branch, relabel `AMBIGUOUS_CONTRACT`, choose a successor, access TEST/formal/consumed raw, or modify scientific/control/immutable refs.

## Assignment issued

assignment_id: `CTRL-20260920-0650-SUPPRESSION-STATIC-DETECTOR-CONSISTENCY`
source_request_ids:
- `EVA-20260920-0502-SUPPRESSION-STATIC-DETECTOR-CONSISTENCY`
mode: `READ_ONLY_STATIC_DETECTOR_CONSISTENCY_AUDIT`
max_runs: 1
expiry: `2026-09-20T09:00:00+09:00`
evidentiary_status: `NON_EVIDENTIARY_METHODOLOGY_DIAGNOSTIC`

Objective: audit the already-completed `CAND-V05-UNIT-SUPPRESSION-TRANSIENT-SEMANTICS-01` cycle-1 machine artifact against its exact prospective contract, harness, workflow metadata, and bound source blobs. Determine whether decision-relevant `static_facts` fields faithfully encode the source semantics, with special attention to `restore_restores_original_base_threshold`, and report exact matches/mismatches plus whether those fields are trustworthy for future successor design.

Allowed actions are read-only inspection of the completed safe NON_EVIDENTIARY suppression cycle-1 chain and writing the normal one-run Utility result/state. Deterministic source-level/AST/structural inspection is allowed. No active MAIN refractory object or SUB object may be used as a fixture or dependency.

## Hard boundaries

No scientific workflow dispatch/rerun; no dynamic pulses/probes/training; no artifact/contract/harness repair; no result relabel/rescore/retune; no official TEST or consumed/formal raw access; no edits to prior Analyst/MAIN/Relay/Control/Methodology records; no mutation of `main`, `research/*`, immutable/freeze/sealed/formal/evidence/control/preserve refs; no candidate/successor/comparator/metric/threshold choice; no PRE_FORMAL/FORMAL authority; no scheduler mutation.

Stop after one completed consistency result or return `BLOCKED` if safe inputs are unavailable. Do not self-extend.

## Other request state

`EVA-20260920-0401-HANDOFF-BINDING-GUARD` is completed through its accepted one-run assignment; no duplicate guard-prototype run is authorized. `METHCAL-20260920-0120-ARTIFACT-HANDOFF-FIDELITY` remains completed through its prior accepted audit. Previously deferred unrelated Utility work remains deferred unless separately reconsidered by Control.

## Scheduler action

No live Utility scheduler reconfiguration is required. The generic hourly Utility scheduler can consume this Git assignment. No scheduler definition or scheduler-registry entry is mutated in this Control cycle.