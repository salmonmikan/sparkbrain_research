# Control Brain Utility Decisions — 2026-09-19 22:50 JST

Control authority: `CONTROL_BRAIN`
Evidentiary status: control-plane only; not scientific evidence.

## EVA-20260919-1943-TOPK-CROSS-SEED-SUPPORT — ACCEPT

Disposition: `ACCEPT`

Reason: this is the highest-value currently unhandled Utility request. The prior one-run cycle-1 support diagnostic is complete and returned `MIXED / ADD_PER_STRATUM_SUPPORT`; MAIN is now explicitly holding with no active central object, while Top-k remains held pending exactly this cross-seed support question. The requested work is bounded, read-only, NON_EVIDENTIARY, uses only already-produced cycle-1/cycle-2 DEV artifacts, and can distinguish a broadly supported architecture interaction from repeated sparse-stratum sensitivity without rerunning models or changing completed labels. It does not collide with MAIN's current HOLD or SUB's independent Discovery lane.

Control conditions: verify exact heads/artifact digests; analyze support by seed/magnitude, leave-one-seed/leave-one-stratum stability, episode-cluster robustness, and the predeclared descriptive `n>=3/5/10` sensitivity family where feasible. Do not optimize a replacement support rule, rerun/retrain/reprobe, access TEST/formal raw or consumed evidence, mutate research branches, alter completed Architecture labels, create PRE_FORMAL/FORMAL authority, or become a MAIN dependency. If safe existing artifacts are insufficient, return `BLOCKED_MISSING_SAFE_INPUT`.

Assignment issued: `CTRL-20260919-2250-TOPK-CROSS-SEED-SUPPORT`, max 1 Utility run.

## Existing request dispositions

- `METHCAL-20260919-1818-ARCH-SIGNAL-SUPPORT`: prior `ACCEPT` is complete; one bounded Utility result returned `MIXED / ADD_PER_STRATUM_SUPPORT`. No extension is authorized under that assignment.
- `LIT-20260919-1830-TOPK-HYBRID-TRANSIENT`: remains `DEFER`. The 21:30 Literature update strengthens the ordinary reduction ladder with basin/attractor selection and WTA hysteresis, but mechanism decomposition should still wait for the accepted cross-seed support diagnostic and fresh Evidence Analyst review. Do not spawn an overlapping Utility job now.

## Dedupe / collision / lifecycle check

The previous assignment `CTRL-20260919-1850-ARCH-SIGNAL-ROBUSTNESS` reached its max-runs stop and Utility state records it `COMPLETED`; its stale `assignment/current.md` status is replaced by the new assignment below. The new cross-seed diagnostic is not a duplicate of the completed cycle-1-only robustness run because it adds cycle-2/cross-seed stability under fixed completed labels. MAIN has no active scientific object and must not wait for Utility. SUB's current independent Discovery candidate is unrelated.
