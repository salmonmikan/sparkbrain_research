# Utility autonomous task result — equivalence trust-boundary reconciliation

schema_version: 2
autonomous_task_id: AUTOUTIL-20260922T1422+0900-EQUIV-TRUST-BOUNDARY-7B3D91E4
assignment_mode: AUTONOMOUS_IDLE
status: COMPLETED
completed_at: 2026-09-22T14:24:00+09:00
run_count: 1
max_runs: 1
evidentiary_status: NON_EVIDENTIARY
scientific_authority: NONE
outcome_classification: G10_CLOSED_CI_AND_FRESH_REVIEW_GATES_BUT_DECLARED_DIGEST_TRUST_BOUNDARY_REMAINS

## Result

The existing Utility request's ordinary engineering gates are now satisfied: exact-head CI is green and fresh Repository Steward G10 has structurally reviewed the isolated verifier. The remaining blocker is narrower and semantic: the implementation proves only equality/inequality of **declared certificate digests under internally self-consistent bindings/envelopes**, not independent scientific equivalence, independently authenticated producer provenance, raw-to-digest derivation, or process isolation.

Exact isolated branch remains `utility/equivalence-certificate-v0-1-A42D7C19@9f9d18065b481d8597236b0b682f0574c251b319`, three commits ahead of `main@ebed6abfa941c83b36d2e3bddd04e7c5fb0dbe9d`, zero behind, with only:

- `src/sparkbrain/equivalence_certificate.py`
- `tests/test_equivalence_certificate.py`

No repository callsite exists outside the synthetic tests on that branch because those are the only two changed files relative to main.

## Exact trust boundary

What the verifier actually establishes:

1. top-level schema is exact and fail-closed;
2. source/protocol/package/input/evaluator identity objects are syntactically bound;
3. privilege/resource envelopes are canonicalized and their hashes must match both member attestations;
4. the two members must present distinct `producer_id`, `process_id`, `challenge_nonce`, and positive `os_pid` values;
5. equality verdict is based only on equality of the two supplied `ordered_trajectory_sha256` and `checkpoint_sequence_sha256` values.

What it does **not** establish:

- that producer IDs/process IDs/nonces/PIDs were externally authenticated or generated by independent trusted processes;
- that the supplied trajectory/checkpoint digests were recomputed from preserved raw streams;
- that the producers were actually isolated or equivalently privileged beyond the declared envelope hashes;
- that matching digests imply scientific, semantic, causal, candidate, Funnel, PRE_FORMAL, or FORMAL equivalence.

The module docstring already says this is tooling-only and the function docstring already narrows equivalence to a byte-digest statement. The ambiguity is therefore concentrated in outward-facing tokens that are stronger than the implementation's trust boundary:

- verdict `VALID_EQUIVALENT`;
- reason `EXACT_SEMANTIC_DIGEST_MATCH`;
- certificate name `equivalence-certificate-v0.1`;
- independence failure label `PRODUCERS_NOT_INDEPENDENT:*`, which can be read as proving actual producer independence when the code only checks distinct declared fields.

## Smallest safe reviewed follow-up

A bounded non-scientific integration repair can remain confined to the same two files. The safest contract is to make the outward names match the existing implementation semantics, for example:

- `VALID_DECLARED_DIGEST_EQUIVALENT` / `VALID_DECLARED_DIGEST_NOT_EQUIVALENT`;
- reason `EXACT_DECLARED_TRAJECTORY_AND_CHECKPOINT_DIGEST_MATCH`;
- an explicit trust-boundary marker such as `DECLARED_CERTIFICATE_INTERNAL_ONLY`;
- rename independence diagnostics to state `DECLARED_ATTESTATION_FIELDS_NOT_DISTINCT` rather than claim trusted independence;
- retain an explicit statement that authenticated producer provenance and raw-to-digest verification are separate prerequisites for any scientific use.

This is not a requested or approved patch in this run; no source/test mutation was made. Fresh Steward/Control review should decide exact naming before normal reviewed integration. The existing Utility request should not be self-approved or self-closed by Utility.

## Collision / freshness

- Control-owned pointer remained clean schema-v2 IDLE with `active_assignment_id: null`.
- Evidence Analyst remained `EVA-20260922T140541+0900-R63-E8C421B7`.
- SUB remained `SUB-20260922T133738+0900-PRENOOP-R62-NOFRESHDELTA-6C8A21F4` with no independent active target.
- The latest durable MAIN/Relay generation available to Utility remained `MAIN-20260922T125400+0900-RELAY-H7-DEVR1-ARCH-C2-C7F421A9`; independently refreshed live repository state shows H7 DEV-R2 has since materialized and advanced to `research/main-h7-dev-r2-comparator-protocol-closure-r63-cycle3@d6655549c179caf391d4b43bd2ebea49f2bfc82b` under the existing R63 implementation-only authority.
- That live H7 progress does not collide with this generic, read-only tooling reconciliation and was not touched.
- Control remained `CTRL-20260922T124800+0900-R33-E7C421B6`.
- Repository Steward fresh review is `STEWARD-20260922T135014+0900-G10-4D8C21A7`.

## Funnel v2.1 preservation

No research candidate was touched. No Funnel field was created, changed, reinterpreted, or satisfied. No theory-backward, Discovery, PRE_FORMAL, FORMAL, or promotion credit is claimed.

## Integrity checks

- workflow dispatch/rerun: none
- scientific workflow/experiment: none
- source/test/tool/docs mutation: none
- scheduler mutation: none
- protected/evidence/control/preserve mutation: none
- candidate typing/readiness mutation: none
- PRE_FORMAL/FORMAL action: none
- research PR merge: none
- hidden MAIN dependency: none

stop_reason: COMPLETED_BOUNDED_TRUST_BOUNDARY_AND_CALLSITE_RECONCILIATION
follow_up_recommendation: >-
  Keep the existing Utility request pending Control disposition; do not duplicate it.
  If Control chooses integration, make the verifier's public verdict/reason/trust-boundary
  names explicitly describe declared-digest equivalence before any normal reviewed main
  integration. External producer authentication and raw-to-digest verification remain
  separate requirements for any later scientific use.
