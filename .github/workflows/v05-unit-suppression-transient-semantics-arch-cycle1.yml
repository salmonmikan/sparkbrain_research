name: V05 unit suppression transient semantics Architecture cycle 1

on:
  push:
    branches:
      - research/main-v05-unit-suppression-transient-semantics-arch-study-20260920
    paths:
      - analysis/architecture/v05_unit_suppression_transient_semantics_cycle1_contract_20260920.json
      - analysis/architecture/v05_unit_suppression_transient_semantics_cycle1_20260920.py
      - .github/workflows/v05-unit-suppression-transient-semantics-arch-cycle1.yml

permissions:
  contents: read

concurrency:
  group: v05-unit-suppression-transient-semantics-arch-cycle1-${{ github.ref }}
  cancel-in-progress: false

jobs:
  exact-head-ci:
    runs-on: ubuntu-latest
    strategy:
      fail-fast: false
      matrix:
        python-version: ["3.11", "3.13"]
    steps:
      - uses: actions/checkout@v4
        with:
          fetch-depth: 0
      - uses: actions/setup-python@v5
        with:
          python-version: ${{ matrix.python-version }}
      - name: Verify exact head and main ancestry
        run: |
          test "$(git rev-parse HEAD)" = "${GITHUB_SHA}"
          git merge-base --is-ancestor ebed6abfa941c83b36d2e3bddd04e7c5fb0dbe9d HEAD
      - name: Install repository CI dependencies
        run: python -m pip install -e '.[dev,learned,spiking]'
      - name: Compile and lint Architecture harness
        run: |
          python -m py_compile analysis/architecture/v05_unit_suppression_transient_semantics_cycle1_20260920.py
          ruff check analysis/architecture/v05_unit_suppression_transient_semantics_cycle1_20260920.py
      - name: Run ordinary repository tests
        run: python -m pytest
      - name: Verify prospective binding only
        run: |
          python analysis/architecture/v05_unit_suppression_transient_semantics_cycle1_20260920.py --preflight-only

  architecture-study:
    needs: exact-head-ci
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
        with:
          fetch-depth: 0
      - uses: actions/setup-python@v5
        with:
          python-version: "3.11"
      - name: Verify exact head and main ancestry again
        run: |
          test "$(git rev-parse HEAD)" = "${GITHUB_SHA}"
          git merge-base --is-ancestor ebed6abfa941c83b36d2e3bddd04e7c5fb0dbe9d HEAD
      - name: Install package
        run: python -m pip install -e '.[dev]'
      - name: Preflight before static interpretation
        run: |
          python analysis/architecture/v05_unit_suppression_transient_semantics_cycle1_20260920.py --preflight-only
      - name: Execute one static NON_EVIDENTIARY Architecture cycle
        run: |
          python analysis/architecture/v05_unit_suppression_transient_semantics_cycle1_20260920.py \
            --output-dir run-artifacts/v05-unit-suppression-transient-semantics-cycle1
      - name: Upload raw-before-summary Architecture artifacts
        uses: actions/upload-artifact@v4
        with:
          name: v05-unit-suppression-transient-semantics-cycle1-${{ github.sha }}
          path: run-artifacts/v05-unit-suppression-transient-semantics-cycle1
          if-no-files-found: error
          retention-days: 30
