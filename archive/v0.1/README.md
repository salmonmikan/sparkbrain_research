# v0.1 Conversation Prototype

These files preserve the first minimal hand-authored Spark Field prototype. They are retained for design-history comparison and are not imported by the v0.2 package.

The v0.2 reference implementation supersedes them with explicit events, graph state, Workspace broadcast, metrics, tests, checkpoint/replay, and a visualizer.
