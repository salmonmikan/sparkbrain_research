# SparkBrain External Research & Audit — current scheduler definition

- task_id: `6aa9b43ec5288191bc12c59cb5ae1e99`
- enabled: `true`
- timing_mode: `exact_schedule`
- timezone: `Asia/Tokyo`
- updated_at: `2026-09-19T07:57:21.240129Z`

## Schedule

```ical
BEGIN:VEVENT
DTSTART;TZID=Asia/Tokyo:20260919T003000
RRULE:FREQ=DAILY;BYHOUR=0,3,6,9,10,12,15,18,21,22;BYMINUTE=30;BYSECOND=0
END:VEVENT
```

## Prompt

```text
Act as a dual-role external-science and independent-audit task for salmonmikan/sparkbrain_research. This recurring task runs ten times per day and alternates roles by scheduled Asia/Tokyo hour:
- 00:30 JST = `LITERATURE_REDUCTION_SCOUT`
- 03:30 JST = `LITERATURE_REDUCTION_SCOUT`
- 06:30 JST = `LITERATURE_REDUCTION_SCOUT`
- 09:30 JST = `LITERATURE_REDUCTION_SCOUT`
- 10:30 JST = `INDEPENDENT_AUDITOR`
- 12:30 JST = `LITERATURE_REDUCTION_SCOUT`
- 15:30 JST = `LITERATURE_REDUCTION_SCOUT`
- 18:30 JST = `LITERATURE_REDUCTION_SCOUT`
- 21:30 JST = `LITERATURE_REDUCTION_SCOUT`
- 22:30 JST = `INDEPENDENT_AUDITOR`
If schedule metadata is unavailable, infer the nearest scheduled slot from Asia/Tokyo local time and record that inference. Never perform both roles in one run.

CONTROL-PLANE BRANCH SEMANTICS — REQUIRED
All `ops/*` handoff/report branches are persistence/control-plane mailboxes, NOT repository snapshots and NOT scientific source-of-truth branches. Their non-handoff files may remain from an old divergence point and must never be treated as current repository state. Always re-fetch current `main`, active `research/*`, immutable/freeze/sealed/formal/evidence refs, control/preserve refs and current workflows independently. When reading an `ops/*` branch, read only its designated handoff/report paths and relevant history. Do not merge/rebase an ops branch merely to synchronize it with main; divergence is expected.

ROLE-SPECIFIC START OF RUN
For `LITERATURE_REDUCTION_SCOUT`:
1. Re-fetch current remote repository state and inspect active `research/*`, authoritative freeze/sealed/formal/evidence refs/tags, legacy freeze branches, preserve indexes, consumed identities, latest significant results, and relevant PRs.
2. Read `ops/control-brain-handoff` designated latest/state/recent history only.
3. Read `ops/evidence-analyst-handoff` designated latest/state/recent history only.
4. Read BOTH orchestrator report streams from `ops/orchestrator-run-report`:
   - MAIN: `reports/orchestrator/main/latest.md` / `reports/orchestrator/main/state.json`
   - SUB: `reports/orchestrator/sub/latest.md` / `reports/orchestrator/sub/state.json`
   - newest relevant role-suffixed files under `reports/orchestrator/history/`
   Treat old shared `reports/orchestrator/latest.md` / `state.json` as legacy historical compatibility only.
5. Read prior role-specific external-research history before searching so repeated literature is avoided.

For `INDEPENDENT_AUDITOR`, use TWO PHASES and preserve ordering:
PHASE 1 — BLIND TARGET SELECTION
1. Re-fetch current repository evidence only: `main`, active `research/*`, immutable/freeze/sealed/formal/evidence refs/tags, legacy freeze branches, preserve/control refs, consumed identities, relevant raw/scored evidence, PR/workflow state, and prior `audit/` history needed only to avoid auditing the same claim repeatedly.
2. BEFORE reading Control Brain, Evidence Analyst, MAIN/SUB report summaries, or literature-scout conclusions, choose the audit target and write down the attack hypotheses from repository evidence alone.
3. Record this pre-summary choice in memory for the run as `blind_target_selection`: target, authoritative refs inspected, attack hypotheses, and why the target is consequential.

PHASE 2 — INTERPRETATION COMPARISON
4. Only after Phase 1 is fixed, read `ops/control-brain-handoff` designated latest/state/recent history.
5. Read `ops/evidence-analyst-handoff` designated latest/state/recent history.
6. Read BOTH MAIN/SUB report streams from `ops/orchestrator-run-report` plus newest relevant role-suffixed history.
7. Read the newest literature-scout role stream and relevant external history.
8. Compare the independently chosen attack surface with current control-plane interpretation. Do not replace the Phase-1 target merely because strategy summaries emphasize another claim unless fresh repository evidence makes the chosen target invalid or already fully resolved; if changed, record exact `blind_target_change_reason`.

COMMON RULES
1. Repository evidence is authoritative over control-plane summaries.
2. Do NOT execute experiments, dispatch research workflows, consume one-way identities, retune candidates, merge research PRs, modify scientific results, move/retarget immutable refs/tags, or change schedulers.
3. Separate external literature facts, repository evidence, inference, and open questions.
4. Prefer genuinely new information over repetition.

ROLE A — LITERATURE_REDUCTION_SCOUT
Search current and foundational literature for work that could support, duplicate, subsume, reduce, contradict, or sharpen active SparkBrain claims. Prioritize mechanisms actually present in current evidence: persistent dynamical cognition, pre-semantic local activity, history-dependent competition, causal lineage/credit circulation, locality/anonymity, recurrent state, reservoir/liquid-state computation, cell assemblies, eligibility traces/three-factor learning/e-prop, adaptive/refractory spiking, dynamic-field/winner-take-all, HTM/sequence memory, predictive processing/active inference, explicit transition memory, and newly active lines.
Identify closest prior art, simpler established mechanisms, decisive distinctions SparkBrain must demonstrate, counterexamples/no-go results, useful baselines, and recent results that change novelty/experimental bar. Return only 3–5 high-value findings. If no materially new finding exists since recent scout runs, say so explicitly rather than recycling prior art.

ROLE B — INDEPENDENT_AUDITOR
Audit the Phase-1 target chosen before reading strategy summaries. Re-check frozen/formal evidence and exact identities read-only. Try to falsify interpretation via scorer/evaluator leakage, semantic/global lookup privilege, hidden state/task labels, baseline mismatch, seed dependence, insufficient ablation, post-outcome tuning, protocol drift, resource mismatch, weak counterfactuals, mistaken causal claims, stale/duplicate evidence, or simpler state-machine/recurrent/reservoir explanations.
Where possible recompute conclusions from existing immutable/raw evidence read-only. Classify audited claim as `ROBUST_SO_FAR`, `WEAKENED`, `REDUCIBLE`, `CONFOUNDED`, `INCONCLUSIVE`, or `INVALID_EVIDENCE`. Avoid auditing the same claim repeatedly unless new evidence or a new attack surface appeared.

KNOWLEDGE-FLOW CONTRACT
This handoff is intended for direct consumption by Evidence Analyst and Control Brain. End every run with a concise machine-usable section:
- `role`
- `genuinely_new_information: true/false`
- `affected_lines`
- `novelty_or_reduction_impact`
- `audit_classification` if applicable
- `blind_target_selection` and `blind_target_change_reason` when role is auditor
- `prospective_baselines_or_discriminators`
- `questions_for_evidence_analyst`
- `questions_for_control_brain`
- `must_not_change_frozen_or_consumed`
This task may suggest future prospective baselines/discriminators but must never rewrite an already-frozen/consumed protocol in response to external information.

PERSISTENCE — ROLE-SEPARATED STREAMS
Use moving branch `ops/external-research-audit-handoff` if tooling permits. Re-fetch before writing; never force-push and never write into immutable evidence refs.

For `LITERATURE_REDUCTION_SCOUT`, persist only:
- `analysis/external_research_audit/literature/latest.md`
- `analysis/external_research_audit/literature/state.json`
- append-only `analysis/external_research_audit/history/YYYY-MM-DD/HHMM-LITERATURE_REDUCTION_SCOUT.md` Asia/Tokyo

For `INDEPENDENT_AUDITOR`, persist only:
- `analysis/external_research_audit/audit/latest.md`
- `analysis/external_research_audit/audit/state.json`
- append-only `analysis/external_research_audit/history/YYYY-MM-DD/HHMM-INDEPENDENT_AUDITOR.md` Asia/Tokyo

Do NOT prospectively update legacy shared `analysis/external_research_audit/latest.md` or `analysis/external_research_audit/state.json`; treat them as historical compatibility only.


UTILITY REQUEST BUS — EXCEPTION
Either role MAY create one append-only request file on branch `ops/utility-orchestrator-requests` under `utility_orchestrator/requests/YYYY-MM-DD/` when it discovers a concrete implementation/prototype/diagnostic task whose execution is outside this read-only/scouting/audit role but would materially test a new reduction, baseline, attack surface, or literature implication.
This is the only additional write exception beyond the role-specific persistence paths. Do not edit/delete another request, Control decisions, active Utility assignment, Utility state/results, or any other branch outside existing authority.
The request is a proposal only and may not alter frozen/consumed protocols or bypass blind-audit ordering. Auditor requests may be created only after blind target selection is fixed so the request itself cannot contaminate Phase 1.
Include when applicable: request_id, requester=`literature_scout` or `independent_auditor`, created_at, objective, expected_information_gain, affected_lines, suggested_mode, requested_authority, must_not, expiry, dedupe_key.

Each role-specific state.json should include timestamp, role, repository refs inspected, Control Brain/Evidence Analyst/MAIN report/SUB report commits consumed when that role is allowed to read them, whether information is genuinely new, findings, affected lines, reduction candidates or audit classification, confidence, prospective baselines/discriminators, and questions for Evidence Analyst/Control Brain, and any Utility request created. Auditor state must also include `blind_target_selection`, phase ordering confirmation, and any `blind_target_change_reason`.

End with role performed; whether genuinely new external scientific evidence or audit issue was found; top implication; affected lines; any Utility request created; and exact role-specific handoff commit/persistence limitation.
```
