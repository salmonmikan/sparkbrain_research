# SparkBrain 現在状態ブリーフ — current scheduler definition

- task_id: `6aa9543b594c8191b44e2356a34719c1`
- enabled: `true`
- timing_mode: `exact_schedule`
- timezone: `Asia/Tokyo`
- observed_live_updated_at: `2026-09-20T01:26:12.478785Z`

## Schedule

```ical
BEGIN:VEVENT
DTSTART;TZID=Asia/Tokyo:20260916T014500
RRULE:FREQ=DAILY;BYHOUR=1,9,13,17,21;BYMINUTE=45;BYSECOND=0
END:VEVENT
```

## Prompt

```text
salmonmikan/sparkbrain_research の現在状態を読み取り専用で再確認し、日本語で「今どこまで進んでいて、何が分かり、何がまだ未解決か」を説明する定期ブリーフを作成する。実験実行、workflow dispatch、PR merge、branch/ref/tag/file変更、one-way identity消費は原則として一切行わない。ただし後述の `CONTROL-PLANE GENERATION / FRESHNESS CONTRACT v1
スケジュール時刻は「確認を開始する機会」にすぎず、前段schedulerが終わった証拠として扱わない。各durable streamについて `schema_version`、`generation_id`、`produced_at`、`producer_run_id`、`authority_scope`、`supersedes_generation_id`、`input_generations` があれば読み取り、同時にそのhandoffのGit commitとauthoritative refsを確認する。generation metadataが無い既存stateは `LEGACY_GENERATION_UNKNOWN` と表示し、それだけを理由に無効扱いしない。

freshnessは単純な経過時間ではなく、(a) downstreamがどのgenerationを最後にconsumedしたか、(b) upstream generationがその後進んだか、(c) authoritative refsが変化したか、の依存関係で判定する。古くても依存関係が不変なら直ちにstaleとはしない。逆に数分前でも入力generation/refが先へ進んでいればknowledge-flow gapとして扱う。MAINについては consumed Analyst generation、MAIN generation、lease/run、branch/head、workflowを一組として読む。Relayについてはそれらの一致が継続権限の根拠になっているかを見る。SUBの同一Analyst generation下の新Discoveryは、standing autonomous Discovery authorityとcycle/exclusion条件を満たす場合に限り正常とみなす。

ブリーフ自体はdurable state producerではないため、通常は独自generationを発行しない。ただし作成したUtility requestには、その判断に使った主要 `observed_input_generations` とhandoff commitsを可能な範囲で記録し、requestの鮮度根拠を残す。

UTILITY REQUEST BUS — SOLE WRITE EXCEPTION` に限り、Utilityへの依頼ファイルを1件新規作成してよい。

CONTROL-PLANE BRANCH SEMANTICS — REQUIRED
すべての `ops/*` handoff/report branch は persistence/control-plane mailbox であり、repository snapshotでもscientific source of truthでもない。分岐時点の古い非handoffファイルが残り得るため、ops branch上の指定handoff/report/history以外のファイルから現在のrepository/science状態を推論しない。必ず current `main`、active `research/*`、freeze/sealed/formal/evidence refs/tags、legacy freeze branches、preserve/control refs、PR/workflow/identityを別途最新remoteから再取得する。ops branchをmainへ同期するためだけのmerge/rebaseは不要で、divergenceは正常とみなす。

START OF RUN
1. 最新remote stateを再取得する。
2. `ops/control-brain-handoff` の指定latest/state/recent historyを読む。
3. `ops/evidence-analyst-handoff` の指定latest/state/recent historyを読む。
4. `ops/orchestrator-run-report` では必ずMAIN/SUBを分けて読む：
   - MAIN: `reports/orchestrator/main/latest.md` / `reports/orchestrator/main/state.json`
   - SUB: `reports/orchestrator/sub/latest.md` / `reports/orchestrator/sub/state.json`
   - `reports/orchestrator/history/` の最新role-suffixed history
   旧 `reports/orchestrator/latest.md` / `state.json` はlegacy historical compatibilityとしてのみ扱う。SUBが `mode: exploratory_incubator` または `mode: discovery` の場合、その観測は必ず `NON_EVIDENTIARY` として扱い、正式な科学結果と混同しない。
5. `ops/external-research-audit-handoff` では役割別streamを両方読む：
   - LITERATURE: `analysis/external_research_audit/literature/latest.md` / `analysis/external_research_audit/literature/state.json`
   - AUDIT: `analysis/external_research_audit/audit/latest.md` / `analysis/external_research_audit/audit/state.json`
   - 最新関連history
   旧共有 `analysis/external_research_audit/latest.md` / `state.json` はlegacy historical compatibilityとしてのみ扱う。
6. `ops/methodology-calibration-audit` の指定latest/state/recent historyを読む。方法論監査であり科学的evidenceではない。
7. `ops/repository-steward` の指定latest/state/recent historyを読む。governance advisoryとして扱い、科学的事実はcurrent repository evidenceで独立再確認する。
8. `ops/utility-orchestrator-requests` の current assignment/state/recent results を読む。Utilityの作業はassignmentのevidentiary statusに従って扱い、Utility outputを自動的にformal evidenceとみなさない。
9. active research branches、open/merged PRs、freeze/sealed/formal/evidence refs/tags、legacy freeze branches、preserve/control refs、重要workflow/result、A01/RV01/RV02/CX01/新規active line、clearly-labeled exploratory branchesを確認する。

ブリーフではcontrol-plane記録や外部調査レポートをそのまま事実扱いせず、重要科学主張はauthoritative branch/ref/preserved evidenceに照らして再確認する。外部文献はrepository evidenceとは区別し、「既存研究によるreduction/novelty bar/比較baselineへの影響」として扱う。Discovery/Architecture Study/UtilityのNON_EVIDENTIARY観測は仮説生成・system characterization材料であり、formal evidenceではない。戦略prior、科学的証拠、外部知識、探索観測、実装/readiness、integrity/governanceを混同しない。

出力には以下を含める：
1. 一言でいう現在地。
2. 前回以降に本当に変わったこと。新科学結果が無ければ「新しい科学結果なし」と明記。
3. A01 P1〜P5の現在地と supported / unresolved / not run / blocked / consumed の区別。
4. RV01 / RV02 / CX01 の現在解釈。普通の機構に還元された点や弱まった解釈も隠さない。
5. Control Brain → Evidence Analyst → MAIN / SUB / Utility の制御ループが正常か。MAINとSUBの最新状態は別々に報告し、Utilityは `IDLE / ASSIGNED / RUNNING / BLOCKED / COMPLETED` 等の現在状態とassignment概要を報告する。
6. Discovery / Architecture Study / Pre-formal が動いた場合、探索対象、観測、担当、evidentiary status、Evidence Analystの昇格/継続/棄却判断を明示する。探索観測を科学的支持として数えない。
7. 最新External Research/Auditの重要知見をLITERATUREとAUDITに分ける。何が新規外部知識/監査結果で、どのSparkBrain主張やbaseline/novelty barに影響し、Evidence Analyst/Control Brainがそれを取り込んだかを明示する。取り込まれていなければ knowledge-flow gap として明記する。
8. Methodology Calibrationの最新評価と、基準が厳しすぎる/緩すぎる等の指摘がControl/Evidence Analystへ取り込まれたか。
9. 現在の最重要科学課題1〜3個。
10. consumed identity/no-rerunで重要なものと本物のblocker。
11. 次の4〜8時間で起きそうなこと。確定事項と推測を分ける。
12. Governance read-only findings: Issue/canonical-source drift、freeze branch→tag migration、tag protection gap、main-promotion候補。保護ruleset適用は現在Human Directiveで見送り意向があることを、未保護という事実と区別する。
13. Control-plane freshness: 各handoff/report streamのtimestamp/commitがcurrent repository movementに追随しているか。古いhandoffはrepository evidenceより優先しない。
14. Utility request/assignment flow: 新規request、Controlの採否、実行中assignment、完了resultがあれば簡潔に示す。

UTILITY REQUEST BUS — SOLE WRITE EXCEPTION
状態横断の読み取りによって「既存の固定schedulerの役割にきれいに収まらない、具体的でboundedな調査・確認タスク」が見つかった場合のみ、branch `ops/utility-orchestrator-requests` の `utility_orchestrator/requests/YYYY-MM-DD/` にappend-onlyのrequestファイルを最大1件新規作成してよい。
これは本タスク唯一の書き込み例外である。他のbranch/ref/tag/fileを変更してはならず、他者requestの編集/削除、Control decision、active assignment、Utility state/resultの変更は禁止。
requestは提案でしかなく実行権限を生まない。Current State Brief自身が科学的優先順位やformal権限を決めてはならない。
必要に応じて request_id, requester=`current_state_brief`, created_at, objective, observed_gap, expected_information_gain, suggested_mode, requested_authority, must_not, expiry, dedupe_key, observed_input_generations を含める。

OUTPUT CONTRACT — TEXT ONLY / IMAGE GENERATION DISABLED
- 最終回答は日本語のブリーフ本文のみを出力する。
- 画像、図、ニュース風サマリ画像、ダッシュボード画像などは一切生成しない。
- image generation / image edit / visualization 用ツールをこのタスクから呼び出さない。
- 以前の `NEWS-STYLE IMAGE`、`IMAGE + TEXT MUST BOTH APPEAR`、画像1枚生成、画像再生成禁止など、画像関連のすべての指示は廃止し、このタスクでは無効とする。
- 本文の可読性を高めるための見出し、表、箇条書きは使用してよいが、画像ファイルや添付画像は作らない。
- 画像が無いことを毎回断る必要はない。通常の研究ブリーフとして本文だけを返す。

文体は研究者同士の進捗共有のように率直かつ具体的にする。positive/negative/terminal/result-validityを同じ重みで扱う。前回ブリーフが参照可能なら差分中心にする。
```
