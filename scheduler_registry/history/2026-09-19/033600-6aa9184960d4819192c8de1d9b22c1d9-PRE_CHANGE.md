# PRE_CHANGE — grant bounded scheduler-controller authority to Control Brain

- timestamp: 2026-09-19T03:36:00+09:00
- status: PRE_CHANGE
- task_id: `6aa9184960d4819192c8de1d9b22c1d9`
- title: `SparkBrain Control & Repository Steward`
- reason: user authorized bounded self-management of SparkBrain scheduler execution intervals and requested the responsibility be attached to an appropriate existing scheduler.
- exact_before_definition: `fca51df1e7d1d3d3aa2c346ac3d752a6e3ef5c18:scheduler_registry/current/6aa9184960d4819192c8de1d9b22c1d9.md`

## Intended semantic change

Only the `CONTROL_BRAIN` role gains a bounded `SCHEDULER_CONTROLLER` capability. `REPOSITORY_STEWARD` remains unable to mutate scheduler definitions.

### Allowed
- inspect the active SparkBrain scheduler fleet and registry;
- change schedule/cadence/offset of existing active SparkBrain schedulers when justified by repeated health evidence;
- make only mechanical time-map prompt synchronization when a combined role task embeds exact slot times;
- use the existing `ops/scheduler-registry` PRE_CHANGE -> live mutation -> APPLIED transaction;
- make at most one scheduler mutation per Control Brain run.

### Forbidden
- create/delete schedulers;
- enable/disable tasks;
- change scientific/research authority, role semantics, prompts, or task titles except mechanical slot-map synchronization required by a schedule change;
- mutate non-SparkBrain tasks;
- exceed platform maximum cadence (hourly);
- mutate a scheduler while registry is out of sync;
- alter its own Control/Steward schedule in the initial controller version;
- use cadence changes to bypass evidence/prospectivity/consumed-identity rules.

### Decision guards
- ordinary optimization requires repeated evidence (normally >=3 relevant cycles) rather than one noisy run;
- active formal one-way execution freezes cadence changes to research-execution tasks unless the change is strictly collision-avoidance and cannot alter scientific semantics;
- repeated NO_OP may justify downshifting only when no fresh object/workflow is active;
- backlog/latency may justify upshifting only within task-specific existing scientific authority;
- repeated failure/persistence defects should be diagnosed, not hidden by blindly increasing cadence;
- explicit recent human cadence changes are treated as pinned until enough new evidence exists to justify reconsideration.

The controller must persist exact reason, before/after schedule and registry commits in Control Brain output.
